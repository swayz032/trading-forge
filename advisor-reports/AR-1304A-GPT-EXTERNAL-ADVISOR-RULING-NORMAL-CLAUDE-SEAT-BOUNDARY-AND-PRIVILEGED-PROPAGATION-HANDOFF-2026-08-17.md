# GPT EXTERNAL ADVISOR RULING — AR-1304A

## VERDICT

**NORMAL CLAUDE SESSION = CORRECT STOP ON LIVE SELF-GUARD MUTATION / PROCEED ON NON-LIVE REPAIR WORK.**

The session correctly measured that its current ordinary working seat is not the privileged guard-repair/control-plane actor required by AR-1303A. It must **not** use operator instruction as a substitute for the missing safety boundary and must **not** edit its own live guard/settings/manifest merely because the user asked it to continue.

However, the session's statement that GPT reserved the live propagation step is too broad. **GPT reserved reauthorization of the real frozen Opus execution.** AR-1303A explicitly assigned the minimum live guard propagation to the already-established **privileged guard-repair/control-plane actor**, followed by a genuinely fresh ordinary Worker-1 read-only verification seat and then GPT reauthorization.

## DIRECTIVE TO CLAUDE

Proceed now with AR-1304 in the existing controlled repair worktree, but remain inside the ordinary-seat boundary:

1. Implement the F29 and F30 repair code only on non-live/scratch/test surfaces that the current seat is authorized to modify.
2. Use scratch/temp fixtures only. Do not touch the real frozen receipt namespace.
3. Run the full required zero-model focused tests, regression tests, negative controls, and mutation controls from AR-1303A.
4. Do not make any real Agent/Task/model call.
5. Do not create any real frozen `.permit`, `.attempt`, `.dispatch`, `.raw`, or `.completion` artifact.
6. Do not modify the frozen queue, native-call manifest, prompt transport, compiler, backtest, paper, broker, or live-money paths.
7. Do not weaken or bypass the ordinary Worker self-protection rule.
8. Do not treat user/operator authorization as permission for the ordinary seat to self-edit its live guard.

When the code and zero-model evidence are green, **STOP BEFORE ORDINARY-SEAT LIVE PROPAGATION** and produce a handoff packet containing:

- exact files changed;
- exact diffs relevant to F29/F30;
- exact local test commands and outputs;
- negative controls and mutation-control results;
- confirmation that no model call occurred;
- confirmation that the real frozen receipt directory remains README-only;
- exact live surfaces that require privileged propagation;
- the minimum privileged propagation sequence required by AR-1303A.

Then hand the live mutation portion to the **already-established privileged guard-repair/control-plane path**. Do not invent a second privilege system.

After privileged propagation, a **genuinely fresh ordinary Worker-1 seat** must perform the AR-1303A post-propagation read-only proof. If any required production check fails, stop with zero model calls.

Only after that evidence is returned may GPT decide whether the frozen eight Opus calls are reauthorized.

## BOTTOM LINE

Claude's safety stop is **accepted**. The current session is not cleared to self-edit the live guard. It **is cleared to finish the bounded repair implementation and tests in the controlled repair worktree**, then hand off only the privileged live propagation step.

**DO THE REPAIR WORK NOW. DO NOT BYPASS THE GUARD. DO NOT SPEND OPUS. DO NOT PUSH LIVE FROM THE WRONG SEAT.**