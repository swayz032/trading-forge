# GPT EXTERNAL ADVISOR RULING — AR-1340A

**Date:** 2026-08-18  
**Repository:** `swayz032/trading-forge`  
**Worker:** `worker-1` / `compiler-factory`  
**Reviewed report:** `AR-1340-WORKER1-AR1338A-CERTIFICATION-BAR-BLOCKED-DECISION-2026-08-19.md`

## DISPOSITION

**PASS — THE WORKER'S STOP WAS CORRECT. OPTION A IS AUTHORIZED, WITH A PRECISE STEADY-STATE FACTORY BAR: RUN THE CURRENT HARDENED CERTIFICATION MACHINERY ONCE PER SOURCE UNIT, FAIL CLOSED ON ANY UNRESOLVED/FAILED RESULT, AND DO NOT REPLAY THE HISTORICAL sVkm MULTI-ROUND REPAIR CAMPAIGN PER VIDEO.**

The measured blocker is real. The first deterministic library video produced a modern extraction and 13/13 spine conditions reached the current Tier-3 fall-through path after anchor location and Tier-1. That means the current certifier genuinely requires semantic adjudication for this source unit; Worker 1 was correct not to silently reinterpret `certified` or invent a lower bar.

The sVkm history does **not** establish that every future video must repeat every historical G2D correction round. Those rounds were the development/correction history by which the certification machinery itself was hardened. The steady-state factory contract is to apply the **current corrected machinery** to new frozen inputs. If that machinery returns RED/indeterminate, the source unit refuses; the worker does not start iterative correction rounds merely to manufacture a PASS.

---

## 1. FACTORY-TIER CERTIFICATION BAR

For each extracted strategy in each source video:

1. freeze the transcript/source pin and modern extraction artifact;
2. run the current `pilot_conveyor.prepare_strategy` / current source-grounding path;
3. adjudicate the emitted Tier-3 packet exactly once through the current blind Stage-1 contract;
4. adjudicate the current revealed Stage-2 support contract exactly once for the required items;
5. run the current conflation/enumeration/compile-integrity inputs exactly once where the current certificate contract requires them;
6. finalize through the existing `finalize_certificate` / certificate machinery;
7. compile only when the current certificate is genuinely clean under its own present-day grading contract;
8. otherwise emit the exact frozen refusal disposition and continue to the next source unit.

No manual vote changing. No threshold weakening. No repeated semantic re-grades whose purpose is to turn a failed source unit into a pass.

A retry is allowed only for a measured infrastructure/transport failure that produced no valid adjudication result, using the same frozen inputs and leaving a retry receipt. A semantic FAIL is a result, not a retry trigger.

---

## 2. WHY THIS IS NOT A LOWERED SOURCE-FIDELITY BAR

The current `pilot_conveyor.py` already contains the fixes discovered during the historical campaign: literal anchor verification, dual-read agreement, content-overlap protection, two-stage Tier-3 support checking, residual sampling, blinding leak scans, non-drop accounting, compile-integrity lints, conflation fencing, and enumeration consistency wiring.

Therefore the historical sVkm multi-round sequence is **not** the production algorithm. It is the provenance showing how the current algorithm became trustworthy.

The factory may call an output `FAITHFUL_COMPILE_READY_FOR_BACKTEST` only when the present machinery reaches the present clean certificate/compile conditions. A source unit that remains ambiguous or fails the current machinery receives refusal and does not compile.

This is screening-grade eligibility for a source-faithful backtest, not a claim that the strategy is proven profitable or ready for PAPER/live capital.

---

## 3. DISPATCH ECONOMY — DO NOT TURN ONE PACKET INTO AN ENSEMBLE

The existing conveyor builds one Tier-3 packet per extracted strategy containing the strategy's fall-through items. Preserve that packet-level design.

Where the current dispatcher/schema supports answering all items in a packet in one call, use **one Stage-1 packet dispatch** and **one separate Stage-2 support dispatch** for that strategy. Stage 1 and Stage 2 must remain separate because Stage 1 is intentionally blind.

Do not launch one new agent/session merely because a packet contains 13 items if the current rater interface can consume the packet as a batch.

If the existing dispatcher is genuinely item-granular and cannot consume the packet as one adjudication without redesigning the contract, use the existing item-granular contract, honor the current adjudication ceiling, and do not build a new dispatcher during the pilot.

No 8-Opus ensemble. No multi-rater majority vote. No historical G2D replay per video.

Record the actual model-call count in the pilot receipt.

---

## 4. FRESH `transcript_quote` FIELDS — SAFE OPTIMIZATION ONLY

The fresh `75DJN5UVQnw` extraction contains `transcript_quote` on the important entry/stop/target/confluence fields. Those bytes are useful source evidence, but extractor output is not self-authenticating.

They may be used as a zero-model-call anchor candidate **only if** the existing literal transcript verification proves the quote is an exact source span. If a small existing seam already supports that reuse, use it. Otherwise do not pause the 3-video pilot to redesign `pilot_conveyor`; the current anchor-locator path remains valid.

Any direct-quote fast path must mechanically verify the source substring and must fall back/refuse rather than trust an extractor quote that does not resolve literally.

---

## 5. MISSING STRATEGY `name` IS NOT A CERTIFICATION BLOCKER

The worker correctly measured that the fresh production extraction does not emit a strategy `name`; sVkm's name was later-added campaign metadata.

Do not add fabricated names to the extractor merely to match legacy rows.

Source-unit identity is the pinned `video_id` plus extracted-strategy index/content. Projection to the three live-library rows is separate from extraction naming:

- if the source video yields exactly one modern strategy and the three manifest rows are already-proven market materializations of that source strategy, project under the existing materialization authority;
- if market/timeframe materialization is not authorized, emit `MARKET_OR_TIMEFRAME_UNRESOLVED`;
- if a video yields multiple modern strategies and the legacy row-to-modern-strategy mapping cannot be proven without semantic invention, emit the appropriate measured refusal rather than guessing by name.

Legacy names/specs may be locators/hints only, never source authority.

---

## 6. PILOT CONTINUATION

Resume the same deterministic three-video pilot:

1. finish modern extraction for `FqxEKDxemtI` and `E8Wg6tFPYjo`;
2. apply this single-pass current-certifier bar to all three pilot videos;
3. compile any genuinely clean certificates through the existing production compiler;
4. project exactly one frozen disposition to every linked manifest row;
5. rerun from the frozen artifacts to prove deterministic factory output;
6. if pilot integrity is GREEN, automatically continue through the remaining 37 source videos under AR-1338A without another routing pause.

Low certification/compile yield does not fail the conveyor. False certification, hidden condition loss, source invention, nondeterminism, or identity loss does.

---

## 7. REQUIRED PILOT RECEIPT ADDITIONS

In addition to AR-1338A's existing receipt requirements, include:

- per video/strategy Tier-1 classified vs Tier-3 fall-through counts;
- Stage-1 dispatch count;
- Stage-2 dispatch count;
- other semantic-lint/model call count required by the current certifier;
- certificate grade/status and exact failed axis when not clean;
- whether any direct `transcript_quote` fast path was used and its literal-verification result;
- proof that no semantic FAIL was retried merely to obtain a different answer.

## FINAL RULING

**Worker 1 may proceed immediately under Option A as clarified above. The current hardened certifier is the steady-state factory bar. Apply it once to frozen source units; compile genuine passes, refuse genuine failures, and do not repeat the historical sVkm repair campaign 40 times.**