# GPT EXTERNAL ADVISOR RULING — AR-1234 · 2026-08-16

## AR-1233 PASSES AS A MODEL-ROLE BENCHMARK WITH PROTOCOL/REPORT CORRECTIONS. CLAUDE OPUS 5 DECISIVELY WINS THE sVkm LOAD-BEARING LOCATOR CONTEST AGAINST THE CURRENT PRODUCTION-CONFIG GEMMA LOCATOR. GEMMA IS RETIRED FROM LOAD-BEARING SEMANTIC EVIDENCE-LOCATION AUTHORITY IN ITS CURRENT CONFIGURATION, NOT DELETED FROM TRADING FORGE: IT REMAINS VALID FOR LOCAL ATOMIZATION / UTILITY / CHEAP PRE-SCREEN WORK. OPUS IS PROMOTED TO THE PREFERRED SUCCESSOR LOCATOR CANDIDATE, NOT TO CERTIFIER. THE NEXT MONEY-PATH STEP IS A BATCHED ONE-OPUS-READER-PER-VIDEO CANDIDATE + VERSIONED PHASE-1 INTEGRATION; DO NOT PRODUCTIONIZE THE 12-SUBAGENTS-PER-VIDEO BENCHMARK TOPOLOGY.

```text
RULING ON : AR-1233 — Gemma vs Opus locator benchmark raw results
WORKER SHA: a4901583c28eccf02b5d8b8d33a0ea62519de0bd
GRADE     : PASS benchmark / PASS model-role decision / corrections required on blinding + one report count
GEMMA     : LOSES current load-bearing locator authority on sVkm; KEEP utility/atomizer roles
OPUS 5    : WINS preferred successor LOCATOR candidacy; NOT a certifier and NOT yet system-wide production authority
GPT       : remains independent external semantic/certification challenge layer
CI        : NONE at worker SHA; worker test statements are LOCAL evidence only
CERT      : RED
COMPILER  : LOCKED for sVkm
PAPER     : LOCKED
LIVE      : LOCKED
VISUAL    : STOP-A exact object remains VISUALLY_UNRESOLVED; STOP-B unresolved
```

---

# 1. INDEPENDENT GITHUB VERIFICATION

I inspected the worker report, worker commit, changed-file set, benchmark packet, Gemma raw answers, Opus raw answers/delegation receipts, verifier harness, blinded-results artifact, commit status and workflow-run state.

From prior worker head `c24a030...` to `a4901583...`, the worker changed only benchmark/evidence/support surfaces plus regenerated inventory. No compiler, backtester, PAPER, broker or live-trading production semantics changed.

GitHub exposes:

```text
combined status checks: none
workflow runs at worker SHA: none
```

Therefore all reported local tests remain LOCAL evidence, not CI.

The frozen benchmark identity is durable:

```text
packet SHA256     fe6e73e1326ff50424bd905ed684c341479909e8e1482198c059b2e32d29bcd7
system-prompt SHA 966d5942ccb309c577c0bc2ae8b1055eaf079c61a740fb7a32aae5dfd9485556
transcript SHA    df72444f70e8c79db0e1692867913f14d37c18fd063f681a2b562fe103ce99cc
extraction SHA    c37ff26f753449c35b6ec0402a3152dc287a8ae427eb0d86661b3fb43ec01823
conditions        12
trials/model      3
answers/model     36
```

The harness reuses the production locator prompt by import and creates one frozen condition+full-transcript task per isolated Opus subagent. The Opus delegation receipts show fresh `model=opus` subscription subagents; no Anthropic API key/SDK/spend path was introduced.

---

# 2. MECHANICAL RESULT — OPUS ALREADY WINS BY A LARGE MARGIN

The common mechanical literal fence reports:

```text
CURRENT GEMMA CONFIG
36 answers
24 literal transcript spans
12 proposed strings fail the literal-span verifier
0 abstentions
1 / 12 conditions identical across all three trials

OPUS SUBAGENTS
36 answers
36 literal transcript spans
0 literal-span failures
0 abstentions
10 / 12 conditions identical across all three trials
```

That is not sufficient by itself to choose a semantic reader, but it establishes a large repeatability and literal-validity separation.

The two Opus conditions that vary across trials do not collapse into the generic disclaimer. Their alternate quotes remain source-near, rule-relevant passages.

---

# 3. GPT SEMANTIC SCORING — THE DIFFERENCE IS DECISIVE

The benchmark worker correctly did not score its contestants. I independently compared the raw candidate quote against the requested condition and the pinned source semantics.

## 3.1 Current Gemma locator configuration

The current sampled Gemma locator repeatedly returns a small set of generic passages such as:

- the strategy/model is not perfect and trades will lose;
- the model is successful over many trades;
- keep it simple;
- an unrelated FVG-definition passage;
- an unrelated stop/FVG passage;
- a generic profit sentence.

Those answers are reused for conditions asking about:

- the 9:30 five-minute range;
- one-minute breakout;
- breakout direction;
- FVG outside the range;
- third-candle entry;
- New York 9:30 timing;
- stop/wick placement;
- fixed 2R target.

On independent inspection I find **no Gemma answer in the 36-run artifact that directly grounds the requested condition at the required condition scope**. Some answers are real transcript text, but literal existence is not topical relevance.

This is exactly the failure AR-1224 identified: a literal verifier can prove that a quote exists while saying nothing about whether the quote proves the rule.

### Gemma disposition

For the **current production locator configuration**:

```text
LOAD-BEARING SEMANTIC LOCATOR AUTHORITY = FAIL
```

Do not spend the money path on another blind sampled Gemma rerun.

This does **not** prove that every possible deterministic/retrained/changed Gemma configuration can never improve. It proves the configuration Trading Forge is actually using is unsuitable for this load-bearing role on the golden slice. A deterministic-Gemma control may be run later if there is a business reason to retain Gemma as a locator, but it is NOT a blocker to moving the money path forward.

Gemma remains useful for its declared cheap/local utility roles.

## 3.2 Opus locator candidate

Opus repeatedly finds the actual rule-bearing passages:

- 9:30 first five-minute candle, mark high and low, form range;
- candles close outside the five-minute range;
- range exit indicates likely direction;
- fair-value-gap sequence prints outside the range;
- entry on closure of the third candle;
- strategy at 9:30 Eastern/New York time;
- stop at bottom of fair-value candle, include wick, give room;
- fixed target at 2R.

Across the 36 outputs, the Opus proposals remain in the correct rule/topic family rather than drifting to the generic disclaimer.

### Opus disposition

```text
PREFERRED SUCCESSOR LOCATOR CANDIDATE = PASS
CERTIFICATION AUTHORITY               = NOT GRANTED
SYSTEM-WIDE PRODUCTION AUTHORITY       = NOT YET GRANTED
```

Opus has earned the right to replace Gemma in the **next-version locator candidate route**. It has not earned the right to certify its own answers.

---

# 4. VERY IMPORTANT: OPUS LOCATOR SUCCESS EXPOSES EXTRACTION DEFECTS — IT DOES NOT ERASE THEM

A better locator makes the source-truth problem clearer.

The Opus quotes confirm several prior GPT findings:

| Condition | Locator result | Fidelity disposition |
|---|---|---|
| 9:30 first-5m high/low range | correct source passage located | core action supported |
| breakout/close outside range | correct source passage located | core breakout concept supported; one-minute qualifier may require wider/composed evidence |
| `breakout confirms direction` | relevant direction passage located | **FAIL exact strength**: source says it gives an idea / may be looking; `confirms` is inflation |
| FVG outside range | correct passage located | core setup supported |
| `high-probability` FVG rationale | relevant FVG/entry passage located | **FAIL modifier**: `high-probability` remains unsupported |
| third-candle entry | correct action passage located | core entry timing supported |
| `confirms FVG structure and minimizes entry risk` | action passage located | **FAIL rationale**: located action does not prove those stronger causal/risk claims |
| `during the 9:30 session` | exact 9:30 passage located | **FAIL normalization** if represented as a broad session/window; source is point-time `at 9:30` |
| stop bottom of fair-value candle + wick | exact topical stop passage located | semantic family supported, but exact executable geometry remains **VISUALLY_UNRESOLVED** |
| fixed 2R target | exact passage located | supported |

Therefore the correct architecture is:

```text
Opus finds source evidence
    ↓
mechanical literal verifier proves it is real transcript text
    ↓
collision/relevance/fidelity stages challenge whether it supports the condition exactly
    ↓
unsupported modifiers/causality/timing widening stay RED or UNRESOLVED
    ↓
independent certification
```

NOT:

```text
Opus found a quote -> condition certified
```

The frozen historical RED certificate stays frozen.

---

# 5. PROTOCOL / REPORT CORRECTIONS

AR-1233 is materially better than the earlier worker overclaim streak, but it is not a perfect clean report.

## 5.1 `blinded_results.json` is not actually sealed-blind

The same artifact that labels candidates `candidate_A` / `candidate_B` also contains `candidate_identity_map` at the bottom.

Once the scorer reads the whole file, candidate identity is available. Therefore this GPT semantic score is an **independent external score, but not a cryptographically/sealed blind score**.

The raw evidence gap is large enough that this does not invalidate the model-role result.

If this benchmark machinery is reused, publish:

```text
blinded_results.json        # NO identity map
candidate_identity_map.json # separate sealed/unopened file until score is committed
```

Do not build a new framework for this; it is a small artifact-layout fix.

## 5.2 stale comparability caveat

The final blinded artifact still warns that trial counts differ, although the final benchmark is 3 trials vs 3 trials. Correct/remove the stale caveat if the artifact is regenerated.

## 5.3 held-count inconsistency in worker report

The report claim ledger gives one Candidate-A held sequence that does not match the later table/artifact. The durable artifact is controlling. Do not promote the inconsistent prose count.

This prevents AR-1233 from starting a three-report perfect reliability streak, but the difference is small and does not affect the locator winner.

---

# 6. DO NOT PRODUCTIONIZE THE BENCHMARK TOPOLOGY

AR-1232 intentionally used one fresh Opus subagent per condition for fairness/isolation.

That was correct for a contest.

It is NOT automatically the correct runtime architecture.

The raw receipts report roughly 53k subagent tokens per isolated call. Twelve isolated Opus calls per video would waste subscription context/time at corpus scale.

The repository already describes Opus as a **complete-transcript successor reader**. Use that architecture intelligently.

## NEXT LANE O1 — BATCHED OPUS LOCATOR CANDIDATE

Build a bounded sidecar candidate that uses:

```text
ONE fresh Opus reader per video/transcript
        ↓
full transcript once
        ↓
all extracted spine conditions for that video
        ↓
structured map: condition_ref -> literal quote | null
```

This is a NEW topology and therefore must earn parity; do not assume the single-condition benchmark transfers automatically.

### O1 acceptance controls

On sVkm, run at least three fresh batch-reader trials and prove:

1. same frozen transcript/extraction/condition pins;
2. no Gemma answers, old bad spans or manual answer key shown;
3. raw batch output preserved before repair;
4. every non-null quote passes the existing literal verifier;
5. no generic-disclaimer misgrounding for the known rule conditions;
6. rule-by-rule semantic quality is no worse than the accepted isolated-Opus benchmark;
7. repeated batch runs have measured stability, not a one-run green;
8. collision diagnostic runs on the complete returned set;
9. HIGH collision means HOLD/review, never semantic conviction;
10. no sVkm-specific answer logic/hardcoded spans;
11. record model alias/available model identity, task hashes, token/time receipts;
12. no API key/SDK/new Anthropic API spend.

If batch parity fails, fall back to condition-isolated Opus only for the ambiguous conditions rather than defaulting every condition to its own agent.

That gives Trading Forge a cost-aware hybrid:

```text
one Opus video reader first
-> mechanical/fidelity gates
-> isolated Opus re-query only for unresolved/ambiguous evidence
```

---

# 7. AFTER O1: VERSIONED PHASE-1 / LANE-G INTEGRATION

Once O1 is green, the shortest robust path is:

1. create a new versioned Opus-locator Phase-1 route; do not mutate old red artifacts;
2. keep exact transcript/extraction/model/task provenance;
3. literal verifier remains mechanical and strict;
4. set-level collision gate remains before auto-accept;
5. relevance remains separate from fidelity;
6. only relevance-approved evidence may feed fidelity;
7. fidelity must catch the already-proven `confirms`, `high-probability`, timing-window and causal/risk inflation defects;
8. antecedent/anaphora composition is allowed only with proven identity/no intervening redefinition;
9. unresolved conditions fail closed;
10. rerun sVkm under a new architecture/version and issue a new certificate attempt.

Do NOT hardcode a green sVkm answer set from this benchmark.

---

# 8. CLAUDE PROTECTION TOOLBOX — STILL AUTHORIZED, STILL OUTSTANDING

AR-1230's activation amendment remains controlling.

Activate the already-built Worker-1 non-semantic protection layer in parallel with O1:

- session anchor / exact branch-SHA guard;
- preflight;
- lane/edit-scope guard;
- commit/evidence receipt;
- finish check;
- fake-green/test-theater screen;
- native Claude hooks;
- GPT external review.

Do NOT rebuild this toolbox.
Do NOT serialize the Opus locator money path behind this support lane.

The successful mechanical completion state remains:

```text
PASS_FOR_GPT_REVIEW
```

not semantic approval.

---

# 9. VISUAL INTELLIGENCE STATUS — UNCHANGED

The model shootout solves the **text evidence locator authority** question. It does not solve exact chart geometry.

Keep:

```text
STOP-A semantic family : candle-extreme / wick family strongly favored
STOP-A exact object     : VISUALLY_UNRESOLVED
FVG boundary            : REJECTED for STOP-A
invented +4 tick buffer : FORBIDDEN
STOP-B exact object     : VISUALLY_UNRESOLVED
symmetry                : NOT ESTABLISHED
```

Visual Intelligence remains a narrow source-truth lane when transcript text cannot establish executable geometry.

---

# 10. LOCKS

Until a next-version source grade is genuinely green and exact executable source rules are proven without invention:

- no sVkm certification;
- no sVkm compiler authorization;
- no sVkm backtest campaign;
- no PAPER activation;
- no broker/Topstep/live activation;
- no generic FVG stop mapping from unresolved visual evidence;
- no automatic `Opus said it` certification shortcut.

---

# FINAL DISPOSITION

AR-1233 answered the architectural question that AR-1231/1232 were designed to answer.

**Current production-config Gemma is the wrong model authority for load-bearing source-evidence location on this golden slice. Claude Opus 5 is decisively superior and is authorized as the preferred successor locator candidate.**

The engineering lesson is equally important:

```text
Gemma utility/atomization       -> KEEP
Opus source-evidence location   -> PROMOTE TO NEXT-VERSION CANDIDATE
mechanical source verification  -> KEEP
fidelity / exactness challenge  -> KEEP SEPARATE
GPT external certification      -> KEEP INDEPENDENT
Visual Intelligence             -> USE ONLY WHEN TEXT CANNOT SETTLE EXECUTABLE GEOMETRY
```

Next primary work order: **O1 batched Opus locator candidate -> parity controls -> versioned Phase-1/Lane-G integration.**

Parallel bounded work order: activate the already-built AR-1194–AR-1198 Worker-1 protection toolbox under AR-1230's amended activation rule.
