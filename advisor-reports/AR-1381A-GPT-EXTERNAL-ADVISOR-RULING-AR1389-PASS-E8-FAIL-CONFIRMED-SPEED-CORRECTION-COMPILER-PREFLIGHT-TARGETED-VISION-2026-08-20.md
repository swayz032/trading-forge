# GPT EXTERNAL ADVISOR RULING — AR-1381A

**Date:** 2026-08-20  
**Repository:** `swayz032/trading-forge`  
**Architecture stage:** 3 — Strategy Factory  
**Worker branch inspected:** `claude/worker1-h1-20260815 @ f8b00b268c3217dd236644b21e96fbc7527f6e59`  
**Prior controlling ruling:** AR-1380A @ `e60128743afdfe3e717eef74241ad98ccdd3362a`  
**Report graded:** AR-1389  

## DISPOSITION

**AR-1389 = PASS.**  
**THE E8 ROUND-3 V2 FAIL IS INDEPENDENTLY CONFIRMED.**  
**BOTH GPT HIGH FINDINGS ARE REAL; ALL 9 PARTIAL CLAIMS ARE REAL; NO LOAD-BEARING GPT FINDING WAS DISPROVED.**  
**E8 CANDIDATE `b50729b9...` REMAINS REJECTED UNDER THAT SHA.**  
**DO NOT BUILD MORE SEMANTIC-AUDIT MACHINERY AROUND THIS CANDIDATE. THE V2 AUDIT PATH IS SUFFICIENT FOR THIS CALIBRATION.**  
**DO NOT START A BLIND ROUND-4 OPUS RECONSTRUCTION YET.**  
**FASTEST ROBUST NEXT MOVE: READ-ONLY COMPILER-READINESS PREFLIGHT -> TARGETED VISUAL INTELLIGENCE ONLY FOR HARD BLOCKERS -> ONE FRESH CANDIDATE IDENTITY -> SEMANTIC AUDIT/CLAUDE CHALLENGE -> COMPILE OR HONEST REFUSAL.**

GitHub reports no status checks and no workflow runs at current Worker HEAD.

**CI: NONE; tests and model-audit evidence are local-only plus independent repository inspection.**

---

## 1. AR-1389 — PASS; THE ROUND-3 V2 FAIL SURVIVES INDEPENDENT ATTACK

The exact GPT-5.6 Sol response was fetched byte-for-byte from GPT rulings, ingested through the accepted V2 harness, and produced the expected `GPT56_SEMANTIC_AUDIT_FAIL` receipt.

The receipt binds:

- candidate SHA `b50729b928e51980088f2e4a73c30771eb3665147443753edcc8be44d5fb0041`;
- transcript SHA `62036e6e62ae927c165a7d501e20ae0fcd15684933cd4419c5832ba74756ec67`;
- task SHA `1b524bd6500238057e2bfa6d835b9dcaf37e8c00d93dd4b9039932cb41786380`;
- semantic-contract ID `AR-1378A-SS6-ROLE-ASSIGNMENT-CONTRACT-V1`;
- semantic-contract SHA `79b0d960fe9f40d3c93a3f573a32a994a4afbae696dd831b41a11d1aaae4a9de`;
- delivered prompt SHA `7bb2d0631f8b3db50d027e4049ed4755d9dd8c20a8a458ae2b975d2d137b54ba`;
- exact GPT response SHA.

No identity seam is open in this round's receipt.

Claude independently re-read the transcript, candidate, and authoring law rather than grading GPT prose. Its challenge confirms:

- HIGH A `entry_sequence` cross-splice = CONFIRMED;
- HIGH B `targets` priority ranking = CONFIRMED;
- all 9 PARTIAL rows = CONFIRMED;
- `trigger_vs_source_gaps=FAIL` = CONFIRMED;
- `target_definition_conflicts=FAIL` = CONFIRMED;
- `directional_symmetry=UNRESOLVED` = CONFIRMED;
- 10 sampled ENTAILED rows = genuinely ENTAILED.

There is no GPT-vs-Claude adjudication dispute left in this case.

---

## 2. HIGH A — ORDERED ENTRY SEQUENCE CROSS-SPLICE IS A REAL EXECUTABLE DEFECT

The permanent extraction schema independently confirms that `entry_sequence` is an **ordered list of entry steps exactly as the speaker describes them**. It is not an unordered evidence bag.

The E8 candidate currently places:

1. the explicit NZDUSD **buy-side** Fibonacci drawing procedure `low -> high`; then
2. the GBP AUD **sell-side** short entry at 71%.

The sell-side worked example never narrates its Fibonacci anchor direction. The candidate itself discloses that gap.

Therefore following the current ordered sequence literally can apply the buy-side anchor convention to the sell-side 71% entry. Because the 71% price depends on Fibonacci anchor direction, this is a genuine wrong-geometry execution risk.

**Ruling: confirmed load-bearing source-fidelity failure.**

The next candidate must not represent opposite-direction worked-example geometry as one linear executable trigger chain.

---

## 3. HIGH B — TARGET PRIORITY IS A REAL UNSOURCED RANKING

The permanent extraction schema independently defines `targets` as **profit targets in priority order as named by the speaker**.

The current E8 candidate assigns:

- sell-side worked-example target = `priority: 1`;
- buy-side worked-example target = `priority: 2`.

Those are not TP1/TP2 for one trade. They are mutually exclusive direction-specific worked-example targets.

The round-3 authoring law also explicitly forbids inventing `priority` between alternatives the source does not rank.

**Ruling: confirmed load-bearing source-fidelity failure.**

The next representation must keep direction-specific targets inside their own directional alternative/branch or otherwise represent them without a fabricated cross-direction ranking.

---

## 4. SPEED CORRECTION — STOP ADDING AUDIT MACHINERY

The project has correctly repaired several real trust defects:

- candidate evidence binding;
- semantic role contract;
- task-contract cryptographic binding;
- delivered-prompt binding;
- V1 preservation;
- V2 response/receipt identity.

Those repairs were justified. However the money path is now sufficiently protected for this calibration.

**Do not open another semantic-harness architecture packet unless an actual new false PASS/false FAIL mechanism is demonstrated by evidence.**

The current bottleneck is no longer audit identity. It is **source completeness + compiler readiness**.

The engineering objective is now to determine, as quickly as possible, whether E8 can become a source-faithful compilable strategy or must honestly refuse and yield the lane to another source.

---

## 5. LANE A — READ-ONLY E8 COMPILER-READINESS PREFLIGHT NOW

Authorize one bounded read-only preflight against the actual downstream certifier/compiler contracts and current E8 source gaps.

**This is NOT authorization to compile, certify, backtest, edit compiler code, or substitute defaults.**

Worker must produce one durable matrix for every current E8 `source_gap` and every field needed downstream:

| item | exact source gap / field | downstream consumer | hard compile blocker? | source text already sufficient? | visual evidence could resolve? | safe unresolved representation exists? | expected disposition |
|---|---|---|---|---|---|---|---|

At minimum inspect:

- sell-side Fibonacci anchor points;
- buy-side stop wick / exact stop geometry;
- higher-timeframe trading-range construction;
- BOS qualification;
- imbalance/FVG qualification;
- liquidity-sweep recency/proximity;
- BOS/FVG structure timeframe;
- 75% level role;
- invalidation / level-violation action;
- pending-order expiry;
- any other source gap that the actual compiler/certifier requires to emit deterministic code.

For each item, classify one of:

- `COMPILE_BLOCKER_SOURCE_MISSING`;
- `COMPILE_BLOCKER_REPRESENTATION_DEFECT`;
- `SAFE_TO_REMAIN_UNRESOLVED`;
- `NON_EXECUTABLE / NOT REQUIRED`;
- `UNKNOWN_DOWNSTREAM_CONTRACT`.

No guessed defaults. No semantic substitution. If the compiler/certifier does not yet have a contract for the fresh source-faithful representation, report that exact architectural absence rather than building it in this lane.

**Purpose:** do not spend Visual Intelligence or another Opus round resolving facts the compiler does not actually require.

---

## 6. LANE B — TARGETED VISUAL INTELLIGENCE ONLY AFTER/PARALLEL WITH PREFLIGHT

Visual Intelligence is authorized only as a source-evidence lane, not as a guessing engine.

Start with the two highest-value E8 questions already proven missing in text:

### VI-E8-1 — SELL-SIDE FIBONACCI ANCHORS

Question: in the GBP AUD sell-side worked example, what exact chart points are used to anchor the Fibonacci tool, and in what direction is it drawn?

Evidence required for a positive answer:

- exact source-video time/frame range;
- visible chart identity / worked-example identity;
- visible tool anchors or demonstrable drag path;
- enough visual evidence to distinguish high->low from low->high;
- no inference merely from what would be conventional trading practice.

Allowed result: `NOT_SHOWN / UNRESOLVED`.

### VI-E8-2 — BUY-SIDE STOP WICK

Question: in the NZDUSD buy-side worked example, which exact candle/wick receives the stop, and can that wick be deterministically tied to a source-supported structural/Fibonacci level?

Evidence required:

- exact source-video time/frame range;
- visible candle/wick selected by the cursor/magnet action;
- visible relation to the entry/Fibonacci range if determinable;
- no invented label if the chart does not make the relationship explicit.

Allowed result: `NOT_SHOWN / UNRESOLVED`.

Only add more E8 visual questions when Lane A proves they are actual compile blockers and the video plausibly contains the answer.

**Do not perform a broad "watch the whole video and reinterpret everything" visual pass.** Targeted questions are faster, easier to certify, and reduce visual hallucination surface.

---

## 7. LANE C — ONE FRESH E8 CANDIDATE ONLY AFTER THE EVIDENCE MAP

Do not create Round 4 before Lane A identifies the actual compiler blockers and Lane B returns available visual evidence for those blockers.

Then authorize **one** fresh candidate identity, not an edit of `b50729b9...`.

The next authoring law must require:

1. one shared **direction-neutral spine** for source-common setup logic;
2. explicitly labeled buy-side and sell-side directional alternatives for Fibonacci draw/entry/stop/target geometry;
3. no buy-side geometry may be inserted into a sell-side executable chain or vice versa;
4. no cross-direction target ranking;
5. all 9 confirmed atomic-binding defects narrowed/split/rebound;
6. Visual Intelligence facts may be used only when attached to exact admissible frame/time evidence;
7. unresolved visual/text facts stay `source_gaps`;
8. no default may be invented to obtain compiler acceptance.

After that one fresh candidate:

`literal validation -> V2 GPT semantic audit -> independent Claude challenge`.

If clean, proceed to deterministic certifier/compiler eligibility.

If semantic-clean but compiler preflight still shows an unresolved hard source requirement, emit an **honest compile refusal** for E8 and move to the next source rather than cycling indefinitely trying to manufacture a survivor.

This is now a campaign rule: **no endless reconstruction until green.**

---

## 8. NON-BLOCKING GOVERNANCE SPEED FIX — LATEST-AUTHORITY SCAN + CURRENT_STATE POINTER

AR-1388 disclosed that the newest GPT ruling was initially missed because the Worker filename-sorted report names rather than checking branch heads by commit time.

That is a real speed/reliability defect in onboarding, but it must not block the E8 money path.

Authorize a tiny parallel governance patch:

### A. Onboarding authority scan

Update the GPT/Worker onboarding rule so every cold start must:

1. inspect the actual head/most-recent commit of each relevant `external-advisor/*` branch;
2. resolve the newest controlling GPT ruling by repository evidence/commit time;
3. never infer "latest ruling" from lexicographic or numeric filename sorting alone;
4. then inspect reports after that ruling.

### B. Tiny `CURRENT_STATE` navigation pointer

Create one small navigation-only state file, e.g. `docs/governance/CURRENT_STATE.md`, containing only:

- architecture stage;
- latest controlling GPT ruling path + commit;
- Worker branch + last verified head;
- current locks;
- exact next money-path action.

It must say **NAVIGATION POINTER — NOT AUTHORITY**. If it conflicts with branch heads/rulings, repository authority wins.

Keep this file tiny. Do not turn it into another report archive.

The purpose is to reduce repeated onboarding archaeology, not create a new governance subsystem.

---

## 9. AGENT-LOGS / LOW-TIER HARNESS LAXITIES

Do not widen a guard or reopen audit infrastructure merely to satisfy the previously disclosed `AGENT-LOGS.md` logging inconvenience in this money-path packet. Durable reports under `docs/replay-results/` are sufficient evidence for this active lane; any policy cleanup can ride with the non-blocking onboarding patch if it can be done without guard weakening.

Inherited LOW-tier V1/V2 hygiene items such as non-strict `coverage_statement` handling or lone-surrogate hashing do not justify another harness packet now absent a demonstrated money-path exploit.

**Accepted residual / deferred.**

---

## 10. NEXT EXECUTION ORDER

Run in parallel where non-conflicting:

### Lane A — highest priority

Read-only compiler-readiness preflight of E8 source gaps against actual downstream requirements.

### Lane B — evidence only

Targeted Visual Intelligence for VI-E8-1 and VI-E8-2; add another visual question only when Lane A proves it is a hard compiler blocker.

### Lane C — WAIT ON A/B

No Round-4 candidate until A/B produce the exact evidence map.

### Lane D — non-blocking

Tiny onboarding latest-authority scan repair + navigation-only `CURRENT_STATE` pointer.

Then:

`one fresh E8 candidate -> literal clean -> V2 GPT audit -> Claude challenge -> certifier/compiler OR honest refusal -> next source`.

---

## LOCKS

Still forbidden:

- hand-editing/reusing rejected candidate SHA `b50729b9...`;
- blind Round-4 Opus reconstruction before preflight/evidence map;
- invented Fibonacci anchors, wick identity, trading-range boundaries, BOS/FVG thresholds, expiry, or other source rules;
- compiler/certifier code changes in the preflight lane;
- actual certifier/compiler promotion before a clean semantic candidate;
- SOURCE_FAITHFUL backtest;
- broad Factory rerun / 160-video intake;
- PAPER;
- broker/Topstep/live;
- new semantic-audit infrastructure absent a demonstrated new trust defect.

---

## FINAL RULING

**AR-1389 PASSES. E8 ROUND-3 V2 FAIL IS FULLY CONFIRMED BY INDEPENDENT CLAUDE CHALLENGE. THE TWO HIGH FINDINGS ARE REAL EXECUTION/SCHEMA PROBLEMS, NOT AUDIT PEDANTRY: THE ORDERED ENTRY SEQUENCE CROSS-SPLICES BUY-SIDE FIBONACCI GEOMETRY INTO A SELL-SIDE ENTRY, AND THE PRIORITY-ORDERED TARGET ARRAY INVENTS A RANK BETWEEN MUTUALLY EXCLUSIVE DIRECTIONAL EXAMPLES.**

**THE FAST/ROBUST CORRECTION IS NOW ACTIVE: STOP BUILDING MORE AUDIT MACHINERY, DO A READ-ONLY COMPILER-READINESS MAP, USE TARGETED VISUAL INTELLIGENCE ONLY ON PROVEN HARD SOURCE GAPS, THEN ALLOW ONE FRESH E8 CANDIDATE. IF SOURCE-FAITHFUL COMPILATION STILL CANNOT BE PROVEN, REFUSE E8 AND MOVE ON RATHER THAN RUN ENDLESS RECONSTRUCTION ROUNDS.**