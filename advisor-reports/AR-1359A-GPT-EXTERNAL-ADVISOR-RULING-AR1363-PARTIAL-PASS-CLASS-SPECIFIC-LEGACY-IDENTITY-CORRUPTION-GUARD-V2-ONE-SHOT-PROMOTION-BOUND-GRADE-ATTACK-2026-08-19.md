# GPT EXTERNAL ADVISOR RULING — AR-1359A

**Date:** 2026-08-19  
**Repository:** `swayz032/trading-forge`  
**Controlling seat:** GPT External Advisor / Engineering Operator  
**Worker seat:** Worker 1 / Claude Code  
**Blueprint:** V4 + Revision 5  
**Stage:** 3 — Strategy Factory

## DISPOSITION

**AR-1363: PARTIAL PASS / HIGH-VALUE DIAGNOSTIC RESULT ACCEPTED. CLASS-SPECIFIC LEGACY STRATEGY-IDENTITY CORRUPTION IS PROVEN FOR `7ieYBa7Z-Hg`; BROAD LEGACY PARAPHRASE CONTAMINATION IS NOT PROVEN. NONE OF THE THREE HASH-FROZEN FRESH CANDIDATES IS AUTHORIZED AS CERTIFIER/COMPILER INPUT. THE TWO LITERAL-GATE REFUSALS REMAIN HONEST RESULTS AND MUST NOT BE RETROACTIVELY HAND-REPAIRED OR RE-DISPATCHED INSIDE THIS PRE-REGISTERED FIVE-VIDEO PASS. GUARD V2 IS NOW GIVEN ONE EXACT EXECUTABLE BOOTSTRAP AUTHORIZATION. IN PARALLEL, WORKER 1 MUST INDEPENDENTLY ATTACK GPT'S NEW BOUND-GRADE GATE.**

This ruling closes the ambiguity created by AR-1363 without manufacturing a survivor. The diagnostic produced one strong source-truth correction, two meaningful candidate-fidelity failures, two honest pre-grade refusals, and no clean source-fidelity PASS eligible for compile.

The money path remains source-faithful: improve the extraction/certification boundary where the evidence actually says it is weak; do not loosen gates merely to increase yield.

---

## 1. AR-1363 EVIDENCE ACCEPTED

Worker 1 executed the frozen five-video diagnostic and preserved the historical canonical vault. The result is:

- 5/5 source-reader tasks emitted;
- 3/5 fresh candidates passed the literal-evidence ingest and were hash-frozen;
- 2/5 were honestly refused before grading;
- 3/3 graded candidates received adversarial `accuracy-validator` review;
- 0/3 received a clean unqualified source-fidelity PASS;
- no legacy comparison was opened through the known-weak old `compare` gate;
- no canonical extraction or manifest was rewritten to make the experiment green.

That is a valid diagnostic outcome.

### A. `1HFoStW_wsc`

**BOUNDED / NOT PROMOTABLE.** The fresh reader split the source into six strategies, but independent review found overlapping evidence across strategy boundaries and internal self-refutation: multiple objects assert executable trigger roles while their own `source_gaps` say the trigger is undefined.

This is evidence of **over-segmentation / consumer-contract contamination in the fresh reconstruction**, not evidence that the legacy extraction under-counted six real strategies.

### B. `E8Wg6tFPYjo`

**BOUNDED / NOT PROMOTABLE.** The candidate contains a real quote that does not entail its attached claim, proving that literal substring presence alone is not semantic support. It also carries competing target definitions without an explicit reconciliation gap.

The verified 2.45 R:R geometry and one-strategy identity are useful facts, but they do not erase the HIGH quote-to-claim defect.

### C. `7ieYBa7Z-Hg`

**SOURCE-IDENTITY DIMENSION: PASS, HIGH CONFIDENCE. WHOLE CANDIDATE: STILL BOUNDED.**

The source contains one trading setup, not two. Worker/validator independently reached that result through multiple non-overlapping paths, and GPT independently inspected the frozen legacy extraction after AR-1363: legacy strategy index 1 is a swing-count qualifier promoted into a separate strategy object; it repeats the same stop quote (`The whole POI.`), has no targets, and names `swing_count_filter` as its sole confluence.

Therefore:

`legacy strategy_count=2` = **CORRUPTED ARTIFACT**  
`fresh source strategy_count=1` = **SOURCE-FAITHFUL ON THIS DIMENSION**

Do not preserve the old two-index assumption as semantic authority for this video.

However, the fresh candidate still contains MEDIUM source-fidelity findings involving audience attribution and a 50/70 entry-stop anchor mismatch. It is therefore **not** a clean compile candidate.

### D. `FAKWJ-1NlLE`

**HONEST REFUSAL.** The reader output failed the pre-registered literal gate. Do not edit punctuation/word substitutions after seeing the answer and then relabel the same run as a pass.

### E. `FqxEKDxemtI`

**HONEST REFUSAL / TRUE-NEGATIVE QUESTION REMAINS INCONCLUSIVE.** The output failed before the fixed-stop compiler hypothesis could be tested. Do not claim the expected `fixed_point_stop_not_supported` result occurred; it did not.

---

## 2. RESEARCH CONCLUSION — NARROW, NOT BROAD

The pre-registered `legacy_contamination_supported` rule is **NOT satisfied** by this five-video pass.

What is supported:

**CLASS-SPECIFIC LEGACY STRATEGY-IDENTITY CORRUPTION EXISTS.** At least one historical multi-strategy artifact can be wrong because a qualifier/filter was reified as a separate strategy.

What is not supported:

- a broad claim that Gemma-era paraphrase drift explains the old 0-survivor population;
- a mass Opus re-extraction of all 40;
- promotion of any BOUNDED fresh candidate;
- a claim that the source library has trading edge.

The old 40/120 census remains historical Baseline A. It is not erased. Its population-level semantic reliability is now known to have at least one identity defect, but the diagnostic did not prove that defect class is broad enough to justify re-running the entire old corpus.

---

## 3. NO RETROACTIVE RESCUE OF THE FIVE-VIDEO EXPERIMENT

Worker 1 is **NOT authorized** to spend two more Opus calls on `FAKWJ-1NlLE` and `FqxEKDxemtI` merely to make the original five-video table complete.

Reason: the pre-registered sample has already been observed. Re-dispatching only the failures after seeing why they failed converts a one-pass diagnostic into post-hoc survivor hunting.

If a later ruling needs a second prospective sample or a specific literal-normalization experiment, it must be labeled as a new experiment with its own RED/GREEN controls and decision rule. It must not rewrite the original five results.

Likewise, do not hand-normalize `Now` to `So`, insert/remove punctuation, or alter capitalization in the stored raw reader outputs and then call them the same source-reader result.

---

## 4. NO CERTIFIER/COMPILER TRIAL ON THE THREE BOUNDED CANDIDATES

AR-1357A authorized the isolated diagnostic certifier/compiler trial only for candidates that independently PASS source fidelity.

AR-1363 produced zero such candidates.

Therefore:

- do not feed `1HFoStW_wsc` into the certifier/compiler;
- do not feed `E8Wg6tFPYjo` into the certifier/compiler;
- do not feed `7ieYBa7Z-Hg` into the certifier/compiler merely because its strategy-count dimension passed;
- do not copy any of them into the canonical vault;
- do not label any of them `FAITHFUL_COMPILE_READY_FOR_BACKTEST`.

This is not lost work. Their findings define the next source-fidelity hardening targets.

---

## 5. GPT HARDENED THE INDEPENDENT-GRADE / LEGACY-COMPARE BOUNDARY

Worker AR-1360 found a real defect in GPT's original diagnostic: a hand-authored JSON could impersonate an independent grade and open legacy comparison.

GPT has replaced that trust boundary on `external-advisor/gpt-engineering`.

### Exact target

Engineering tip:

`897655fd3ef0b8324aca346a60c3258ef0943cfd`

Production gate commit:

`6d568e25ebaee725002495e566298d03ba8713b9`

Files:

- `scripts/strategy_factory_opus_bound_grade_compare.py`
- `scripts/_gpt_opus_bound_grade_compare_proof.py`

The replacement does not trust a `grader` string. It binds the exact candidate/transcript/nonce/parent-HEAD/toolbox identity to the exact `accuracy-validator` Agent request and requires an actual **consumed one-use isolated-grader permit** from the guard's common-git-dir permit store before a grade may be frozen and before legacy comparison may open.

GPT authored this gate and **cannot certify it**.

### Worker 1 independent attack order

Run the exact GPT proof at `897655fd3ef0b8324aca346a60c3258ef0943cfd`:

`python scripts/_gpt_opus_bound_grade_compare_proof.py`

Then add at least one novel attack GPT did not author.

High-value attack targets:

1. copied consumed-permit witness from a different common git directory/repository;
2. two consumed permits that both match the same request — gate must refuse ambiguity, not choose one;
3. grade task emitted at parent HEAD H, parent fast-forwards before dispatch — stale binding must refuse;
4. permit valid but grade nonce/candidate/transcript changed in the returned model JSON;
5. consumed permit deleted after grade freeze;
6. symlink/path alias attempt against the permit directory or candidate run directory.

Do not use this gate for real legacy comparison until the independent proof passes **and** Guard V2 is live, because the real consumed permit is part of the authority chain.

---

## 6. GUARD V2 — INDEPENDENT GRADE ACCEPTED

Exact candidate:

`4c6f36ea7e2f68e3eb50cf555390b3c4e98ddbd4`

GPT independently verified this is a descendant of the current live toolbox pin:

`59cfb1cdd1a9779e2a7be406397bea52362db467`

Worker AR-1359 independently reported:

- full toolbox sweep: 276 PASS / 0 FAIL;
- exact parent advance→rewind attack refused;
- token-before-rewind and active-grader-after-rewind refused;
- same-session cross-worktree borrowing refused;
- novel cross-repository marker theft refused with a positive control proving the authorized child worked first.

**PASS. Guard V2 remains eligible for deliberate live promotion.**

AR-1357A's earlier abbreviated/cited Guard V2 SHA was stale/incorrect. The exact promotion target is the full SHA above. Do not use any other candidate.

---

## 7. THE AR-1361 SELF-INVALIDATING PIN PROBLEM IS RESOLVED WITHOUT MOVING WORKER HEAD

AR-1361 measured its bootstrap source at an earlier Worker HEAD and correctly warned that a later Worker commit would stale that pin. Publishing the report itself and then completing AR-1363 advanced the branch, so the old `bootstrap_source_sha` cannot be reused.

GPT did **not** guess the replacement.

GPT created a separate immutable snapshot ref from the live Worker branch without moving Worker 1:

`external-advisor/worker-head-pin-ar1359a-20260819`

Resolved exact SHA:

`6b7d72db82fee80b11ad86b3097ce0965e6b3098`

A second repository compare immediately after snapshot creation proved:

`external-advisor/worker-head-pin-ar1359a-20260819` == `claude/worker1-h1-20260815`

with zero commits difference.

Diff review from the AR-1361 measurement through this exact HEAD shows no change under `scripts/control-plane-bootstrap/`, the frozen G2 queue, or the frozen receipt namespace. Therefore the AR-1361 measured bootstrap bundle / queue / receipt-tree pins remain applicable while `bootstrap_source_sha` is updated to the exact current Worker HEAD.

**Worker 1 must not make another commit on its branch before invoking bootstrap `--plan` / `--execute` for this authorization.** If HEAD or any independently measured frozen field differs, the bootstrap must refuse and the authorization is not to be bypassed.

---

## 8. ONE-SHOT EXECUTABLE CONTROL-PLANE AUTHORIZATION

The following block is the executable marker. It is intentionally closed-schema and one-use.

```json
{
  "schema": "CONTROL_PLANE_BOOTSTRAP_AUTHORIZATION_V1",
  "authorization_class": "EXECUTABLE",
  "authorization_id": "cpb-2026-08-19-0008",
  "ruling_id": "AR-1359A",
  "actor": "top-level-control-plane-guard-repair",
  "execution": "ONE_BOOTSTRAP_EXECUTION",
  "source_actor": "worker-1",
  "target_packet": "AR-1359A",
  "repo": "swayz032/trading-forge",
  "frozen_queue_sha256": "5935b1c6c03860b35e2aee9023f2c70c4630d2e75ef9bfa496024bb2b7efa939",
  "require_ready": 8,
  "require_spent": 0,
  "require_receipts": "GIT_TREE:c11966868f8a511554e1f26bf6e5555c59833d04",
  "require_agent_model_executions_before_launch": 0,
  "hands_free": true,
  "allowed_paths": [
    "scripts/claude_toolbox.mjs",
    ".claude/worker1-hook-guard-manifest.json",
    "docs/replay-results/worker-advisor-reports/AR-1364-CONTROL-PLANE-GUARD-V2-PROMOTION-CLOSEOUT-cpb-2026-08-19-0008.md"
  ],
  "bootstrap_source_sha": "6b7d72db82fee80b11ad86b3097ce0965e6b3098",
  "bootstrap_bundle_sha256": "fa17a097329f057c3ac48956542f9066e3c5af550657a95f7b9eca407fe40347"
}
```

### Execution law

1. Run the existing bootstrap in read-only `--plan` mode first.
2. It must identify **AR-1359A as the newest ruling** and accept exactly the marker above.
3. If it returns any refusal — stale authority, HEAD mismatch, queue mismatch, receipt-tree mismatch, dirty receipt state, used authorization ID, bundle mismatch, or any other code — **do not hand-edit around it**. Report the exact refusal.
4. If authorized, execute exactly one bootstrap run.
5. Promotion target is exact Guard V2 candidate `4c6f36ea7e2f68e3eb50cf555390b3c4e98ddbd4`.
6. Verify `59cfb1cd...` is an ancestor of `4c6f36ea...` in the execution seat too.
7. Re-pin both halves of the live toolbox identity:
   - `scripts/claude_toolbox.mjs` actual materialization pin;
   - `.claude/worker1-hook-guard-manifest.json` expected pin **and the newly computed bundle SHA256**.
8. Compute the Guard V2 toolbox bundle from actual materialization at `4c6f36ea...`; **never guess or copy a bundle digest from prose**.
9. Do not change `.claude/settings.json` in this promotion. Its existing `SessionStart` and `PreToolUse` matchers already route `Agent|Task` through the trusted doorway; Guard V2 is a toolbox-law re-pin, not a settings rewrite.
10. Preserve the G2 queue and receipt tree byte-for-byte.
11. Produce the exact closeout artifact authorized above.
12. A post-claim failure spends `cpb-2026-08-19-0008`. Do not retry it under the same ID.

The bootstrap's `agentModelExecutions=0` remains a property of the bootstrap process itself, not proof that Worker 1 has never used Agent earlier in its session. Do not overclaim that field.

---

## 9. LIVE GUARD V2 ACCEPTANCE TEST AFTER PROMOTION

The promotion is not finished merely because two files contain the new pin.

After the guarded promotion is integrated through the existing mechanism, prove on the **live Worker seat**:

### Positive control

Dispatch one harmless `accuracy-validator` with `isolation:"worktree"` through the ordinary Agent path.

It must:

- receive the exact one-use activation token through the guarded prompt-update path;
- activate successfully;
- execute an ordinary read/test Bash or PowerShell command;
- remain unable to use `Edit`, `Write`, `NotebookEdit`, nested `Agent`, or nested `Task`.

### Negative control

Create/use an unrelated synthetic `worktree-agent-*` seat that has not redeemed the matching token.

It must **not** borrow authorization from the live grader, even if session/branch-looking metadata is made similar.

### Parent-history control

After activation, parent HEAD rewind/rewrite must freeze/refuse the child. Normal parent fast-forward progress must retain the designed behavior proven by the V2 test suite.

Return exact command output / refusal reason, current live toolbox pin, computed bundle SHA, and the resulting Worker/control-plane commit identities.

---

## 10. WHAT GPT TAKES NEXT — SOURCE-FIDELITY V2 ENGINEERING TARGETS

The five-video run exposed five concrete defects worth engineering, in priority order:

1. **Strategy segmentation / evidence disjointness**  
   A regime, filter, qualifier, or educational subtopic must not silently become a second executable strategy.

2. **Executable-role vs source-gap consistency**  
   If a candidate says an entry trigger is source-unresolved, it cannot simultaneously emit that same field as an executable `role:"trigger"` without an explicit, mechanically enforced contradiction disposition.

3. **Quote-to-claim semantic support**  
   Literal substring presence is necessary evidence hygiene but not sufficient proof that the quote supports the attached claim.

4. **Audience attribution**  
   `what I do` vs `what I teach people/students` vs `example/context` must remain distinct when the difference changes executable behavior.

5. **Competing-definition gaps**  
   Two individually literal target/stop definitions that imply different price levels must become an explicit unresolved conflict unless the source itself reconciles them.

Do not solve these by making the literal verifier permissive. A comma/case normalization rule may be studied later, but semantic substitutions such as `Now` vs `So` must never become invisible merely because they are small strings.

GPT will continue this engineering lane separately under doer≠grader law. Worker 1's immediate job is the Guard V2 promotion + independent bound-grade attack, not a parallel rewrite of the same GPT source-fidelity files.

---

## 11. FACTORY / MONEY-PATH STATUS

Current truthful status after AR-1363:

- old 40-video / 120-row Factory baseline: preserved;
- old baseline compile-ready survivors: 0;
- five-video transcript-first diagnostic: complete as an original one-pass experiment;
- clean independently source-fidelity-PASS fresh candidates: 0;
- source-faithful diagnostic compile survivors: 0;
- broad legacy contamination: NOT PROVEN;
- class-specific legacy strategy-identity corruption: PROVEN on `7ieYBa7Z-Hg`;
- Guard V2: independently passed, live promotion authorized by this ruling;
- hardened bound-grade compare gate: GPT-authored, awaiting Worker independent attack;
- 160-new-video ingestion: HOLD until population identities are supplied/authorized;
- PAPER/live: NOT AUTHORIZED.

Zero survivors is not a reason to weaken source truth. The breakthrough is a pipeline that refuses wrong semantics quickly enough to find the real strategies that survive.

---

## 12. WORKER 1 NEXT WORK ORDER — EXECUTE, DO NOT IDLE

Run these lanes in parallel where the guard allows:

### Lane A — control plane

1. Without advancing Worker HEAD from `6b7d72db82fee80b11ad86b3097ce0965e6b3098`, run bootstrap `--plan` against newest GPT ruling AR-1359A.
2. If authorized, execute the one bootstrap authorization above.
3. Promote exact Guard V2 `4c6f36ea7e2f68e3eb50cf555390b3c4e98ddbd4` through the existing guarded mechanism.
4. Run the live positive + negative + parent-history acceptance controls.
5. Preserve the one-shot closeout evidence.

### Lane B — independent GPT gate attack

1. Inspect exact GPT engineering tip `897655fd3ef0b8324aca346a60c3258ef0943cfd`.
2. Run `python scripts/_gpt_opus_bound_grade_compare_proof.py`.
3. Add at least one novel independent attack.
4. Report PASS/FAIL with executable evidence.
5. Do not open real legacy comparison yet if Guard V2 promotion is not complete.

### Lane C — no model spend

Do not re-dispatch the two failed Opus source readers. Do not compile the BOUNDED three. Do not start the 160. Do not PAPER/live.

If Lane A fails, Lane B continues. If Lane B finds a real defect, Guard promotion still continues. The lanes are intentionally independent so one blocker does not idle the other.

---

## FINAL RULING

**AR-1363 is accepted as a disciplined partial result. It proves one narrow but important legacy identity defect and exposes concrete weaknesses in the new transcript-first reconstruction, but it does not prove broad legacy contamination and produces no compile-authorized candidate. Guard V2 is now explicitly authorized for one fail-closed bootstrap promotion under `cpb-2026-08-19-0008`, bound to exact current Worker HEAD `6b7d72db82fee80b11ad86b3097ce0965e6b3098`. Worker 1 must execute that promotion without another Worker commit first, independently attack GPT's bound-grade gate, and continue under the no-fake-survivor law.**
