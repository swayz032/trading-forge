# GPT EXTERNAL ADVISOR RULING — AR-1162

**Date:** 2026-08-14  
**Branch:** `external-advisor/gpt-rulings`  
**Parent GPT ruling:** AR-1161 @ `e29943a9528d2aca3b02a75382a663af68fcf68d`  
**Status:** CORRECTION / V4 OWNERSHIP / AR-NUMBER DISAMBIGUATION  

---

# 1. CORRECTION

The authoritative Blueprint V4 chain is:

```text
GRAPH ENGINEERING
-> COMPILER
-> STRATEGY FACTORY
-> CONTEXT OBSERVER
-> QUALIFICATION
-> AUTONOMOUS RUNTIME
```

AR-1161's shorthand description of Worker 1 as primarily "compiler / money path" is incomplete. The correct Worker 1 ownership after the current paused order is closed is:

```text
WORKER 1 / TEAM LEAD
GRAPH ENGINEERING
-> COMPILER
-> STRATEGY FACTORY
```

Worker 1 may continue into later stages only when explicitly authorized and when doing so does not collide with Worker 2's runtime/safety ownership.

Graph Engineering is Stage 1, not an optional side project. It owns exact source decisions, dependencies, ordering, state transitions, invalidations, entry requirements, and source-owned exits using the existing DecisionAtom/state-machine/decision-closure/dependency-graph backbone.

Do not create a competing graph architecture.

---

# 2. CURRENT CLAUDE ORDER — AR-1138, NOT AR-1148

Repository evidence shows the active unfinished worker order remains **AR-1138**.

The V4 architecture addendum explicitly states:

```text
The active worker order remains AR-1138, as clarified by AR-1140.
```

The worker's own AR-1139 correction also states that the real grading run under AR-1138 is the only authorized next step.

Therefore when Claude quota returns:

```text
resume AR-1138
-> run the exact authorized grading/geometry work
-> test/evidence
-> commit/push
-> worker report
-> STOP for GPT external grading
```

Do not replace that with AR-1148.

---

# 3. WHAT AR-1148 ACTUALLY IS

AR-1148 is a **GPT external-advisor static audit** titled:

```text
AR-1148 GPT static audit: custom PAPER Massive feed and restart warmup
```

It is a downstream pre-audit packet produced on the GPT branch while Claude is quota-paused.

It is not the worker's paused implementation order.

This distinction matters because the repository contains multiple AR numbers from different actors:

```text
worker reports/orders
GPT external-advisor rulings
architecture/control rulings
```

The number alone does not identify the owner. Always read the title, branch, status, and parent context.

---

# 4. WHY "TWO WORKERS" IS ASSOCIATED WITH AR-1160

AR-1160 does **not** mean "two workers = AR-1160."

`AR-1160` is simply the sequential report/ruling identifier assigned to the GPT ruling that documented the Claude Code Agent Teams + Ubuntu/WSL topology.

Conceptually:

```text
AR-1158 = strategy rotation packet
AR-1159 = acceleration / worker-topology control
AR-1160 = Agent Teams + WSL amendment
AR-1161 = master reset execution sheet
AR-1162 = this correction
```

The worker count remains a separate operating decision:

```text
MAX CLAUDE WORKERS = 2
```

AR numbers are report IDs, not worker IDs.

---

# 5. CORRECT TWO-WORKER V4 TOPOLOGY

After AR-1138 reaches a committed externally reviewed decision point:

```text
GPT EXTERNAL ADVISOR / FLASHLIGHT
             ↓
CLAUDE WORKER 1 / TEAM LEAD
GRAPH ENGINEERING -> COMPILER -> STRATEGY FACTORY
             ↕ direct Agent Teams messages
CLAUDE WORKER 2
PAPER / AUTONOMY / EXECUTION-SAFETY
```

Worker 2 may consume prepared disjoint packets involving:

```text
PAPER receipt / qualification
Massive Futures PAPER feed
3AM durable evidence join
strategy rotation
cold-start / recovery
Topstep / Slumhouse execution safety
duplicate-order defense
position reconciliation
kill switch / flatten
```

Worker 2 must not change Graph Engineering/compiler semantic authority owned by Worker 1.

---

# 6. V4 STAGE MAPPING LAW FOR FUTURE PACKETS

Every new GPT packet and every Claude task must declare which V4 stage it belongs to:

```text
GE  = Graph Engineering
C   = Compiler
SF  = Strategy Factory
CO  = Context Observer
Q   = Qualification
AR  = Autonomous Runtime
SUP = Supporting capability such as Visual Intelligence
```

If a task crosses stages, state the seam and primary authority explicitly.

This prevents future shorthand from making Graph Engineering disappear from the plan.

---

# 7. MASTER-SHEET PRECEDENCE

AR-1162 supersedes only the incomplete Worker-1 shorthand in AR-1161.

AR-1161 remains the reset entrypoint, but Claude must apply this correction when reading it:

```text
AR-1161 says Worker 1 = compiler/money path
READ AS:
Worker 1 = Graph Engineering -> Compiler -> Strategy Factory money path
```

All other AR-1161 queue-control, two-worker maximum, one-packet-per-worker, non-collision, and GPT-grading rules remain valid.

---

# 8. BOTTOM LINE

1. **YES — Graph Engineering is Stage 1 of Blueprint V4 and must remain visible in the worker plan.**
2. **NO — Claude's current paused order is not AR-1148. Repository evidence says AR-1138 remains active.**
3. **AR-1148 is a GPT PAPER/Massive-feed pre-audit packet, not the current worker order.**
4. **AR-1160 is just the report number of the Agent Teams/WSL ruling; it does not mean the two workers themselves are called AR-1160.**
5. **After AR-1138 is closed, the two-worker topology is Worker 1 = Graph Engineering/Compiler/Strategy Factory and Worker 2 = PAPER/Autonomy/Execution Safety.**
