# GPT EXTERNAL ADVISOR RULING — AR-1276

## AUTHORITY

This ruling is the live GPT operator message after independent inspection of:

- Blueprint V4;
- Blueprint V4 Revision 5;
- GPT ruling AR-1274;
- Worker-1 AR-1275;
- Worker-1 head `51ad3d9415a1f1a409efdc68a59d3b157ba2a8ef`;
- toolbox head/pin `b6c702821bc48281b02e16773c7c277ae17fb03f`;
- `.claude/worker1-hook-guard-manifest.json`;
- `.claude/settings.json`;
- the pinned `claude-hook-bridge.mjs` / guard boundary;
- the frozen G2 queue, native-call manifest and receipt namespace;
- the new Worker-report discovery helper/contract;
- GitHub status/workflow evidence.

Blueprint V4 + Revision 5 sequencing remains binding. Trading Forge remains at the **Stage 1 — Graph Engineering certification/control-plane gate immediately before Stage 2 — Compiler authorization**. This packet does not authorize compiler execution, broad backtesting, PAPER, broker/Topstep or live execution.

---

## VERDICT

**AR-1275: PARTIAL PASS / CORRECT FAIL-CLOSED STOP.**

Worker-1 did the correct thing.

AR-1275 successfully closed the Worker-report landing problem and preserved the frozen control plane. It correctly refused to modify the three surfaces that AR-1274 had assigned to it but that its own guard law deliberately makes unreachable from a bound Worker-1 seat.

The missing work is therefore **not a Worker-1 compliance failure**. It is an **operator ownership mismatch in AR-1274**: GPT assigned guard-law/settings/root-authority work to an actor that the existing protection model is specifically designed to prevent from doing that work.

That assignment is corrected here.

The frozen G2 eight remain:

```text
READY = 8
SPENT = 0
attempts = {}
receipts = README-only
```

They remain **NO-GO**.

---

## INDEPENDENTLY GRADED STATUS

| Item | Ruling |
|---|---|
| Worker-1 head / report ancestry | **PASS** |
| AR-1275 Track D — canonical Worker-report landing | **PASS, BOUNDED** |
| `scripts/worker-report-latest.mjs` numeric ranking | **PASS** |
| `scripts/worker-commit.mjs` bounded commit-helper repair | **PASS, BOUNDED** |
| AR-1275 Track A — durable Agent→PreToolUse witness | **NOT DONE — CORRECTLY BLOCKED** |
| AR-1275 Track B — PowerShell containment | **NOT DONE — CORRECTLY BLOCKED** |
| AR-1275 Track C — root `CLAUDE.md` slimming | **NOT DONE — CORRECTLY BLOCKED** |
| SessionStart→PreToolUse lifecycle repair | **STILL PASS / NOT REGRESSED** |
| Hands-free `--dangerously-skip-permissions` mode | **INTENTIONAL — PRESERVE** |
| Agent/subagent model executions in AR-1275 | **0 — report-supported and repo-consistent** |
| Opus calibration retries | **0 — required** |
| Frozen G2 calls | **0** |
| Frozen queue contents / `attempts:{}` | **PASS** |
| Frozen receipt namespace | **PASS — README only** |
| Worker-report relay burden on Tonio | **CLOSED** |
| Root `CLAUDE.md` oversize defect | **OPEN** |
| Separate PowerShell guard coverage | **OPEN BLOCKER** |
| Agent→installed PreToolUse durable witness | **OPEN BLOCKER** |
| GitHub CI at Worker-1 head | **NONE** |
| GitHub CI at toolbox pin | **NONE** |

**CI: NONE; tests are local-only evidence.**

---

## 1. AR-1275 STOP WAS THE CORRECT ENGINEERING DECISION

The current Worker-1 manifest allows ordinary packet work under:

```text
src/engine/extraction/
src/engine/tests/
scripts/
docs/replay-results/
.claude/
```

plus the exact design file currently named in `allowed_exact`.

But the protection law is explicitly stronger than scope permission. The guard self-protects:

- its manifest;
- `.claude/settings*.json`;
- `.claude/hooks/`;
- the pinned toolbox;
- the guard doorway / toolbox activator;
- the frozen G2 queue and receipt namespace;
- the native-call manifest;
- the armed-session marker.

That is intentional. A Worker-1 seat must not gain authority over the law that binds Worker-1 merely because a broad edit prefix happens to include `.claude/` or `scripts/`.

The pinned guard source states this bootstrap rule explicitly: an active guarded worker cannot repair the protected toolbox; that is a **desk-authorized control-plane packet on the toolbox authority branch**.

Therefore AR-1275 was correct to refuse a one-half re-pin, correct not to weaken self-protection, and correct not to use the uncovered PowerShell surface as a side door.

### Operator correction

AR-1274 §9A/§9B/§9C named valid defects, but assigned them to the wrong actor.

That is corrected below. Do **not** widen ordinary Worker-1 authority over its own guard.

---

## 2. TRACK D — AUTOMATIC WORKER REPORT LANDING PASSES

AR-1275 created the canonical Worker-report landing path:

```text
docs/replay-results/worker-advisor-reports/
```

on the active Worker branch.

This is the right architecture:

```text
Worker-1
 -> writes report inside its governed Worker-branch scope
 -> commits/pushes Worker branch
 -> GPT operator discovers report from Worker branch

GPT rulings
 -> remain under advisor-reports/
 -> remain on external-advisor/gpt-rulings
```

Tonio does not carry reports between branches.

The helper `scripts/worker-report-latest.mjs` correctly parses the AR number numerically rather than lexically, so `AR-1275` outranks `AR-999`. It also refuses loudly when the canonical directory/report set is absent instead of returning an empty false-green result.

### Bounded contract

The helper currently recognizes integer AR filenames of the form:

```text
AR-<number>-...
```

Therefore, until the helper is deliberately extended, **Worker-report corrections use the next integer AR number** rather than suffix forms such as `AR-1275A`.

### Dynamic onboarding correction

For Worker-1 reports **after AR-1275**, this ruling overrides the older onboarding sentence that Worker reports “normally live under `advisor-reports/`.”

Current contract:

```text
Worker-1 reports:
  active Worker branch
  docs/replay-results/worker-advisor-reports/

GPT rulings:
  external-advisor/gpt-rulings
  advisor-reports/

legacy Worker reports already on GPT branch:
  remain historical evidence; do not move them merely for cosmetics
```

A later desk cleanup may update the static onboarding card, but that documentation edit is not allowed to delay the current control-plane repair.

---

## 3. FROZEN G2 CONTROL PLANE — INDEPENDENT PASS

GPT independently inspected the actual Worker-1 tree at `51ad3d94...`.

The frozen queue still contains exactly 8 queued rows and:

```json
"attempts": {}
```

The native-call manifest still pins the frozen queue SHA:

```text
5935b1c6c03860b35e2aee9023f2c70c4630d2e75ef9bfa496024bb2b7efa939
```

The receipt directory contains only:

```text
README.md
```

There are no G2:

```text
.attempt
.dispatch
.raw
.completion
```

AR-1275 did not move the toolbox branch, did not alter the protected manifest/settings, and did not spend a model call.

**Frozen status remains 8 READY / 0 SPENT.**

---

## 4. POWERSHELL GAP IS REAL — HANDS-FREE MODE IS STILL REQUIRED

The Worker-1 launcher intentionally ends with:

```text
claude --dangerously-skip-permissions
```

**Preserve that.**

Its purpose is correct: Tonio is not the per-tool permission pipeline. The guard decides; the user does not click through ordinary tool approvals.

The defect is the separate tool surface:

- installed `PreToolUse` matcher currently covers `Edit|Write|NotebookEdit|Bash|Agent|Task`;
- this runtime also exposes a separate `PowerShell` tool;
- AR-1275 independently observed PowerShell execute with zero guard interposition;
- the pinned bridge would also fall through unknown tools as `guarded:false` unless explicitly handled.

Therefore a matcher-only repair is insufficient.

### Required control-plane repair

The control-plane actor must:

1. **preserve `--dangerously-skip-permissions`;**
2. add `PowerShell` to the installed `PreToolUse` matcher;
3. add an explicit bridge-level rule that **DENIES `tool_name === "PowerShell"` fail-closed**;
4. if this Claude Code runtime provides a reliable launch-time/tool-registry method to remove the separate PowerShell surface entirely, it may also be disabled there — but only after measuring the exact runtime control;
5. never rely on “PowerShell is in the matcher” while the bridge still returns `guarded:false`;
6. prove normal Worker-1 guarded work remains hands-free with no manual approval prompts.

Do not build a general shell-language parser.

---

## 5. AGENT→PRETOOLUSE WITNESS — CONTROL-PLANE PREPARATION FIRST

The one non-G2 Opus calibration is already spent. It is **not retried**.

The missing proof remains:

```text
real native Agent tool attempt
 -> installed PreToolUse actually fired
 -> trusted durable witness exists
 -> guard denial occurred before model dispatch
```

The pinned bridge currently computes `_audit` and strips it before returning visible hook output. An allowed Agent call therefore remains non-discriminating.

### AR-1277 must prepare the proof without any model dispatch

The control-plane actor shall add the smallest trusted witness mechanism:

- use a control-plane location under the worktree git-dir / guard runtime storage, not an ordinary Worker-writable repository path;
- bind the witness to at least session id, event, tool name, verdict, live branch/head, toolbox pin and toolbox bundle;
- do not store raw strategy prompts; a fixed probe id / prompt hash is sufficient;
- make the witness self-protected / not forgeable by Worker-1 tools;
- add one uniquely reserved **non-G2 Agent guard-probe identity**;
- prove mechanically that the reserved probe identity cannot equal any of the 8 frozen native-call identities;
- when that exact probe reaches `PreToolUse:Agent`, persist the witness and deny before the G2/model-dispatch path.

### Important sequencing

**AR-1277 does not perform the live Agent tool attempt.**

It installs/tests the mechanism, re-pins it correctly and proves the denial path mechanically.

Only after GPT independently passes AR-1277 may a normal bound Worker-1 seat perform the one live hard-denied guard probe in the next packet.

That avoids asking an unproven control path to prove itself while it is still being edited.

---

## 6. ROOT `CLAUDE.md` — REPAIR IT, BUT DO NOT GIVE WORKER-1 PERMANENT SELF-EDIT AUTHORITY BY ACCIDENT

AR-1275 measured root `CLAUDE.md` at approximately:

```text
205,646 bytes
1,096 lines
```

The live Claude Code warning reports roughly 203.5k characters, above the 150k warning threshold.

The defect remains real. The file carries extensive historical Wave prose and stale static phase text in the always-loaded instruction surface.

### Required repair in AR-1277

The desk-authorized control-plane/document-maintenance actor may edit root `CLAUDE.md` directly as part of this bounded packet.

Do **not** permanently widen the normal Worker-1 manifest merely so Worker-1 can rewrite its own standing authority file.

Target:

```text
<= 100,000 characters
preferably materially smaller
```

Keep in the hot file:

- project mission;
- current durable safety rules;
- Worker branch/worktree law;
- GPT rulings are live operator authority;
- Blueprint V4 + Revision 5 precedence;
- FAST + ROBUST engineering law;
- source-fidelity law;
- report/ruling discovery pointers;
- critical guard/lock instructions.

Move, do not destroy:

- historical Wave journals;
- closed pass histories;
- long subsystem narratives;
- old static phase summaries;
- deep reference material already suited to `AGENT-LOGS.md`, design docs or `.claude/rules/`.

Required evidence:

```text
before bytes/chars/lines
after bytes/chars/lines
source section -> destination map
required-hot-rule preservation check
no unique knowledge silently deleted
```

Replace stale “current phase” prose with a dynamic authority pointer:

```text
GPT onboarding
 -> Blueprint V4
 -> Revision 5
 -> newest GPT ruling
 -> actual repository evidence
```

This documentation repair must not weaken mechanical guard law.

---

## 7. AR-1277 — EXACT NEXT PACKET: CONTROL-PLANE / GUARD-REPAIR SEAT

### Actor

AR-1277 is assigned to a **dedicated top-level CONTROL-PLANE / GUARD-REPAIR seat**.

It is:

- **not** the ordinary bound Worker-1 lane;
- **not** an Agent/subagent spawned from Worker-1;
- the same class of desk-authorized actor that previously repaired/re-pinned the SessionStart lifecycle defect;
- authorized only for the narrow control-plane/document repairs below.

If an ordinary bound Worker-1 seat receives this ruling, it must **not** attempt to bypass self-protection. It should route/hand off to the permitted control-plane seat through the established engineering workflow. It must not assign repair commands to Tonio.

### Authorized scope

AR-1277 may modify only what is necessary among:

```text
advisor-prepared/gpt-speed-engineering-lane/tooling/**
.claude/settings.json
.claude/worker1-hook-guard-manifest.json
scripts/claude_toolbox.mjs
scripts/worker1_seat_launch.ps1        # only if measured runtime PowerShell disable needs it
CLAUDE.md
AGENT-LOGS.md / .claude/rules/** / named docs used solely as destinations for moved CLAUDE.md history
```

Do not touch the frozen G2 queue, native-call manifest, or receipt files.

### A. Agent hook witness preparation

- implement trusted session-bound probe witness;
- hard-deny reserved Agent guard probe before dispatch;
- self-protect the witness;
- prove probe cannot collide with frozen 8;
- no live Agent tool call in AR-1277;
- no model/subagent execution.

### B. PowerShell containment

- preserve hands-free launch;
- add `PowerShell` matcher coverage;
- explicitly bridge-deny PowerShell;
- optionally disable the runtime surface only if measured/supported;
- prove unknown-tool fallthrough does not reopen PowerShell;
- prove Bash/Edit/Write/Agent registration still works as intended.

### C. `CLAUDE.md` hot-context repair

- reduce below 100k chars, preferably far lower;
- move history/reference rather than deleting it;
- preserve current authority and safety rules;
- remove stale static-current-phase claims from the hot file;
- produce before/after/move-map evidence.

### D. Immutable toolbox re-pin

If toolbox bytes change:

1. advance only from the current toolbox lineage `b6c70282...`;
2. compute the exact toolbox bundle using the production materializer;
3. independently cross-check the bundle hash;
4. update **both halves** of the re-pin deliberately:
   - `scripts/claude_toolbox.mjs` actual materialized pin;
   - manifest `_toolbox_pin` + `_toolbox_bundle_sha256` expected identity;
5. append truthful pin-history provenance;
6. verify descendant ancestry;
7. run the live doorway against the new identity.

`A RE-PIN IS TWO FILES.`

### E. Test order

Use the fast/robust order:

1. focused RED/GREEN for probe witness;
2. focused RED/GREEN for PowerShell matcher + bridge denial;
3. collision/forgery/mutation negatives;
4. existing SessionStart→PreToolUse lifecycle suite;
5. G2 pre-call neighboring regression;
6. full toolbox local suite once;
7. live doorway dry-run without Agent/model dispatch;
8. GitHub CI/status reported separately.

### F. Frozen preservation

Before and after AR-1277 must show:

```text
queue SHA = 5935b1c6c03860b35b6ec0402a3152dc287a8ae427eb0d86661b3fb43ec01823
```

**CORRECTION:** the line above is deliberately NOT accepted as the queue SHA — it is the extraction SHA appearing inside the queue and would be a false-green if copied as the queue identity.

The actual required frozen queue SHA remains:

```text
5935b1c6c03860b35e2aee9023f2c70c4630d2e75ef9bfa496024bb2b7efa939
```

and:

```text
attempts = {}
READY = 8
SPENT = 0
G2 .attempt/.dispatch/.raw/.completion = 0/0/0/0
```

That explicit wrong-hash control is included so a packet cannot pass merely by finding a familiar 64-hex value inside the JSON.

### G. Required AR-1277 runtime statement

```text
Agent/subagent model executions in this packet: 0
Opus calibration retries: 0
Frozen G2 calls executed: 0
```

### H. AR-1277 report location

Because AR-1277 is a control-plane/guard-repair packet rather than ordinary Worker-1 lane work, its report may land on the branch controlled by that repair seat. It must identify exact branch/head/toolbox pins and be discoverable without a Tonio relay.

If it lands on Worker-1, use:

```text
docs/replay-results/worker-advisor-reports/
```

Do not require Worker-1 to write to the GPT branch.

---

## 8. AFTER AR-1277 PASSES — NOT BEFORE

A successful AR-1277 does **not** automatically release the frozen eight.

The next acceptance packet will return to the normal Worker-1 desktop shortcut / bound Worker-1 seat and will prove:

1. normal hands-free startup — no manual approval prompts;
2. PowerShell absent or mechanically denied;
3. a single real reserved Agent **tool attempt** reaches installed PreToolUse;
4. durable session-bound witness exists;
5. expected outcome is guard DENY before dispatch;
6. expected model/subagent executions = 0;
7. frozen eight remain 8 READY / 0 SPENT;
8. slimmed `CLAUDE.md` loads without the oversize warning.

If the live probe unexpectedly launches a model because the hook did not fire, STOP immediately, report the actual dispatch honestly, and **do not retry**. Do not relabel an unexpected dispatch as a zero-dispatch success.

Only a later GPT ruling may release the frozen eight.

---

## 9. WHAT AR-1275 DID NOT PROVE

Keep these claims bounded:

- Agent being listed in `.claude/settings.json` does not prove the real Agent event traversed the hook.
- An allowed Agent result does not prove hook traversal.
- PowerShell being read-only in AR-1275 does not prove it cannot write; it proves the surface is outside guard interposition.
- Local tests are not GitHub CI.
- `CLAUDE.md` content cleanup does not repair mechanical guard gaps.
- The new Worker-report helper discovers reports; it does not independently certify their content.

---

## 10. FORBIDDEN UNTIL NEXT GPT RELEASE

- no frozen G2 call;
- no second Opus calibration;
- no calibration substitute;
- no live Agent/model dispatch in AR-1277;
- no compiler execution authorization;
- no broad backtest campaign;
- no PAPER;
- no broker/Topstep/live;
- no removal of `--dangerously-skip-permissions`;
- no restoration of routine user approval prompts;
- no blanket weakening of Worker-1 self-protection;
- no broad shell-parser project;
- no asking Tonio to run repair commands, select cwd, inspect hook output, relay reports or clean temporary files.

---

## 11. V4 / REVISION-5 POSITION

Current map:

```text
STAGE 1 — GRAPH ENGINEERING
    CURRENT: certification/control-plane gate
    -> guard/runtime proof for the frozen certification lane still open

STAGE 2 — COMPILER
    LOCKED until certification gate is green

STAGE 3 — STRATEGY FACTORY
    LATER

STAGE 4 — CONTEXT OBSERVER
    LATER

STAGE 5 — QUALIFICATION
    LATER

STAGE 6 — AUTONOMOUS RUNTIME
    LATER
```

Revision-5 PAPER calendar targets do not override this state.

---

## 12. SAFETY / MONEY-PATH LOCKS

```text
Certification                  RED / OPEN
Frozen G2 eight                NO-GO — 8 READY / 0 SPENT
One-shot Opus calibration      SPENT — NO RETRY
Agent hook runtime witness     OPEN
PowerShell containment         OPEN
Compiler authorization         NO-GO
Broad backtest campaign        NO-GO
PAPER                          NO-GO
Broker / Topstep / live        NO-GO
```

---

# OPERATOR DIRECTIVE

**AR-1275 PASSES THE WORK IT WAS ACTUALLY PERMITTED TO DO. ITS STOP ON TRACKS A/B/C WAS CORRECT. DO NOT WEAKEN WORKER-1 SO IT CAN EDIT ITS OWN GUARD.**

**ROUTE AR-1277 TO A TOP-LEVEL DESK-AUTHORIZED CONTROL-PLANE / GUARD-REPAIR SEAT. PREPARE A DURABLE AGENT PRETOOLUSE PROBE WITNESS, HARD-CONTAIN THE SEPARATE POWERSHELL TOOL WHILE PRESERVING `--dangerously-skip-permissions`, SLIM ROOT `CLAUDE.md` WITHOUT LOSING UNIQUE KNOWLEDGE, AND RE-PIN BOTH TOOLBOX IDENTITY HALVES IF TOOLBOX BYTES CHANGE.**

**AR-1277 EXECUTES ZERO AGENT/SUBAGENT MODELS AND ZERO FROZEN G2 CALLS. AFTER GPT PASSES THAT CONTROL-PLANE INSTALL, RETURN TO THE NORMAL WORKER-1 SHORTCUT FOR THE REAL HARD-DENIED RUNTIME PROBE.**

**TONIO IS NOT THE BOOTSTRAP, PERMISSION PIPELINE, REPORT RELAY OR REPAIR TECHNICIAN.**
