# Wave Close-Out Registry & Historical Campaign Detail

> Moved verbatim from CLAUDE.md §2 during the 2026-08-18 token-optimization pass.
> On-demand reference only — not always-loaded. Historical record of closed waves/campaigns.

### Wave close-out registry (full history archived)

> The full close-out paragraphs (tests, migrations, env vars, audit actions, carry-forwards) were moved **verbatim** to `AGENT-LOGS.md` → `## CLAUDE.md Wave Close-Out Archive (moved 2026-07-09)`. This table is the index. Do not re-add close-out prose here — new wave closes get ONE table row + a full entry in AGENT-LOGS.md.

| Wave / close | Summary |
|---|---|
| Wave 24 CLOSED 2026-05-23 | 23 of 24 backlog items shipped (95.8%). |
| Wave 25 active 2026-05-23 onward | institutional confluence + adaptive exit engine. |
| Wave 25 Pass 2 CLOSED 2026-05-24 | institutional-grade hardening pass. |
| Wave 25 Pass 2 (5-TF + HTF narrative) CLOSED 2026-05-24 | separate from the hardening track. |
| Wave 25 Pass 3 (Persistent Liquidity Map Engine) CLOSED 2026-05-24 | `liquidity_levels` table (17 level_types: PDH/PDL/PWH_ISO/PWL_ISO/PMH/PML/Asian/London/HOD/LOD/naked_poc/untouched_fvg/untouched_ob/EQH/EQL) with sweep_probability heuristic (age decay + touch decay + distance attenuation + HTF significance) + composite rank scoring. |
| Wave 25 Pass 4 (Confluence Decay Engine) CLOSED 2026-05-24 | every confluence carries time-based decay confidence. |
| Wave 25 Pass 5 (Order Flow Layer: VWAP bands + Anchored VWAP + SMT ES↔NQ) CLOSED 2026-05-24 | VWAP bands (1σ, 2σ) + anchored VWAP via `compute_vwap_with_bands()` + `compute_anchored_vwap()` in `src/engine/indicators/core.py` (session-resetting at 18:00 ET Globex boundary). |
| Wave 25 Pass 6 (Regime 3→5 + Narrative Continuity) CLOSED 2026-05-24 | 4 new regime classifications (EXPANSION/COMPRESSION/HIGH_VOL_MACRO/LOW_LIQ_CHOP) via `classify_institutional_regime()` in bias_engine.py with RegimeEvidence dataclass capturing ATR percentile + volume ratio + range vs ATR + macro event flag + session health) + 4 new playbooks + A/M/E narrative state machine. |
| Wave 25 Pass 2.5 (Pre-Market Institutional Expansion) CLOSED 2026-05-24 | 18 new `pre_market_sessions` fields (TICK/ADD/VOLD/TRIN at open, DXY/10Y direction, cross_asset_aligned, bond_auction_today, extended_calendar_events, nearby_naked_pocs, london_range_high/low/points, pmh/pml, pwh_iso/pwl_iso, first_30min_volume_ratio) + market-internals-service + 09:00 ET Discord briefing. |
| Wave 25 Pass 7 (Adaptive Exit Engine, SCAFFOLD) CLOSED 2026-05-24 | Pure-function adaptive-exit-engine.ts with all 5 sub-tracks; 3 wiring gaps deferred to Wave 25.5; Style C 33/33/34 remained LIVE default. |
| Wave 25 (Passes 1-7 + 2.5) total: 7+1 dispatches, 600+ new tests, 11 migrations (0134-0144), 14 new subsystems registered | End-to-end retail→institutional transformation infrastructure shipped; Wave 25.5 closes the final 3 wiring gaps to make adaptive exits LIVE. |
| Wave 25.5 (Adaptive Exit Wiring) CLOSED 2026-05-24 | Gap A (paper-execution-service.ts position-open wires computeExitPlan + paper_positions.exit_plan JSONB via migration 0145 idx 147 + fail-soft fallback to static_styleC) + Gap B (backtester._apply_trade_management routes to adaptive_exits.py) + Gap C (runner-trail execution) — adaptive exits LIVE. |
| Wave 26 (post-Wave-25.5 stabilization + cohort instrumentation) CLOSED 2026-05-24 | 7 of 8 Wave 26 candidates shipped (item 5 CONFLUENCE_REQUIRE_DISTRIBUTION_PHASE deferred for 30-day data). |
| Wave 26 Pass A (transcript_extractor → gemma4:e2b LOCAL-FIRST) CLOSED 2026-05-24 | model-only swap, prompt/KB/few-shot UNCHANGED. |
| Wave 27 Pass 1 CLOSED 2026-05-25 | quantum backtest replay-grading harness LIVE (5 of 6 sub-tracks shipped, A6 architect close-out this commit). |
| Wave 27 Pass 1.5 CLOSED 2026-05-25 | quantum replay auto-fire + weekly Discord verdict LIVE (autonomous-agents-drive-everything operator reality). |
| Wave 27.5 Pass C CLOSED 2026-05-25 | 5 Backtest Engine HIGH findings closed; ALL 11 CRITICAL+HIGH findings in Wave 27.5 now closed. |
| Wave 27.5 Pass B CLOSED 2026-05-25 | 3 Walk-Forward HIGH findings closed + B14 ci_high gate wired at PAPER → DEPLOY_READY. |
| Wave 27.5 Pass A CLOSED 2026-05-25 | Monte Carlo CRITICALs closed; B14 Survival Twin unblocked for hard-gate wiring. |
| Wave 26 Phase 1 CLOSED 2026-05-24 | institutional error-to-success feedback loop (4 of 6 passes shipped, 2 BLOCKED on Wave 25 Pass 7 merge). |
| Wave 26 Pass G / Pass A (Archetype Expansion) CLOSED 2026-05-26 | 2 new engine archetypes LIVE + multi-path source resolver + paper-signal observability wired (4 sub-tracks, 175 cumulative new tests GREEN). |
| Wave 27.5 Pass D + MASTER CLOSE 2026-05-25 | institutional-grade certification achieved (19 of 19 findings closed across 14 commits). |
| Wave 27 Carry-Forward MASTER CLOSED 2026-05-25 | all 4 Wave 27.5 carry-forwards executed (8 commits + ~250 new tests). |
| Wave 28 Pass A CLOSED 2026-05-26 | composite-health observability bus + aggregator LIVE in PURE OBSERVABILITY MODE (3 rounds, 4 commits, 156 new tests GREEN). |
| Wave 28 Pass B CLOSED 2026-05-26 | composite-health SHADOW GATE MODE LIVE (2 rounds, 3 commits, 49 new tests GREEN; cumulative Wave 28 = 205 tests). |
| Wave 29 Pass C CLOSED 2026-05-26 | quantum RL agent bridged to production state + composite 13th subsystem + A/B paper routing (2 rounds + architect close, 3 commits, ~190 cumulative new tests GREEN). |
| Wave 29 MASTER CLOSED 2026-05-26 | Slumdawg V2.1 quantum RL bridge + SHADOW lifecycle stage + frozen-policy SHA-256 contract + A/B paper-routing observability surface SHIPPED (4 passes, 5 commits + master close, ~432 cumulative new tests GREEN). |
| Wave 29 Pass B CLOSED 2026-05-26 | frozen-policy SHA-256 contract + HMAC-signed override endpoint + daily regime-drift-detector cron with auto-demotion (2 rounds + architect close, 53 new tests GREEN; Wave 29 cumulative ~354 = Pass C 190 + Pass A 111 + Pass B 53). |
| Wave 29 Pass A CLOSED 2026-05-26 | SHADOW lifecycle stage + PBO overfit hard gate + shadow-signal divergence promotion gate (Round 1 + architect close, 111 new tests GREEN; Wave 29 cumulative = ~301 with Pass C 190 + Pass A 111). |
| Backtest Engine Institutional-Grade Hardening CLOSED 2026-06-22 | adversarial 3-agent audit + 7 fixes. |
| /goal deep-scan CLOSED 2026-07-11 (landed thru `7256838b`) | 16-charter adversarial scan @ f8d5855d → 31+3 findings. **ALL 34 ADDRESSED — 33 FIXED+VERIFIED + WIRE-1 documented (operator OHLC-sourcing decision)** incl. ALL 3 CRITs (A-1 compliance-heartbeat, CAP-1 kill-switch L3 force-close, F1 adaptive-trail parity): 10 non-instrument + 18 of 19 instrument. Ratify-skill operator-amended → autonomous-under-independent-grade (operator non-coder). Method: adversarial find→independent refute→fix→independent grade (agent-loop)→binding integration-verify→land→record. ★ LESSON: base-verify before blaming a fix — CMP-1 + regime-drift wrongly excluded on pre-existing failures, recovered. Final batch fixed autonomously: N8N-2, MIG-2, API-2, QC-2, MIG-3. Only WIRE-1 (architecture decision) documented. Full: AGENT-LOGS + memory project_goal_deepscan_2026_07_11. |
| /goal deep-scan (goalscan-authpine) CLOSED 2026-07-16 @ base `61bb20a3`, landed FF on `hardening/phase-0` | 9-finder scan run CONCURRENTLY with the "$250–$1K/day" campaign; strict deconfliction (campaign owns the instrument/execution surface). 14 findings closed on the NON-overlapping control-plane/delivery/telemetry surface: auth guards (A1 `/cascade`, A2 frozen-config PATCH, A3 liquidity-batch secret-OR-office, A4 vacation-mark, A5 SHADOW-delete, A6 false-HMAC-doc), observability (O1 alert-correlationId factory, O2 outage-SSE, O3 human_admin→human), pine (P1 phantom-success export status, P2 warn-only doc), C1 (`check:2026-compliance` now guards `FIRM_CONTRACT_CAPS`), n8n N1–N4 (5P cron/cred, watchdog fail-loud HMAC, snapshot no-restore). **VERIFIED BAND 8** (accuracy-validator, doer≠grader, no inflation — initially 7, RE-GRADED to 8 after the two reservations closed: F-1/F-2 fabricated comments corrected + a runtime guard integration test proving the Office guard EXECUTES at `/cascade`; 28/28 auth tests; 3 CI gates green). ZERO carry-forwards (operator mandate §11c). All CRIT/HIGH findings in campaign-owned files (DLL-blackout, manual-path gate-skip, invariant/CPCV SHADOW→PAPER dead gates, backtest bare-except, ES/NQ/CL micro-mislabel) HANDED OFF verified. Full: AGENT-LOGS 2026-07-16 goalscan entry. |
| /goal deep-scan ROUND 2 (goalscan-r2) CLOSED 2026-07-17 @ base `24664dc3` | Scheduler CRIT×2 (n8n-backup zero-pad guard never fired; weekly-drift auto-HALT dead during pauses → ALWAYS_RUN_JOBS) + graduation leader-name cross-market collision + per-trade recon/CME-day helpers + RL false-green three-way exit (0/1/3, non-paging degraded) + **bias_state persistence ROOT-CAUSE** (mig 0134 DDL never executed live → writer threw daily 2026-05-24→07-17 into swallowed warn behind 594 green audits; forward-fix mig 0203 applied, 3 drift victims closed) + **schema-drift canaries** (boot canary + reader contract test + bias-state freshness cron — canary is POLICY after 2nd silent death in class). RL Path-A ratify packet STAGED w/ demonstrated feasibility artifact + fork anchored A-only + retro-tag audit clean-by-vacancy (`docs/rl-path-a/`). **VERIFIED BAND 8** (accuracy-validator doer≠grader, 2-3 paths per claim, fault-injection RED-proof; RL cage CONFIRMED 4/4). ZERO carry-forwards. Full: AGENT-LOGS 2026-07-17 entry. |
| /goal deep-scan WAVE 2 (goalscan-crit) CLOSED 2026-07-16, landed FF `0896aaca` | The 7 CRIT/HIGH the authpine wave HANDED OFF, fixed under **explicit operator authorization** (band-8 target). All fail-closed/fail-loud hardening (stricter-only): DLL 95% force-close now fires during the news-blackout window (`paper-signal-service.ts`); B-2 invariant + CPCV-Style-C gates now enforce on SHADOW→PAPER + the manual `_promoteStrategyInner` path runs the same shared B14/WFE/param-drift/DSR/MC-survival evaluators as the cron (`lifecycle-service.ts` + `check-gate-parity.mjs` manual-path region); backtester DSL re-raises `ZeroVolumeOnTradeCriticalBar` + ES/NQ/CL via `get_contract_spec()` (`backtester.py`). **VERIFIED BAND 8** (accuracy-validator, doer≠grader, no inflation; 25/25 new tests; rebased clean over campaign W2 stop-geometry). Grader-surfaced pre-existing follow-up: `b14-ci-gate.ts` `legacy_proceed` returns passed:false vs docstring PROCEED (mirrored not fixed; needs own packet). Full: AGENT-LOGS 2026-07-16 WAVE 2 entry. |
| CAMPAIGN M3-core PAPER Authority Flip CLOSED 2026-07-17 | PAPER lifecycle state flipped from TradersPost-authoritative to internal-engine-only (D6/D7); new `paper-authority-states.ts` single source of truth replaces 3 duplicated literals; new sibling stop-on-exit-from-PAPER block closes the double-writer hazard at the DEPLOY_READY boundary; `detectStalePaperSessions()` gained its first-ever lifecycle-state guards. DEPLOY_READY+ unchanged. **VERIFIED BAND 7** (accuracy-validator, doer≠grader; 3 findings closed same-wave — F-1 CRITICAL fabricated safety-test claim on the live-capital boundary, F-2 HIGH untested double-writer safeguard, F-3 MODERATE correlation_id gap — all RED-proofed non-vacuous). 10 pre-existing (not M3-caused) test-window/literal fragility failures independently root-caused via base-commit comparison and fixed same-wave; zero new regressions across the full 12.4k-test suite. Full: AGENT-LOGS 2026-07-17 M3-core entry. |
| CAMPAIGN W7 Close-out CLOSED 2026-07-18 | Final campaign gate: full-tree regression verification (every full-suite failure — ~53 vitest + 100 pytest — git-blamed to root causes predating campaign base `61bb20a3`, zero campaign regressions; opportunistically repaired the vitest subset + 2 real production fixes); end-to-end composed-wiring trace on real PGlite data proving W1+W2+C-05+M2 compose on one real trade (new `w7-composed-correlation-id-trace.test.ts`); independent accuracy-validator cross-system truth-test (P&L reconciliation + 2%-risk-at-managed-stop both CONFIRMED on fresh non-overlapping scenarios; surfaced + fixed a real entry-side bar-time asymmetry bug, the F-3 bug class missed on entries); trading-forge-architect cross-cutting pass (confirmed M3 convergence, found + fixed one orphan in `paper-journal-recon.ts` + 2 stale doctrine comments); doctrine coherence sweep (2 stale strings fixed); fresh from-zero independent re-grades of W3A and W3B (both **VERIFIED BAND 7**, matching their own claimed bands exactly — no inflation found; the W3B re-grade also surfaced + fixed a real stale-session auto-stop bug, `detectStalePaperSessions()` keying on signal production instead of feed liveness, now more consequential post-M3). ZERO carry-forwards — 2 genuine findings recorded, not silently adopted: the C4 survival-gate's `raw_survival_score` is architecturally hardcoded to 0.0 (dormant, 0 backtests live today, needs a scoped design decision before backtests flow); the ~80-file pre-existing test-debt backlog (enumerated, git-blamed, explicitly an operator scope decision per an advisor consult mid-session — not this campaign's to solo-adopt). Full: AGENT-LOGS 2026-07-18 W7 close-out entry. |
| /goal deep-scan "deepscan-b" CLOSED 2026-07-19, landed FF `98bd9838` | 28 findings (11 CRIT/HIGH confirmed, 0 refuted). Non-instrument: notification-service drop-audits, economic-calendar audit-status fix, admin-recovery HMAC session_id binding, admin.ts office-guard gaps, pine_compiler phantom-var fix, M3 orphaned-PAPER-position force-close. Instrument ratify packets: stop-geometry mini-ceiling parity, confluence-decay bar-unit fix, Pine faithful-flag confluence detection, fill-time gate re-verify, backtest roll-spread fail-loud. **Self-caught mid-wave regression:** the gate-contract-restoration packet initially reverted BIF/WFE/parameter-drift's `85e1500b`-hardened (2026-07-12, operator-authored) legacy-null/CPCV-exempt BLOCK behavior back to grandfather-PROCEED, on the false premise (borrowed from a same-day sibling commit `707810b7`) that this was the sibling-gate contract — it wasn't; `85e1500b`'s own AGENT-LOGS entry states it deliberately hardened PBO/WFE/DSR/BIF/parameter-drift/B14-CI/slippage-survival to fail closed. Caught via a failing test literally named for the hardening commit; corrected same-wave (restored all 3 gates + PBO's true legacy-null to BLOCK, citing `85e1500b` in corrected docstrings) after 2 operator-confirm questions (PBO legacy-null → BLOCK; a newly-found real bug in `paper-to-deploy-ready-gates.ts` where Gates 4/5 checked a stale status-string allowlist instead of the gate's own `passed` field → fixed, RED-proofed). slippage-survival's own later deliberate walk-back (`7cc4ab56` "Restore advisory slippage promotion gate") correctly left untouched — not every doc/code disagreement is a bug; check which side is the deliberate later commit. **VERIFIED**, 362/362 gate-related tests, zero carry-forwards. Full: AGENT-LOGS 2026-07-19 deepscan-b entry. |

**Deep-scan #23 fix-wave CLOSED 2026-07-10 — 4 non-instrument fixes landed after a same-day concurrent-campaign reconciliation.** A Workflow-orchestrated 15-finder scan (pinned to an orphaned fork of `hardening/phase-0`) certified VERIFIED BAND 2, but by the time its fix wave landed a concurrent session had independently pushed ~200 commits fixing nearly the entire finding set. Reconciled by hand against the live tip rather than trusting git ancestry (a straight rebase surfaced 85 unrecognized commits and was aborted). Landed: (1) 2 weekly-cron day-of-week bugs — `weekly-drift-2sigma-check` + `n8n-drift-detector-weekly` were both registered on Monday instead of Sunday and had never fired since shipping; re-expressed on top of the concurrent session's `scheduleUtc()` UTC-timezone wrapper via a new pure helper `weekly-cron-et-guard.ts::isSundayAtEtHour()`; (2) re-synced 2 stale committed n8n workflow export files that still showed webhook auth missing; (3) corrected the MFFU doc's false claim that a same-device `instance_id` collision check exists in code (it doesn't — real carry-forward before a 2nd MFFU account shares device proximity); (4) fixed `wave26-gemma4-smoke-test.ts --parity-only` silently skipping `runStaticParityTests()` — the actual documented merge-gate check — which on being fixed correctly surfaced a genuine pre-existing data gap (4 production fixtures missing `speaker_concepts`), not a false green. tsc clean, 8/8 relevant vitest green, all 3 CI gates green. Full incident + corrected memory record: `project_deepscan23_band2_landing_gap_2026_07_10`.

**Full-codebase `/goal` deep-scan fix-wave CLOSED 2026-07-10 (distinct from, and concurrent with, Deep-scan #23 above — verified zero commit/file overlap before recording) — 9 fixes landed `dfdc36e0`→`99927f1a`, OVERALL BAND 3 (per-item table; not re-derived after the follow-up fix below — see caveat).** Same divergent-history hazard as #23 (Phase-0-pinned base SHA was not an ancestor of the live tip; resolved by hand-porting against the live tip instead of git-mechanical cherry-pick — see AGENT-LOGS for the full pattern). Landed: `/api/live-order` auth-bypass mount-order fix; client-IP-spoofing fix on `admin.ts`/`rate-limit.ts` via new `relay-client-ip.ts` + relay-side `ip-sanitize.js` header strip+stamp; `broker-router.ts` fail-soft `firm_id` kill-switch prefetch; monthly n8n-cron ET-guard fix (`Intl.DateTimeFormat` locale-parsing bug, new `monthly-cron-et-guard.ts`); atomic consecutive-failure upsert in `trade-critique-service.ts`; `strategies.ts`/`backtests.ts` cascade-delete transaction + 17 previously-missing FK tables + `deriveSymbolSync()`; a 19-sibling-file migration `IF NOT EXISTS` hardening sweep (72 statements) plus 4 newly-found forward-reference/grammar bugs (`0003`, `0038a`, `0068`, `0104`, `0119`); **`KNOWN_OUT_OF_BAND_APPLIED_HASHES` emptied from 10 entries to 0** in `migration-journal-utils.ts` (all 10 formerly-backfilled migrations proven genuinely idempotent via a new 2-pass fresh-bootstrap PGlite replay test with an adversarial fixture-injection self-check) — production implication: the next real boot-migration run executes those 10 migrations' real DDL for the first time in that environment, expected no-op against an already-migrated schema. **A Phase-6 independent re-certification (mandatory doer≠grader re-scan, not the in-wave "safe to push" self-report) caught a CRITICAL regression the wave itself introduced**: the relay-IP fix's unconditional `x-forwarded-for` strip silently defeated `office-control-guard.ts`'s Office-only privilege-separation heuristic, granting pipeline-control/strategy-deploy/compliance-mutation authority to any relay-tunneled caller holding the shared Bearer API_KEY, no admin cookie required — set the wave's overall band to 3 despite 8 of 9 individual items independently verifying at band 6-8. **Verified never live in production**: Railway's `tf-relay` auto-deploys from `main`, not `hardening/phase-0`, and `main` never contained the vulnerable commit at all (`git merge-base --is-ancestor` confirmed) — a git-branch-only exposure window, closed before any merge could have shipped it; no operator deploy/restart/rotation action needed. Fixed same session (commit `7a92ff77`, keying the guard on presence of the relay's own `x-relay-verified-ip` header) and independently re-certified VERIFIED BAND 8 by a second from-zero accuracy-validator pass **scoped to that one commit** — the wave's overall band was not re-derived after the fix and should not be read as "now an 8." **Track B: 11 instrument-code findings (sizing/gate/classifier/measurement-harness logic) recorded, HELD, zero code written — these are one-line findings, NOT yet full 5-part ratify packets; packet authorship is itself a carry-forward, and none may proceed on wave momentum regardless.** 2 items flagged for operator PRODUCT decision, not code fixes: `runPromptEvolution()` dead-code disposition; MFFU collaborative-trading gate's warn-only (not hard-block) status. Full findings ledger + production-liveness evidence: AGENT-LOGS entry this date + memory `project_full_goal_deepscan_2026_07_10`.

---

---

## Wave 26 Pass G Pass B — audit-action canonical additions

> Moved from CLAUDE.md §10b during the 2026-08-18 token-optimization pass.

### Audit-action canonical additions (Wave 26 Pass G Pass B — 2026-05-26)
The graduation hot-path gained 4 new audit-action namespaces. Future agents should treat these as part of the canonical action vocabulary:
- `graduation.rejected_incomplete_bidirectional` — Gate 1 reject (graduator-side)
- `graduation.bidirectional_incomplete_rejected` — Gate 1 (helper-side; helper-only callers — graduator path suppresses via `skipAuditRow:true`)
- `graduation.factor_quality_classified` — Gate 2 telemetry (helper-owned; every graduation + backfill rows tagged `backfill:true`)
- `graduation.thin_confluence_warning` — Gate 3 advisory (graduator-side)
- `extraction.parity_test_run` — B1 5-fixture parity-test hook
