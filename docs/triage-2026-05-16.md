# Wave 0 Triage Report — 2026-05-16

**Agent:** observability-reliability
**Purpose:** Read-only ground-truth capture before Wave 1 begins. No code edits.
**Captured at:** 2026-05-17T01:42Z (UTC)

---

## 1. Forcing Functions

| Signal | Status | Evidence |
|---|---|---|
| **system-map** | RED | Exit 0 but `status:"drift"` — 2 driftItems present (see CI Hard Gates) |
| **production-isolation** | GREEN | `CLEAN — 4 file(s) checked, 0 violations` — exit 0 |
| **2026-compliance** | GREEN | `OK — MFFU + Topstep aligned with canonical 2026 docs` — exit 0 |
| **n8n-drift (audit:n8n)** | UNKNOWN | Script hardcoded to `N8N_API_URL=http://localhost:5678` (stale Windows user-level env var overrides dotenv). Script exits 0 even on `fetch failed`. Real drift was audited manually via Railway API — see Section 5. |
| **Validation Cadence** | RED | Panel score 20/100. `daysSinceLastLiveBacktest.isRed: true` (null — never run). `strategiesThisMonth.count: 0`. Reality check score: 20 (fidelity only). |
| **AGENT-LOGS write mandate** | GREEN | This session log is being written. |

---

## 2. CI Hard Gates

| Script | Exit Code | Output |
|---|---|---|
| `npm run check:production-isolation` | 0 (PASS) | `CLEAN — 4 file(s) checked, 0 violations` |
| `npm run check:2026-compliance` | 0 (PASS) | `OK — MFFU + Topstep aligned with canonical 2026 docs` |
| `npm run system-map:check` | 0 (exit 0) but RED | `status: "drift"` — driftItems: `["Generated topology section is stale relative to the current repo state", "Registry is missing 2 scheduler job mappings"]` |
| `npm run audit:n8n` | 0 (misleading) | BROKEN — script connects to `http://localhost:5678` (stale Windows user env `N8N_API_URL` shadows dotenv). `fetch failed` → ECONNREFUSED. Script swallows the error and exits 0. See Section 8 blockers. |

**System-map snapshot (as of triage):**
- Routes: 62
- Scheduler jobs: 77 (registry missing 2 mappings)
- Workflow files on disk: 28 (canonical count should be 29-30)
- Engine subsystems: 25
- Database tables: 92
- Registry subsystems: 21

---

## 3. Test Baseline (Vitest — BEFORE Wave 1)

**Invocation:** `npx vitest run`
**Duration:** ~8.8s

| Metric | Count |
|---|---|
| Test Files: PASS | 134 |
| Test Files: FAIL | 7 |
| Test Files: SKIP | 3 |
| Tests: PASS | 2177 |
| Tests: FAIL | 19 |
| Tests: SKIP | 37 |
| **Total tests** | **2233** |

**Failing suites (19 failures across 7 files):**

| File | Failures | Root cause |
|---|---|---|
| `scout-pending-endpoint.test.ts` | 13 | Tests returning HTTP 422 instead of 201 — likely a schema validation or Zod enum mismatch on the `/api/agent/scout-ideas/pending` route (route accepts `apify`, `tf_search` etc. but something changed) |
| `scout-extract.test.ts` | 3 | `non_json` error path + ES/NQ/CL micro-only filter — scout-extract route contract changed or mocked behavior drifted |
| `health-signals.test.ts` | 1 | Test expects `n8n.status='disabled'` when `N8N_BASE_URL` unset — but test env picks up `N8N_BASE_URL` from process env |
| `kb-loader.test.ts` | 1 | Expects `transcript_extractor` role to have `qwen3-coder:30b` fallback — model was swapped to `qwen2.5-coder:7b` in Pass 21 but test fixture not updated |
| `production-convergence.test.ts` | 1 | 0A health monitor test — likely JSON fixture drift vs current workflow shape |
| `production-status.test.ts` | 0 listed (suite FAIL) | Suite-level failure — setup/teardown issue |
| `strategy-production-check.test.ts` | 0 listed (suite FAIL) | Suite-level failure — setup/teardown issue |

**Note:** Per AGENT-LOGS (2026-05-11 Pass 20 Track K): "Pre-existing 15 test failures (scout-pending-endpoint, production-status, scout-extract, strategy-production-check) need investigation in a dedicated session." Current count is 19 failures — 4 more than the baseline of 15 noted then.

---

## 4. Migration Ledger

**Railway Postgres state:**
- Drizzle migrations table columns: `id, hash, created_at` (no `migration_name` column — hash-based tracking)
- Total migrations applied (rows in `drizzle.__drizzle_migrations`): **70**
- Max migration ID: **97**

**Local journal (`meta/_journal.json`) state:**
- Total journal entries: **104** (idx 0–103)
- Highest idx in journal: **103** (`0103_cross_source_validation`)

**Filesystem SQL files:** 109 total (excluding `.down.sql`)
- Last 3 on disk: `0103_cross_source_validation.sql`, `0104_concept_fingerprint.sql`, `0105_wide_fingerprint.sql`

**Status of 0104 and 0105:**

| Migration | In Journal | In DB (applied) | Columns verified in DB |
|---|---|---|---|
| `0104_concept_fingerprint.sql` | NO (not in `_journal.json`) | APPLIED (columns exist) | `concept_name`, `layer_coverage_json` present in `strategy_pending_buckets` |
| `0105_wide_fingerprint.sql` | NO (not in `_journal.json`) | APPLIED (columns exist) | `wide_fingerprint_hash` present in `strategy_pending_buckets` |

**Assessment:** Both 0104 and 0105 were applied to Railway Postgres directly (schema DDL confirmed by column inspection), but the Drizzle journal file was never updated to record them. Drizzle's migration tracker is **out of sync** with the actual DB. Running `npm run db:migrate` could cause issues because Drizzle may try to re-apply them or fail due to hash mismatch. This must be resolved in Wave 3 before any new migration is added.

---

## 5. n8n Inventory

**Source:** Railway n8n API `/api/v1/workflows?active=true` + `/api/v1/executions?limit=100`
**Active workflow count:** **29** (canonical target is 30 — one workflow is missing or was deactivated)

**Missing workflow:** Comparing Pass 21 records (30 → Tavily decommission → 29 active). Pass 21 deactivated+deleted `5I-tavily-scout`. The canonical count should now be 29, but CLAUDE.md §15 still says "30 active workflows." CLAUDE.md needs updating.

| # | Workflow ID | Name | Last Fired | Last Status | Stale? |
|---|---|---|---|---|---|
| 1 | `2Dt49I5ZfMfnnWBw` | 5A-weekly-tournament | Not in last 84 execs | UNKNOWN | YES — not seen in last 7 days |
| 2 | `4vENMZrmtcubvD4Z` | Nightly Self-Correction | 2026-05-16T03:00Z | success | NO |
| 3 | `8m80dUaH5gAubR4T` | Daily Portfolio Monitor | 2026-05-15T21:00Z | success | NO |
| 4 | `9pcJoQ5mPM2TG1C3` | 11A-critic-optimization | 2026-05-16T07:00Z | success | NO |
| 5 | `9poOSNOCuWsJQidZ` | 5G-brave-search-scout | 2026-05-16T22:00Z | success | NO |
| 6 | `EmQcMQqV5UE7Hlb4` | Weekly Compliance Re-Parse | Not in last 84 execs | UNKNOWN | POSSIBLY (weekly schedule) |
| 7 | `ILILGYZLKMGJFb0T` | 7A-auto-evolution | 2026-05-16T07:00Z | **error** | NO (firing but ERRORING) |
| 8 | `MRIUobBbHwcCZv8k` | 5P-nemo-scenario-generator | Not in last 84 execs | UNKNOWN | YES — not seen in last 7 days |
| 9 | `PlX9TmXAkIoRRKTO` | 5H-reddit-scout | 2026-05-16T22:30Z | success | NO |
| 10 | `RTtmSrB1In5jYlAF` | Strategy Deep Analysis Pipeline | 2026-05-12T22:39Z | **error** | YES — last 4 execs all errors (smoke tests) |
| 11 | `UhAvoHwWdrgxyAod` | 10A-master-orchestration | 2026-05-16T09:00Z | **error** | NO (firing but ERRORING) |
| 12 | `XCuWv1w463P8FIWa` | Strategy Tournament | 2026-05-16T10:00Z | success | NO |
| 13 | `ZUq9UufuWh5gZJi2` | Nightly Strategy Research Loop | 2026-05-15T06:00Z | **error** | NO (firing but ERRORING) |
| 14 | `cF1ZuhfdSEev0C4i` | Strategy Generation Loop | Not in last 84 execs | UNKNOWN | POSSIBLY (cron schedule dependent) |
| 15 | `e7M2IFn9erCPspu2` | 8B-source-quality-review | Not in last 84 execs | UNKNOWN | POSSIBLY |
| 16 | `eRImAsypo4Ivmi8f` | 8A-idea-to-strategy | 2026-05-16T23:30Z | success | NO |
| 17 | `fCwCHrq0zXYtbTox` | 3A-workflow-backup | 2026-05-16T07:00Z | success | NO |
| 18 | `fNueuLzzqFYhG0m1` | Monthly Robustness Check | Not in last 84 execs | UNKNOWN | monthly — not stale yet |
| 19 | `gz4wg79gCfFbPOoB` | Macro Data Sync - Evening | 2026-05-16T23:00Z | success | NO |
| 20 | `hiGZoK75NTUeSfrr` | 6D-compliance-gate | 2026-05-16T08:00Z | **error** | NO (firing but ERRORING) |
| 21 | `hyghBN13CJi02vK4` | Daily Compliance Check | 2026-05-15T13:00Z | success | NO (last fired yesterday — missed today's window) |
| 22 | `hzoWiVeKdhXSI31v` | Weekly Strategy Hunt | 2026-05-16T12:00Z | **error** | NO (firing but ERRORING) |
| 23 | `iTftiIkCZndPXVt3` | 0A-health-monitor | Not in last 84 execs | UNKNOWN | POSSIBLY (health monitor fires on error trigger) |
| 24 | `j2L7VguuQeem8Vez` | Anti-Setup Refresh | Not in last 84 execs | UNKNOWN | POSSIBLY |
| 25 | `m44VoQzNljYr4Zl4` | Post-Session Skip Review | 2026-05-15T21:30Z | success | NO |
| 26 | `oJj0gDj4Ft4nMb3x` | Pre-Session Compliance Gate | 2026-05-15T13:15Z | success | NO |
| 27 | `wL2GrvUVHvpVL5Pi` | Macro Data Sync - Morning | 2026-05-16T11:00Z | **error** | NO (firing but ERRORING) |
| 28 | `xRCVObu8NsmnTowY` | Pre-Session Skip Check | 2026-05-15T13:00Z | success | NO |
| 29 | `ztsYJXUvAC11Ay9E` | 9A-nightly-self-critique | 2026-05-16T06:00Z | success | NO |

**Workflows with persistent errors (firing but failing on every run):**

| Workflow | Error pattern | Notes |
|---|---|---|
| `7A-auto-evolution` | error every daily at 07:00 | Firing since at least 2026-05-13; root cause unknown without execution detail |
| `10A-master-orchestration` | error every daily at 09:00 | Same pattern — needs root-cause investigation |
| `6D-compliance-gate` | error every daily at 08:00 | Same pattern |
| `Weekly Strategy Hunt` | error on 2026-05-16 weekly run | First error seen for weekly |
| `Nightly Strategy Research Loop` | error every nightly at 06:00 | Consistent errors |
| `Macro Data Sync - Morning` | error every daily at 11:00 | Consistent |

**Pass 21 carry-forward cross-reference:**

| Pass 21 carry-forward | Status |
|---|---|
| "Watch next 24-72h cron firings of rewritten workflows" | UNVERIFIED — several workflows show persistent errors but root cause not checked |
| "Strategy_Deep_Analysis_Pipeline 400 from downstream /api/backtests/:id" | STILL-PENDING — last 4 executions 2026-05-12 all errors |
| "Wait first cron firings of rewritten workflows on Railway" | PARTIALLY VERIFIED — many fire, but 6 workflows error consistently |

---

## 6. Tower Process Health

**pm2 status probe:** pm2 is installed as a PowerShell script (`pm2.ps1`) and consistently backgrounds in the Claude Code execution environment — process output cannot be captured directly. Alternative evidence used:

**Evidence via backend health endpoint (`GET http://localhost:4000/api/health`):**
- `status: "ok"` — trading-forge-api is confirmed **ONLINE** on :4000
- `database.status: "ok"` with `latencyMs: 66`
- `ollama.status: "ok"`
- `python.status: "ok"` (Python 3.13.13)
- `n8n.status: "ok"` with `latencyMs: 89`
- `scheduler` section shows 70+ registered cron jobs with recent `lastRun` timestamps

**Evidence via relay health endpoint (`GET https://tf-relay-production.up.railway.app/__relay/health`):**
- Response: `{"ok":true,"tower":true,"pending":1}` — `tower: true` confirms tower-relay-client is **ONLINE** and connected

**Prior verification (AGENT-LOGS 2026-05-12):** `pm2 status` confirmed all 4 processes online:
- `trading-forge-api` — online (confirmed via :4000 health)
- `tower-relay-client` — online (confirmed via relay health `tower:true`)
- `openclaw-gateway` — status UNKNOWN (no independent probe available without pm2 output; CLAUDE.md §15 notes `/__ocg/health` → `{"ok":true,"status":"live"}` when online)
- `discord-bot` — status UNKNOWN (no independent probe; Discord critical-alerts channel activity implied by scheduled heartbeats)

**Assessment:** `trading-forge-api` and `tower-relay-client` are confirmed ONLINE. `openclaw-gateway` and `discord-bot` are UNKNOWN (pm2 output inaccessible in current shell environment).

---

## 7. Relay Health

| Probe | URL | Result | Status |
|---|---|---|---|
| Relay health | `https://tf-relay-production.up.railway.app/__relay/health` | `{"ok":true,"tower":true,"pending":1}` | GREEN |
| OpenAI proxy usage (through relay) | `https://tf-relay-production.up.railway.app/api/openai-proxy/usage` | HTTP 200, `{"date":"2026-05-17","tokensUsed":143974,"dailyBudget":3000000,"percentUsed":4.8,...,"cache":{"cacheHitRatio":86.5}}` | GREEN |

Both relay directions confirmed healthy. Tower-to-Railway WS link is live (pending:1 in-flight request at capture time). OpenAI budget at 4.8% used for the day, 86.5% cache hit ratio.

---

## 8. Wave 1 Blockers

### RED signals blocking or requiring attention before/during Wave 1:

**BLOCKER 1 — Validation Cadence is RED (score 20/100)**
- `daysSinceLastLiveBacktest: null` (no backtests ever run in this environment)
- `strategiesThisMonth.count: 0`
- This is expected and acceptable per plan: Waves 1–5 ARE the cadence remediation. Not a scope change.
- Wave 1 (correlation_id + commissions + mini-guard) does not satisfy cadence on its own; Wave 5 end-to-end smoke run will.

**BLOCKER 2 — 19 failing tests (pre-existing + model-swap drift)**
- 15 failures were pre-existing as of 2026-05-11 (noted in AGENT-LOGS).
- 4 additional failures introduced since then (kb-loader: qwen3-coder:30b fallback test not updated after model swap).
- Wave 1 must not regress test count below current 2177 passing. Any Wave 1 work must confirm tests at or above this baseline.
- Specifically: `kb-loader.test.ts` failure (`transcript_extractor qwen3-coder:30b fallback`) is a Wave 1 adjacent item — model-swap from Pass 21 left a test fixture un-updated.

**BLOCKER 3 — `npm run audit:n8n` is non-functional**
- Script reads `N8N_API_URL` (stale Windows user-level env var set to `http://localhost:5678`) which overrides the `.env` `N8N_BASE_URL` value.
- Script exits 0 on connection failure — CI gate gives false green.
- Wave 3 must fix: either remove the stale Windows env var or add `N8N_API_URL=https://n8n-production-84ff.up.railway.app` to `.env`.

**BLOCKER 4 — system-map:check exits 0 but reports drift**
- 2 driftItems: stale topology section + 2 missing scheduler job mappings.
- Not a hard blocker for Wave 1 (check exits 0) but the drift must be cleared via `npm run system-map:sync` at end of Wave 1 per plan.

**BLOCKER 5 — Migration journal out of sync (0104, 0105)**
- Both migrations applied in DB but not recorded in `_journal.json`.
- Drizzle may try to re-apply or skip depending on hash comparison.
- Wave 3 must resolve before any new migration is added. Do NOT run `npm run db:migrate` until journal is reconciled.

**BLOCKER 6 — 6 n8n workflows erroring on every scheduled run**
- `7A-auto-evolution`, `10A-master-orchestration`, `6D-compliance-gate`, `Nightly Strategy Research Loop`, `Macro Data Sync - Morning`, `Weekly Strategy Hunt` all show consistent errors.
- Root cause not confirmed at Wave 0 (would require pulling execution detail per workflow — out of scope for read-only triage).
- Wave 3 (n8n sweep) must investigate.

**NOT a blocker (noted for completeness):**
- `5A-weekly-tournament`, `5P-nemo-scenario-generator` not seen in last 84 executions — their schedules (weekly/less frequent) may simply not have fired yet.
- Relay `pending: 1` — normal in-flight request, not a concern.

---

## 9. Carry-Forward Verification Status

Source: AGENT-LOGS.md, last 8-10 session entries (sessions from 2026-05-11 through 2026-05-12 Pass 21).

### From Pass 21 — n8n Robustness Sweep (2026-05-12)
| Carry-forward | Status |
|---|---|
| Watch next 24-72h cron firings to confirm 21 unfired workflows run cleanly | UNVERIFIED — 6 workflows erroring consistently; individual root causes not checked |
| Strategy_Deep_Analysis_Pipeline: verify with real backtestId once production data available | STILL-PENDING — last executions (2026-05-12) all errors with fake IDs |
| Consider importing proper ZZ error sink to Railway | STILL-PENDING (low priority; current setup works) |

### From Pass 21 — 4-Layer Defense (2026-05-12)
| Carry-forward | Status |
|---|---|
| First drift-check cron fires at 06:00 ET tomorrow | VERIFIED — `graduated-strategy-drift-check` shows in scheduler health with recent lastRun |
| L1 gate tested live when a bucket graduates | UNVERIFIED (no graduation events since deployment confirmed) |

### From Pass 21 — Discord Alert Triage (2026-05-12)
| Carry-forward | Status |
|---|---|
| Verify pm2 auto-resurrects on Windows reboot | UNVERIFIED — no reboot since last session observed |
| Watch Discord for text-embedding-3-small ramble (should be gone) | UNVERIFIED — no Discord logs available in this session |
| Wait first cron firings of rewritten workflows | PARTIALLY VERIFIED — most fire, but 6 workflows error consistently |
| Optional: run `npm run system-map:sync` to fold pm2/model inventory into System Map | STILL-PENDING — system-map:check shows drift; sync not run since session |

### From Pass 21 — LLM Stack Right-Sized (2026-05-12)
| Carry-forward | Status |
|---|---|
| Verify pm2 auto-resurrects on Windows reboot | UNVERIFIED |
| Watch Discord for text-embedding-3-small ramble | UNVERIFIED |
| Wait first cron firings, confirm Railway→relay→tower→qwen2.5-coder:7b chain | PARTIALLY VERIFIED — relay is live (health green), but 6 workflows error means chain completion unconfirmed |
| Optional: `npm run system-map:sync` | STILL-PENDING |

### From Pass 21 — Stable Railway Relay (2026-05-12)
| Carry-forward | Status |
|---|---|
| Verify first real Railway→tower webhook callback | VERIFIED — relay health `tower:true`, openai-proxy/usage returns live data |
| Run `npm run system-map:sync` to fold tf-relay into System Map | STILL-PENDING (system-map shows drift) |
| Optional: rotate RELAY_TOKEN after incident | STILL-PENDING |

### From Pass 21 — Framework Overlay + Autonomous Scout (2026-05-11)
| Carry-forward | Status |
|---|---|
| Watch first organic 3-layer graduation from autonomous cycle | UNVERIFIED — no DB query run to check current bucket state |
| Consider deprecating 5R/5P/5Q once autonomous cron runs multiple clean cycles | STILL-PENDING |

### From Pass 20 Track K — 3 Observability Bugs (2026-05-11)
| Carry-forward | Status |
|---|---|
| Pre-existing 15 test failures need dedicated investigation | STILL-PENDING (now 19 failures) |

---

## 10. Environment Notes

- `psql` is NOT installed on the Windows tower PATH. Migration ledger was queried via Node.js with the `postgres` npm package directly.
- `pm2` on this system is a PowerShell script (`pm2.ps1`) — calling it from within the Claude Code execution environment consistently backgrounds the process. Tower health was confirmed via the backend API and relay health probe instead.
- `N8N_API_URL` is set at Windows user-level environment (to `http://localhost:5678` — stale from before Railway migration). This silently overrides `.env` `N8N_BASE_URL` for any script using `process.env.N8N_API_URL`. Must be cleared or overridden in `.env` as `N8N_API_URL=https://n8n-production-84ff.up.railway.app`.
- Vitest exit code was 1 (non-zero due to test failures) even though `npm test` exit code appears as 0 in some invocations — this is a shell capturing artifact. Confirmed 19 failures via direct output.
