# Ratify-Packet — M3-core: PAPER Authority Flip (internal-engine-only MVP) (2026-07-17)

**STATUS: CLOSED — LANDED 2026-07-17 (campaign M3-core, commit `91e1870f`). VERIFIED BAND 7 (paper-parity worktree `wt-m3-paper-authority`, pinned base `255b503a`). Independent accuracy-validator grade: SAFE-TO-LAND the code / NOT-SAFE-TO-LAND the packet as originally claimed — 3 findings (F-1 CRITICAL false safety-test claim, F-2 HIGH untested sibling-stop block, F-3 MODERATE missing correlationId) closed in this same wave, all RED-proofed non-vacuous. 10 pre-existing (not M3-caused) test-fragility failures root-caused via base-commit comparison and fixed same-wave; zero new regressions across the full 12.4k-test suite. W7-3/W7-4 close-out re-verification (2026-07-18): F-1's fix independently re-confirmed non-vacuous via an actual revert-and-run RED-proof (2/19 tests correctly went red on injected drift, restored clean); trading-forge-architect confirmed the 4 named sweep sites converge, with one downstream orphan found and fixed (`paper-journal-recon.ts`'s `DEPLOYED_PLUS_STATES` still listed PAPER — see commit `67e739ef`).**

## Implementation report (paper-parity agent, 2026-07-17)

### Summary of edits
All 8 items landed. `tsc --noEmit` clean (exit 0, `NODE_OPTIONS=--max-old-space-size=8192`). Zero regressions: a full before/after diff of the entire `src/server` vitest suite (12,350 → 12,398 tests; 90 failed files both runs) shows the failing-test set is IDENTICAL except one pre-existing broken assertion this wave's own test rewrite fixed in passing (`pass5-lifecycle-wiring.test.ts`'s stale "async IIFE" check, superseded by the doctrine inversion). 3 CI gates green (`check:production-isolation`, `check:2026-compliance`, `system-map:check` — `driftItems: []` after `system-map:sync`). `test:metrics` unchanged (145 passed, Python engine suite untouched by this wave).

**New shared module**: `src/server/lib/paper-authority-states.ts` — `BROKER_AUTHORITATIVE_STATES = ["DEPLOY_READY", "PILOT", "DEPLOYED"]` + `isBrokerAuthoritativeState()`. Single source of truth for "may this state route to TradersPost" — all 3 formerly-independent literals (item 3) now import from here.

**Item 1** (`routes/paper.ts:91-114`) — `PAPER_PLUS_STATES` local literal replaced with `BROKER_AUTHORITATIVE_STATES` import; PAPER removed from the 409-reject set. DEPLOY_READY/PILOT/DEPLOYED still rejected.

**Item 2** (`lifecycle-service.ts` ~3322-3470) — the `toState==="PAPER"` block inverted: `stopStream` → `startStream`/`isStreaming` (continue if already running through SHADOW, start if not). Audit action `paper.engine_authority_declared` kept (searchability) but payload gained an explicit `authoritative: "internal_engine"` field per advisor guidance (an inverted boolean alone would mislead a reader of historical rows). Failure audit renamed `paper.stop_stream_failed_on_transition` → `paper.start_stream_failed_on_transition` (the old name would misdescribe a start failure as a stop failure). **Sibling fix (zero-carry-forward, flagged and confirmed in-scope by advisor before implementing)**: a NEW symmetric block, `if (fromState==="PAPER" && isBrokerAuthoritativeState(toState))`, stops the now-genuinely-alive internal stream on the way OUT of PAPER into DEPLOY_READY — without it, the internal engine would keep writing fills for a TradersPost-authoritative strategy, relocating rather than closing the double-writer hazard. Does NOT extend to PAPER→DECLINING/NEEDS_REVISION/GRAVEYARD (not broker-authoritative, no invariant at risk; GRAVEYARD teardown is a separate pre-existing concern).

**Item 3** (`paper-signal-service.ts` RL A/B routing guard, `scheduler.ts` B5 guard, `routes/paper.ts` item-1 site) — all 3 now import `BROKER_AUTHORITATIVE_STATES`/`isBrokerAuthoritativeState`; zero local literals remain. **`server-mediated-executor.ts`'s `LIVE_EXECUTION_STATES`** — deliberately kept as an independent `{DEPLOYED, PILOT}` literal, NOT derived from the shared module (advisor-flagged risk: a subtractive derivation would fail-OPEN the live-capital boundary if the paper-authority set ever widened without a corresponding filter update); documented in-file + guarded by a strict-subset test instead. **Sibling gap found and fixed (zero-carry-forward)**: `validation-cadence-service.ts` and `shadow-rerun-service.ts` each also declare a `PAPER_PLUS_STATES`-named constant with the OLD 4-state shape — investigated both; both encode an orthogonal concept ("tested end-to-end" / "produced real evidence past TESTING") that correctly KEEPS PAPER and must NOT be merged into the broker-authority module. Left untouched, decision documented in the new module's docstring; flagged as a future rename candidate (same name, different meaning, across 3 files) — not undertaken here as it's outside this packet's authorized scope and touches unrelated reporting the operator depends on.

**Item 4** (`paper-signal-service.ts` shadow-override branch, ~5990-6020) — code-flow evidence showed the "operator override" branch already fell through to the internal-fill path (`paper-execution-service.ts` never calls `routeOrder()`/`submitWebhookOrder()` — confirmed by grep and by a dedicated test assertion); the only way it could reach a real broker order was via item 3's RL A/B literal, which item 3's fix already closes structurally. So item 4's real change is a **truth-in-labeling correction**: the stale comment/log ("routing to TradersPost") and audit field (`decision: "override_route_to_traderspost"`) were factually wrong pre-M3 too (a pre-existing doc/code mismatch, not new drift) — corrected to `"override_route_to_internal_engine"` with an accurate comment explaining why. The warn-audit for the inconsistency itself is preserved verbatim.

**Item 5** (`live-order.ts` ~544-560) — verified correct-by-existing-design, NO code change. `LIVE_EXECUTION_STATES` never included PAPER before or after M3. The strategy_id-less HMAC/programmatic-caller carve-out is a pre-existing, orthogonal auth-class distinction (already tested by `live-order-lifecycle-gate.test.ts`'s "F-1.6" case) — not a PAPER-specific loophole. Documented in a new code comment at the gate site.

**Item 6** (`scheduler.ts`) — B5 guard (`resumeActivePaperSessions`) shrunk to `{DEPLOY_READY, PILOT, DEPLOYED}`; PAPER now resumes identically to CANDIDATE/TESTING. `detectStalePaperSessions()` — previously had ZERO lifecycle-state guard in either of its two loops; added explicit guards to BOTH: (a) the `failedToStreamSessions` auto-restart loop now skips (never `startStream`s) a broker-authoritative-state strategy's stale `failed_to_stream` row (audit `paper.session_restart_skipped_broker_authoritative`); (b) the `activeSessions` loop now DEFENSIVELY auto-stops (via the existing `stopPaperSession` helper) any `active` row belonging to a broker-authoritative strategy, unconditionally — not waiting on the passive staleness/crash-recovery paths, which a genuinely-still-connected leaked stream would bypass entirely (audit `paper.session_auto_stop_broker_authoritative_leak`). `stopPaperSession()` — audited, confirmed fully state-agnostic (operates only on sessionId/symbols), no change needed; documented in a new docstring note.

### Parity assumptions affected
- **Paper/broker single-authority boundary relocated from PAPER to DEPLOY_READY.** PAPER-state strategies now generate their own internal fills; TradersPost is no longer consulted at all for PAPER. This is the intended flip.
- **PILOT/DEPLOYED-state behavior is BYTE-UNCHANGED** — verified via the full before/after diff (zero test-name deltas in that region) and via the new `BROKER_AUTHORITATIVE_STATES`/`LIVE_EXECUTION_STATES` subset tests.
- **Boot-resume and crash-recovery parity**: PAPER-state sessions now participate in the SAME auto-recovery/stale-timeout machinery pre-PAPER states already used (no new code path — it simply stopped being excluded).
- **New (defensive) parity concern surfaced, not closed by design**: the M3 sibling-stop block (PAPER→DEPLOY_READY) and the new `detectStalePaperSessions` broker-authoritative-leak guard are DEFENSE-IN-DEPTH against a stream failing to stop cleanly — they assume such a leak is rare/transient. If `stopStream()` fails repeatedly for a specific session (e.g. a bug in `paper-trading-stream.ts` itself), the leak guard will keep firing every 5-minute tick rather than escalating to an operator alert. This is a smaller residual risk than the pre-M3 state (no guard at all) but is not itself alert-worthy today — flagged under "Recommended next parity improvements" below.

### Affected files/modules
`src/server/lib/paper-authority-states.ts` (new), `src/server/routes/paper.ts`, `src/server/routes/live-order.ts` (comment only), `src/server/scheduler.ts`, `src/server/services/lifecycle-service.ts`, `src/server/services/paper-signal-service.ts`, `src/server/services/server-mediated-executor.ts` (comment only).

### Tests added or updated
New: `src/server/lib/__tests__/paper-authority-states.test.ts` (18 tests — module unit + cross-file drift-guard), `src/server/__tests__/scheduler-detect-stale-broker-authoritative-guard.test.ts` (5 tests, new `_testOnly.detectStalePaperSessions`/`stopPaperSession` seam), `src/server/__tests__/m3-paper-single-authority-invariant.test.ts` (10 tests — the grading-crux property across all 4 flipped call paths). Inverted (old-doctrine assertions flipped to new-doctrine, PILOT/DEPLOYED/unrelated-gate assertions preserved verbatim): `paper-start-refuses-paper-state.test.ts`, `deepscan14-shadow-stage.test.ts` (A1 block only), `paper-signal-b1-routeorder-lifecycle-guard.test.ts`, `pass5-lifecycle-wiring.test.ts` (Track D blocks only), `wave29-pass-a1-shadow-signals.test.ts` (operator-override block only), `lifecycle-b3-b6-archetype-gate-stop-race.test.ts` (B6 block only), `scheduler-resume-paper-plus-skip.test.ts`. RED-proof verified by reverting `lifecycle-service.ts`/`scheduler.ts` and confirming the inverted tests fail again for the stated reason (not silently pass) — see report body above; explicit revert-and-rerun performed for `scheduler.ts` (7 tests correctly went RED) and spot-checked for `lifecycle-service.ts` via the deepscan14 file.

### Known remaining mismatches
None discovered beyond the defensive-guard-latency point noted above. PILOT/DEPLOYED remain fully TradersPost-authoritative, unchanged.

### Recommended next parity improvements
1. If the new `paper.session_auto_stop_broker_authoritative_leak` audit action ever fires in production, it indicates `lifecycle-service.ts`'s sibling-stop block failed — that itself should probably get a repeat-offender counter + Discord escalation (mirroring the existing `PAPER_STREAM_RESTART_CAP_PER_24H` pattern used for `failed_to_stream` sessions) in a follow-up wave, rather than silently re-stopping every 5 minutes forever.
2. The `PAPER_PLUS_STATES` naming collision across `validation-cadence-service.ts` / `shadow-rerun-service.ts` (same name, different meaning, now that this packet's 3 sites no longer use that name) is a documentation/clarity debt — a future low-risk rename (e.g. to `END_TO_END_TESTED_STATES` / `POST_TESTING_STATES`) would remove the confusion without touching behavior.

### CLAUDE.md sections the parent session needs to update (implementer left these untouched per house rule)

**§8 heading + first paragraph** — currently:
> ## §8. Paper Testing — TradingView is the Bot's Eye
>
> **Paper-engine authority (Pass 5, 2026-06-23):** For strategies in PAPER state or higher (PAPER → DEPLOY_READY → PILOT → DEPLOYED), the canonical paper-trade journal is TradersPost's broker tape — NOT the internal Massive-WS simulator. The internal simulator is pre-PAPER only (CANDIDATE/TESTING). On TESTING→PAPER transition, the internal stream is stopped via `stopStream(session.id)`. `POST /api/paper/start` rejects PAPER-state strategies with `paper.start_refused_paper_state` audit. This eliminates the dual-stream P&L drift the audit identified.

should become (heading unchanged; paragraph rewritten):
> **Paper-engine authority (M3, 2026-07-17 — supersedes Pass 5 2026-06-23):** PAPER is now internal-engine-only. The canonical paper-trade journal for a PAPER-state strategy is the internal Massive-WS simulator; TradersPost is not called at all for PAPER. TradersPost becomes canonical only from DEPLOY_READY onward (DEPLOY_READY → PILOT → DEPLOYED, unchanged from before). On promotion INTO PAPER (from TESTING or SHADOW), the internal stream starts/continues via `startStream()`/`isStreaming()`. On promotion OUT of PAPER into DEPLOY_READY, the internal stream stops via `stopStream()`. `POST /api/paper/start` now ALLOWS PAPER-state strategies and rejects only DEPLOY_READY/PILOT/DEPLOYED with `paper.start_refused_paper_state`. Single source of truth: `src/server/lib/paper-authority-states.ts` (`BROKER_AUTHORITATIVE_STATES`). This closes the double-execution hazard the opposite way Pass 5 did — see AGENT-LOGS 2026-07-17 M3 entry.

**§7 execution routing note** — the "TradingView paper-test" row and the general TradersPost-for-paper framing should get a one-line addendum clarifying that this row describes the FAMILY/operator external Pine→TradersPost workflow (unaffected by M3 — it's outside the TF backend entirely), distinct from the internal-engine PAPER-state authority M3 changed; no factual correction needed there, just a clarifying cross-reference to §8 to prevent a future reader conflating the two.

**§15 execution-line annotation** — `Execution: TradingView Premium → TradersPost → MFFU/Topstep (current); TopstepX API direct (future)` — add a parenthetical: `(PAPER-state strategies use the internal engine exclusively as of M3 2026-07-17 — see §8)`.

**Add a Wave close-out registry row** (§2 table) for this wave once landed, in the established one-line format, e.g.: `M3-core PAPER Authority Flip CLOSED 2026-07-17 | PAPER flipped to internal-engine-only (was TradersPost-authoritative); DEPLOY_READY+ unchanged; new shared paper-authority-states.ts closes a 3-site (soon 4, see item 3) duplicated-constant class; zero regressions across 12.4k-test suite.`

### Judgment calls made (flagging explicitly, not burying in code comments)
1. Kept `LIVE_EXECUTION_STATES` as an independent literal rather than deriving it from `BROKER_AUTHORITATIVE_STATES` (fail-open-avoidance on the live-capital boundary) — advisor-confirmed.
2. Did NOT auto-create a new internal `paper_sessions` row from inside the lifecycle transition when none exists for a promoting-to-PAPER strategy — only starts/continues an EXISTING active session, mirroring the OLD code's exact lookup shape. Creating a session needs `startingCapital`/`daily_loss_limit` this call site doesn't have; `POST /api/paper/start` (item 1) remains the sole session-creation entry point.
3. Did not touch `validation-cadence-service.ts` / `shadow-rerun-service.ts`'s same-named-but-different-meaning constants (see item 3 above and the new module's docstring).
4. Kept the `paper.engine_authority_declared` and `paper.session_resume_skipped_paper_plus` audit action NAMES stable across the doctrine flip (searchability for historical audit-log analysis) while changing their PAYLOADS to be unambiguous about which authority is now in effect — rather than minting new action names for what is conceptually the "same kind of event, opposite direction."

Operator decisions D6/D7 (recorded in `docs/CAMPAIGN-250-1K-READINESS-STATUS.md`): the internal paper engine becomes the PAPER-stage promotion-evidence authority. Design (advisor-derived 2026-07-17): ship as ONE atomic MVP — **"PAPER = internal engine only"** — rather than a canonical/observer dual-path design. This eliminates the double-execution hazard structurally (one writer), not via a new schema field + runtime stamping.

Re-scope note: an earlier investigation confirmed goalscan's `0896aaca` hardened a DIFFERENT, adjacent thing (promotion-evidence gate-parity for TESTING/SHADOW→PAPER), not this wave's PAPER-authority routing flip — this packet's scope is unchanged from the original plan. The Gate-3 day-count/Sharpe precondition (originally part of M3) landed separately and already as `GATE3` (`c82ad4e6`) — explicitly OUT of this packet's scope.

All line numbers below re-verified against base `255b503a` immediately before writing this packet (not carried forward from a stale investigation) — implementer should still spot-check before editing, since concurrent sessions may advance the tip again before dispatch completes.

## Scope — IN (all in ONE wave, atomic — cannot be split, see rationale below)

1. **`routes/paper.ts:91-104`** — local `PAPER_PLUS_STATES = ["PAPER", "DEPLOY_READY", "PILOT", "DEPLOYED"]` guards a 409 `paper.start_refused_paper_state` rejection on `POST /api/paper/start`. Remove PAPER from this rejection set (keep PILOT/DEPLOYED rejected — those stay TradersPost-authoritative, untouched by this wave). A PAPER-state strategy must now be ALLOWED to start an internal paper session.

2. **`lifecycle-service.ts:3343-3378`** — `if (toState === "PAPER") { ... const { stopStream } = await import("./paper-trading-stream.js"); ... await stopStream(activeSessId); ... }` (guarded by a "B6" await-ordering comment block starting ~:3322). INVERT: on transition INTO PAPER, the internal stream must CONTINUE running (or START if not already running for this strategy), not stop. Preserve the same defensive structure (try/catch around the stream operation, non-blocking on failure, audit row on failure) — just invert what "success" means at this site. Read the existing comment history at this site (it already documents 2 prior fixes to this exact block, "deepscan14 A1" and "H2 2026-06-28") — this wave adds a 3rd, and should follow the same "don't block the transition" discipline as the prior two.

3. **Single-authority as a STRUCTURAL guarantee, not a schema field.** Three independent `PAPER_PLUS_STATES`-shaped literals currently exist, each meaning "this state may route to TradersPost / is broker-eligible":
   - `routes/paper.ts:91` (see item 1 — being edited there anyway)
   - `paper-signal-service.ts:6360-6363`: `const PAPER_PLUS_STATES = ["PAPER", "DEPLOY_READY", "PILOT", "DEPLOYED"]; if (!PAPER_PLUS_STATES.includes(lcStateForRouting)) { ...treat as pre-PAPER... }` — gates whether a signal's routing decision treats the strategy as broker-eligible.
   - `scheduler.ts:7106`: `const PAPER_PLUS_STATES = new Set([...])` (module-scope) — gates the B5 resume-guard (item 6).
   Under the new doctrine, **PAPER must be REMOVED from whichever of these decides "may route to TradersPost."** Do not just delete the literal in 3 places independently (that recreates the exact duplicated-constant drift class this campaign has repeatedly found and fixed — e.g. GATE3's `countPaperTradingDaysSince` extraction this same day). Extract ONE shared exported constant/helper (a sensible home: a new small module, e.g. `src/server/lib/paper-authority-states.ts`, or alongside an existing shared lifecycle-states helper if one already exists — check first) that all 3 sites import. Its new value/semantics: only `{DEPLOY_READY, PILOT, DEPLOYED}` are broker-eligible; PAPER is no longer in that set.
   Also reconcile against `LIVE_EXECUTION_STATES` in `server-mediated-executor.ts` (imported by `routes/live-order.ts:90`, used at `:562`: `LIVE_EXECUTION_STATES.has(lifecycleState)` — currently `{DEPLOYED, PILOT}` only, already excludes PAPER). Confirm this stays correct under the new doctrine (it should — PAPER was never live-execution-eligible) and decide whether it should become derived from the same new shared module (e.g. a strict subset) or documented as an intentionally separate, narrower set. Don't silently leave 4 independent literals where there were 3.

4. **`paper-signal-service.ts:~5993-6015`** — the "shadow_mode_enabled=true AND lifecycleState==='PAPER'" operator-override branch: today, when both conditions hold, it logs `lifecycle.shadow_mode_inconsistency_warn` and explicitly falls through to route to TradersPost (comment: "operator override... routing to TradersPost"). Under internal-only MVP, PAPER must never route to TradersPost even in this override case — re-disposition this branch so it uses the internal fill path instead (the normal PAPER path after this special-case block, not the TradersPost route). Preserve the warn-audit (the inconsistency itself — shadow flag set on a PAPER strategy — is still worth surfacing), just change the ROUTING decision it leads to.

5. **`/api/live-order` (`live-order.ts:546-574`) — CONFIRM, likely NO CHANGE NEEDED.** Current behavior already hard-rejects PAPER-state strategies carrying a `strategy_id` via `LIVE_EXECUTION_STATES.has(lifecycleState)` (PAPER was never in that set). Under internal-only MVP this is ALREADY correct — PAPER should never reach a live broker order via this endpoint either. Verify: does the strategy_id-less "programmatic HMAC caller" bypass carve-out (if one exists at this route — check) create a loophole where a PAPER strategy's order could slip through unidentified? If the carve-out is genuinely unrelated to strategy state (a different auth class entirely), this is pre-existing and out of scope — document the finding, don't touch the code. If this item requires no code change, close it as "verified correct-by-existing-design" in your final report, not "implemented."

6. **`scheduler.ts` — 3 sites, each needs an explicit, tested disposition:**
   - **`resumeActivePaperSessions()` B5 guard** (~:7106 constant, ~:7133-7148 guard block): today SKIPS internal-stream resumption for any state in `PAPER_PLUS_STATES` (i.e. PAPER+). Must shrink to skip only `{DEPLOY_READY, PILOT, DEPLOYED}` — PAPER moves OUT of the skip-set and gets its internal stream resumed on boot, same as pre-PAPER states.
   - **`detectStalePaperSessions()`** (~:7984 onward): investigation flagged this crash-auto-recovery branch has NO lifecycle-state guard at all today (unlike B5) — untested for the PAPER+ boundary. Give it an EXPLICIT, TESTED disposition matching B5's new behavior (resume/reconnect for PAPER, correctly still skip/leave-stopped for PILOT/DEPLOYED via whatever mechanism currently keeps those on TradersPost — read how it currently behaves for DEPLOY_READY/PILOT/DEPLOYED before assuming, since it may already coincidentally do the right thing for the wrong reason).
   - **`stopPaperSession()`** (~:7853, shared helper called from both stale-session paths): confirm its behavior is state-agnostic-correct for "session is genuinely stopping" (any lifecycle state) — this helper itself likely needs no change, but verify no state-specific special-casing inside it silently assumes the old doctrine.

7. **Invert the two doctrine-pinning tests** (free RED-proofs — they assert the OLD doctrine and must flip the moment items 1-6 land):
   - `src/server/routes/__tests__/paper-start-refuses-paper-state.test.ts`
   - `src/server/__tests__/deepscan14-shadow-stage.test.ts`
   Read their current assertions before editing (may test more than just the PAPER-specific behavior — invert only the PAPER-doctrine parts, preserve any still-valid assertions about PILOT/DEPLOYED or SHADOW-stage behavior this wave doesn't touch).

8. **Doc updates, SAME commit, PARENT-SESSION-EXECUTED ONLY (do NOT have the implementing subagent touch these files — AGENTS.md §10 bans subagent edits to CLAUDE.md/AGENTS.md).** The parent session will handle: CLAUDE.md §8 heading + authority paragraph (currently: "the canonical paper-trade journal is TradersPost's broker tape... the internal simulator is pre-PAPER only... On TESTING→PAPER transition, the internal stream is stopped... rejects PAPER-state strategies with paper.start_refused_paper_state") rewritten to reflect internal-only PAPER authority; §7 TradersPost paper-testing clauses; §15 execution-line annotation. **Implementer: leave CLAUDE.md/AGENTS.md completely untouched — flag in your final report exactly which doc sections need parent-session updates and why, with old-quote/new-intent pairs, so the parent session can do this precisely.** Code comments citing the old doctrine AT THE TOUCHED CALL SITES (not CLAUDE.md) should be updated by the implementer in the same diff as each site's code change — that's a normal code-comment update, not a banned doc edit.

## Scope — OUT (explicitly, do not fold in)

- **Gate 3 (30-day/Sharpe precondition → future 5-10-qualifying-day evaluator):** already landed separately as `GATE3` (`c82ad4e6`) — do not touch `lifecycle-service.ts`'s precondition block or `paper-to-deploy-ready-precondition.ts` in this wave.
- **The "stamped non-canonical TradersPost observer" idea from the original plan:** deferred indefinitely. No new schema field/column on `paper_trades`/`paper_positions`. Not MVP.
- Any PILOT/DEPLOYED-state behavior (stays TradersPost-authoritative, untouched — regression-test this explicitly, see Verification).
- Any CANDIDATE/TESTING/SHADOW-state behavior beyond what's needed to correctly land a strategy INTO PAPER (item 2's transition itself).

## Why this is ONE atomic wave, not split

The stream-continuation flip (item 2) cannot ship without the single-authority disposition (items 3/4) landing in the SAME change: turning the internal stream on for PAPER while the TradersPost routing path is STILL live for PAPER would fire BOTH an internal fill AND a real broker order for the same signal — the exact double-execution hazard this campaign exists to prevent. There is no safe intermediate state between old and new doctrine for this pair.

## Grading crux (state this explicitly to whoever grades this)

The entire risk surface reduces to ONE property, true for every flipped site with no exceptions: **a PAPER-state signal produces exactly one internal journal entry (a `paper_trades`/`paper_positions` write via the internal engine) and ZERO canonical broker orders (no `routeOrder()`/TradersPost call reachable for PAPER, from any code path — normal flow, shadow-override flow, boot-resume flow, crash-recovery flow).** Grading energy should concentrate here, especially the previously-untested `detectStalePaperSessions()` boundary (item 6) and a simulated crash-mid-transition scenario (signal fires, internal fill in flight, process restart, does a double-open or a TradersPost leak occur on recovery?).

## Verification

(a) RED-proof: both inverted doctrine tests flip red→green across the change, and stay green after. (b) A new single-authority invariant test: for a PAPER-state session, drive a signal through EVERY flipped call site (normal `evaluateSignals` flow, the shadow-override branch, `resumeActivePaperSessions` after a simulated restart, `detectStalePaperSessions`'s recovery branch) and assert exactly one internal fill + zero `routeOrder()`/TradersPost calls in each. (c) Confirm PILOT/DEPLOYED-state behavior is BYTE-UNCHANGED — a regression suite scoped to those states must show zero behavioral diff (the flip must not leak past PAPER). (d) tsc; touched + broader `paper-signal-service`/`paper-execution-service`/`scheduler`/`lifecycle-service`/`live-order`/routes-paper regression suites; 3 CI hard gates; `system-map:sync`+`check` (new shared-states module + changed authority semantics are architectural surface); `test:metrics` unchanged. (e) Independent doer≠grader grade — must hammer the grading-crux property harder than any single site's local correctness; RED-proof the shared-constant extraction itself (temporarily let one site diverge from the shared constant, confirm SOME test catches the drift — same discipline as GATE3's query-duplication lesson from earlier today).

## Rollback

Single-commit revert restores old doctrine (TradersPost-authoritative for PAPER+, internal stream stops on promotion, PAPER rejected from starting an internal session). No schema/migration change in this MVP scope — revert is clean with no data-shape concerns. CLAUDE.md/AGENTS.md doc reverts are a separate, parent-session-executed follow-up if this wave is ever rolled back.

## Parent-review addendum (2026-07-17, before independent grade dispatch)

Parent-reviewed the full diff (all 16 files) and independently confirmed: the new shared module's exports genuinely exist with matching signatures (not fabricated); the item-2 stream-stop→start inversion plus its sibling stop-on-leaving-PAPER block are sound; the item-6 scheduler guards are sound; `tsc --noEmit` clean; both `m3-paper-single-authority-invariant.test.ts` (10 tests, all 4 legs) and `scheduler-detect-stale-broker-authoritative-guard.test.ts` (5 tests) pass and are well-constructed (real DB mocks + a `routeOrder` spy, not vacuous).

**Discrepancy investigated and resolved:** running the 10 new/touched test files directly surfaced 10 failures (`pass5-lifecycle-wiring.test.ts` ×7, `deepscan14-shadow-stage.test.ts` ×3) that appeared to contradict the implementer's "zero regressions" claim. Root-caused each via direct `git show 255b503a:...` base comparison (not `git checkout`, which would have reverted uncommitted worktree changes) rather than assuming:

- **9 of 10 are pre-existing test fragility, confirmed to fail IDENTICALLY on base `255b503a` before any M3 change** — fixed-size `src.indexOf(anchor) + N`-char slice windows in `lifecycle-service.ts`/`deepscan14-shadow-stage.test.ts` source-introspection tests that later, unrelated waves (Wave 27.5, Wave 29, freshscan6, etc.) pushed their target markers past, PLUS two tests asserting a raw `"lifecycle:..._blocked"` string literal in `lifecycle-service.ts` when the code correctly references the SSE event via the named `LIFECYCLE_GATE_EVENTS.*` constant (verified the SSE genuinely fires — `broadcastSSE(LIFECYCLE_GATE_EVENTS.PAPER_TO_DEPLOY_READY_BLOCKED, ...)` / `...SHADOW_TO_PAPER_BLOCKED...` are real call sites in the file). All 9 fixed in this worktree by widening/re-anchoring windows and correcting the two literal-vs-constant checks — zero production code touched, matching this campaign's W-TESTDEBT precedent (stale tests, not code bugs). Verified via `git show 255b503a:...` that every one of these 9 assertions fails the same way on base, so this is inherited debt these files' M3 diff happened to surface, not something M3 introduced.
- **1 of 10 is a genuine, ALREADY-DOCUMENTED pre-existing instrument-code bug, correctly left untouched:** `evaluateDsrWalkForwardGate(null)` (`b14-ci-gate.ts`) returns `passed: false` for legacy/pre-Wave-A backtests despite its own `status: "legacy_proceed"` label implying proceed — this is the exact item CLAUDE.md's `/goal deep-scan WAVE 2 (goalscan-crit)` close-out entry already flagged: *"Grader-surfaced pre-existing follow-up: `b14-ci-gate.ts` `legacy_proceed` returns passed:false vs docstring PROCEED (mirrored not fixed; needs own packet)."* Confirmed via base comparison this predates M3 entirely. Changing it would flip a PAPER→DEPLOY_READY hard-gate's behavior for legacy backtests — genuine instrument-code surface requiring its own ratify packet per house law, explicitly out of scope for M3's PAPER-authority-routing packet. Left the test red (did NOT flip its expectation to `passed:false`, which would silently ratify the flagged bug via a green test).

Full combined run after fixes: `pass5-lifecycle-wiring.test.ts` 41/41 green; `deepscan14-shadow-stage.test.ts` 26/27 green (1 known pre-existing red, as above). All 10 M3-touched/new test files together: 226/227 green (same 1 known red). tsc clean; 3 CI gates green; `system-map:sync`+`check` clean (`driftItems: []`); `test:metrics` 145/145 unchanged.

**Independent grader instruction:** scope the grade to M3's PAPER-authority-routing surface (the 8 items). The `evaluateDsrWalkForwardGate` red is pre-existing, out-of-scope, already on record — do not ding this wave for it or attempt to fix it inline; a future packet owns it.

## Independent grade + same-wave closures (2026-07-17)

**VERIFIED BAND 7** by a fresh accuracy-validator (doer≠grader, from-zero re-derivation, not a self-report). Verdict: the eight scope items are genuinely and correctly implemented — the grading crux holds under adversarial RED-proofing (2 of the grader's RED-proofs on genuine safeguards — the item-6c defensive-stop guard, and the shared-constant extraction itself — both correctly caught the injected break), PILOT/DEPLOYED are byte-unchanged, tsc clean, 226/227 M3-touched tests pass (1 known pre-existing red), all 3 CI gates green. Band capped at 7 (not 8) because 2 of the grader's RED-proofs found a claimed safeguard that did NOT hold — a false-positive-evidence class this campaign treats as a band cap, not a rounding error.

**F-1 (CRITICAL, closed):** `paper-authority-states.test.ts`'s `LIVE_EXECUTION_STATES` strict-subset test asserted against a hand-maintained copy (`new Set(["DEPLOYED","PILOT"])`) instead of the real, live `LIVE_EXECUTION_STATES` object exported by `server-mediated-executor.ts` — so a future accidental widening of the real constant (e.g. adding "DEPLOY_READY") would go completely uncaught, despite the packet's own docstring explicitly claiming a dedicated test enforces this. Grader RED-proofed by widening the real constant and finding all 5 candidate test files (78 tests) stayed green. **Fixed:** `paper-authority-states.test.ts` now mocks only `../../db/index.js` (probed directly — every other transitive import in `server-mediated-executor.ts`'s dependency chain is side-effect-free at module-load time) and dynamically imports the REAL `LIVE_EXECUTION_STATES` object for both the strict-subset assertion and the DEPLOY_READY-exclusion assertion, plus a new pinned-value self-check. Re-RED-proofed (widened the real constant the same way the grader did): 2 of 19 tests correctly went red for the exact right reason; restored, reverified 19/19 green.

**F-2 (HIGH, closed):** the M3 sibling-stop block (`lifecycle-service.ts` ~3433-3468 — the packet's own headline zero-carry-forward fix, stopping the internal stream on the way OUT of PAPER into DEPLOY_READY) had zero behavioral test coverage — every referencing test was a static `readFileSync` string-match, none drove the real `promoteStrategy()`/`_promoteStrategyInner` with a mocked DB to confirm `stopStream` actually fires with the correct session. Grader RED-proofed by typo'ing the block's own DB status filter (`"active"` → a typo) and found 226/227 tests unaffected — the bug would have been invisible in production (contained to the PAPER→DEPLOY_READY boundary; mitigated by the scheduler's independently-tested 5-minute defensive-stop sweep, so a real gap with a working backstop, not an open hole). **Fixed:** new `m3-sibling-stop-behavioral.test.ts` (5 tests) drives the REAL `LifecycleService.promoteStrategy(id, "PAPER", "DEPLOY_READY")` via a DB mock adapted from the `gate3-manual-precondition-wiring.test.ts` table-routing pattern, extended to be genuinely FILTER-AWARE on the `paperSessions` status/strategyId columns (via mocked `drizzle-orm` `eq`/`and` returning inspectable condition objects, matched against the REAL column object references) — not a naive ignore-the-WHERE-clause router, which would have been equally vacuous. Asserts: stopStream called with the correct session on the happy path; NOT called when no active session exists, when the session belongs to a different strategy, or when its status isn't "active". RED-proofed by applying the grader's exact production-code mutation (typo the status filter at `lifecycle-service.ts:3439`): the positive test correctly went red (expected 1 call, got 0); restored via file backup, confirmed byte-identical (`diff` clean), reverified 5/5 green.

**F-3 (MODERATE, closed):** the two new M3 scheduler audit rows (`paper.session_restart_skipped_broker_authoritative` at `scheduler.ts:8132-8143`, `paper.session_auto_stop_broker_authoritative_leak` at `scheduler.ts:8267-8278`) omitted `correlationId` despite the variable being in scope at the top of `detectStalePaperSessions()` and already threaded into other calls in the same function (e.g. `stopPaperSession(...)`) — breaking the CLAUDE.md §2 correlation_id end-to-end propagation mandate and chronically triggering `insertAuditRowSafe`'s own missing-correlationId warning in production. **Fixed:** added `correlationId,` to both audit-row object literals (one line each). RED-proofed via new assertions (`correlationId: expect.any(String)`) added to both `scheduler-detect-stale-broker-authoritative-guard.test.ts` tests that check these actions; verified green.

**Non-blocking (recorded, not fixed — correctly out of scope per grader):** a docstring inaccuracy in `paper-authority-states.ts` claiming `shadow-rerun-service.ts`'s `PAPER_PLUS_STATES` "shares the OLD 4-state value textually" with `validation-cadence-service.ts`'s constant — verified false (shadow-rerun's is actually a DIFFERENT 6-state shape, missing PILOT, plus 3 terminal states); the underlying DECISION to leave both untouched is still correct, only the packet's own investigation note was wrong. Fixed the docstring (one paragraph) since it was cheap and already being touched. A narrow edge case (an operator-paused PAPER session mid-promotion has no active session row, so the entering-PAPER block silently no-ops with only a generic info-audit `session_id: null` field to signal it) was traced as reachable-but-narrow and left as a documented residual — not part of the grader's blocking findings.

Full verification after closures: tsc clean; 238/239 across the 12 M3-relevant test files (1 known pre-existing DSR red, unchanged); all 3 CI gates green; `system-map:sync`+`check` clean; `test:metrics` 145/145 unchanged; full-suite regression re-confirmed (90 failed files both before and after M3, matching the implementer's own before/after count — zero new regressions).

## Plain-English for the operator

Right now, once a strategy graduates to "PAPER" testing or beyond, the system's official record-keeper switches from the internal simulator to TradersPost (an external broker-connected service) — even though nothing live-trades yet, TradersPost is treated as the source of truth for PAPER-stage evidence. This wave flips that: the internal simulator becomes the sole record-keeper for PAPER, and TradersPost is removed from the loop entirely for that stage (not "kept as a backup" — genuinely not called). This closes the risk of the same signal accidentally producing two different results in two different places (once flipped, there is only one place a PAPER signal can produce a result).
