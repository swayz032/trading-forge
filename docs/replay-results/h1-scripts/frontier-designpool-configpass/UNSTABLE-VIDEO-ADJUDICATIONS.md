# Enumeration-unstable videos — blind adjudications settled (2026-07-13)

Per the k=5 policy, the two ENUMERATION-UNSTABLE design-pool videos (mode<4/5) got one blind adjudication each (fresh-context, canonical 3-rule standard) to settle the count before their conditions weigh in the config-pass verdict.

- **IyFioFkRgWo → exactly 1** (consistent with the re-mint). Support-bounce long scalp; 13/200 EMA = Rule-3 variants; "big move" breakdown-short = Rule-1 unpromoted mention (gestural exit). k=5 mode 1 = in-window ✓.
- **DLwVqcLRcfw → 1-or-2.** Firm floor 1 = VWAP+20SMA long confluence scalp (Rule 3, scalp-exit + 20SMA-trail-exit variants). Ceiling 2 = "short the rejection" flip (Rule-2 opposition) held borderline by Rule-1 (concrete entry, NO exit stated). k=5 mode 1 = in-window ✓. BINDING: the short-flip content must survive as ATTRIBUTED content in either reading (a content-condition for this video's extraction).

Both settled; both mode-1 acceptable. Config-pass verdict may count both.
