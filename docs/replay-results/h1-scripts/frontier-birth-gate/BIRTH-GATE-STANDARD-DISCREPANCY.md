# Frontier-extractor birth gate — STANDARD-DISCREPANCY ALARM (2026-07-13)

6 fresh-context frontier-brain (Claude-family, in-subscription) enumerators, each running the FROZEN `strategy-enumerator.md` instrument on ONE spent-16 transcript, blind to ground truth.

## RESULT: 3 match / 3 mismatch — mismatches are SYSTEMATIC, not noise

| Video | GT (blind-adj rubric) | frontier (enumerator instrument) | match | frontier's discriminator |
|-------|:---:|:---:|:---:|---|
| R5L890juvRw | 2 | **2** | ✓ | SPLIT on opposition (trend-continuation vs mean-reversion; opposite dir+target) — the merge-silencing case, correctly split |
| IyFioFkRgWo | 1 | **2** | ✗ | SPLIT — bounce vs support-BREAK (opposite entry logic); frontier flagged "lenient could call it 1" |
| -igpOZs8LsM | 1 | **1** | ✓ | GROUP — bull/bear mirror; order-block = confluence not 2nd skeleton |
| 4cT8WTyxhYY | 1 | **1** | ✓ | GROUP — manual/indicator = confirmation variant; late tips = confluences |
| WEhmadJArQo | 2 | **1** | ✗ | GROUP — same opening-range-FVG skeleton, TF/confirmation/target variants (= the instrument's OWN worked example) |
| E9MzEC_yNoM | 3 | **1** | ✗ | GROUP — "3 ways to trade these levels" (breakout/retest/sweep), ONE explicitly-unified exit; frontier flagged sweep-reversal could split |

## THE ROOT: two enumeration standards, diverging on one case class

- **`strategy-enumerator.md` (the instrument the frontier brain runs, campaign-frozen this week):** distinct = own **entry logic OR exit/management logic**; timeframe / **confirmation-mechanic** / target differences = **VARIANTS** within one strategy. Over-split is "the single most damaging failure mode."
- **The blind-adjudication rubric (minted the GT fixtures; my earlier dispatch):** runnable = concrete **entry AND exit/stop**; each distinct **entry trigger** = a distinct strategy.

They disagree exactly on **"different entry TRIGGER on a SHARED skeleton + unified exit"** — the enumerator calls it a variant; the rubric calls it a distinct strategy. All 3 mismatches are this class.

## THE FRONTIER BRAIN IS COHERENT (and possibly SUPERIOR for certification)
It splits on genuine **opposition** (R5L890 opposite-direction → 2; IyF bounce-vs-break → 2) and groups on **compatible config/confirmation variation** (WEh, E9MzEC, 4cT8). That is precisely the merge-silencing-aware ontology: contradictory setups get separate objects (they'd trip direction_conflation if fused), compatible variants share one. Note: pass-2 GEMMA (weaker) got E9MzEC=3 matching the crude GT; the FRONTIER (stronger) grouped to 1 — the stronger brain reads the unified-exit structure.

## DOER SELF-CHECK (on the record)
I built the frontier path; I am motivated to see it pass — weigh this independently. The frontier is NOT cleanly "right": on E9MzEC it grouped a liquidity-SWEEP REVERSAL (fade of a failed break — opposite to the breakout) into one object, which by its OWN opposition-principle arguably splits off → the case is genuinely hard and the standard is underspecified. This is not a doer pass/fail; it is a load-bearing pre-registration question.

## FOR OPERATOR RULING (load-bearing, defines "faithful" for the whole conveyor)
1. WHICH enumeration standard is canonical — the `strategy-enumerator.md` instrument (opposition-splits, config-groups) or the runnable-trigger rubric (every distinct entry trigger = a strategy)? This choice defines what a "strategy object" IS, hence what faithful extraction must carry.
2. The birth-gate fixtures must be RE-DERIVED under the chosen canonical standard (fresh blind adjudication under that exact rubric) before the gate can judge the frontier brain — the current GT was minted under a rubric the instrument does not share.
3. Only then is a birth pass/fail meaningful. This is the two-path-derivation law: disagreement on a load-bearing input IS the alarm; resolve the standard before any count counts.
