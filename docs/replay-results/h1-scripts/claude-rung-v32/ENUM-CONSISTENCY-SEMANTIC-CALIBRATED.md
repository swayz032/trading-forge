# SEMANTIC ENUMERATION-CONSISTENCY CHECK — CALIBRATED (path-1, 2026-07-15)

Minted law: prose-format structural guarantees are SEMANTIC cross-vendor (mechanical reserved for compiled specs/H2). The lexical lint was grader-FALSIFIED (paraphrase-bypass; IyF catch = key-format accident) -> replaced.

## Semantic check (gpt-5.4-FLEX HIGH, cross-vendor): per variant, "does it re-promote an enum-EXCLUDED mention (by MEANING)?"
- CALIBRATION BOTH POLARITIES HOLD:
  - IyF -> INCONSISTENT, offending variant `breakdown_continuation` re-promotes the excluded "breakdown big move" short mention (judged by MEANING, paraphrase-robust — NOT the lexical key accident).
  - 6 clean witnesses (0xyg, 2DX-s0, E9MzEC-s0, PVMg, ZF8-s1, dV7) -> CONSISTENT (variants are legitimate skeleton configs).
- COVERAGE (F-1 honesty, claim-scoping law): **7 of 22 carried variants and were genuinely semantically checked; 15 passed trivially with no variants field (no variant-promotion possible).** Headline states what was measured, never more.
- Cost: pennies, under standing envelope, hard-cap enforced.

## Terminal-read wiring: the enumeration_consistency axis now consumes the SEMANTIC verdict (consistent->PASS, inconsistent->FAIL), fail-closed per-axis (independent of the conflation axis). The lexical lint (enumeration_consistency.py) is SUPERSEDED-FOR-PROSE, retained for H2 structured layer only.

## Cert scope line (operator): "variant promotion beyond enumeration: measured lower-bound 1/22 (semantically checked on the 7 variant-bearing strategies), lint armed at terminal read."

## NEXT: independent grade the semantic check (both polarities + paraphrase-robustness + honest coverage) -> then dress rehearsal completes with the semantic enum axis -> green -> operator seal-trigger message.
