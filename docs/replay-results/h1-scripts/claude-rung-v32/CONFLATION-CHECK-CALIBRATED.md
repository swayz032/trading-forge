# SEMANTIC CONFLATION CHECK — BUILT + CALIBRATED (path-(d), 2026-07-15)

Cross-vendor anti-merge-silencing axis: gpt-5.4-FLEX HIGH judges "one coherent trade, or two opposite trades welded together?" on the object's CO-REQUIRED conditions. Replaces the mechanical fence (blind on prose format) at the terminal read; 3 mechanical lints re-station to the compiled-spec/H2 layer.

## CALIBRATION — both polarities through real judgment, HOLDS
- REJECT: R5L890-FUSED adversarial (continuation-to-outer-bands + reversion-to-center welded, co-required contradictory targets) -> REJECT.
- PASS (mirror): -igp fvg_trend_continuation (one skeleton, side by context) -> PASS.
- PASS (labeled variant): IyF support_bounce_scalp (co-required long coherent; short breakdown is a labeled variant-alternative, enumerator's axis not this check's) -> PASS.
- v1.1 SCOPE fix = applying the path-(d) definition correctly: fusion = opposite logic WELDED into co-required conditions; a labeled opposite-direction variant is an alternative, not a fusion.

## RETRO-22 = 22/22 MEASURED CLEAN
Design-pool 22 all PASS. "Probably legitimately clean, unchecked" -> MEASURED clean by an independent cross-vendor axis. Cost: $0.26 (initial 23) + $0.03 (triplet re-cal) = $0.29, under the $0.60 ticket, hard-cap enforced.

## HONEST SECONDARY FINDING (IyF) — extraction-variance, NOT a seal issue
IyF's v3.2 staging extraction PROMOTED the breakdown into a short `variants[2]` (breakdown_continuation, direction:short). But the FROZEN enumeration ruled the breakdown an UNPROMOTED MENTION (retrospective, no runnable entry) and EXCLUDED it. So the v3.2 staging extraction is inconsistent with the certified enumeration on IyF. This is a v3.2 extraction-variance data point in the SHAKEDOWN corpus (the 22) — it does NOT touch the sealed twelve, and the conflation check correctly PASSes IyF's co-required long logic. Recorded for cert-consistency awareness; the certified enumeration (frozen) is authority (breakdown = mention).

## NEXT (path-(d) sequence): wire fail-closed into terminal_read_grade (ratify-packet + independent grade; 3 lints -> H2 layer) -> witness pair through real harness -> read-shape staging + full-dress rehearsal on 2-3 spent videos -> STOP at green -> operator seal-trigger.
