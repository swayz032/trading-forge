AR-1290: bootstrap retry-safety repair — shared claim store + attempt identity, C1-C10 closed

Repairs both defects AR-1289 found. The claim now lives in the Git common directory shared by
every worktree of a repository (new file claim-store.mjs), so a sibling worktree created by
git worktree add can see a claim the bootstrap wrote in the source worktree without any commit,
checkout, or channel through the marker/manifest/model prompt/env. The legacy committed claim
directory stays untouched and immutable; replay refusal is now a union of both stores, so
retiring the old storage mechanism cannot un-spend an old authorization.

Branch and worktree derivation now takes the authorization id alongside the target packet, so a
fresh authorization for the same packet never collides with a prior spent attempt's names, and
the spent attempt never needs deleting to make room.

Three new tests exercise the actual fix against real git worktrees and repositories rather than
fakes: claim-store writes and reads across a freshly created sibling worktree, a repository
isolation control, and an O_EXCL replay control. One of the sibling-worktree tests reproduces the
pre-fix reader shape inline and proves it would have missed the claim, satisfying the literal
requirement that the control fail under a reversion to the old storage.

All 65 tests pass: 56 pre-existing unchanged, 9 new. The production bundle grew from eight files
to nine (claim-store.mjs added, since its bytes decide claim resolution) and the stale
seven-file count in CONTRACT.md is corrected in the same pass, since this packet is the one
actively invalidating it further.

Authoring and testing only, per this packet's authorization: zero bootstrap executions against
the real repository, zero privileged seat launches, zero Agent or subagent calls, zero frozen G2
spend. The spent cpb-2026-08-17-0001 claim, branch, and worktree from AR-1289 are left exactly as
found - not touched, not cleaned up. Terminal frozen proof: ready 8, spent 0, receipts
README-only.

Run on Sonnet 5 per the operator's model switch, following this packet's own required stop when
this session was still Opus. Disclosed: the session has no way to introspect its own running
model, so the switch is relayed, not independently measured.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
