AR-1392

RULING : AR-1382A (`188b41e39908518f8909f6e9e54a45c346813276`) sections 2 and 6 — execute targeted Visual Intelligence for VI-E8-1, VI-E8-2, VI-E8-3.

PIN : branch `claude/worker1-h1-20260815`, HEAD `90316e07` before this commit.

CHANGED :
- NEW `docs/replay-results/gpt-engineering/opus-transcript-first-diagnostic/visual-intelligence-e8-round1/E8Wg6tFPYjo/vi_findings.md` — full findings, durable.
- NEW `.../frames/` — 22 committed PNG frames + 2 full-video scan contact sheets + 2 magnifications, all sha256-listed in the findings file.
- NEW `scripts/_worker_vi_e8_calibrate.py` — the pixel→price calibration generator.
- MOD `.../vi_task.json` — executor status.

🛑 **RETRACTION FIRST — AR-1391's CENTRAL CLAIM WAS FALSE AND THE OPERATOR CAUGHT IT.**

AR-1391 reported "no Visual Intelligence executor capability exists in this repository" and filed Lane B as a ruling-premise contradiction. **That was wrong.** VI exists as a documented worker procedure with committed precedent (AR-1204/1205, AR-1208/1212, AR-1218/1219, AR-1220/1221) and committed artifacts at `docs/replay-results/svkm-extraction-certified/grade/visual-stopA/paired-hires/` — including the exact reproduction command that this packet then used unchanged. I produced that false absence by grepping `src/` and `scripts/` for *code named* "visual intelligence", when the capability is a procedure plus frame artifacts under `docs/replay-results/`. **I published a null without a positive control on the surface I searched** — the precise failure I had lectured about two paragraphs earlier in the same report, having just caught a `NEEDS_REVISION`-substring false positive. Struck; the record is retained in AR-1391 rather than rewritten.

Second correction, also from AR-1391 and also mine: the yt-dlp 403 was NOT signed-URL IP binding (egress IP measured `46.110.208.220`, exactly matching the `ip=` in the URL). **Actual root cause: yt-dlp was version 2026.07.04, ~7 weeks stale. Upgrading to 2026.08.19 made the already-documented command work on the first attempt, unmodified.** Nothing about the repo's method was broken; a dependency had decayed and presented as a hard access wall. I published two wrong mechanisms before the right one.

RED : the documented command at yt-dlp 2026.07.04 — `python -m yt_dlp -f 137 ...` — `ERROR: unable to download video data: HTTP Error 403: Forbidden`. Reproduced across player clients `android_vr`/`web`/`web_safari`/`web_embedded`/`tv`/`mweb`/`ios`/`android_music`, with and without `--js-runtimes node`, with and without `--remote-components ejs:github`.

GREEN : `python -m pip install -U yt-dlp` (2026.07.04 → 2026.08.19), then the SAME unmodified command: `[download] 100% of 33.92MiB in 00:00:04`. Downloaded twice, byte-identical (35,570,757 bytes, sha256 `06af188d…3e2841`, 1920x1080, 1177.60 s). Frames then extracted with the documented `ffmpeg -ss <ts> -i hi.mp4 -frames:v 1` form.

CONTROL : pixel→price calibration to the AR-1221 standard — the axis scale is fitted from price-axis anchors only, then checked against distances **the position tool itself printed and which were not calibration inputs**. Sell: derived stop 0.00242 vs printed 0.00241, derived target 0.00589 vs printed 0.00590. Buy: derived stop 0.00071 vs printed 0.00071, derived target 0.00122 vs printed 0.00122, and 0.71 predicted from the fib anchors 0.55898 vs observed entry handle 0.55898 — exact on all three. Generator committed. Separately, VI-E8-1's orientation carries a **discriminating** control: the opposite fib orientation predicts the 0.71 line at y≈655; it is observed at y=455, so the alternative is refuted rather than merely disfavoured.

RESULTS :

- **VI-E8-1 `MULTIMODAL_RESOLVED`.** The sell-side (GBPAUD) Fibonacci is anchored level `1` at the swing HIGH (2.02682) and level `0` at the swing LOW (2.01851) — i.e. **drawn high→low, the direction-mirror of the buy-side's narrated low→high**, not a repeat of it. The buy-side (NZDUSD) is the inverse: `0` at the high (0.56073), `1` at the low (0.55826). **This confirms AR-1380A HIGH A on direct visual evidence**: the candidate's ordered `entry_sequence` runs the buy-side low→high drawing step immediately into the sell-side 71% short entry, which executed literally inverts the retracement and puts the entry at the wrong price.
- **VI-E8-2 `MULTIMODAL_RESOLVED`.** "That wick" is the **Fibonacci level-`1` anchor itself** — the swing low the fib was dragged from, 0.55826, where the stop line, the `1` label and the wick coincide and from which the tool's printed 0.00071 risk reproduces exactly. Unified direction-aware rule covering both examples: **stop = fib level `1` (the impulse-origin anchor); entry = the `0.71` level** (exact on both sides). Residual stated: agreement is at ~1-pip calibration precision; a sub-pip hand-placement offset like AR-1221's ~4-tick residual would not be detectable and is NOT claimed absent.
- **VI-E8-3 `VISUAL_UNRESOLVED`** — and re-checked at the operator's instruction after I first called it from a single frame, which was not good enough. Full-video scan: **240 samples at 5-second intervals across the entire 19:38, every one reading `· 15 ·`**; a 4H chart would read `· 240 ·` and no such tile exists. **Positive control on the scanner:** the same scan cleanly resolves the GBPAUD→NZDUSD symbol change at ~11–12 min, so the field is being read and the absence is real, not instrument blindness. Frames at the exact premium/discount teaching moment show only the indicator's categorical verdict badge (`4H | Premium` / `4H | Discount`); no 4H high, low, or 50% line is ever plotted. Per AR-1382A section 2 no range selector is proposed. This stays a hard `COMPILE_BLOCKER_SOURCE_MISSING` and is **not resolvable from this source at all** — not from its text, not from its pixels.

FINDINGS :

- **NEW, not in the GPT-5.6 audit — `SOURCE_CONFLICT` on the buy-side target.** The narration says the take-profit is dragged *"to the high of the Fibonacci range"*. That level is `0` = 0.56073. The target is actually at **0.56020 — 0.00053 (~5.3 pips) below it**, ~21% into the range rather than at its boundary; the tool's printed `Target: 0.00122` reproduces from 0.56020 and not from 0.56073, so it is not a reading artifact. The **sell-side target sits exactly on its level `0`** (2.01851). So the narrated target rule holds on one worked example and fails on the other. Same shape as AR-1212's *teacher's words disagree with his own chart*. This bears on **HIGH B**: beyond the invented cross-direction `priority`, the two targets are not produced by one consistent stated rule. Flagged for adjudication; no representation proposed.
- Findings against myself: the two struck claims above (false VI absence; false IP-binding mechanism). Both were published before being measured. The VI absence in particular was a null without a positive control on the searched surface.
- Media hygiene: the 34MB source .mp4 is deliberately **not** committed (re-derivable in ~5 s from the documented command); only frames + hashes are. I could not add a `.gitignore` rule — repo-root `.gitignore` is outside this packet's `edit_scope` — so the blob was removed explicitly rather than left for the Stop-hook WIP auto-commit to sweep. Worth a durable ignore rule if VI runs again; not self-widening scope to do it.

STOP : none. Lane B is executed and closed. No Round-4 authoring started (AR-1382A section 7 reserves it, and Lane C waits on GPT reading these results).

NEXT : GPT adjudication. The decision AR-1382A section 7 already framed is now live and evidence-backed: two of the three hard blockers are resolved from source, but **VI-E8-3 is proven unresolvable from this video**, which is the ruling's own trigger for an honest E8 source-completeness refusal rather than another reconstruction round. That call is GPT's. Also owed a ruling: the new buy-side target `SOURCE_CONFLICT`, since it constrains how `targets` may be represented in any successor candidate.
