# EXTERNAL READ — 2026-08-09 — WIDEN STEP 3 TO BOTH GENUINE OPENING-RANGE DEFINITIONS

> **PROVENANCE:** **OPERATOR-RELAYED CHAT**, 2026-08-09, resolving the scope stop `AR-825` raised.
> **CONSUMED BY `R-732`** — banner written BY that ruling, IN its commit (`R-722 §1`).
>
> ⭐⭐ **THE STOP WORKED.** `R-731 §3` forbade hardcoding a video id to force the population back to
> one; `AR-825` hit a second member, **read it before judging it**, refused both available cheats, and
> routed the scope call upward. **The reader then verified the second extraction independently and
> reached the same census state this desk measured** (`WAIT_STRUCTURE` · `APPROXIMATED` ·
> `compute_structure_state` · taught bindings `0`). ★★★ **`THE MEASURED RESULT JUSTIFIED WIDENING THE
> SEMANTIC CAPABILITY — NOT WEAKENING THE CONTROL.`**
>
> 🛑 **THE TRAP NAMED IN `SCOPE WARNING` IS THE REAL ONE:** the second video is **better specified**
> than the golden one — it teaches a close-based breakout and retest. **That clarity is exactly why it
> must not be compiled now.**
> **A CHANNEL IS NOT AN AUTHOR** — audited on merit.

---

GPT EXTERNAL ADVISOR RULING — WIDEN STEP 3 TO BOTH GENUINE OPENING-RANGE DEFINITIONS

DECISION

WIDEN THE AUTHORIZATION.

Do not hold the classification, reject the second true definition, or hardcode either video.

INDEPENDENT VERIFICATION

I checked the second frozen extraction directly:

`dENM6gt8ZRg__s0`
`orb_5m_retest`

It explicitly teaches:

- `09:30–09:35`;
- the first five-minute candle forms the opening range;
- the candle high and low form its two levels;
- the midpoint lies halfway between them.

The census currently records that genuine definition as:

- type: `WAIT_STRUCTURE`;
- status: `APPROXIMATED`;
- primitive: `structure_engine.compute_structure_state`;
- executable: `true`;
- taught bindings: `0`.

Therefore, this is not classifier overreach. It is a second measured occurrence of the same semantic defect.

AUTHORIZED STEP 3 POPULATION

The current frozen population may move exactly these two conditions:

1. `st5e-YJRfKc__s0`
2. `dENM6gt8ZRg__s0`

Both become:

`OPENING_RANGE_DEFINITION`

The authorization is semantic, not ID-based.

- Production code must not contain either video or strategy ID.
- Tests may name both frozen members to enforce exact population membership.
- If the current frozen scan finds a third member, stop again.

REQUIRED POST-CONDITION

For both definitions:

- explicit type is emitted;
- source prose, provenance, role and ordering remain preserved;
- primitive is `None`;
- refusal reason is `opening_range_adapter_not_implemented`;
- refusal is returned deliberately;
- neither condition reaches `compute_structure_state`.

CENSUS EXACT-DELTA

Convert the census control to permit exactly two intended classification changes.

Any mechanically derived condition IDs or hashes must be mapped and identified.

Everything else must remain identical:

- all other golden conditions;
- all other `orb_5m_retest` conditions;
- every unrelated census condition;
- genuine structure routing.

Do not update the known-wrong census.

TEST TRANSITION

After classification lands:

GREEN:

- both genuine definitions receive the new type;
- breakout/reference sentences remain unchanged;
- anaphoric references remain unchanged;
- both definitions refuse explicitly;
- neither falls back to the structure evaluator;
- exact-delta census control;
- six classifier controls;
- neighboring structure behavior.

RED UNTIL STEP 4:

- no executable opening-range adapter;
- no reachable typed opening-range output contract.

SCOPE WARNING

The second video clearly teaches a close-based breakout and retest sequence. Do not compile those rules now merely because they are clearer than the golden video.

STEP 3 covers only opening-range definition typing.

AR-825 MAY RESUME AND LAND THE TWO-MEMBER CLASSIFICATION.

The worker stopped correctly. The measured result justified widening the semantic capability—not weakening the control.
