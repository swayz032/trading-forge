# ADVISOR-STATE — money-path / H1 seat

> **Rewritten in place, never appended.** Cold-start read: this file, then the
> last 3–5 rulings, then the newest 1–2 ARs. Never read the ledger from the top.
> Last rewritten: 2026-07-28, current through **R-408** (census arc closed).

## SEAT
Ledger at **R-408**. Newest AR: **AR-373, RULED** (referent census COMPLETE).
Worker: **ACTIVE on the FVG sizing experiment** (item 1 below; first observable
= the two-spec routing table). PR #27 **MERGED** (`7dd27a0d`, 19/19 green).
Rig: 2s content-hash report poll + 15-min idle watchdog — **one rig, never
new-plus-old**; the `ADVISOR-RULINGS.md` watcher under the OTHER `claude.exe`
is the worker's ear, never kill it.
★★★ **INVOKE `advisor-ruling` BEFORE EVERY RULING** (hook-enforced; a
remembered skill is a stale skill — the file mutates).
★★ **YOU DECIDE** (operator-ordered): merges · worktree updates · deploys of
verified work · reversible CI-gated production writes · model/tooling choices.
Reserved to operator: real capital · spend beyond envelope · irreversible
destruction · unboundable blast radius. Never answer from ignorance when
`WebSearch` exists.
★★ Desk laws minted today, all hook- or skill-enforced: **NAME THE JOIN KEY**
(R-400) · **READ THE CONDITION TEXT — a type field is a claim about text**
(R-404) · **a shared label is not a shared cause** (R-408).

## THE PLAN — money-path phase ladder (BLUEPRINT, R-053..R-061)
**READ THIS BEFORE ANSWERING "what phase are we in."**
- **Phase 1 — SPEC COMPILATION (WE ARE HERE).** Exit: *"≥1 tier-A spec compiles
  with ALL load-bearing conditions concretely bound."* Pinned before-figure
  (R-401, cite exactly): `0/16 specs fully bound. Flags-off: 0 of 155
  bound_and_concrete. Flags-on hypothetical: 6 of 155. Source:
  dual-denominator-remeasure-2026-07-21.json, frozen, refresh BLOCKED by
  REVIVAL_FAMILY.` ★★★ **R-408: PHASE 1 IS EXITABLE ON corpus_A — through
  `CLDE`, and only through `CLDE`.**
- **Phase 2 — BATTERY/WAVE** (failure-attribution pre-registered before any
  verdict). **Phase 3 — CONVEYOR** (paper + shadow concurrent per strategy).
  **3→4 DEPLOY-IN-SEASON** (regime must be live). **Phase 3.5 — FIRST THIRTY
  FUNDED DAYS** (written before funding; CONSISTENCY lane on record).
  **LAST MILE (operator spend):** buy Combine + TopstepX API when the first
  real-fidelity wave shows promise.
- Broker-safety chain (R-359..R-401): closed and DEPLOYED today; not on the
  ladder.

## THE CENSUS RESULT (R-402→R-408, the day's arc — details in those rulings)
Three targets died at three layers (membership `hcHu…` / type↔text `IyFio` /
text↔referent `E9Mz`), then the 27-condition referent census over the three
clean specs settled the standings:
- ★★★ **`CLDE` = THE GATE TARGET — 10/10 reachable, NO spec edit.** Defines its
  own draw-on-liquidity (Asia/London/PD H-L, EQH/EQL) + hard 9:30 clock. Needs:
  FVG flag routing (6 conds name FVGs) + a **session-LEVELS lane** ([0],[1]).
- **`-igp` = second — rides the FVG flag to 9/11**, capped by 2 mis-typed
  conditions (FVG geometry typed WAIT_SESSION; extraction-side only).
- **`_LS6` = third** — VWAP strategy (5/6 conds), composite session+VWAP+volume
  binding; STOCK-domain caveats (inv. 5: whole-dollar = equities; any mapping
  is a graded adaptation); no taught stop (framework owns it, inv. 4).
- ★★★ **ONE TYPE, THREE CAUSES:** the survivors' six unbound `WAIT_SESSION`
  conditions = mis-typed geometry (`-igp`) · session-derived LEVELS (`CLDE`) ·
  genuine session TIME (`_LS6`, "opening bell"). **No generalisation survives —
  read each condition.**
- ★★★ **THE BUILD IS ROUTING, NOT DETECTION [all MEASURED at the desk]:**
  FVG → `fvg_native.compute_fvg_signal` (approximation=False) behind
  `TF_FVG_IDENTITY_ENABLED` default OFF (its graduation owes the SDS review,
  not just our sizing) · session-levels → type vocabulary ALREADY enumerated
  (`config.py` `LiquidityLevelSnapshot`: pdh/pdl/asian_/london_/eqh/eql/…),
  tokens ALREADY recognized as `session_range` kind (`spec_family_bindings
  :~1120`, `classify_session_role`) but never zone-mapped — **lane = resolve +
  route on an existing classifier** · VWAP → `core.py:271/:379`, **zero calls
  from the binding layer**.
- **SESSION_CLOCK standalone: RETIRED** (yield 2/27, both `_LS6` conjuncts —
  binding a window alone would drop the VWAP rule = fidelity lie). "opening
  bell" vocabulary folds into the future `_LS6` composite build.
- **corpus_B respin: RE-SCOPED to recovery lane** (E9Mz/IyFio + mis-types),
  AFTER the gate. R-405's "nothing survives" clause did not fire.
- Pinned (R-405): **`concrete` requires the teacher's named referent resolved
  from the teacher's material** — a framework default (EMA proxy) never
  qualifies. `transcript_chars: 0` on ALL FOUR specs read today.

## AUTHORIZED NOW (worker, in order)
1. **FVG SIZING EXPERIMENT — `-igp` + `CLDE` (R-406/407, running):** in-process
   binding-plan diff, `TF_FVG_IDENTITY_ENABLED` off vs on, per-condition
   routing, concrete-by-the-artifact's-definition marked; approximates reported
   as approximate. **Process-local env only; no file writes; no `.env`; no
   publish; producer untouched.** Reading `compute_fvg_signal` in scope (is the
   taught 3-candle geometry faithful?).
2. On its result **I write the CLDE build contract** (FVG path + session-LEVELS
   lane, cheaper-first per sizing). **The lane is NOT yet authorized to build.**
3. **Parity check's third leg** — make `check-pglite-ddl-parity` read the
   MIGRATIONS (R-403: its "schema.ts is the source of truth" remedy nearly
   deleted a real column from a correct test).
4. **Date-stamp `OUT_PATH`** — `dual_denominator_remeasure.py:103` hardcodes
   the 2026-07-21 filename; a publish would overwrite pinned evidence misdated.
5. **Revival-probe diagnosis** (`SESSION_ACKNOWLEDGMENT_ENTRY_WITHDRAWN` /
   `_PAIR_DRIFTED` → `NO_ASSERT_FIRED`). Diagnosis only; repair after I rule.

## NOT AUTHORIZED (worker)
Real-capital actions · spend · credential decryption · `.env` writes · flag
flips outside the experiment process · spec file edits (incl. re-typing) ·
frozen instruments · sealed-read set · `db:generate` · editing applied
migrations · running the producer without `--draft`.

## STATE, WITH EVIDENCE GRADES
**[MEASURED HERE]** Tower runs `f5b5b10d`; both paper rows `paper_sim` on the
live DB; zero `traderspost` rows of any firm; broker-egress chokepoint live
(CI-enforced); boot probe import-inert. `hmac_secret_encrypted` EXISTS on the
live DB (0128's DDL really ran — catalogue checked, not journal).
**[MEASURED HERE]** PR #27 merged `7dd27a0d` after 19/19 green; parity check
`EXIT=0 CLEAN` at this desk; schema.ts carries the hand-added column.
**[MEASURED HERE]** Ledger branch on origin; every ruling through R-408 pushed.
**[RELAYED — producer]** runs but refuses to publish (`REVIVAL_FAMILY`, exit 2)
— a PASS: stale by refusal, not neglect.
**[MEASURED — worker]** draft mode honest (3 runs byte-identical; `SystemExit`
gate, `python -O`-proof).
**[UNPROVEN]** FVG flag yield (experiment running) · `liquidity_levels`
engine-side computation path · `_LS6` honest ceiling (hyp: ≤5/6).
**[UNENUMERATED]** the other 21 unbound conditions' cause-split (parked, off
the gate path) · whether `session_range` recognition was live in the 07-21
artifact run · 0x9F bugcheck driver · deploy records as standing mechanism.
**[MEASURED HERE]** Suite baseline 13,440 / 9 known failures — tripwire.
`migrations/schema.ts` snapshot: hand-maintained, **never `db:generate`**.
Operator is a THIRD WRITER on runtime-production — `status -sb`, never
one-directional rev-list.

## KNOWN-BENIGN (do not investigate)
`M src/engine/tests/fixtures/session_windows_parity.json` phantom · monitor
event naming an OLD AR = torn mid-write read · 3 red CI badges on one PR = one
defect mirrored · `.playwright-cli/` = operator tooling · **`| head`/`| tail`
MASK EXIT CODES — rerun unpiped before believing any exit status.**

## HAZARD REMOVED (R-394) — do not recreate
`wt-codex-transcript-vault/node_modules` junction into the RUNNING tower's
`node_modules`: CUT, target intact, 0 junctions point at runtime-production.
~25 others point at the campaign checkout — same shape, lower stakes.

## OPERATOR-FACING
Nothing waits on you. Today: broker-safety chain deployed and verified in
production · Phase 1 measured (0/16) and proven EXITABLE through the CLDE
recipe · the build ahead is wiring existing parts, not inventing new ones ·
PR #27 merged. 3am-agent decision on record: GPT-5.6 Sol on flex (retry +
fallback tier required; first nights observe-only). `.claude/skills/` is still
disk-only, no backup.
