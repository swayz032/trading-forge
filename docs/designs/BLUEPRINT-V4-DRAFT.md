# BLUEPRINT v4 — DRAFT (money-path acceleration plan)

> **STATUS: DRAFT — NOT LAW.** Authored 2026-07-29 at the operator's request by a
> consulting seat (Fable), for the seated advisor to execute. **Ratification path
> (in order): (1) adversarial red-team pass (R-062 standing rule) — ★ COMPLETE
> 2026-07-29, `accuracy-validator`, disposition NEEDS-CORRECTIONS, every finding
> resolved in this revision (§13) · (2) external GPT read, carried as `[EXTERNAL
> OPINION]` (operator standing order 2026-07-29) · (3) the seated advisor adopts
> by ruling and updates BOTH carriers (`ADVISOR-STATE.md ## THE PLAN` +
> `advisor-onboarding` §1a).** Until then, v3/v3.1 govern unchanged.
>
> **Nothing here repeals a v2/v3/v3.1 line.** R-070 §3: v3.1 amends and extends
> v2; no later ruling repealed any v2 line. v4 = the same ladder, decomposed to
> owned tasks, plus the missing exit criteria, plus repairs of two measured
> carrier erosions.
>
> **Evidence basis of this document:** targeted ledger reads by three research
> agents this session (line-numbered excerpts of R-053..R-062, R-070/072/075/077,
> R-303, R-396/397, R-401, R-405, R-409, R-415, R-425..R-434, R-437..R-441,
> AR-410..AR-414, STRANDED-CAPABILITY-REGISTER.md) + direct reads at this desk
> (ADVISOR-STATE.md in full · transcript-extractor.md:160-177, :608-621 ·
> forensics module file existence). Grades marked per claim. Figures inherited
> from the ledger are **[ARTIFACT-SOURCED]** unless marked otherwise.

---

## §0 — POPULATION GLOSSARY (three populations; never merge them)

| Name | What it is | Size | Source |
|---|---|---|---|
| **tier-A / spearhead** | depth lane; first-dollar critical path (R-055 §1) | **11 specs**, 53 load-bearing conditions, 26 UNTYPED, EXACT-NOW=0 (R-303) | ledger L2250 |
| **corpus_A** | shakedown set under `docs/replay-results/` | **16 specs**, 155 taught conditions (128 bindable + 27 unbound) | R-401 L2128 |
| **POP-120-LIVE** | the live library | **120 rows = 40 videos × 3 markets, triples byte-identical — size per-video, always** | state file [MEASURED HERE by prior desk] |

R-259 banked the distinction as **11-vs-18**; the shakedown corpus is **16 by
R-401's artifact** — cite the pair with its history, never today's number under
the old ruling. **[UNENUMERATED]** the exact overlap map between the
three populations — any task below that spans populations must state its own.

---

## §1 — THE CRITICAL PATH, NAMED (why the wall has not moved)

**Phase-1 exit (R-055 §5, verbatim, BOTH legs):** *"≥1 tier-A spec compiles with
ALL load-bearing conditions concretely bound AND the compile-fidelity forensics
gate passes calibration."*

The measured chain that decides everything:

1. **[ARTIFACT-SOURCED, R-401]** 0/16 corpus_A specs fully bound; flags-off 0/155
   conditions bound_and_concrete; flags-on hypothetical 6/155.
2. **[ARTIFACT-SOURCED, R-409]** Phase 1 is **NOT exitable on corpus_A as
   extracted**. The pre-registered corpus_B clause FIRED: next move is
   **extraction-side (corpus_B respin with referent capture)** — charter: re-type
   the mis-types, capture the referents, quantify-or-drop the adjectives.
   corpus_B is **on the critical path** by ruling.
3. **[RELAYED — R-426's own grade on this figure, which it acted on anyway]**
   `C8` (extractor-manufactured conditions) is
   **51.1% of all blockage and the only class that unlocks anything alone**:
   cumulative C8→6 · +C3→15 · +C2→27 · … · +C9→120 (row counts; 120 rows = 40
   videos ×3 — the per-video decomposition of the first 6 is **[UNVERIFIED]**).
   Fixing the entire session gap alone unlocks **zero** (37/40 videos also carry
   a C8 refusal).
4. **[MEASURED HERE, this session]** the C8 cause is the extraction brief itself:
   `transcript-extractor.md:169` (≥3-factor quota, "re-scan before accepting a
   1-or-2-factor extraction") · `:171` ("Bias toward INCLUSION when in doubt") ·
   `:616` (each step of a sequential workflow emitted as a confluence factor —
   which also flattens "A then B then C" toward one-bar AND semantics, the
   meaning-change the desk forbids downstream). R-427: a SPECIFICATION defect —
   fixing the parser would change none of those instructions.
5. **[ARTIFACT-SOURCED, R-429 L942]** the C8 fix is **HELD on seven prerequisites
   — and as of this draft ALL SEVEN ARE OPEN with NO OWNER ASSIGNED.**
6. **[ARTIFACT-SOURCED, R-397]** "the missing piece is the BINDING LANE, not the
   detectors"; build order ratified (R-303): **SMC first**, then ORB+RANGE_EVENT,
   then BAR_TIMING — **SESSION_CLOCK later RETIRED as a standalone build (R-408);
   do not rebuild it from the stale ladder.** Completing SMC completes ONE spec.
7. **[ARTIFACT-SOURCED, R-415]** two binding lanes exist (campaign 160,049 B vs
   production 35,046 B; they already disagree on ≥1 binding; production emits a
   false `approximation=False`). Registered gate, assigned to the advisor seat:
   *"Before the first Band-C backtest is run, this desk rules which binding lane
   is authoritative and the executing tree carries it."*

**Therefore the fastest honest path to Phase 2 is a finite, currently-unowned
list:** discharge the seven C8 prerequisites (items 2–3 ARE the packet
ratification — one milestone, not two; items 4–5 run against the candidate fix)
→ corpus_B respin on a TARGETED slice → complete the SMC binding lane on the
target spec → rule lane authority → re-affirm the forensics leg → declare exit in
the pinned citable form. Everything else is a parallel lane and must be labeled
one.

**What "faster" is NOT:** relaxing any refusal class, widening any tolerance,
moving any goalpost, or counting a higher pass-rate as success. Directionally
this campaign's compiler may only ADD refusals until validated replacements
exist (standing, R-434 era). Speed comes from aiming, owning, and shipping —
never from loosening.

---

## §2 — CARRIER-EROSION REPAIRS (measured this session; fold into both carriers at ratification)

1. **The attribution read has FOUR bins, not three.** R-061 §1 verbatim:
   **{edge-absent · compile-fidelity-loss (approximation residue) ·
   OVERLAY-CONFLICT (house exits vs taught-exit edge) · gate-artifact}**. Both
   current carriers dropped **`gate-artifact`** — the "the instrument lied" bin —
   which tonight's session (four false greens; a CI step with no path to red)
   proved is the modal real-world failure. RESTORE.
2. **The Phase-1 exit criterion keeps BOTH legs verbatim.** R-401's verdict line
   quoted it without the forensics-calibration leg. The citable form is R-055
   §5's full sentence.
3. **v3-2's "effective-N tuples distinguish arms"** (honest trial counting)
   dropped out of the carriers. RESTORE — it is the anti-double-count law for
   every dual-arm read.
4. **Label hygiene:** the tags `v3-1..v3-5` exist only in the carriers, not in
   the ledger (R-061 numbers them 1..5; "v3.1" is R-062, a different ruling). A
   future grep of the ledger for `v3-` finding zero hits is EXPECTED, not a
   vanished blueprint.
5. **Carrier discipline rule (new):** this file is the canonical plan once
   ratified; carriers duplicate the LADDER verbatim and POINT here for detail.
   A summary rewritten by hand erodes — measured twice (3-of-5 upgrades lost
   2026-07-28; the fourth bin lost by the next day).

---

## §3 — PHASE 1 DECOMPOSED (owners, gates, red-proofs)

> Standing law carried: R-409 — *"a SURVIVAL verdict at layer N is never a
> REACHABILITY verdict at layer N+1 — enumerate the next layer's blockers before
> promising a gate."* Each task below names its layer.

### 1A — C8 prerequisite burndown (seven items, R-429 L942 — assign ALL now)

| # | Prerequisite (verbatim intent) | Proposed owner | Shape |
|---|---|---|---|
| 1 | Consumers of the `≥3` quota **enumerated** | worker | mechanical; **compute the TRANSITIVE closure, not the grep** (R-441 law), publish surfaces + exclusions |
| 2 | Two-arm ablation **defined and authorized** | seated advisor | pre-registration doc; includes the R-429 pre-registered trap: **conditions-per-strategy WILL DROP and that is the fix working — declared before data** |
| 3 | **Independent grader assigned** | seated advisor | name `accuracy-validator` in the authorizing ruling (doer ≠ grader) |
| 4 | Control arm proves the C8 classifier **FIRES at ~51%** | worker + grader | run the ablation control; instrument-audit first (reproduce a published census figure before believing new output) |
| 5 | Treatment arm proves genuine market-state conditions **PRESERVED** | worker + grader | the load-bearing discriminator; per-video counts, never raw rows |
| 6 | Annotations **stored, never discarded** | worker | rides in the respin harness |
| 7 | Artifacts **immutable, versioned by `spec_hash`** | worker | migration-author immutability manifest applies |

Stop conditions carried unchanged: no C8 implementation before all seven; **no C8
remedy at the binder or preflight** (extractor only — R-426 L1171).

### 1B — Aim before firing: unlock-distance ranking (NEW, cheap, do it first)

From the frozen census artifacts, per spearhead spec: blocking conditions by
class under a {C8-fixed} counterfactual → ranked list (spec · distinct videos ·
residual non-C8 blockers). **Instrument audit:** the ranker must reproduce
R-426's published cumulative chain (C8→6 · +C3→15 · … · +C9→120) before its
ranking is believed. Output picks the corpus_B respin slice — expected to be the
SMC-first spec(s) per R-303 plus the C8-only-blocked videos. **Owner: worker.
This converts "re-extract and hope" into "re-extract these named videos,
expected to fully bind spec X."**

### 1C — Corpus_B charter + targeted respin (the R-409 charter, executed)

Charter content pinned by R-409: re-type mis-types (e.g. CLDE [3], a retest
typed WAIT_BIAS) · capture referents · quantify-or-drop adjectives (e.g. CLDE
[9] "displacement, shouldn't be choppy" — any invented threshold is the
campaign's number, never the teacher's: **quantify from the source or drop as
UNRESOLVED_SOURCE_AMBIGUITY**). Respin runs the FIXED extractor brief (the 1A
packet), on the 1B slice only — not the 40-video library. Extraction fidelity is
gated by the `transcript-audit` procedure per video (probe-green is not the
gate). **Owner: worker, after 1A prerequisites discharge. Grader:
`accuracy-validator` on the packet.**

### 1D — Binding-lane completion on the target spec (the R-397 promotion, kept)

SMC binding build first (R-303 order); 26 UNTYPED / 27-of-53 BOUNDARY conditions
get typed at re-extraction (1C) or refused honestly. Any flag graduation rides
`ratify-packet` with independent grading. **Layer discipline:** binding
completion is claimed only per condition per lane, tree named.

### 1E — Lane authority PULLED FORWARD (repair of a scheduling trap)

R-415 gates the first backtest on the lane ruling. v4 pulls it **before the
corpus_B binding measurement**: exit numbers computed in a lane that will not
run Phase 2 are `MEASURED ≠ MEASURED-WHERE-IT-RUNS` — the desk's own convicted
shape. Sequence: worker delivers the structural inventory of the 160KB↔35KB
divergence (byte-anchored categories, registered deliverable of R-415) → the
seated advisor rules which lane is authoritative on **compiler correctness,
never on which lane produces better numbers** (criterion pinned R-415) → the
executing tree carries the ruled lane → the levelzone pair
(`TF_LEVELZONE_ROUTING/RESOLVER`, register rows 1-2) ships or retires WITH that
ruling, acceptance = the six pinned condition identities, never the count.

### 1F — Forensics leg re-affirmed + exit declaration

Tooth-1 is real code (`src/engine/forensics/compile_fidelity.py`,
`calibration_battery.py` — **[MEASURED HERE]** files exist) and was
**STATUS_CALIBRATED on 2026-07-21 (R-278), 7/7 mutation classes convicted,
independently reproduced — at the detector-LOGIC layer**, with written scope:
zero production callers, certificate-independence untested, a producer-consumer
schema mismatch, and a DEAD certified reader vintage (R-294) parked at the
REDESIGN/go-live step. For Phase-1 exit: **re-run the calibration application on
the exiting spec's vintage** (cheap; definition already verified in code,
R-344), and keep the wiring debts explicitly on the Phase-2 entry checklist
(§4). Exit is then declared ONLY in the pinned citable form (R-401), both legs,
tree named, per-video counts. **PROPOSED TIGHTENING (red-team F1; pin at
ratification): the forensics leg counts only when re-affirmed in the lane ruled
authoritative for Phase 2 (§3-1E) AND after the producer-consumer schema
mismatch is fixed — a calibration that may not describe the data shape Phase 2
will feed it is a layer-N proof offered at layer N+1, this campaign's
twice-convicted shape.**

---

## §4 — PHASE 2 (BATTERY/WAVE) — entry checklist + the missing exit criterion

**v3 content preserved:** upgrade 1 (four-bin failure-attribution read,
pre-registered before any verdict is interpreted) · upgrade 2 (overlay A/B,
taught-exit specs only, effective-N tuples).

**ENTRY CHECKLIST (new — Phase 2 begins when ALL are receipted):**
1. Lane authority ruled and the executing tree carries it (R-415, via §3-1E).
2. **Battery-rig NULL-CALIBRATION (v4-new):** the wave rig has never fired
   (backtests total = 0, four tables). Before the first real wave, the rig runs
   a fixture spec with a hand-computable expected outcome, plus (a) a planted
   compile-infidelity the forensics leg must CONVICT in-wave and (b) a planted
   rig defect the attribution read must file under `gate-artifact`. **RED
   without, GREEN with — a rig that has never gone red is not an instrument.**
3. Reader-vintage successor certified (R-294: the certified Leg-B reader's model
   no longer exists; R-075 protocol governs succession) — currently invisible in
   both carriers; surfaced here.
4. Attribution read + overlay A/B pre-registrations frozen (drafted during
   Phase 1, so Phase-2 entry is same-day, not a paperwork phase).
5. Tooth-1 wiring debts from §3-1F receipted: certificate-independence verify
   (the schema-contract fix moves UP into Phase-1 exit under §3-1F's proposed
   tightening).

**EXIT CRITERION (PROPOSED — R-055 §5 promised per-phase exits; Phases 2/3/3.5/4
were never written; these four fill that hole, to be pinned at ratification):**
*Phase 2 exits when the first battery wave over ≥1 fully-bound tier-A spec
completes with (a) the null-calibration receipt on record BEFORE the wave, (b)
every failure carrying its four-bin attribution, and (c) EITHER ≥1 survivor
passing pre-registered gates OR an attribution-complete no-survivor verdict
whose named cause routes work back (extraction / binding / overlay / rig). Both
(c) outcomes are valid exits. An unattributed failure is not an exit — and the
attribution itself is INDEPENDENTLY GRADED (doer ≠ grader) with a receipted
`gate-artifact` hunt before any `edge-absent` verdict is accepted: a seat under
pressure defaults to the easiest bin, and this session proved the instrument is
the modal liar.* Verdicts
scoped corpus+battery+engine+snapshot; the VIDEO is the independence unit; a
pre-registered power floor (value pinned at ratification) below which no
per-class claim is published.

---

## §5 — PHASE 3 (CONVEYOR) — preserved + sharpened

Conveyor, never a queue (R-055 §3): internal-paper + shadow-accumulation
concurrent per strategy; ~20-signal shadow floor overlaps the paper window;
multiple strategies at different stages. **Eval-odds pre-compute** (v3 upgrade 3)
at pre-flight: B14/survival machinery aimed at the EVAL's own parameters per
survivor → per-attempt pass probability AND expected duration from the
survivor's own P&L distribution — with R-061's revision carried: **evals can
pass in DAYS at bot cadence** (supersedes R-060's "1-2 months" phrasing).

**Purchase timing unchanged and un-nagged (R-060 verbatim intent):** the Combine
+ TopstepX API purchase happens when operator funds allow; briefing is
informational, never a countdown. **R-077 fold-in:** practice is free WITH the
Combine — **one purchase opens both doors** (adapter-certification sandbox AND
the eval); survivors keep — a graduate waits in paper without expiring.

**EXIT (PROPOSED):** *≥1 survivor completes paper + shadow floor with parity
inside pre-registered bands, its eval-odds briefing delivered, and its
in-season status determined → operator purchase briefing on record. Exit is the
briefing, not the purchase — the wallet is never assumed.*

---

## §6 — PHASE 3→4 SEAM — deploy-in-season, with its hidden prerequisite named

v3 upgrade 4 preserved: survivors deploy only when their forensics-named regime
is LIVE per the running classifier; out-of-season survivors hold in paper
standby. **Named prerequisite (was implicit): the regime classifier is
structurally dormant today.** Standing policy applies: ship it to SHADOW, not
OFF, well before the seam — a wire to a dormant classifier is deploy-never, and
a rushed classifier at the seam is deploy-blind. Regime-drift demotion remains
the post-deploy guard.

---

## §7 — PHASE 3.5 (FIRST THIRTY FUNDED DAYS) — preserved with its money-math

Drafted BEFORE funding (R-055 §4). Contents pinned by R-056/057/058 (the
JOINT-optimization clause below is R-061 §5's): the
**Consistency-lane election** (advisor recommendation on record: caps
$6K/$8K/$12K vs Standard $4K/$6K/$10K, ~2× payout clock; operator decides at
election with audit-verified terms) · **dilute-don't-taper** (window extension
is the lever; a monster day costs timing, not dollars; taper = last resort near
a request deadline; re-elect per account on measured P&L shape if switchable —
audit verifies) · **DLL-toggle mandatory capture** (in-repo docs first, live
pages confirm; part-of / prerequisite-to / separate-from the election must be
resolved) · payout-cadence × 20/80-reserve-unlock × drawdown-room-sizing JOINT
optimization decided calm · live verification that every auto-guard fires on
the funded account · **stop-gates symmetric to go-gates** (v3 upgrade 5): eval
failed 2× → attribution loop, never a blind retry; funded loss-streak → the
pre-written post-mortem before any redeploy.

**EXIT (PROPOSED):** *thirty funded days with zero firm-rule breaches, every
auto-guard either observed firing live or affirmatively exercised, payout
cadence either executed per the elected lane or — if no natural qualifying
window occurred in the thirty days — affirmatively exercised with a test
request so the request/receipt cycle is proven, and no stop-gate tripped twice
without attribution → Phase 4 scaling unlocked.*

---

## §8 — PHASE 4 (SCALE) — doctrine already ruled; carried so it stops being invisible

R-059 verbatim doctrine: within-account = contract pyramid on micros (base
9/9/18, +3 per proven tier, ceiling 50 micros); account size locked to the
passed Combine forever; cross-account = HORIZONTAL up to the firm's per-trader
limit (operator plan ~5), copy-scaled. Any account-size-upgrade-ladder structure
is fiction (swept once; keep it dead). Cold-recovery drill receipt (R-062 #1,
owned by the ops lane) is required **before Phase-4 scale**.

---

## §9 — PARALLEL LANES REGISTER (bounded; NONE may block the spearhead)

| Lane | State | Bound |
|---|---|---|
| CI honesty (ubuntu-only failures → order-dependent test → plugin-presence step → pytest BLOCKING) | in flight at the seated desk; order already correct | governance; AR-414 preconditions; **never a merge gate for spearhead packets** (R-440 anti-gate-creep) |
| Session-role resolver yield (`C2`, register row 3) | assigned to advisor seat; RUN IT FIRST | post-C8 **multiplier** — C2 alone unlocks 0; measure cheap now, port later |
| Semantic role classifier migration (register row 5) | R-432 packet protocol; HOLDOUT-26 covenant intact | validity lane, OFF the Phase-1 critical path; never a Phase-2 gate |
| `trigger` provenance packet | queued (R-441) | documentation/provenance; fail-closed regardless |
| WIRE1 HTF columns (register row 4) | confirm R-080 withdrawal covers it → RETIRE the row | retirement is a disposition, not a deletion |
| Compliance-refresh audit (R-054 §1) + reader-succession residue | open per amendment chain | required before Phase-3.5 election / first candidacy respectively |

Standing rule kept: no new detector/binder/classifier work is commissioned until
the STRANDED-CAPABILITY-REGISTER is consulted in the authorizing ruling.

---

## §10 — THROUGHPUT LAWS (where the acceleration actually comes from)

1. **Kill the false-green tax.** Four false greens in one session; a CI step
   with no path to red for 4+ hours. Every mutation asserts its own edit TOOK;
   completion signals are never results; exit codes captured unpiped. The CI
   lane (§9 row 1) is the structural fix.
2. **Aim before firing.** The unlock-distance ranking (§3-1B) spends one cheap
   measurement to make every expensive step targeted.
3. **Ship before building.** Three of five stranded capabilities aim at the
   three largest blockers — but shipping all three unlocks ZERO strategies until
   C8 clears. Sequence: C8 first; ports as multipliers, with yield measured in
   the executing lane before any port is designed.
4. **Assign every prerequisite.** The seven C8 prerequisites sat unowned; a
   prerequisite assigned to nobody is a stall order (advisor-ruling §0.5).
5. **Seat hygiene.** Swap seats at natural boundaries; schedule heavy measured
   runs off the account's peak-limit window (the 22:30–23:30 usage-limit hour is
   measured); keep packets small and graded.
6. **Carrier discipline** (§2.5): ladders duplicated verbatim, detail pointed
   at, never re-paraphrased.

---

## §11 — WHAT THIS PLAN DOES NOT PROMISE

No step above manufactures edge. Phase 2 may return an attribution-complete
no-survivor verdict; under this plan that outcome ROUTES (to extraction, binding,
overlay, or rig) instead of killing the mission or tempting a goalpost move.
"Out-of-this-world results" are not a deliverable any honest desk can pre-commit
to; **what is deliverable is that every week of work lands on the critical path,
every claim carries its grade, and the first battery wave — when it fires — is
believable.** That is the breakthrough available by engineering.

## §12 — OPEN / UNVERIFIED (named so they cannot masquerade as settled)

- The per-video decomposition of R-426's "C8 alone unlocks 6" **[UNVERIFIED]** —
  1B computes it.
- Disposition of R-409's authorized items (FVG fidelity check · flag-yield
  sweep · corpus_B charter draft) — **[UNENUMERATED]** whether each completed;
  the seated advisor reconciles before 1C dispatch.
- Population overlap map (§0) — **[UNENUMERATED]**.
- The remaining 160KB↔35KB divergence beyond one measured row (R-415) — the 1E
  inventory closes it.
- Whether `spanOk=False` rows carry usable evidence (AR-413 tail) — a grading
  act, advisor-owned.
- This document itself: red-teamed (§13); **externally UNREAD** until
  ratification steps 2–3 complete.

---

## §13 — RED-TEAM DISPOSITION (ratification step 1 receipt)

Adversarial pass 2026-07-29 (`accuracy-validator`, full read access, brief =
refute; the ledger moved during the audit — R-442/R-443 landed — so the seated
desk is live and this file deliberately touches neither relay file).
**Disposition returned: NEEDS-CORRECTIONS. Every finding resolved in this
revision:**

- **F1** forensics-leg gameability → §3-1F proposed tightening (authoritative
  lane + schema fix before the leg counts).
- **F2** attribution gameability → §4 exit requires independently graded
  attribution + receipted gate-artifact hunt.
- **F3** stale build order → SESSION_CLOCK retirement (R-408) annotated in §1.6.
- **F4** cold-recovery-drill owner: red-team read R-062 ("working agent"), draft
  said ops lane. **RESOLVED BY MEASUREMENT AT THIS DESK [MEASURED HERE]: R-063
  (ledger L6603) transfers R-062 items #1 and #6 OUT to the ops pair — "Our
  agent drops them from its carry list." The draft stood; the red-team had not
  read six rulings past its anchor.** Disagreeing graders resolve by
  measurement, never by averaging.
- **F5** figure drift → §0 cites 11-vs-18 (R-259) beside 16-by-R-401.
- **F6** vacuously-satisfiable payout clause → §7 exit gains the
  affirmative-exercise fallback.
- **F7** false discreteness → §1 chain annotated (prereqs 2–3 = the
  ratification milestone).
- **F8** grade fidelity → §1.3 re-tagged [RELAYED], matching R-426's own grade.
- **F9** citation umbrella → §7 attribution split (R-056–058 + R-061 §5).

**Confirmed sound by the same pass:** the Phase-1 exit criterion verbatim-exact
against R-055 §5 · the critical path (an active shorter-route hunt via the
zero-boundary ORB spec found none — R-303 §5 had already tried and rejected it)
· all named artifacts, worktrees and code lines exist where claimed · no
explicit relaxation anywhere. **Not covered by it:** R-278/R-294/R-344 read
directly · AR bodies · R-430/431 · `dual_denominator_remeasure.py` internals —
a later reader inherits those as open, not as cleared.
