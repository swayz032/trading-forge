# H1 Wave-3 — TERMINAL RESULT: human-in-loop fork FIRES (instrumentation + economics grounds) 2026-07-12

The FINAL corrected rebuild VOIDED on both binary checks (reproduced twice, real Gemma). Per the operator's pre-committed FINAL-attempt rule, the entanglement is ruled IRREDUCIBLE and the human-in-loop fork fires. No third rebuild. This is a pre-written, legitimate H1 sub-answer, not a failure.

## The corrected rebuild (faithful to the amended spec) — VOID
Built exactly as ruled: 5-option schema RESTORED byte-identical to the checkpoint, neutralizer `emitted ambiguous → cannot-determine` (subtractive, NOT →context), structural pre-call decline gate untouched (declines exactly 5/112). Self-verified twice with real `gemma4:e4b-it-qat`, independent fresh caches:
- **(i) BOUNDARY BYTE-IDENTICAL: FAIL** — 10 (run 1) / 11 (run 2) gate↔context flips among NON-declined items vs frozen pass-1 (W1-0044/0087/0133/0159/0174/0183/0185/0216…). The boundary drifts.
- **(ii) NEUTRALIZER DORMANT: FAIL, badly** — the model emitted `ambiguous` **24 / 22 times** (~25% of LLM calls; target 0). A dominant sampling mode, not noise. Total routing 29/112 & 28/112 (5 structural + ~23 neutralized).
- **Birth-gate: FAIL** — W1-0147 emits `ambiguous` → declines instead of gate-strength (both runs). Segmentation exact; structural-gate routing = exactly 5/112 (correct/unchanged).

## THE ROOT CAUSE — a load-bearing premise of the amendment is FALSIFIED BY EXECUTION
The amendment assumed: *restore the 5-option schema and the neutralizer is DORMANT, because `ambiguous` was never the argmax in pass-1.* **This is empirically FALSE.** The recovered `h1-tier2-design-pass1-raw.json` + checkpoint header confirm: **pass-1's routing-0.0 came from FORCE-DETERMINATION — gemma REFUSED the offered decline option — NOT from a dormant/unperturbed distribution.** With the 5-option schema live + an honest decline path, this Gemma makes `ambiguous` the argmax ~23×, and the non-declined boundary STILL drifts 10-11 items.

**So the VOID review's central claim — '`ambiguous` was never argmax in pass-1, so restoring it is boundary-neutral' — is falsified by execution.** (Two-path rule: the build's execution refuted the ruling's premise — the alarm fired at the right seat.)

## Why the entanglement is IRREDUCIBLE (no schema-side fixed point)
- Pass-1: routing 0.0 — but ONLY under force-determination (decline refused). NOT §1-conformant (no honest decline path).
- Rebuild-1 (remove `ambiguous`): boundary perturbs, 16 flips, absorption UP. VOID.
- Rebuild-2 (restore `ambiguous` + neutralizer): model declines ~23×, boundary drifts ~10. VOID.
- **There is NO schema-side fixed point.** Pass-1's routing-0.0 boundary exists ONLY under force-determination; the moment you add a live honest decline path (any schema that offers it), gemma uses it and the boundary moves. Removing the option flips one way; restoring it flips the other + declines. The one thing that could suppress declines — editing the schema — is exactly what the amended ruling (correctly) forbids. The schema IS the boundary; a conformant decline path and a pass-1-stable boundary cannot coexist for this model on this pool.

## THE VERDICT — human-in-loop fork, on TWO independent grounds
1. **Instrumentation:** the tier-2 discourse pass CANNOT be conformantly instrumented (no schema-side fixed point for live-decline + stable/attributable boundary; 2 rebuilds, both void). Proven by execution, not argued.
2. **Economics:** the moment the decline path is live, routing ≈ 0.26 (28-29/112) = **~5-9 tier-3 adjudications per video**, far above the ≤1/video rider. Even setting conformance aside, the economics do NOT close.

## WHAT H1 THIS MEANS (pre-reg §2 — economics miss, NOT a validity failure)
- **NOT a source-thesis falsification** (that is H2's job). The certificate MACHINERY works — Wave-1 shakedown proved 92% inter-rater consistency @ ~1.1K tok/item; tier-1 deterministic detection works (Wave-2, honest 0.818 gate-pass). What fails is CHEAP-CONVEYOR economics for the discourse tier.
- **H1 NARROWS to 'fidelity achievable only at human-in-loop cost'** — the pre-written smaller-library / higher-per-strategy-investment fork. The conveyor produces certificates, but at ~5-9 human/tier-3 adjudications per video, not ≤1. Smaller, more expensive, STILL certifiable.
- **The sealed sets stay PRISTINE** — the sealed 77 and the Wave-5 16-video sealed set were NEVER opened; there was no conformant instrument to read them on, and the fork fires on the design-pool instrumentation result per the operator's pre-commitment. They are preserved for the human-in-loop fork's own read.

*Terminal. doer≠grader held to the end: two builds, three independent reviews, all void-or-verified by ablation/execution; the wave converted H1's open economics question into a hard, honest number. Downstream (execute the human-in-loop fork / Phase-2 H2 under human-in-loop economics) is the operator's call.*

## §INDEP-CONFIRM — independent reviewer VOID (doer≠grader closed on the terminal verdict)
The FINAL verdict is not builder-self-reported alone. A separate independent reviewer re-derived it via TWO non-overlapping measurement paths (the harness `--json` run + an independent driver capturing per-item `{label, resolved_by}`) + 26 targeted reproducibility reruns (**26/26 self-agreement** — rules out temp=0.1 noise) + confirmed the model weights predate pass-1 (no silent drift). Both builds → both VOID by independent ablation/execution.
- **F-1 birth-gate:** W1-0147 → cannot-determine, 5/5 reproducible. Disqualifying alone (pre-reg §4).
- **F-2 boundary:** 11 non-declined mismatches vs pass-1 (not 0). **Nuance: 10 of 11 flips RAISE correctness** (pass-1-wrong → consensus-match) — but per the scope-lock, an absorption RAISE voids *regardless of whether it looks like an improvement*, because a shifted boundary is unattributable. VOID stands.
- **F-3 neutralizer:** 26/112 (23.2%) llm-margin declines (not 0); total decline 31/112 (27.7%). The 0%→28% routing shift IS the schema-shift signature.
- **Structural gate ablation:** direction-safe in isolation (0/5 forced-wrong) — but the boundary drift + declines live in the margin path the gate doesn't touch, so it can't excuse them.
- The pass-1-producing module was never committed/vaulted, so "boundary byte-identical" can NEVER be diff-verified again — only empirically, which failed twice. That unrecoverability is itself part of why the entanglement is terminal.
Independent reviewer disposition: **fire the human-in-loop fork; do not license a further rebuild.** Matches the terminal verdict. Fork CONFIRMED.
