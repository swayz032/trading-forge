# EXTERNAL READ — 2026-08-04 — AR-787 / LANE 30 / 80-vs-81 DISPOSITION

> **PROVENANCE:** Arrived as **OPERATOR-RELAYED CHAT**, not on
> `origin/external-advisor/gpt-rulings` (stale at `953a907c`, 2026-08-02, per `R-698`).
> Committed here VERBATIM under `R-700 §1`: `AN ADOPTION BY REFERENCE TO A SOURCE NOT IN
> THE REPOSITORY IS AN ADOPTION OF NOTHING.` Consumed by `R-709`.
> **A CHANNEL IS NOT AN AUTHOR** — `R-709` adopts on merit and records what it refuses
> (the `Gate 1..5` scale, refused a SEVENTH time) and what it found the read did not
> (the missing canonical-population MANIFEST, `R-709 §4`).

---

GPT EXTERNAL ADVISOR RULING — ACCEPT AR-787; CANONICAL POPULATION SUBSTITUTION APPROVED, HISTORICAL 80-VS-81 DIFFERENCE CLOSED AS UNRECOVERABLE

AR-787 is accepted.

The substitution was the correct call.

The historical 80-file and 81-file populations cannot be honestly reconciled because:

- neither population was committed;
- neither generating script exists in repository history;
- only prose descriptions survive;
- an independent reconstruction produces neither population;
- a third reconstruction cannot determine which member differed between two lost instruments.

Naming a historical differing member from the new 93/95-member population would be fabrication.

HISTORICAL DISPOSITION

Close the 80-versus-81 obligation as:

`UNRECOVERABLE_INSTRUMENT_PROVENANCE`

Approved statement:

> The former 80-file and 81-file regression populations cannot be reproduced or compared by member because their generating instruments and exact member lists were never preserved.

Do not claim:

- the populations were equivalent;
- one included the wrong file;
- the current canonical population recreates either historical set;
- the historical discrepancy has been technically explained.

The original reconciliation request is superseded because its required evidence no longer exists.

CANONICAL REPLACEMENT APPROVED

The newly committed deterministic population is approved as the sole regression population for this compiler surface going forward, provided it includes:

- a repository-contained generator;
- deterministic normalized repository-relative paths;
- committed derivation rules;
- committed member output or reproducible manifest;
- a non-empty population assertion;
- a known inclusion control;
- a known exclusion control;
- a break-control proving member differences are detected;
- a test that running from a different working directory produces the same population.

Its adoption begins at commit:

`b8321dc9`

It must be described as a new canonical baseline, not a reconstruction of the historical 80 or 81.

Approved transition record:

> Historical populations retired due to missing provenance. Canonical population established at `b8321dc9`.

LANES 28–30 DISPOSITION

The three-lane batch is accepted provisionally pending independent re-grade.

Lane 28:

- all enforced handlers classified;
- non-consuming routes refuse;
- `_h_structure` no longer accepts parameters merely to alter cache identity;
- real cache-separation tests now target the consuming `WAIT_BIAS` route.

Lane 29:

- parameter acknowledgement occurs before the short-frame return;
- supplied parameters cannot disappear on frames below 30 bars;
- the pre-existing taught-frame insufficiency refusal is now reachable;
- parameterless short-frame behavior remains unchanged.

Lane 30:

- ordering witness now observes a real execution-side sentinel;
- F-F's "before evaluator" claim is now true and executable;
- both partial-recognition directions are covered;
- the census is repository-anchored and non-vacuous;
- all six producer forms are exercised;
- a deterministic canonical regression population replaces the lost throwaway instruments.

PRE-REGISTRATION MISSES

The two under-specified RED predictions are recorded:

- Lane 29 predicted 7 RED; full absence produced 8.
- Lane 30 predicted 11 RED; the plant produced 13.

These do not invalidate the repairs because the additional RED tests were appropriate detectors and the expected-GREEN populations remained stable.

However, future mutation receipts must distinguish:

- `MINIMUM_EXPECTED_RED_SET`
- `EXPECTED_GREEN_SET`
- `OBSERVED_RED_SET`

Do not claim that a plant proves the complete guard surface merely because it reddens the minimum expected set.

Approved rule:

> A pre-registered RED set is a lower-bound prediction unless the mutation is proven equivalent to complete feature absence.

The final grade must evaluate the observed mutation populations, not only the original predicted counts.

INDEPENDENT RE-GRADE AUTHORIZED

Dispatch `accuracy-validator` against pinned commit:

`b8321dc9`

Expected duration:

`60–90 minutes`

The receipt must state before starting:

- pinned commit;
- start timestamp;
- expected duration;
- runner identity;
- durable receipt path;
- canonical population command and manifest identity.

Required grade scope:

1. Every enforced handler either consumes supported parameters or refuses.
2. No parameter may affect only cache identity.
3. Short-frame returns cannot bypass parameter acknowledgement.
4. Flag OFF cannot accept parameterized bindings.
5. Flag ON cannot accept unused parameters.
6. Complete unknown-key fallback refuses.
7. Both partial-recognition directions refuse.
8. Insufficient-history behavior refuses without default substitution.
9. Refusal occurs before confirmation, evaluator execution, cache creation, and state mutation.
10. The census works from arbitrary current working directories.
11. The census detects all six supported constructor forms.
12. The census fails on an empty scan surface.
13. The canonical regression population is deterministic from a clean checkout.
14. The historical 80/81 populations are not represented as reconstructed.
15. All ten prior mutations and all new Lane-28–30 mutations fail permanent tests.

MANDATORY NOVEL ATTACKS

The grader must independently attempt:

- reclassify `_h_structure` as consuming while leaving its evaluator unchanged;
- use parameters only in a cache key;
- move acknowledgement below the short-frame return;
- move refusal after `candle_confirmation_check`;
- honor fast and default slow;
- default fast and honor slow;
- accept an unknown alias as absent;
- disable the census by changing current working directory;
- make the census scan zero files;
- introduce a positional production parameter constructor;
- introduce an aliased or module-qualified constructor;
- perturb canonical population ordering or path normalization.

Every attack must either fail a permanent test or be recorded as:

`UNCOVERED_HOLE`

GATE-2 CLOSING RULE

Gate 2 closes only on:

`PASS`

or:

`PASS_WITH_BOUNDED_FINDINGS`

with no finding in these categories:

- silent substitution;
- partial recognition;
- unused accepted parameters;
- flag-OFF parameter loss.

A finding about lost historical 80/81 provenance does not by itself block Gate 2 now that:

- it is explicitly classified as unrecoverable;
- no historical equivalence is claimed;
- a committed deterministic replacement exists.

The replacement's correctness and reproducibility must still pass the re-grade.

CURRENT POSITION

Gate 1 — evaluator consumption: COMPLETE
Gate 2 — implementation complete, independent re-grade pending
Gate 3 — typed dispatcher object: BLOCKED
Gate 4 — source lesson to sealed specification: NOT STARTED
Gate 5 — end-to-end breakthrough: NOT STARTED

The correct answer to the external-read question is:

> Do not pursue the historical 80-versus-81 member difference without its original instruments. Preserve the uncertainty, adopt the committed canonical population prospectively, and grade that replacement directly.
