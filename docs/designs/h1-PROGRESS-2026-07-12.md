# H1 — PROGRESS / next-session dispatch (2026-07-12 morning)

## DONE + vaulted (cert ref `corpus-v3-gate3-cert-2026-07-06`)
- Phase 0 CLOSED: 0.5 overlay read certified (@1755fc3a); overlay LOSS-REDUCING near-mechanical, opt-in.
- H1 W0 pre-reg frozen (dd81a2d1) + sealed 16-video set (2bb8ba12, sha 8e39ffe1, PRISTINE).
- H1 W1 shakedown DELIVERED (112a1244): demotion fidelity 67% conf/25% contestable, inter-rater 92%, Afrikaans resolved, tier-2 quarantine split (112 design/77 sealed-eval, PRISTINE).
- H1 W2 tier-1 detectors LANDED (236cc620): guard-stripped honest 0.818<=1.85, false-fire 9.09%, 30/30.
- H1 W3 tier-2 TERMINAL FORK (92b803ad): human-in-loop fork FIRES. 2 rebuilds VOID (schema-boundary entanglement irreducible; pass-1 routing-0.0 was FORCE-DETERMINATION not dormant). H1 narrows to fidelity-at-human-in-loop-cost. NOT source-thesis falsification. Machinery works.
- H1 PILOT pre-reg FROZEN (this session): `docs/designs/h1-pilot-preregistration-2026-07-12.md` — bar >=60% cert-grade on sealed 16, economics reframed to affordability ceiling ~15 adjudications/video, agent-adjudicators, handoff to H2 two-read rule.
- **H1 WAVE 4 LANDED (`e951ca92`, 2026-07-12):** certificate assembler + 5 compile-integrity lints (`src/engine/extraction/compile_lints.py` + `cert_assembler.py` + birth fixtures + 2 test files, 52/52). Cert schema tier-2 field RETIRED (classifying_tier {1,3}); tier-1+tier-3 provenance. **Independent ablation grade VERIFIED band 6, LAND-WITH-CARRY-FORWARDS** (doer≠grader, from-zero re-run, out-of-fixture adversarial birth-gate proof, isolation confirmed no-sealed-set/no-vectorbt, reuse/parity real). Vaulted FF; sealed 16 + 77 sealed-eval PRISTINE.

## PILOT IS GATED — resolve F-1 BEFORE unsealing (operator decision)
**F-1 (CRITICAL, from the independent grade):** the certificate schema §4 names 5 compile-integrity lints, but 3 of them (direction-conflation / unsat-SAT / OR-alternatives) check AND/OR/direction TOPOLOGY that exists only in the ENGINE's compiled DSL spec — a stage DOWNSTREAM of extraction. Tier-1 emits no topology; nothing in the repo produces `ConditionTopology`/`or_branches`/`same_bar_fill`/`signal_lag` at the certificate layer. So in the frozen pilot conveyor (tier-1 + tier-3, no compile stage) those 3 lints VACUOUSLY PASS every certificate, and causality's same-bar leg is also unreachable — only ~1.5 of 5 lints + classification/anchoring have discriminating power. **A pilot run today cannot honestly assert §4's "all 5 compile_integrity lints pass."** Two frozen docs are in tension: phase-1 §4 (all-5) vs pilot §0 (conveyor = tier-1+tier-3 only). Strictest Law-4 reading of §4 = all 5 must actually RUN. RESOLUTION (operator, pre-registered before the read): **(A)** build a compile-stage topology producer + run extraction→compile→assemble so all 5 lints are live (faithful §4 test; instrument work, ratify-packet); OR **(B)** written operator acceptance scoping the pilot's cert-grade to the live subset (classification + anchoring + f2 + causality-regex), with the scope traveling on every certificate (Law 7). Do NOT unseal until A or B is chosen.

## NEXT — the ruled dispatch (operator: fork executes AS the road to H2)
1. **[DONE — LANDED `e951ca92`]** Wave-4 assembler + lints + grade. (F-3 docstring honesty corrected; F-4 assert→raise hardening folded into whichever F-1 resolution touches `assemble_certificate`.)
2. **RESOLVE F-1 (above)** — the pilot's honesty gate. A (build producer) or B (scoped acceptance). Pre-register the choice before unsealing.
3. **HUMAN-IN-LOOP PILOT** — after F-1 resolved: unseal the 16, run tier-1+tier-3 conveyor, read cert-grade fraction ONCE per the frozen pilot pre-reg, independent re-verify. Tier-3 = shakedown blind-adjudication protocol (control-gated, blinding leak-scan before rater dispatch).
4. **H2 handoff** — certs -> full battery on G4 engine -> H2 frozen two-read rule (survivor any-N -> forensics; rate N>=30). h2-source-thesis-preregistration-2026-07-07.md.
5. **PARKED (registered)**: stronger-model cloud tier-2 on the ~30% margin = future pre-reg economics experiment, measured vs adjudication agreement, doesn't touch gemma budget. Waits until fork baseline economics measured.

## Standing laws minted this campaign (skill-amend candidates)
ablation-proves-motivation; vault-before-iterate + pass-accounting-2path; schema-IS-the-boundary + force-determination-no-fixed-point corollary. Plus doer!=grader, two-path-derivation, read-once, flag-law.
