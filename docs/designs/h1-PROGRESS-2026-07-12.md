# H1 — PROGRESS / next-session dispatch (2026-07-12 morning)

## DONE + vaulted (cert ref `corpus-v3-gate3-cert-2026-07-06`)
- Phase 0 CLOSED: 0.5 overlay read certified (@1755fc3a); overlay LOSS-REDUCING near-mechanical, opt-in.
- H1 W0 pre-reg frozen (dd81a2d1) + sealed 16-video set (2bb8ba12, sha 8e39ffe1, PRISTINE).
- H1 W1 shakedown DELIVERED (112a1244): demotion fidelity 67% conf/25% contestable, inter-rater 92%, Afrikaans resolved, tier-2 quarantine split (112 design/77 sealed-eval, PRISTINE).
- H1 W2 tier-1 detectors LANDED (236cc620): guard-stripped honest 0.818<=1.85, false-fire 9.09%, 30/30.
- H1 W3 tier-2 TERMINAL FORK (92b803ad): human-in-loop fork FIRES. 2 rebuilds VOID (schema-boundary entanglement irreducible; pass-1 routing-0.0 was FORCE-DETERMINATION not dormant). H1 narrows to fidelity-at-human-in-loop-cost. NOT source-thesis falsification. Machinery works.
- H1 PILOT pre-reg FROZEN (this session): `docs/designs/h1-pilot-preregistration-2026-07-12.md` — bar >=60% cert-grade on sealed 16, economics reframed to affordability ceiling ~15 adjudications/video, agent-adjudicators, handoff to H2 two-read rule.
- **H1 WAVE 4 LANDED (`e951ca92`, 2026-07-12):** certificate assembler + 5 compile-integrity lints (`src/engine/extraction/compile_lints.py` + `cert_assembler.py` + birth fixtures + 2 test files, 52/52). Cert schema tier-2 field RETIRED (classifying_tier {1,3}); tier-1+tier-3 provenance. **Independent ablation grade VERIFIED band 6, LAND-WITH-CARRY-FORWARDS** (doer≠grader, from-zero re-run, out-of-fixture adversarial birth-gate proof, isolation confirmed no-sealed-set/no-vectorbt, reuse/parity real). Vaulted FF; sealed 16 + 77 sealed-eval PRISTINE.

## F-1 RESOLVED — two-grade ladder frozen + §A repair landed (operator ruling B-repaired + A-as-precondition)
**F-1 was:** cert schema §4 names 5 lints, but 3 structural lints + causality's same-bar leg check compiled-DSL topology no stage produces at the certificate layer → they'd VACUOUSLY PASS in the topology-less pilot conveyor. **RESOLUTION (operator-authored addendum, frozen `72e183d6`):** two-grade certificate ladder — (§A) NO lint vacuously passes: 3-state PASS/FAIL/**NOT_EVALUATED**, structural lints + causality same-bar leg emit NOT_EVALUATED honestly on the artifact; (§B) **pilot-grade** = classify+anchor + live lints (f2 + causality-regex) — the pilot's actual question (fidelity + economics); (§C) **full-grade** = all 5 lints PASS on real topology, and the compile-stage topology producer (option A) is a **pre-registered PRECONDITION of the H2 battery read** (attribution integrity — a pipeline-mangled strategy must not be misread as "source failed"); every pilot-grade cert UPGRADES to full-grade before any battery verdict is attributed to a source; (§D) §4's all-5 kept as the full-grade standard, no waiver. **§A repair LANDED `0b494c92`, independent grade VERIFIED band 8 SAFE-TO-LAND** (doer≠grader own out-of-fixture adversarial law1 + spoof-hunt, presence-not-truthiness confirmed, full_grade proven unreachable in pilot conveyor by design, 59/59). Carry N-1 (LOW, dormant until §C): pilot_grade ignores a structural-lint FAIL when topology present. **Survived a mid-session power-outage git-index corruption (index rebuilt from HEAD, all working files intact, re-verified 59/59.)**

## NEXT — the ruled dispatch (operator: fork executes AS the road to H2)
1. **[DONE — LANDED `e951ca92`]** Wave-4 assembler + lints + band-6 grade.
2. **[DONE — F-1 RESOLVED]** Two-grade addendum frozen `72e183d6`; §A NOT_EVALUATED repair landed `0b494c92`, band-8 grade.
3. **[DONE — LANDED `634151ec`, band-6 VERIFIED SAFE-TO-LAND]** Pilot conveyor runner (`src/engine/extraction/pilot_conveyor.py` + 20 tests): two-phase `prepare_video`/`finalize_certificate` glue, non-adjudicator by construction (AST import-walk clean), Wave-1 blind leak-scan (parity-fixtured, Set-A controls reused verbatim), transcript-fetch seam documented (Node `fetchTranscriptWithRetry`, not VPN), dry-run proves schema-valid DRY-RUN cert `full_grade=False`. Independent grade caught 2 findings ↓.
4. **[DONE — RESOLVED via Addenda 2+3, landed]** SEGMENTATION (was: `_segment_transcript` unregistered instrument, moved numbers + silently dropped conditions). Resolution: Addendum 2 (extractor's condition IS the unit, F-5-class) + Addendum 3 (A-prime anchor-locator). Landed: anchor-locator `2b278757` (band-8), conveyor rework `7a937e17` (band-7 mechanical, extractor-as-unit + locator wired, real dry-run 6/6 located N=1). HIGH dormant carry still open: `blinding_leak_scan` plain-English bypass (unreachable today; harden before any dispatcher extension).
5. **CATCH #4 — BLOCKS THE UNSEAL (current gate, operator ruling pending).** Independent re-grade (F-2026-07-12-A, CRITICAL) proved Addendum 3 §4's safety claim FALSE as wired: tier-1 now classifies the located quote (correct+endorsed DIRECTION — measures the trader's surface, not our paraphrase; reverting to condition text = worse harbor), but a mis-grounded-but-literal anchor with tier-1 surface FIRES and is counted OK — tier-1 fires never reach `_build_tier3_packet` (fallthroughs only), AND the tier-3 packet hardcodes condition-context None (raters judge "from the quote alone", never see the condition). So a wrong `surface_class` can silently count pilot_grade-OK → corrupts the §1 numerator + the H2 attribution chain. Span-choice INTEGRITY is the open question (not the direction). **Operator's PRE-COMMITTED lean fix: dual-read agreement gate** — run the DETERMINISTIC tier-1 detectors on BOTH the extractor's condition text AND the located quote at ZERO extra calls; agreement → classify, disagreement → honest fallthrough to tier-3 (two-path law compiled into the architecture, fails toward suspicion per item, for free). Ranked behind: content-word overlap floor (F-2-style gross-mismatch reject); tier-3 audit-targeting of coupling-produced classifications. Alt: accept scope-narrowing in writing (pilot-grade measures "extractor + locator-groundability + tier-1 surface-detection", NOT condition-faithfulness). Sealed 16 STAY SEALED until the ruling lands. (Docstring on `pilot_conveyor.py` carries the gap; blocker recorded in commit `7a937e17`.)
6. **THEN PILOT:** after catch-#4 fix (dual-read) built + re-graded + dry-run re-proven + tower/transcript/gemma checklist green + fresh capacity → unseal the 16 → run → read the §1 ≥60% pilot-grade bar + adjudications/video vs the ~15 ceiling ONCE → independent re-verify. Certs PILOT-GRADE (NOT_EVALUATED loud).
7. **A-PACKET (H2 precondition, addendum §C):** compile-stage topology producer wiring extraction→compiler→assembler so all 5 lints go live on real topology → pilot-grade certs UPGRADE to full-grade. Own ratify-packet + build/grade loop; sequenced between pilot and H2. Also closes N-1 + F-4.
8. **H2 handoff** — FULL-GRADE certs -> full battery on G4 engine -> H2 frozen two-read rule (survivor any-N -> forensics; rate N>=30). h2-source-thesis-preregistration-2026-07-07.md. NO survivor read through a scoped (pilot-grade) certificate.
9. **PARKED (registered)**: stronger-model cloud tier-2 on the ~30% margin = future pre-reg economics experiment, measured vs adjudication agreement, doesn't touch gemma budget. Waits until fork baseline economics measured.

## Standing laws minted this campaign (skill-amend candidates)
ablation-proves-motivation; vault-before-iterate + pass-accounting-2path; schema-IS-the-boundary + force-determination-no-fixed-point corollary. Plus doer!=grader, two-path-derivation, read-once, flag-law.
