# EXTERNAL READ — 2026-08-08 — DISTANCE ESTIMATE + THE VERTICAL-FOCUS PLAN (PHASES A–E)

> **PROVENANCE:** Arrived as **OPERATOR-RELAYED CHAT** on 2026-08-08, introduced by the operator as
> *"gpt has a new plan to speed everything up."* **NOT** from `origin/external-advisor/gpt-rulings`
> (stale at `953a907c`, 2026-08-02). Committed here **VERBATIM**.
>
> **CONSUMED BY `R-722`** — and this banner is written BY that ruling, in its commit, never in advance.
> (`R-722 §1`: a pre-written provenance claim is a forward-dated lie. The 2026-08-04 read carried
> `Consumed by R-721` before `R-721` existed, and it was not.)
>
> **A CHANNEL IS NOT AN AUTHOR.** Audited on merit in `R-722 §5–§8`: what is adopted, what is relayed
> without adoption, and the one item this desk re-ordered against the reader's own priority.

---

GPT EXTERNAL ADVISOR RULING — THE BREAKTHROUGH IS NOT FAR IN DISTANCE, BUT THE MAIN BLOCKER IS STILL UNSOLVED

Right now, I would place the complete compiler breakthrough at roughly:

35–45% complete in end-to-end proof.

The safety foundation may be closer to 70–80% complete, but the actual breakthrough is stricter:

A real YouTube strategy must travel from the lesson all the way into executable bot decisions without losing or inventing meaning.

WHAT IS ALREADY DONE

✅ The engine can use taught numbers correctly when they are supplied.

✅ Different parameter sets no longer collide in the cache.

✅ Silent replacement with engine defaults has been heavily guarded.

✅ Non-consuming routes now refuse parameters instead of pretending to use them.

✅ Short data frames can no longer bypass those refusals.

✅ The comparison tool's fake stop-loss and take-profit values were repaired.

✅ One real strategy has already been selected:

opening_range_breakout

WHAT IS BLOCKING THE BREAKTHROUGH

The selected strategy currently produces:

0 of 99 conditions successfully bound.

All 11 measured candidate strategies currently produce zero executable bindings.

That means the system has not yet connected a real extracted strategy to the bot engine.

The open question is where the meaning is being lost:

1. Did extraction fail to capture the actual rules?

2. Did extraction capture them, but the binding vocabulary cannot translate them?

3. Does the engine lack the exact opening-range detectors or state machine?

4. Is the original video itself missing required information?

The independent review is supposed to answer that before another repair is chosen.

WHAT REMAINS AFTER THE CAUSE IS FOUND

Milestone 1 — Fix the first broken handoff

Take one condition from the selected opening-range strategy and prove:

source wording
→ extracted condition
→ canonical meaning
→ engine binding

Milestone 2 — Bind the complete strategy

Preserve the strategy's actual:

- opening-range time;
- range high and low;
- breakout definition;
- direction;
- entry timing;
- stop;
- target;
- session;
- ordered steps.

Milestone 3 — Execute it correctly

Run the compiled strategy on controlled candles and produce the correct entries, exits, stops, and targets.

Milestone 4 — Independent comparison

Compare every decision bar by bar against a separate reference implementation.

Milestone 5 — Mutation proof

Deliberately change a rule, number, direction, or condition order and prove the conformance test catches it.

That final result is the breakthrough.

REALISTIC DISTANCE

Best case:

One major binding problem is causing most of the 0/99 result.

Then the breakthrough may be about 3–6 focused engineering lanes away.

Harder case:

The strategy needs several missing pieces, such as extraction repairs, canonical vocabulary, and a new opening-range state machine.

Then it may take roughly 8–15 focused lanes or more.

The independent causal review decides which situation you are in.

BABY-BOT VERSION

We built a lot of safe wiring inside the bot.

But the YouTube teacher's instructions still are not reaching that wiring.

The breakthrough happens when one real strategy says:

"Build this opening range, wait for this breakout, enter here, stop here, and target here,"

and the bot performs those exact instructions candle by candle.

So you are not starting over.

But you are also not one tiny test away.

The main road is built; the missing bridge between the extracted strategy and the engine still has to be found and completed.

---

GPT EXTERNAL ADVISOR RULING — THE ENGINEERING IS HIGH-QUALITY, BUT IT IS NOT YET THE FASTEST PATH TO THE BREAKTHROUGH

You are doing unusually strong safety engineering.

The project now catches problems most retail bot builders would never notice:

- invented default numbers;
- half-honored parameter sets;
- cache collisions;
- tests that pass without reaching the real code;
- guards that pass after scanning zero files;
- early returns that bypass safety checks;
- comparison tools reporting fabricated stops and targets;
- comments claiming protection that the code does not provide.

That rigor is valuable. It is preventing Trading Forge from producing profitable-looking backtests for strategies the teacher never actually taught.

But the process has also become slower than necessary.

The biggest issue is not weak engineering. It is too much engineering around the road before proving where the road is broken.

The current main fact is:

> The selected strategy produces zero executable bindings.

The fastest correct next move is not another broad guard census, gate framework, or repository-wide sweep.

It is to take one condition from the selected opening-range strategy and trace it through five exact objects:

1. What the teacher said.
2. What extraction recorded.
3. What canonical instruction the compiler expected.
4. Why binding refused it.
5. Whether the exact engine capability exists.

That one trace should determine whether the primary failure is:

- extraction;
- terminology/canonicalization;
- parameter schema;
- missing engine primitive;
- missing state machine;
- or unclear source teaching.

Once the first condition binds, repeat the same proven method for the remaining conditions in that one strategy.

WHAT YOU ARE DOING WELL

Keep these practices:

- independent graders;
- planted mutations;
- consume-or-refuse behavior;
- exact failure membership instead of counts alone;
- frozen golden strategy;
- refusing invented assumptions;
- separate compiler fidelity from profitability;
- durable repository-contained test instruments.

WHAT HAS BEEN COSTING TIME

Reduce these patterns:

- inventing or repeatedly renaming abstract gates;
- widening a lane before the current causal question is answered;
- building temporary instruments that are not committed;
- auditing all neighboring surfaces before completing one vertical slice;
- treating every newly found tooling issue as equal to the compiler's money-path blocker;
- continuing very long advisor or worker sessions after error rates rise;
- using whole-suite populations when a small causal fixture can answer the question;
- repeatedly proving that production has zero parameterized bindings instead of building the producer that must create one.

THE BEST-AND-FASTEST ENGINEERING PLAN NOW

PHASE A — CAUSAL TRACE

Choose the first load-bearing condition in `opening_range_breakout`.

Produce one table:

source
→ extraction
→ canonical meaning
→ binding attempt
→ engine capability
→ exact failure

No broad repair until that table identifies the broken handoff.

PHASE B — ONE CONDITION BINDS

Repair only the measured broken link.

Require:

- RED before;
- GREEN after;
- independent negative control;
- no invented values.

PHASE C — ONE COMPLETE STRATEGY

Compile every required condition for the frozen opening-range strategy.

Build the temporal sequence:

range formation
→ breakout
→ optional confirmation/retest
→ entry
→ stop
→ target
→ exit

PHASE D — END-TO-END PROOF

Run deterministic candles and compare every decision against an independent reference.

Plant changes to:

- opening-range duration;
- breakout rule;
- direction;
- stop;
- target;
- event order.

Every meaningful mutation must fail.

PHASE E — BATCH FACTORY

Only after that proof, send all 120 strategies through the compiler.

The system may refuse many of them. That is acceptable.

The first batch run should measure:

- exact compiles;
- extraction failures;
- vocabulary failures;
- missing primitives;
- missing state machines;
- source ambiguity;
- duplicates.

Then prioritize repairs by how many strategies each capability unlocks.

BOTTOM LINE

You are doing some of the best integrity-focused engineering possible.

You are not currently doing the fastest possible breakthrough engineering because too much effort has gone into proving and repairing surrounding controls before tracing the selected strategy's first failed condition end to end.

The corrected strategy is:

> Keep the strong safety rules, but narrow all active work to the frozen opening-range strategy until one complete source-to-engine path works.

That gives you both:

- high confidence;
- faster movement toward the 120-strategy edge search.

Do not become less rigorous.

Become more vertically focused.
