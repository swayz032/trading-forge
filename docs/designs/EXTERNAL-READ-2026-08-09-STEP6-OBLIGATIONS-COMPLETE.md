# EXTERNAL READ — 2026-08-09 — STEP 5 PASSES; ALL ELEVEN STEP 6 OBLIGATIONS ARE NOW BANKED

> **PROVENANCE:** GPT external advisor read, **operator-relayed chat, 2026-08-09**, arriving after
> `AR-831`. **BANKED, NOT YET RULED.**
>
> 🛑🛑★★★★★ **READ THIS BEFORE USING IT: THIS READ PREDATES `AR-832` AND CANNOT HAVE ADJUDICATED IT.**
> It closes with *"STEP 6 REMAINS AUTHORIZED AND IN PROGRESS. No additional wait is required."*
> **`AR-832` then stopped `STEP 6` before its first line of code** on a blocker this read never saw:
> the per-candidate parameter shape is a **PARAMETER GRAMMAR**, and the grammar is **reserved to the
> advisor desk by `R-678 §6`** — verified by the worker on two independent paths and re-verified at
> this desk (`spec_family_bindings.py:820-844`, the field's own docstring: ***"THIS IS NOT THE
> PARAMETER GRAMMAR"***).
> ★★★★★ **`A READ CANNOT AUTHORIZE PAST A BLOCKER IT NEVER SAW, AND ITS "NO ADDITIONAL WAIT IS
> REQUIRED" IS SCOPED TO THE WORLD IT WAS WRITTEN IN.`** ⇒ **`STEP 6` IS HELD pending a read on
> `AR-832` and the ruling that follows it. The operator re-asserted the wait (6th assertion) at
> exactly this point, and he was right — this desk was mid-ruling on a stale premise.**
>
> ⚠️ **STATUS: `[EXTERNAL OPINION]` — ZERO AUTHORITY.** Reproduced **VERBATIM**, not paraphrased.
> **Nothing below is adopted until a ruling adopts it.** ★ Its `STEP 5` verdict and its TypeScript
> parity ruling are **unaffected by the `AR-832` blocker** and are expected to survive adjudication;
> its `STEP 6` *sequencing* is what `AR-832` disturbs.
>
> ✅ **WHY THIS FILE EXISTS: `AR-831 §5` measured that `R-737 §8` adopted "the read's ten proof
> obligations" while the read lived only in chat — the worker could name `4` of `11` and correctly
> refused to invent the rest. This is the authoritative complete enumeration.**

---

## VERBATIM TEXT

GPT EXTERNAL ADVISOR RULING — R-738 — STEP 5 PASSES; ALL ELEVEN STEP 6 OBLIGATIONS ARE NOW BANKED

MAIN VERDICT

STEP 5 IS COMPLETE.

The new controls found and repaired a real ordering defect:

- corrupt infinite data previously reached the finite-bar inversion exception;
- it now reaches the correct market-data refusal;
- finite inverted bars still raise;
- all six non-finite cases refuse with no leaked numbers;
- both interval-misdeclaration directions are measured;
- the correctly declared interval still computes.

`31 passed`, unpiped exit `0`, is accepted.

The worker was also correct not to claim the four unrepeated gates. They must run after STEP 6 changes production.

TYPESCRIPT PARITY RULING

The worker's measurement is adopted:

The TypeScript parity gate compares binding declarations, not expanded candidates.

Therefore:

- Python performs candidate expansion;
- TypeScript mirrors the family declaration only;
- TypeScript must not implement a second expansion engine;
- the parity fixture must prove declaration equality after the family becomes supported.

THE COMPLETE STEP 6 PROOF CONTRACT

The missing obligations are enumerated here. This ruling is the authoritative complete list.

1. EXACT EXPANSION MEMBERSHIP

Production must emit exactly the variants taught by each typed definition.

Frozen expected population:

- `st5e-YJRfKc__s0`: 5m, 15m, 30m;
- `dENM6gt8ZRg__s0`: 5m.

Total: four executable candidates from two source conditions.

The source-condition census remains two. Candidate expansion is a later layer.

2. EVERY CANDIDATE REACHES THE REAL ADAPTER

Each candidate must execute:

`compute_opening_range_state`

Primitive registration or return annotations alone do not satisfy this obligation.

3. DURATION CHANGES BEHAVIOR

Use deterministic candles designed so the 5m, 15m, and 30m windows produce different high, low, width, and midpoint values.

Three identical outputs would not prove three variants executed.

4. DURATION MUTATION CHANGES THREE IDENTITIES

Changing only the duration must change:

- candidate identity;
- cache identity;
- computed output.

All other source provenance must remain constant.

5. MISSING-CANDIDATE CONTROL

Delete one taught variant in the planted mutation.

Exact-membership testing must fail and name the missing candidate.

A count-only check is insufficient.

6. COLLISION CONTROL

Plant two variants with the same cache identity.

The guard must fail by exact member identity.

Every duration must occupy a distinct cache namespace.

7. WRONG-ROUTE CONTROL

Route `OPENING_RANGE_DEFINITION` back to:

`structure_engine.compute_structure_state`

The conformance suite must fail.

8. NEIGHBOR PRESERVATION

A genuine `WAIT_STRUCTURE` condition must remain classified, bound, and executed through the existing structure route.

Fixing opening ranges may not break legitimate market-structure behavior.

9. ORDERED RED TRANSITION

Both existing production REDs must turn green without weakening or removing their predicates:

- production binding reaches the opening-range adapter;
- the reachable primitive exposes the typed opening-range output contract.

10. EXECUTABLE COMPLETE-STATE PROOF

Starting from the frozen extraction artifact, at least one expanded candidate must execute on deterministic bars and return:

- `opening_range_complete=True`;
- status `COMPLETE`;
- correct high;
- correct low;
- derived width;
- derived midpoint.

Metadata-only success is forbidden.

11. CROSS-PROCESS IDENTITY STABILITY

Generate candidate and cache identities in two genuinely separate Python processes.

The identities and exact candidate membership must be byte-identical.

Do not use Python's process-randomized `hash()`, unordered-set iteration, filesystem order, or incidental dictionary order.

Use canonical serialized fields and a deterministic digest.

STEP 6 PRODUCTION CONTRACT

The binding must carry a typed definition or serializable typed parameter domain. It must not remain `parameters=None`.

Expansion must consume `definition.variants`; it must not re-parse the teacher's prose or hardcode video IDs.

Every candidate identity must include at least:

- strategy/spec identity;
- source condition identity;
- variant label;
- duration;
- session start;
- source timezone;
- market scope;
- trading-day rule.

Production must never execute the unexpanded template. Missing candidate context must produce a deliberate refusal, not a crash, missing lookup, fallback, or default duration.

SAME-COMMIT ROUTING REQUIREMENTS

The STEP 6 routing commit must update together:

- Python `FAMILY_META`;
- primitive resolver;
- enforced dispatch;
- TypeScript family declaration;
- focused parity fixture;
- candidate expansion;
- candidate/cache identity;
- production execution tests.

No after-the-fact parity repair is permitted.

ACCEPTANCE

After STEP 6, rerun:

1. adapter suite;
2. expansion and identity suites;
3. eight-control discrimination instrument;
4. production guards;
5. opening-range conformance group;
6. real TypeScript parity binary.

The two ordered REDs must be green, with no unexpected new failure.

The known `test_cloud_backend.py` hang remains outside this lane.

STOP CONDITIONS

Stop if:

- any duration is invented or selected;
- the exact four-candidate population disagrees;
- a source condition is duplicated merely to represent variants;
- production needs a video ID;
- an unexpanded template can execute;
- cache identity changes across processes;
- another family's behavior moves;
- the breakout rule enters the patch.

STEP 6 REMAINS AUTHORIZED AND IN PROGRESS.

No additional wait is required.

---

## DESK NOTES — MEASURED WHILE BANKING, RULED NOWHERE

⚠️ **These are facts recorded so the eventual ruling does not have to re-derive them. THEY ADOPT
NOTHING.**

1. ✅ **OBLIGATION 1's FROZEN POPULATION IS CORROBORATED ON DISK — I CHECKED IT BEFORE TRUSTING IT**
   (`[external-sha-fabrication]`: this campaign has caught an external reader inventing identifiers).
   `[MEASURED HERE, `docs/replay-results/h1-battery/opening_range_definition_discrimination.py:92-95`]`
   the instrument's own `AUTHORIZED_MEMBERS` pins **exactly two** source conditions:
   `("st5e-YJRfKc__s0", "WAIT_STRUCTURE:once-you-take-the-price-that-s-establish#0")` and
   `("dENM6gt8ZRg__s0", "WAIT_STRUCTURE:the-first-five-minute-candle-from-09-30#0")`.
   ⇒ **the second member is REAL, and its condition slug literally reads `the-first-five-minute-candle-from-09-30`, independently corroborating the read's `5m`.** ✅ **`3 + 1 = 4` candidates from
   `2` source conditions is CONSISTENT with the standing `census stays EXACTLY TWO` invariant.**

2. ✅ **THE TYPED CARRIER OBLIGATION 12 ASKS FOR ALREADY EXISTS.**
   `[MEASURED HERE, `src/engine/opening_range_definition.py:125,147,163,181-182`]`
   `OpeningRangeDefinition` carries **`variants: tuple[OpeningRangeVariant, ...]`**, validated
   non-empty, and its own message states it *"carries N source-sanctioned alternatives … and selects
   NONE of them."* ⇒ **the read's *"typed definition OR serializable typed parameter domain"* is
   satisfiable by the TYPED DEFINITION — the branch that does not touch the reserved field.**

3. 🛑 **BUT IT IS NOT REACHABLE FROM THE BINDING TODAY.**
   `[MEASURED HERE]` `ConditionBinding.object` is a plain **`str`** (`spec_family_bindings.py:805-820`),
   and `grep -n "OpeningRangeDefinition" src/engine/spec_family_bindings.py` returns **NO HITS**.
   ⇒ **there is no binding→typed-definition link. Whether one may be added is part of the reserved
   question, not separable from it.**
   ⚠️ **AND IT INTERACTS WITH PARITY:** `[MEASURED, AR-831 §4]` the TS gate compares **exactly ten
   named per-binding fields**; an eleventh Python field would be **invisible to the gate** — which is
   convenient and is therefore exactly the kind of thing that must be decided deliberately rather than
   discovered.

4. 🛑 **THE READ'S *"It must not remain `parameters=None`"* COLLIDES HEAD-ON WITH `R-678 §6` AND WITH
   THE FIELD'S OWN DOCSTRING, AND THE READ DID NOT KNOW THAT.** `[MEASURED HERE,
   `spec_family_bindings.py:821-844`]`: *"NOTHING in this repo populates it … the only writer is a
   test … THIS IS NOT THE PARAMETER GRAMMAR … reserved to the advisor desk (R-678 §6) … when it lands
   it must RECEIVE the shape `src/server/lib/indicator-params.ts` already emits."*
   ★★★★★ **`THE FIRST PRODUCTION WRITER OF A FIELD RESERVED FOR A FUTURE DESIGN IS THE DESIGN`**
   (`AR-832 §3`). ⇒ **This conflict is the substance of the pending ruling and is DELIBERATELY LEFT
   OPEN HERE.**
