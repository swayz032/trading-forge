# EXTERNAL READ — 2026-08-08 — AR-806 MEASUREMENT ACCEPTED; CO-PRIMARY CLASSIFICATION NOT YET ACCEPTED

> **PROVENANCE:** Arrived as **OPERATOR-RELAYED CHAT**, 2026-08-08, in response to `AR-806`.
> **NOT** from `origin/external-advisor/gpt-rulings` (stale at `953a907c`, 2026-08-02).
> **CONSUMED BY `R-724`** — banner written BY that ruling, IN its commit, never in advance (`R-722 §1`).
>
> ⚠️ **TIMING JOIN, STATED SO NO SEAT MISREADS THE SCOPE:** this read responds to `AR-806`. Its
> *"Not yet proven: no opening-range constructor exists elsewhere"* is **correct as a certification
> statement and already partly answered as a measurement** — `AR-805` measured
> `compute_opening_range_breakout` present and live. **Existence is MEASURED (worker, uncertified);
> REACHABILITY from the binder is measured ABSENT; the repository-wide reachable closure is
> UNMEASURED.** See `R-724 §6`.
>
> **A CHANNEL IS NOT AN AUTHOR.** Audited on merit in `R-724`. ★ **Its single-primary ruling is
> ADOPTED OVER THIS DESK'S WORKER — see `R-724 §5` for why the reasoning, not the authority, won.**

---

GPT EXTERNAL ADVISOR RULING — AR-806 MEASUREMENT ACCEPTED; CO-PRIMARY CLASSIFICATION NOT YET ACCEPTED

DECISION: ROW 1 MAY CLOSE MECHANICALLY. CONTINUE TO ROW 2. NO REPAIR AUTHORIZED.

AR-806 establishes three important facts:

1. The production harness reproduces the real refusal.
2. The positive control proves the session binder is alive.
3. The family label, not the paragraph's meaning, controls dispatch for 11 of the 14 tested families.

That third result is significant, but it is currently a trust-boundary finding—not yet proof of 11 production misbindings.

The test deliberately recast one paragraph under counterfactual family labels. A typed dispatcher may legitimately trust its type if an upstream canonicalizer has already validated and sealed that type. It becomes a compiler defect if machine-generated or unvalidated family labels can reach that dispatcher without semantic enforcement.

ROW 2 must determine which architecture Trading Forge actually has.

CLASSIFICATION RULING

Do not promote two co-primary blockers yet.

The directly measured primary blocker remains:

> The preserved opening-range prose never becomes a valid typed opening-range instruction.

Measured secondary:

> None of the 14 tested family routes on this binding surface represents opening-range construction.

Still unmeasured:

> Whether the correct opening-range engine capability is absent repository-wide.

Absence from the 14-family dispatcher does not prove absence from the engine. The reachable import closure or equivalent call-graph evidence is still required.

The anti-monocause rule requires downstream blockers to be recorded. It does not require every suspected downstream blocker to be labeled co-primary before direct measurement.

Also distinguish these claims precisely:

- Proven: 11 counterfactual family labels accepted this unrelated paragraph.
- Not yet proven: 11 real production conditions are misbound.
- Proven: current binding depends heavily on declared family identity.
- Not yet proven: type dispatch itself violates the intended architectural contract.
- Proven: no opening-range constructor appeared among the eight returned primitives.
- Not yet proven: no opening-range constructor exists elsewhere.

ROW 2 AUTHORIZATION

Proceed only with `WAIT_STRUCTURE#0`.

Trace:

1. The exact frozen `action` text.
2. The code that assigns `WAIT_STRUCTURE`.
3. Every input available when that label is assigned.
4. Whether that assignment examines semantic meaning or merely shape/position.
5. The exact typed object delivered to `bind_condition()`.
6. The primitive selected by that label.
7. The primitive's real signature and required inputs.
8. Whether its behavior is semantically identical to the taught condition.
9. Why the census calls it `APPROXIMATED`.
10. Whether that approximation is explicit refusal metadata or an executable neighboring-concept substitution.

Required controls:

- Preserve the family label and mutate the prose materially.
- Preserve the prose and mutate the family label.
- Show whether binding identity or evaluator output changes.
- Exercise the selected primitive with deterministic inputs.
- Compare its output with an independent interpretation of the frozen artifact text.

Do not expand this into all nine `APPROXIMATED` rows. One complete causal trace is sufficient.

STOP CONDITIONS

- If upstream classification validates meaning and constructs a sealed typed condition, type-only dispatch may be legitimate; inspect the classifier's correctness.
- If the label is generated without semantic validation, identify that exact trust-boundary failure.
- If `WAIT_STRUCTURE#0` is routed to a neighboring concept that cannot express the artifact's instruction, classify `ENGINE_PRIMITIVE_WRONG_IDENTITY`.
- If the primitive is correct but required parameters were lost, classify the earlier schema or lowering handoff.
- If an opening-range constructor exists elsewhere but is unreachable, classify the missing production route.
- If no such constructor exists after reachable-closure measurement, then promote `ENGINE_PRIMITIVE_MISSING`.

TRANSCRIPT DESK ANSWER

No frozen transcript has been provided to this GPT advisor. Record its status as `UNKNOWN / NOT PROVIDED`, not absent.

Perform one bounded authoritative exact-ID search if that has not already occurred. If it remains unavailable, open one durable V1.0 source-certification blocker and stop repeating the desk question in each row. Artifact-to-engine diagnosis should continue.

FINAL POSITION

AR-806 has exposed a potentially serious architectural weakness:

> Trading Forge may be trusting generated family labels as semantic truth.

ROW 2 must determine whether that trust was earned upstream or whether unrelated prose can genuinely acquire executable neighboring semantics without refusal.

That is now the fastest causal question.
