# INDEPENDENT GRADE — LANES 28 · 29 · 30 (AR-783 / AR-785 / AR-787)

**Grader:** `accuracy-validator` (independent; NOT the doer)
**Date:** 2026-08-04
**Pin graded:** `b8321dc95454bdc469df5953f809694f78ed7902` (`b8321dc9`)
**Tree:** `C:/Users/tonio/Projects/wt-h1-wave4-20260712`, branch `h1-wave4-sealed12-driver`
**Rulings read:** `R-702`, `R-703`, `R-704`, `R-705`, `R-707`, `R-708`
**Reports read:** `AR-782`, `AR-783`, `AR-784`, `AR-785`, `AR-786`, `AR-787`

---

## 0. VERDICT

> ## `PASS_WITH_BOUNDED_FINDINGS` — **VERIFIED band 7**
>
> **GATE 2: THE FOUR DISQUALIFYING CATEGORIES ARE ALL CLEAR.**
> No finding in this grade involves **silent substitution**, **partial recognition**,
> **unused accepted parameters**, or **flag-OFF parameter loss**.
> Every finding below is a **coverage hole in a TEST INSTRUMENT** or a **latent code shape**,
> not a live parameter-loss defect. I did not soften anything to reach that sentence, and
> I did not manufacture anything to avoid it.

**Scoped bands** (a composite band hides which half is weak):

| Sub-system | Band | Basis |
|---|---|---|
| Parameter consume-or-refuse discipline (F-A · F-B · F-2 · F-2D · F-3 · flag-OFF · ordering) | **8** | 16 of 16 adversarial mutations caught; residual risks documented `MEASURED HERE` |
| `F-E` census instrument | **7** | 6 of 6 enumerated shapes caught (was 1 of 6); 8 unenumerated shapes evade `MEASURED HERE` |
| Canonical regression-population instrument | **5** | headline transitive property has **no path to red** `MEASURED HERE` |
| **Composite, as delivered** | **7** | one open HIGH forbids 8; band 9 additionally requires zero open HIGHs |

**Band 10 is not reachable and was never a candidate.** Band 9 is refused because F-1 is open.

**Findings:** 0 CRITICAL · 1 HIGH · 2 MEDIUM · 3 LOW · 4 honest nulls (refutations attempted, failed).

---

## 1. WHAT THE PIN ACTUALLY IS — AND HEAD MOVED TWICE UNDER ME

`[MEASURED HERE]` `HEAD` moved **twice** during this grade:
`b8321dc9` → `2adc695d` → `1f5fd513`. **Every commit touched `docs/` only.**

All **eight** graded blobs are **byte-identical at the pin, at every observed HEAD, and in the
working tree** `[MEASURED HERE, sha256]`:

```
621302a56987f19b  src/engine/spec_condition_compiler.py
c21f123fca7c8f26  src/engine/tests/test_flag_off_parameterized_refusal.py
30d7dda169bc4081  src/engine/tests/test_bias_refusal_surface.py
40c518576f221594  src/engine/tests/test_parameter_acceptance_guard.py
5c53ff75731486b3  src/engine/tests/test_short_frame_parameter_acknowledgement.py
e8e38291595cd2db  src/engine/tests/test_parameter_collision.py
64ac16f866955a83  src/engine/tests/test_bias_parameter_transmission.py
5da49e25409f0658  src/engine/tests/test_bias_wired_path_parameters.py
```

`git status --porcelain -- src/` shows exactly one modified file, `test_synthetic_market_simulator.py`
— the sibling seat's, never touched by me `[MEASURED HERE, at grade open AND at grade close]`.

**Lane surface reproduces the doer's headline exactly:** `108 passed` over the seven-file surface
`[MEASURED HERE, isolated tree]`. AR-787's `108` is confirmed by re-execution, not relayed.

---

## 2. ISOLATION — AND THE HAZARD THAT DID NOT BITE

**I planted NOTHING in the shared campaign worktree. Zero plants, at any moment, for any duration.**

All **~33 plants** lived in `C:\tf30`, an isolated extraction:
`git archive b8321dc9 src | tar -x -C /c/tf30` `[MEASURED HERE]`, plus throwaway `tempfile.mkdtemp()`
trees for census fixtures. `[MEASURED HERE]` the isolated tree reproduces the shared tree's
baseline exactly (`83 passed` on the five-file subset in both; `108 passed` on the seven-file surface).

⇒ **The `pre-commit` stash hazard never had a plant to corrupt.** No result needs re-deriving.
The desk's commits at ~14:15Z / ~14:17Z / ~14:32Z / ~14:41Z could not have touched any measurement
of mine, because no measurement of mine ever depended on the shared tree's mutability.
The only shared-tree operations I performed were **reads**: `git show`, `sha256sum`, and one
read-only `pytest` collection at grade open.

⚖️ **CONSEQUENCE FOR THE DESK, REPORTED NOT CLAIMED:** `R-708` put the worker on a MEASURED hold on the
premise *"the grader plants into `src/engine/tests`"*. `[MEASURED HERE]` that premise was false for
this grade. The hold was correct as risk management and unnecessary as fact —
**a hazard model built from a PREDICTED method is not a measurement of the method used.**

---

## 3. THE TEN MANDATED MUTATIONS — ALL TEN RE-RUN, ALL TEN CAUGHT

`[MEASURED HERE]` Production-side, isolated tree, **every anchor occurrence-count asserted `== 1`
before replacement** (a no-op mutation fabricates an uncovered hole), every arm restored with
`sha256 PRE == POST` verified. Baseline `108 passed`. **Reported as OBSERVED red sets, per the
adopted rule that a pre-registered red set is a LOWER BOUND.**

| # | Mutation | Observed | Verdict |
|---|---|---|---|
| M1 | Complete silent default (`unrecognised = []`) | 7 new red | **CAUGHT** |
| M2 | Partial recognition — honour `fast_period`, default the rest | 22 new red | **CAUGHT** |
| M3 | **MIRROR** — honour `slow_period`, default the rest | **20 new red** | **CAUGHT** ★ was UNCOVERED |
| M4 | Alias unknown names onto canonical | 7 new red | **CAUGHT** |
| M5 | Acknowledgement deferred until **after** the dispatch loop | **13 new red** | **CAUGHT** ★ was UNCOVERED |
| M6 | Fully-wired path accepts and discards | 1 new red | **CAUGHT** |
| M7 | All-False on insufficient history with supplied params | 1 new red | **CAUGHT** |
| M8 | Malformed value silently replaced by the default | 4 new red | **CAUGHT** |
| M9 | Production producer added while the census stands | **6 of 6 shapes RED** | **CAUGHT** ★ was 1 of 6 |
| M10 | Flag ON falls back to the flag-OFF ladder on refusal | 26 new red | **CAUGHT** |

★★★★★ **THE TWO HOLES THE PRIOR GRADE LEFT OPEN ARE GENUINELY CLOSED, AND CLOSED BY A STRONGER
INSTRUMENT THAN THE ONE THAT CONVICTED THEM.** M3 and M5 both scored `58 passed / 0 red` at the
Lane 25-27 grade. They now redden 20 and 13 permanent tests respectively.

**Independent corroboration of AR-787's own honesty:** my M5 plant is *not* the doer's
(mine is AST-located after the `for b in spine_bindings:` loop; the doer's was line-anchored at
`:1726`). **Both produce exactly 13 reds**, and both of the two tests AR-787 admits it failed to
predict — `test_census_every_non_consuming_handler_actually_refuses` and
`test_an_unrouted_primitive_also_refuses` — **are in my observed set.** `MEASURED HERE`, two
non-overlapping plants, same answer. The doer's recorded miss is real and correctly recorded.

**M9 detail** `[MEASURED HERE, at PYTEST level — the guard's own verdict, not the helper's]`:
control with no plant → GREEN; each of the six enumerated shapes planted alone into `src/engine/` →
**RED**; plant removed → GREEN. `1-keyword · 2-positional · 3-aliased · 4-module-qualified ·
5-dataclasses-replace · 6-aliased-replace` — six for six.

---

## 4. THE SUPPLEMENT'S ADDITIONAL ATTACKS — 8 CAUGHT, 2 UNCOVERED

`[MEASURED HERE]` Same discipline. Baseline `108 passed`.

| Attack | Observed | Verdict |
|---|---|---|
| N1 reclassify `_h_structure` as CONSUMING, evaluator unchanged | 9 new red | **CAUGHT** |
| N2 parameters used **ONLY** in a cache key (evaluator gets defaults) | 12 new red | **CAUGHT** |
| N3 acknowledgement moved **below** the short-frame return | 7 new red | **CAUGHT** |
| N4 refusal moved **after** `candle_confirmation_check` | 9 new red | **CAUGHT** |
| N5 census scan root made **relative** (cwd-dependent) | 1 new red | **CAUGHT** |
| N6 census scan root points at nothing (**zero files**) | 3 new red | **CAUGHT** |
| N7 canonical population **ORDER** perturbed (reversed) | **0 new red** | 🛑 **UNCOVERED_HOLE** |
| N8 canonical population **PATH NORMALIZATION** changed (absolute) | 2 new red | **CAUGHT** |
| N9 canonical population **TRANSITIVITY** deleted (1-hop only) | **0 new red** | 🛑 **UNCOVERED_HOLE** |
| N10 population tests run from a **foreign cwd**, unmutated | 2 passed | structurally cwd-independent |

🛑 **A CORRECTION AGAINST MY OWN INTEREST.** My first N7 plant produced `??` (no result line) and
would have read as a third uncovered hole. `[MEASURED HERE]` it was a **`SyntaxError` in my own
mutation** — `sorted(<genexp>, reverse=True)` is not valid Python — so the file never collected.
**A broken mutation reads exactly like an uncaught one.** I re-derived N7 with valid syntax
(`list(reversed(sorted(...)))`), confirmed the file collected (`20 passed` baseline **and**
`20 passed` mutated), and only then recorded the hole. `graders_own_harness_can_have_the_bug` — third conviction.

---

## 5. FINDINGS

### Discrepancy F-1: the canonical regression population's TRANSITIVE CLOSURE has no path to red
**Severity: HIGH** (guard that cannot fail — *not* one of the four gate-disqualifying categories)
**Claim:** `AR-787 §5` — *"ONE CANONICAL POPULATION, COMMITTED, DETERMINISTIC, WITH A BREAK-CONTROL"*;
the committed docstring at `:489` — *"Test files that **transitively** import any target module."*
**Reality:** the transitive half of that sentence is **unguarded**. Deleting the entire recursion
so the rule becomes a 1-hop direct-import check collapses the population **95 → 23** — **72 members,
76%, silently dropped** — and **every committed assertion stays green.**

**Sources compared:**
- *Path A — helper-level replay, in-process*: `_regression_population` = 95; a hand-written
  1-hop variant over the identical file walk = 23; symmetric difference = 72 members.
  All four committed assertions replayed against the 23-member set → **all True**.
- *Path B — pytest-level source mutation, isolated tree*: recursion deleted in the committed blob →
  **`108 passed`, 0 new red** on the full lane surface; `20 passed`, 0 new red on the file itself.

**Why every guard misses it** `[MEASURED HERE, read at the executable lines]`:
- `assert len(population) > 0` — satisfied by 23.
- `assert "engine/tests/test_flag_off_parameterized_refusal.py" in population` — that file
  imports `spec_condition_compiler` **directly**, so it survives a 1-hop rule.
- `assert population == _regression_population(...)` — both calls use the **same edited rule**,
  in the **same process**, on the **same tree**. It cannot detect a rule edit.
- The break-control's planted tree is `test_importer.py` → `from src.engine.spec_condition_compiler import X`
  — a **1-hop direct** import — and `test_bystander.py` → `import json` — **0 hops**.
  **The break-control never plants a 2-hop path, so it never exercises the recursion it exists to prove.**

**Source of truth:** the 95-member set. It is the over-approximation and the one the doer ran.
**Fix point:** `src/engine/tests/test_flag_off_parameterized_refusal.py:550`
(`test_the_population_derivation_discriminates`) — the planted tree needs a **third** module
forming a 2-hop chain (`test_indirect.py` → `helper.py` → target) that must appear, plus a
2-hop bystander chain that must not.
**Repro:**
```
python - <<'PY'
import sys; sys.path.insert(0, r"C:\Users\tonio\Projects\wt-h1-wave4-20260712")
import importlib; m = importlib.import_module("src.engine.tests.test_flag_off_parameterized_refusal")
print(len(m._regression_population(m._SCAN_ROOT, m._CLOSURE_TARGETS)))   # 95
PY
# then, in an ISOLATED `git archive` tree, delete the `for other, tail in tails.items(): ...`
# recursion from `reaches()` and re-run the file: 20 passed, 0 red.
```
**Blast radius:** every future regression claim scoped to "the canonical population". The instrument
was shipped specifically to **end** population disagreements; on its headline property it currently
cannot report one. `[HYPOTHESIS — NOT MEASURED]` no downstream consumer exists yet, because the
population has been used exactly once (AR-787 §6).

---

### Discrepancy F-2: no committed member manifest, and the ORDER is unpinned too
**Severity: MEDIUM** (reproducibility of the replacement instrument)
**Claim:** `AR-787 §5` — *"The next disagreement is resolvable because the instrument is now in the repository."*
**Reality:** the **generator** is committed; the **members** are not. `[MEASURED HERE]` a repo-wide
search finds **no artifact pinning the 95 members** — the only two files naming the population's
own test file are my prior grade document and the test file itself.
`[MEASURED HERE]` N7: reversing the returned order reddens **nothing**.
`[MEASURED HERE]` N8: changing path normalization to absolute **is** caught (2 red) — so path
**form** is pinned while **membership** and **order** are not.

**This is the 80-vs-81 disease one level up, and it is the coordinator's own item A — confirmed, not
refuted.** If the rule is edited, the population moves and there is still nothing to compare against.

**On the three sub-questions asked:**
1. **Is it a real hole?** `[MEASURED HERE]` **Yes.** Nothing else pins the members.
2. **Does the determinism assertion bite?** `[MEASURED HERE]` **No, on all three axes.** Both runs
   share one process, one cwd and one rule version, so it cannot catch cwd-dependence, a rule edit,
   or filesystem-ordering drift (`rglob` output is already `sorted()` before it is consumed).
   It proves only that the function is not randomised.
3. **Is there a cwd control for the population?** `[MEASURED HERE]` **No — confirmed.** The only
   cwd control is `test_fe_census_is_identical_from_a_different_cwd`, which covers the **census**.
   N10 shows the population tests *do* pass from a foreign cwd (`parents[3]` anchoring works), so
   the property **holds** — it is simply **unguarded**. It survives today by inheriting `_SCAN_ROOT`
   from the census: N6 reddens the population test only as collateral of breaking the shared anchor.

**Fix point:** same file — commit the sorted member list as a constant and assert set-equality,
with the derivation as the generator of record.

---

### Discrepancy F-3: the census detects six producer shapes and misses at least eight
**Severity: MEDIUM** (incomplete surface under an ABSENCE claim)
**Claim:** `AR-787 §4` — *"ALL SIX producer shapes get their own controlled positive fixture."*
That claim is **TRUE and verified**. The enclosing absence claim —
*"no production site builds a parameterized binding"* — is what is bounded.
**Reality:** `[MEASURED HERE, two paths: `_census()` helper directly, and the guard's own pytest verdict —
both agree on all 14 shapes]`

```
DETECTED (6/6, the lane's enumerated set):
  1-keyword · 2-positional · 3-aliased · 4-module-qualified
  5-dataclasses-replace · 6-aliased-replace
EVADED (8/8, not enumerated anywhere):
  ConditionBinding(**{'parameters': ...})      <- the most idiomatic evasion
  dataclasses.replace(old, **{'parameters': ...})
  ConditionBinding(*args)          setattr(b, 'parameters', ...)
  object.__setattr__(b, 'parameters', ...)     factory forward: def mk(**kw): return ConditionBinding(**kw)
  sfb.ConditionBinding(**dict(parameters=...))  ConditionBinding(**kw)
```
**Root cause, at the executable line:** `:356` `by_keyword = any(k.arg == "parameters" for k in node.keywords)`
— a `**` splat arrives as a keyword with `arg is None`, so it can never match; and `:359`
`by_position = ... len(node.args) > _PARAMETERS_FIELD_INDEX` — a `*args` splat is one `ast.Starred`
node, so `len(node.args) == 1`. **The predicate is syntactic; the property is semantic.**

**BLAST RADIUS TODAY: ZERO, AND THAT IS MEASURED, NOT ASSUMED.** `[MEASURED HERE]` 212 non-`src/`
`.py` files scanned: **0** `ConditionBinding` constructions outside the census root. 288 files inside
it: **14** constructions, **0** passing `parameters` — reproducing AR-787's `14` by an independent
walk. So the absence claim is **true today** and its instrument is **weaker than the claim's surface**.

**Note the scope boundary, which is real but currently harmless:** `_SCAN_ROOT = _REPO_ROOT / "src"`.
A producer under `scripts/` would be invisible. `[MEASURED HERE]` there are none.

---

### Discrepancy F-4: the original F-A code shape survives at `_h_structure`, defanged only from upstream
**Severity: LOW** (latent, not live)
`[MEASURED HERE, read at the executable lines]` `spec_condition_compiler.py:639-643`:
```python
cache_key = b.parameters
cache = ctx["wait_structure_cache"]
if cache_key not in cache:
    cache[cache_key] = self._eval_wait_structure(ctx["n"], ctx["df"])
```
`_eval_wait_structure(n, df)` **takes no period.** This is verbatim the shape the prior grade
convicted as F-A. It is unreachable with non-empty parameters **only** because
`_acknowledge_parameters` refuses first — a guard **75 lines upstream in a different method.**
**It is correctly guarded and correctly classified `REFUSES_ALL_PARAMETERS`**, and both N1
(reclassify as consuming → 9 red) and N2 (cache-key-only → 12 red) confirm the guard surface
around it is alive. Recorded so the shape is not mistaken for a repair.

---

### Discrepancy F-5: `MINUS test_cloud_backend.py` is an inoperative exclusion in AR-787 §6
**Severity: LOW** (caption accuracy)
**Claim:** `AR-787 §6` — *"POPULATION: the canonical 95 files MINUS `test_cloud_backend.py`."*
**Reality:** `[MEASURED HERE]` `test_cloud_backend.py` imports `src.engine.cloud_backend` and
`src.engine.hardware_profile`, **not** the closure targets — it is **not among the 95**. Subtracting
it removes nothing; the population is 95 either way.
**Direction of the error is safe** (over-cautious, not over-claiming), but it implies a removal that
never occurred. **In `AR-783 §3` and `AR-785 §5` the same exclusion IS operative** — those populations
are all of `src/engine/tests/` — so the declaration is honest there and vestigial here.
`[MEASURED HERE]` the file is genuinely excluded from every population it *could* have entered,
and is honestly declared `UNENUMERATED` / NOT green / NOT covered in AR-783 §7, AR-785 §6 and AR-787 §7.
**The exclusion's honesty is CONFIRMED; only this one arithmetic caption is vestigial.**

---

### Discrepancy F-6: the desk's hazard model for this grade was predicted, not measured
**Severity: LOW** (process)
See §2. `R-708` held the worker on the premise that the grader plants into `src/engine/tests`.
`[MEASURED HERE]` no plant of mine ever existed in the shared tree. Recorded because
**a hold justified by a predicted method costs real worker time and is falsifiable in one command.**

---

## 6. HONEST NULLS — REFUTATIONS I ATTEMPTED AND FAILED TO LAND

These are reported because a clean report is only trustworthy if it says what it *tried*.

1. **HYPOTHESIS (mine): `test_bias_refusal_surface.py` inherits its flag state from the caller's shell**
   — the exact defect (`R-697 §5.7`) that its own sibling's `flag_off` fixture docstring cites as fixed.
   The file declares no `flag_on`/`flag_off` fixture on any of its 12 tests.
   **REFUTED BY MY OWN CONTROL** `[MEASURED HERE]`: an `autouse=True` fixture at `:69-90` sets
   `TF_FAMILY_META_ENFORCED=true` explicitly. Two-arm run — flag unset (production default) and flag
   forced on — gives `12 passed` in **both** arms. The claim-3/claim-4 surface is flag-pinned, not ambient.

2. **HYPOTHESIS: a mirror-image guard inversion (`R-702`'s shape) exists in Lanes 28/29/30.**
   **NOT FOUND** `[MEASURED HERE]`. I built the ordering-guard × flag-state matrix from the test
   signatures: flag-**OFF** is covered by M5 (`_last_bias_periods`) and F-F (`candle_confirmation_check`);
   flag-**ON** is covered by Lane 29's `test_refusal_precedes_confirmation_evaluator_and_cache_on_a_full_frame`
   and Lane 28's `test_h_structure_refuses_parameters_before_evaluation_and_cache_mutation`.
   **N4 reddens BOTH arms** (9 red spanning both files), so neither arm is the only witness.
   The `A GUARD THAT ONLY WATCHES THE PATH YOU TURNED ON` inversion is **not repeated in either direction**.

3. **HYPOTHESIS: a handler is reachable with parameters it does not consume (F-A survivor).**
   **NOT FOUND** `[MEASURED HERE]`. The guard's routing key at `:1425`
   (`ENFORCED_DISPATCH.get(b.primitive or "")`) is the **identical expression** the dispatcher uses at
   `:912` — **the join key is the claim, and here the two are the same expression, not merely
   corresponding ones.** `PARAMETER_CONSUMING_HANDLERS` is *derived* from
   `HANDLER_PARAMETER_CLASSIFICATION`, not maintained in parallel, so the two cannot drift.
   `[MEASURED HERE]` `_dispatch_enforced`, `_h_wait_bias`, `_eval_wait_bias` and
   `_acknowledge_parameters` have **no callers outside the compiler** (the three grep hits are
   docstrings/strings, not calls), and `.parameters` is read outside the compiler only by the
   dataclass's own hash/serialise methods in `spec_family_bindings.py`.

4. **HYPOTHESIS: the committed population instrument silently includes the MEASURED-HUNG
   `test_cloud_backend.py`, so anyone running "the canonical population" would hang.**
   **REFUTED** `[MEASURED HERE]` — it is not in the 95 (see F-5). The trap does not exist.

---

## 7. CLAIM-BY-CLAIM DISPOSITION

| # | Claim | Status | Basis |
|---|---|---|---|
| 1 | F-A consume-or-refuse across EVERY enforced handler; cache-key ≠ consumption | **VERIFIED** | N1 9 red · N2 12 red · M9 6/6 · census over all 16 routed primitives, exercised not declared; join key identical to the dispatcher's |
| 2 | F-B acknowledgement before early return / evaluator / cache / state reset / dispatch | **VERIFIED** | N3 7 red · N4 9 red · M5 13 red; state-reset witnessed by `_assert_nothing_was_published` (sentinel + `last_trace`) |
| 3 | F-2 complete substitution + F-2D **both** hybrid directions refuse, naming only the offending key | **VERIFIED** | M1 7 red · M2 22 red · M3 20 red · M4 7 red; M3's control pins the exact `EMA(20,90)` hybrid and asserts it differs from **both** neighbours |
| 4 | F-3 insufficient-frame refusal names id, keys, `n`, floor | **VERIFIED** | M7 1 red; boundary pair `n=29` refuses / `n=30` computes with periods (3,8) shorter than both frames |
| 5 | flag-OFF refusal `parameterized_binding_requires_enforced_dispatch` | **VERIFIED** | M10 26 red · M5 13 red · N3/N4 |
| 6 | pre-evaluation ordering, before `candle_confirmation_check` and any cache mutation | **VERIFIED (evaluators) / CORROBORATED (cache construction)** | N4 reddens both flag arms. Cache **construction** at `:1577` has no direct counter — it is proven **structurally** (refusal at `:1497` precedes it) and **transitively** (`_last_bias_periods` implies the evaluator, which implies the write) |
| 7 | census portability — repo-anchored, cwd-independent, 5 non-vacuity controls, 6 forms each with a fixture | **VERIFIED, BOUNDED** | N5 1 red · N6 3 red · 6/6 fixtures RED at pytest level. **Bounded by F-3: 8 further shapes evade; scope is `src/` only** |
| 8 | ALL TEN original mutations still caught | **VERIFIED** | §3 — 10/10, including the two that were UNCOVERED at the prior grade |

**Every "already declared unmeasured" item checked and found honestly declared** `[MEASURED HERE]`:
`runtime-production` unmeasured · no `tsc` / no `vitest` / no TypeScript parity claim ·
`TF_FAMILY_META_ENFORCED` **default OFF** (so the live blast radius today is genuinely zero) ·
producer untouched · historical `80`/`81` unreconstructable. **None of these is overstated in any of
the three ARs.** The `80`/`81` stop is correct and was reached at a pre-registered stop condition,
not rationalised after the fact.

---

## 8. MANDATORY COVERAGE SECTION

### 8.1 Every data path used
1. `git show b8321dc9:<path>` / `git show HEAD:<path>` + `sha256sum` — blob identity across pin, HEAD, worktree.
2. `git log`, `git diff --stat`, `git status --porcelain -- src/` — tree state, twice (open and close).
3. Direct reads of executable lines in `spec_condition_compiler.py` and all seven lane test files.
4. `pytest` execution of the seven-file lane surface in the **isolated** tree (`108 passed`).
5. `pytest` execution of a five-file subset in the **shared** tree, read-only, at grade open (`83 passed`).
6. In-process import of the committed test module and direct invocation of `_census`,
   `_regression_population`, `_FORMS`, `_assert_the_census_actually_looked`, `_fail_if_offenders`.
7. Source-level mutation + `pytest` in the isolated tree (19 mutations across two batches).
8. Independent AST walk of all 212 non-`src/` `.py` files for `ConditionBinding` constructions.
9. Repo-wide grep for a committed member manifest.
10. Two-arm environment differential (`TF_FAMILY_META_ENFORCED` unset vs `true`).

**Non-overlap, per claim:** every mutation verdict rests on (a) reading the executable line and
(b) executing a mutant — a source read and a runtime observation, never one instrument twice.
The census and population findings each additionally have a **helper-level** path and a
**pytest-level** path that were computed independently and agree.

### 8.2 Positive-control witnesses for every absence claim
| Absence claimed | Positive control that proves the instrument can report a difference |
|---|---|
| "8 shapes evade the census" | the **6 enumerated shapes are DETECTED by the same harness**, at both helper and pytest level; and no-plant → GREEN, plant → RED, plant removed → GREEN |
| "0 `ConditionBinding` constructions outside `src/`" | the same AST walk finds **14** inside `src/` — the matcher is alive |
| "no committed member manifest" | the same grep **does** return the two files that name the test file |
| "N7/N9 redden nothing" | the **other 17 mutations from the same harness redden 1-26 tests each**; and N8, on the *same function*, reddens 2 — the population tests are not universally dead |
| "N7 is a real hole" | re-derived after my first N7 was a `SyntaxError`; the corrected mutant **collected** (`20 passed` both arms) before the hole was recorded |
| "no ordering-guard inversion" | N4 reddens tests in **both** flag arms — the matrix is not one-sided |
| "no external caller of the dispatch path" | the same grep returns the 3 in-file docstring hits, so it is searching the right surface |

### 8.3 Join keys for every correspondence claim
- **"blobs identical at pin / HEAD / worktree"** → join key = `sha256` of the blob content, per path, 8 paths × 3 sources.
- **"the isolated tree reproduces the shared tree"** → join key = `pytest` pass count on an identical explicit file list (`83`/`83`, `108`/`108`), plus the compiler `sha256` `621302a56987f19b` in both.
- **"my M5 agrees with AR-787's M5"** → join key = **test node-id names**, not counts; both of AR-787's two unpredicted names appear in my observed set. (The counts also match at 13, but the count is not the key.)
- **"my census `14` reproduces AR-787's `14`"** → join key = construction count under an independently written AST walk over the same root.
- **"mutation restored"** → join key = `sha256` PRE vs POST on the mutated file, asserted in the same command that ran the arm.
- 🛑 **NOT JOINED, DELIBERATELY:** my `95` is **not** joined to the historical `80` or `81`. Different instruments, different populations. AR-787 refuses that join pre-emptively and it is **right to**.

### 8.4 Where each plant physically lived
| Plants | Location | Shared tree touched? |
|---|---|---|
| 9 production mutations (M1-M8, M10) | `C:\tf30\src\engine\spec_condition_compiler.py` | **NO** |
| M5 (AST-relocated) | `C:\tf30` | **NO** |
| 14 census producer files (M9: 6 claimed + 8 evasions) | `C:\tf30\src\engine\planted_producer.py` | **NO** |
| 8 census fixtures (helper-level battery) | `tempfile.mkdtemp()` throwaway trees | **NO** |
| 9 + 1 supplement attacks (N1-N9, N7-corrected) | `C:\tf30` (`spec_condition_compiler.py`, `test_flag_off_parameterized_refusal.py`) | **NO** |
| 3 planted trees (break-control, tail-collision) | `tempfile.mkdtemp()` | **NO** |

**No measurement in this grade was taken while any plant was resident in the shared tree, because no
plant was ever resident there.** `[MEASURED HERE]` `git status --porcelain -- src/` returned exactly
`M src/engine/tests/test_synthetic_market_simulator.py` at grade open and at grade close, and all
eight graded blobs hash identically to the pin at both moments. **Nothing needs re-deriving.**

### 8.5 WHAT I DID NOT VERIFY
- 🛑 **`runtime-production` tree — `UNENUMERATED`.** Not opened. **This grade is not a pass on it.**
- 🛑 **No `tsc`, no `vitest`, no TypeScript execution of any kind.** The TS half of the parameter
  contract is **entirely unmeasured here**. **This PASS is NOT a pass on the TS contract.**
- 🛑 **`test_cloud_backend.py` — `UNENUMERATED`.** I confirmed it is absent from the canonical 95 and
  honestly declared everywhere; I did **not** run it and did **not** re-verify the hang.
- 🛑 **The whole-suite arms (`171`/`172`/`180`/`181`) are `RELAYED`.** I did not re-run
  `src/engine/tests/` in full. AR-785's `172` vs AR-783's `171` disagreement is **not adjudicated
  by me** — I neither confirm nor refute it. AR-785's three-arm by-name argument that Lane 29 did
  not cause it is `ARTIFACT-SOURCED`, not re-measured.
- 🛑 **The seven `ENVIRONMENT_GATED_UNVERIFIED` handlers' CONSUMPTION capability.** I verified they
  **refuse** (they are in the census loop); I did **not** enable any experiment flag, so whether
  their evaluators *could* consume a parameter remains `UNENUMERATED` — as AR-783 §7 declares.
- 🛑 **The producer.** Nothing populates `ConditionBinding.parameters` in production today
  (`MEASURED HERE`, 0 offenders / 14 constructions / 0 outside `src/`). The refusal grammar's
  correctness **against a real producer** cannot be graded until one exists.
- 🛑 **`_h_wait_bias` partial teaching** — supplying only `fast_period` yields `EMA(7, 50)` by the
  documented three-way taxonomy (`GENUINELY ABSENT → legacy default`). I read the executable lines
  and judged this **in-contract, not partial recognition**. I did **not** seek a ruling on whether
  the taxonomy itself is the right policy — that is a desk question, not a grader question.
- 🛑 **Cache *construction* ordering** has no direct runtime witness (only structural + transitive).
  I did not build one.
- 🛑 **The historical `80` and `81`** — I did not attempt reconstruction. AR-787's proof that no
  import-closure instrument was ever committed is `ARTIFACT-SOURCED`; I did not re-run its search.
- 🛑 **Lineage declared:** I am the same grader whose Lanes 25-27 pass produced F-A, F-B, F-E, M3 and
  M5 — the defects these three lanes were built to repair. I graded `spec_condition_compiler.py` in
  four prior passes. **I re-derived every band in this document from the current artifacts only and
  ignored my own prior scores**; but the reader should weigh that F-1 and F-3 are extensions of
  findings I originally minted, and an independent second grader would be the stronger check on them.

---

## 9. RECOMMENDED DISPOSITION

**GATE 2 MAY CLOSE ON THIS GRADE.** `PASS_WITH_BOUNDED_FINDINGS`, and **no finding touches any of the
four disqualifying categories.** The parameter consume-or-refuse surface is the strongest-tested
instrument I have graded on this campaign: 16 of 16 adversarial mutations caught, including the two
that were provably uncaught six hours ago.

**Owed, and NOT blocking Gate 2** (all are test-instrument repairs, zero production change):
1. **F-1 (HIGH)** — add a 2-hop chain to the population break-control. One planted module.
2. **F-2 (MEDIUM)** — commit the 95 members as a manifest and assert set-equality.
3. **F-3 (MEDIUM)** — extend the census predicate to `**`-splat and `*args` shapes, or state the
   enumerated-shape boundary inside the absence claim itself.

**Not owed:** F-4, F-5, F-6 are recorded for the audit trail.
