# EXTERNAL READ — 2026-08-09 — STEP 4 PASSES; CLOSE TWO DATA-GAP CONTROLS, THEN STEP 6 IS RELEASED

> **PROVENANCE:** GPT external advisor read, **operator-relayed chat, 2026-08-09**. Consumed and
> adopted by **`R-737`** (`0613cb52`). **Banked to disk 2026-08-09 in response to `AR-831 §5`.**
>
> 🛑🛑★★★★★ **WHY THIS FILE EXISTS, STATED AGAINST MYSELF: `R-737 §8` wrote *"THE READ'S TEN PROOF
> OBLIGATIONS ARE ADOPTED IN FULL"* and never transcribed them, while the read existed only in chat.
> `[MEASURED, AR-831 §5]` the worker could name `4` of `11` and correctly refused to claim the other
> seven.** ⇒ **`R-737` adopted a list BY REFERENCE TO A DOCUMENT ITS EXECUTOR COULD NOT OPEN.**
> ★★★★★ **`AN AUTHORIZATION THAT POINTS AT A DOCUMENT THE WORKER CANNOT READ IS AN UNENUMERATED
> ORDER WEARING A COMPLETE ONE'S CLOTHES.` The campaign's own law — `HANDOVER §0`: *"NEVER ASSUME
> DELIVERY — write the authorization INTO the ruling, self-contained, never only into a message"* —
> is quoted in `R-737` itself. I broke it in the ruling that cited it.**
>
> ⚠️ **STATUS: `[EXTERNAL OPINION]` — ZERO AUTHORITY. Reproduced VERBATIM below, not paraphrased**
> (`v4 §2.5` carrier-discipline: duplicate verbatim and point at it; paraphrase is what eroded the
> BLUEPRINT ladder twice). **`R-737` records where this desk ADOPTED it, where it ADDED to it
> (obligation `11`), and where it left a question open. Disagreement resolves BY MEASUREMENT.**
> ★ Its two blob citations were **verified real** at `R-737 §1` before anything was adopted.

---

## VERBATIM TEXT

GPT EXTERNAL ADVISOR RULING — R-737 — STEP 4 PASSES; CLOSE TWO DATA-GAP CONTROLS, THEN STEP 6 IS RELEASED

MAIN VERDICT

STEP 4 IS COMPLETE.

I verified the pushed branch directly:

- adapter blob: cdab9577…
- test blob: e0cdf37…
- delegation reaches `compute_opening_range_breakout`;
- duration and session start are passed explicitly;
- incomplete windows cannot reach the legacy calculator;
- the lock-time readout row is excluded from aggregation;
- the false futures claim is removed;
- the locality mutation is correctly paired;
- production routing remains untouched.

The worker's inline-max/min mutation is strong evidence: exactly the delegation test failed while the other twenty remained green. That proves the new control detects wiring, not merely matching numbers.

STEP 5 STATUS

STEP 5 is almost complete but requires two small measured controls before closure.

1. NON-FINITE MARKET DATA

A window containing `NaN`, positive infinity, or negative infinity must never return `COMPLETE`.

Because this is bad market data—not unsupported configuration—it must return:

`INCOMPLETE_OPENING_WINDOW`

All numeric output fields must remain `None`.

2. MISDECLARED BAR INTERVAL

Convert the worker's derived claim into two fixtures:

- actual 1-minute observations declared as 5-minute bars;
- actual 5-minute observations declared as 1-minute bars.

Both must refuse with `INCOMPLETE_OPENING_WINDOW`, with a correct declaration as the positive witness.

No additional STEP 5 work is authorized. Real-market-data testing is not required here; deterministic candles are the correct instrument.

STEP 6 IS CONDITIONALLY RELEASED

Once those STEP 5 controls are green, the worker may proceed directly into STEP 6 without waiting for another ruling.

THE STEP 6 DESIGN

The teacher supplied alternatives. Production must expand them, never choose among them.

For every `OpeningRangeDefinition`:

1. Preserve one source condition.
2. Read its complete typed `variants` collection.
3. Produce one executable candidate for every taught variant.
4. Give every candidate an explicit:
   - duration;
   - variant label;
   - session start;
   - timezone;
   - source condition ID;
   - provenance;
   - candidate ID;
   - cache identity.

For the golden strategy, exact candidate membership is:

- opening-range 5m;
- opening-range 15m;
- opening-range 30m.

No default candidate exists. No "primary" duration exists. No operator chooses one.

In simple English:

THE SOURCE CONDITION SAYS "5, 15, AND 30."

THE COMPILER MAKES THREE RUNNABLE VERSIONS.

IT DOES NOT PICK A FAVORITE.

PRODUCTION ROUTING REQUIREMENTS

In the same routing commit:

- `FAMILY_META["OPENING_RANGE_DEFINITION"]` becomes supported;
- its primitive names the typed adapter;
- `PRIMITIVE_RESOLVERS` gains the real resolver;
- `ENFORCED_DISPATCH` gains the real executable route;
- the TypeScript mirror changes with it;
- parity remains exact;
- no fallback to `compute_structure_state` exists.

A bare adapter registration that turns the old REDs green while production lacks a variant is forbidden.

The binding may be called executable only when deterministic expansion supplies the explicit candidate variant.

STEP 6 REQUIRED PROOF

Starting from the frozen extraction artifact:

1. Production emits exactly the taught candidate set.
2. Each candidate reaches `compute_opening_range_state`.
3. Deterministic candles make 5m, 15m, and 30m produce deliberately different ranges.
4. Changing one duration changes:
   - candidate identity;
   - cache identity;
   - computed output.
5. Removing one candidate makes exact-membership testing fail.
6. Giving two variants the same cache identity fails.
7. Routing the definition back to `compute_structure_state` fails.
8. The genuine neighboring structure condition remains green.
9. Both existing ordered production REDs turn green without weakening their predicates.
10. The executable test—not merely primitive metadata—proves the adapter computed a `COMPLETE` state.

STOP CONDITIONS

Stop and report if:

- a duration must be invented;
- a video or strategy ID is needed in production code;
- the typed source cannot supply its variants;
- expansion changes unrelated condition cardinality;
- another family's route changes;
- candidate cache identities collide;
- a raw unexpanded condition can execute;
- the breakout sentence enters this work.

UNCHANGED SCOPE

Do not touch:

- breakout confirmation;
- close-versus-wick ambiguity;
- stops or targets;
- the remaining golden conditions;
- the second teacher's breakout/retest logic;
- futures portability;
- unrelated dirty files;
- the known cloud-test hang.

BOTTOM LINE

The calculator breakthrough is complete.

The next breakthrough is connecting it to production without choosing between the teacher's three versions.

STEP 4: COMPLETE.
STEP 5: TWO BOUNDED CONTROLS FROM COMPLETE.
STEP 6: RELEASED AFTER THOSE CONTROLS PASS.

---

## THIS DESK'S DISPOSITION — where `R-737` ADOPTED, ADDED, and LEFT OPEN

- ✅ **ADOPTED IN FULL:** the `STEP 5` two controls (`R-737 §6`) · the `STEP 6` design (`§8`) · the
  production-routing requirements · the ten proof obligations · the stop conditions · unchanged scope.
- ✅ **ADOPTED WITH REASONING, NOT DEFERENCE (`R-737 §7`):** *"real-market-data testing is not required
  here."* **Scope of that settlement is the ADAPTER LAYER ONLY** — it says nothing about fidelity
  against real data downstream, which is `R-648` stage 5 and remains open.
- ⭐ **ADDED BY THIS DESK — OBLIGATION `11` (`R-737 §8`), because the read left a hole:** requirements
  `4` and `6` are **both satisfied by a cache identity that is RANDOM PER RUN** — it never collides and
  it always changes. ⇒ **`11`: CACHE IDENTITY IS STABLE ACROSS RUNS for an unchanged candidate, proven
  by computing it twice in SEPARATE PROCESSES and asserting equality.**
  ★★ **`A UNIQUENESS TEST AND A SENSITIVITY TEST ARE BOTH SATISFIED BY RANDOMNESS; ONLY A STABILITY
  TEST EXCLUDES IT.`**
- ⭐ **STOP CONDITION EXTENDED to match:** candidate cache identities colliding **or proving unstable**.
- ✅ **LEFT OPEN BY THE READ, ANSWERED BY MEASUREMENT (`AR-831 §4`):** the read requires *"the
  TypeScript mirror changes with it; parity remains exact"* without saying whether parity compares
  **expanded candidates** or **binding declarations**. `R-737 §8` refused to assume and ordered it
  measured first. `[MEASURED, AR-831 §4, `scripts/check-spec-binding-plan-parity.ts:109-133`]` the
  compared shape carries **no candidate, variant, duration or expansion field** ⇒ **IT COMPARES
  DECLARATIONS; THE MIRROR MUST *NOT* EXPAND.** ★ **The symmetrical assumption would have produced
  expansion in two languages for a gate that inspects neither.**
