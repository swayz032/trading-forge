# EXTERNAL READ — 2026-08-08 — AR-804 ACCEPTED AS CALIBRATION EVIDENCE; CLASSIFICATION REMAINS OPEN

> **PROVENANCE:** Arrived as **OPERATOR-RELAYED CHAT**, 2026-08-08, in response to `AR-804`.
> **NOT** from `origin/external-advisor/gpt-rulings` (stale at `953a907c`, 2026-08-02).
> **CONSUMED BY `R-724`** — this banner is written BY that ruling, IN its commit, never in advance
> (`R-722 §1`).
>
> ⚠️ **READ THIS BEFORE THE TEXT: THIS READ SAW `AR-804` AND *NOT* `AR-805`.** `AR-805` landed at
> `23:39:30Z`, after the read was composed. **Two of its five `AUTHORIZED NEXT ORDER` items were
> already discharged on arrival, and its `ENGINE_PRIMITIVE_MISSING` provisional was already refuted
> by measurement.** See `R-724 §4`. **This is not a defect in the read — it is a timing join, and the
> read's own discriminator table anticipated the result correctly.**
>
> **A CHANNEL IS NOT AN AUTHOR.** Audited on merit in `R-724`: what is adopted, what was already
> answered, and the one frame the campaign's own `R-676` refuted before.

---

GPT EXTERNAL ADVISOR RULING — AR-804 ACCEPTED AS CALIBRATION EVIDENCE; CLASSIFICATION REMAINS OPEN

DECISION: CONTINUE PHASE A. NO REPAIR AUTHORIZED.

AR-804 successfully proves that the trace rig works and has exposed a real handoff failure:

The frozen artifact preserves `9:30 a.m. Eastern to 9:35 a.m. Eastern` as prose, but that meaning is never lowered into executable typed semantics.

The compiler's refusal is correct and must remain intact. Mapping that exact five-minute interval to `ny_am = 07:00–10:00 ET` would be a fabricated substitution.

CLAIM CORRECTION REQUIRED

The headline claim that "the teacher left nothing out" is not proven.

The defensible claim is:

> The frozen extraction artifact preserved this particular clock-window fact verbatim. Source-to-artifact completeness remains unverified because the frozen transcript has not been located.

Amend the durable artifact accordingly. Do not claim lesson fidelity without lesson-side evidence.

PRELIMINARY CAUSAL CLASSIFICATION

For this measured fact:

- `SOURCE_INCOMPLETE` is not supported.
- `EXTRACTION_MISSING_REQUIRED_INFORMATION` is not supported for the clock values themselves.
- The earliest measured failure is currently `CANONICAL_TERM_UNRESOLVED`: preserved prose has no typed canonical representation.
- `PARAMETER_SCHEMA_MISMATCH` is secondary because no start, end, timezone, or duration structure crosses the boundary.
- `ENGINE_PRIMITIVE_MISSING` remains provisional until the import-closure measurement is complete.

Do not implement arbitrary start/end parameters on `is_in_killzone` yet.

An opening-range mechanism may require more than clock-window membership:

- accumulate high and low during the interval;
- freeze them when the interval closes;
- prevent breakout evaluation before formation completes;
- preserve the correct trading date and timezone;
- expose the resulting levels to later ordered conditions.

Therefore, a widened kill-zone predicate may be the wrong capability identity even if it accepts exact times.

TRANSCRIPT RULING

No frozen transcript has been supplied to this advisor. Its existence is `UNKNOWN`, not `NO`.

Perform one bounded exact-ID search across authoritative extraction roots, manifests, archives, and repository history for `st5e-YJRfKc`.

If found:

- record its authoritative path;
- hash it;
- verify its provenance;
- run Probe A.

If absent:

- record `SOURCE_BASELINE_UNAVAILABLE`;
- continue artifact-to-engine diagnosis;
- treat transcript acquisition as a V1.0 certification blocker, not a Phase A blocker.

Do not silently substitute a newly retrieved transcript for the original frozen source baseline.

AUTHORIZED NEXT ORDER

1. Run Probe C.
2. Run Probe B using the result of Probe C.
3. Measure the reachable import closure for any exact-clock or opening-range constructor.
4. Complete Row 2 for `WAIT_STRUCTURE#0`.
5. Preserve exactly one primary blocker per row.
6. Make no production repair yet.

THE NEXT REPORT MUST DISCRIMINATE

- Exact opening-range primitive exists and is reachable:
  classify the failure at canonicalization or binding.

- Primitive exists but is unreachable from the production compiler:
  classify the missing handoff precisely.

- Only named-session membership exists:
  classify the opening-range capability as missing or wrong-identity.

- Full opening-range construction is absent:
  classify `ENGINE_PRIMITIVE_MISSING`, with the required temporal behavior stated without implementing it.

- Transcript contradicts or materially expands the artifact:
  stop and report source-to-artifact loss separately.

AR-804 is a valuable Phase A result. It does not authorize a fix yet, but it sharply narrows the investigation:

> The rule survived extraction as prose, the current compiler correctly refused a false substitute, and the unresolved question is whether Trading Forge lacks semantic lowering, the correct opening-range engine capability, or both.
