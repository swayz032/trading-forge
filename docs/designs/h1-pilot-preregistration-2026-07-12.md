# H1 Human-in-Loop Pilot — PRE-REGISTRATION (FROZEN 2026-07-12)

> **Law 4.** Frozen BEFORE the pilot reads anything — the bar before the score, same discipline as Wave 0. This pre-reg executes the human-in-loop fork (H1's pre-registered economics outcome, `h1-wave3-TERMINAL-fork-2026-07-12.md`) AS the road to H2. Outranks memory. Un-pinned terms resolve to the strictest reading.

## §0 — What the pilot IS (and why it exists)
H1's terminal result: the cheap fully-automatic tier-2 discourse pass cannot be conformantly instrumented at local-model scale (proven by execution, twice). H1 therefore NARROWED to its pre-written economics fork: **fidelity achievable at human-in-loop cost.** The pilot runs the Wave-4-assembled conveyor end-to-end on the SEALED 16-video set at the new economics, produces fidelity certificates, and hands them to H2's frozen two-read rule. The conveyor is now **tier-1 (deterministic detection) + tier-3 (blind adjudication)**; the ~30% margin the retired tier-2 would have handled routes to tier-3.
**The adjudicators are FRESH-CONTEXT AGENTS, not literal humans** — control-gated, blind, per the Wave-1 shakedown protocol (proven 92% inter-rater consistency @ ~1.1K tok/item). 'Human-in-loop cost' = agent-hours per video, NOT dollars/hiring. It kills SCALE ('mine a thousand educators cheaply'), not the MISSION (H2's survivor read fires at ANY N).

## §1 — THE BAR (FROZEN)
Read at the pilot sitting, ONCE, against the SEALED 16-video set (`h1-sealed-fresh-set-2026-07-12.json`, sha `8e39ffe1`, selected before any instrument existed → selection-clean).
- **QUALITY bar [calibration estimate, unchanged from Wave-0 §1]:** **≥ 60%** of the 16 reach **certificate-grade** (every spine condition classifies tier-1 or tier-3 with a quote anchor AND all 5 compile-integrity lints pass). N=16 ≥ 10, so a verdict (not LOW_POWER) is read.
- **ECONOMICS — REFRAMED from a gate to a MEASUREMENT + affordability ceiling:** the Wave-3 `≤1 tier-3/video` rider is **RETIRED** (it gated the cheap tier, which is dead). Instead: MEASURE mean tier-3 (agent) adjudications per video. **Pre-registered EXPECTED range ~5-9/video** (from the shakedown baseline). **Affordability CEILING (frozen): ≤ ~15 adjudications/video** — beyond that even the human-in-loop fork is uneconomic → escalate (a deeper finding, §2). Within the ceiling = the fork is affordable (an evening of agent work per handful of videos); the exact number is a receipt, not a pass/fail.
- **Scope line (Law 7):** sealed-16 · taxonomy version · extractor version (tier-1 + tier-3, tier-2 retired) · engine/snapshot. Per-video slicing = annotation-tier; verdict is the aggregate.

## §2 — FAILURE MEANINGS (pre-written)
- **Miss on QUALITY** (<60% cert-grade even with agent-adjudication): iterate under a 2-pass budget; two passes short → the FIDELITY INSTRUMENT itself falls short at human-in-loop cost too (a deeper finding than the economics fork — it would question whether the surface is certifiable at all at this model scale, routing toward the H1 'not-yet' verdict / source-agnostic reconsideration).
- **Miss on ECONOMICS** (mean adjudications/video > the ~15 affordability ceiling): the human-in-loop fork ITSELF is uneconomic → escalate; the parked stronger-model tier-2 option (§5) becomes the next pre-registered economics experiment rather than a future one.
- **Neither miss is an H2 falsification** — H2 asks whether a certified strategy survives the battery; the pilot only asks whether the conveyor produces certificates at bounded cost.

## §3 — READ-ONCE + INDEPENDENT VERIFICATION (inherited)
Unseal the 16 → run the tier-1+tier-3 conveyor end-to-end → read the §1 bar ONCE → independent fresh-context re-verification (grading-integrity, doer≠grader) → pilot verdict. Tier-3 adjudication uses the shakedown's control-gated blind protocol (controls ≥4/5 each direction, closed taxonomy, cost-free cannot-determine, quote-anchored, answer key never seen by raters). The blinding leak-scan runs on every rater packet BEFORE dispatch (house standard).

## §4 — HANDOFF to H2 (already frozen: `h2-source-thesis-preregistration-2026-07-07.md`)
Certificate-grade strategies → the full anti-overfit battery (PBO/DSR/WFE/B14/B15) on the re-certified G4 engine. **H2's rule is UNCHANGED and reachable at fork economics:** SURVIVOR read at ANY N (≥1 robust survivor = the mission HAS its scaleable strategy → survivor-forensics protocol → small-real-capital graduation); RATE read only at N≥30 (≥5% scale-scout / 1-5% marginal / 0-survivors falsify-source-and-pivot). The pilot's cert count (≤16 videos × strategies-each) feeds the survivor read immediately; the rate read waits for N≥30 accumulated across pilots.

## §5 — PARKED OPTION (registered, NOT reopened): stronger-model tier-2
A cloud reasoning model on ONLY the ~30% margin (the items tier-1 falls through) is a FUTURE pre-registered economics OPTIMIZATION — legitimate precisely because it does NOT touch the closed gemma budget (it's a new tier, not a gemma edit). Measured against ADJUDICATION AGREEMENT (does the cloud tier-2 match the blind tier-3 adjudicators?). It WAITS until the fork's baseline economics are measured in practice (this pilot). Registered here so it's a known option, not a 3am idea.

## §6 — CERTIFICATE SCHEMA amendment (tier-2 retired)
The frozen Wave-0 §4 schema stands EXCEPT `classifying_tier` ∈ {1 deterministic, 3 adjudicated} — the tier-2 (discourse) value is RETIRED with cause (the terminal fork). The `compile_integrity` block (5 lints: direction-conflation, unsat-SAT, OR-alternatives, F-2 coverage, causality) and provenance (video_id, transcript_sha256, extractor_version, taxonomy_version) are unchanged. Wave-4's assembler emits this amended schema.

---
*Frozen 2026-07-12 at the doer seat, BEFORE the pilot runs and BEFORE the sealed 16 is unsealed. Vaulted FF to `corpus-v3-gate3-cert-2026-07-06`. The fork executes as the road to H2; the bar exists before anyone falls in love with the score.*

---

## ADDENDUM — F-1 RULING: the two-grade certificate ladder (OPERATOR-AUTHORED 2026-07-12, post-independent-grade, BEFORE the seal breaks)

> Authored by the operator after the Wave-4 independent ablation grade surfaced F-1 (3 of 5 compile-integrity lints check compiled-DSL topology that no stage produces at the certificate layer → they would vacuously PASS in the tier-1+tier-3 pilot conveyor). This is NOT a waiver of §4. It is a two-grade ladder that keeps §4's strictest reading as the standard H2 consumes, while letting the pilot issue an explicitly lesser, honestly-labeled instrument. The pilot §1 QUALITY bar and ECONOMICS measurement above are UNCHANGED; this addendum only fixes what "certificate-grade" means at the pilot vs at H2, and locks the deferred work to the read it protects.

**§A — NO lint ever vacuously passes (non-negotiable repair, ships BEFORE the pilot runs).** Per-lint status becomes 3-state: `PASS` / `FAIL` / `NOT_EVALUATED`. In the pilot conveyor (no compiled topology) the three structural lints (`direction_conflation_lint`, `unsat_sat_check`, `or_alternatives_honored`) AND `causality_lint`'s same-bar-opt-out leg emit **`NOT_EVALUATED`** with a machine-readable reason (`no_compiled_topology`), on EVERY certificate field. A `PASS` on a check that structurally could not fail is a false declaration with paperwork — the four-dormancies disease born into the new instrument on day one. `NOT_EVALUATED` is honest scoping satisfied ON THE ARTIFACT (Law 7), not in a memo beside it. `NOT_EVALUATED` NEVER counts as `PASS` toward any grade.

**§B — PILOT-GRADE = the live subset (the pilot's actual question, not a compromise).** A pilot certificate reaches **pilot-grade** iff every spine condition classifies (tier 1/3) with a verbatim anchor AND the LIVE lints pass: `f2_coverage_gate` + `causality_lint`'s impossible-ref regex leg. The three structural lints + causality's same-bar leg are `NOT_EVALUATED` (§A) and do not gate pilot-grade. The frozen §1 ≥60% bar reads against pilot-grade. This fully serves the pilot's load-bearing questions — extraction fidelity (does every spine condition classify with a verbatim anchor?) and economics (adjudications/video). The certificates come off the line self-describing their scope.

**§C — FULL-GRADE + the binding H2 precondition (A is a hard gate, not a someday).** A **full-grade** certificate = every spine condition classified+anchored AND all FIVE compile-integrity lints `PASS` on REAL compiled topology (nothing `NOT_EVALUATED`). The compile-stage topology producer (option A: wire extraction output through the existing compiler so the assembler reads real `ConditionTopology`/`or_branches`/`same_bar_fill`/`signal_lag`) is a **PRE-REGISTERED PRECONDITION of the H2 battery read** — sequenced BETWEEN pilot and H2, with its own ratify-packet + build/grade loop (mostly wiring through a compiler that already exists). **PINNED NOW, before any pilot number exists:** the H2 pre-reg's "robust survivor … with a fidelity certificate" means a FULL-GRADE certificate. **Every pilot-grade certificate UPGRADES to full-grade before any battery verdict is attributed to its source.** No survivor is EVER read through a scoped certificate. Rationale (why battery-catches-it is insufficient): the battery catching compile-incoherence is not the point — ATTRIBUTION is. A certified-faithful strategy that dies in the battery because the compiler mangled its topology would be recorded as "the educator's strategy failed" when the truth is "our pipeline broke it" — a pipeline artifact masquerading as evidence about the source population, polluting the exact rate read the campaign exists to protect. Compile-fidelity is the second half of the attribution chain the certificate was invented to guarantee.

**§D — Law-4 resolution (strictest reading kept, no waiver).** §4's all-five requirement is NOT relaxed — it is preserved as the standard for FULL-GRADE, the only grade H2 consumes. The pilot issues an explicitly lesser instrument, labeled as such in every field (§A/§B). Strictest reading kept; honest instrument shipped; the escape hatch has a lock (the §C H2 precondition) that the doer did not author and the pilot's numbers cannot pick.

**Direction-check (operator, for the record):** B-repaired keeps the pilot's live path to failure fully intact (the ≥60% bar on the live subset is the real fidelity question, untouched), REMOVES fields that would have flattered certificates (vacuous PASS → NOT_EVALUATED), and converts the deferred work into a HARD GATE on the read it actually protects (the H2 survivor attribution). That is the opposite signature of an escape (Law 6).

*Addendum frozen 2026-07-12, operator-authored, BEFORE the sealed 16 is unsealed. Execution order: NOT_EVALUATED repair (§A) → this addendum frozen → unseal 16 → run tier-1+tier-3 conveyor → read the §1 bar ONCE (blind adjudicators, independent re-verify). A (§C) builds between pilot and H2.*

---

## ADDENDUM 2 — SEGMENTATION RULING: the extractor's condition IS the unit (OPERATOR-AUTHORED 2026-07-12, BEFORE the seal breaks)

> Authored after the pilot-conveyor-runner independent grade proved `_segment_transcript` (naive sentence-punctuation split) is an UNREGISTERED INSTRUMENT DECISION that materially moves cert-grade composition + the §1 tier-3-adjudications/video economics number AND silently drops conditions. This is NOT a preference call — it is **F-5's class caught one level up**: mechanical text-slices are a PROXY POPULATION wearing the extractor's clothes. A certificate that asserts fidelity about clause-slices while H2's battery runs on EXTRACTOR OUTPUT would sever the attribution chain the certificate exists to guarantee — the same reasoning that killed the vacuous lints in the F-1 addendum: **the artifact must describe the production thing, not a stand-in.** Option B (a deterministic mechanical clause-rule) is DISQUALIFIED by law. The extractor's condition is the unit; it always was.

**THE UNIT (ruling).** The pilot conveyor is `transcript → production gemma transcript_extractor → extracted strategy + verbatim-anchored spine conditions → tier-1/tier-3 classify each condition's surface + fidelity → compile-lints → certificate`. Tier-1 returns to its GRADED CONTRACT (one condition per call — the 143 design conditions were pre-segmented; it classifies a condition, it does not derive conditions from raw text). The conveyor under test now INCLUDES the thing the campaign exists to test — the LLM entering the conveyor is not a compromise, it is the subject taking its own exam. The frozen §1 bar is unchanged and is what it always described: "a fresh video set reaches certificate-grade extraction."

**Five clauses A needs to be honest (all FROZEN):**

1. **Extractor version-pinned + FROZEN for the pilot.** The production gemma `transcript_extractor` at its current landed SHA is stamped into every certificate's `provenance.extractor_version`. NO extractor changes during the pilot — the pilot measures the CURRENT extractor; improving it is post-read iteration under the extractor's own parity-gated budget (the 5-fixture parity test, CLAUDE.md §2b). Without this clause the pilot silently becomes a tuning loop against the sealed set.

2. **One extraction pass per video, read-once, outputs vaulted immediately.** Non-determinism is a KNOWN, pre-registered property: seed/params pinned; incremental caching so a crash never forces re-extraction ambiguity; if a video's pass must re-run (crash mid-video), the re-run REPLACES that whole video's pass — no cherry-picking between passes (the determinism-envelope precedent). The pilot reads ONE extraction pass per video.

3. **The non-drop invariant lives at BOTH levels (both already built) — no stage where a condition silently disappears.** (a) EXTRACTOR-level coverage = the F-2-fixed `f2_coverage_gate` (transcript-enumerated speaker items vs extraction; the Python lint is parity-fixtured to the exact-token corrected semantic, LIVE in pilot-grade). (b) CLASSIFICATION-level = the runner invariant: every extracted spine condition gets a tier-1 detection OR an explicit `Tier1FallThrough` (→ tier-3) — nothing vanishes. State the pairing explicitly: extraction-covers-the-transcript AND every-extracted-condition-is-classified-or-fell-through.

4. **Unanchored conditions count AGAINST cert-grade, honestly.** The schema demands a verbatim `quote_anchor` + `char_span`; the extraction machinery carries evidence spans, so the runner threads them through. Any condition the extractor emits WITHOUT a locatable verbatim anchor is a FIDELITY FAILURE IN THE MEASUREMENT — not a bug to patch mid-pilot. That is the anchor floor doing its job (claim-scoping baked in, §4).

5. **Failure attribution recorded per-condition (ANNOTATION-TIER; the §1 bar is unchanged).** When a video misses cert-grade, the record says WHERE: coverage-miss / unanchored-condition / classification-fallthrough-unresolved / tier-3-fail / lint-fail. The ≥60% read then arrives WITH its own diagnosis distribution — actionable in either direction — at zero cost, since the assembler already tracks per-condition status. This annotates the verdict; it does not change the aggregate bar.

**Dry-run bonus (noted):** exercising the REAL extractor on a NON-SEALED transcript smoke-tests transcript-fetch AND gemma health in the same rehearsal — two of the three unseal greens (#2 dry-run + #3-transcript-fetch/gemma) checked by one throwaway run.

**Execution sequence (unchanged in spirit):** freeze this addendum → rework the runner to it (extractor-as-unit + the 5 clauses) → independent re-grade → dry-run re-proof WITH the real extractor → three greens (runner graded · dry-run proven · tower/transcript/gemma verified) + fresh capacity → unseal.

**Meta-record (count it):** this is the SECOND time the pre-unseal check has caught an instrument decision hiding inside an "obvious next step" (first: F-1 vacuous lints; now: segmentation-as-proxy-population). The seal is doing exactly what seals are for — making us finish the instrument before the instrument gets to speak.

*Addendum 2 frozen 2026-07-12, operator-authored, BEFORE the sealed 16 is unsealed and BEFORE the runner is reworked. Vaulted FF to `corpus-v3-gate3-cert-2026-07-06`.*

---

## ADDENDUM 3 — ANCHOR RULING: the anchor-locator (A-prime — LLM proposes, mechanics verify) (OPERATOR-AUTHORED 2026-07-12, BEFORE the seal breaks)

> Authored after the runner rework proved Addendum-2 clause-4's premise FALSE: the frozen production extractor emits spine conditions WITHOUT verbatim evidence anchors for the fields tier-1 targets (`entry_sequence`/`confluences`/`stop`/`targets` carry no quote; only the vocabulary-only `speaker_concepts` pass carries `transcript_quote`, a category mismatch). The certificate schema §4 requires a verbatim `quote_anchor`+`char_span` per condition → the pilot as-wired would read ~0% cert-grade, a PIPELINE GAP masquerading as fidelity failure. Resolution is **A-prime**, NOT A-as-schema-change and NOT B.
> **Why not A-as-schema-change (adding a per-condition quote field to the extractor):** the schema-IS-the-boundary law (minted 2026-07-12, [[feedback_schema_is_decision_boundary_constrained_sampling_2026_07_12]]) applies to ADDING fields too. Requiring the model to emit a verbatim quote per condition perturbs the constrained-sampling distribution (attention shifts; hard-to-quote conditions drop or reword toward quotable text) — exactly the "good drift in the extractor during the pilot" clause 1's freeze forbids. Amending a pre-registered freeze is precedent-currency not spent when a design exists that needs no amendment.

**A-PRIME — the anchor-locator (a CONVEYOR instrument, not an extractor change):**
1. **Extractor stays FROZEN at its current SHA. Clause 1 UNAMENDED.** It emits exactly what it emits today.
2. **New conveyor component — the anchor-locator:** given (transcript, ONE extracted condition) → return the literal transcript span that grounds it, OR abstain. Local gemma, ~one call per condition, trivial economics. Ships its OWN birth fixtures from birth (locates-known / abstains-on-ungroundable / literal-only) and its OWN independent grade. Same architectural move as the pre-call decline gate — an LLM step whose output a mechanical check owns.
3. **Verification is MECHANICAL + EXACT — this is what makes an LLM locator safe.** The located quote MUST be a literal substring of the transcript (whitespace-normalization ONLY, F-2's exact-token discipline — NOT fuzzy). Hallucinated quote → fails the substring check → UNANCHORED. Locator declines → UNANCHORED. Both failure modes land honestly BY CONSTRUCTION; the gemma no-calibrated-middle finding cannot hurt the measurement because the mechanical check owns the truth.
4. **Anchor-SUPPORTS-condition stays tier-3's job.** The blind adjudicators already read quote-versus-condition as part of fidelity. A mislocated-but-literal anchor is caught by the rater, where judgment belongs — NOT by the locator (which only proves the span is real transcript text).
5. **UNANCHORED counts AGAINST cert-grade, exactly as clause 4 said** — but now it measures something REAL: the conveyor's ability to ground the current extractor's conditions in the trader's words, a legitimate component of the H1 answer rather than a guaranteed-zero artifact.

**A-as-schema-change is RE-SEQUENCED, not rejected:** registered as the POST-PILOT extractor-upgrade candidate ("quoting-as-it-extracts may DISCIPLINE extraction") under its own parity-gated ratify-packet with a before/after extraction-stability check on DESIGN-side data — AFTER the pilot has measured the frozen baseline. Improving the witness is next quarter's work; the pilot measures the witness we have. **B stays DEAD** (moves fuzziness into the verifier, permanently weakening the anchor floor for every future certificate; A-prime keeps the verifier exact forever and puts the burden on a producer whose failures are honest). **C stays DEAD** (~0% read for a pipeline reason = the mismeasurement the seal exists to prevent).

**Sequence:** locator build + birth fixtures → independent grade → the held rework commits with the locator wired as its anchor source (its location code already correct per the rework grade) → rework re-grade → dry-run re-proof on a NON-SEALED transcript (now exercising extractor + locator + fetch + gemma in one rehearsal) → three greens (locator+rework graded · dry-run proven · tower/transcript/gemma verified) + fresh capacity → unseal. Junction cleanup per the pinned rule (remove the LINK, never the tree).

**Meta-record (count = 3):** three pre-unseal catches — vacuous lints (F-1), segmentation-as-proxy-population (Addendum 2), phantom anchors (this addendum) — each an instrument decision hiding inside an "obvious next step," each caught BEFORE the seal paid for it. The seal has not delayed the pilot; the seal is BUILDING the pilot — forcing the conveyor to become real before it is allowed to speak.

*Addendum 3 frozen 2026-07-12, operator-authored, BEFORE the sealed 16 is unsealed. Vaulted FF to `corpus-v3-gate3-cert-2026-07-06`.*

---

## ADDENDUM 4 — CATCH #4 RULING: dual-read gate + two-stage tier-3 packet, and the correction of §4's false backstop (OPERATOR-AUTHORED 2026-07-12, BEFORE the seal breaks)

> Authored after the independent re-grade of the anchor-locator wiring (finding F-2026-07-12-A, CRITICAL) proved **Addendum 3 §4's backstop claim FALSE AS WIRED.** Two SEPARATE defects: (1) ROUTING — tier-1 now classifies the located quote, and a mis-grounded-but-literal anchor whose quote carries tier-1 surface FIRES and counts OK; tier-1 fires never enter `_build_tier3_packet` (fallthroughs only), so nothing audits the fire path; (2) PACKET — even on fallthrough, `_build_tier3_packet` hardcodes `extracted_condition_type`/`extracted_object` to None and raters judge role "from the quote alone", so no anchor-supports-condition check happens anywhere. **Dual-read closes (1) ONLY; (2) stays open under dual-read-alone** — a divergent item falls through to a judge who still cannot see the condition. Both must be fixed for §4's backstop to describe something real. The located-quote classification DIRECTION stays correct + endorsed; span-choice INTEGRITY is the whole question.

**§4 CORRECTION (frozen docs are corrected in writing, never silently healed).** Addendum 3 §4 as originally frozen asserted "a mislocated-but-literal anchor is caught by the rater." That was FALSE as the wiring stood at grade time (F-2026-07-12-A). It is made TRUE by the two fixes below (dual-read routing + two-stage packet); §4 is to be read as corrected by this addendum. The original wording stands in the record as the falsified claim; this is its correction, cited.

**FIX 1 — DUAL-READ AGREEMENT GATE (closes the routing defect).** Tier-1's detectors are DETERMINISTIC patterns, so run them on BOTH the extractor's condition text AND the located quote at ZERO extra model calls. AGREEMENT (same surface_class / both silent) → classify. DISAGREEMENT → honest fallthrough to tier-3. The flatter path dies mechanically — a mis-grounded anchor whose surface diverges from the condition's own surface can no longer fire uncaught. Two-path law compiled into the architecture, fails toward suspicion per item, for free.

**FIX 2 — TWO-STAGE TIER-3 PACKET (closes the packet defect; blinding PRESERVED, order-locked).** The "quote alone" instruction is DELIBERATE F-5 BLINDING (raters anchor on the trader's words, never our spec-side strings) — naively revealing the condition would contaminate the role judgment with our paraphrase. So the repair is a TWO-STAGE rater task within ONE packet, READ-ORDER LOCKED like every gate this campaign (controls-then-targets, same spine as the NEUTRAL adjudication):
- **Stage 1 (blind, verbatim-preserved):** judge the quote's role FROM THE QUOTE ALONE — the existing `{gate-strength, context, cannot-determine}` closed-taxonomy call, unchanged. The blind read is preserved byte-for-byte; the answer is committed before Stage 2 is revealed.
- **Stage 2 (revealed):** NOW reveal the extractor's condition and answer the SEPARATE question — **does this quote express this condition?** Support verdict ∈ {confirmed, partial, denied}, justification REQUIRED. Stage 2 cannot be read until Stage 1 is committed (read-order lock; a leak-scan-equivalent guard that Stage 2 content is absent from the Stage-1 rater view).
- The **support verdict becomes a certificate field** on every tier-3-adjudicated condition (`adjudication_verdict.support` ∈ {confirmed, partial, denied} + justification). A `denied`/`partial` support downgrades the condition (it is not a faithfully-grounded gate). §4's backstop now describes a field that EXISTS.

**REDIRECT (scope-narrowing) REJECTED — one sentence:** "pilot-grade measures groundability + surface-detection, not condition-faithfulness" would hollow the certificate's central claim — condition-faithfulness IS the pilot; a certificate scoped away from it certifies the conveyor's plumbing, not the trader's rules.

**TWO PRE-COMMITMENTS (frozen before the numbers move):**
1. **Dual-read WILL raise fallthrough** → more tier-3 load, which the ~15-adjudications/video affordability ceiling absorbs. If the disagreement rate comes back LARGE (e.g. most tier-1 fires diverging), that is an HONEST MEASUREMENT of how far the extractor's paraphrases drift from the trader's words — RECORDED as a first-class pilot finding, NEVER tuned away mid-pilot (variance guard + anti-goalpost).
2. **This addendum IS §4's written correction** (above). No silent doc-healing.

**META-LESSON PINNED (catch #4 EXTENDS the engagement-evidence law).** The four dormancies were features claimed default-ON that nothing fed. This was a BACKSTOP claimed present that nothing wired. **Claimed safeguards are engagement-evidence targets exactly like default-ON features:** every "X is caught downstream by Y" assertion in a frozen doc owes a WIRING-VERIFY, not a nod. The grader proved a protocol claim false by reading the plumbing; that check now belongs to every future pre-reg review. (Memory: `feedback_claimed_safeguards_owe_wiring_verify`.)

**Sequence:** one builder scope-locked to EXACTLY these two changes (dual-read gate + two-stage packet + the support certificate field) → independent re-grade whose DECISIVE targets are the grader's own three constructed flatter cases (each must now (a) fall through under dual-read AND (b) arrive at tier-3 as a two-stage packet with Stage-1 blinding intact and a Stage-2 support verdict) → dry-run re-proof → three greens + fresh capacity → unseal. The rework's clean core is already vaulted (`7a937e17`); this is the last plank — and if a fifth catch hides in it, better a build cycle than the seal.

*Addendum 4 frozen 2026-07-12, operator-authored, BEFORE the sealed 16 is unsealed. Vaulted FF to `corpus-v3-gate3-cert-2026-07-06`.*

---

## ADDENDUM 5 — CATCH #5 RULING: the content-overlap floor + the 3-axis composed defense (OPERATOR-AUTHORED 2026-07-12, BEFORE the seal breaks)

> Authored after the combined re-grade of the catch-#4 fix proved Addendum 4's dual-read + two-stage packet INSUFFICIENT (finding F-N, CRITICAL, reproducible): the dual-read gate compares `frozenset(surface_class)` ONLY, so a SAME-surface-class DIFFERENT-content mis-grounding (a stop-rule condition [imperative] mis-grounded to an entry-rule quote [imperative]) passes as "agreement", classifies at tier-1 carrying the wrong rule's quote, pilot_grade-OK, and never reaches tier-3 so the Stage-2 support backstop never sees it. Dual-read closed only the SURFACE-DIVERGENT sub-case. This addendum adds the missing axis and names the residual.

**THE 3-AXIS COMPOSED DEFENSE (all at the classify boundary; classify ONLY if a fire survives all mechanical axes, else fallthrough → two-stage tier-3):**
- **Axis 1 — SURFACE (dual-read, Addendum 4 FIX 1, built):** tier-1 on BOTH condition text and located quote; different surface_class → fallthrough. Kills surface-divergent mis-grounding.
- **Axis 2 — CONTENT (the content-word overlap floor, THIS addendum):** the located quote must share **≥ 1 content-bearing token** with the condition text (GROSS-mismatch semantics — zero shared content tokens = the quote cannot be grounding this condition = fallthrough). Kills cross-rule gross mis-grounding (stop-quote grounding an entry-condition = zero shared content = fallthrough → tier-3 → Stage-2 support catches it). Zero model calls.
- **Axis 3 — SAMPLING (the tier-3 idle-budget audit, pre-committed rank-3, THIS addendum):** the per-video tier-3 budget's IDLE capacity audits ONE RANDOM tier-1 FIRE through the full two-stage packet (Stage-1 blind role + Stage-2 support). Cannot-determines get the budget FIRST; when none exist, the random-fire audit RIDES FREE inside the ~15-adjudications/video ceiling. Makes the subtle residual (below) statistically visible instead of invisible.

**Axis-2 FLOOR — pinned exactly (Law 4):**
- **Floor = ≥ 1 shared content-bearing token.** Zero → fallthrough. **REJECTED upgrades (named, so no future agent re-opens them):** (a) raising to ≥ 2 would false-fallthrough terse genuine groundings (real tier-3 cost for zero caught fraud); (b) an action-class axis (entry-vs-exit-vs-stop token compatibility) is a SEMANTIC CLASSIFIER in mechanical costume — it would need its own design set, fixtures, and grade, a 6th instrument at the classify boundary. The floor's job is GROSS mismatch; it stays DUMB enough to be incorruptible.
- **Single authority, WHOLE definition (anti-duplication):** reuse the F-2 machinery COMPLETELY — tokenizer, word-boundary logic, AND its stopword/content-word definition. NO second tokenizer, NO new stopword list (duplicate-enforcement is a named disease; the certificate boundary does not get a new site). Numbers + symbol-names COUNT as content tokens ("50", "SMA", "TP1", "15m") — the most distinctive graft points in trading language; F-2's exact-token discipline already treats them first-class.

**THE NAMED RESIDUAL (honesty — ≥1 leaves one gap, covered by axis 3, NOT pretended-closed):** trading transcripts are vocabulary-dense — an entry rule and an exit rule about the SAME indicator share surface class AND content tokens ("cross", "50", "SMA"): same-surface, overlapping-vocabulary, different-rule mis-grounding passes BOTH mechanical axes. No honest mechanical floor closes it without becoming the rejected semantic checker. Axis 3 (sampling audit) makes it statistically visible; it is NOT claimed mechanically closed. This is the wiring-verify discipline applied to our own fix: the residual is named and its coverage (sampling) is wired, not asserted.

**PRE-COMMITTED DRY-RUN EXPECTATION (variance guard):** genuine groundings almost always share ≥ 1 content token with their condition — the locator FOUND the span BY the condition. So the floor should catch gross mis-groundings at near-zero cost to real ones. If the re-proof instead shows a LARGE zero-overlap rate among LOCATED conditions, that is NOT floor miscalibration — it is a LOCATOR-QUALITY signal, recorded honestly, NEVER tuned away pre-unseal.

**BUILD POSTURE:** the catch-#4 fix (dual-read + two-stage packet + support field) is held UNCOMMITTED as a correct partial; the content-overlap floor is built INTO THE SAME predicate line + axis-3 sampling added, for ONE combined re-grade (land-then-churn on the same line is how diffs stop meaning things). **Combined re-grade DECISIVE targets:** (a) the stop-vs-entry ZERO-overlap case falls through and arrives at tier-3 as a two-stage packet; (b) a constructed same-surface SHARED-vocabulary different-rule case is documented as the named residual AND shown covered by the sampling audit; (c) genuine groundings STILL classify — the gate must not have become a fallthrough machine.

*Addendum 5 frozen 2026-07-12, operator-authored, BEFORE the sealed 16 is unsealed. Vaulted FF to `corpus-v3-gate3-cert-2026-07-06`.*

### ADDENDUM 5 — WRITTEN CORRECTION (post combined re-grade; frozen docs don't silently heal)

**F-1 — the SMA/TP1 example clause YIELDS to the reuse clause.** Addendum 5's illustrative prose said "Numbers + symbol-names COUNT as content tokens (`'50'`, `'SMA'`, `'TP1'`, `'15m'`)." That is FACTUALLY WRONG about `SMA`/`TP1`: the F-2 authority this addendum ordered reused-WHOLE (`extraction-coverage-gate.ts` `contentTokens`: `len>=4 OR (has-digit AND len>=2)`, minus stopwords) EXCLUDES both (non-numeric, len<4; `TP1` splits to `tp`+`1`, each too short). Only `50` (digit carve-out) and `15m` (→`15`) survive. **Authorship error owned:** the operator wrote the example against an ASSUMED definition, not a READ one — the same premise-class error as catch #3, this time at the advisor's desk. **RATIFIED PRECEDENT:** when a ruling's example conflicts with the authority the ruling says to reuse, THE AUTHORITY WINS and the divergence surfaces; the builder's priority ordering (mirror F-2 whole, do NOT special-case "sma" — special-casing would be the forbidden new content-word list) is cited and ratified. Independent grade confirmed the Python mirror is byte-faithful to the TS authority.
**Direction is strictly FAIL-TOWARD-SUSPICION (why acceptance is easy):** excluding short symbols REMOVES potential shared tokens → makes AGREEMENT/passage HARDER, never easier. A mis-grounding gains nothing; no flatter path exists in this direction. The only party who pays is a genuine grounding whose SOLE commonality is a lone short symbol — and it pays a tier-3 TAX (falls through → Stage-2 support hands it back correctly), NOT an integrity cost. **So the lone-short-symbol case is judged on ECONOMICS, not integrity:** how often do real rule-pairs hinge on a lone short symbol = extra tier-3 load. Cosmetic → done; material → recorded as measured load, NEVER softened mid-pilot. **Remedy path (pinned):** any tokenizer evolution (a trading-symbol lexicon promoting SMA/FVG/POC to content status, or lowering the numeric-token length floor) is a POST-PILOT upgrade to the F-2 AUTHORITY ITSELF — one change, BOTH consumers (coverage gate + certificate floor), parity-fixtured across languages — NEVER a certificate-boundary special case. One tokenizer, two syntaxes, forever.

**F-1 SECOND CORRECTION (the wiring-verify law operating on THIS correction).** Two claims in the first pass were themselves unverified assumptions the independent re-grade falsified by reading the plumbing — corrected here, not silently:
- **The "bonus fallback inherited for free" claim is FALSE.** F-2's short-symbol fallback is `u.includes(normName)` at `extraction-coverage-gate.ts:484` — an ASYMMETRIC whole-NAME substring test living in `classify()`, NOT in `contentTokens()`. Addendum 5 scoped reuse to `normalize()`+`contentTokens()`+`STOPWORDS` only — `classify()`'s fallback needs a discrete "name" field to match and does NOT apply to axis-2 (which compares two full free-text sentences with no name field). So it is correctly NOT inherited (not a missed-reuse gap), and "authorities carry solved edge cases their examples don't advertise" was, HERE, itself a claim asserted-without-reading — the same premise-class error one level deeper.
- **The vulnerable population is BIGGER than the SMA example implied.** The digit carve-out requires `len>=2` even for numeric tokens, so a SINGLE-digit period is excluded: `content_tokens("5 SMA") == frozenset()` (both "5" [len 1] and "SMA" [len 3] drop). So bare symbols (SMA/EMA/FVG/POC/TP1) AND single-digit-period symbols ("5 SMA", "9 EMA", "8 EMA" — common 5/8/9/21-EMA scalp vocabulary) produce zero content tokens; two-or-more-digit periods ("50 SMA", "200 EMA", "21 EMA") ARE protected. Direction STILL strictly fail-toward-suspicion (verified by both parties independently — tier-3 tax, no flatter path). **NOT softened to "cosmetic" (single-digit EMA periods are common) and NOT called high (unmeasured against real transcripts — no sealed-set access in a code grade).** SCOPED AS AN OPEN, TRACKED ITEM: the `fallthrough_axis2_zero_content_overlap` outcome label (already instrumented per-condition) is a FIRST-CLASS pilot metric — track its rate during the real pilot run, same discipline as Addendum 4's paraphrase-drift pre-commitment (of which this is a specific named sub-case). Material rate → the post-pilot F-2-authority upgrade above; never a certificate special-case.

**F-2 — axis-3 budget ceiling is PER-STRATEGY, not per-video (MEDIUM carry-forward, non-blocking; recorded, not softened).** The ~15-adjudications/video affordability ceiling is checked inside `prepare_strategy` (per strategy), and `prepare_video` calls it once per extracted strategy with NO cross-strategy budget accumulator — an INHERITED characteristic (tier-3 packets have been per-strategy since Wave-1/2, predating catch-4/5), not a new regression. On a video with N>1 strategies each strategy independently gets its own ~15 budget, so video-level tier-3 load + axis-3 audits can exceed the stated per-video ceiling. Does NOT touch axis-2/axis-3 CORRECTNESS (the audit is a monitor, the economics NUMBER is measured honestly) — only the ceiling's PRECISION on multi-strategy videos. Whether it matters is empirical (strategies-per-video, revealed by the dry-run/pilot): if videos are ~1 strategy each, per-strategy == per-video and it's moot. Fix point if needed: a per-video budget accumulator threaded through `prepare_video` (~line 1190) into `axis3_adjudication_ceiling()`/`_select_axis3_audit_fire`. Operator decision pending; the economics read carries this scope caveat (Law 7) until resolved.

*Correction frozen 2026-07-12, operator-directed (F-1) + independent-grade-surfaced (F-2), BEFORE the sealed 16 is unsealed.*

---

## ADDENDUM 6 — F-2 UNIT-ACCOUNTING RULE (OPERATOR-AUTHORED, PINNED BLIND 2026-07-12, BEFORE the dry-run emits any strategies-per-video count)

> Pinned SIGHT UNSEEN — before the dry-run re-proof reveals how many strategies a video yields — because authoring a counting rule with the answer in view is the exact Law-4 violation the whole campaign is built against. A video can yield >1 extracted strategy; the §1 QUALITY bar reads "≥ 60% of videos reach certificate-grade extraction", so the video→certificate-grade mapping must be fixed before any count exists.

**THE UNIT RULE (frozen):** A **VIDEO reaches certificate-grade iff AT LEAST ONE complete extracted strategy from it reaches certificate-grade** (pilot-grade per Addenda 1-5: every spine condition classifies tier-1/3 with a verbatim anchor AND the live lints pass). Rationale: the video yielded a certified strategy — which is exactly what the conveyor exists to produce. A video that yields two strategies, one certifiable and one not, is a video that produced a good strategy; it counts.
- **The per-strategy grade is the REAL PRODUCT.** Every extracted strategy's individual certificate + grade lands in the record (the per-strategy table travels with the verdict). The video-level ≥60% fraction is JUST the §1 bar's DENOMINATOR — an aggregation over videos, not a discarding of per-strategy detail.
- **Economics (adjudications/video) is measured PER-VIDEO AGGREGATE** — summed across ALL of that video's strategies' tier-3 adjudications (fallthroughs + axis-3 audits) — vs the ~15 affordability ceiling. This subsumes the F-2 carry (axis-3's per-strategy budget scoping): the ceiling is judged on the honest video-aggregate load, and a multi-strategy video exceeding ~15 is a RECORDED economics finding (never softened, per the standing variance guard), not a scoping bug to paper over. If the dry-run/pilot shows videos are ~1 strategy each, the F-2 distinction is moot; if multi-strategy, the aggregate is what the ≤~15 rider reads against.

*Addendum 6 frozen 2026-07-12, operator-authored, pinned BLIND before the dry-run re-proof and before the sealed 16 is unsealed. Vaulted FF to `corpus-v3-gate3-cert-2026-07-06`.*

---

## ADDENDUM 7 — ECONOMICS-READING RULE (OPERATOR-AUTHORED, PINNED BLIND 2026-07-12, before the seal breaks)

> Pinned SIGHT UNSEEN — before the sealed 16's strategy-composition is known — because the dry-run re-proof previewed the exact temptation: a 2-strategy video read 21 adjudications (over the ~15 ceiling in AGGREGATE, under it PER-STRATEGY). When the pilot number exists, the per-strategy reading will flatter, and re-deriving the criterion after the number is the Law-4 violation this pin forecloses.

**THE VERDICT reads the ~15 affordability ceiling as PER-VIDEO AGGREGATE** — summed across all of a video's strategies' tier-3 adjudications (fallthroughs + axis-3 audits), exactly as Addendum 6 fixed the unit. **Law 4, strictest reading, NO re-derivation after the number exists.** The §1 economics measurement + the §2 "miss on economics" outcome are judged on the per-video aggregate vs ~15.

**THE PER-STRATEGY NORMALIZATION is an ANNOTATION, never the verdict.** Cost-per-certified-strategy is — honestly — the truer long-run economics question for a conveyor whose PRODUCT is strategies. So it is RECORDED alongside the aggregate (both numbers side by side) and informs the CONSUMPTION decision AFTER the read — but it does NOT move the frozen criterion. If the pilot lands **"miss on aggregate, viable per-strategy,"** that is a LEGITIMATE, interesting outcome that arrives with its frozen criterion intact AND its better question documented — precedent: the 0.5 overlay read arrived exactly this way (frozen directional criterion + a better-question annotation) and aged well.

**THE ECONOMICS-MISS PREVIEW, said plainly (nobody surprised at 4am):** the rehearsal's root cause — traders NARRATE their rules, tier-1 is prescriptive-tuned + low-recall by design, tier-2 is DEAD (human-in-loop fork), so narration floods tier-3 — means the sealed 16 MAY read economics miss. That is a PRE-WRITTEN §2 OUTCOME, NOT a failure: it escalates to the parked stronger-model tier-2 experiment as the next pre-registered move. The conveyor measuring its own cost honestly IS the product working. **A pilot that returns "fidelity YES, economics NO" would still be the most valuable measurement this project has ever taken.** The economics reading NEVER blocks or alters the FIDELITY (§1 ≥60% cert-grade) read — they are separate verdicts.

*Addendum 7 frozen 2026-07-12, operator-authored, pinned BLIND before the sealed 16 is unsealed. Vaulted FF to `corpus-v3-gate3-cert-2026-07-06`.*

---

## ADDENDUM 8 — MID-RUN RULINGS (OPERATOR-AUTHORED 2026-07-12, during the read-once run, BEFORE the moments that need them)

> Authored after the Phase-1 relay (11/16 through the conveyor; a YouTube transcript-fetch throttle on videos 12-16; provisional per-video-aggregate economics mean 14.64 visible). Two decisions are visibly incoming (the throttle's denominator question; the economics statistic on a knife-edge number) and neither gets improvised. Pinned now.

**§1 — THE DENOMINATOR IS 16. IMMOVABLE.** If the throttle outlasts a genuine backoff, the remedies are TIME-AND-ROUTE, NEVER SCOPE. (a) Keep spacing single-video probes with lengthening backoff. (b) If the tower IP stays cold, fetch the five through a DIFFERENT LEGITIMATE EGRESS — the relay/cloud path, the SAME production `youtube-transcript` library, a different IP. How the transcript bytes ARRIVE does not touch what they SAY, and every fetch is `transcript_sha256`-stamped in provenance (a re-fetch that changes the bytes would be caught). (c) Reading on 11 is DEAD ON ARRIVAL — the Gate-3 precedent already rejected re-scoping a denominator to its "measurable members," and doing it AFTER seeing 11 videos' texture is denominator-shopping in its purest form. (d) Substitution is DEADER — swapping exam questions after partial results destroys the seal's entire meaning. The sealed set does NOT expire; the read-once rule has NO deadline; patience costs only hours. **Wait as long as it takes.** The verdict reads on all 16 or it does not read.

**§2 — VIDEO-LEVEL AGGREGATION IS EMBARGOED until all 16 are rated.** Rating MAY proceed on the cached 11 during the wait (raters are blind + per-item; parallelism is free and costs no integrity). BUT the conductor computes NO per-video pass/fail, NO running fidelity fraction, NOTHING bar-shaped until the population is WHOLE (all 16 rated). Partial-verdict knowledge accumulating during a wait is how patience gets contaminated — **"we already know N passed" must not exist as a computed value anywhere in the pipeline before all 16 are in.** Per-item rater verdicts accumulate; aggregation waits.

**§3 — THE ECONOMICS STATISTIC IS THE MEAN PER-VIDEO AGGREGATE ≤ ~15 — pinned on LINEAGE, not choice.** The rider is ambiguous ("~15 per video" never said mean or max) and the number is knife-edge (14.64), so any choice made now is exposed to the shopping accusation. The pin therefore rests NOT on choice but on CITABLE LINEAGE: the economics rider has been MEAN-based since **Wave-0's original frozen text** ("mean tier-3 adjudication ≤ 1/video"); the pilot's re-derivation restructured only the UNIT (per-video aggregate, Addendum 7) and INHERITED the statistic unchanged. **Verdict: mean per-video-aggregate adjudications ≤ ~15.** The per-video DISTRIBUTION — including how many individual videos run hot (>15) — is recorded as ANNOTATION: consumption-relevant, verdict-irrelevant. **CONTAMINATION ACKNOWLEDGMENT, verbatim (Law 7 honesty):** the provisional mean (14.64 @ 11/16) WAS visible when this pin was written. The pin's authority is Wave-0's frozen text, NOT the visible number's convenience. Recorded so the lineage — not the coincidence — is what anyone auditing sees.

**§4 — INFRA FIX (b) EARNS THE RE-VERIFIER'S TEETH.** The driver-injected propose_fn (recovering gemma's transient empty-content crashes) claims "byte-for-byte the instrument's own propose path + frozen decline semantic." That claim is instrument-adjacent and gets VERIFIED, not narrated: the Phase-4 independent re-verifier confirms (i) the propose path is the instrument's own, (ii) persistent-empty→UNANCHORED is preserved, (iii) forced-abstain=0 held THROUGH ALL 16 (no condition pushed to UNANCHORED by the recovery path → no anchoring bias). The UTF-8 fix is plumbing; the propose-seam fix is plumbing-claiming-to-be-plumbing, which is exactly the class that gets checked. Both fixes documented verbatim (diffs) in the durable artifacts for audit.

*Addendum 8 frozen 2026-07-12, operator-authored, during the read-once run, before the throttle-denominator and economics-statistic decisions were forced. Vaulted FF to `corpus-v3-gate3-cert-2026-07-06`.*

### ADDENDUM 8 §5 — SEAL-INTEGRITY AFFIRMATION + CONDUCT CALIBRATION (operator-authored, mid-run)

> Occasioned by video `WEhmadJArQo` fetching 10,527 chars at pilot time vs the seal's existence-probe count of 10,253 (~2.7%, YouTube caption-segment drift over the day). Throttle CLEARED BY TIME (no reroute); all 16 flowed in one consistent pass.

**§5a — THE SEAL IS ID-BASED BY CONSTRUCTION (not a judgment call, AFFIRMED).** §3's frozen text says the sealing agent selected and sealed IDs ONLY, opening NO transcripts. A seal whose creator was FORBIDDEN to read the bytes CANNOT be byte-committing — you cannot commit to content you were forbidden to see. The `transcript_char_count` was an EXISTENCE PROBE (retrievability proof, metadata-level); live-fetch-at-pilot-time was the frozen design from the start. The property the seal protects — the population was fixed before the instrument existed, so nobody tuned to the test — is UNTOUCHED by YouTube re-chunking its own captions: the drift is UPSTREAM, UNCHOSEN, and the instrument was frozen before either byte-version existed. The byte-based reading is STRUCTURALLY INCOHERENT with the seal's own record. The `WEhmadJArQo` delta is non-breaking; `transcript_sha256` is stamped in that video's certificate provenance for audit. CLOSED.

**§5b — THE RESIDUAL RISK ALREADY HAS ITS NET.** If caption drift were ever MATERIAL (re-generated WORDING, not re-segmentation), the only place it could carry weight is a load-bearing certificate — an H2 SURVIVOR. The survivor-forensics protocol (frozen months ago) re-verifies the ENTIRE chain fresh at exactly that moment, including re-fetching + re-checking anchors. So benign drift is handled by the hash stamp today; MATERIAL drift is caught by the forensics gate on the only certificates whose integrity ever carries capital weight. The system was shaped for this question before it arrived.

**§5c — CONDUCT CALIBRATION (precedent, not this instance).** The conductor JUDGED an unrouted seal-adjacent novelty and continued, surfacing it in parallel. The judgment was correct, the surfacing prominent, and the embargo meant nothing irreversible happened before the operator's affirmation — protection held IN SUBSTANCE. But the LETTER, for next time: **seal-adjacent novelties are HARD-PARK class** — pause the affected ITEM, surface, proceed ONLY on ruling; NOT judge-and-continue. It cost nothing here precisely because the embargo preserved full EXCISABILITY (per-item, nothing aggregated, everything hash-stamped — had the ruling gone the other way, that video's processing was cleanly removable). That excisability is what turned a judgment-in-motion into a SAFE one; the calibration ensures the next conductor does not need the LUCK of having been right.

*§5 frozen 2026-07-12, operator-authored, mid-run, recorded beside the read. Vaulted FF.*
