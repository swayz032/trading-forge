# EXTERNAL READ — 2026-08-08 — AR-809 (three corrections) + AR-810 (lossy canonicalization)

> **PROVENANCE:** **OPERATOR-RELAYED CHAT**, 2026-08-08, two rulings in one relay, responding to
> `AR-809` and `AR-810`. **CONSUMED BY `R-725`** — banner written BY that ruling, IN its commit.
> ★ **Its `AR-809` half supersedes the `AR-808` read's `SOURCE_BASELINE_STATUS = UNKNOWN_NOT_PROVIDED`
> order: "TRANSCRIPT BLOCKER CLEARED".** `R-725 §3` records that this desk had already refused that
> status on a refuted premise, and that the reader corrected itself on seeing the evidence.
> 🛑 **ONE ORDER IS NOT RUNNABLE:** the `AR-809` half requires a **visual-source check**. `[MEASURED,
> R-725 §4]` **no video, frame, or timed-caption artifact exists for `st5e-YJRfKc`** — positive control:
> 24 media files ARE tracked elsewhere. See `R-725 §4` for the disposition.
> **A CHANNEL IS NOT AN AUTHOR** — audited on merit.

---

## PART 1 — AR-809 ACCEPTED WITH THREE MATERIAL CLASSIFICATION CORRECTIONS

DECISION: TRANSCRIPT BLOCKER CLEARED. COMPLETE ONLY LANES 3 AND 5; CONVERT LANE 2 INTO THE PHASE B RED FIXTURE. NO MORE ROW TRACING.

ACCEPTED FINDINGS

- The frozen transcript is now identified, hashed, tracked, and joined.
- Eleven checked source facts survived into the extraction artifact.
- `EXTRACTION_MISSING_REQUIRED_INFORMATION` is refuted for those eleven facts only.
- The extractor correctly avoided inventing a scalar from garbled audio.
- All three measured repairs are individually insufficient.
- Source-to-artifact evidence is now strong enough to continue the golden trace without the former transcript ceiling.

CORRECTION 1 — `level: null` IS NOT NECESSARILY AN EMPTY SLOT FOR THE TAUGHT NUMBER

The lesson's `52¢` is example arithmetic, not necessarily a fixed strategy parameter.

The reusable taught rule appears to be dynamic:

- opening-range width = `OR high − OR low`;
- half range = `width / 2`;
- midpoint = `(OR high + OR low) / 2`;
- stop or target derives from that daily range.

Do not populate `level` with `0.52` unless the schema explicitly defines it as an example-only field.

Measure what `level` means:

- absolute price;
- fixed distance;
- runtime-derived level;
- or descriptive example value.

If it accepts only a scalar, the real problem is not that extraction left it null. The schema cannot represent a source-defined expression. Classify that as `PARAMETER_SCHEMA_MISMATCH` or missing canonical expression support.

The correct production representation must preserve the formula, not hardcode the example result.

CORRECTION 2 — SOURCE AMBIGUITY IS NOT FINAL UNTIL THE VISUAL LESSON IS CHECKED

The frozen transcript proves what was and was not spoken. It does not alone prove what the complete video lesson demonstrated visually.

Before declaring the breakout trigger permanently ambiguous, inspect the synchronized chart evidence at the relevant timecodes for:

- first trade above the range;
- wick penetration;
- candle close above;
- entry on the next candle;
- pullback entry;
- any displayed written rule.

A single chart example may support a conclusion, but it must not be promoted into a universal rule unless the teacher presents it as such.

If no transcript or visual evidence resolves the trigger, disposition must be:

`SOURCE_AMBIGUOUS — BREAKOUT_CONFIRMATION_UNSPECIFIED`

No compiler default may choose wick, touch, close, or next-bar entry.

VARIANT RULING

The absence of one preferred `5/15/30` duration does not necessarily block compilation.

If the teacher explicitly presents all three as valid alternatives, compile three source-sanctioned child candidates:

- OR-5;
- OR-15;
- OR-30.

Each must retain common provenance and its exact duration. Do not select one silently, average them, or optimize the choice inside the compiler.

Edge qualification may later determine which variant performs best. That is not compiler invention because the alternatives came from the teacher.

CORRECTION 3 — MARKET DESK RULING

Do not claim MES, MNQ, MCL, or general futures portability from this lesson.

The measured evidence supports an equity or S&P-linked example, but the exact traded instrument must be resolved from the video, chart symbol, metadata, or source description. The `616–617` price region alone is insufficient identification.

For V1:

- compile the rule within its demonstrated market scope;
- preserve the exact source instrument if identifiable;
- mark futures application `MARKET_OR_TIMEFRAME_UNRESOLVED`;
- treat any futures transfer as a later portability hypothesis;
- never label a futures backtest as the source-faithful result without explicit portability evidence.

A mathematical rule may later work on futures, but profitability on futures cannot retroactively prove that the teacher taught it for futures.

FINAL PHASE A WORK

Authorize only:

1. Lane 3: identify who assigns `WAIT_STRUCTURE`, which evidence it examines, and whether the family label is semantically validated before type dispatch.
2. Lane 5: determine whether an exact opening-range constructor exists anywhere reachable from production.
3. Visual-source check for the unresolved breakout semantics and exact instrument identity.

Do not begin another condition row.

LANE 2 becomes the Phase B RED fixture:

- execute the current structural primitive on deterministic data;
- prove it reacts to structural candidates;
- prove it does not compute the opening-range midpoint;
- prove its value is non-gating;
- preserve the real frozen artifact as the input.

PHASE B TARGET REMAINS

Build the first genuine production path:

frozen source artifact
→ typed opening-range expression
→ exact duration and timezone
→ daily range state
→ high and low
→ completion state
→ midpoint and width
→ production consumer

No `0.52` hardcode. The value must be recomputed from each day's opening range.

GOLDEN-SLICE RELEASE CONDITION

Do not reselect the golden strategy yet.

First perform the bounded visual-source check. If the breakout trigger and operational boundaries remain materially ambiguous afterward, this strategy cannot be forced into a positive faithful compile.

At that point:

- retain `st5e-YJRfKc__s0` permanently as the negative refusal/conformance golden;
- select a second source-complete real strategy for the required positive V1.0 compile;
- predeclare selection by source completeness and stable manifest order;
- do not select based on profitability or ease.

That would not violate the instruction against choosing an easier strategy. It would be an evidence-required response to a source that cannot uniquely specify executable behavior.

BOTTOM LINE

AR-809 clears extraction as the primary failure for the eleven checked facts.

The remaining money-path failure is now sharper:

> Trading Forge preserved dynamic trading formulas as prose and examples, but it lacks a faithful typed expression, verified family assignment, and decision-affecting opening-range execution path.

---

## PART 2 — AR-810 ACCEPTED; THE CAUSAL FAILURE IS LOSSY CANONICALIZATION WITH AN UNREACHABLE EXISTING CAPABILITY

DECISION: RUN LANE 2 AS THE PERMANENT RED FIXTURE, THEN END PHASE A. NO ADDITIONAL CONDITION ROWS.

AR-810 establishes the causal chain:

source meaning
→ classifier recognizes "opening range" as a level construct
→ classifier seals the coarse type `WAIT_STRUCTURE`
→ that type cannot distinguish level construction from market-structure events
→ binder dispatches to the only primitive attached to the family
→ `compute_structure_state` computes an unrelated concept
→ the actual opening-range capability remains on a separate unreachable compilation surface

CLASSIFICATION CORRECTION

The AR-806 amendment is accepted in part:

- Accepted: the text is consulted upstream.
- Accepted: the family assignment is deliberate, not a default-sink accident.
- Accepted: type dispatch inside the binder is behaving according to its declared input.
- Rejected: "the trust boundary is not the defect" is too broad.

The classifier seals a type that is not precise enough to guarantee primitive identity. Therefore, the canonicalization boundary still loses meaning.

`WAIT_STRUCTURE` combines at least two computationally different categories:

- construction or reference of price levels;
- detection of market-structure events.

A sealed type is trustworthy only if every value of that type can be handled faithfully by its permitted evaluator. That invariant is false here.

Also, an opening range is not merely "a level construct, not a clock window." It is a time-bounded stateful aggregation that produces levels. Its correct representation must preserve both dimensions:

clock window
+ high/low aggregation
+ completion state
+ resulting level references

The current one-family classification cannot express that composition.

CURRENT DISPOSITIONS

Primary:

`CANONICAL_TERM_UNRESOLVED — OPENING_RANGE_COLLAPSED_INTO_COARSE_WAIT_STRUCTURE`

Secondary:

`ENGINE_PRIMITIVE_WRONG_IDENTITY — STRUCTURE_EVENT_EVALUATOR_SELECTED_FOR_LEVEL_CONSTRUCTION`

Measured architectural gap:

`EXISTING_OPENING_RANGE_CAPABILITY_NOT_REACHABLE_FROM_SPEC_BINDING_PATH`

Do not classify the opening-range capability as repository-wide missing. Lane 5 refutes that broad claim.

Do not yet certify the existing OR-column surface as suitable. Addressability by name is not proof of semantic or production compatibility.

LANE 2 — FINAL PHASE A DELIVERABLE

Run the five controls already registered.

Add one direct comparison on the same deterministic candles:

- independently calculate the expected opening-range high and low;
- capture `compute_structure_state` output;
- capture `orh_5m` and `orl_5m` if that surface can be exercised without modifying production;
- prove the structural-state result is not the opening-range construction.

Preserve this as the RED fixture for Phase B.

After Lane 2, Phase A is closed. Do not trace the remaining eight rows.

PHASE B STEP ZERO — BOUNDED BRIDGE-ELIGIBILITY CHECK

Before implementing anything, measure whether the existing OR-column surface preserves the golden source's required semantics:

- exact start time;
- duration;
- timezone;
- correct trading-date reset;
- high/low construction;
- readiness only after formation completes;
- 5/15/30-minute variants;
- deterministic behavior around missing bars and session boundaries;
- availability inside the actual production evaluator.

Produce one compatibility table: `SOURCE REQUIREMENT / EXISTING SURFACE / EXACT, MISMATCH, OR UNMEASURED`.

This is not another repository audit. It is the repair-selection test.

REPAIR DECISION

If the existing OR surface is semantically exact:

- create a typed `OPENING_RANGE_DEFINITION`;
- lower the real frozen artifact into it;
- bridge that typed definition to the existing feature columns;
- make the production evaluator consume the resulting levels.

If the existing surface differs materially:

- do not widen or reinterpret it silently;
- implement the smallest exact opening-range primitive required by the frozen source.

In either case, prevent `OPENING_RANGE_DEFINITION` from lowering to `compute_structure_state`.

Do not repair the entire `WAIT_STRUCTURE` taxonomy during this step. Add only the smallest typed distinction and fail-closed guard required for the golden path.

IMPORTANT SEPARATION

The existing topology example `close >= orh_15m` must not be adopted as the golden breakout rule merely because it already exists.

Opening-range construction and breakout confirmation are separate semantics.

The OR high and low may be reusable even if the example comparator invents a candle-close requirement. Until visual/source evidence resolves the breakout trigger, build only the range-construction path.

PHASE B ACCEPTANCE

The first GREEN must prove:

real frozen artifact
→ typed opening-range definition
→ production OR construction
→ exact deterministic high and low
→ completion state
→ source-visible level references

Required mutations:

- duration;
- start time;
- timezone;
- input high;
- input low;
- daily reset boundary.

Every mutation must change or invalidate the expected result.

No hand-built production binding, named-session substitution, fixed `0.52`, or neighboring structural primitive is permitted.

BOTTOM LINE

Phase A has found the broken handoff:

> The extractor preserved the opening-range meaning, the classifier recognized part of it, but the canonical taxonomy compressed a time-bounded level constructor into a generic structure family whose evaluator computes an entirely different concept—while the repository's real opening-range capability remained stranded on another compilation surface.

Run Lane 2, close diagnosis, and begin the bounded bridge decision.
