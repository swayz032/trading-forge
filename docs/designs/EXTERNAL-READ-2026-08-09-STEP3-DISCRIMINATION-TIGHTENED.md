# EXTERNAL READ — 2026-08-09 — STEP 3 INTERPRETATION CONFIRMED + AR-824 ACCEPTED, DISCRIMINATION RULE TIGHTENED

> **PROVENANCE:** **OPERATOR-RELAYED CHAT**, 2026-08-09, two rulings in one relay.
> **CONSUMED BY `R-731`** — banner written BY that ruling, IN its commit (`R-722 §1`).
>
> 🛑 **THIS ARRIVED MID-BUILD.** `AR-824` is executing `STEP 3` with an ETA of ~60 min and is writing
> the discrimination rule right now. **PART 2 changes its design**: the rule must be **ORDERED
> SEMANTIC EVIDENCE — trigger/reference evidence BLOCKS definition classification — and explicitly
> NOT a blacklist on the word `breakout`.**
>
> ⭐ **PART 1 validates `R-729 §4`:** *"The ten-minute GitHub verification was not wasted process. It
> prevented production work from starting behind the wrong regression test."*
> **A CHANNEL IS NOT AN AUTHOR** — audited on merit.

---

## PART 1 — STEP 3 INTERPRETATION CONFIRMED

The main advisor's reading is correct.

The ten-minute GitHub verification was not wasted process. It prevented production work from starting behind the wrong regression test.

STEP 3 MUST DO ONLY THIS

- Introduce the explicit `OPENING_RANGE_DEFINITION` shape.
- Classify the frozen range-construction sentence into that shape.
- Preserve the taught `09:30` Eastern start and `5/15/30` durations without selecting a default.
- Preserve source evidence and provenance.
- Fail closed because no adapter is connected yet.

STEP 3 MUST NOT

- calculate the range;
- connect an evaluator;
- decide wick, touch or close;
- classify "break above the opening-range high" as range construction;
- fall back to `WAIT_STRUCTURE`;
- touch the other strategy rules.

REQUIRED NEGATIVE CONTROL

This sentence must not become `OPENING_RANGE_DEFINITION`:

`break above the opening-range high`

It references an already-constructed level. It does not define how that level is constructed.

Its breakout semantics remain:

`UNRESOLVED_SOURCE_AMBIGUITY`

CENSUS CONTROL

Convert the census equality check in the same commit.

The new control must prove that exactly the intended classification changed:

- target type changes;
- any mechanically derived target ID/hash may change;
- source prose, role, ordering and provenance remain unchanged;
- every unrelated condition remains unchanged;
- the genuine structure neighbor remains routed to the structure evaluator.

Do not update the old census or make production equal the known-wrong classification.

STEP 3 PASS CONDITION

The frozen extraction produces the new explicit definition type, the breakout-reference sentence remains separate, the census reports only the authorized difference, and downstream compilation refuses safely because wiring has not been built.

No calculation and no executable binding are expected yet.

---

## PART 2 — AR-824 START RECEIPT ACCEPTED; DISCRIMINATION RULE TIGHTENED

The worker identified the correct hard case before implementation.

A simple rule such as:

`duration language AND range language`

is insufficient because the breakout sentence contains both.

REQUIRED CLASSIFICATION ORDER

1. REFUSE TRIGGER/REFERENCE SENTENCES FIRST.

Examples:

- "after the 30-minute range is over, look for a breakout"
- "break above the opening-range high"
- "enter when price crosses the range"

These refer to an already-constructed range. They do not define its construction.

2. REQUIRE POSITIVE DEFINITION EVIDENCE.

`OPENING_RANGE_DEFINITION` requires:

- an explicit clock or duration;
- language describing formation, establishment, calculation, high/low construction or price captured during that window.

3. REFUSE ANAPHORIC CLOCK REFERENCES.

"In between these time periods" does not carry its own typed duration and must not move during STEP 3.

Do not implement this as only a blacklist for the word `breakout`. Use ordered semantic evidence: trigger/reference evidence blocks definition classification.

REQUIRED CONTROLS

Before landing production classification, prove:

- the real definition condition moves;
- the breakout sentence does not move;
- the anaphoric sentence does not move;
- removing duration evidence prevents definition classification;
- removing construction evidence prevents definition classification;
- the unrelated genuine structure condition remains `WAIT_STRUCTURE`.

The corpus-wide read-only blast-radius measurement is authorized because it tests this classifier change. It does not authorize repairing additional strategies.

STOP if any condition beyond the authorized golden definition changes. Do not hardcode the video or strategy ID to force the population back to one.

FAIL-CLOSED MEANING

After STEP 3:

- the definition must be typed `OPENING_RANGE_DEFINITION`;
- it must not route to `compute_structure_state`;
- absent adapter wiring must produce an explicit measured refusal;
- a crash, missing dictionary entry or accidental exception is not an acceptable refusal.

EXPECTED TEST TRANSITION

The classification RED should turn GREEN.

The remaining state should prove:

- correct executable opening-range route still missing;
- typed opening-range output still missing;
- wrong structure fallback is impossible;
- census exact-delta control is GREEN;
- every neighboring condition is unchanged.

CENSUS CONVERSION

The same production commit must replace historical equality with exact authorized difference:

- one target classification changes;
- any mechanically derived target ID/hash change is identified;
- source prose, provenance, role and ordering remain unchanged;
- every non-target member remains identical.

Do not update the known-wrong census.

AR-824 MAY PROCEED.

Scope remains STEP 3 only: typed representation and exact classification, with no adapter, calculation, evaluator, breakout decision or neighboring repair.
