# H1 WAVE-6 — §2 QUALITY-MISS ITERATION: quote-as-you-extract — PRE-REGISTRATION (FROZEN 2026-07-12, OPERATOR-AUTHORED, before any pass-1 code exists)

> The pilot returned FIDELITY MISS (0/16, verified) → the pre-written §2 quality-miss branch. This pre-reg freezes the ≤2-pass iterate BEFORE the upgrade is designed and BEFORE the fresh set is sealed's transcripts are ever opened. Bar before score, again. Outranks memory. Un-pinned terms resolve to the strictest reading (Law 4).

## §0 — THE DIAGNOSIS EXCULPATES THE SOURCE; THE TWO PASSES AIM AT THE EXTRACTOR
What failed was NOT the source. The traders' words were right there — the anchor-locator grounded **89%** of conditions in verbatim speech, and two blind raters agreed **169/169** on what that speech meant (role agreement 96.8%). What DRIFTED was OUR PARAPHRASE: "close above" written from a quote saying "close below"; a 5-minute chart written from a 15-minute mouth. That is an **us-problem with the traders' words as prosecution evidence.** Pivoting sources now would falsify the WRONG party — H2 (do educators have edge?) remains UNTESTED precisely because our copier drifts. So the ≤2 passes aim at the **EXTRACTOR**, not the source. The source is exculpated by the evidence itself.

## §1 — THE UPGRADE: quote-as-you-extract (the campaign's saved extractor amendment)
The extractor emits **each condition's verbatim evidence quote AS it writes the condition** — the trader's words sit inside the generation window while the condition is being written, so a direction flip ("close below" → "close above") becomes near-impossible (the model cannot write a condition contradicting the quote it is simultaneously emitting). **Target (pre-registered, written before pass-1): the ~25% per-condition support-drift must fall to ~8%** — a 3× cut — for the strategy-level all-conditions-clean bar to clear at ≥60% of videos. Ambitious, but aimed at a **precisely-named mechanism** (support-drift / direction-flip), not a fog.

## §2 — THE FRESH SEAL (tonight, BEFORE the upgrade is designed — anti-set-shopping)
A **fresh-context agent seals a NEW video set (N≥10)** by the frozen selection criteria (same as the pilot's §3: day-trader / futures-mechanic-compatible / strategy-teaching / English / transcript-retrievable / ZERO overlap with the existing corpus AND the spent-16 AND the sealed-77), **IDs + metadata ONLY, opening NO transcript.** Sealed BEFORE the upgrade is designed → the student cannot study to the test. **The spent 16 are now RE-PURPOSED**: opened, read, rater-labeled, they become a **gold-labeled DESIGN + REGRESSION corpus** — their failure classes inform the fix, and the pass-2 READ happens on the FRESH set nobody has seen (per-item teaching-to-the-test is structurally dead). Same sealing machinery, same anti-set-shopping property, sealed before the student studies — again.

## §3 — ≤2 DESIGN PASSES on the extractor, under the SCHEMA-CHANGE PROTOCOL
This is the ONE extractor change the campaign has been saving its amendment for; schema-is-the-boundary ([[feedback_schema_is_decision_boundary_constrained_sampling_2026_07_12]]) applies IN FULL. Each pass is gated by:
- **5-fixture parity gate** (`tsx scripts/wave26-gemma4-smoke-test.ts --parity-only` MUST report PASS before merge — CLAUDE.md §2b).
- **Coverage-stability check** — the upgrade must NOT DROP conditions (quote-as-you-extract must not make the model emit fewer conditions to make quoting easier; measured against the design-pool coverage baseline).
- **Pre-registered design-pool support-rate target** — written BEFORE the first measurement (the ~8% drift / support-rate target above, measured on the DESIGN-side data, never the fresh sealed set).
- Doer≠grader independent grade on each pass, as every instrument change this campaign.
Budget: **≤2 design passes**, then STOP.

## §4 — ONE READ on the FRESH set — THE BAR DOES NOT MOVE
After the upgrade clears its design gates: unseal the fresh set → run the SAME full conveyor → read the SAME frozen bar ONCE, independently re-verified. **The bar is UNCHANGED: ≥60% of videos certificate-grade, ALL-CONDITIONS-CLEAN per strategy, economics rider (mean per-video-aggregate ≤ ~15) intact.** All-conditions-clean stays **on PRINCIPLE, not stubbornness**: a strategy with one wrong-direction condition IS A DIFFERENT STRATEGY — pruning a denied condition to certify the remainder would be **unfaithful-by-omission, laundering at the certificate layer.** The bar is the bar; it is NOT loosened now that 0/16 is known (Law 4).

## §5 — THE FORK (if the fresh read still misses after the ≤2-pass budget)
Two passes short → the campaign-plan's **Phase-1 fork fires exactly as pre-written**, and ONLY THEN does the option space open — cloud-model extraction economics, human-in-loop extraction, source reconsideration — **decided AT that fork with THAT evidence, not tonight.** Do not pre-decide the fork's options now.

---
*Frozen 2026-07-12, operator-authored, BEFORE any pass-1 code and BEFORE the fresh set's transcripts are opened. The witness is built; now we teach it to quote. Vaulted FF to `corpus-v3-gate3-cert-2026-07-06`.*
