# H1 WAVE-6 — §2 QUALITY-MISS ITERATION: quote-as-you-extract — PRE-REGISTRATION (FROZEN 2026-07-12, OPERATOR-AUTHORED, before any pass-1 code exists)

> The pilot returned FIDELITY MISS (0/16, verified) → the pre-written §2 quality-miss branch. This pre-reg freezes the ≤2-pass iterate BEFORE the upgrade is designed and BEFORE the fresh set is sealed's transcripts are ever opened. Bar before score, again. Outranks memory. Un-pinned terms resolve to the strictest reading (Law 4).

## §0 — THE DIAGNOSIS EXCULPATES THE SOURCE; THE TWO PASSES AIM AT THE EXTRACTOR
What failed was NOT the source. The traders' words were right there — the anchor-locator grounded **89%** of conditions in verbatim speech, and two blind raters agreed **169/169** on what that speech meant (role agreement 96.8%). What DRIFTED was OUR PARAPHRASE: "close above" written from a quote saying "close below"; a 5-minute chart written from a 15-minute mouth. That is an **us-problem with the traders' words as prosecution evidence.** Pivoting sources now would falsify the WRONG party — H2 (do educators have edge?) remains UNTESTED precisely because our copier drifts. So the ≤2 passes aim at the **EXTRACTOR**, not the source. The source is exculpated by the evidence itself.

## §1 — THE UPGRADE: quote-as-you-extract (the campaign's saved extractor amendment)
The extractor emits **each condition's verbatim evidence quote AS it writes the condition** — the trader's words sit inside the generation window while the condition is being written, so a direction flip ("close below" → "close above") becomes near-impossible (the model cannot write a condition contradicting the quote it is simultaneously emitting). **Target (pre-registered, written before pass-1): the ~25% per-condition support-drift must fall to ~8%** — a 3× cut — for the strategy-level all-conditions-clean bar to clear at ≥60% of videos. Ambitious, but aimed at a **precisely-named mechanism** (support-drift / direction-flip), not a fog.

## §2 — THE FRESH SEAL (tonight, BEFORE the upgrade is designed — anti-set-shopping)
A **fresh-context agent seals a NEW video set (N≥10)** by the frozen selection criteria (same as the pilot's §3: day-trader / futures-mechanic-compatible / strategy-teaching / English / transcript-retrievable / ZERO overlap with the existing corpus AND the spent-16 AND the sealed-77), **IDs + metadata ONLY, opening NO transcript.** Sealed BEFORE the upgrade is designed → the student cannot study to the test. **The spent 16 are now RE-PURPOSED**: opened, read, rater-labeled, they become a **gold-labeled DESIGN + REGRESSION corpus** — their failure classes inform the fix, and the pass-2 READ happens on the FRESH set nobody has seen (per-item teaching-to-the-test is structurally dead). Same sealing machinery, same anti-set-shopping property, sealed before the student studies — again.

## §3 — ≤2 DESIGN PASSES on the extractor, under the SCHEMA-CHANGE PROTOCOL
This is the ONE extractor change the campaign has been saving its amendment for; schema-is-the-boundary ([[feedback_schema_is_decision_boundary_constrained_sampling_2026_07_12]]) applies IN FULL. Each pass is gated by:
- **5-fixture parity gate** (`tsx scripts/wave26-gemma4-smoke-test.ts --parity-only` MUST report PASS before merge — CLAUDE.md §2b).
- **Coverage-stability check** — the upgrade must NOT DROP conditions (quote-as-you-extract must not make the model emit fewer conditions to make quoting easier; measured against the design-pool coverage baseline).
- **Pre-registered design-pool support-rate target** — written BEFORE the first measurement (the ~8% drift / support-rate target above, measured on the DESIGN-side data, never the fresh sealed set).
- Doer≠grader independent grade on each pass, as every instrument change this campaign.
Budget: **≤2 design passes**, then STOP.

## §4 — ONE READ on the FRESH set — THE BAR DOES NOT MOVE
After the upgrade clears its design gates: unseal the fresh set → run the SAME full conveyor → read the SAME frozen bar ONCE, independently re-verified. **The bar is UNCHANGED: ≥60% of videos certificate-grade, ALL-CONDITIONS-CLEAN per strategy, economics rider (mean per-video-aggregate ≤ ~15) intact.** All-conditions-clean stays **on PRINCIPLE, not stubbornness**: a strategy with one wrong-direction condition IS A DIFFERENT STRATEGY — pruning a denied condition to certify the remainder would be **unfaithful-by-omission, laundering at the certificate layer.** The bar is the bar; it is NOT loosened now that 0/16 is known (Law 4).

## §5 — THE FORK (if the fresh read still misses after the ≤2-pass budget)
Two passes short → the campaign-plan's **Phase-1 fork fires exactly as pre-written**, and ONLY THEN does the option space open — cloud-model extraction economics, human-in-loop extraction, source reconsideration — **decided AT that fork with THAT evidence, not tonight.** Do not pre-decide the fork's options now.

---
*Frozen 2026-07-12, operator-authored, BEFORE any pass-1 code and BEFORE the fresh set's transcripts are opened. The witness is built; now we teach it to quote. Vaulted FF to `corpus-v3-gate3-cert-2026-07-06`.*

---

## §6 — THE TERMINAL FRESH READ, PRE-COMMITTED NOW (before the pass-1 build, so its shape is never negotiated after the number exists)

The ONLY clean-room moment in Wave-6 is the terminal read on the sealed 12 (`h1-wave6-sealed-fresh-set-2026-07-12.json`, sha `4d7b3c29…`). Everything before it — schema change, prompt change, the four design-pool gates, the independent grade — is loop-work buildable under any context. The read's structure is FROZEN here, identical to the pilot's: **fresh-context conductor · same unseal discipline (pre-unseal checklist: transcript-fetch live / disk / tower+gemma → unseal → full conveyor once) · TWO independent blind control-gated raters (two-stage packets, leak-scanned) · read ONCE (Law 3) · independent fresh-context re-verification (doer≠grader) · the BAR UNCHANGED (≥60% videos certificate-grade, all-conditions-clean per strategy, economics rider ≤ ~15 mean per-video-aggregate).** The upgraded extractor is FROZEN at its pass-cleared SHA before the read; the read measures that frozen extractor on ground it has never seen. The 2-pass budget (§3) governs how many times the extractor may be revised BEFORE a terminal read; a fresh read is taken only on an extractor that has cleared the design-pool gates.

## §7 — RECORD NOTES (weighted, kept because each names something the campaign should not forget)

**§7a — The diagnosis was CORRECTED BY EVIDENCE, and it names a deeper disease than the authored example.** Direction-flips (the vivid case cited in the ruling) are only 11% of the support-miss; the dominant **37% is ELABORATION-BEYOND-EVIDENCE** — handed a definitional quote, the extractor writes an *actionable condition the trader never stated*. That is an LLM completing toward strategy-shaped specificity — and it has LINEAGE: the pinned lesson once praised this extractor for "refusing to fabricate parameters" (CLAUDE.md §2b), and the prompt generations since pushed toward strict-fill; somewhere in that evolution, refusal drifted into INVENTION. Quote-as-you-extract counters it at the root (the trader's words literally in view as each condition is written), and the two-stage Stage-2 support check now PROSECUTES elaboration by name. This is the **7th time evidence corrected an authored assumption this campaign — and the FIRST time it happened BEFORE the fix was built, not after.** Measuring before assuming is becoming the reflex.

**§7b — The GBNF property-order fix is the schema-boundary law WEAPONIZED FOR US — the first time it cuts our way.** Every prior encounter with schema-is-the-decision-boundary was a wound (deleted enums perturbing boundaries; added fields threatening the sampling distribution). Here the SAME physics is the cure: constrained decoding walks schema properties in DECLARATION ORDER, so declaring `transcript_quote` FIRST in each condition object means the evidence is GENERATED BEFORE the condition — evidence-conditioned generation by schema GEOMETRY, not by prompt pleading. **The field's POSITION is the fix.** The deepest law of this campaign, finally holding the pen instead of the knife.

**§7c — The COVERAGE GUARD is load-bearing; the record says why.** Quote-first creates a NEW temptation: the model dropping conditions that are hard to quote, which would raise the support rate BY OMISSION. Elaboration (§7a) and omission are MIRROR INFIDELITIES — one invents beyond the evidence, the other hides what it can't ground. The coverage gate (≥90% aggregate AND no single strategy loses >50% of its own conditions) is what forces the fix to be FAITHFULNESS, not selective silence. The four gates together fence every escape route: pass-1 can only clear by actually staying tethered to the trader's words.

*§6-§7 frozen 2026-07-12, operator-authored, BEFORE the pass-1 build. Vaulted FF.*

---

## §8 — READ-ORDER PIN + PASS-2 PRE-COMMITMENT (frozen 2026-07-13, BEFORE the aggregate coverage number exists)

**§8a — READ-ORDER: COVERAGE (Gate 2) is read BEFORE SUPPORT (Gate 3).** A support-rate win is UNINTERPRETABLE without its coverage context — because the cheapest way to "quote faithfully" is to STOP EXTRACTING THE HARD PARTS. Gate 2 is what makes Gate 3 mean something: only a support improvement that held coverage ≥90% is a real improvement; a support number bought by dropping unquotable conditions is omission wearing a good score. So the relay reads coverage first, support second, always.

**§8b — PASS-2 PRE-COMMITMENT: the NO-QUOTE SENTINEL (written now, before the coverage aggregate is known).** Quote-first corners the model on an unquotable condition into a FALSE binary: fabricate a quote (caught by the mechanical substring check) OR drop the condition (caught by the coverage gate). Both are infidelities. The honest THIRD DOOR is a **no-quote sentinel**: the schema permits a condition emitted with an explicit empty/`null` `transcript_quote` marker — which **routes UNANCHORED, counts against support truthfully, but PRESERVES coverage.** The model should NEVER face a choice between inventing evidence and silencing the trader; **"I extracted this condition but cannot ground it in a verbatim quote" is a legitimate, honest output the pipeline already punishes fairly** (UNANCHORED → not certificate-grade, exactly as it should be). ROUTING (pre-committed, conditional on Gate 2's outcome, decided before the number): **if aggregate coverage ≥ 90% → Gate 2 HOLDS, pass-1's coverage is faithful, and the sentinel stays a REGISTERED DESIGN NOTE (not built this pass).** **If aggregate coverage < 90% → Gate 2 FAILS, pass-1 fails honestly (the omission fence doing exactly its job), and the no-quote sentinel IS the pre-positioned PASS-2 shape** — the second of the ≤2-pass budget, aimed at the named mechanism (the fabricate-or-drop corner), not improvised after the fail. Either way, pass-1's verdict on Gate 2 stands as written; the sentinel does not retroactively rescue a pass-1 coverage miss.

*§8 frozen 2026-07-13, operator-authored, BEFORE the aggregate coverage number exists. Vaulted FF.*

---

## §9 — THE COVERAGE GUARD GETS GROUND TRUTH: two-stage guard + the 3-case adjudication routing (frozen 2026-07-13, BEFORE the adjudication result)

> Pass-1 Gate-2 read: aggregate 233/253 = 92.1% (≈101% de-crashed) MET; per-strategy guard tripped on 4 strategies-at-zero = 1 CRASH (`CLDEIsNpVRc::s0` 22→0, CLI exit 1) + 3 STRATEGY-COUNT divergences (`4cT8WTyxhYY::s1`, `IyFioFkRgWo::s1`, `WEhmadJArQo::s1` — pass-1 read the video as 1 strategy where baseline had 2). The vs-baseline guard cannot distinguish "silenced a real strategy" from "stopped reproducing the old extractor's over-split" — because the baseline is the FLAWED old copier, NOT ground truth. Resolution: replace the broken reference with the transcript (the gold neither extractor is).

**§9a — THE GUARD'S PERMANENT FORM (two-stage, so this ambiguity never recurs).** The per-strategy coverage guard is hereby TWO-STAGE: (stage 1) the vs-baseline count check is a cheap SCREEN — it never fails the gate by itself; (stage 2) a trip ESCALATES to TRANSCRIPT-ADJUDICATION of the specific tripped cases (blind, transcript-anchored, from source). Only an adjudicated-real loss fails the gate. The vs-baseline guard was always design-pool SCAFFOLDING; now it knows it.

**§9b — THE 3-CASE ADJUDICATION ROUTING (pre-committed before the result).** Three fresh-context BLIND agents (one per contested video: `4cT8WTyxhYY`, `IyFioFkRgWo`, `WEhmadJArQo`), each receives the TRANSCRIPT ALONE + one question — "how many DISTINCT strategies (each with its own entry/exit logic) does this video teach? Enumerate them." — seeing NEITHER extractor's output. Compare to pass-1's 1-strategy read. ROUTING:
- **All three adjudicate ONE-strategy** (baseline over-split) → the guard tripped on BASELINE ERROR → **Gate 2 MET in substance** (recorded decomposed: aggregate 92.1% ≈101% de-crashed; the 3 divergences adjudicated-faithful vs source) → proceed to Gate 3.
- **Any adjudicates a GENUINE second strategy pass-1 SILENCED** → **Gate 2 FAILED, honestly** — and this is a SIGNIFICANT finding, not noise: quote-first anchoring the model to CONTIGUOUS evidence could plausibly silence a strategy whose teaching is DIFFUSE/INTERLEAVED = the omission disease at the STRATEGY level, not the condition level. Pass-2's shape then carries BOTH levers: the no-quote sentinel (§8b, condition-level) AND a STRATEGY-COMPLETENESS STAGE (enumerate all distinct strategies FIRST, then quote-first extract within each).
- **Mixed → STRICT:** any real loss fails the gate; the adjudicated-faithful cases are recorded as baseline error. The guard's purpose is omission; one silenced strategy is one too many.

**§9c — THE TERMINAL READ NEVER HAD THIS PROBLEM.** The fresh 12 have NO baseline; their coverage instrument is the F-2 transcript-anchored lint — already the right reference (source, not a prior extractor). §9's vs-baseline scaffolding is a design-pool-only artifact and does not touch the terminal read.

**§9d — CRASH SELF-REFERENCE CHECK (cheap, before dismissing as plumbing).** `CLDEIsNpVRc` is ALSO the pass-1 prompt's worked EXAMPLE — so extracting its transcript makes the model re-encounter its own exemplar's source. Almost certainly plumbing (CLI exit 1), but "almost certainly" has died 7 times this campaign; the check is one log read. Then two-branch: re-extract + fold in (the crash is infra, not a coverage signal).

*§9 frozen 2026-07-13, operator-authored, BEFORE the 3-case adjudication result. Vaulted FF.*

---

## §10 — THE GUARD DECIDES ON PURPOSE (silenced CONTENT), not ontology (strategy-count) — the WEhmadJArQo resolution (frozen 2026-07-13, BEFORE the content check runs)

> The 3-case adjudication (§9b): `4cT8WTyxhYY`=1 (high), `IyFioFkRgWo`=1 (medium) — both baseline-over-split, pass-1 FAITHFUL. `WEhmadJArQo`=2 (medium), BUT the adjudicator flagged "a reasonable adjudicator could score this as 1" (two variants sharing the IDENTICAL opening-range-FVG-retest skeleton). The 1-vs-2 count is ONTOLOGICALLY UNDECIDABLE (same skeleton, different dress). Resolving it by advisor thumb (rule it 1) is the forbidden move; re-rolling it (a 2nd adjudicator) re-asks an undecidable question + invites rater-shopping optics. So the ruling re-grounds the question in the guard's FROZEN PURPOSE.

**§10a — §7c HUNTS SILENCED CONTENT, NOT STRATEGY-COUNT METAPHYSICS.** "No strategy loses >50%" was always a CHEAP PROXY for "the extractor must not drop what the trader taught." Whether WEhmadJArQo teaches one strategy with two configurations or two strategies sharing a skeleton, the decidable empirical question is: **did pass-1's output PRESERVE variant-B's teaching, or SILENCE it?** — answerable from artifacts in hand, no ontology needed.

**§10b — THE CONTENT CHECK (mechanical, blind to stakes).** A fresh-context checker receives: the TRANSCRIPT + the adjudicator's enumerated variant-B distinctives (the 15-minute timeframe, the passive-LIMIT confirmation, the 2:1 target) + PASS-1's extracted WEhmadJArQo condition set (`.../wave6-pass1-design-pool/extraction-vault/WEhmadJArQo.json`). **The BASELINE is IRRELEVANT — it is the reference for nothing now.** Element by element: is each variant-B distinctive PRESENT or ABSENT in pass-1's conditions, with quotes? Mechanical lookup, not worth-judgment.

**§10c — PRE-COMMITTED BRANCHES (before the check):**
- **Variant-B distinctives PRESENT in pass-1's conditions → NOTHING was silenced** — the teaching survived, re-segmented into one strategy object → **Gate 2 MET.** Record states plainly: the ontology question was MOOT; however you count the strategies, the trader's content crossed intact; the "-10" was RE-SEGMENTATION, not loss. → proceed to Gate 3.
- **Distinctives ABSENT → the trader taught it and the extractor DROPPED it → Gate 2 FAILED per §9b strict** — and it fails REGARDLESS of what we call the dropped thing (the omission fence was never about nomenclature). → pass-2 carries BOTH levers (no-quote sentinel §8b + strategy-completeness stage §9b).

**§10d — This is STRICT-ON-CONTENT, not strict-on-ontology.** The check is constructed to ASK THE QUESTION THE PROXY ALWAYS STOOD FOR, not to pass — it can absolutely still fail the gate. It is §9's own two-stage design continuing one level down: screen tripped → adjudication → adjudication ambiguous on the proxy axis → decide on the PURPOSE axis, from SOURCE. Supporting context (NOT verdict): the system's own library ontology already treats timeframe/confirmation/target variations as VARIANTS OF ONE CONCEPT (the variant-families architecture) — which is why strategy-count was always a fragile axis for this guard, and content-preservation the durable one.

*§10 frozen 2026-07-13, operator-authored, BEFORE the WEhmadJArQo content check runs. Vaulted FF.*

### §10 fold-in — CRASH RESOLVED + adjudications settled (Gate-2 status, 2026-07-13)
- **CLDEIsNpVRc crash = content-independent PLUMBING, self-reference RULED OUT (evidence).** The real error (hidden in the log's stdout_tail; the shown stderr_tail was only npm-version noise) was `callScoutExtractLlm returned null (both Ollama and cloud fallback exhausted)` — a transient mid-batch Ollama+cloud double-miss (video 5 of 16; the known ollama cold-load blip, memory `reference_ollama_coldload_spiral_fix`). Re-extraction SUCCEEDED 3/3 stable (16 conditions, identical), and the model did NOT reproduce its own exemplar's below/above mismatch → not exemplar-collision. **FOLD-IN:** CLDEIsNpVRc pass-1 = 16 vs baseline 22 = 72.7% retained → CLEARS the >50% guard. Aggregate coverage with CLDEIsNpVRc folded = 249/253 = **98.4%**.
- **Adjudications (§9b):** `4cT8WTyxhYY`=1 (high) + `IyFioFkRgWo`=1 (medium) → baseline over-split, pass-1 FAITHFUL, guard tripped on BASELINE ERROR (as §9 anticipated). `WEhmadJArQo` → resolved on the CONTENT axis (§10 check, in flight).
- **CARRY-FORWARD (observability defect, non-blocking):** `extractor_bridge.py:108-112` truncates the CLI error to `stderr_tail` + `stdout_tail` slices, and the design-pool error string surfaced only the stderr (npm noise) — the real error was in the un-surfaced stdout. Improve the error capture so a null-return root cause isn't hidden behind npm notices.
- **GATE-2 now hinges SOLELY on the §10 WEhmadJArQo content check:** all-present → Gate 2 MET (aggregate 98.4%, all guard-trips resolved as baseline-error/plumbing/re-segmentation) → Gate 3; any-absent → Gate 2 FAIL per §9b strict → pass-2 both levers.

### GATE-2 VERDICT — FAILED (content check resolved, 2026-07-13)
The §10 content check (blind, mechanical, purpose-axis): **ALL THREE variant-B distinctives ABSENT from pass-1's spine conditions** — pass-1 collapsed `WEhmadJArQo` to a single scalping strategy object; none of its 11 spine conditions captures the 15-min opening range (silenced EVERYWHERE, even speaker_concepts), the passive-limit-order confirmation, or the 2:1 target (the latter two leak into the freeform `speaker_concepts` appendix ONLY, not as actionable conditions). All three are genuinely taught in the transcript. → **per §10c: the trader taught it and the extractor DROPPED it → GATE 2 FAILED (per §9b strict), regardless of the 1-vs-2 nomenclature.** This is the STRATEGY-LEVEL omission the operator predicted: quote-first anchoring to contiguous evidence, on a video where two variants share a skeleton and are taught INTERLEAVED, merged them and silenced the second. **PASS-1 (quote-first) FAILS — the first of the ≤2-pass budget.** Coverage failing means **Gate 3 (support) is NOT read as a verdict** (§8a: a support number against failed coverage is uninterpretable — the omission §8a warned of is exactly what happened). **PASS-2 (second and final pass) carries BOTH pre-registered levers:** the STRATEGY-COMPLETENESS STAGE (§9b: enumerate all distinct strategies FIRST, then quote-first extract within each — directly targets this failure) + the NO-QUOTE SENTINEL (§8b: honest empty-quote → UNANCHORED, condition-level). OPEN: operator authorizes/shapes pass-2. Not an H2 falsification; the instrument (the content check) worked — it caught the silenced teaching by source.

---

## §11 — PASS-2 (the final swing): TWO-PHASE extraction — enumerate the lessons, then quote each (frozen 2026-07-13, operator-authored, BEFORE the pass-2 build)

> Pass-1 (quote-first) improved condition grounding but SILENCED an interleaved second variant (Gate-2 FAIL, §10). Pass-2 separates the two questions quote-first conflated: strategy SEGMENTATION (global — needs the whole transcript) vs condition GROUNDING (local — contiguous quotes). METHOD LINE minted this run and now house method: **proxy trips → adjudicate → adjudication ambiguous → decide on PURPOSE, from source** (when ontology coin-flips, measure content).

**THE ARCHITECTURE (two phases):**
- **PHASE A — GLOBAL ENUMERATION (new instrument):** whole transcript IN → strategy INVENTORY out. Its standard is the one tonight's three blind adjudicators just proved (3-for-3 sensible, transcript-alone): **distinct = own entry/exit logic; timeframe / confirmation / target variations = VARIANTS WITHIN ONE strategy, enumerated as such** (matching the library's variant-family ontology). Their task IS Phase A's task; their standard IS the prompt's standard.
- **PHASE B — QUOTE-FIRST CONDITION EXTRACTION, SCOPED PER ENUMERATED STRATEGY:** pass-1's quote-first schema (transcript_quote FIRST property) + the §8b NO-QUOTE SENTINEL added. Local grounding within a globally-established segmentation.

**THREE DESIGN-SPECIFIC GUARDS (pinned before build):**
1. **OVER-ENUMERATION is a named gaming vector — fenced now.** A degenerate enumerator that splits every condition-cluster into its own "strategy" manufactures tiny easily-certified strategies (the ≥1-clean-strategy-per-video bar would reward FRAGMENTATION). Fence: Phase-A's variant-standard (above) + the per-strategy guard now cuts BOTH DIRECTIONS on the design pool (checked against blind-adjudicated counts — over-split AND under-split both trip) + content-preservation as the deciding standard on any trip (the §9→§10 ladder just proven).
2. **`WEhmadJArQo` is a PERMANENT REGRESSION FIXTURE.** Pass-2 MUST preserve variant-B's distinctives (15m opening-range TF / passive-limit confirmation / 2:1 target) as EXTRACTED CONTENT — the exact pass-1 failure, frozen as a test forever. One-strategy-two-variants OR two-strategies is acceptable EITHER WAY iff the distinctives survive into conditions. Content is the bar; ontology stays moot.
3. **PHASE A is a NEW INSTRUMENT → BIRTH FIXTURES FROM BIRTH.** Founding test set: the three adjudicated videos (`4cT8WTyxhYY`=1, `IyFioFkRgWo`=1, `WEhmadJArQo`=1-or-2-with-variant-B-preserved) + WEh — fires-correctly proven BEFORE it counts (engagement-birthright Law 1).

**GATES CARRY FORWARD WHOLE:** parity (Phase-B schema changed — sentinel added) · coverage with the now-formalized TWO-STAGE guard (screen → transcript adjudication → content-preservation) · design-pool support-miss ≤8% one-rater · doer≠grader independent grade. Plus the CLDEIsNpVRc re-extraction folds in (resolved plumbing; the crash question closes).

**THE PRE-COMMITMENT, RESTATED (the one that matters):** this is the budget's LAST SWING. Design-pool gates CLEAR → extractor FREEZES at its SHA → terminal read on the fresh 12 (`h1-wave6-sealed-fresh-set` sha `4d7b3c29`), clean conductor, two blind raters, read ONCE, ≥60% all-conditions-clean, economics rider — **the bar does NOT move.** Gates SHORT → the Phase-1 fork fires with its options decided THERE, on that evidence. Either ending is an answer H1 was built to survive giving.

*§11 frozen 2026-07-13, operator-authored, BEFORE the pass-2 build. The witness learned to quote but went quiet on one lesson; pass-2 teaches it to count the lessons first. Vaulted FF.*

---

## §12 — DESIGN-POOL REPORTING COMPLETENESS: both support numbers (NOT a gate change — gates frozen) (2026-07-13, before the pass-2 measurement runs)

**THE VECTOR CLOSED.** The §8b sentinel is 15% non-gating and nulls are EXCLUDED from the support-miss ratio. That combination leaves a theoretical self-defeating GAMBIT: null the hard conditions, ace the support ratio over the EASY remainder, clear the design gate — then FAIL the terminal read anyway, because unanchored (null) conditions count AGAINST all-conditions-clean at certificate time (§4). The gambit CANNOT WIN — but it could WASTE THE LAST SWING by freezing a SHA on FALSE design-pool confidence (a support number the fresh 12 will not reproduce).

**THE REQUIREMENT (reporting, not gating).** The design-pool report carries BOTH numbers SIDE BY SIDE:
1. **GATED support-miss** — over QUOTED conditions only (nulls excluded). This is what the FROZEN §3/§11 gate reads (≤8%). Unchanged.
2. **TERMINAL-EQUIVALENT support-miss** — over ALL conditions (nulls counted as MISSES). This is the number the fresh 12 will ACTUALLY grade at certificate time.

The GATE reads #1 (frozen, no re-litigation). The **SHA-FREEZE DECISION reads #2** — because a SHA frozen on a gated number that the terminal read will not reproduce is a wasted final swing. Same precedent as the failure-attribution annotation and the economics per-strategy annotation: **the verdict on the frozen criterion, the truth beside it.** If #1 clears ≤8% but #2 is alarming (high null-rate inflating the terminal-equivalent), that is a documented reason to NOT freeze the SHA on this pass even though the gate passed — surfaced to the operator, decided before the fresh 12 are touched.

*§12 frozen 2026-07-13, before the pass-2 design-pool measurement. Vaulted FF.*

---

## §13 — THE OVER-SPLIT DISPOSITION: asymmetric to under-split, on physics (frozen 2026-07-13, BEFORE the design-pool over-split number exists)

> Phase-A birth fixtures already show `IyFioFkRgWo`→2 where its blind adjudication ruled 1 (a hand-wavy "big move" mention, no runnable second setup). An over-split. Its rule is pinned before its design-pool number.

**NAME THE DISEASE:** an enumerator writing a runnable strategy from a hand-wavy mention is ELABORATION-BEYOND-EVIDENCE at the STRATEGY level — the same disease family as pass-1's dominant 37% condition-level class, one floor up. Fabrication and silencing are BOTH infidelity — but they are NOT symmetric at the certificate layer, and the rule follows the physics:
- **SILENCED content (under-split) is UNRECOVERABLE downstream** — a certificate that omits what the trader taught lies forever; nothing later can know. → **under-split = STRICT FAIL** (§9b/§10, already enforced).
- **FABRICATED strategies (over-split) are SELF-CORRECTING downstream** — a phantom built from narration has no runnable rules to quote, so its conditions can't anchor and can't survive Stage-2 support; **it fails cert-grade ON ITS OWN.** The certificate layer catches INVENTION; it cannot catch OMISSION. The strictness budget goes where the net doesn't reach.

**THE RULE (pre-committed):** an ADJUDICATED over-split (transcript teaches no runnable second setup — the transcript-adjudication of the §9a-screen trip decides this, as IyF's already does) →
1. the phantom is **EXCLUDED from the video's strategy set for gating** (it does not count as a strategy toward the ≥1-clean bar, over OR under),
2. **recorded as a strategy-level elaboration defect**, and
3. its wasted adjudications **count honestly against the video's economics AGGREGATE** (§8b/Add-7).
**NOT an automatic Gate-2 fail — UNLESS SYSTEMATIC:** adjudicated fabrications in **more than 2 of the 16 design videos = the enumerator is unreliable = Gate 2 FAILS.** (Threshold is a CALIBRATION ESTIMATE, set now while only the one birth-fixture case is known, grounded as "the known case PLUS ONE" — beyond that, the defect generalizes.) Since this is the budget's FINAL pass, no prompt iteration is available regardless — the rule decides whether pass-2 FREEZES with a recorded characteristic or FAILS on a systematic one.

**WHAT THIS PRESERVES:** Addendum 6 → a phantom NEVER blocks the real strategy's certification; the exclusion rule → it never HELPS either; the economics tax → its cost is never HIDDEN. **Invention can't win, can't block, can't hide — it can only be counted.**

*§13 frozen 2026-07-13, operator-authored, BEFORE the design-pool over-split number exists. Vaulted FF.*
