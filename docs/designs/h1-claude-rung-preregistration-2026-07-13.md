# H1 — CLAUDE EXTRACTION RUNG — PRE-REGISTRATION (frozen 2026-07-13, BEFORE any number)

> gpt-5.4's rung closed at the joint bar (grounding won 10.8→2.0; content-preservation missed 3/10 subtle items within its licensed swings). Ladder advances to the Claude seat, carrying the instrument gpt-5.4 improved. Two passes, same as everyone; bar unmoved.

## PIN 1 — the instrument goes to frontier-v3 BEFORE Claude sits (structural, brain-agnostic)
The ZF8 diagnostic proved the deepest miss was an INFORMATION-FLOW gap, not shyness: Phase-A enumerated the ATR-height filter; Phase-B (scoped) was never held accountable for it and dropped it. Fix = the **Phase-A→Phase-B COVERAGE CONTRACT** (`transcript-extractor-frontier-v3.md`): the enumerator's element inventory rides into every Phase-B call as a CHECKLIST; every inventoried element must APPEAR in the extraction OR be marked ABSENT-WITH-REASON in `coverage_notes` — silence is forbidden. A downstream **inventory-vs-extraction diff lint** runs on every video forever (a free new fence). Generically worded (no answer key). Consistency rule: the next rung never sits an exam on a diagnosed defect. Direction-check: the contract can only ADD content to the denominator → makes its own grounding test harder (legitimate-fix signature).

## PIN 2 — cross-vendor grading restored by FLIPPING seats
Claude extracts → so the content-preservation panel must not be all-Claude (correlated-priors risk the pre-reg named). **At least one content-preservation grader is an OpenAI brain (gpt-5.4)** — it knows nothing of Claude's outputs and spent a week expert at exactly this failure class. The MECHANICAL axis (locator + F-2) stays the anchor authority as always. doer≠grader stays cross-COMPANY where the bar bites. Pennies.

## PIN 3 — provenance + the STANDING joint bar
- **Model ID frozen: `claude-opus-4-8[1m]`** (a rung certifies at prompt+model — no exception for the home team).
- k=5 modal Phase-A carries; single-draw Phase-B carries.
- **The JOINT BAR — grounding ≤8% locator support AND content-preservation clean — is now the STANDING design-pool bar for EVERY future rung** (tonight proved the single bar can be bought by omission).
- 10 REGRESSION FIXTURES (the original 7 + 0xyg stop + ZF8 ATR-height + dV7 meta-discipline block) guard every reader.
- Budget: standard TWO passes. Miss → terra + gpt-5.5 both sit the 6-fixture birth gate in parallel (<$1 total), cheapest passer first (terra at half 5.5's price gets first claim). human-in-loop holds the floor.

## PIN 4 — pacing
Claude extraction draws the SAME subscription windows as grading + orchestration — ZERO dollars, but paced. Birth gate same-day; the 16 over a day or two interleaved with grading. Balance $4.70 untouched; sealed 12 sealed.

## THE EXAM (order)
1. **Birth gate:** Claude Phase-A enumeration (strategy-enumerator.md, UNCHANGED) on the 6 fixtures, k=5 modal consensus, judged vs the re-minted windows + content. (Claude re-qualifies under the current standard — its shakedown run predated the 3-rule standard.)
2. Pass → **design pool 16** on frontier-v3: Claude Phase-A (k=5) + Phase-B (coverage contract) → JOINT bar (grounding ≤8% AND content-preservation clean via the flipped-vendor panel + coverage lint). Sealed 12 stay sealed until the 16 clear.
3. Joint-clear → freeze prompt+model SHA → terminal-read preconditions. Miss → terra+5.5 parallel birth gate.

*Frozen 2026-07-13, operator-ruled, BEFORE any Claude number. Vaulted FF.*
