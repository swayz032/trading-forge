# EXTERNAL READ — 2026-08-04 — AR-794 / LANE 32 / FINAL GATE-2 RE-GRADE AUTHORIZATION

> **PROVENANCE:** Arrived as **OPERATOR-RELAYED CHAT**, not on
> `origin/external-advisor/gpt-rulings` (stale at `953a907c`, 2026-08-02, per `R-698`).
> Committed here VERBATIM under `R-700 §1`. Consumed by `R-712`.
> **A CHANNEL IS NOT AN AUTHOR** — `R-712` adopts on merit, records what it refuses
> (the `Gate 1..5` scale, again), and notes where the read independently reached a
> correction this desk had already made (`R-711 §2`, the cloud-backend wording).

---

GPT EXTERNAL ADVISOR RULING — ACCEPT AR-794; LANE 32 CLOSED, CANONICAL REGRESSION WITNESS DURABLE, GATE 2 AWAITS FINAL INDEPENDENT RE-GRADE

AR-794 is accepted at commit:

`a3f75aa7`

Lane 32 is closed.

The worker correctly repaired the final known verification weakness in the canonical regression-population witness without changing production behavior.

Measured production status remains:

- `spec_condition_compiler.py` unchanged;
- compiler SHA-256 remains `621302a56987f19b`;
- activation flag remains OFF;
- producer and sealed specification remain untouched;
- no new source-to-execution claim is authorized.

LANE-32 DISPOSITION

`5/5 ITEMS SATISFIED`

Accepted results:

1. The current tail-name collisions were evaluated using the real committed population function.
2. Neither measured collision is load-bearing in the present tree.
3. The canonical 95-member regression population is committed by member and order.
4. The population derivation now has a genuine transitive two-hop witness.
5. Removing recursion reproduces the grader's `95 → 23` collapse and turns permanent tests RED.
6. Membership deletion, addition, and reordering each produce a manifest mismatch.
7. The same derivation and comparator are used by both the guard and its break-controls.
8. Population generation is independent of the process current working directory.
9. The canonical scoped population runs and attributes zero failures to the Lane-32 test changes.

APPROVED CLAIM

> The canonical compiler regression population is now a committed, deterministic, repository-contained artifact whose membership and ordering are checked against the real transitive derivation.

The historical 80-member and 81-member populations remain:

`UNRECOVERABLE_INSTRUMENT_PROVENANCE`

The 95-member manifest is prospective evidence beginning with the canonical baseline. It is not a reconstruction of either historical population.

TRANSITIVE RED-PROOF ACCEPTED

The recursion-deletion mutation is a valid proof.

Measured mutant behavior:

- canonical population: `95`;
- recursion-deleted population: `23`;
- permanent two-hop fixture: RED;
- canonical-manifest fixture: RED;
- one-hop control: GREEN.

This establishes that the former break-control tested only direct reachability and could remain green while most of the transitive population disappeared.

Approved lesson:

> A reachability guard must contain a path whose membership depends exclusively on recursion. A direct-edge fixture cannot certify transitive closure.

TAIL-NAME COLLISION RULING

The current collisions are accepted as harmless only for the measured tree:

- `core`;
- `liquidity`.

No present collision has exactly one module reaching a target.

This is not authorization to treat last-segment joining as structurally safe forever.

The independent re-grade must plant a controlled load-bearing tail collision and verify that the committed manifest guard turns RED.

If that attack can alter the semantic population without failing a permanent test, record:

`UNCOVERED_HOLE — AMBIGUOUS MODULE-TAIL JOIN`

Do not redesign the derivation before the grade unless that attack proves the current witness blind.

MANIFEST ORDER RULING

Pinning order is approved.

The population is used as an executable test sequence, and order-dependent inherited tests have already been measured elsewhere. Therefore order is part of the reproducibility contract, not cosmetic formatting.

The manifest test must continue to fail on:

- missing member;
- unexpected member;
- reordered members.

Future intentional population changes require:

- explicit derivation evidence;
- reviewed manifest update;
- before-and-after member diff;
- explanation of every added or removed test file.

PRE-REGISTRATION RECORD

The Lane-32 RED prediction matched exactly:

- minimum expected RED set: 2;
- observed RED set: 2;
- expected GREEN set remained GREEN.

This is accepted as a clean prediction.

It does not erase the Lane-29 and Lane-30 under-predictions.

The standing mutation-report format remains:

- `MINIMUM_EXPECTED_RED_SET`
- `EXPECTED_GREEN_SET`
- `OBSERVED_RED_SET`

A mutation proves the full guard surface only when it is shown to be equivalent to complete feature absence.

TOOLING DISCLOSURE ACCEPTED

The first scoped-regression invocation was invalid because Windows Python and MSYS Bash resolved `/tmp` to different locations, leaving pytest with zero explicit paths and causing broad collection.

The invalid run must not be cited as evidence.

The corrected Python-to-subprocess execution is accepted.

Approved operational rule:

> A scoped pytest command must assert that its resolved test-path list is non-empty before invoking pytest.

Add or retain that assertion in the canonical runner so an empty list cannot silently become a whole-suite run.

CLOUD-BACKEND WORDING CORRECTED

`test_cloud_backend.py` was never a member of the canonical 95-file population.

Therefore do not describe the canonical population as:

> 95 files minus `test_cloud_backend.py`

Correct description:

> The canonical 95-file import-closure population does not include `test_cloud_backend.py`.

The separate directory-wide engine population does require an explicit exclusion because that file is measured HUNG.

The file remains:

`HUNG / UNENUMERATED / DESK-OWNED`

It is not passing, covered, or repaired.

LANE-32 SCOPED REGRESSION ACCEPTED

Current canonical population result:

- `31 failed`;
- `2204 passed`;
- `3 skipped`;
- `2 xfailed`;
- `0` failures attributed to the Lane-32 edited test file.

The 31 failures remain inherited and undiagnosed.

Do not join them to an earlier population merely because the integer matches. The final grader must record their full node IDs from the pinned commit.

CURRENT GATE POSITION

Gate 1 — evaluator parameter consumption: COMPLETE

Gate 2 — activation-safety implementation and permanent witnesses: COMPLETE

Gate 2 — independent certification: PENDING

Gate 3 — typed dispatcher object: BLOCKED

Gate 4 — source lesson to sealed specification: NOT STARTED

Gate 5 — complete end-to-end breakthrough: NOT STARTED

Gate 2 does not pass merely because Lane 32 is accepted.

FINAL INDEPENDENT RE-GRADE AUTHORIZED NOW

Dispatch `accuracy-validator` against pinned commit:

`a3f75aa7`

The grade must include all production and test changes from:

- Lane 28: `556122b7`;
- Lane 29: `d9684c64`;
- Lane 30: `b8321dc9`;
- Lane 32: `a3f75aa7`.

Expected completion window:

`60–90 minutes`

Before execution, publish:

- start timestamp;
- pinned commit;
- runner identity;
- expected duration;
- durable receipt path;
- canonical manifest SHA-256;
- canonical population command;
- production compiler SHA-256.

If unfinished at 90 minutes, publish a liveness update rather than leaving an empty receipt.

MANDATORY GRADE SCOPE

The independent checker must verify:

1. Every enforced handler either consumes supported parameters or refuses them.
2. No parameter is accepted merely because it changes cache identity.
3. `_h_structure` refuses all non-empty parameters.
4. `WAIT_BIAS` consumes off-default periods through the real evaluator.
5. Different periods remain cache-isolated.
6. Identical periods reuse one real computation.
7. Declaration order cannot contaminate either result.
8. Flag OFF refuses parameterized bindings.
9. Flag ON refuses parameters on non-consuming routes.
10. Short-frame returns cannot bypass acknowledgement.
11. Complete unknown-key fallback refuses.
12. Both partial-recognition directions refuse.
13. Insufficient history cannot silently return a default-based result.
14. Refusal precedes confirmation, evaluator execution, cache construction, and state mutation.
15. The production-binding census is repository-anchored and non-vacuous.
16. All six supported producer forms are detected.
17. The canonical regression population follows genuine transitive paths.
18. Membership and order match the committed manifest.
19. The population remains identical from a different current working directory.
20. An empty resolved test-path list cannot launch pytest silently.

MANDATORY NOVEL ATTACKS

The grader must attempt at least:

- restore `_h_structure` accept-and-discard behavior;
- use parameters only in its cache key;
- move acknowledgement below the short-frame return;
- move refusal after `candle_confirmation_check`;
- honor fast while defaulting slow;
- honor slow while defaulting fast;
- treat an unknown alias as an absent parameter;
- disable the census by changing current working directory;
- make the census scan zero files;
- add each supported constructor form as a production parameter writer;
- delete the recursive branch from population derivation;
- drop one canonical population member;
- add one unexpected member;
- reorder two members;
- introduce a load-bearing module-tail collision;
- supply zero resolved pytest paths.

Every attack must either fail a permanent test or be recorded as:

`UNCOVERED_HOLE`

GATE-2 CLOSING RULE — UNCHANGED

Gate 2 closes only on:

`PASS`

or:

`PASS_WITH_BOUNDED_FINDINGS`

with no finding in these categories:

- silent substitution;
- partial recognition;
- unused accepted parameters;
- flag-OFF parameter loss.

A bounded finding concerning historical 80-versus-81 provenance does not block Gate 2 because that evidence is formally retired and no reconstruction claim remains.

A new finding that allows the canonical population or parameter guard to pass vacuously must be repaired before Gate 2 closes.

TRADE-COMPARISON TOOL

The statement that the trade-comparison tool answers a different question than the current plan assumes cannot be ruled on from AR-794 because this packet contains no executable call path, inputs, outputs, or intended comparison contract for that tool.

That item remains on HOLD.

Do not use the trade-comparison tool as breakthrough evidence or a completion gate until its separate report provides:

- the question the plan expects it to answer;
- the question it actually computes;
- one controlled case where the answers diverge;
- the exact production consumer of its result;
- whether the mismatch can create a false pass or false failure.

NEXT ACTION

Start the independent Gate-2 re-grade now.

No additional worker implementation is authorized while it runs.

If the grade satisfies the unchanged closing rule, the next build is Gate 3:

> Route one complete typed moving-average instruction object through the enforced dispatcher and certified evaluator path.

AR-794 closes the regression-witness work. The only remaining Gate-2 question is now independent and binary: can the full activation-safety surface survive hostile mutations without accepting, replacing, ignoring, or losing a taught parameter?
