# H1 — FRONTIER-EXTRACTOR PRE-REGISTRATION — frozen 2026-07-13, BEFORE any number

> The deeper fork, ruled on Option R's evidence: **the conveyor gets a frontier-class brain in the EXTRACTION seat. Gemma keeps the FILTER seat. Nothing else changes.** This is a fresh phase with its own budget, not a continuation of Wave-6.

## §0 — THE RULING & ITS REASONING (why the strong brain reads)
Option R proved the recombination architecture RIGHT (locator 10.8% miss beats the generated quote field's 16.5%) AND proved the disease is the reader, not the discipline: pass-2's ~11% locator-miss on its own conditions **matches the pilot extractor's ~11% under the same mechanical instrument.** Two full prompt-architecture passes moved condition-drift essentially zero — a capability ceiling, measured cleanly twice. Prompting a 4B model twice proved prompting can't cut the drift.

**The arithmetic demands a bigger reader:** raters are STRICTER than the locator (pilot: 11% locator-miss → 25% rater-drift → 0/16 certificates), and the terminal ≥60% bar COMPOUNDS per-condition support across every condition in a video. A ~3-point shave was never going to matter; drift must be cut by HALF or more. The remaining lever is the reader's capability.

**Engine = HYBRID conveyor:** gemma stays where its economics are unbeatable — high-volume discovery/triage across hundreds of videos at zero marginal cost (the FILTER seat). The EXTRACTION seat — the one job where an 11% reading error compounds into 0/16 — goes to a fresh-context **frontier-class (Claude-family) agent, IN-SUBSCRIPTION** (no new API spend surface, no operator wallet gate). Everything downstream stays exactly as this week hardened it: **locator as mechanical anchor authority, F-2 content floor, the merge-silencing fence, two-stage tier-3 packets, blind raters.**

**Why NOT human-in-loop first (it stays the licensed fallback):** its economics passed (13.94 ≤ 15), but repair-after-weak-extraction at ~25% rater-drift means frontier agents doing high-volume REPAIR of a weak reader's output. Putting the strong brain in the READING chair spends the same resource strictly more directly. If residual drift survives, **the human-in-loop lane remains licensed as the fallback rung, economics already certified** — the campaign never stalls waiting for a ruling the evidence has already shaped.

## §0b — THE NAMED RISK (pre-registered + mitigated): correlated paraphrase-tolerance
A Claude-family extractor + Claude-family raters could **correlate**: the extractor's drift looking "supported" to a rater sharing its priors. **The independent axis that breaks this is already built and just proved itself: the mechanical locator + F-2 content floor are MODEL-FREE.** They stay the anchor authority; **raters judge ONLY above the mechanical floor** (a condition that fails to ground mechanically never reaches a rater as "supported"). Named, mitigated, pre-registered — the model-free axis is non-negotiable and does not become a Claude-judges-Claude step.

## §1 (ORDER 1) — ARCHITECTURE: only the brain swaps
Pass-2's two-phase design survives WHOLE: **Phase-A global enumeration → Phase-B quote-first per strategy.** Only the model in the extraction seat changes (gemma → frontier-class in-subscription agent). **Schema conformance stays in POST-PROCESSING** (schema-is-the-boundary law [[feedback_schema_is_decision_boundary_constrained_sampling]]: the schema/enum IS the decision boundary; the frontier agent returns best-effort structured output, a deterministic post-processor conforms it — conformance is post-processing, never a schema edit that perturbs the decision surface). The Phase-A enumerator prompt and Phase-B extractor prompt carry over unchanged; the dispatch mechanism swaps to an in-subscription subagent.

## §2 (ORDER 2) — BIRTH FIXTURES FROM BIRTH (Law 1), and the campaign is now RICH
The new extractor's enumeration is proven fires-correctly on **six adjudicated ground-truth fixtures BEFORE anything counts** — the blind adjudications this week minted them:
| Video | ground-truth enumeration | role |
|-------|:---:|---|
| `WEhmadJArQo` | **2** (variant-B distinctives MUST survive) | the pass-1 silencing witness |
| `R5L890juvRw` | **2** (trend-continuation + mean-reversion) | **the merge-silencing witness now guards the NEW extractor's birth** |
| `IyFioFkRgWo` | **1** (must NOT phantom a second) | the over-split witness |
| `4cT8WTyxhYY` | **1** | baseline-error witness |
| `E9MzEC_yNoM` | **3** | baseline-undercount witness |
| `-igpOZs8LsM` | **1** | baseline-overcount witness |
Fires-correctly on all six is a hard birth gate; nothing measures until it passes.

## §3 (ORDER 3) — DESIGN POOL FIRST, SAME BARS (no bar moves either direction)
Measured on the **spent-16 design pool** under the IDENTICAL frozen bars: **≤8% one-rater support-miss** (locator-anchored, the Option R instrument) + the **two-stage enumeration guard** (bidirectional over/under-split fence + §13 disposition). No bar moves up OR down. **The sealed 12 stay SEALED until the design pool clears.**

## §4 (ORDER 4) — BUDGET PINNED AT FREEZE
**Two passes total: one build + one iteration** — mirroring the discipline that kept Wave-6 honest. **Pass-accounting STATED in every dispatch, CONFIRMED by the builder** [[feedback_vault_before_iterate_and_pass_accounting]]. Halt over-budget BEFORE any keepable number.

## §5 (ORDER 5) — ECONOMICS RIDER
Adjudication load per video RECORDED. Hypothesis under test: **it falls well below 13.94 as drift falls** (fewer fallthroughs/mis-groundings → fewer tier-3 adjudications). Reported, not gated.

## §6 (ORDER 6) — CLEAR/MISS, both ways
- **CLEARS the design pool (≤8%)** → freeze at **prompt+model SHA** → **terminal read** with every precondition already pinned tonight: (a) the **fence witness probe (R5L890) through the REAL terminal-read harness** confirming REJECTED/INDETERMINATE; (b) **topology engagement on a known-good fixture** (the A-packet producer wired, so structural lints EVALUATE not NOT_EVALUATE — else every video is INDETERMINATE by the fence's forcing function); (c) the **frozen read shape** (fresh conductor, 2 blind raters, ≥60% all-conditions-clean via `terminal_read_clean_fraction`, economics rider); (d) **read ONCE.**
- **MISSES** → the **human-in-loop primary lane fires WITHOUT returning to the operator** — that rung is PRE-LICENSED now (economics certified 13.94 ≤ 15), so the campaign does not stall on a ruling the evidence has already shaped.

## Provenance
Frontier extractor = fresh-context in-subscription Claude-family agent. Locator/F-2/fence/raters = model-free or independently-graded, UNCHANGED. Downstream hardening (this week): locator `2b278757`, 3-axis defense `11be7e7b`, merge-silencing fence `34b2984a`/`71157801`. Prior fork evidence: Option R MISS `71157801`, pilot 0/16 `a73c1f60`.

*Frozen 2026-07-13, operator-ruled, BEFORE any birth-fixture or design-pool number. Vaulted FF on `corpus-v3-gate3-cert-2026-07-06`.*

---

## ADDENDUM 1 — CANONICAL STANDARD RULED + instrument aligned (dated 2026-07-13, pre-number; PASS 1 of 2)
The birth gate exposed a two-ontology ambiguity between the fixtures (runnable-trigger rubric) and the instrument (config-groups). Operator ruling: neither wholesale — the **three canonical rules** (`h1-canonical-enumeration-standard-2026-07-13.md`): Rule 1 evidence floor (§13), Rule 2 opposition-always-splits (fence law), Rule 3 compatible-variation-flexible-content-preserved (§10a/§11). **`strategy-enumerator.md` amended to encode all three explicitly** (Rule-2 override added to the over-split/under-split/ambiguity sections so "don't over-split" never licenses fusing opposites; Rule-1 evidence floor; Rule-3 content-is-the-bar). Pre-birth instrument alignment under an operator-ruled standard — NOT a bar move (reconciliation to frozen law). PASS-ACCOUNTING: this is PASS 1 of the pinned 2 (one build + one iteration); the amendment + re-mint + rung-1 build are all pass 1.

## ADDENDUM 2 — BRAIN-AGNOSTIC LADDER (dated 2026-07-13, pre-number)
The EXTRACTION seat is filled by a ladder; **identical instrument, birth gate, and frozen bars at every rung**; nobody skips the line, nobody gets an easier test:
- **Rung 1 — gpt-5.4-mini** (2.5M/day free pool). First: free, pennies.
- **Rung 1.5 — gpt-5.4 (full)** (250K/day free).
- **Rung 2 — Claude in-subscription.**
- **Rung 3 — human-in-loop** (economics already certified 13.94 ≤ 15).
Each rung: pass the 6-fixture birth gate (re-minted windows + content checks) → design-pool 16 under the SAME ≤8% bar → clear → sealed-12 terminal read. Miss → next rung, same exam. **Tonight's Claude enumeration run = INSTRUMENT SHAKEDOWN (it caught the standards bug; purpose spent + honored), NOT a rung-2 result.** Rung order applies from here. GRADING stays Claude's seat (the ladder governs only the extraction seat).

## ADDENDUM 3 — GOVERNOR BEFORE ANY METERED CALL (dated 2026-07-13)
Nothing calls OpenAI until the token governor is BUILT + PROVEN: hard **1M/day extraction partition**, per-video receipts, **fail-closed wall under the free ceiling**, **synthetic-budget trip test as engagement proof** (the wall demonstrably trips before any real call), plus the measured **7-day pull from `ai_inference_log`** (designed-vs-measured, two-path). Order: standard → re-mint fixtures (Claude adjudicators, blind) → governor proven → rung-1 birth gate.

## ADDENDUM 4 — the free-brain roster, parallel birth, two-pool governor (dated 2026-07-13, pre-number; operator-ruled)
Fixtures are re-minted under the 3-rule standard (two-path 6/6). Now the extraction-seat roster and its rules:
- **Roster:** gemma FILTERS hundreds of videos (local, unlimited); **gpt-5.4-mini** is first in the reading chair (2.5M/day); **gpt-5.4 full** is the pre-qualified understudy + judgment specialist (250K/day); **Claude GRADES everything** (subscription); wallet sealed behind the governor on every path.
- **Parallel birth:** BOTH OpenAI brains sit the 6-fixture birth gate IN PARALLEL (a few K tokens each — basically free). **Mini holds rung-1 priority** for the design-pool-16 measurement (10× daily tokens → the better production reader if good enough). Big-5.4 pre-qualified = **zero idle days** — if the mini stumbles it steps into the same exam chair immediately.
- **Role-split CONTINGENCY (licensed, not default):** if the exams reveal the mini is strong at the MECHANICAL half (Phase-B condition copying) but weak at the JUDGMENT half (Phase-A enumeration — where tonight's ontology subtlety lives), the pre-registered split fires: **gpt-5.4 does Phase-A enumeration** (~15K/video → 250K pool ≈ 15 videos/day), **mini does Phase-B copying** (2.5M pool). This is a licensed contingency the EVIDENCE must indicate, and it **requires its OWN birth re-check** (the split pipeline re-passes the 6-fixture gate). Default stays: one brain (mini), whole job.
- **Governor walls BOTH pools (built + PROVEN 2026-07-13, 22/22 fail-closed checks, `extraction-token-governor.ts` + `h1-governor-trip-test.ts`):** mini 1M/day; **gpt-5.4 200K/day with 50K HELD BACK** as a critique-system reserve (when live trading starts, trade-critique draws on the big pool; extraction must NEVER starve a production consumer). Same fail-closed rule both pools: hit the wall, stop, resume tomorrow, card untouchable. An override that tries to claim the reserve is clamped.
- **INTEGRITY LINE — OpenAI NEVER grades OpenAI:** the big GPT can READ (extraction seat) but never GRADES an OpenAI extraction. Cross-vendor checking is what made this stronger (OpenAI copies → mechanical word-checker verifies → Claude judges). OpenAI-grades-OpenAI would recreate the student-grades-classmate risk just engineered out. **Grading stays Claude's seat, period.**

---

## ADDENDUM — SUPERSESSION: §6(b) A-packet-as-terminal-read-precondition is RETIRED (dated 2026-07-16, per ADVISOR-RULINGS R-014 + R-016 §5)

> House pattern: frozen docs are corrected by dated addenda, never silent edits. The original §6(b) text stands unedited in the record as the superseded claim.

**What changed:** §6(b) of this pre-reg listed the A-packet (compiled-topology producer) as a **terminal-read PRECONDITION** — required so the 3 mechanical structural lints (direction-conflation / unsat-sat / or-alternatives) could EVALUATE instead of returning NOT_EVALUATED. That framing is **RETIRED.**

**By what, and why:** ADVISOR-RULINGS **R-014** (2026-07-15, path-(d)) moved the terminal read's structural guarantees to **SEMANTIC CROSS-VENDOR checks** (conflation axis + enumeration-consistency axis in `terminal_read_grade`), under the minted law "on prose-format artifacts, structural guarantees are semantic cross-vendor; mechanical/lexical checks are reserved for structured formats — the H2 compiled-spec layer." Consequently the **3 mechanical lints are re-stationed to the H2 layer** and the **A-packet reverts to its original H2-battery charter — OFF the terminal-read critical path** (`ADVISOR-RULINGS:54`). The A-packet's own ratify-packet was independently graded NOT-SAFE as a trivial-pass terminal gate, corroborating the move.

**Current authority for the terminal read:** the sealed-12 terminal-read driver spec = ADVISOR-RULINGS **R-015 §6-CONSOLIDATED as corrected by R-016**. The A-packet is NOT among its preconditions. Any future reader must NOT re-inherit §6(b)'s topology-precondition requirement for the terminal read.

*Addendum authored 2026-07-16 by the working agent under R-016 §5; the ruling is the authority, this addendum is the pointer.*
