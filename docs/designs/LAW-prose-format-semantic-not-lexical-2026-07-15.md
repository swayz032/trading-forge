# MINTED LAW (operator, 2026-07-15) — prose-format structural guarantees are SEMANTIC cross-vendor

On PROSE-FORMAT artifacts (the certified reader's natural-language extractions), every STRUCTURAL guarantee
(merge-silencing / enumeration-consistency / any "is this object well-formed" axis) is a SEMANTIC CROSS-VENDOR
check (gpt-5.4 judges MEANING), NOT a mechanical/lexical diff. Reason, falsified twice tonight:
- Merge-silencing fence: 3 mechanical lints BLIND on prose (no per-condition direction/comparator) -> path-(d) semantic conflation check.
- Enumeration-consistency: lexical snake_case-key substring match PARAPHRASE-BYPASSABLE; the IyF catch was a key-format ACCIDENT (grader falsified it by demonstration). -> semantic check.
A mechanical diff works only when BOTH sides carry structure. Across a seam where one side is an internal key
vocabulary the extractor never sees and the other is prose, a lexical denylist is bypassable BY CONSTRUCTION.
MECHANICAL/LEXICAL checks are RESERVED for STRUCTURED formats — compiled specs, the H2 layer — where their signal
natively exists (the A-packet's real home). The next axis inherits this; no re-litigation.
