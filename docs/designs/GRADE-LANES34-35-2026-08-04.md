# GRADE — LANES 34 + 35 (AR-801) — INDEPENDENT

**Grader:** `accuracy-validator`, dispatched as grader. **I am not the doer and did not design, build, or previously grade Lane 34 or Lane 35.** No lineage to declare.
**PIN:** `81a48b7604b38e1a5daddfef0c6e478a7a3d4165` · **BASELINE:** `600ff606c7e6719281cb096ca066210a43b5589b`
**TREE (source of pin):** `C:/Users/tonio/Projects/wt-h1-wave4-20260712`, branch `h1-wave4-sealed12-driver`
**TREE (all execution):** isolated `git archive` extractions in my scratchpad — `…/94a4cb82-…/scratchpad/{base,pin,red,mut}`. **The shared worktree was never written except this receipt; `test_synthetic_market_simulator.py` was never touched.**
**Date:** 2026-08-04

## DISPOSITION: `PASS_WITH_BOUNDED_FINDINGS`

| System | Band | Status | Evidence | Open risks |
|---|---|---|---|---|
| **Lane 34** (`shadow_runner.py` taught-stop repair) | **8** | **VERIFIED** | 12-mutation battery (11 caught, 1 correct no-op); red-proof independently reproduced *and* extended 65× wider than the doer tested; every claim on ≥2 non-overlapping paths | F-2 (dead tuple member + false justification), F-3 (fixed-point sentinel collision — doer-disclosed, desk-owned) |
| **Lane 35** (tier-A census read) | **6** | **VERIFIED (as arithmetic)** | Three in-artifact recomputations that join exactly; instrument absence positive- *and* negative-controlled | F-1 (echo wider than disclosed), F-4 (`bind_status` single-source), artifact 7 days stale and not regenerated |
| **AR-801 §6** (safety/blast-radius account) | **4** | **REFUTED IN PART** | Four executable downstream hops read `passed`; the doer's absence claim used the wrong join key | **F-5 — the strongest finding in this receipt** |

**Baselines re-derived from current artifacts only.** I ignored all prior bands and all "fixed" claims.

---

## HEAD MOVEMENT DURING GRADE

HEAD was `93e201e1` at grade start — **already 2 commits past the pin** (`fe422c64` R-719, `93e201e1` AR-802). `git diff --stat 81a48b76 HEAD` = **`ADVISOR-RULINGS.md` + `AGENT-REPORTS.md` only, 2 files, docs-only.** Every blob I graded is byte-identical at pin and HEAD. **This receipt describes `81a48b76` exactly.**

---

## LANE 34 — CLAIMS C1–C7

### C1 — `getattr(sl,"value",1.8)` returned constants. **VERIFIED.**
- **Path A (static, MEASURED HERE):** `src/engine/config.py:349-352` — `StopConfig` declares exactly `type`, `multiplier`, `fixed_points`. **No `value`.** `model_fields` at runtime = `['fixed_points','multiplier','type']`.
- **Path B (dynamic, MEASURED HERE):** I called the **committed baseline function** across 7 taught multipliers × 3 legal types. Pre-repair returns `{1.8}` for stop and `{0.0}` for TP — **a constant on every row**. Post-repair returns the taught values exactly.
- **Sub-claim verified:** `StopConfig(type="atr", value=3.7)` **constructs**, yields `multiplier=2.0` (the default), and `getattr(s,"value",…)` is `<<ABSENT>>`. Pydantic silently drops it. The wrong field name is unfalsifiable at the call site, as claimed.

### C2 — `trailing_atr` never matched. **VERIFIED.**
Pre-repair tuple is `("atr_multiple","atr")`; the `type` Literal is `("atr","fixed","trailing_atr")`. **Path A:** the executable line excludes it. **Path B:** executed — pre-repair `trailing_atr` → `1.8` constant; post-repair → taught values. **Mutation M8** (drop `trailing_atr` from the repaired tuple) reddens `test_trailing_atr_stop_carries_the_taught_value`, so the claim has a live path to red.

### C3 — red-proof predicted 6, observed 6. **VERIFIED, AND STRENGTHENED.**
Built `$SP/red` = pin checkout + **baseline** `shadow_runner.py`. Plant blob-verified **both directions**: `shadow_runner` RED==base (`561e8dfa…`) and RED!=pin (`8d7f28cd…`); test file RED==pin (`1d6f3bec…`) — the instrument is the **shipped** one. Old code present, new code absent.

**Result: `6 failed, 29 passed` — the same 6 by name.** 29 = 27 pre-existing + 2 green pins, as claimed.

> **NOVEL EXTENSION neither report ran.** The doer's red-proof population was **one file (35 tests)**. I ran the **full canonical 97 (2270 tests)** against pre-repair code: `37 failed` vs `31` at pin. **Delta = exactly 6, exactly the predicted names, zero outside `test_shadow_runner.py`, and zero tests that pre-repair code made pass.** C3's "no unpredicted member" survives a population **65× wider** than the one it was asserted over.

### C4 — the class guard discriminates. **VERIFIED — AND IT IS STRONGER THAN CLAIMED. (T1)**
12 mutations, each verified to have **landed on disk** before its run (a mutation that silently fails to apply reads exactly like an uncaught one). Pristine copy green before and after; reverted clean.

| # | mutation | class guard red? | red set size |
|---|---|---|---|
| M1 | SL reads a **different** nonexistent field (`magnitude`) | **YES** | 4 |
| M2 | SL reads the **original** `value` (the regression) | **YES** | 4 |
| M3 | SL returns hard constant `2.5` | **YES** | 4 |
| M4 | SL returns the sentinel `1.8` | **YES** | 4 |
| M5 | SL **varies but is wrong** (`multiplier*2`) | **YES** | 4 |
| M6/M7 | TP nonexistent field / constant | **YES** (TP guard) | 3 |
| M8 | drop `trailing_atr` | no (correct — guard only uses `type="atr"`) | 1 |
| M9 | drop `atr` | **YES (both guards)** | 5 |
| M10 | drop the **dead** `atr_multiple` member | no — **0 red, correct no-op** | 0 |
| M11 | fixed-point returns taught value | no — **C7's sentinel pin reddens** | 1 |
| M12 | bypass the helper inside `_reconstruct_dsl` | no — **the consumer test reddens** | 1 |

**T1 answered: the guard is not vacuous.** C4's stated property ("varies, names no field") holds for the whole nonexistent-field class. **M5 shows the guard is strictly stronger than C4 claims** — `assert got == pytest.approx(taught)` also catches a *varying but wrong* output, which the "varies" wording alone would not.

### C5 — none of the 27 pre-existing tests could have caught it. **VERIFIED AT THE EXECUTABLE LINE. (T5)**
- Baseline `test_shadow_runner.py` collects **exactly 27 tests**.
- `grep "_extract_stop_multiple\|_extract_tp_multiple"` on the baseline test file → **exit 1, zero matches**, with a positive control (`_reconstruct_dsl` greps non-zero in the same file).
- `SimpleNamespace` appears **18×** — the idiom claim is measured, not asserted.
- **The mechanism itself, executed, not read from the docstring:** pre-repair, `SimpleNamespace(type="atr", value=m)` returns `[1.0, 1.8, 2.5, 3.7, 5.0, 7.25, 9.9]` — **the taught values. A SimpleNamespace fixture is GREEN on the broken code.** Post-repair the same fixture returns `{1.8}`. The fixture choice, not the assertions, is what makes the new tests bite. **C5 is a true mechanism claim and it is now measured.**

### C6 — `31/2234` vs `31/2226`, `+8` = the 8 new tests. **VERIFIED BY NAME. (T2)**
My independent runner (written by me, not reused from the doer) asserts manifest count == 97, resolves all paths, asserts non-empty, and drives pytest by subprocess with an explicit path list.

| | failed | passed | skipped | xfailed | **total** |
|---|---|---|---|---|---|
| doer @ pin | 31 | 2234 | 3 | 2 | **2270** |
| **me @ pin** | **31** | **2224** | **13** | **2** | **2270** |
| doer @ base | 31 | 2226 | 3 | 2 | **2262** |
| **me @ base** | **31** | **2216** | **13** | **2** | **2262** |

- **The 31 are IDENTICAL BY NODE ID at pin and baseline.** `only_at_pin = NONE`, `only_at_base = NONE`, 31/31 shared. **This is the join C6 needs and the doer explicitly did not perform.** Full node-id list captured; all 31 are pre-existing and unrelated to the parity engine (`test_a_plus_gate_parity` ×18, `test_accuracy_fixes` ×2, and 11 others).
- **`+8` joined by name:** collected-set diff = **8 added, 0 removed**, and all 8 are `TestTaughtStopAndTakeProfitReachTheDSL::*`. **Not an integer match — a named-set match.**
- **My absolute counts differ from the doer's by exactly 10, and it reconciles exactly** — see *Instrument disclosures* below. **Totals are identical (2270 / 2262) on both sides.**

### C7 — fixed-point sentinel deliberately not fixed, pinned by test. **VERIFIED.**
`test_fixed_point_stop_returns_documented_sentinel` exists, docstring reads `RECORDED, NOT ENDORSED`. **M11** proves the pin is live: making the fixed branch return the taught multiplier reddens it. The collision (sentinel `1.8` vs a genuine taught `1.8`) is real, correctly scoped as a separate defect, and correctly routed to the desk.

---

## LANE 35 — CLAIMS C8–C10

### C8 — `11 / 99 / 53 / 0 / 0`. **VERIFIED VIA THREE IN-ARTIFACT PATHS. (T3)**

| figure | Path A: summary fields | Path B: per-spec rows | Path C: per-condition rows |
|---|---|---|---|
| `len(specs)` | 11 | 11 | 11 |
| conditions | `n_conditions`=99 | `sum(n_conditions_total)`=**99** | `sum(len(conditions))`=**99** |
| spine | — | `sum(n_spine_or_trigger)`=**53** | `sum(load_bearing_spine)`=**53** |
| eligible strict | `n_eligible_strict`=0 | rows with `all_taught_binds` = **[]** | — |
| eligible spine | `n_eligible_spine_only`=0 | rows with `spine_fully_binds` = **[]** | — |

**All joins exact.** `spine_bind_status_counts` recorded `{UNBOUND:28, APPROXIMATED:25}`; my recount over the 53 spine conditions returns **the same**.

**T3 answered honestly.** The eligibility fields **are** a pure aggregation of per-condition `bind_status` (generator `:339-340` → `:398-399`), so they are **not** an independent judgment — the self-certifying shape is real. **But I recomputed the aggregation myself from the rawest field, and the margin is enormous:** only **5 of 99** conditions BIND at all (`UNBOUND 47 / APPROXIMATED 47 / BINDS 5`), and **0 of 53 spine conditions bind anywhere**. The nearest spec to strict eligibility is 5 conditions short; to spine eligibility, 3 short. **No plausible aggregation error changes the answer.** The residual is F-4.

### C9 — 0 of 11 name an instrument we hold; universe = MES/MNQ/MCL. **VERIFIED, TWO PATHS, BOTH CONTROLLED. (T4)**
- **Path A — token search, rebuilt by me rather than trusted:** case-sensitive word-boundary regex over each spec's full JSON. **Naive substring = 791 hits. Word-boundary case-sensitive = 0, across all 11.**
  - **Positive control:** identical regex on `"We trade MES and MNQ and MCL on the ES/NQ/CL data path."` → fires **6/6**.
  - **Negative control:** the exact trap shape — `"sometimes it becomes clean and closes"` → naive `{MES:2, ES:3, CL:2}`, word-boundary **NONE**. The trap is reproduced *and* defused.
- **Path B — committed classification fields, independent of any text search:** `asset_class` / `instrument_class_extracted` read verbatim: **3 forex, 1 index, 4 equities/stocks, 3 unclassified. Zero futures.**
- **Universe (MEASURED HERE, `config.py:130-142`):** `CONTRACT_SPECS = {MES, MNQ, MCL}` + `ES/NQ/CL` aliases mapping to the **same micros** ⇒ **3 distinct instruments.** Caption line numbers check out.

### C10 — the prior "~53" is the SPINE reading; strict denominator is 99. **VERIFIED.**
Both measured above. 53 = spine/trigger; 99 = all taught. The correction changes the denominator, not the conclusion — both readings return zero.

---

## FINDINGS

### F-1 — MEDIUM — C8's headline is an ECHO, and the echo is wider than the doer disclosed
**Claim:** *"THE COMMITTED CENSUS ALREADY CARRIES THE ANSWER IN ITS OWN FIELDS … it is the strongest single line in this report."* Disclosure given (A4): *"Lane 35's `st5e-YJRfKc__s0` **row** REPRODUCES the GRADE artifact — an ECHO. The other 10 **rows** are NEW measurement."*
**Reality:** the echo disclosure is scoped to **rows**. But `GRADE-GOLDEN-SLICE-ZERO-PASSABLE-ROWS-2026-08-03.md:315` — **dated one day before Lane 35 ran** — already states verbatim: `n_eligible_strict = 0` **and** `n_eligible_spine_only = 0`. **S5 holds under both readings.** And `:365` lists `census eligible_strict=[] / eligible_spine_only=[]` as a *path* in its own evidence table.
**Source of truth:** the prior GRADE artifact. **The two fields C8 rests on — and calls its strongest line — were already published as evidence in the artifact the doer names as its echo source.**
**Assessment:** the *number* is correct (I verified it three ways). What is overstated is its **novelty**. Lane 35 applied an existing field to a new question (*"is there a better candidate?"* vs *"does the golden slice pass?"*) — legitimate, and the answer required **no new measurement**. Per this desk's own law, a reading that reproduces a prior artifact's instrument is not a second path.
**Fix point:** AR-801 §4 — restate the echo boundary at the **eligibility-field** level, not only the row level.
**Repro:** `git show 81a48b76:docs/designs/GRADE-GOLDEN-SLICE-ZERO-PASSABLE-ROWS-2026-08-03.md | grep -n "n_eligible_strict\|eligible_spine_only"`

### F-2 — LOW — `"atr_multiple"` is dead code and its stated justification is a false mechanism claim
**Claim (code comment, `shadow_runner.py:157-160`):** *`"atr_multiple"` … is retained only for dict-shaped callers, since these helpers accept `Any`.*
**Reality (MEASURED HERE):** a **dict-shaped stop cannot reach that branch at all** — `getattr({...}, "type", "")` returns `""`, so a dict falls through to the sentinel **both pre- and post-repair**. And `_extract_stop_multiple` opens with `sl = config.stop_loss`, **direct attribute access**, so a dict-shaped *config* raises `AttributeError` before any of this. The retained member can only serve an **object** whose `.type == "atr_multiple"`, and `StopConfig.type` is a Literal that **cannot hold that value**.
**Witness:** **M10 — deleting `"atr_multiple"` produces 0 red across all 35 tests.** Dead by measurement, not by reading.
**Blast radius:** ZERO. The only production call site is `shadow_runner.py:84-85`, which passes a real `StrategyConfig` (positive-controlled enumeration: 9 references tree-wide, all accounted for).
**Fix point:** `src/engine/parity_engine/shadow_runner.py:157-161` — delete the member or correct the comment. Cosmetic; no behaviour change.

### F-3 — LOW — behaviour inversion on `value`-shaped fixtures (correct, but undocumented)
Post-repair, any object-shaped stop carrying `value` (the pre-repair-compatible shape) now returns the sentinel. **This is correct** — `value` was never a real field — and blast radius is zero in-tree. Recorded so a future reader does not mistake it for a regression.

### F-4 — MEDIUM — `bind_status` is single-source; I could not build a second path to it
Every Lane 35 figure ultimately rests on per-condition `bind_status`, computed by the compiler **at census-generation time (2026-07-28)** and never recomputed since. **Single-source truth = unverifiable** by this desk's own standard.
- The census blob `23f30eb0` is **byte-identical at baseline, pin, and HEAD** — **Lane 35 did not regenerate it**, correctly, since the lane was contracted read-only.
- Its `extraction_source` is a **scratchpad path in another session's temp directory** (`…/d96dba1d-…/scratchpad/SEALED-READ/phase_b`). It **does still exist today** (262 files; 13 in `phase_b`) — **but a temp directory is not a durable provenance root.** When it is reaped, the census becomes unreproducible.
**Recommendation:** if the `0/11` is to carry campaign weight, the extraction inputs need a durable home before that scratchpad is collected. **I did not regenerate the census** (see *What I did NOT verify*).

### F-5 — MEDIUM, AND THE STRONGEST FINDING HERE — AR-801 §6's safety account is refuted at the executable line; the doer measured the neighbouring object
**Claim (AR-801 §6):** *"the only consumer of `passed` is a `print(…, file=sys.stderr)` — no raise, no exit code, no promotion decision. THE CONCLUSION HOLDS."* and *"`[MEASURED HERE, positive-controlled grep]` NO consumer reads `result_extras.parity_shadow`."*
**Reality (MEASURED HERE, static read at the pin):** the absence claim is **literally true but joined on the wrong key.** The doer checked the **persisted JSONB column** (`result_extras.parity_shadow`). The real consumer reads the **in-memory result field one hop earlier**, plus a second stderr side-channel. `passed` drives **four downstream hops**:

```
_extract_stop_multiple -> _reconstruct_dsl -> run_parity_shadow -> passed
  |- backtest-service.ts:1291  parityFailed = parityShadow.ran===true && parityShadow.passed===false
  |- :1323                     severity = CRITICAL when parityCatastrophic (pnl_diff_pct > 1.0)
  |- :1349  insertAuditRow({ action:"backtest.parity_shadow_drift",
  |                          status:"failure", decisionAuthority:"system", correlationId })   <- AUDIT_LOG ROW
  |- :1368  logger.error(...)
  |- :1383  broadcastSSE("backtest:truthiness_failure", {..., correlationId})                 <- SSE
  |- :1396  notifyCritical("BACKTEST TRUTHINESS FAILURE", ...)  on CRITICAL                   <- DISCORD
```
The stderr path is also live, not a dead print: `python-runner.ts:239` parses the sentinel → `:428` emits `backtest.parity_shadow_drift_detected` → `:498` attaches `_truthiness_events` → **`backtest-service.ts:1245` reads it back and merges it into `result.parity_shadow`.**

**Why it matters:** the doer used §6 to bound the defect's severity, concluding *"a defect that only prints is a bad log line; a defect that persists is a future citation."* **Measured, it is neither — it is a system-authored `audit_log` row with `status:"failure"` and `decisionAuthority:"system"`, carrying a `correlationId`, plus an SSE broadcast and a family-facing Discord alert** (*"A backtest failed unexpectedly … Call Tony if backtests keep failing."*). Pre-repair, with the flag on, every strategy's parity comparison used a constant `1.8` stop and `0.0` TP — so any drift verdict would have **attributed engine disagreement to the wrong cause, in the audit trail, under system authority.**
**What survives:** the doer's narrow conclusion — **no raise, no exit code, no promotion gate** — I found none, and **`PARITY_SHADOW_ENABLED` defaults to `"false"` at both call sites** (`backtester.py:6158`, `:8553`, MEASURED HERE), so **none of this fires today.** This is a blast-radius correction, not a live incident.
**Fix point:** AR-801 §6 — the reach statement. **Correct it at the report, and re-run the absence grep against `result.parity_shadow`, not `result_extras.parity_shadow`.**
**Repro:** `sed -n '1228,1420p' src/engine/../src/server/services/backtest-service.ts` at the pin.

---

## ATTACK ITEMS A1–A4 — DISPOSITIONS

**A1 — the 52/43/4 taxonomy and `instrument_classification` strings `[RELAYED]`. BROKEN OPEN — in the doer's favour.**
The taxonomy is **not** subagent arithmetic. It is an **exact partition of a committed field** I re-derived here from `unbound_reason`:

| bucket | `unbound_reason` | count |
|---|---|---|
| bindable | `None` (= `bindable=True` = `executed=True` = APPROXIMATED 47 + BINDS 5) | **52** |
| untyped | `unknown_condition_type` (= all 43 `type=="UNTYPED"` rows) | **43** |
| session | `no_recognized_session_keyword` | **4** |
| | | **= 99 exactly** |

Likewise the per-spec classification strings are committed fields (`asset_class`, `instrument_class_extracted`), which I read verbatim. **The doer under-claimed: it labelled as `[RELAYED]` figures that are fully artifact-sourced and re-derivable.** Conservative mislabelling, not inflation.

**A2 — the 640-combination sweep. DISCHARGED.** AR-801 §7 states plainly: *"the 640-combination FLAG SWEEP was run **only** for `st5e-YJRfKc__s0` … My `0/99` is at DEFAULT FLAGS ONLY. ⇒ `"zero under all flag combinations"` IS NOT ESTABLISHED FOR THE OTHER 10 AND I DO NOT CLAIM IT."* **The report does say so, in the required direction.** Corroborated in the source artifact at `:246`/`:251`.

**A3 — is the subagent re-derivation real or a restatement? PARTIALLY ANSWERABLE, AND I SAY WHICH.** Every load-bearing figure (11/99/53/0/0) **is** independently derivable from the committed artifact — I derived all five via two-to-three in-artifact paths. So the figures are **correct and checkable**, which is the claim's truth value. **Whether the doer actually executed that derivation is not observable from the artifacts** and I do not certify it. What I can say: the figures are right, the stated source is the right source, and the doer's `[RELAYED]` flags are *over*-inclusive rather than under-inclusive (A1).

**A4 — the echo/new split. DISCHARGED AS STATED, BUT INCOMPLETE.** The prior GRADE artifact **does** explicitly disclaim the other 10 (`:394-396`: *"The claim 'other specs are exposed' is HYPOTHESIS, not measurement"*), so the doer's row-level split is accurate. **But see F-1** — the split is silent about the eligibility fields, which is where the actual conclusion lives.

---

## INSTRUMENT DISCLOSURES — BUGS IN MY OWN TOOLING

1. **I nearly published a fabricated finding.** My first look at the census `extraction_source` used `ls -la … | head -5`, saw 2 JSON files, and I was one step from reporting *"the extraction source is incomplete — only 2 of 11 specs remain, the census is unreproducible."* **`head -5` had truncated the listing.** Enumerating with `find` showed **13 files in `phase_b`, 262 under `SEALED-READ`** — fully intact. **A truncating pager biased me toward the absence answer, which is the same class as this desk's four crude-search failures.** Caught before publication by enumerating instead of sampling.
2. **Git-Bash → native-Windows path break.** A `/c/Users/...` path passed into `Program Files\Python313\python` resolved as `\c\Users\...` and raised `FileNotFoundError`. Caught immediately (it failed loudly rather than silently); re-run with `C:\`-form paths.
3. **My §6 trace was wrong on its first pass and I corrected it.** I grepped the **camelCase local** `truthinessEvents`, found consumers only in test files, and concluded *"no production consumer."* **The attached property is snake_case `_truthiness_events`,** and `backtest-service.ts:1245` reads it in production. **My first answer was a clean-looking absence that happened to agree with the doer's conclusion — the most dangerous kind.** F-5 exists because I re-grepped on the other casing.
4. **My isolation costs exactly 10 tests, and I reconciled it rather than reporting a discrepancy.** My `git archive` checkouts have **no `.git`** and **no untracked files**. That skips 8 tests in `test_spec_family_bindings.py:1916` (*"revision X unavailable: fatal: not a git repository"*) and 2 at `:901` (*"governed grade unavailable … blind-second-judge-LOCKED.json"* — untracked in the shared tree). **8 + 2 = 10**, matching my deficit exactly on **both** runs (`2234−10=2224`, `2226−10=2216`), with totals identical at 2270/2262. **Had I reported "the doer's 2234 is wrong," that would have been a manufactured finding.** The doer's absolute numbers are **corroborated**, not contradicted.

## A DESK QUESTION DISCHARGED ON THE WAY PAST

**R-719 recorded an open sha256 non-join:** *"AR-801's figures match neither the working tree nor HEAD, I could not reproduce them by LF or CRLF, and the isolated checkout it names is already deleted."*
**MEASURED HERE — the non-join does not exist.** Both AR-801 figures reproduce on **all three surfaces**:

| surface | `shadow_runner.py` | `test_shadow_runner.py` |
|---|---|---|
| `git cat-file blob` @ pin | `8d7f28cdbac14d51` | `1d6f3bec770b7fef` |
| shared worktree on disk | `8d7f28cdbac14d51` | `1d6f3bec770b7fef` |
| my `git archive` extraction | `8d7f28cdbac14d51` | `1d6f3bec770b7fef` |

Full digests: `8d7f28cdbac14d5122c17a24bac8dcad837eda588082fd2d927efe9516db52c2` · `1d6f3bec770b7fef4c55c75e165e32ec7b670ca2edc63cb03bba0407094f9ce7`. **Both files are pure LF (0 CRLF), so the LF/CRLF hypothesis was never the variable.** AR-801's hashes are of the committed blob content and they join. **The defect was in the checking instrument, not in AR-801.** Recommend R-719's question be closed.

## ALSO VERIFIED (not in the claim set)

- **§5.1 lint account is exactly right.** Under the hook's own flags (`--select=E,F,W,I --ignore=E501,E741,E722`): **baseline = 13 errors, "12 fixable" + 1 `F841` requiring a hidden/unsafe fix** — precisely the doer's *"12 auto-fixed; 1 (F841) fixed by hand."* **Pin = "All checks passed!"**
- **Gating is real:** `os.environ.get("PARITY_SHADOW_ENABLED","false").lower()=="true"` at `backtester.py:6158` and `:8553`. Default off.
- **The 2 excluded specs are genuinely excluded:** `dropped_not_clean = ["SeqVUNanFeY__s0","XbRI0sfcXU4__s0"]`, absent from `specs[]`. Named, not silently dropped, as the doer said.

---

## CLOSING COVERAGE

### 1. What I verified, and via which two-plus non-overlapping paths

| claim | path 1 | path 2 | path 3 |
|---|---|---|---|
| C1 no `value` field / constant output | executable decl `config.py:349-352` + `model_fields` | **executed** baseline fn, 7 mults × 3 types | pydantic keyword-drop probe |
| C2 `trailing_atr` unmatched | pre-repair tuple, executable line | **executed** pre/post comparison | mutation M8 → red |
| C3 red set = 6 | my own plant, blob-verified both ways → same 6 | **full-97 run**: 37 vs 31, delta = the same 6 | doer's account (RELAYED, corroborated) |
| C4 guard discriminates | 12-mutation battery, landing-verified | red-set-by-name per mutation | M5 shows it exceeds the claim |
| C5 27 tests could not catch it | collect-only = 27; grep exit 1 + positive control | **executed** SimpleNamespace-passes-broken-code | idiom count 18× |
| C6 31 unchanged, +8 | **node-id join**, pin vs base: 31/31 identical | collected-set diff: 8 added / 0 removed | totals reconcile 2270/2262 |
| C7 sentinel pinned | test exists, docstring read | mutation M11 → red | — |
| C8 11/99/53/0/0 | summary fields | per-spec row fields | **per-condition raw rows** |
| C9 0 of 11 name a held instrument | word-boundary case-sensitive search | **committed `asset_class` fields** | `CONTRACT_SPECS` executable lines |
| C10 53=spine, 99=strict | `n_spine_or_trigger` sum | `load_bearing_spine` recount | — |
| F-5 reach of `passed` | `backtest-service.ts:1245-1400` executable lines | `python-runner.ts:239/428/498` stderr path | `PARITY_SHADOW_ENABLED` gate read |

### 2. Positive-control witnesses for every absence claim I make

| absence claim | witness that the method can fire |
|---|---|
| "0 of 11 specs name MES/MNQ/MCL/ES/NQ/CL" | identical regex on control text → **6/6 fire**; and the trap string `"sometimes…clean…closes"` → naive `{MES:2,ES:3,CL:2}` vs word-boundary **NONE** |
| "no pre-existing test calls either extractor" | `_reconstruct_dsl` greps **non-zero in the same file** |
| "only 1 production call site" | same grep returns **9 references** tree-wide, all enumerated and accounted for |
| "no production consumer of `_truthiness_events`" | **RETRACTED — the control found one.** See instrument disclosure #3 |
| "census artifact present at pin" | `git ls-tree` control on `shadow_runner.py` returns a row in the same invocation |
| "prior GRADE artifact cites the eligibility fields" | control token `zzz_token_not_present` → **0**; real grep → 3 hits |
| "no failures outside `test_shadow_runner.py` under pre-repair code" | the same run **did** produce 6 failures inside it, and 31 elsewhere unchanged |

### 3. Join keys checked for every "identical / unchanged / matches" claim
- **The 31 failures:** joined on **full pytest node id** (`file::Class::test`), not on the integer 31. Set equality, both directions, 31/31.
- **The +8:** joined on **collected node id**, 8 added / 0 removed.
- **Blob identity pin↔HEAD:** `git diff --stat` = 2 docs files; every graded blob byte-identical.
- **Census identity:** blob **`23f30eb0`** at baseline, pin, **and** HEAD — same object, not same path.
- **Plant identity:** sha256 both directions (RED==base **and** RED!=pin; RED test file == PIN test file).
- **Census internal joins:** `sum(n_conditions_total)` ≡ `sum(len(conditions))` = 99; `sum(n_spine_or_trigger)` ≡ `sum(load_bearing_spine)` = 53; `spine_bind_status_counts` ≡ my recount.
- **F-5:** joined on the **field name** — `result.parity_shadow` (consumed) vs `result_extras.parity_shadow` (not consumed). **These are different keys and that distinction is the finding.**

### 4. What I did NOT verify
1. **I did not regenerate the census.** The generator exists (`tier_a_compile_census.py`, 697 lines) and its inputs still exist, but I did not re-run it. **`bind_status` therefore has no second path in this grade** (F-4). A regeneration would be the single highest-value follow-up for Lane 35.
2. **No `tsc`, no `vitest`, no TypeScript execution.** F-5's chain is a **static read of the pin's TS source**. I did not execute it, did not observe an `audit_log` row, an SSE frame, or a Discord message. The hops are `MEASURED HERE` **as executable lines**, not as observed runtime behaviour.
3. **runtime-production is UNMEASURED and out of scope.** I claim nothing about what is deployed, and nothing about whether `PARITY_SHADOW_ENABLED` is set anywhere outside this tree.
4. **The 31 inherited failures are not diagnosed.** I proved they are unchanged and unrelated; I did not root-cause any of them, and I did **not** join them to AR-794's 31 (a different artifact, a different population — the doer's own warning stands).
5. **The 640-combination flag sweep was not re-run** for any spec, including `st5e`. A2 is discharged on the report's *disclosure*, not on my re-measurement.
6. **The 2 excluded specs** (`SeqVUNanFeY__s0`, `XbRI0sfcXU4__s0`) were not evaluated — outside the committed population, as the doer said.
7. **No DB, no bars, no battery, no broker.** Whether we physically hold MES/MNQ/MCL data remains `[UNCOMMITTED]`; C9 is answered on the **naming** reading only, exactly as the doer scoped it.
8. **Whether the doer personally executed its re-derivation** (A3) is not observable from artifacts; I certify the figures, not the process.
9. **Uncertainty bound, stated as a bound:** across Lane 34 I ran **12 mutations, 11 caught**. That supports *"the guards catch the mutation classes I could construct"* — **not** *"no mutation escapes."* An unexplored mutation space remains; the residual escape rate is bounded only by the classes I enumerated, and I did not attempt semantic-equivalence or multi-site mutations.

---

## CLEANUP
~50 mutations and plants were applied **exclusively** inside `…/94a4cb82-…/scratchpad/{base,pin,red,mut}`. Every one was reverted within its harness and every harness verified green after revert. **The shared worktree carries ZERO plants.** The only file this grade writes to `wt-h1-wave4-20260712` is this receipt.

**Signed:** `accuracy-validator` — independent grader, no lineage to Lanes 34 or 35.
