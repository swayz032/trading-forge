# EXTERNAL READ — 2026-08-08 — AR-811 (Phase A closes) + AR-812 (Phase A frozen; only Phase B0 authorized)

> **PROVENANCE:** **OPERATOR-RELAYED CHAT**, 2026-08-08, two rulings in one relay, responding to
> `AR-811` and `AR-812`. **CONSUMED BY `R-725`** — banner written BY that ruling, IN its commit.
> ★ **TIMING JOIN:** the `AR-811` half says *"the mid-series control is still required"* — **`AR-812`
> had already run it** when this arrived, and the `AR-812` half then confirms it closes the objection.
> `R-725 §3` records the requirement as **DISCHARGED**, not outstanding.
> **A CHANNEL IS NOT AN AUTHOR** — audited on merit.

---

## PART 1 — AR-811 CLOSES PHASE A; PHASE B VERTICAL REPAIR IS AUTHORIZED

DECISION: ACCEPT AR-811 AS PHASE A WORKER COMPLETION. DO NOT CONTINUE INTO THE EIGHT UNTRACED ROWS.

Phase A's causal finding is now sufficiently measured:

> The source meaning survives extraction, but canonicalization compresses a time-bounded opening-range constructor into `WAIT_STRUCTURE`; that coarse type routes to a market-structure-event primitive independent of the taught opening range, while a potentially correct opening-range capability exists on a separate unreachable compilation surface.

`ENGINE_PRIMITIVE_WRONG_IDENTITY` is supported by the combined evidence:

- incompatible primitive identity;
- incompatible inputs and outputs;
- prose-insensitive dispatch;
- label-sensitive dispatch;
- deterministic output disagreement;
- non-gating runtime behavior;
- existing OR capability located elsewhere.

CLAIM-SCOPE AMENDMENT

The byte-identical mutation result is strong but must remain scoped because the mutated OR bar was positioned at the series edge.

Replace the broad statement "independence proved" with:

> The bound primitive was completely insensitive to a 24-point opening-range mutation in the registered edge-window fixture, while responding to its own structural inputs.

The mid-series control is still required in the permanent RED fixture. This limitation does not overturn `WRONG_IDENTITY`, because the implementation, signature, output domain, and non-gating dispatch independently establish the mismatch.

CERTIFICATION ORDER

AR-811 completes the worker's Phase A contract; it does not independently certify itself.

Start the independent grade now. While grading runs, perform the read-only Phase B bridge-eligibility check. Do not land the production repair until the grade reproduces the critical Phase A evidence.

If the grade fails a causal claim, stop and amend it before mutation.

PHASE B — EXACT VERTICAL REPAIR CONTRACT

The objective is not a general taxonomy rewrite.

The objective is:

> Make the real frozen artifact produce an exact opening-range state and use that state in one genuine source-defined runtime condition.

STEP 1 — FREEZE THE RED

Preserve a committed fixture proving the current path:

- assigns `WAIT_STRUCTURE`;
- selects `compute_structure_state`;
- does not compute OR high or low;
- ignores the registered OR mutation;
- cannot compute the half-range formula;
- does not drive the source stop or exit;
- nevertheless claims an executable or approximate binding.

Add the mid-series OR-window mutation and a positive control proving the primitive responds to genuine structural inputs.

STEP 2 — BRIDGE-ELIGIBILITY CHECK

Measure the existing `orh_{n}m`, `orl_{n}m`, and `or_range_{n}m` surface against:

- exact source start time;
- Eastern timezone;
- 5/15/30-minute durations;
- high/low aggregation;
- completion timing;
- daily reset;
- session-date behavior;
- missing-bar behavior;
- availability inside the production evaluator;
- deterministic agreement with an independent reference.

Return exactly one repair selection:

- `REUSE_EXACT`;
- `REUSE_WITH_TYPED_ADAPTER`;
- `REJECT_SEMANTIC_MISMATCH`.

Do not adopt topology examples such as `close >= orh_15m`; breakout construction and breakout confirmation remain separate.

STEP 3 — ADD THE SMALLEST PRECISE TYPE

Introduce `OPENING_RANGE_DEFINITION` or an equivalent explicit type containing only measured source semantics.

It must represent:

- start/end or duration;
- timezone;
- range high;
- range low;
- width;
- midpoint;
- daily recomputation;
- source-sanctioned duration variants.

Do not store the worked `0.52` result as a fixed parameter. Preserve the dynamic formula.

Any market-calendar or interval-boundary convention not taught by the source must be explicitly marked framework-owned.

STEP 4 — FAIL CLOSED ON THE OLD ROUTE

For opening-range prose:

- `WAIT_STRUCTURE → compute_structure_state` must no longer succeed;
- the old false `BINDS`/approximation result must disappear;
- missing required typed fields must produce a measured refusal;
- unrelated prose under a generated opening-range type must fail validation.

Do not broadly rewrite every `WAIT_STRUCTURE` condition.

STEP 5 — PROVE A REAL CONSUMER

Computing OR columns alone is not yet a taught executable binding.

Use the resulting state in the clearest measured source condition: the half-range stop.

Required source formula:

`midpoint = (OR high + OR low) / 2`

The real artifact must produce that rule. On deterministic position data, the source-defined stop must affect the source-only exit decision.

Keep framework-overlay risk separate:

- source-defined stop;
- framework-owned risk stop.

Neither may impersonate the other.

STEP 6 — MUTATION SUITE

The conformance test must fail when changing:

- opening-range start;
- duration;
- timezone;
- OR high;
- OR low;
- midpoint formula;
- trading-day reset;
- structural swing inputs;
- source-stop consumer wiring.

Expected identity controls:

- changing structural swings must not move the source midpoint;
- changing OR high or low must move it;
- removing the runtime consumer must fail exactness;
- using the old structure primitive must fail.

PHASE B PASS CONDITION

Phase B passes only when:

real frozen artifact
→ precise typed opening-range definition
→ production OR computation
→ dynamic midpoint
→ source-defined half-range stop
→ observable source-only exit decision

agrees with an independent deterministic reference.

A state producer may be labeled `SOURCE_STATE_COMPUTED`, but it must not count as `BINDS` until an exact source consumer uses it.

FINAL DIRECTION

No more horizontal diagnosis.

No more untraced-row census.

Independent grade and bridge eligibility may proceed together. After the grade passes, land the smallest exact vertical repair and deliver the first genuine source-derived runtime decision.

---

## PART 2 — AR-812 ACCEPTED; PHASE A IS FROZEN AND ONLY PHASE B0 IS NOW AUTHORIZED

DECISION: ACCEPT THE SCOPE CORRECTION AND MID-SERIES CONTROL.

The unauthorized fallback to the eight untraced rows is retracted. No further condition rows are authorized.

The mid-series experiment closes the specific edge-position objection. Its correct conclusion is:

> Semantic identity is disproven by counterexample: a correct opening-range evaluator must respond to every valid opening-range mutation, but the bound primitive ignored a valid 24-point mutation while remaining responsive to its own structural inputs.

Do not broaden this into a universal mathematical claim that the primitive can never correlate with an opening range. It may occasionally coincide when an OR bar also becomes a structural pivot. One decisive counterexample is sufficient to prove it is not the taught primitive.

PHASE A STATUS

Worker work: complete.

Independent certification: pending.

Frozen causal result:

`CANONICAL_TERM_UNRESOLVED`
→ opening range compressed into coarse `WAIT_STRUCTURE`

`ENGINE_PRIMITIVE_WRONG_IDENTITY`
→ market-structure-event evaluator selected

`EXISTING_CAPABILITY_UNREACHABLE`
→ OR columns exist on a separate compilation surface

No further Phase A expansion is permitted.

INDEPENDENT GRADE

Freeze and submit the Phase A evidence packet containing:

- transcript and extraction hashes;
- three completed rows;
- Probe A/B/C outputs;
- classifier assignment evidence;
- reachable-import evidence;
- edge and mid-series mutation fixtures;
- positive controls;
- exact commands and expected outputs.

The worker must not certify its own classification.

PHASE B0 — ONLY AUTHORIZED WORK

While the independent grade proceeds, run one bounded, read-only bridge-eligibility measurement against the existing:

- `orh_{n}m`;
- `orl_{n}m`;
- `or_range_{n}m`.

Produce one table with these rows:

- calculation formula;
- start-time source;
- timezone source;
- duration source;
- high/low inclusion boundaries;
- completion timing;
- trading-day reset;
- 5/15/30-minute support;
- missing-bar behavior;
- actual production caller;
- reachability from the frozen spec path;
- agreement with an independent deterministic reference;
- hidden defaults or hardcoded assumptions.

Required controls:

1. Run the existing OR producer on deterministic candles.
2. Independently calculate the expected OR high, low, and width.
3. Mutate the OR high and low.
4. Mutate duration.
5. Mutate the day boundary.
6. Prove the instrument can detect disagreement.

Return exactly one disposition:

- `REUSE_EXACT`
- `REUSE_WITH_TYPED_ADAPTER`
- `REJECT_SEMANTIC_MISMATCH`

STOP CONDITIONS

- Do not create the adapter.
- Do not edit production code.
- Do not route the frozen artifact yet.
- Do not adopt `close >= orh_*` or any other breakout confirmation.
- Do not inspect the remaining eight conditions.
- Do not widen into a census of every topology feature.
- If the OR producer cannot be invoked without mutation, stop at static compatibility and state precisely what remains unmeasured.
- If the independent grade rejects Phase A, stop Phase B0 and repair the evidence first.

NEXT RECEIPT REQUIRED

Report only:

1. Independent-grade status.
2. Existing OR-surface semantics.
3. Exact reachability boundary.
4. Deterministic comparison results.
5. One of the three bridge dispositions.
6. The smallest proposed Phase B1 patch boundary—but make no patch.

AR-812 correctly stops where its authority ends. The next task is now narrowly authorized: determine whether Trading Forge's existing opening-range machinery is exact enough to reuse before writing any new engine capability.
