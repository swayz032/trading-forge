# MEASUREMENT SPEC — dual-denominator DoD re-measure with §6a coverage

**STATUS: STAGED, BLOCKED on inputs.** R-143 §3 item 3. **Not dispatchable until the flip step
and the session resolver both land graded** — each moves the number this measures, and
measuring between them would produce a figure belonging to neither state.

**★ R-150 — DECLARED MEASUREMENT CONFIGURATION (standing form, no re-litigation per instance).**
R-076 §3's sentence again: *"the DoD is met" and "the wire ships" are different sentences.* This
re-measure runs with **BOTH level/zone flags explicitly ON**, and **that configuration is NAMED in
the artifact's scope-line, beside the dual denominators and the §6a coverage.** **Production
defaults stay OFF** until the flip-step's grade and the standing gates open them. **A measurement
of a hypothetical, labeled as such, is honest; an unlabeled one is the convicted class.**
**Blocked-on-inputs list gains a third input: the measurement flag configuration, declared.**

**This is a MEASUREMENT, not an instrument change.** It changes no binding. Its output is a
number other decisions trust — so it carries every reporting law this campaign has minted.

---

## 1. What it measures

The corpus binding-approximation rate, re-derived after:
- **the flip step** — `named_sr_level` (4) + `order_block_edge` (2) moving to
  `approximation=False`; **`swing` stays `True` (n=1, UNVERIFIED-BY-SAMPLE)**;
- **the session resolver** — up to **17** `WAIT_SESSION` conditions moving from unbound to
  **REAL, non-approximate** bindings (`session_windows`, `base_approximation=False`).

## 2. ★ MANDATORY REPORTING FORM — every clause was minted from a specific failure

1. **DUAL DENOMINATORS** (R-093 §3): **124 with-narration / 111 without-narration (PRIMARY).**
   **Both travel. The with-narration figure is never deleted.** The quarantine moved the rate
   optimistic; both numbers ride until the reclassification is separately graded.
2. **★ §6a COVERAGE — the third number** (R-082 §4): **bound-and-concrete ÷ ALL TAUGHT.**
   The approximation rate is computed over **executed-bindable** conditions only, so **a
   condition the compiler cannot bind at all VANISHES from the denominator** — meaning a spec
   could improve its score by becoming *less* bindable. **26 of 155 (16.8%) were vanishing.**
   **The unbound count travels beside the rate, always.**
2a. **★ THE TRIGGER-ROLE CONDITIONS ENTER THE COVERAGE ACCOUNTING (R-153 §4, ordered IMMEDIATE).**
   The dispatch loop iterates **`role == "spine"` only**, so **921 trigger-role conditions are never
   evaluated at all** — `WAIT_BIAS` 42 · `FILTER` 39 · `INVALIDATE` 105 · `ENABLE_ENTRY` 480 ·
   `ENTER` 255. **This is taught content the engine never tests.** It belongs in the §6a denominator
   the moment it is known, not after the enforcement build. **The unbound count grows; the honest
   number gets honester.** Reporting a coverage rate that silently excludes 921 untested conditions
   is the vanishing-denominator defect §6a exists to close.

3. **PER-KIND / PER-FAMILY ATTRIBUTION** — how much movement is `named_sr_level`, how much
   `order_block_edge`, how much session. **An aggregate delta hides which change earned what**,
   and this measure will be cited to justify further work.
4. **APPEND-ONLY** — `wire1-dod-HONEST-FLOOR.json` (0.9938 → 0.9793) is **untouched**; the new
   measure is a **new file**. **No certified artifact is re-baselined in place** — a dead or
   superseded number stays on the record, annotated.
5. **THE CEILING TRAVELS** — at most **6 of 16** level/zone rows de-approximated; **9 remain
   UNRESOLVABLE-AS-BUILT** (bare anaphora); **1 is `swing`, routed-but-approximate**; session
   recovers up to 17 of 27.
6. **NULL AND n on every rate** (R-100 §2, R-129 §1) — or the stated reason no baseline is
   defined (enumerated counts have none).
7. **A RATE INHERITS ITS WINDOW** (R-129 §1) — state the corpus, the spec count, and the
   condition count the rate is over. **A rate without its window has already misled once here
   by 50×.**

## 3. ★ WHAT IT MAY NOT DO

- **May not claim a fidelity result the grades did not license.** The flip's claim covers two
  kinds; the session resolver's covers its own graded population. **The re-measure reports
  what those grades earned — it does not extend them.**
- **May not report a single headline number** without its dual denominators and coverage.
- **May not be used to justify T1** until the DoD itself is graded (R-082 §4 amendment: **bars
  may never be set from the rate alone**; the unbound count must accompany it).

## 4. Verification

- The generator **ships with the artifact** (generator-with-artifact law) and **exits non-zero
  on failure**, so it gates rather than narrates.
- Recomputation from the committed inputs must be **reproducible** — the harness behind a
  headline number **commits**, no exemptions (R-138 §1, minted from an uncommitted script).
- **Independent grade before the number is cited anywhere.**
