# H1 Wave-3 — Budget-violation ruling + conformance-rebuild license (operator-ruled 2026-07-12)

## The violation (on the record, strict reading STANDS)
'design pass' was un-pinned in the Wave-3 brief (authorship gap, logged). Law 4 resolves un-pinned terms to the STRICTEST reading = the builder's own accounting: **two design passes spent, budget CLOSED.** `a52ebbdbc` (dispatched by the orchestrator under a pass-count misread) was therefore OVER-BUDGET and correctly HALTED before it produced a keepable number. It also overwrote the original (un-vaulted) module — original code lost from the working tree; frozen design-pool numbers safe (bias 0.875 / walkthrough 0.781 / routing 0.0).

## The license (does NOT conflict with the violation): a conformance rebuild is REQUIRED
The budget CANNOT convert a non-conformant instrument into an eval-able one by exhaustion. **routing = 0.0 across 112 items ≠ a low score — it means the delivered thing is NOT the specified instrument** (§1's cost-free decline is non-negotiable precisely because a force-determining tier-2 is a false-certificate machine). So the sealed 77 was NEVER legitimately readable on the original module either. Fix-instrument-then-re-run, all three legs pass:
- **Defect real:** 0/112 declines, decline path wired-but-dead (verified).
- **Fix is result-independent (the clean proof):** activating honest decline can only push absorption DOWN or hold it (a decline NEVER absorbs; it converts force-guesses — right AND wrong — into routed items). The conformance fix makes the bar HARDER, the opposite of goalpost-moving. The license is outcome-independent — same order if the numbers had been above the bar.
- **Frozen rule can still fail:** with honest declining, walkthrough's 78.1% likely falls FURTHER, toward the human-in-loop fork.
`a52ebbdbc`'s sin was BUNDLING the direction-safe conformance fix WITH 'lift the walkthrough class' (absorption-tuning, forbidden). The rebuild does ONLY the former.

## Gaming guard — the decline gate is STRUCTURAL, not tunable
Decline fires when the cross-reference binding resolver returns NO anchor — a deterministic existence check, ideally **PRE-CALL** (unlocatable binding → decline WITHOUT invoking gemma: cheaper AND drift-free). **NO confidence thresholds, NO tunable parameters** — a threshold chosen after seeing which items decline is absorption-tuning in a conformance costume. Independent reviewer ABLATES the gate and measures partitions (Wave-2 law), never reads its docstring.

## Recovery sequence (scope-locked)
1. **Fresh builder, one job:** original architecture + the structural (pre-call) decline gate + birth fixtures green (incl. W1-0147). **Any diff line that could RAISE a class's absorption is out of scope and VOIDS the rebuild.**
2. **Independent review:** diff-confinement + the ablation check.
3. **One design-pool CHARACTERIZATION run** — a measurement, NOT an iteration: no edits follow it. Purpose = knowing the conformant instrument's real numbers (routing now nonzero; absorption wherever honesty puts it) before the gate.
4. **The sealed 77, read ONCE** — controls first, per-class second, fresh grader. Both classes ≥85% with live decline → Wave 4; short → the **human-in-loop fork: pre-written, legitimate, COSTED** (~5-9 adjudications/video vs ≤1, from the shakedown's 92% consistency @ ~1.1K tok/item = a smaller, more expensive, still-certifiable library). H1 answered honestly either way.

## Two permanent process pins (extraction-campaign skill-amend candidates)
1. **VAULT-BEFORE-ITERATE:** any module a new agent is about to modify gets COMMITTED first (the battery vault rule extended to build artifacts). Tonight the lost original cost nothing ONLY because it was non-conformant anyway — luck, not protection.
2. **PASS-ACCOUNTING IS A TWO-PATH CHECK:** every iteration dispatch STATES which pass number it believes it is, and the builder CONFIRMS against its own count BEFORE touching code. Disagreement = the alarm, work stops. Tonight's violation lived entirely inside that unchecked assumption.
