# EXTERNAL READ — 2026-08-09 — FULL GITHUB GRADE READ; PHASE A PASSES; **B1 AUTHORIZED**

> **PROVENANCE:** **OPERATOR-RELAYED CHAT**, 2026-08-09, after the desk pushed the branch to
> `swayz032/trading-forge` (operator-decided; the repo is PUBLIC and the push published `631` commits).
> **CONSUMED BY `R-727`** — banner written BY that ruling, IN its commit, never in advance (`R-722 §1`).
>
> ⭐ **THIS IS THE FIRST READ IN THE CAMPAIGN MADE FROM THE PRIMARY ARTIFACT RATHER THAN A RELAY.** The
> reader inspected commit `d26f8615857d7f3b8a7dc2319ab27b29434d9be8` on branch
> `h1-wave4-sealed12-driver` directly and confirms `53,393` bytes, `F-1…F-10`, `C1…C10`, verdict
> `PASS WITH BOUNDED FINDINGS`, manifest `275/275`. ★★★ **It also confirms the desk's `R-726 §6` catch:
> *"My earlier missing-findings list was incomplete. F-5 and F-6 were also absent."***
>
> 🛑 **THIS READ IS THE OPERATIVE `B1` CONTRACT.** `R-727` adopts **STEPS 1–8 VERBATIM BY REFERENCE**
> and does **not** re-paraphrase them — paraphrase eroded this campaign's ladder twice
> (`advisor-onboarding §1a`). **Execute from THIS file, not from the ruling's summary of it.**
> **A CHANNEL IS NOT AN AUTHOR** — audited on merit; desk amendments are in `R-727`, not inlined here.

---

GPT EXTERNAL ADVISOR RULING — FULL GITHUB GRADE READ; PHASE A PASSES WITH BOUNDED FINDINGS AND B1 IS AUTHORIZED

REPOSITORY EVIDENCE VERIFIED DIRECTLY

Repository:
`swayz032/trading-forge`

Branch:
`h1-wave4-sealed12-driver`

Grade commit:
`d26f8615857d7f3b8a7dc2319ab27b29434d9be8`

Branch head inspected:
`ccf898a235156a88dd40c3af7657f546c1326afd`

Grade:
`docs/designs/GRADE-PHASE-A-ROWS-1-3-2026-08-08.md`

Verified:

- 53,393 bytes;
- all ten findings, F-1 through F-10;
- all ten graded claims, C1 through C10;
- verdict `PASS WITH BOUNDED FINDINGS`;
- manifest verification `275/275`;
- complete Phase B0 and AR-819 closeout.

My earlier missing-findings list was incomplete. F-5 and F-6 were also absent from the partial attachment.

DECISION

Phase A is accepted as mechanically sound with bounded findings.

`REUSE_WITH_TYPED_ADAPTER` is ratified.

B1 production repair is now authorized within the boundary below.

Do not perform more Phase A diagnosis, trace the remaining eight rows, rerun the disputed structure-engine experiment, or build a repository-wide primitive census.

WHAT THE FULL GRADE ESTABLISHES

- C1, C2, C3, C7, and C10 survive hostile independent reproduction.
- C4 and C5 reached substantially correct conclusions using evidence that overstated what it proved.
- C6 provenance is stronger than originally ruled because the freeze-time manifest exists and verifies.
- C8's execution-based "independence" proof is dead.
- C9 survives specifically as a name-binding gap, not module absence.
- The architectural conclusion survives:

> Opening-range meaning is preserved, classified into an overly coarse structure family, and routed to a primitive whose output schema contains no opening-range state.

ALL TEN FINDINGS ARE BINDING

F-1:
`approximation=False` is a family constant, not condition-level exactness evidence. Do not use it to certify B1.

F-2:
The original Probe B discharge used the wrong text key. Correct the durable citation.

F-3:
The 12-primitive literal census was structurally blind. Eighteen is a measured floor, not a proven repository-wide ceiling. No broad recount is authorized.

F-4:
The artifact is not universally prose-only. Typed `level` and variant fields exist. However, a scalar `level` cannot represent the dynamic midpoint formula by itself.

F-5:
`MANIFEST.sha256` exists and verifies all 275 entries. Replace the false "no manifest" limitation.

F-6:
`d73abe52` is the CRLF-repair commit, not the phase-B spec blob. Correct the citation while preserving the valid byte-identity join.

F-7:
The trace's closing limitations are stale. Correct the probe count, condition count, and traced `APPROXIMATED` count at their original locations.

F-8:
`n_taught_binds` applies no taught-condition filter. Do not count B1's state producer as a taught executable binding.

F-9:
Delete every claim that edge or mid-series execution proved independence. Do not build the permanent regression around that assertion.

F-10:
`indicators.core` is already loaded, but the OR function name is not bound into the spec compiler. State the problem as missing explicit routing/name binding—not module unreachability.

AR-819 RULING

The B0 closeout is accepted.

The six pre-lock observations are correct:

`09:30, 09:35, 09:40, 09:45, 09:50, 09:55`

for a 30-minute range using five-minute bars and `[09:30,10:00)`.

There is no off-by-one error. The 10:00 bar is excluded from formation and is the first reporting bar.

The partial-window defect is confirmed directly by the implementation:

- any nonempty subset is aggregated;
- only zero in-window bars produces null;
- no completeness validation exists.

In the measured fixture, removing bars narrows width from `1.05` to `0.80`.

Correct one statement in the durable report:

> A partial range moves thresholds inward and can cause earlier or additional breakout opportunities.

Do not claim it necessarily "makes the strategy trade more." Actual trade count depends on the unresolved breakout rule, later gates, price path, and execution model.

PHASE B1 CONTRACT

B1 has one objective:

> Make the real frozen five-minute opening-range artifact produce exact, typed, complete, timezone-correct opening-range state through the production compiler path.

B1 does not implement breakout confirmation, entries, stops, targets, futures portability, or profitability testing.

STEP 1 — EVIDENCE-CORRECTION COMMIT

Before production code, amend the existing trace at the original false claim locations.

Correct F-1 through F-10 where applicable. Do not merely append another correction section.

This must be a documentation-only commit with no new instrumentation.

STEP 2 — PERMANENT RED USING THE REAL ARTIFACT

The RED must start from the real frozen extraction artifact—not a manually constructed production binding.

Assert that current production cannot return:

- typed opening-range high;
- typed opening-range low;
- width;
- midpoint;
- completion state;
- complete-window status.

The current system must fail because it routes to `compute_structure_state`.

Do not assert that structural output must remain numerically unchanged.

Required path-to-red mutation:

> Deliberately route the repaired opening-range type back to `structure_engine.compute_structure_state`.

The conformance test must fail.

Include one responsive-position structure control if retaining any behavioral statement about that primitive, but field/output identity remains the load-bearing proof.

STEP 3 — PRECISE TYPED REPRESENTATION

Create `OPENING_RANGE_DEFINITION` or an equivalently explicit typed subtype.

Do not hardcode the video ID, strategy ID, artifact path, or strategy name.

Source-owned fields:

- local start time;
- duration;
- source timezone;
- source variant identity;
- provenance/evidence reference.

Framework/data-contract fields:

- input timestamp timezone;
- bar interval;
- interval convention;
- session-date convention;
- completeness policy.

Computed state:

- OR high;
- OR low;
- width;
- midpoint;
- lock/completion state;
- completeness result.

For the first B1 slice, compile the explicitly taught five-minute variant:

`09:30–09:35 America/New_York`

Preserve 15- and 30-minute variants as source-sanctioned alternatives. Do not silently select, discard, average, or optimize them.

Do not populate `level` with the worked `0.52`. The reusable rule is dynamic:

`width = OR high − OR low`
`midpoint = (OR high + OR low) / 2`

STEP 4 — TYPED ADAPTER

Prefer a strict adapter around the existing `compute_opening_range_breakout` rather than modifying its behavior for every existing caller.

The adapter must:

- bind the OR producer explicitly into the spec compiler;
- always pass start and duration explicitly;
- construct validated `ts_et` from timezone-aware input;
- use `America/New_York`, not a fixed UTC offset;
- never rely on the function's defaults;
- validate the complete timestamp grid before calling or accepting output;
- return a structured refusal when required inputs are absent;
- prevent the old opening-range-to-structure route.

If changing `compute_opening_range_breakout` globally becomes necessary, stop and report the exact reason and caller blast radius before editing it.

STEP 5 — COMPLETE-WINDOW CONTRACT

For fixed-interval bars, require exactly one observation at every expected timestamp in `[start, lock)`.

Fail closed on:

- missing prefix bar;
- missing interior bar;
- missing final formation bar;
- duplicate timestamp;
- unexpected interval;
- unknown or naive input timezone;
- duration not divisible by the declared bar interval;
- partial window at lock.

An incomplete session must emit:

`INCOMPLETE_OPENING_WINDOW`

It must produce no usable OR state and no dependent trading signal for that session.

A different complete day must remain usable and act as the positive control.

STEP 6 — PRODUCTION ROUTING

The opening-range condition must not use `_h_structure`.

If a new primitive identifier is added:

- register it in enforced dispatch;
- classify its handler as consuming supported typed parameters;
- refuse unknown or unused parameters;
- maintain load-time dispatch coverage.

If `FAMILY_META` or another Python/TypeScript mirrored surface changes, update the TypeScript mirror in the same commit and add a focused parity fixture. Do not leave another convention-only drift point.

Use explicit imports and routing. Do not add a dynamic-import shortcut merely because one exists elsewhere.

STEP 7 — REQUIRED TESTS

The suite must prove:

- actual artifact → typed definition;
- exact five-minute OR high/low/width/midpoint;
- null before lock;
- state available at lock;
- in-window mutation changes OR;
- after-lock mutation does not;
- second complete day remains correct;
- every incomplete-window pattern refuses;
- winter EST conversion;
- summer EDT conversion;
- daily reset;
- missing timezone refuses;
- missing duration refuses;
- wrong primitive plant fails;
- unrelated genuine structure conditions retain existing behavior;
- no hand-built production binding substitutes for artifact lowering.

STEP 8 — STATUS HONESTY

Successful range construction may report:

`SOURCE_STATE_COMPUTED`

It must not increment `n_taught_binds` or claim a complete executable strategy.

`approximation=False` is not acceptable proof of fidelity.

B1 passes through independent deterministic agreement and mutation controls, not a family boolean.

B1 PASS CONDITION

B1 passes only when:

real frozen artifact
→ typed five-minute OR definition
→ explicit production adapter
→ complete timezone-correct window
→ existing OR computation
→ exact high/low/width/midpoint
→ locked source state

agrees with an independent reference and every required mutation can turn the test RED.

EXPLICITLY OUT

- disputed structure-engine experiment rerun;
- remaining eight conditions;
- generalized WAIT_STRUCTURE rewrite;
- breakout touch/wick/close choice;
- half-range stop wiring;
- MES/MNQ/MCL portability;
- futures trading-day implementation;
- backtesting;
- profitability;
- full-library batch run.

NEXT AFTER B1

B2 will consume the exact midpoint as the source-defined half-range stop and prove an observable source-only exit decision.

FINAL POSITION

The complete GitHub evidence is now sufficient to lift the production-repair hold.

The fastest correct move is no longer another audit:

> Correct the durable evidence, then build the smallest typed adapter that makes the real frozen five-minute opening-range artifact produce exact, complete source state while refusing partial data and the old structural substitution.
