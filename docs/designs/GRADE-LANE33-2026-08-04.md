# GRADE — LANE 33 (R-715 §5) · INDEPENDENT · 2026-08-04

**Grader:** `accuracy-validator`, fresh instance. **I am not the doer**; I did not design, build, or
review Lane 33 before this grade.

## DISPOSITION: `PASS_WITH_BOUNDED_FINDINGS` · **VERIFIED band 7**

| System | Band | Status | Evidence | Open risks |
|---|---|---|---|---|
| Lane 33 — bare-relative repair + manifest re-pin + fixture | **7** | **VERIFIED** | 3 non-overlapping paths agree on the 97 and on the same `+2/−0`; 9 mutations + 7 plants; runtime reconciliation over all 338 test files | `F-1` level arithmetic has **no path to red**; `F-2` tail-join over-inclusion reaches the new branch; `F-3` dynamic imports statically invisible |

**CLAIMED band:** the doer (`AR-799`) claims no band; it claims delivery + red-proof and explicitly
disclaims Gate 3. **No claim inflation to reconcile** — the report is unusually self-limiting and
volunteers its own weakest link (see §"Doer claims graded").

### Pins — and the head moved twice DURING this grade
- **Graded commit:** `1163f36657773fef4dec52daa09c2207cf85b839` (2026-08-04T14:44:32-04:00)
- **Baseline:** `a3f75aa7efff54b3d555ea660dda51e7fa3ce50e`
- **Tree:** `C:/Users/tonio/Projects/wt-h1-wave4-20260712`, branch `h1-wave4-sealed12-driver`
  (`rev-parse --git-common-dir` = `C:/Users/tonio/Projects/trading-forge/trading-forge/.git` → a
  **linked worktree**, not a standalone repo).
- HEAD was `3ad530e3` at grade start and `08079e0c` at grade end — **4 commits past the pin, ALL
  docs-only** (`ADVISOR-RULINGS.md`, `ADVISOR-STATE.md`, two handover docs).
  **Both graded blobs are byte-identical at pin and at HEAD** `[MEASURED HERE]`:
  `test_flag_off_parameterized_refusal.py` = `83f7d6653537fc0d`,
  `canonical_regression_population.txt` = `8852cff1c179958e`. **This verdict describes both.**

---

## THE TEN MANDATORY ITEMS

| # | Item | Verdict | How (paths) |
|---|---|---|---|
| 1 | Both files genuinely belong | **PASS** | independent tracer + runtime `sys.modules` + instrument |
| 2 | Included for the CORRECT dependency reason | **PASS** | traced chain; cutting bare-relative edges loses exactly these 2 |
| 3 | Runtime loading actually reaches them | **PASS** | fresh-subprocess import; 6 negative controls |
| 4 | Diff is exactly `+2 / −0` | **PASS** | member-set diff of the two committed blobs |
| 5 | Removing either → a permanent test FAILS | **PASS** | M3, M3b |
| 6 | Adding an unrelated file → FAILS | **PASS** | M4 (manifest) **and** real planted files (tree) |
| 7 | Deterministic from a clean checkout | **PASS** | 3 in-process + 3 separate processes + 2nd archive |
| 8 | cwd does not alter the result | **PASS** | 6 real `chdir` roots incl. `C:\WINDOWS`, `$HOME` |
| 9 | Static and runtime methods genuinely independent | **PASS** | **P3/P4 make them disagree in OPPOSITE directions** |
| 10 | No shared blind spot can make both agree wrongly | **PASS, bounded** | see below — the shared surface is enumerated, not assumed |

### Item 10 — attacked directly (the sharpest item)
Convergence is evidence **only if the two methods cannot fail the same way.** I built a
discriminating fixture that forces them apart `[MEASURED HERE]`:

| Plant | Static (instrument) | Static (my tracer) | Runtime | |
|---|---|---|---|---|
| P1 `from . import X` only route | True | True | True | agree |
| P2 `from .. import X` only route | True | True | True | agree |
| **P3 DYNAMIC import only** | **False** | **False** | **True** | ***DISAGREE*** |
| **P4 import present, NEVER EXECUTED** | **True** | **True** | **False** | ***DISAGREE*** |

They fail in **opposite directions** on constructed input. They are not one instrument in two hats.

**What they DO share, enumerated (not hand-waved):**
1. **The target list** `("spec_condition_compiler", "spec_family_bindings")` — a shared *definition*.
   If the intended population is defined differently, both agree wrongly. Not a blind spot; a premise.
2. **Name matching.** Both target basenames are **unique in the tree** (1 file each, of 628 modules)
   `[MEASURED HERE]`, so a tail-name join and a full-dotted-key runtime check **cannot diverge on the
   target**. My runtime probe computed exact-tail AND substring matches and they were identical for
   every one of 338 modules — the substring/hex-collision defect that bit this desk earlier today does
   not reach here.
3. **The `"tests" in path.parts` membership filter.** Shared by both static paths; the runtime path
   does not use paths at all (it joins on module name), so the path→module mapping is the join key.
4. Tree-wide ambiguous basenames: **only 3** (`__init__`×30, `core`×2, `liquidity`×2), none a target.

---

## FINDINGS

### Discrepancy F-1: the `node.level` package arithmetic has NO PATH TO RED — and the fixture's stated proof of it is refuted
**Severity: MEDIUM** (unfalsifiable guard sub-component; caption-is-a-claim)
**Claim** (docstring of `test_the_population_derivation_sees_bare_relative_imports`, echoed in `AR-799 §3`):
> "`level=1` is the form that actually occurs in the tree (all 8 occurrences), `level=2` **proves the
> package arithmetic rather than assuming it**."

**Reality:** it does not. Four separate mutations to that arithmetic leave **all 25 tests green**
`[MEASURED HERE]`:

| mutation of `pkg` / `keep` / `base` | red |
|---|---|
| `keep = len(pkg)` (ignore `node.level` entirely) | **0** |
| `base = "ZZZ_NONSENSE"` | **0** |
| `keep = 0` (base always empty) | **0** |
| `keep = len(pkg) - node.level` (off-by-one) | **0** |

And six variants — including **deleting the arithmetic outright** and writing `deps.add(a.name)` —
produce the **identical 97-member population**.

**Mechanism, read at the executable line** (`test_flag_off_parameterized_refusal.py:534-535`):
```python
for other, tail in tails.items():
    if tail == parts[-1] and other != mod and reaches(other, seen):
```
The join uses only `parts[-1]`. For `from . import X`, `a.name` is always a **bare** name, so
`parts[-1] == a.name` **regardless of `base`**. The only other consumer is
`any(p in targets for p in parts)`, and no *package* in the tree is named after a target. Therefore
`pkg`, `keep` and `base` **cannot change the result**. The arithmetic is **inert by construction**,
not merely inert on this tree.

**What this does NOT undermine:** the repair's essential content — *stop skipping bare relative
imports* — **is** covered. Reverting the whole branch reddens **two** permanent tests (see below).
**Source of truth:** the mutation results; the docstring sentence is wrong.
**Fix point:** `src/engine/tests/test_flag_off_parameterized_refusal.py:534` (make the join use the
resolved name) *or* correct the docstring to claim only what is proven.
**Repro:** replace `base = ".".join(pkg[:keep])` with `base = "ZZZ_NONSENSE"`; run the file → 25 passed.
**Blast radius:** none today. **Latent:** the prior grade's F-2 recommends repairing the tail-name
join. The moment that happens the arithmetic becomes load-bearing **with no test protecting it.**

### Discrepancy F-2: the new branch computes the correct fully-qualified name and then DISCARDS it — same-basename over-inclusion now reaches through Lane 33's code path
**Severity: MEDIUM** (footgun, not an uncovered hole — the manifest *does* redden)
**Reality** `[MEASURED HERE, plant P7]`: a file whose only import is `from . import g_amb`, resolving
to an **innocent** `engine/tests/g_amb.py` that imports nothing, is **admitted to the population**
because an unrelated `engine/other_g/g_amb.py` reaches a target. The new branch correctly computes
`base='engine.tests'` → `dep='engine.tests.g_amb'`, and `reaches()` then throws that away and joins on
`'g_amb'`.
**Sources compared:** instrument = **member** | my proper-resolution tracer = **not a member** | intended = not a member.
**Source of truth:** the independent tracer. Same root cause as F-1.
**Fix point:** same line, `:534`.
**Blast radius: ZERO MEASURED on this tree** — 3 ambiguous basenames, none a target. The manifest
comparison reddens on it, so it cannot land silently; a **blind regeneration** would embalm it, which
is exactly what the new manifest header now forbids.

### Discrepancy F-3: dynamic imports are invisible to the derivation
**Severity: LOW** (known static limitation; bounded here)
`importlib.import_module('src.engine.spec_family_bindings')` as the only route → **both** static
instruments miss it, runtime catches it (plant P3).
**Blast radius, measured:** census of `src/` = 6 `import_module` + 8 `__import__` calls across 11
files, 8 of them test files. Of those, `test_spec_producer.py` dynamically imports a target **and is
already a member**; the rest name no target. **Under-inclusion across all 338 test files at
module-import time = 0.** Residual: `test_ict_bias_aligned_continuation.py` carries a **non-literal**
dynamic import argument → **statically unenumerable**; it is not a member and is runtime-clean at
import; its execution-time behaviour is **UNENUMERATED**.

### Discrepancy F-4: dispatch line-count captions do not match the artifact
**Severity: LOW** (desk caption, not an artifact defect)
Dispatch says `canonical_regression_population.txt (+14/−5)` and the test file `(+101)`. Measured
`git diff --numstat`: **`+12/−2`** and **`+98/−3`** (101 lines *changed*). The load-bearing figure —
**member diff `+2/−0`** — is exactly right.

---

## THE CENTRAL CLAIM: IS THE SELF-CERTIFICATION ACTUALLY BROKEN? — **YES**

This was the reason Gate 3 was withheld: the manifest was minted by the same blind rule that checked
it, so the comparison was green *by construction* for this class. **Revert the repair** (restore
`elif isinstance(node, ast.ImportFrom) and node.module:`) → `[MEASURED HERE]`

```
2 failed, 23 passed
RED  test_the_population_derivation_sees_bare_relative_imports
RED  test_the_canonical_population_matches_its_committed_manifest_by_member
```

**Both** halves redden: the new fixture *and* the manifest pin (derivation falls to 95, manifest pins
97). The class is now detectable by a permanent test, from two directions. This also answers the
dispatch's CRITICAL question — **the fixture would NOT pass under the old rule.**

### Mutation / plant battery — 9 mutations, 7 plants

| # | mutation | red | expected |
|---|---|---|---|
| M1 | revert the `node.level` repair | **2** | yes |
| M2/2b/2c/2d | break the level *arithmetic* 4 ways | **0** | **→ F-1** |
| M3 / M3b | drop either Lane-33 member from the manifest | **1** each | yes |
| M4 | add an unrelated real non-member | **1** | yes |
| M5 | reorder two members | **1** | yes |
| P1/P2 | plant files reachable only via `from . import` / `from .. import` | picked up; manifest reddens | yes |
| P5/P6 | `from .hop import X` / `from ..hop import X` (doer's disclosed out-of-scope case) | resolved correctly | — |
| P7 | bare-relative to an innocent same-basename sibling | falsely admitted | **→ F-2** |

**Plant P2 answers the dispatch directly: a nested `from .. import X` IS picked up — not a new hole.**
**P5/P6 show the doer's disclosed "lossy `from .sub import X`" is inert on this tree** (saved by the
same tail-join that causes F-2).

---

## TWO-PATH RECONCILIATION AT POPULATION SCALE

| path | n | notes |
|---|---|---|
| A committed manifest | **97** | artifact |
| B instrument `_regression_population` @pin | **97** | member set **and order** identical to A |
| C my independent tracer (proper module resolution, **no tail-name join**) | **97** | member set identical to A and B |
| D my tracer with **bare-relative edges CUT** | **95** | loses **exactly** the two files |

D independently reproduces the pre-repair 95 **and** the exact `+2` delta, from a different
resolution algorithm. The traced chain (my tracer, matching `AR-799 §4` exactly):
```
test_extractor_bridge / test_wave6_pass2_orchestration
   --ABSOLUTE-->       engine.extraction.extractor_bridge
   --BARE-RELATIVE-->  engine.extraction.pilot_conveyor
   --BARE-RELATIVE-->  engine.extraction.compile_lints
   --ABSOLUTE-->       engine.spec_family_bindings          [TARGET]
```
**Two bare hops — which is precisely why they were invisible.**

**Runtime reconciliation over ALL 338 test files** (fresh subprocess each, 120 s cap):
- cleanly probed **338/338**, zero timeouts, zero errors
- **UNDER-INCLUSION (runtime YES, manifest NO) = 0** ← the number that matters
- 40/97 members confirmed loading a target at module-import time
- 57 members not confirmed at import time — **and this is an artifact of MY probe, measured not
  assumed: 57/57 have a deferred (in-function) import on the shortest chain** (e.g.
  `engine.backtester --> engine.spec_condition_compiler` is inside a function). My probe imports the
  module; it does not run the tests. **This is not a manifest defect and I do not report it as one.**

---

## DOER CLAIMS GRADED (`AR-799`)

| claim | verdict |
|---|---|
| "8 bare relative imports, ALL level=1, ALL in `engine/extraction`" | **CORROBORATED** — my own AST walk: exactly 8, all level=1, all in `engine/extraction` |
| `95 → 97`, `+2/−0`, the two named files | **MEASURED HERE** — exact |
| the two-bare-hop chain | **MEASURED HERE** — my tracer reproduces it edge-for-edge |
| "revert the fix → exactly 2 red, no unpredicted member" | **MEASURED HERE** — 2 red, the same two |
| `sha256(spec_condition_compiler.py)` = `621302a56987f19b`, unchanged | **MEASURED HERE** — identical at `a3f75aa7` and `1163f366` (differs at `9484c161`, pre-Lane-32, as expected) |
| "Edited file 25 passed" | **MEASURED HERE** — 25 passed |
| "+22 = 21 from the two added files + 1 from the new fixture" | **MEASURED HERE, first principles** — the two files collect and pass **21**; the new fixture is **1**; 21+1 = 22 ✓ |
| "`level=2` proves the package arithmetic" | **REFUTED → F-1** |
| "my predictor and the real function are both static AST and SHARE physics; the genuinely independent leg is the grader's runtime path, and it is not mine" | **TRUE, and verified independently.** The doer names its own weakest link correctly. Its two instruments are *not* mutually independent; the runtime leg is, and I re-ran it myself rather than inherit it |

---

## KNOWN-UNMEASURED DECLARATIONS — checked, not re-reported as novel

| declaration | status |
|---|---|
| no `tsc`/`vitest` — NOT a TypeScript pass | **TRUE, and I did not run them either.** This grade says nothing about the TS contract |
| `runtime-production` tree unmeasured | **TRUE** — I measured only `wt-h1-wave4-20260712` |
| `TF_FAMILY_META_ENFORCED` OFF | **not independently verified by me** |
| `test_cloud_backend.py` HUNG, and not a member | **member claim CONFIRMED FALSE-FREE: it is NOT in the 97** `[MEASURED HERE]`. Nuance: it **imports** cleanly in 
my probe (2121 modules, no timeout), so the hang is an **execution-time** property, which I did not measure |
| 31 inherited scoped-regression failures undiagnosed | **TRUE — I did not diagnose them and did not re-run the scoped regression** |

---

# MANDATORY COVERAGE SECTION

### 1. What I verified, and via which non-overlapping paths
- **Population membership** — 3 paths: committed manifest (artifact) · the shipped instrument
  (tail-name join) · **my own tracer with proper module resolution**, written from scratch. Plus a
  4th, the **runtime `sys.modules`** probe, reconciled over all 338 test files.
- **Dependency reason** — static trace + an edge-cut necessity control (cut bare-relative → lose
  exactly those two).
- **Red-proof** — mutation of the artifact in an isolated checkout, observing pytest.
- **Doer arithmetic** — recomputed from first principles by collecting the tests.

### 2. Positive-control witnesses for every absence claim
- *"the two files are not incidental"* → **cut-edge control**: removing bare-relative edges drops
  exactly those 2 and nothing else (97→95).
- *"the derivation discriminates"* → the shipped 0-hop bystander control, plus my P4/P7 plants.
- *"runtime doesn't load a target in non-members"* → **6 negative controls** loading 207–2257 modules
  each, all target-free; and a **positive control** (`test_flag_off_parameterized_refusal`) loading both.
- *"the arithmetic is inert"* → 6 variants **including total deletion**, all giving the identical 97.
- *"no under-inclusion"* → the probe demonstrably **can** report YES (P1/P2/P3 and 40 real members).
- *"the 57 over-inclusions are my probe's artifact"* → **57/57 measured** to carry a deferred import.
- *"the dispatch's `+14/−5` is wrong"* → `git diff --numstat` on the same blob pair.

### 3. Join keys checked for every "identical / unchanged / matches" claim
- **commit SHA** `1163f366...` for every artifact statement; **blob sha256** for the two graded files
  at pin **and** at a moving HEAD (identical).
- **member path string** `engine/tests/<file>.py` for all population comparisons — plus **order**,
  compared as an ordered list.
- **module dotted name** for the static↔runtime join (`engine/tests/x.py` ↔ `src.engine.tests.x`).
- **`sha256(spec_condition_compiler.py)`** across `9484c161` / `a3f75aa7` / `1163f366`.
- **Line endings measured in Python bytes, per file** — the shared worktree's manifest is **CRLF on
  disk** (120 CRLF) while the blob is LF; `git status` calls it clean because the `text` attribute
  normalises on compare. Content is identical after normalisation, and I confirmed the suite passes
  **25/25 against a genuinely CRLF manifest**.

### 4. Where each plant physically lived — 🛑 THE SHARED TREE CARRIES ZERO
Every mutation and plant lived in **`git archive`-backed checkouts under my scratchpad**, never in the
campaign worktree:
`…\scratchpad\pin` (mutations M1–M5) · `…\scratchpad\plant` (P1–P4) · `…\scratchpad\plant2` (P5–P7) ·
`…\scratchpad\det2` (determinism + CRLF).
**Final check `git status --porcelain src/` in the shared worktree returns exactly one line —
`test_synthetic_market_simulator.py`, the other seat's, which I never touched.** The desk's
pre-commit-stash hazard therefore had nothing of mine to corrupt.
The only file I wrote into the shared tree is **this receipt**, uncommitted, as instructed.

### 5. Disclosures against myself
- **My first independent tracer returned 95 and EXCLUDED both files under grade** — a clean-looking
  refutation. It was **my bug**: I indexed `pkg/__init__.py` as `pkg.__init__` and never as `pkg`, so
  `from src.engine.extraction import …` resolved to nothing. Caught by a positive control before
  publication. **A broken instrument reads exactly like a finding, and it landed on the pre-repair
  number 95, which would have made the fabrication look corroborated.**
- **My CRLF counter printed `CRLF=0 LF=0` on a file that was 100% CRLF** — f-string escaping
  (`b'\\r\\n'` matches a literal backslash sequence). Re-measured in raw bytes before any claim.
- **My mutation harness restored via `read_text`/`write_text`, which on Windows rewrote my scratch
  checkout LF→CRLF.** Verified **content-identical after normalisation**, and a second independent
  archive (`det2`) reproduced 25/25 green, so no verdict depends on it.

### 6. 🛑 WHAT I DID **NOT** VERIFY
1. **No `tsc`, no `vitest`, no TypeScript anything.** This is **NOT** a pass on the TS contract.
2. **The scoped 97-file regression was not re-run.** The doer's `31 failed / 2226 passed` is
   **RELAYED**; I verified only its `+22` arithmetic from first principles. **The 31 inherited
   failures are undiagnosed and I did not join them by name to AR-794's 31.**
3. **Test-EXECUTION-time runtime behaviour.** My runtime path is a **module-import** probe. Imports
   that only execute when a test runs are invisible to it — that is precisely the P4 shape and it
   explains all 57 over-inclusions. A pytest-execution-time `sys.modules` probe is the missing 3rd
   leg and I did not build it.
4. **`test_cloud_backend.py`'s hang** — I measured it imports cleanly; I never ran it.
5. **`TF_FAMILY_META_ENFORCED` and other env-gated surfaces** — untouched, unenumerated.
6. **The `runtime-production` tree and every other worktree.** Law 10: this verdict names exactly one
   tree, `wt-h1-wave4-20260712`. I ran **no** cross-tree filesystem sweep.
7. **Whether the target list `("spec_condition_compiler", "spec_family_bindings")` is the RIGHT
   population definition.** Both methods take it as a premise; I graded consistency, not correctness,
   of that choice.
8. **Non-literal dynamic imports** (`test_ict_bias_aligned_continuation.py`) — statically
   unenumerable; execution-time behaviour unmeasured.
9. **Anything about Gate 3 or Phase-1 exit.** Not asked, not measured, not implied by this band.

---

## BOTTOM LINE FOR THE INCOMING ADVISOR

Lane 33 does what it says. The two files are **real members**, added for the **right reason**
(two bare relative hops), found by **three methods that I proved can disagree**, with **zero
under-inclusion** anywhere in the 338-file test corpus. The self-certification **is** broken:
reverting the repair reddens two permanent tests, including the manifest pin.

The two MEDIUM findings share one root cause — **`reaches()` joins on the last path segment
(`:534`)** — which makes the new branch's careful `node.level` arithmetic both **unprotected by any
test** (F-1) and **over-inclusive on basename collisions** (F-2). Neither bites on the tree today
(measured: zero blast radius, both target basenames unique, only 3 ambiguous basenames tree-wide).
**Both would become live the moment someone repairs the tail-name join** — so F-1 and F-2 should be
closed *in the same wave* as any fix to `:534`, never before and never separately.

**Band 7, not 8:** part of the shipped code — the level arithmetic — has **no path to red**, and a
guard that cannot fail is not a guard.
**Band 7, not 6:** every disqualifying question was attacked as a real mutation, at population scale,
through genuinely independent instruments.
