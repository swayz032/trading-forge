# INDEPENDENT GRADE — `c304b098` (LEDGER-E oracle contract)

**Grader:** accuracy-validator (independent; did not design, build, or previously grade this object or any of its three rejected predecessors)
**Date:** 2026-07-31
**Object:** `c304b098b156106a5a81b714c7a5a3ed166d68ef`
**Branch:** `hardening/ledger-e-delivery-r497-20260730`
**Worktree:** `C:/Users/tonio/Projects/wt-ledger-e-delivery-r497-20260730`
**Parent verified:** `git rev-list --count 9af37b8f..c304b098` → `1` [MEASURED HERE]. `git rev-parse c304b098^` → `9af37b8ff36a13c05fb0ec26752c42a97fc300d7` [MEASURED HERE].

---

## VERDICT: **NOT-SOUND**

The delivery **does** close the two defects it was rejected for on `8187b730` — both are measured closed here, by execution, against a clean control. But the *same defect class* is still live one level down: an oracle **row's expectation** can be silently deleted by a typo'd key or a wrong-typed value, leaving the gate's output **byte-identical (identical md5) and exiting 0**. That is the identical phrasing this desk used to condemn `8187b730`, reproduced against `c304b098`.

Additionally, the "caption falsifies its own line" pattern is present for the **ninth** time, in this delivery's own new code, at the very line whose comment documents the eighth.

### Per-sub-claim verdicts

| # | Sub-claim | Verdict |
|---|---|---|
| 1 | Deleting a fully-adjudicated oracle row makes the gate FAIL and NAME it | **SOUND** |
| 2 | Stripping a row's `authority` makes the gate FAIL and NAME it | **SOUND** |
| 3 | The oracle contract is validated at RUNTIME | **PARTIALLY SOUND** — it does run at runtime; its own caption "the WHOLE contract" is falsified by F-2 |
| 4 | Row census asserts MEMBERSHIP, both directions, may grow but not shrink | **PARTIALLY SOUND** — true for `conditions`; **false** for `reasons_must_differ_from` (F-3) |
| 5 | `conditions_unadjudicated_ids` adjudicated XOR unadjudicated, population independence | **SOUND** |
| 6 | The DECLARED `invalidations` exclusion is stated IDENTICALLY in code and ratify packet | **NOT-SOUND** — stated in code, stated nowhere in the packet |

**Band: 6/10** (implemented, adversarially tested, real repairs measured closed — but one HIGH false-green of the same class the delivery exists to close remains open). CLAIMED band was not supplied to me; I re-derived from current artifacts only.

---

## RIG INTEGRITY (established before any attack was believed)

- **Corpus provenance.** Attack corpora were generated FRESH from the SHIPPED fixtures at `c304b098` — all 13 files md5-identical to the shipped copies [MEASURED HERE]. This directly addresses the campaign's prior stale-corpus incident.
- **CLEAN CONTROL.** Gate run against the fresh scratch corpus via `TF_SPEC_BINDING_SAMPLES_DIR`: `EXIT=0`, `PASS`, 14 declared-unadjudicated cells [MEASURED HERE]. My rig is not always-red.
- **REFORMAT-ONLY NOOP CONTROL.** My harness rewrites `ORACLE.json` through `JSON.stringify(...,2)`. Tag `A0_noop_reformat_only` produced stdout **byte-identical** to the clean run (`diff` empty) [MEASURED HERE]. Every red below is therefore attributable to its mutation alone, not to reformatting. *A red with two possible causes is not evidence for either.*
- **POSITIVE CONTROL for the oracle path.** `P1_wrong_value` (flip `20-nyam.sess.bindable` to `false`) → `EXIT=1`, both lanes named [MEASURED HERE]. The oracle comparison genuinely has a path to red.
- **Instrument audit.** `npx tsc` false-cleans in a worktree on this machine, so typecheck used `node --max-old-space-size=8192 node_modules/typescript/bin/tsc --noEmit` → `EXIT=0`, 0 errors [MEASURED HERE]. `grep -c` returning 0 exits non-zero and silently truncated one `&&` chain; that run was re-issued without `&&` and with a positive control.

**Tree integrity.** `git status --porcelain` in the graded tree, BEFORE and AFTER: identical, a single pre-existing ` M .claude/agents/accuracy-validator.md` **which is not mine** (present at session start). `HEAD` still `c304b098…` after all work [MEASURED HERE]. No edit, checkout, reset, push, merge, tag, or branch operation was performed on the graded tree. All mutation testing ran in `scratchpad/corpus-*` via the env var.

---

## Finding F-2 — HIGH — an oracle row's EXPECTATION is silently deleted by a typo'd key; output byte-identical, exit 0

**Severity:** CRITICAL/HIGH (false green — same class as the defect `8187b730` was rejected for, one granularity level down)

**Claim under attack:** sub-claims 3 and 4 — "The oracle contract is validated at RUNTIME", and the code's own caption at `scripts/check-spec-binding-plan-parity.ts:368-372`: *"Deliberately validating the WHOLE contract rather than only the two fields the reported attacks named: FIX THE PATTERN CLASS, NOT THE INSTANCE."*

**Reality [MEASURED HERE]:** `validateOracleContractOrExit()` validates the **presence** of `authority` and the **types of the structural fields**, but it never rejects an **UNKNOWN key** on an `OracleRow`, and never type-checks an **expectation value**. Six single-key mutations each removed a live expectation and produced stdout **byte-identical to the clean PASS** (same md5 `eb99c6ccdc373ab4a6e0c3e9c47a1351`) with `EXIT=0`:

| mutation | oracle path | expectation destroyed | result |
|---|---|---|---|
| `reason_names` → `reason_name` | `10-lunch-orphan.sess` | P-4 zone attribution | EXIT 0, byte-identical |
| `reason_excludes` → `reason_exclude` | `21-fivemin-chart.sess` | P-6 distinctness | EXIT 0, byte-identical |
| `bindable` → `bindible` | `20-nyam.sess` | core bindability | EXIT 0, byte-identical |
| `session_zone` → `session_zne` | `20-nyam.sess` | zone value | EXIT 0, byte-identical |
| `reason_null` → `reason_nul` | `20-nyam.sess` | reason nullity | EXIT 0, byte-identical |
| `reason_null: true` → `"true"` | `20-nyam.sess` | reason nullity (wrong TYPE, key intact) | EXIT 0, byte-identical |

The last row is distinct and worth isolating: `reason_null` is the only expectation read through a **double equality** (`want.reason_null === true` at `:729`, `=== false` at `:732`). Any non-boolean makes **both branches skip**, so a JSON type slip disarms it with the key still spelled correctly. Every other expectation field fails closed on a wrong type (`bindable`, `approximation`, `session_zone`, `primitive_null`, `spine_*`, `compiled` all compare with `!==`/`JSON.stringify` and go red) — verified by inspection of the executable lines and corroborated by `N6` below.

**Why the existing checks do not reach it [MEASURED HERE]:**
- The **row census** (`checkOracleRowCensus`, `:464-514`) asserts membership at **`condition_id` granularity** and never at **field** granularity. The row is still present, so the census is satisfied.
- The **contract validator** iterates `Object.entries(fx.conditions)` and checks only `row.authority` and `row.unadjudicated` (`:404-409`). An unrecognised sibling key is never examined.
- This is precisely the argument the commit message itself makes against `39948d3c`'s deleted comment — *"Membership operates at FIXTURE-FILE granularity and never at `condition_id` granularity"* — reproduced one level down: **the census operates at `condition_id` granularity and never at FIELD granularity.**

**The asymmetry that makes this a defect rather than a scope limit [MEASURED HERE]:**
1. The gate applies *"A NEW FIELD IS A NEW DRIFT BY DEFAULT"* to the **plan under test** — `projectExhaustively()` (`:285-339`) rejects `UNMAPPED TS FIELD`, `MISSING SOURCE FIELD`, `DUPLICATE DESTINATION`, `UNCONSUMED MAPPING ENTRY`. It applies **no equivalent rule to the oracle that judges the plan.**
2. Plan-level scalars **are** protected: `N6_typo_spine_total` (`spine_total` → `spine_totl`) → `EXIT=1`, named, because omitting a scalar demands a `scalars_unadjudicated` reason (`:667-675`). The per-row expectations have **no equivalent guard**. The mechanism to close this already exists in the file, twice, and was not applied here.

**Fix point:** `scripts/check-spec-binding-plan-parity.ts:404-409` — the per-row loop inside `validateOracleContractOrExit()` must reject unknown `OracleRow` keys against a closed key list and type-check each expectation value (and `:729/:732` must reject a non-boolean `reason_null` rather than skipping).

**Repro (exact):**
```
node scratchpad/attack.mjs N4_typo_bindable
cd C:/Users/tonio/Projects/wt-ledger-e-delivery-r497-20260730
TF_SPEC_BINDING_SAMPLES_DIR='...\scratchpad\corpus-N4_typo_bindable' npx tsx scripts/check-spec-binding-plan-parity.ts
# EXIT=0 ; stdout md5 eb99c6ccdc373ab4a6e0c3e9c47a1351 == clean PASS md5
```
**Clean control attributing the red to this cause alone:** `A0_noop_reformat_only` — same harness, same rewrite, **no** key change → byte-identical clean PASS. The only variable between A0 and N4 is the single renamed key.

**Blast radius:** every CLAIM-2 correctness expectation in `ORACLE.json`. A hand-edit, a merge artifact, or a rename during a future oracle amendment silently withdraws an adjudication with **zero** signal on a green run — the failure mode this entire four-delivery lineage exists to eliminate.

---

## Finding F-3 — MEDIUM — `reasons_must_differ_from` can be deleted entirely; output byte-identical, exit 0

**Severity:** MEDIUM (false green; uncensused adjudication population)

**Claim under attack:** sub-claim 4 — *"a population that may grow but must not shrink is checked in BOTH directions."*

**Reality [MEASURED HERE]:** the claim holds for `conditions` and fails for `reasons_must_differ_from`, which the file itself calls *"the oracle's sharpest assertion"* (`:1405-1407`).

| mutation | result |
|---|---|
| `N7` delete `10-lunch-orphan.reasons_must_differ_from` entirely | `EXIT=0`, stdout **byte-identical** to clean PASS |
| `N8` set it to `[]` | `EXIT=0`, stdout **byte-identical** to clean PASS |

The cross-fixture loop at `:1408-1409` iterates `expect.reasons_must_differ_from ?? []`. An absent or empty array yields zero iterations and zero output. Nothing censuses this population — no count is printed, no membership is asserted, no declared-gap reason is demanded (contrast `scalars_unadjudicated`, which *is* demanded for omitted plan scalars). `R-496` finding 4 hardened the **unresolvable-reference** path inside this loop; it did not protect the **existence** of the loop's input.

**Positive control that this population is live when present:** the clean run resolves the pair `10-lunch-orphan.sess` ↔ `21-fivemin-chart.sess` in both lanes and stays silent; `A1`/`A2` prove the surrounding machinery can go red. The N7/N8 silence is therefore a real disarm, not a dead code path.

**Fix point:** `scripts/check-spec-binding-plan-parity.ts:1408` — assert and print the count of resolved distinctness pairs, and require a declared reason for a fixture that drops one.

**Repro:** `node scratchpad/attack.mjs N7_delete_reasons_must_differ_from`, then run the gate. `cmp` against the clean stdout returns equal.

---

## Finding F-1 — MEDIUM — the FAIL summary's bucket caption names FIVE feeding checks; the bucket is fed by SIX. The omitted one is this delivery's headline repair.

**Severity:** MEDIUM (caption falsifies its own line — ninth instance on this codebase; never a false PASS)

**Claim:** `scripts/check-spec-binding-plan-parity.ts:1517` prints
`GATE CHECKS (membership · tripwire · axis-4 · TS-schema · reason-distinctness)`,
and its own comment at `:1509-1516` asserts *"this bucket was labelled `MEMBERSHIP`, but it is fed by FIVE checks"*. The ratify packet line 683 ratifies that five-name caption as **FIXED**.

**Reality [MEASURED HERE]:** `failures` has **six** feeders —
`:1298` membership · `:1309` **rowCensus** · `:1319` tripwire · `:1334` axis-4 · `:1364` TS-schema · `:1435`/`:1444` reason-distinctness.
`R-497` added `failures.push(...rowCensus)` at `:1309` and did not extend the caption. The **row census — the headline repair of this delivery — is the one feeder the caption omits.**

**Reproduced by execution, not by reading.** Four separate census-only failures all print under a caption naming five subsystems, none of which produced the failure:

```
### A1_delete_adjudicated_row :: EXIT=1
ORACLE ROW CENSUS FAILURE (1):
  - ORACLE ROW CENSUS: 20-nyam-evaluable.spec.json declares entry condition "sess" which is NEITHER
    adjudicated ... NOR named in conditions_unadjudicated_ids ...
FAIL: 1 failure(s) — CLAIM 1 AGREEMENT: PASS ... · CLAIM 2 ORACLE CORRECTNESS: PASS ·
GATE CHECKS (membership · tripwire · axis-4 · TS-schema · reason-distinctness): 1 failure(s)
```
Same shape for `A5_xor_both_sides`, `A6_gap_id_not_declared`, `A7_shrink_gap_list` [all MEASURED HERE].

This is defect class **D** from `39948d3c` (*"a bucket label naming one of five feeding checks — aimed triagers at the wrong subsystem"*) re-committed at the very line that documents it. It never produces a false PASS — the exit code and the count are correct and the failure IS printed above by its own banner — but the summary line a triager reads sends them to five wrong subsystems.

**Fix point:** `scripts/check-spec-binding-plan-parity.ts:1517` (caption) and `:1511` (the comment asserting FIVE), plus ratify packet line 683.

---

## Finding F-5 — MEDIUM — "No engine change: both lane files are byte-identical to the base" is false as written

**Severity:** MEDIUM (false provenance claim in the commit message — a reader who trusts it skips re-verifying a changed engine)

**Claim, verbatim (commit message):** *"No engine change: both lane files are byte-identical to the base."* The same message pins the base: *"ONE commit relative to the pinned base `9af37b8f`."*

**Reality [MEASURED HERE]:**

| lane file | `9af37b8f` | `c304b098` | identical? |
|---|---|---|---|
| `src/engine/spec_family_bindings.py` | `2a31942f…` | `2a31942f…` | **YES** |
| `src/server/lib/spec-family-bindings.ts` | `8053598b…` | `1853e7d9…` | **NO** |

The TS lane carries a **substantive behaviour change**: `lunch_blackout` and `overnight` are moved out of `SESSION_KEYWORDS` into a new exported `REFUSED_SESSION_KEYWORDS`, and `bindCondition()` gains a new refusal branch returning `bindable:false, primitive:null, approximation:true, reason:"session_zone_refused_uncomputable_window:<zone>"`. This changes the emitted binding plan for any spec whose object text mentions lunch / midday / noon session / overnight / globex / asia session / pre market / premarket.

**The charitable reading, tested and reported:** the TS lane blob `1853e7d9` is **identical across all four deliveries** (`2011e8de`, `39948d3c`, `8187b730`, `0d3db53c`, `c304b098`) [MEASURED HERE]. So the sentence is TRUE against the *prior delivery* and FALSE against the *stated base*. This delivery introduces no NEW engine change; it inherits one.

**Why it still matters:** at `9af37b8f` the Python lane **already had** `REFUSED_SESSION_KEYWORDS` (3 hits) and `refused_session_zone` (2 hits) while the TS lane had **zero** of either [MEASURED HERE]. The base therefore carried a **live TS↔Python parity break** — exactly the class this gate exists to detect — which this lineage repairs. Describing that footprint as "no engine change" understates it. The gate script also *imports* `refusedSessionZone` from the TS lane (`:47`), so the gate cannot compile without this engine change; it is load-bearing, not incidental.

**Fix point:** the commit message (unamendable now) and any downstream summary that repeats it.

---

## Finding F-4 — LOW — ORACLE prose counts an INVALIDATE row inside "13 of 15 conditions" that is not one of the 15

**Severity:** LOW (caption; printed verbatim on every passing run)

**Claim:** `ORACLE.json` → `fixtures["00-control-shipped.spec.json"].conditions_unadjudicated` =
*"13 of 15 conditions (all non-session families, the unknown type, and the INVALIDATE row) — authority section 6 leaves non-session families and invalidations unadjudicated."*

**Reality [MEASURED HERE], join key = condition id:**
- fixture declares **15** `entry_conditions`: `london, unknown-session, structure, verify, bias, direction, retest, filter, confirmation, enable, enter, hint, reset, exception, unknown`
- fixture declares **1 separate** `invalidations` entry: `{"id":"invalidate","type":"INVALIDATE"}` — **not** among the 15
- oracle adjudicates **2** (`london`, `unknown-session`); `conditions_unadjudicated_ids` enumerates **13**; 2+13 = 15 ✓ — the **enumeration is correct and the census is satisfied**
- the 13 enumerated ids contain **no** invalidate row

So the parenthetical claims the 13 covers "the INVALIDATE row", but that row is not one of the 15 conditions and is not in the list. It is genuinely excluded — by the *census population boundary* (`invalidations` are out of population, `:460-462`), a different mechanism than the one the prose names. The executable path (`conditions_unadjudicated_ids`) is right; the prose describing it is wrong. Also note "all non-session families": within this fixture 12 non-session families appear and all 12 are listed — true as scoped to the fixture, though `INVALIDATE` is one of the 13 pre-registered non-session families and appears only in `invalidations`.

**Fix point:** `ci/fixtures/spec-binding-parity-expanded/ORACLE.json`, `fixtures["00-control-shipped.spec.json"].conditions_unadjudicated`.

---

## What the delivery DOES close — measured, not relayed

These are real and I could not break them.

**Sub-claim 1 — SOUND.** `A1_delete_adjudicated_row` (delete `ORACLE.fixtures["20-nyam-evaluable.spec.json"].conditions.sess`) → `EXIT=1`, and the message **names the fixture and the condition id** [MEASURED HERE]. On `8187b730` this was byte-identical/exit 0. Runs before any plan compiles (`:1304`).

**Sub-claim 2 — SOUND.** Three variants, each `EXIT=1` with the exact path named, refusing to compile a single plan [MEASURED HERE]:
- `A2` delete `conditions.sess.authority` → `ORACLE.fixtures["20-nyam-evaluable.spec.json"].conditions["sess"].authority is absent or empty — this ROW cites no authority`
- `A2b` set it to `"   "` (whitespace) → same finding; the `str()` helper trims, so whitespace does not pass
- `A2c` delete the **fixture-level** `authority` → named at fixture granularity

**Sub-claim 3 — validated at RUNTIME: SOUND as to timing.** `validateOracleContractOrExit(oracleRaw, oraclePath)` at `:1186` runs on the parsed JSON *before* `oracle` is cast and *before* `verifyAuthorityFreshnessOrExit` and any compile [MEASURED HERE — read the executable line and confirmed by A2's output appearing with no plan output at all]. The `JSON.parse(...) as Oracle` bare cast is gone. **Downgraded to PARTIALLY SOUND only by F-2**, which falsifies the caption "the WHOLE contract".

**Sub-claim 4 — census is MEMBERSHIP, both directions: SOUND for `conditions`** [all MEASURED HERE]:
- `A7_shrink_gap_list` (remove `"filter"` from `conditions_unadjudicated_ids`) → `EXIT=1`, names `"filter"` as having *"LEFT BOTH SIDES OF THE LEDGER"* — the shrink direction
- `A6_gap_id_not_declared` (add `"NOT_A_REAL_ID"`) → `EXIT=1`, *"a declared gap must point at something real"* — the grow/reverse direction
- `A10_typo_gap_key` (`conditions_unadjudicated_ids` → `…_idz`) → `EXIT=1` with **13** findings — fails closed, does not silently treat the gap list as absent
- **Not** a cardinality check: the messages name identities, not counts.

**Sub-claim 5 — XOR and population independence: SOUND** [MEASURED HERE]:
- `A5_xor_both_sides` (add already-adjudicated `"london"` to the gap list) → `EXIT=1`, *"is BOTH adjudicated AND named unadjudicated. A row cannot be judged and unjudged at once."*
- Population independence verified by reading the executable line `:473-474`: `declared` is read from `JSON.parse(readFileSync(join(samplesDir, file))).spec.entry_conditions`, i.e. **authored fixture input**, never from the compiled plan. The expectation is not read out of the code under test.

**The `if (!fx) continue; // membership already denies an undeclared fixture, by name` comment at `:472` is TRUE** — and I tested it precisely because the identical comment shape was defect C on `39948d3c`. **Positive control `N10_fixture_on_disk_not_in_oracle`** (copy a fixture to `99-undeclared.spec.json` on disk only) → `EXIT=1` with the file named **three** times: twice by membership (missing from `required_members`, missing from `ORACLE.fixtures`) and once by `checkOracle` (`NO ORACLE ROW for fixture 99-undeclared.spec.json`) [MEASURED HERE]. Unlike its predecessor, this comment does not falsify its line.

**Frozen-oracle byte restoration — INDEPENDENTLY VERIFIED (the builder-produced proof this grade was asked to replace).** [ALL MEASURED HERE]
- `ORACLE.json` blob at `c304b098` = `f57a9d00…`, identical to the self-correction commit `0d3db53c`; the pre-mojibake blob at `8187b730` (and at `2011e8de`, `39948d3c`) = `2042ceb4…`.
- Full `diff -u` between `2042ceb4` and `f57a9d00` is a **pure single-hunk addition** of the intended `conditions_unadjudicated_ids` key (15 lines, one location). **No other byte differs.** The 35 mutated strings are gone.
- `iconv -f UTF-8 -t UTF-8` round-trip: **VALID UTF-8**. Mojibake marker scan (`Ã`, `â€`, `ï¿½`, `Â`, BOM): **0 hits each** — with a **positive control** (planted `Ã¢â‚¬â€` into a copy → the same scan returned **16** hits), so the zero is a measured absence, not a broken pipeline.
- Note on lineage: `0d3db53c` is **not** an ancestor of `c304b098` (`git merge-base --is-ancestor` → NO); it sits on `hardening/ledger-e-parity-20260730`. `c304b098` is a clean single commit on `9af37b8f` that carries the corrected bytes.

**Other checks confirmed live [MEASURED HERE]:**
- `P1_wrong_value` → `EXIT=1`, both lanes, plus the *"lanes AGREE and are BOTH WRONG"* note — the two-lane blind spot is genuinely covered by the oracle.
- In-run self-controls all print and all discriminate: queue-reason tripwire (1 armed detected / 1 same-shape safe silent), axis-4 duplicate-id (named `PLANTED_DUP`, `2x`, clean neighbour silent), array multiplicity + order detected, identical plans silent.
- P-7 property: 13 families × 8 phrases = **208** probe assertions + **16** adjacent `WAIT_SESSION` positive-control assertions across both lanes.
- Authority freshness is **computed, not asserted**: `sha256=3494d4bb…` over 16314 bytes of `docs/designs/ORACLE-AUTHORITY-ORPHAN-ZONES-2026-07-30.md`, computed in-process and compared to the pin.
- **Door 1 (`satisfies`) has a real path to red** — mechanism-level positive control [MEASURED HERE]: exact match compiles (exit 0); a **missing** mapping key is rejected `TS1360`; an **excess** key is rejected `TS2353`. Repo typecheck clean: `EXIT=0`, 0 errors.
- **CI wiring is real, not decorative:** the gate is added to BOTH `ci.yml` (line ~352) and `fast.yml` (line ~147), neither under `continue-on-error`; `npm run check:spec-binding-plan-parity` exists in `package.json:28`; both workflows run `actions/setup-python@v5` so the Python driver can execute. (The commit message's "No CI run exists" refers to no *executed* run, which I did not attempt to verify.)

---

## Sub-claim 6 — NOT-SOUND

**Claim:** *"The DECLARED `invalidations` exclusion is stated IDENTICALLY in code and in the ratify packet."*

**Code states it** — `scripts/check-spec-binding-plan-parity.ts:460-462` [MEASURED HERE]:
> `★★ DECLARED SCOPE LIMIT: `invalidations` are NOT in this population, because `checkOracle()` can only index `plan.bindings`. That gap is pre-existing and separately declared; this census does not silently widen to cover it.`

**The packet does not state it anywhere** [MEASURED HERE]. `docs/designs/LEDGER-E-PARITY-RATIFY-PACKET-2026-07-30.md` (696 lines) contains **2** occurrences of `invalidation`, and neither is the scope limit:
- line 171 — a clause about what the **old comparator** failed to emit
- line 495 — a table row `| 50-family-axis-invalidations.spec.json | inv_in_entry · bindable | 1 |`

The packet does not describe the census at all: `conditions_unadjudicated_ids` **0**, `ROW CENSUS` **0**, `row census` **0**, `R-497` **0**. The 4 hits for `census` are unrelated (2 concern `pop120_census.py`, 1 the membership census line, 1 the R-496 finding-3 table row).

**POSITIVE CONTROL for these zeros** (an absence claim owes one) — the same `grep -c` pipeline over the same file finds what must be there: `required_members` **4**, `ORACLE` **13**, `R-496` **3**, `invalidation` **2** [MEASURED HERE]. The zeros are measured absences, not a broken search.

**Reconciliation.** The commit message *itself* discloses this: *"The ratify packet carried here is UNCHANGED and is STALE with respect to this delivery: R-497's allowed-files list does not include it, so it was not updated rather than silently widening scope."* That conduct is defensible — declining to widen scope beyond an allowed-files list is the right call. **But it directly contradicts sub-claim 6 as handed to me.** The exclusion cannot be "stated IDENTICALLY in code and in the ratify packet" when the packet does not state it at all. Sub-claim 6 is refuted; the underlying *behaviour* is not criticised.

Related, and it compounds F-1: packet line 683 records finding 3 as **FIXED** with the five-name caption quoted as the remedy. That ratified caption is now stale — the bucket has six feeders.

---

## MANDATORY CLOSING COVERAGE SECTION

### 1. What I verified, and via which two-plus non-overlapping paths

| Claim | Path A | Path B | Path C |
|---|---|---|---|
| Commit topology (1 commit off `9af37b8f`) | `git rev-list --count` | `git rev-parse c304b098^` | `git diff --name-status` (23 paths) |
| Oracle bytes restored, no mojibake | full `diff -u` of blob `2042ceb4` vs `f57a9d00` (pure single-key addition) | `iconv` UTF-8 round-trip + marker scan **with planted positive control** | blob-hash identity `c304b098` == `0d3db53c` |
| Sub-claim 1 (row deletion denies) | execution `A1` → EXIT 1 + named | read executable line `:478-495` (census logic) | clean control `A0` byte-identical PASS isolates the cause |
| Sub-claim 2 (authority strip denies) | execution `A2`/`A2b`/`A2c` → EXIT 1 + exact path | read `:400`/`:407` (`str()` trims, so whitespace fails) | contract failure precedes any plan output |
| Sub-claim 3 (runtime validation) | read call site `:1186` (before cast, before compile) | execution `A2` produced **no** plan output at all | typecheck clean, bare cast removed |
| Sub-claim 4 (membership both directions) | execution `A6`, `A7`, `A10` | read `:478-512` (three loops: declared→, adj→, gap→) | `N6` shows the analogous scalar guard fires |
| Sub-claim 5 (XOR + population independence) | execution `A5` → EXIT 1 | read `:473-474` (population from fixture input, not plan) | fixture/oracle arithmetic 2+13=15=declared |
| Sub-claim 6 (packet parity) | `grep -c` over the packet, 4 probes = 0 | positive control, 4 probes non-zero | commit message's own staleness disclosure |
| F-1 (six feeders, five named) | `grep -n 'failures.push'` enumeration | **execution** — 4 census-only runs print under the 5-name caption | packet line 683 ratifies the 5-name caption |
| F-2 (silent expectation disarm) | 6 executions, all EXIT 0 | `md5sum` byte-identity vs clean PASS | `N6` contrast proves the guard exists elsewhere in the same file |
| F-5 (engine byte-identity) | blob hashes base vs graded, both lanes | symbol counts (`REFUSED_SESSION_KEYWORDS` base=0/graded=6) | blob identity across all four deliveries |

### 2. Positive-control witnesses for every absence claim

| Absence claimed | Positive control | Result |
|---|---|---|
| No mojibake in `ORACLE.json` | planted `Ã¢â‚¬â€` into a copy, same scan | 16 hits — scanner works |
| Packet never states the census/exclusion (4 zero-counts) | same `grep -c` for `required_members`/`ORACLE`/`R-496`/`invalidation` | 4/13/3/2 — pipeline works |
| Typo'd oracle keys produce **no** output | `A0` clean control (green) + `P1`/`A1`/`A2` (red) | rig can produce both greens and reds |
| `reasons_must_differ_from` deletion produces no output | clean run resolves the pair silently; `A1`/`A2` prove the surrounding machinery reddens | disarm is real, not dead code |
| `satisfies` would catch a new field | missing-key and excess-key fixtures | `TS1360` / `TS2353` — real path to red |
| `if (!fx) continue` comment is true | `N10` undeclared fixture on disk | named 3× by 2 independent checks |

### 3. Join keys checked for every "identical / unchanged / matches" claim

- **Oracle bytes unchanged:** git **blob SHA** (`f57a9d00…`), not path or filename — plus a full content `diff`.
- **Engine lane unchanged:** git **blob SHA** per lane file path, base vs graded, both lanes separately.
- **Fixture ↔ oracle correspondence:** **fixture filename** for the three-way membership sets; **`condition id`** for the row census (`spec.entry_conditions[].id` ↔ `ORACLE.fixtures[f].conditions` keys ↔ `conditions_unadjudicated_ids[]` ↔ `plan.bindings[].condition_id`). Verified the 15/2/13 arithmetic closes on the actual ids, not on counts alone.
- **Corpus provenance:** **md5 per file**, all 13 scratch files vs shipped.
- **Attack attribution:** **stdout md5** vs the `A0` reformat-only control.
- **Prior-object comparison:** commit SHA, and confirmed `0d3db53c` is *not* an ancestor rather than assuming the branch relationship.

### 4. What I did NOT verify, and why

- **CLAIM 1 (TS↔Python agreement) as a correctness property.** I ran the comparator and it passed on 12 fixtures, but I did not independently reimplement either lane. Both lanes being identically wrong on an unadjudicated cell is outside what I measured — the gate says so itself, and 14 cells are explicitly unadjudicated.
- **The oracle's expected VALUES against the authority document.** I verified every row *cites* an authority and that the authority file's sha256 matches its pin. I did **not** re-derive the 15 adjudicated expectations from `ORACLE-AUTHORITY-ORPHAN-ZONES-2026-07-30.md` prose. A correctly-cited but mis-transcribed value would survive this grade. **This is the largest single gap.**
- **`scripts/materiality-receipt-ledger-e.ts` (248 lines) and its `ci/fixtures/materiality-control/` corpus.** Not executed, not read in depth. The "materiality receipt" third of the headline claim is **UNDETERMINED**. The commit message states materiality is hand-run.
- **`ORACLE-AUTHORITY-ORPHAN-ZONES-2026-07-30.md` (238 lines) content.** I hashed it; I did not audit its rulings.
- **Door 2 (`projectExhaustively`) against a real TS-only field.** I read its four rejection paths and confirmed door 1's mechanism, but I did not plant an actual undeclared field on a live `BindingPlan` object — that needs a full-repo scratch clone. **Door 2's runtime path to red is UNPROVEN by me** (HYPOTHESIS that it works as written).
- **Whether the TS engine refactor is semantically inert.** I read `sessionPhraseHit` as equivalent to the loop it replaced (`.some()` vs early-return; `trimmed` hoisted but identical), but I did **not** differentially execute base vs graded `resolveSessionKeyword` across an input corpus. The *zone move* is unambiguously a behaviour change; the *refactor* being inert is **CORROBORATED by reading, not MEASURED**.
- **Any actual CI execution.** I verified the workflow YAML wiring, the npm script, and Python setup statically. No run exists; I triggered none.
- **The 14 unadjudicated cells.** By construction these carry no expectation. I confirmed they are printed and counted; I did not judge whether leaving them open is correct.
- **Gate-B incidence**, declared UNMEASURED by the builder — I did not measure it either.

### 5. Sub-claims remaining UNDETERMINED

- **The "materiality receipt" leg** of the headline claim — not exercised (see above). The claim as handed to me decomposed into 6 sub-claims, none of which covered materiality, so it was graded neither SOUND nor NOT-SOUND.
- **Door 2 runtime exhaustiveness** — UNDETERMINED.
- Sub-claims 1, 2, 5 are SOUND; 3 and 4 are PARTIALLY SOUND with the named defects; 6 is NOT-SOUND.

### 6. Independence declaration

I did not design, build, or previously grade `c304b098` or any of `2011e8de` / `39948d3c` / `8187b730` / `0d3db53c`. No lineage to declare. Bands were re-derived from current artifacts only; no prior score was consulted or carried. All six findings were produced by novel hunts — **none** is a member of the delivery's registered fixture set, consistent with the pattern that all four prior defect classes were also missed by those fixtures.

### 7. Findings by severity

| Severity | Count | IDs |
|---|---|---|
| HIGH | 1 | F-2 |
| MEDIUM | 3 | F-1, F-3, F-5 |
| LOW | 1 | F-4 |
| **Total** | **5** | |

**Recommended disposition:** REJECT and return for a fifth delivery. F-2 alone is disqualifying — it is the same defect class (`a check satisfied by ABSENCE rather than by verification`) that rejected `8187b730`, relocated from row granularity to field granularity, and it reproduces the identical symptom: **byte-identical output, exit 0**. F-1 and F-3 are cheap to close in the same wave. F-5 and F-4 are caption repairs.
