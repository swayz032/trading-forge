# H1 Wave-1 Shakedown — RESULT (the certificate machinery's proving run) 2026-07-12

The (b)-lane's owed fidelity adjudication on the 14 demotion concepts, run as the tier-3 certificate machinery's SHAKEDOWN CRUISE (#7-before-#6). Two INDEPENDENT blind raters (control-gated, quote-anchored, closed 3-class taxonomy, answer key never seen) adjudicated 206 scorable targets + 10 controls; 5 Afrikaans (`jlShztsY3oA`) HELD pending certified gloss. Scoring is a mechanical tally against the demotion answer key — reproducible from the vaulted rater-call files + answer key; not a re-rating.

## Control gate
Both raters cleared **5/5 gate, 5/5 context** → both call-sets COUNT.

## Output 1 — PER-CONCEPT FIDELITY VERDICT (the (b)-cert's owed adjudication)
- **Fidelity CONFIRMED: 67%** (both raters agree with each other AND with the demotion role).
- **Demotion role CONTESTABLE: 24-25%** — both blind raters AGREE with each other but DIFFER from the demotion assignment. **Robust: 25%→24% across the aggressive vs strict-spine 5→3 mapping** (mapping sensitivity run — the number does NOT hinge on the contestable OPTIONAL/ALTERNATIVE→gate choice). A genuine fidelity gap, not a mapping artifact.
- **Genuinely ambiguous: 8%** (raters split A≠B — the irreducible surface).
- **Inter-rater agreement: 92%, mapping-INVARIANT** — vs the demotion lane's own 57.7% second-pass agreement. **The certificate machinery is MORE consistent than the process it audits.** That is the shakedown's core positive: the tier-3 blind protocol (closed taxonomy + control-gate + quote-anchor) works.
- Contest concentrated: ORB 10/38, VWAP 7/30, Candlestick 6/25, EMA 5/16, ICT-OrderBlock 5/16.
- **Verdict for the (b) cert track:** demotion-derived runtime role assignments carry a ~1-in-4 contestable rate under blind re-adjudication → the contested quarter needs review before demotion-productionization. The owed fidelity check DELIVERED its number.

## Output 2 — FAILURE-MODE LIST (design input that UNBLOCKS the Wave-2 tier-1 build)
Both raters, INDEPENDENTLY, named the SAME two ambiguity classes (convergence = strong signal):
1. **Walkthrough-narration-that-restates-a-gate** — a real entry rule embedded inside worked-example narration / chart-pointing ('we do have a fair value gap', 'over here where it closes above the third SD band'). Prescriptive framing = gate; chart-pointing / result-narration = context.
2. **Bias-vs-movement-expectation** — declared directional EXCLUSION / bias ('forget shorts', 'we are now bearish') = gate; bare movement/target expectation ('price wants to move to the opposite end') = context.
Hardest concepts: EMA, ICT-CRT, VWAP (directional-bias reasoning blurs with gate logic; VWAP-is-support lines read as the refuted strawman the educator critiques). The 8% rater-split concentrates in these two classes.
**Design consequence (feeds Wave-2 §6 WAVE-1-FEED):** these two classes are EXACTLY what tier-1 deterministic detection must NOT attempt — they are the fall-through-to-tier-2(discourse)/tier-3(adjudication) surface. Tier-1 claims the clean ~67%; the two named classes route onward. The classifier's dying gift confirmed empirically: gate-strength here lives in the discourse frame, not the sentence.

## Output 3 — ECONOMICS BASELINE (the tier-3 cost the ≤1/video rider is judged against)
- Full blind adjudication pass: ~242K (rater A) / ~235K (rater B) tokens over 216 items ≈ **~1.1K tokens/item**.
- **~30% of items required real deliberation** (the two ambiguity classes above); ~67-70% were fast (clean prescriptive gate or clear narration).
- **The ≤1-tier-3-invocation-per-video rider's reachability** hinges on tier-2 discourse-routing catching most of that 30% so tier-3 handles only the residual. That is the Wave-3 (discourse pass) build's job; the shakedown fixes the BASELINE (~30% hard-fraction, ~1.1K tok/adjudication) it will be measured against.

## Load-bearing caveats (Law 7)
- Scope: 14 demotion concepts / 221 items / the committed `dri-audit-2026-07-05.json` first-pass assignment-under-test. NOT the fresh sealed set (that stays sealed until Wave 5).
- The 'answer' is the demotion lane's claim, not ground truth — CONTESTABLE means 'two blind readers disagree with demotion', which is the fidelity signal, not proof demotion is wrong. Raw 5-class preserved per item for any strict-spine re-map.
- 1 concept (`jlShztsY3oA`, Afrikaans, 5 items) HELD — a certified-gloss decision is owed (operator): human translation, or quarantine as a named 13-concept shakedown.
- Scoring self-run by the orchestrator is mechanical (JSON tally); the load-bearing fidelity number got its second derivation via the mapping-sensitivity run (25%→24%). Independent re-tally reproducible from vaulted inputs.
