# EXTERNAL READ — 2026-08-09 — AR-817 ACCEPTED; THE DYNAMIC WRONG-IDENTITY PROOF IS WITHDRAWN AND B1 IS HELD

> **PROVENANCE:** **OPERATOR-RELAYED CHAT**, 2026-08-09, responding to `AR-817`.
> **CONSUMED BY `R-726`** — banner written BY that ruling, IN its commit, never in advance (`R-722 §1`).
> ⭐ **THIS READ WITHDRAWS ONE OF ITS OWN PRIOR STATEMENTS** (that the mid-series result established
> semantic non-identity by counterexample). **This desk had adopted that statement verbatim at
> `R-725 §3` and praised its scope discipline** — the discipline was right, the instance was dead.
> 🛑 **ITS GAP-LIST IS SHORT BY TWO:** it names `F-4, F-7, F-8, F-10` unread; `[MEASURED, R-726 §6]`
> it is **also** missing `F-5` and `F-6`, and **both are findings against the desk.**
> **A CHANNEL IS NOT AN AUTHOR** — audited on merit.

---

GPT EXTERNAL ADVISOR RULING — AR-817 ACCEPTED; THE DYNAMIC WRONG-IDENTITY PROOF IS WITHDRAWN AND B1 IS HELD

DECISION: ACCEPT THE CORRECTION. DO NOT RUN A SALVAGE MUTATION EXPERIMENT. DO NOT BEGIN B1.

AR-817 correctly separates two claims:

- Survives: `compute_structure_state` has the wrong computational contract for opening-range construction.
- Dead: the published executions proved universal insensitivity to opening-range mutations.

The bar-0 and bar-30 nulls were caused by the swing detector's positional horizon. The global positive control did not validate sensitivity at either mutation site.

The following claims must be struck from every durable carrier:

- "the mutation proved independence";
- "the mid-series test closed the objection";
- "wrong identity was proven by execution";
- any ruling language adopting those claims.

This external advisor also withdraws its prior statement that the mid-series result established semantic non-identity by counterexample.

WHAT STILL SUPPORTS WRONG IDENTITY

The present wrong-identity conclusion rests on contract evidence:

- no session or opening-window input;
- no opening-range output;
- structural-pivot computation;
- routing to a market-structure evaluator;
- non-gating use;
- a separate OR implementation that actually computes OR high, low, and width.

That is enough to retain `ENGINE_PRIMITIVE_WRONG_IDENTITY` provisionally, but its evidence label must be:

`SUPPORTED_BY_INTERFACE_AND_IMPLEMENTATION_IDENTITY`

It must not say:

`SUPPORTED_BY MUTATION EXECUTION`

B0 remains logically separate. Its successful mutation was applied directly to the OR producer's active formation window, and the OR output changed at that same site. `REUSE_WITH_TYPED_ADAPTER` therefore remains provisional.

LOCALITY RULE

Future mutation evidence is admissible only when the positive control proves observability at the same:

- input location;
- field;
- consumer;
- evaluation horizon;
- and output assertion

as the tested mutation.

A globally responsive system does not validate a locally null experiment.

FULL-GRADE BLOCKER

The complete stable grade has not been supplied to this external advisor.

The attachment provided here is 11,743 bytes, not the reported 53,393-byte final grade. It contains references to only:

- `F-1`, `F-2`, `F-3`, and `F-9`;
- `C1–C6` and `C8`.

It does not contain the complete text of all ten findings and eight graded claims.

Therefore, this advisor will not authorize production mutation from:

- a partial worker summary;
- a mid-write read;
- a verdict line without its complete findings;
- or unread `F-4`, `F-7`, `F-8`, and `F-10`.

Required next desk artifact:

1. The complete final `GRADE-PHASE-A-ROWS-1-3-2026-08-08.md`.
2. Stable byte size.
3. SHA-256.
4. Git blob or commit identity.
5. Full `F-1…F-10`.
6. Full `C1…C8`.
7. No paraphrased substitute.

F-3 RULING

Strike the claimed 12-primitive census and its literal-grep positive control.

The corrected reachable population is reported as 18, but no broad replacement census is authorized now.

For the golden repair, require only a bounded executable reachability proof:

frozen spec path
→ current family route
→ selected primitive

and separately:

existing OR producer
→ exact missing connection from the frozen spec path

Do not build another repository-wide instrument unless the full grade proves it is necessary for B1 safety.

AUTHORIZED WORK WHILE B1 IS HELD

Only the unfinished B0 closeout controls are authorized:

- execute the missing-opening-bar cases;
- print the exact timestamps and bar interval behind the reported six pre-lock observations;
- resolve any `[start, lock)` off-by-one issue;
- preserve the complete-window positive control.

No structure-engine mutation rerun is authorized. It would attempt to rescue a discarded claim rather than advance the golden source path.

B1 RED MUST BE REFRAMED

The future RED must not assert that structural output never changes when OR bars change.

It must assert the actual contract failure:

> The frozen opening-range instruction is routed to an evaluator whose typed outputs cannot satisfy `OPENING_RANGE_DEFINITION`.

That RED can be proven through output-schema identity, production routing, and comparison with the existing OR producer. It does not need a dead-zone-sensitive claim.

FINAL POSITION

AR-817 demonstrates why the independent grade exists. It invalidated the campaign's strongest advertised execution result without invalidating the central architectural finding or the B0 reuse candidate.

Current status:

- Phase A worker diagnosis: materially supported but findings not fully reviewed.
- Dynamic independence claim: withdrawn.
- `REUSE_WITH_TYPED_ADAPTER`: provisional and still alive.
- B1 production repair: HOLD.
- Next blocking input: the complete immutable grade artifact.
