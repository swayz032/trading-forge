# H1 — CONFIG PASS PRE-REGISTRATION (Phase-B frontier-native) — frozen 2026-07-13, BEFORE any number

> LICENSED after the design-pool MISS (10.8% > 8%). The number stood. Evidence-aimed: gpt-5.4's Phase-B conditions grounded at ~10.8% — the SAME rate gemma's did under Option R (evocative convergence, NOT identity — different condition sets 195 vs 297). Two brains, same gemma-shaped prompt, same rate → the CONFIG is the ceiling, not the brain. 9 conditions short of the bar = config-distance, not capability-chasm. ONE config pass, no second swing on this rung.

## FROZEN (unchanged — the bar never moves, the population never launders)
- **The same 16 videos.** No exclusions, no matter what the anatomy finds. If CLDEIsNpVRc is transcript-garbage, that is a FUTURE production intake-filter rule, pre-registered separately — it NEVER retroactively shrinks this pool.
- **Same ≤8% locator support-miss bar.** Same locator anchor authority (model-free).
- **Same birth-certified Phase-A enumerator, UNTOUCHED** (`strategy-enumerator.md`) — it passed its exam; Phase-A draws are REUSED from cache (free). Only the thing that failed (Phase-B copying) is rewritten.

## CHANGED (pre-registered before any number)
- **Phase-B rewritten FRONTIER-NATIVE** (`transcript-extractor-frontier.md`, `frontier-v1`). Sheds every gemma-4B compensation (strict-fill scaffolding, GBNF property-order tricks, recency-position hacks). In their place, the four things a reasoning model can honor:
  1. **VERBATIM MANDATE** — copy the speaker's exact words; never normalize, never translate to trading vocabulary, never paraphrase.
  2. **REAL ABSTAIN** — null what the speaker did not state; an honest null beats a plausible invention (gpt-5.4 can reliably say "not taught"; gemma could not).
  3. **ELABORATION GUARD** — never complete what the speaker didn't finish; vague-in → vague-or-null-out.
  4. **STAY LITERAL AND COLD** — a copying task, not an analysis task.
- **Pinned reasoning params (chosen ONCE, here, NEVER tuned against results):** `reasoning_effort = "low"` (literal copying; minimal interpretive reasoning that would tempt paraphrase/elaboration). `temperature` is NOT tunable — gpt-5.4 fixes it at 1 and rejects any other value (model constraint, recorded honestly, not a choice).

## BUDGET (single pass)
Operator-signed $2 burst (dollar-true, blend×1.5). Phase-B re-extraction only (Phase-A cached free). Clears (≤8%) → freeze prompt+model SHA → terminal-read preconditions. Misses → the ladder advances to the Claude extraction seat at zero marginal cost, terra at the boundary behind it. **No second config swing on this rung.**

## BEFORE THE READ COUNTS
The two ENUMERATION-UNSTABLE videos (DLwVqcLRcfw, IyFioFkRgWo; k=5 mode<4/5) get their owed blind adjudications per the k=5 policy — settled enumeration before their conditions weigh in.

*Frozen 2026-07-13, operator-licensed, BEFORE any config-pass number. Vaulted FF.*
