# Extraction System — Mission, Honestly Scoped (Fable-5, 2026-07-07)

The clean-board reframe: this converts the week from a defect log into a **coordinate system** — every event slots onto one axis. Three corrections to the plain mission statement ("faithfully extract educator strategies until the library holds real edge; feed production the one worth scaling").

## Correction 1 — the conveyor is HALF-built, not "built with a low floor"
Five stages: scout → extract → compile → backtest → gate.
- **Judging half — BUILT + TRUSTWORTHY** (compile / backtest / gate): hardened this week to the highest standard the program owns.
- **Ingestion half — SCAFFOLDED + SCOPED, not built:**
  - **Extract-at-institutional-fidelity** — the week PROVED this stage isn't built: classifier died at 7/9, the gradient taxonomy is a *map of what the next instrument must do*, (b) covers 14 concepts with single-rater labels still owing their adjudication.
  - **Scout** — never been through any gate; its **selection function is untested**, and it feeds everything.
- **Why the phrasing matters:** "built but low floor" invites *tuning*; "half-built" correctly points the next quarter's work at the **extraction instrument the gradient taxonomy just specced.**

## Correction 2 (load-bearing) — TWO success conditions; only one is engineerable
- **H1 — fidelity can reach the trust threshold.** Fully in the program's control; the week MOVED it (tonight: plausibly reachable via the gradient's named surface classes — conditional-action, exclusion-contrast).
- **H2 — the educator population contains extractable edge at a rate that makes the conveyor economic.** NOT engineerable by any fidelity floor; **genuinely open, untested, and only testable AFTER H1.** A perfect-fidelity pipeline over edgeless teaching produces a perfect-fidelity library of certified FAILs — the falsification instrument working (a valuable output), but the mission-as-written quietly assumed the source population contains survivors. 117 strategies @ ≤0.54 fidelity means **the corpus hasn't been tested — the verdicts so far are about the EXTRACTOR, not the educators.**
- **The mission's real shape = a two-hypothesis experiment**, H2 gated behind H1.
- **★★ PRE-REGISTERED NOW (before the numbers, same blade as every gate this week):** a **0-survivors-at-high-fidelity** result = **falsification of the SOURCE thesis** (educators-as-edge-source), NOT of the pipeline → **pivot the conveyor to better populations** (books, papers, the operator's own hypotheses — the source-agnostic case from way back). Deciding this before the numbers exist is discipline; deciding it after would be **the first laundering opportunity the program has ever left itself.** The day fidelity is solved is the day H2's first honest read arrives — and it could be brutal; the meaning is fixed now.

## Correction 3 — the record is SHARPER than the statement: an operational definition now exists
"Faithful enough to trust the verdict" was a slogan a week ago. It now has a **checkable certificate:**
> A strategy is **extraction-trustworthy** when its gate-strength assignments survive **blind adjudication under the closed disposition taxonomy**, on an **engine with zero forward-window reads.**
Every component of that certificate was built this week: the closed taxonomy (+ the surface-gradient revision), the control-gated blind-rater protocol, the causality lint (forward-window guard), the fidelity adjudication (b) still owes.
**★★ NORTH STAR, restated as the program can now MEASURE it:** *No candidate reaches the library without a **fidelity certificate** AND an **engine-truth certificate** — and both certificates are instruments that have survived their own adversarial review.* **That sentence did not exist a week ago. It is the week's real product.**

## Coordinate summary
- Judging half: built, trustworthy, adversarially reviewed. Ingestion half: scaffolded, scoped, pointed at the right target (the gradient-specced extractor).
- Mission = H1 (engineerable, moving) → H2 (open, untested, pre-committed pivot on falsification).
- Success is now certifiable, not asserted: two certificates, both self-adversarially-validated.
- Scope on every claim: on this corpus, by these instruments, at this population.
