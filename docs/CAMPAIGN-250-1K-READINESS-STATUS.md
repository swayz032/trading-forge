# Campaign Status — "$250–$1K/day Readiness" (Make the Measurement/Execution Chain True)

> **Durable handoff doc — updated in place at each stopping point.** Full plan: `~/.claude/plans/i-want-you-to-luminous-parnas.md`. Session journal: `AGENT-LOGS.md`. This file = the scannable current state.

**Last updated:** 2026-07-17 (after M3) · **Branch:** `hardening/phase-0` · **Tip:** `91e1870f`+ (goalscan/liveness-audit/other concurrent sessions advance it continuously) · **Campaign base:** `61bb20a3` · **Landed: W0, W1, W2, W2b, W3B, W4, W5-indep, W-testdebt, M2, GATE3, W3A, M3** (12 waves). Next: W6 (shadow profit-milestone governor, pin to M2 SHA) → M1a blocked on Massive docs → W7 close-out
**★ Confirmed sole owner (2026-07-17):** an unrelated concurrent session briefly staged its own M3 packet + Workflow after finding it queued in this campaign's docs; the operator stopped it before any commit landed — this campaign has a standing owner-session (this one), not an open backlog for other sessions to adopt. The courtesy-reference packet at `docs/ratify-packets/m3-paper-authority-flip-2026-07-17.md` was superseded — this session authored/landed its own packet at the same path (VERIFIED BAND 7, landed `91e1870f`).
**★ Standing correction (2026-07-17):** this session briefly drifted toward holding M3 for operator/advisor review before dispatch, reasoning it was "doctrine-level." Operator corrected: only irreversible-today or live-capital-today waves get a hold; M3 is neither, so it proceeds autonomous like every other instrument wave, same as this doc's own execution model already specified. See `feedback_standing_launch_protocol_staged_not_started` memory.
**★ Concurrent session active:** a "goalscan" session (now on round-3+ scans) is landing its own audit fixes to the SAME branch (deconflicted — it treats the campaign's instrument surface as hand-off). Shared `node_modules` has been partially wiped 3 times by concurrent/own worktree cleanup (junction `rm -rf` once, `git worktree remove --force` on a junctioned scratch worktree twice, incl. this campaign's own W3A cleanup) — `npm install` restores it each time. **Correction:** an earlier note claimed "prefer symlinks, M2's survived" — falsified by W3A's cleanup, which used the identical `ln -s` creation method and still got wiped (git-bash's `ln -s` on a directory can silently fall back to a junction without elevated privilege; you cannot tell which you got without testing removal behavior — see `reference_rm_rf_junction_deletes_target_2026_07_09` memory for the corrected procedure). Each wave: rebase onto current origin tip → verify COMBINED tree → then FF-push.

## What this campaign is
Make the internal paper engine's evidence TRUE (bar-time exits, unified stop geometry, deterministic durable fills) so it can OWN the PAPER stage on a $29 Massive Starter delayed feed (D5), qualify strategy cohorts in 5–10 days (D7), and let a genuine 2R+ strategy prove itself and scale — without retail gate bloat. Prop firms (Topstep) stay DOWNSTREAM of the lifecycle; a separate **Go-Live Gate Register** (Appendix C of the plan) certifies live execution later (D8-gated on proven edge + combine purchase).

Derived from 4 batches of external GPT review, each verified claim-by-claim against live code (~half stale/wrong). Two adversarial plan-reviews applied. Mandatory **Wiring-Verification Protocol** (producer→consumer→DB→gate→audit proven on REAL data, never mocked; doer≠grader every wave; zero-carry-forward).

## Operator decisions (D1–D10)
D1 profit governor shadow-only · D2 parity flag = A/B report only · D3 no 9-micro cap · D4 DLL bands stay 60/67/95 · D5 Massive Starter $29 delayed feed · D6 internal engine owns PAPER · D7 PAPER window 5–10 qualifying days · D8 Topstep after proven edge → account twin staged · D9 C-05 sizing = risk math always wins (remove healthy-account floor override) · D10 pre-live audit findings → staged Go-Live Gate Register.
Plus: daily upside is UNCAPPED (no fixed TP) → §5 doc $250-500 → $250-1000+.

## Wave status (DAG)
| Wave | State | Notes |
|---|---|---|
| **W0** Pre-flight | ✅ DONE | base `61bb20a3`; tsc green (needs 8GB heap); 2 CI gates green; system-map drift = pre-existing baseline (audit H-11) |
| **W1** F-3 bar-time exits | ✅ **LANDED `78f3475a`** | class-complete (closePosition + bookPartialClose day-key); independent grade band 7 SAFE; 223/223; 2 regression guards hardened; packet CLOSED |
| **W2** Stop-geometry contract | ✅ **LANDED `d694c61a`** | two-role helpers TS+Python + fail-closed parity gate (216 cells); backtest byte-identity FORMALLY PROVEN; grade band 7 SAFE; F-1 gate_block sibling + F-2 docs closed in-wave; test:metrics 144/144 unchanged |
| **W2b** Sizing lowest-wins (C-05) | ✅ **LANDED `bd47b8a8`** | pure `max(0,min(...))` lowest-wins both languages, scalar+vectorized; <1→skip. Band 7 after **3 grade rounds** (core + 2 HIGH vectorized fabrications + 1 firm-cap sibling, all closed in-wave). A/B receipt quantifies the reversal. test:metrics HELD. |
| **M1a** Massive adapter | ⏸ BLOCKED on operator | needs REAL Massive docs (don't build against a guessed protocol) — flag when subscribed |
| **M1b** feed-silence + multi-TF | ⏳ | pin to W1 SHA |
| **M2** determinism + durability | ✅ **LANDED `c4a730d0`** | persisted pendingEntryQueue (new table `paper_pending_entries`, migration 0204, boot re-hydration) + deterministic hash-seeded fill RNG (math untouched, only randomness source) + evidence labels (feed_mode/delay/certified-claims). Grade round 1 = band 7 NOT-SAFE-TO-LAND (3 findings: a false "zero CORE_DDL drift" self-cert claim — real 5th drifted table `lifecycle_transitions.correlation_id` found and fixed; a real coverage gap on the boot-rehydration glue — closed with a RED-proofed behavioral test; an overscoped "same tape twice" replay claim — narrowed to "reconstructible from recorded identity", root cause is the pre-existing H2 per-bar `randomUUID()` correlationId mint). All 3 closed same-wave; re-grade band 8 SAFE-TO-LAND. 90/90 tests (65 new + 1 previously-broken 24-test file unblocked by the sibling CORE_DDL fix), 216/216 parity, 145/145 test:metrics unchanged, 3 CI gates + system-map green. |
| **GATE3** manual/cron precondition parity | ✅ **LANDED `c82ad4e6`** | side-finding from M3 re-scope investigation, verified independently, closed as its own small wave per advisor guidance (not folded into M3). Manual PATCH promotion path never checked the 30-day/1.5-Sharpe precondition the cron enforces — new shared evaluator + shared query function close it for both callers at once. Grade round 1 = band 7 SAFE-TO-LAND with 2 evidence-quality findings (tautological query-parity test, thin audit-row test) closed same-wave despite SAFE-TO-LAND already being issued; round 2 narrow re-check confirmed, band 7 stands. 48/48 tests, gate-parity linter 12/12, 3 CI gates + system-map green. |
| **M3** PAPER authority flip | ✅ **LANDED `91e1870f`** | Ship as ONE atomic "PAPER = internal-engine-only" MVP — new `paper-authority-states.ts` single source of truth replaces 3 duplicated literals; `lifecycle-service.ts`'s TESTING/SHADOW→PAPER stream block inverted (stop→start) with a new sibling stop-on-exit block closing the double-writer hazard at the DEPLOY_READY boundary; `scheduler.ts`'s `detectStalePaperSessions()` gained its first-ever lifecycle-state guards. DEPLOY_READY/PILOT/DEPLOYED byte-unchanged. Parent-review found 10 test failures the implementer's own before/after diff had missed calling out individually; all root-caused via base-commit byte-offset comparison as pre-existing (9 fixed as stale test-fragility, 1 left as an already-documented pre-existing gate bug out of scope). **Independent grade: VERIFIED BAND 7** (not 8 — 2 RED-proofs on genuine safeguards held, but 2 more found real gaps): F-1 CRITICAL (a strict-subset safety test asserted a hand-maintained copy instead of the real live-capital-boundary object — fabricated-safety-claim class), F-2 HIGH (the wave's own headline zero-carry-forward fix had zero behavioral test coverage), F-3 MODERATE (2 new audit rows missing correlation_id) — all 3 closed same-wave with RED-proofed fixes. Zero new regressions across the full 12.4k-test suite (89 failed files after vs 90 before). |
| **W3A** execution hardening | ✅ **LANDED `8191d8f5`** | plan's original item 1 named the WRONG FILE (claimed `paper-execution-service.ts` falls back to `getDailyTradeCapEnvDefault()` — it never does; that function is only used in `paper-signal-service.ts`). Re-investigated against live code before drafting: item 1 is really a kill-switch arming gap (env-default-only sessions never trigger the Python `check_kill_switch` call at all, not just its trade-cap sub-check) with a real regression trap in the obvious fix (naive fallback breaks `TF_MAX_TRADES_PER_DAY=0` debug-disable when a session also has `daily_loss_limit` set) — corrected design uses the existing `resolveEffectiveCap()` helper. Item 3 expanded per "fix the whole class" law: 5 sibling fields share `base_contracts`'s exact silent-fallback shape, folded into one audit-row fix. Grade round 1 = band 6 NOT-SAFE-TO-LAND (Discrepancy F-1: the correct kill-switch-arming fix shifted a mocked call sequence off-by-one in an untouched sibling test file, 6 tests — `feedback_additive_fix_activates_dead_path` extends to test fixtures); closed same-wave; round 2 narrow re-check confirmed, band 7 SAFE-TO-LAND. 95/96 tests (1 pre-existing unrelated), 3 CI gates + test:metrics green. |
| **W3B** prop-firm hygiene | ✅ **LANDED `6415dac5`** | firm_profiles = derived view (4 drifts incl. MFFU 50→40); payout caps wired (/payout + /rank); F-2 prop_compliance 2nd-duplicate synced + cross-dict agreement lock; grade band 7, receipt reproduced bit-for-bit; test:metrics now 145/0 |
| **W4** evidence artifacts | ✅ **LANDED `cef0402d`** | h5 A/B ×3 (MES/MCL re-baseline heavily, MNQ ~unchanged — D2 evidence, flag stays OFF); exit-style inventory: **adaptive cohort EMPTY**, 120 strategies all static_styleC |
| **W5** doc-rot + daily-uncapped | ✅ **LANDED `713d8f88`** (indep. items) | §12 consistency un-staled; §2b 9/9/18; **§5 → $250–$1,000+/day, figures = observed outcomes never quotas/ceilings**; AGENTS ceilings/base fixed. §8 rewrite = M3-gated |
| **W-testdebt** engine test-debt | ✅ **LANDED `55c6ff74`** | 6 grader-handed failures (18 in a_plus_gate) ALL stale tests not code bugs (0 engine source); BE+1-on-TP1 §4 invariant verified intact; 100/100, test:metrics 145/0. Honest gap surfaced: no Python↔TS volume-confirmation parity test |
| **W6** shadow profit governor | ⏳ | pin to M2 SHA `c4a730d0`; observability-only |
| **W7** close-out + wiring audit | ⏳ | campaign-wide end-to-end integration + Go-Live Register doc |

## Execution model (proven on W1)
parent authors/amends ratify packet + SHA-pinned worktree (junctioned node_modules, real tsc) → subagent implements under TDD (RED before GREEN) → parent catches class siblings (zero-carry-forward) → **independent accuracy-validator grade (doer≠grader)** → parent hardens grader findings IN-wave, even ones a SAFE-TO-LAND verdict already excused (GATE3 precedent) → parent final-verify → FF-only land → packet CLOSED + AGENT-LOGS + memory + this doc updated. **No operator hold for any of this** — the grader is the gate, not operator permission (standing protocol, reconfirmed 2026-07-17 after a same-session drift); explicit operator/advisor input is reserved for genuinely irreversible or live-capital decisions only, which nothing in this campaign is yet.

## Known tooling facts
- `tsc` needs `NODE_OPTIONS=--max-old-space-size=8192` (default heap OOMs SIGABRT 134 — NOT a type error).
- `system-map:check` is DRIFTED at baseline (pre-existing "Generated topology stale" + n8n health staleness = audit H-11) — waves verify "no NEW drift", don't attribute it.
- Housekeeping: leftover `wt-f3-bartime` dir (Windows file-lock on removal; git deregistered it) — prune later.
- **`git worktree remove --force` can wipe the shared target `node_modules` mid-command when the worktree's `node_modules` link isn't cleanly unlinked FIRST, as a separate sequential step** (M2 grading session + W3A cleanup, both 2026-07-17) — same underlying hazard as the known junction-`rm -rf` class, but triggered by a git built-in, not a raw shell command. **"Prefer symlinks over junctions" does NOT work as a mitigation** — W3A's `node_modules` was created via the identical `ln -s` command M2's was, yet `rm node_modules` failed with `"Is a directory"` (the junction signature) and the subsequent `git worktree remove --force`, run in the SAME parallel batch as the failed unlink attempt, wiped `.bin/`, `drizzle-orm`, and most of `@types` from the shared tree. Fixed both times with `npm install`. **Actual safe procedure:** test `rm <path>/node_modules` (no `-r`) in isolation first — clean success means true symlink, safe; `"Is a directory"` means treat it as a junction (`cmd //c rmdir` instead) — and NEVER run the unlink step and `git worktree remove --force` in the same tool-call batch; sequence them with a confirmed-successful unlink as a hard gate. Always positively verify (`ls node_modules/.bin/tsc` exists) after ANY worktree teardown — a clean-looking exit 0 from `git worktree remove --force` is not evidence of safety. Full detail: `reference_rm_rf_junction_deletes_target_2026_07_09` memory.
- **A hardcoded "copy" of a real constant inside a test, even with a documented import-side-effect justification, is a fabricated-safety-claim risk — verify the obstacle is real before accepting the copy (M3, 2026-07-17).** `paper-authority-states.test.ts` hand-copied `server-mediated-executor.ts`'s `LIVE_EXECUTION_STATES` value because that file "transitively imports db/index.ts, which throws without DATABASE_URL" — but the actual obstacle was ONE import (`db/index.js`), not the whole dependency tree; a single `vi.mock("../../db/index.js", () => ({ db: {} }))` let the real module load cleanly. The independent grader RED-proofed the hand-copy version by widening the real constant and finding zero test failures anywhere — a live-capital-boundary safety claim that was silently false. Always probe the actual import obstacle directly (a throwaway dynamic-import test) before accepting a "can't test the real thing" test-design compromise.
- **A DB mock whose `.where()` ignores its filter argument is vacuous for any RED-proof that needs to distinguish "query correctly found nothing" from "the mock never looked at the condition anyway" (M3, 2026-07-17).** The `gate3-manual-precondition-wiring.test.ts` table-routing mock pattern (ignore-the-WHERE-clause, route by table identity only) is fine for asserting POSITIVE behavior, but a negative-case test built on it (e.g. "does NOT call X when the session belongs to a different strategy") would pass for the wrong reason — the mock returns the seeded row regardless of the real query's filter. Fix: mock `drizzle-orm`'s `eq`/`and` to return inspectable condition objects `{__op, col, val}`, then have the mock's `.where()` handler compare `col` by REAL OBJECT REFERENCE against the actual imported column (`paperSessions.status`, not a string name guess) — this makes the mock genuinely filter-aware and lets a RED-proof (e.g. typo'ing a status-literal in production) actually flip the test.
