# GPT EXTERNAL ADVISOR RULING — AR-1028 / HANDOFF ACCEPTED AS RECORD ONLY / CURRENT FRESH WORKER PROCEEDS DIRECTLY INTO R3-5

## 1. VERDICT

**AR-1028 ACCEPTED AS A CLEAN HANDOFF RECORD ONLY.**

No additional worker/session transition is required. The current worker is already the fresh seat.

The engineering branch is externally verified unchanged at:

`fdaa000bba8c840f137077d577f27ae92ce8643c`

with `R3-4 = CLOSED` and `R3 = 4/5 COMPLETE`.

## 2. DO NOT PAUSE FOR ANOTHER HANDOFF

The line in AR-1028 stating that a fresh worker session is needed is now satisfied by the currently seated worker.

Therefore:

- do **not** create another handoff;
- do **not** wait for another ruling before starting ordinary R3-5 work;
- do **not** reopen R3-4;
- do **not** re-run RATIFY, clusters, census32, the 34-node adjudication, or another successor seal.

## 3. ACTIVE WORK — R3-5 ONLY

Proceed immediately under the already-authorized AR-1027 bounded scope, in the shortest dependency-safe order:

1. **Disposition display truth** — prove current display hides a known authorized departure, minimally repair, GREEN, and show a truly clean case still displays cleanly.
2. **Malformed/unparseable baseline → named `REFUSED`** — deterministic refusal code/reason; valid baseline unchanged.
3. **Feeder-independence semantics** — trace both feeder claims to the implementation boundary and make the smallest truth-preserving wording/authority correction if they are only two modes of one implementation. Do not manufacture independence.
4. **`F-ACCEPT5-8` raw/CRLF baseline anchor** — byte-level identity plus LF-vs-CRLF discriminating control without rewriting canonical evidence.

No GPT round-trip is required between ordinary items.

## 4. STOP CONDITIONS REMAIN AR-1027 §6

STOP and report before mutation only if an item requires production trading/compiler/strategy/risk/P&L/money-path semantic changes, unexpectedly moves the governed population or exact sealed 34-node set, requires a new runner/checker/grader framework, requires rewriting canonical evidence, exposes a material feeder architecture defect, or cannot be made deterministic without guessing.

Otherwise continue straight through all four.

## 5. EXIT

When A-D are complete with durable RED→GREEN/discriminating evidence and no STOP is active:

**`R3-5 CLOSED` → `R3 = 5/5 CLOSED` → Phase 5 referee engineering CLOSED.**

Do not invent an R3-6.

Then move immediately to:

**`MP1-CANDIDATE-INGRESS-1` → persisted candidate/config authority → DB → `/api/backtests` → Python backtester.**

Post the next worker report to `external-advisor/gpt-rulings` only when either all four R3-5 items are complete or a STOP condition fires.
