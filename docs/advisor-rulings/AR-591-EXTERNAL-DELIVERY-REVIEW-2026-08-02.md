# External GPT review — AR-591

**External opinion only; zero campaign authority. Main advisor owns R-548.**

Plainly: the worker's original nine repairs are real and reproduce on the exact delivered commit. The delivery is nevertheless **not ready for its independent grade** because it committed against a superseded contract and its gate has a new demonstrated false green: an ordinary fixture type regression is labelled an honest miss, the gate prints `PASS`, and the process exits `0`.

## Ruling fields

**RULING ID / TASK ID / DECISION:** external AR-591 review · AR-591 · **REVISE**. Accept the nine-item evidence as reproducible; do not ratify the delivery and do not dispatch the money-path grade yet.

**GRAPH OBJECT:** **NOT ADOPTED.** The graph work remains external candidate work. AR-591 itself correctly says it was not graph-scheduled.

**GRAPH NODE TRANSITION:** none. Recommendation only: keep `P0PC` active; do not advance `P0PG` until the corrected R-546 delivery exists.

**GRAPH FAN-IN / READY SET:** not authoritative before adoption. No graph edge is changed by this review.

**NEWEST AR / TREE / JOIN KEY:** newest report read in full is **AR-591**, commit `1958ba5d61b57ff0215f628bedcb90b3948ceae9`. Verification tree was the isolated detached worktree `C:/Users/tonio/Projects/wt-gpt-review-ar591-20260802` at that exact SHA. The contract join is **ruling commit → report commit ancestry**: `git merge-base --is-ancestor fa876177… 1958ba5d…` exited `0`. R-546 was committed at `03:46:59`; AR-591 at `03:54:33`. Therefore items 10–13 and the six-population partition were part of the report commit's inherited contract, not a later annotation.

## Claims verified

### Reproduced on the exact delivered object

`cd prototypes/p0-vnext-admission` at `1958ba5d`, then:

```text
node run.mjs          -> exit 0
node red-proof.mjs    -> exit 0
node module-tuple.mjs -> exit 0
```

The output independently reproduced:

- like-for-like `44 / 52`, with `missing_ids: []`;
- expanded `50 / 59` and six green neighbours admitted;
- byte-identical `.cts`/`.mts` and `.cjs`/`.mjs` twins separated only by the module-system catcher;
- executed emitted artifacts: ESM top-level `this="undefined"`, CJS positive control `this="object"`;
- the pinned six-file surface hash `56903b2a09c0b0d4085b5b4e6fe2c6978e4aa6db2e364ed5dd6416e158a8d914`;
- clean control green and all registered red-proof classes `11 / 11`.

Those are **MEASURED HERE**. They show the original nine were substantive work, not fabricated output.

### Executable mechanism checked

At `source-admission.mjs:324-330`, every semantic diagnostic returns the single outcome `TYPE_INVALID`. At `run.mjs:55-57`, that becomes `MISS_TYPE_INVALID`. At `run.mjs:206-217`, `MISS_TYPE_INVALID` is absent from `FAILURE_CLASSES`; `run.mjs:229` therefore exits `0` when it is the only defect.

I planted an ordinary fixture-authoring regression in row `35(a)` without changing its expectation:

```text
const plantedFixtureRegression: number = "not-a-number";
```

Measured result on the exact delivery:

```text
35(a) MISS_TYPE_INVALID ... TS2322
GATE: PASS — every enforced class is clean.
MUTATION_EXIT=0
```

The mutation was then removed with `apply_patch`; `git diff` returned empty and the restored control reran at exit `0`. This is a **new demonstrated false green**, not a prose objection.

## Why AR-591 is not the current delivery contract

AR-591 accurately says it delivered R-543 items 1–5 and R-544 items 6–9. But R-546, already in its ancestry, said: **do not restart the nine; add items 10–13 and replace the taxonomy and pre-registration.** The committed object does not do that:

- `run.mjs` has four `miss_type_invalid` references and **zero** references to `surface_invalid`, `fixture_invalid`, `caught_by_typechecker`, or `position_unclassified`.
- The required six populations are not emitted or reconciled.
- `type-value-proof.mjs` is absent from the delivered commit. The implementation has a useful `inTypePosition()` exclusion, but it has neither R-546's same-spelling D/E discriminator nor the required `POSITION_UNCLASSIFIED` fail-closed residual.
- `emitted-freeze.mjs` is absent from the delivered commit, so the per-row source/emitted-behaviour freeze from item 11 is not delivered.
- The design's normative row 54 still says **CJS module-wrapper `this` injection** with the same module green as ESM. AR-591's true container twins contain no injection and are not executed as the row's promised leak/control pair. The generic `typeof this` probe proves container semantics, not the row-54 behaviour contract.
- `34(d-u)` and `54(c)` remain `MISS_TYPE_INVALID`; R-546 required mutation-owned type errors to be reported as `caught_by_typechecker`, while the `52(*)` authoring defects belong in `fixture_invalid` under its binding taxonomy.

The current follow-on files visible in the shared campaign checkout are not consumed here: they are uncommitted, half-written state and are not part of `1958ba5d`.

## Architecture invariants touched

Compiler correctness remains separate from strategy profitability. The prototype still reads no truth ledger or oracle in the reproduced clean run, and no runtime, trading, capital, or production behaviour was changed. This is an instrument correction only.

## Failed or unproven conditions

- **FAIL:** current gate can accept a fixture-authoring semantic regression.
- **FAIL:** R-546 six-population conservation contract is absent.
- **FAIL:** R-546 item 13 row-54 normative correction is absent.
- **UNKNOWN:** whether the in-progress type/value and emitted-freeze work closes those properties; it is not committed and was not graded.
- **UNPROVEN:** the `44 / 52` attribution count remains doer-produced. This external review reproduced it but is not the designated `accuracy-validator` grade.
- **OPEN by design:** dependency-boundary catcher `26(a-c)`, Surface B, G-2, and 140 authority semantics.

## Required corrections

1. Emit and reconcile the R-546 six-population table over both the frozen 52-ID view and the expanded corpus. For the frozen set, the current evidence implies the shape `44 attributed + 3 honest miss + 0 surface-invalid + 4 fixture-invalid + 1 caught-by-typechecker + 0 position-unclassified = 52`; the worker must compute it from row identities, not copy this arithmetic. The expanded table must likewise be computed and disjoint.
2. Make `surface_invalid`, `fixture_invalid`, and `position_unclassified` run-stopping. Add the exact TS2322-style fixture-regression mutation above to the permanent red-proof with a clean control. `caught_by_typechecker` is evidence owned by the compiler and must never be relabelled as 1b-S attribution.
3. Deliver the same-spelling type-only/value-only pair: type-only silent; value-only exclusively `FREE_REF`. Add an unsupported-position mutation that proves `POSITION_UNCLASSIFIED` exists and exits non-zero. No spelling allowlist.
4. Deliver the emitted-behaviour freeze over every edited fixture: source hash, emitted hash, unchanged-emission assertion, and a planted undeclared emit change that stops the run.
5. Correct design row 54 with a dated preservation note. Execute both arms of the **same behaviour-bearing source**, not only a generic `typeof this` probe. The pure container twin remains useful classification evidence but cannot substitute for the leak/control behaviour promised by the matrix row.
6. Re-run the full original nine-item battery after these additions; no previously biting mutation may stop biting.

## Files / scope recommended

Same scope R-546 authorized: `prototypes/p0-vnext-admission/**`, row 54 only in `docs/designs/P0-VNEXT-DESIGN-2026-08-01.md`, and the worker's own AR append. No ledger/oracle read, no production/runtime tree edit, no Gate-B treatment, no deployment.

## Acceptance commands

```text
node run.mjs
node red-proof.mjs
node module-tuple.mjs
node type-value-proof.mjs
node emitted-freeze.mjs
```

Each clean command must exit `0`; each named mutation must exit non-zero for its pre-registered class. The main runner must print the six disjoint populations, their row identities, and sums of exactly `52` and the exact expanded population. After correction, the designated `accuracy-validator` independently re-plants the semantic-regression false green plus R-546's type/value and row-54 attacks.

## Stop condition

Stop if any fixture semantic regression is accepted as an honest miss; any erased type identifier becomes runtime capture evidence; any identifier position is silently classified without a residual; any fixture's emitted JS changes without a pre-registered change; the six populations overlap or fail to conserve; or row 54 is declared corrected without executing the behaviour-bearing source in both containers.

## Recommended next action

Main advisor should issue R-548 as **REVISE / continue P0PC**, not as a grade dispatch. The existing worker seat remains authorized to finish R-546 items 10–13 and this newly demonstrated semantic-regression red path. The money-path grade becomes due only on the corrected committed object.

## Lesson to persist

**A diagnostic gate that treats every compiler error as an honest miss converts fixture corruption into green.** A taxonomy is not reporting decoration; its buckets decide whether the process stops.

**A ruling already in the report commit's ancestry is part of the contract even if the work began before it landed.** Re-read the newest ruling before the delivery commit, not only before the first code write.
