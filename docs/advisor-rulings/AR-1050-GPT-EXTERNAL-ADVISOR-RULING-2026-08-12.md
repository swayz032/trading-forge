# GPT EXTERNAL ADVISOR RULING — AR-1050 / AR-1047 STOP ACCEPTED / AR-1049 BRANCH CORRECTION ACCEPTED / USE CURRENT ENGINEERING PRODUCER AS FORWARD AUTHORITY / REPAIR ONE-VIDEO RISK EXTRACTION FIRST

## 1. VERDICT

**AR-1047 STOP is accepted.** The first measured broken handoff for `sVkmZklJDHI` is upstream extraction: the committed teacher transcript states the stop/wick rule/fixed 2R target, while the measured staging/persisted source artifact omits that risk model.

**AR-1048 is accepted as a second confirmed example for `Qxlu8v_6G3Y`; do not switch to the backup.** The other ten nominated videos remain nominations only, not findings.

**AR-1049 correction is accepted.** My earlier premise that `src/engine/extraction/spec_producer.py` was absent from the engineering branch was wrong. It is present at engineering pin `0bbcabc81ae2ed6350bcda4d8494cff1e618dd81`. The citation failure was real because the GPT branch held an older blob; that has now been corrected for review.

**Do not add one extraction file to `main`.** `main` is not the active engineering authority for this unit and is missing the larger package. A one-file transplant would create presence without wiring. Branch integration is a later repository-management decision, not a compiler-breakthrough dependency.

**Do not launch a broad historical-producer archaeology campaign.** We do not need to discover which old path minted all 40 legacy rows before proving one faithful source-to-engine path.

Instead, for the golden slice, **the current engineering producer at blob `16d9bd288a6e58c21dd28da51f56644458400e7f` becomes the forward producer authority for a NEW regenerated `sVkm` artifact.** The existing 40-row library remains legacy evidence; do not overwrite it and do not claim the new artifact reproduces the historical producer.

## 2. INDEPENDENT GITHUB VERIFICATION

At engineering pin `0bbcabc81ae2ed6350bcda4d8494cff1e618dd81` and on the corrected GPT evidence branch:

- `src/engine/extraction/spec_producer.py` is byte-identical, blob `16d9bd288a6e58c21dd28da51f56644458400e7f`.
- The older GPT-branch producer blob `dbc321a31dba2d175316afec87523f38a13d6e56` and the current engineering producer both stamp the same house-default shape when the exit is untaught:
  `framework_overlay = { exit, exit_source }`.
- Neither of those producer versions emits the reported legacy-library shape `{ sizing: framework_owned, stop: framework_owned, take_profit: framework_owned }`.
- The current producer is explicitly pure/deterministic from a certified `staging_v32` extraction plus certificate into a spec artifact.
- The current producer already has a structural taught-stop input: `strategy_extraction.stop` becomes an `INVALIDATE` condition.
- The producer also READS `strategy_extraction.targets` inside `_untaught_exit`, but its emitted `spec_body` does not serialize a taught target field.

I also independently verified a second downstream defect that the worker report did not fully state:

`src/server/services/spec-onboarding-service.ts` currently defines `SpecArtifactBody` with entry conditions, invalidations, trigger, and optional framework overlay — **no source target field** — and constructs executable strategy config with:

- `stop_loss: { type: "atr", multiplier: 1.5 }`, then
- `applyFrameworkOverlay(...)`, whose documented role in this service is authoritative risk/sizing/exits.

Therefore **fixing extraction alone will NOT make the executable strategy source-faithful on stop/2R.** That is the next adjacent handoff after the extraction RED, not a reason to skip the first link.

The worker's statement that all 40 production rows carry the legacy `framework_owned` shape is worker-measured DB evidence. I have verified that the two reviewed producer blobs cannot emit that shape; I have not independently re-queried the production DB from this desk.

## 3. EXACT NEXT UNIT — ONE-VIDEO FORWARD REGENERATION, FIRST LINK ONLY

Work on the engineering branch only.

### A. Establish the forward-authority receipt BEFORE mutation

For `sVkmZklJDHI`, record these exact anchors in the test/report:

1. raw transcript file/hash;
2. exact teacher spans for stop, wick inclusion, and fixed 2R;
3. pre-repair certified/staging extraction record hash;
4. engineering pin `0bbcabc81ae2ed6350bcda4d8494cff1e618dd81`;
5. producer path + blob `16d9bd288a6e58c21dd28da51f56644458400e7f`;
6. pre-repair producer output `spec_hash` from that exact input.

This receipt is the deterministic replacement for guessing which historical producer made the legacy DB row.

### B. RED first — prove the extraction loss

Build one bounded `sVkm` fixture through the REAL current extraction/certified-record path, not a copied parser and not a hand-authored final spec.

RED must prove:

- the committed transcript contains the source stop/wick/2R spans;
- the current certified/staging extraction record does not carry the corresponding taught stop/target semantics;
- running the current producer from that unmodified record cannot produce a source-faithful risk handoff.

### C. Repair ONLY the extraction/certified-record link

Make the smallest change that causes the REAL extractor/certified-record path for this one video to preserve the teacher-owned risk model using the existing staging vocabulary whenever possible:

- taught stop = the FVG-candle extreme specified by the teacher, with wick inclusion preserved;
- taught target = fixed `2R` / `2:1` risk-to-reward;
- preserve exact raw transcript quote/span authority;
- preserve direction-relative prose/structure without inventing an absolute price or an arbitrary candle index not taught by the source;
- do not substitute ATR, framework default, midpoint, or any other house value.

Do **not** hand-edit a production DB row and do **not** hand-author the final spec JSON.

### D. Regenerate through the CURRENT producer

Feed the repaired certified/staging record into `produce_spec_artifact_from_record(...)` / `produce_spec_artifact(...)` on the same engineering pin lineage.

Required GREEN evidence:

1. the repaired staging record now carries the source stop and fixed-2R target;
2. the producer emits the taught stop as an `INVALIDATE` condition with source evidence;
3. `_untaught_exit(...)` is FALSE for the repaired input — the producer must not stamp `house-default (trader taught none)` for a teacher that taught risk;
4. the output `spec_hash` changes deterministically from the pre-repair hash;
5. rerunning identical input produces byte-identical output/spec hash;
6. all non-risk source fields in the staging record are conserved unless the worker can show a direct causal reason otherwise.

**Important:** because the current spec contract does not serialize the 2R target, this GREEN closes only the FIRST extraction link. Do not call the resulting artifact full-risk-faithful and do not onboard/backtest it yet.

### E. Sharp negative control

At least one discriminator must prove the repair is not transcript-keyword wishful thinking. Acceptable examples:

- same extraction path with the exact stop/target source spans removed or invalidated does NOT mint a taught stop/2R;
- mutate `2R` to another explicit R multiple in the source fixture and show the extracted target value changes accordingly;
- mutate the wick inclusion language and show the extracted stop semantics change accordingly.

The negative must exercise production extraction code, not a test-local copy.

## 4. SAFETY / SCOPE LOCK

Until the next risk-handoff unit is green:

- **NO `sVkm` backtest.**
- **NO onboarding the regenerated artifact into the live strategy library.**
- **NO framework-owned stop/TP substitution for this source.**
- **NO FVG-direction repair yet.** That is downstream of the first broken link.
- **NO broad 40-video re-extraction.** One golden source only.
- **NO DB schema migration.**
- **NO `main` transplant.**
- **NO broad producer-history archaeology.**
- **NO edge/ranking claims.**
- August OOS remains locked.

## 5. BANKED NEXT DEFECT — SOURCE RISK HANDOFF

Once §3 is GREEN, the next unit is already identified and should start immediately after GPT closes this one:

`repaired staging stop/2R -> SpecArtifact taught-risk contract -> onboarding/compiled_spec -> Python execution -> exact source stop + 2R`

The current engineering code proves this lane is not complete:

- target is used by `_untaught_exit` but not serialized into `spec_body`;
- `SpecArtifactBody` has no taught-target field;
- onboarding hardcodes ATR 1.5 stop and then applies framework-owned risk/sizing/exits.

The next repair must reuse an existing risk/exit contract if one already exists elsewhere in the engine. **Do not invent a new generic risk architecture before searching the current production contracts.** If no compatible contract exists, STOP for GPT with the exact minimum schema/consumer delta required.

Only after source risk is transported and executed faithfully should the worker continue to:

`OR side -> close outside -> directional FVG outside same side -> candle-3 close entry`.

## 6. STOP CONDITIONS

STOP and report instead of expanding scope if:

1. the certified/staging extraction schema cannot represent the teacher's stop or R-multiple without inventing a new broad schema;
2. the current extraction path cannot be replayed deterministically for one committed transcript;
3. preserving exact source spans would require relying on the already-known corrupted `evidence` field rather than raw transcript authority;
4. the current producer cannot consume the repaired staging record without a broad rewrite;
5. the repair unexpectedly requires DB migration, `main` branch integration, risk/P&L engine redesign, or changes to paper/live execution;
6. the source's stop-candle identity becomes genuinely ambiguous after reading the exact transcript context — do not guess.

## 7. REPORT BACK

Return one worker report after either:

**A. GREEN:** one deterministic `sVkm` forward-regeneration fixture proves the source stop + fixed 2R survive raw transcript -> certified/staging extraction, and the current producer deterministically emits the taught stop while correctly refusing the house-default-exit provenance stamp; or

**B. STOP:** one of §6 fires, with the exact failing contract and evidence.

If GREEN, I will close this extraction-link repair and authorize the already-identified source-risk handoff next.

**MISSION ORDER:**

`raw sVkm -> repaired extraction -> deterministic forward-produced spec -> source risk handoff -> OR/FVG/entry -> one faithful trade -> OR V1.0 -> backtest -> edge qualification -> paper -> Slumdawg/TopstepX.`
