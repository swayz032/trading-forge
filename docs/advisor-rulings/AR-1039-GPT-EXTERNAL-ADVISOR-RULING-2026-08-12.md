# GPT EXTERNAL ADVISOR RULING — AR-1039 / REAL PRODUCTION ORB FAMILY DISCOVERY ACCEPTED / 16 TEACHERS MUST BE READ INDIVIDUALLY / NO BROAD COMPILER CHANGE YET

## 1. VERDICT

**AR-1038 and AR-1039 are ACCEPTED as discovery/handoff evidence, NOT as executable-strategy certification.**

The engineering branch remains exactly at `0bbcabc81ae2ed6350bcda4d8494cff1e618dd81`; GPT independently compared that pin to `h1-wave4-sealed12-driver` and found them identical. No production/compiler/trading code moved during these reports.

The important correction is now settled:

- the earlier Tier-A fixture population is not the production strategy library;
- the real production library is reported by the worker as 120 strategies / 40 source videos / MES-MNQ-MCL rows;
- the worker's live-DB counts and Railway observations are **worker-local evidence** because this GPT seat cannot independently query that Postgres instance through GitHub;
- the GitHub-proven fact is that the worker reports were read-only and the engineering branch remained unchanged;
- the worker's own corrected enumeration says **16 source videos carry ORB/opening-range teaching language in teacher prose**;
- therefore the next unit must operate on those teacher-specific sources, not on one generic ORB definition and not on the old 13-fixture scan.

## 2. ORB IS A FAMILY, NOT ONE STRATEGY

The operator's correction is adopted as architecture policy:

> **Different teachers teach ORB differently.**

Do not collapse all ORB sources into one universal rule.

For every ORB teacher, preserve separately:

`source/video → market-of-origin metadata → opening window → exact range construction → breakout observation → confirmation/retest requirement → direction rule → entry price/timing → invalidation → source stop/target statements → framework-owned overlay boundaries`.

Examples of differences that MUST remain distinct when the source differs:

- 5m vs 15m vs 30m opening window;
- candle-close breakout vs wick/high-low penetration vs other explicit observation;
- immediate breakout entry vs retest entry;
- confirmation candle required vs not required;
- long/short determined by breakout direction vs source-declared one-sided strategy;
- OR-width target vs fixed-R target vs framework-owned target;
- source stop relative to OR level vs framework-owned risk stop.

A compiler normalization may share reusable typed primitives, but it may not erase teacher-specific semantics.

## 3. MARKET-OF-ORIGIN IS METADATA, NOT A BAN

Supersede any reading of AR-1036 that implied an equities- or forex-taught strategy may not be tested on futures.

Correct contract:

1. Preserve what the teacher actually demonstrated as `source_market_scope` / provenance.
2. A separate Trading Forge **cross-market transfer candidate** may test the same deterministic strategy mechanics on MES, MNQ, MCL, or another market.
3. The transfer result must be labeled as a Trading Forge generalization/transfer experiment, not falsely presented as proof that the teacher taught that market.
4. Market transfer never repairs missing trigger semantics. An ambiguous source remains ambiguous until another source/evidence span resolves it.

Therefore an otherwise-clear ORB taught on stocks or forex is NOT disqualified from futures testing merely because of its origin market.

## 4. AR-1038/1039 LOAD-BEARING DISCOVERY

AR-1038 first under-counted ORB by searching the compiler's existing type labels, then corrected the enumeration using teacher prose. That correction matters because enumerating by the classifier under examination asks the classifier to define the population.

The final worker report states:

- 40 production source videos were scanned;
- 16 contain opening-range teaching language in teacher prose;
- teacher wording is preserved in `compiled_spec.spec.entry_conditions[].evidence` either inline or as a transcript-span reference;
- `object` is only the short semantic label and must NOT be treated as the teacher's full wording;
- inline evidence exists widely and transcript-span references remain for the rest.

The next worker must therefore **read `evidence`, not `object`**, and preserve source order using `span`.

## 5. PRIORITY LEADS — BUT DO NOT STOP AT FOUR IF THEY FAIL

Start with the highest-information leads already identified by the worker:

1. `deymRD3kSD0` — generated strategy name indicates range breakouts with confirmation. This is only a lead until its source evidence is read.
2. `oDLt9zh33LE` — inline evidence includes a reported sentence describing entry at candle close, stop at candle low, target 2R. The worker correctly did NOT yet prove that sentence belongs to this ORB strategy's operative trigger rather than surrounding commentary.
3. `c8VLqF0XDR4` — ORB / first-30-minutes wording missed by type-based enumeration.
4. `e5HQXYBUW-Q` — compiler already sees an opening-range condition despite a generic generated strategy name.

Read these first because they may resolve the money-path blocker quickly.

**If none yields a faithful executable setup, continue through the remaining ORB teachers until all 16 are dispositioned.** Do not revert to the old two-source count.

## 6. TRANSCRIPT RECOVERY CONTRACT

For each candidate:

1. sort conditions/evidence by source span/order;
2. read all relevant inline `evidence` around the setup, not isolated trigger phrases;
3. where `evidence` is a `T-<video>-C<n>` reference, locate the authoritative transcript store before declaring the rule missing;
4. GPT searched the GitHub tree for the example transcript reference/video and did not resolve a repository transcript file, so **GitHub non-discovery is not evidence of transcript absence**;
5. use the live production data/transcript source available to the worker if needed;
6. if a span truly cannot be recovered, mark that fact explicitly instead of filling it from common ORB practice.

No OCR, web reconstruction, or generic trading knowledge may substitute for a missing source span when making a source-faithful claim.

## 7. REQUIRED PER-TEACHER MECHANICS TABLE

For each ORB source inspected, record only the fields needed to choose the fastest faithful candidate:

`video/source id | source market | OR window | range-high/low construction | exact breakout observation | confirmation/retest | direction rule | entry rule | source stop | source target | transcript evidence status | compiled trigger disposition | load-bearing parameters survive? | faithful executable yes/no | blocker`

The table is a bounded engineering artifact, not a new scanner subsystem.

Important distinctions:

- `NOT STATED` is not the same as `OPTIONAL`.
- `SOURCE_AMBIGUOUS` is not the same as compiler vocabulary missing.
- A source-defined entry using no extra confirmation is valid only if the source wording actually says enough to make the observable event deterministic.
- If the source says candle close, that is a different strategy from a source that says wick/high penetration.

## 8. NO BROAD APPROXIMATION OR PARAMETER POLICY CHANGE YET

AR-1037's fixture scan found approximation/parameter-loss concerns. Those remain useful warnings, but they were measured on a disjoint fixture population.

**Do not yet:**

- globally refuse every approximated trigger;
- rebuild the whole parameter channel;
- change `_derive_entry_eligibility` across the library;
- invent a universal ORB breakout primitive;
- alter the accepted OR candidate/adapter machinery merely because one teacher's source was ambiguous.

First measure the real production ORB teachers.

If the best real ORB source has a fully explicit setup but the compiler drops a taught number or binds its exact trigger only to an approximation, then report the **specific smallest broken semantic link** for that source. GPT will authorize the narrow repair needed for the real candidate rather than preemptively changing all strategies.

## 9. SELECTION RULE

Select the first candidate that satisfies all of these without invention:

1. source evidence is recoverable enough to state the setup deterministically;
2. opening window/range definition is explicit;
3. trigger observation is explicit;
4. confirmation/retest status is explicit or clearly absent by source structure, not guessed;
5. direction is source-owned or mechanically determined by the source trigger;
6. all load-bearing taught numbers/levels survive to execution, or the exact missing channel is narrow and repairable;
7. no unrelated proxy silently decides the entry;
8. shortest path to one real end-to-end trade wins.

Do **not** choose based on expected profitability yet.

## 10. ONCE A WINNER IS FOUND

Continue straight through unless a STOP fires:

`teacher evidence → compiled spec → persisted config/candidate as applicable → /api/backtests → Python → real trigger False→True → wrong/near-miss stays False → one framework-executed trade → trade receipt`.

First use deterministic bars/replay to prove semantics. Then run the smallest real historical backtest needed to prove the money path executes.

After that, and only after that, begin edge qualification.

## 11. STOP CONDITIONS

STOP/report before mutation if:

- the source meaning requires choosing close-vs-wick/retest/tolerance/hold semantics the teacher never chose;
- transcript evidence needed for the setup cannot be recovered from the authoritative store;
- the clearest source requires a new compiler semantic rather than wiring an already-defined one;
- the compiler loses a source number/level and repairing it would require a broad parameter-system redesign rather than one bounded channel;
- executing the setup requires an unrelated direction proxy or another non-source semantic;
- a proposed fix changes production trading/risk semantics outside the selected source's measured gap.

Routine RED tests for a narrow authorized wiring repair are not STOPs.

## 12. HANDOFF / NEXT REPORT

AR-1039's worker explicitly handed off at a clean boundary. A fresh worker may start directly from this ruling; do not re-derive the Tier-A history.

Report when either:

- one real production ORB teacher has a complete deterministic mechanics table and is selected for execution; preferably continue through the first real trade if the remaining fixes are bounded and already authorized by the rules above;
- all 16 ORB teachers are dispositioned and none is faithfully executable;
- one STOP condition fires.

**FAST PATH:**

`real 16-teacher ORB family → read actual evidence/transcript spans → pick clearest deterministic teacher-specific ORB → repair only its measured missing link → one real money-path trade → edge qualification`.
