# AR-1111 (worker) — AR-1110 **STEP A ONLY**. IDENTITY CLEANUP + CROSS-SOURCE GUARD LANDED AND RED-PROOFED. **B–F NOT STARTED, AND I AM NOT IMPLYING OTHERWISE.**

**Governing:** AR-1110 (GPT) §6, §11.A
**Implementation commit:** `76212f65d1ebf3cee3053bf9c01407c5f288ba67`
**Inventory commit (pre-push gate):** `468d286d5daa7b5eaa368f4f0a9a484448643c38`
**Remote head:** `468d286d5daa7b5eaa368f4f0a9a484448643c38` `[MEASURED: ls-remote + GitHub REST per-object]`
**Scope:** `src/engine/backtester.py` · `src/engine/tests/test_source_vertical_join.py` ·
`src/engine/tests/test_source_band_c_vertical.py` · new
`src/engine/tests/test_cross_source_provenance_guard.py` · regenerated `SYSTEM-INVENTORY.md`

🛑 **THIS IS ONE OF SEVEN STEPS.** `§11.B` (role carrier), `C` (persistence + Band C
consumption), `D` (§7 A–F discriminators), `E` (acceptance), `F` (independent DISPROVE grade)
are **UNSTARTED**. No timeframe role is persisted or consumed yet. **Nothing here entitles
anyone to a performance run.**

---

## 1. YOUR §2.3 CATCH WAS RIGHT, AND THE DEFECT WAS IN **TWO** FILES

**You named `test_source_vertical_join.py`. `[MEASURED HERE]` the same `svkm-*` identity was
restated independently in `test_source_band_c_vertical.py` — 5 sites across 2 files:**

```
test_source_vertical_join.py:91    SPEC_ID   = "svkm-source-vertical__s0"
test_source_vertical_join.py:158   spec_hash = "svkm-vertical-fixture"
test_source_band_c_vertical.py:70  SPEC_HASH = "svkm-vertical-fixture"
test_source_band_c_vertical.py:97  source_spec_id = "svkm-source-vertical__s0"
test_source_band_c_vertical.py:154 "name": "svkm-source-vertical"
```

**And that is my miss, not yours.** AR-1109 §2 told you the fixture was the wrong teacher's
lesson — **I never opened its `SPEC_ID`.** You did, and you were right to upgrade it from a
citation problem to a provenance defect. The band-C file now **imports** the identity instead of
restating it, so the next rename cannot leave a stale twin.

## 2. DISPOSITION — YOUR OPTION **2**, BECAUSE OPTION 1 IS NOT AVAILABLE

You forbade guessing the owning lesson. `[MEASURED HERE]` the fixture's own committed
`source_quote`, **"the first 15 minute range"**, appears in **ZERO** of the 40 archived
transcripts — as do `first 15 minute range` and `fifteen minute range`. Controls both
directions: `'one minute time frame'` → **5** transcripts, a nonsense sentence → **0**.
Two transcripts contain the different string `15-minute range`, and **attributing on a 3-word
substring is exactly the guess you forbade**, so I did not.

⇒ **The fixture is now explicitly synthetic:** `synthetic-or-fvg-conformance__s0`, no
`svkm-*` id, no source-video claim, and the teacher quote replaced by a string that says it is
synthetic. Its module docstring now carries the whole history and the boundary — **its
behavioural coverage is unchanged and still load-bearing; what it may never again be cited as is
evidence about what a teacher said.**

## 3. THE GUARD — ANCHOR (4) AT THE CONSUMER BOUNDARY

`[MEASURED HERE]` the three existing anchors in `resolve_row_for_execution` prove
receipt ↔ row ↔ payload **and never look at the spec being executed**. There is already a
producer-side identity guard (`spec_producer.py:1001`, `R-778 §4`) — **the consumer had none.**

So `backtester.resolve_candidate_authority` gains anchor (4): **this run's
`compiled_spec.spec_hash` must BE the parent that certified the candidate**, else a named
refusal. Fail-closed on an empty hash, because absent is what a stripped artifact looks like.

**RED-PROOF, run in an ISOLATED WORKTREE — the shared tree was never mutated
(`MUTATION-HARNESS-ATOMICITY-1`):**

| tree | guard present | result |
|---|---|---|
| `wt-basetree-tmp` @ `1c8f554f` | **no** (`grep -c` = 0) | **3 failed / 4 passed** |
| working tree @ `76212f65` | yes (`grep -c` = 1) | **7 passed** |

The three that go red are exactly the three refusal assertions. The four that pass in **both**
trees are the ones that must: the **honest-config positive witness** (proving the guard does not
simply refuse everything), the **legacy no-sidecar path**, the **two-lessons-really-differ** pin,
and the **ablation**.

**The ablation is the part I would want you to attack first.**
`test_the_old_three_anchors_ACCEPT_the_swap` calls `resolve_row_for_execution` **directly and
unmodified** on the swapped pair and asserts it happily returns lesson A's 15-minute candidate.
**That is the defect reproduced on demand by the untouched instrument** — it is why I can
attribute the red to anchor (4) rather than to my own fixture.

**Discriminators, against your §6 list:** (1) two identities differing in a money-deciding fact
(15m vs 5m range) ✅ · (2) A's evidence under B's identity ✅ **and B-under-A**, because
*an assertion that only forbids one of the two wrong answers is satisfied by the other one* ·
(3) the path refuses ✅ · (4) mutation control ✅.

## 4. REGRESSION

```
206 passed  —  source vertical join · band C vertical · new guard · MP1 ingress ·
               MP1 candidate persistence · source-faithful execution mode ·
               source-faithful FVG routing · source trade population ·
               opening-range definition / candidate / lowering
```

**§7.F satisfied:** the `R-736`/`R-743` opening-range suites are **untouched and green** — all
taught alternatives preserved, none chosen.

**The rename is behaviour-neutral, proven by MEMBERSHIP not by count:** baseline tree vs working
tree, `--collect-only` node-ids, **53 vs 53, `diff` empty in both directions.**

## 5. WHAT I GOT WRONG OR COULD NOT CLOSE — ALL OF IT

1. **I tried the stricter guard and it was WRONG.** Refusing a candidate sidecar that arrives
   with **no** inline `compiled_spec` turned **3 real MP1 ingress controls RED**
   (`test_control_1_a_real_candidate_row_is_proven_and_proceeds` + 2). The production ingress
   route legitimately proves a candidate **before** an inline spec exists. **Reverted, and the
   reason is committed in the code.** I am reporting the failed attempt because you cannot judge
   the boundary without it.
2. 🛑 **RESIDUAL, NOT CLOSED:** anchor (4) binds **only** when the run carries an inline
   `compiled_spec`. A candidate-aware run with none is still unguarded by it. Closing that
   belongs to the ingress layer that owns the pairing. **`ONE BOUNDARY CLOSED, NOT THE CLASS`**
   (`[instance-not-condition]`).
3. ⚠️ **TWO FRESHNESS INSTRUMENTS DISAGREE, and I did not paper over it.**
   `system_inventory.py --check` reported **FRESH (exit 0)** before *and* after regeneration,
   while the **pre-push hook REFUSED the push** and the regenerated file genuinely differs
   (`files skipped as tests 379 → 380`, `LOC 123279 → 123323`). **The hook is the stricter
   instrument and it was right.** I have not diagnosed `--check`'s blind spot — flagging it
   because `--check` is cited across this campaign as prior-art evidence.
4. **I did not grade any of this.** Your §11.F independent DISPROVE pass is unstarted; per
   `0-CTRL.2` it is pre-authorized and I will dispatch it — **after `B`–`D` exist**, because
   grading a guard while the unit it belongs to is half-built wastes the pass.

## 6. STATUS

**`§11.A`:** **COMPLETE** — identity cleanup (2 files) + cross-source provenance guard, red-proofed.
**`§11.B`–`F`:** **NOT STARTED.** No timeframe-role carrier exists; the `0.4`-confidence scalar is
still what SOURCE_FAITHFUL would consume.
**`R-736`/`R-743`:** preserved, green, not re-adjudicated.
**Performance:** not run, not authorized, and not closer than your §9 gate says.
**Next in your order:** `§11.B` minimal versioned timeframe-role carrier.
