# GPT External Advisor — Compiler V1 Role-Skill Publication

**Date:** 2026-08-08
**Decision:** **APPROVE the role-skill package for adoption.** This approves
agent-process hardening; it does **not** declare V1.0, V1.1, or a trading-ready
strategy complete.

```text
RULING ID / TASK ID / DECISION: EXT-V1-SKILLS-2026-08-08 / Compiler V1 role skills / APPROVE
GRAPH OBJECT: NOT ADOPTED by this change; graph constraints are process rules only
GRAPH NODE TRANSITION: NONE
GRAPH FAN-IN / READY SET: NONE; no execution-graph state changed
CLAIMS VERIFIED: exact branch identity, nine-path scope, four skill validators, hashes, mirrors, triggers, adversarial behavior
EVIDENCE INDEPENDENTLY CHECKED: yes — fresh accuracy-validator plus post-log blob confirmation
TESTS RERUN: quick_validate.py ×4; hash equality; path-set equality; X/Y replay; two novel false-green attacks
ARCHITECTURE INVARIANTS TOUCHED: agent authorization and evidence flow only
FAILED OR UNPROVEN CONDITIONS: frozen ORB identity, binding state, V1.0 conformance, V1.1 batch remain unmeasured here
REQUIRED CORRECTIONS: none to the published role-skill object
FILES / SCOPE ALLOWED: adopt the nine published paths; local role-pointer installation remains separately disclosed
ACCEPTANCE COMMANDS: use the receipt's validator/hash/path commands and require the published SHA
STOP CONDITION: resolve and record the frozen ORB authority identity, then stop before binding measurement
LESSON TO PERSIST: width is allowed only across real independent edges; the compiler breakthrough stays vertical
```

**Newest AR check:** the relay branch's newest worker report is AR-524. It does
not bear on this independently built role-skill package, and no premise from it
is used below.

## Object reviewed

- Published branch: `docs/role-skills-20260808`
- Published commit: `ab6ca7c358497e52aaa195b3f098f27a91a96eaf`
- Audited skill commit: `31c53532f0fb40914b38cc042b21a9f1ac5b7813`
- Base: `0c57c86b8ce6456ede77a0a54502de8de5c6e3dc`
- Evidence: `docs/superpowers/evidence/2026-08-08-v1-worker-advisor-skills-receipt.md`

The final commit changes only the log and evidence receipt relative to the
audited commit; all four audited `SKILL.md` blobs are unchanged.

## Claims independently checked

1. The branch contains exactly nine intended tracked paths.
2. All four new skills pass `quick_validate.py` and remain below 500 words:
   `vertical-slice-breakthrough`, `critical-path-campaign-manager`,
   `source-to-engine-conformance`, and `batch-disposition-integrity`.
3. Canonical, Codex, and Claude runtime copies are byte-identical on this
   workstation. Worker/advisor onboarding pairs also match their recorded
   hashes. The branch truthfully says those role pointers are local runtime
   installation, not content a clean clone receives.
4. A fresh X/Y adversarial replay retained four separate rows—the unverified
   zero-binding claim, guard, CI, and audit—authorized authority resolution
   only, and stopped before binding measurement.
5. Novel attacks changing evidence bytes behind a stable evidence ID and
   duplicating manifest membership were both quarantined.

Independent validator `/root/final_publication_audit_retry` returned
`SOUND-TO-PUBLISH`. A follow-up confirmed the log-only closeout did not alter
the audited skill blobs.

## Architecture position

The package correctly uses graph engineering only for genuinely independent,
read-only lanes with pinned inputs, explicit fan-in, and missing-lane refusal.
It does not turn the compiler breakthrough into a wide graph. The serial money
path remains the frozen real strategy's first failed condition.

## Current exit and authorization

```text
CURRENT EXIT: frozen st5e-YJRfKc__s0 / opening_range_breakout reaches its first measured failed handoff, then one complete source-to-engine V1.0 path.
CRITICAL PATH: identify the exact ratified frozen ORB artifact before measuring its binding state.
AUTHORIZED NOW: main advisor seat adopts the published role-skill package; worker seat then performs one bounded read-only authority-register lookup and records the exact frozen artifact identity and pin.
PRECEDENCE PROOF: measuring an unidentified ORB can measure a candidate copy and fabricate the causal trace.
DEFERRED REGISTER: ORB-BINDING-001 | worker seat | binding count and exact first-failed-condition trace on the pin-resolved artifact | authority identity is accepted | current zero-binding statements remain claims until tied to the frozen artifact and command output.
WAKE TRIGGERS: after identity resolution, authorize the smallest read-only binding census; after a measured zero/partial result, trace the first load-bearing condition through source → extraction → canonical meaning → binding → engine capability.
STOP: stop after identity resolution before binding measurement; escalate on missing, ambiguous, or inaccessible authority.
```

Guard, CI, audit, and broad tooling findings remain deferred unless measured
evidence proves they block that trace or invalidate its receipt. They are not
discarded; each must retain its own owner, acceptance test, wake trigger, and
evidence row in the main desk's register.

## Unproven conditions

- No compiler, engine, evaluator, database, n8n workflow, or trading behavior
  changed in this package.
- The frozen ORB artifact identity and current binding count were not measured
  by this package.
- Therefore the compiler breakthrough has **not** occurred yet.

## Required next action

The main advisor should consume this notice, adopt the package from
`docs/role-skills-20260808`, and keep the worker's next action narrowly on the
authority-pinned ORB identity. Do not substitute a new strategy, a hand-built
binding, or a broader guard census.
