# AR-1021 — WORKER — 30 / 35 · A TEST HELPER THAT SILENTLY HALVES ITS OWN DATA · 1 SIBLING REFUSES TO BE LUMPED IN

```
RULING : AR-1019 ruling §4 priority 1 (jitter battery). STOP §5[3] from AR-1020 still
         OPEN and still blocking census/seal -- not proceeding past it.
PIN    : c59ee2a37a34f51e419166371fd3da523bef3595
STATUS : 30 of 35 · 5 remain · nothing repaired · no census, no seal.
```

## 1. NEW — `test_stable_monthly_returns_low_rws` — `TEST_CONTRACT_DEFECT`

**Failure:** `assert rws_out["n_windows"] >= 3` → got `2`.

**Causal root — the test helper destroys half its own input.** `_make_base_result` builds the
monthly-returns dict at `:50`:

```python
"monthly_returns": {f"2024-{(i % 12) + 1:02d}": r for i, r in enumerate(monthly_returns)},
```

**`(i % 12) + 1` repeats after 12 months, so a 24-month list collapses into 12 unique keys** — the
second year overwrites the first, silently.

`[MEASURED HERE]`, running the helper's exact expression and then production `compute_rws`:

```
24 values in -> 12 UNIQUE KEYS out   (12 lost to key collision)
   with collision : n_windows = 2     <- the observed failure
   list form, no collision, same 24 values : n_windows = 4
production compute_rws on a plain 24-month list: 12mo->2, 18mo->3, 24mo->4, 30mo->5
```

**The production windowing is arithmetically correct** (`n // window_months`, consecutive
non-overlapping, matching its own docstring). **The fixture is the defect.** Production not
implicated; no repair required.

★ **`A DICT COMPREHENSION IS A SILENT DEDUPLICATOR. THE TEST ASKED FOR 24 MONTHS, GOT 12, AND
NOTHING ANYWHERE SAID SO.`**

## 2. NOT CLASSIFIED — `test_rws_failure_blocks` — AND I AM NOT LUMPING IT IN

**Failure:** `assert out["rws"] > DEFAULT_RWS_THRESHOLD` → got `0.0` vs `0.2`.

It uses the **same** helper and the **same** collision, so the obvious move is to file it under §1.
**Measured, that is wrong:**

```
volatile 24 months through the collided dict -> compute_rws rws = 3.22e16   (NOT 0.0)
the test observes, through run_b15_battery   ->              rws = 0.0
```

⇒ **The collision does not explain this one.** `compute_rws` returns an enormous value directly;
something inside `run_b15_battery` yields `0.0` instead. **Root not yet located ⇒ NOT CLASSIFIED.**
Filing it as "same root" would have been a clean-looking wrong answer.

⚠️ **Adjacent observation, recorded not classified:** those window Sharpes are `±2.28e16` — a
degenerate zero-variance division on constant synthetic returns. It may be relevant to the `0.0`,
or a separate robustness question. **Not investigated, not claimed.**

## 3. TALLY

```
30 CLASSIFIED
   18  a_plus_gate_parity            TEST_CONTRACT_DEFECT
    3  pnl golden fixtures           TEST_CONTRACT_DEFECT   (intrabar stop-breach guard)
    2  production_hardening_g2a_g2b  TEST_CONTRACT_DEFECT   (asserts on source text)
    2  accuracy_fixes                TEST_CONTRACT_DEFECT   (stale rate · empirical constant)
    2  session_role fence            INTENTIONAL_NEGATIVE   (strict xfail)
    1  pnl commission formula        TEST_CONTRACT_DEFECT   (monotonic fixture)
    1  jitter n_windows              TEST_CONTRACT_DEFECT   (key collision)  <- NEW
    1  jitter equity_curve_fallback  PRODUCT_OR_ENGINE_DEFECT  🛑 STOP, awaiting GPT
 5 REMAINING
    1  jitter rws_failure_blocks     root NOT located (§2 above)
    1  apply_trade_management        trail_stop 3991.0 vs BE 4000.0
    1  e2e walk_forward_mode         0 windows, expected 3
    1  three_fixes max_dd            short by 0.0000000000038 vs a 0.01 tolerance
    1  wave_b_intrabar_stops         exit bar 3, expected 5
```

**Nothing is `UNEXPLAINED` yet** — 4 of the 5 are simply **not yet examined**; the 5th (§2) is
examined and unresolved, and will become `UNEXPLAINED` if one more bounded pass does not locate it.

## 4. STANDING

The `AR-1020` STOP (production off-by-one at `parameter_jitter_battery.py:416-422`) is **open and
unanswered**, and per the `AR-1019` ruling §5 no census, seal or canonical run is authorized while
it stands. **I am not approaching any of them.** Continuing only the remaining disposition.
