{
  "name": "TF Health Watchdog — auto-restart + Discord alert",
  "nodes": [
    {
      "id": "cron-60s",
      "name": "Every 60s",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.3,
      "position": [0, 0],
      "parameters": {
        "rule": {
          "interval": [
            { "field": "seconds", "secondsInterval": 60 }
          ]
        }
      }
    },
    {
      "id": "http-health",
      "name": "GET /api/health",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [240, 0],
      "parameters": {
        "method": "GET",
        "url": "https://tf-relay-production.up.railway.app/api/health",
        "options": {
          "timeout": 5000,
          "response": { "response": { "fullResponse": true, "neverError": true } }
        }
      }
    },
    {
      "id": "code-track",
      "name": "Track failures (workflow staticData)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [480, 0],
      "parameters": {
        "language": "javaScript",
        "jsCode": "// Wave 26 Pass H.4 — health watchdog state machine\nconst staticData = $getWorkflowStaticData('global');\nstaticData.consecutiveFails ??= 0;\nstaticData.lastRestartAt ??= 0;\nstaticData.lastDiscordAt ??= 0;\n\nconst resp = $input.first().json;\nconst status = resp.statusCode ?? resp.status ?? 0;\nconst isHealthy = status === 200 && resp.body?.status === 'ok';\nconst now = Date.now();\n\nlet action = 'noop';\nlet detail = `health_status=${status} healthy=${isHealthy}`;\n\nif (isHealthy) {\n  if (staticData.consecutiveFails > 0) {\n    detail += ` recovered_after=${staticData.consecutiveFails}_fails`;\n    action = 'recovered';\n  }\n  staticData.consecutiveFails = 0;\n} else {\n  staticData.consecutiveFails++;\n  detail += ` consecutive_fails=${staticData.consecutiveFails}`;\n  // 3 consecutive fails → trigger self-restart (cool down 5 min between restart attempts)\n  if (staticData.consecutiveFails >= 3 && (now - staticData.lastRestartAt) > 5 * 60 * 1000) {\n    action = 'restart';\n    staticData.lastRestartAt = now;\n  }\n  // 5+ min unhealthy AND haven't pinged Discord in 15 min → alert operator\n  if (staticData.consecutiveFails >= 5 && (now - staticData.lastDiscordAt) > 15 * 60 * 1000) {\n    action = action === 'restart' ? 'restart_and_alert' : 'alert';\n    staticData.lastDiscordAt = now;\n  }\n}\n\nreturn [{ json: { action, detail, consecutiveFails: staticData.consecutiveFails, healthBody: resp.body ?? null } }];"
      }
    },
    {
      "id": "switch-action",
      "name": "Switch on action",
      "type": "n8n-nodes-base.switch",
      "typeVersion": 3,
      "position": [720, 0],
      "parameters": {
        "rules": {
          "values": [
            { "outputKey": "restart",          "conditions": { "options": { "caseSensitive": true, "typeValidation": "loose" }, "conditions": [{ "leftValue": "={{$json.action}}", "rightValue": "restart", "operator": { "type": "string", "operation": "equals" } }] } },
            { "outputKey": "alert",            "conditions": { "options": { "caseSensitive": true, "typeValidation": "loose" }, "conditions": [{ "leftValue": "={{$json.action}}", "rightValue": "alert", "operator": { "type": "string", "operation": "equals" } }] } },
            { "outputKey": "restart_and_alert","conditions": { "options": { "caseSensitive": true, "typeValidation": "loose" }, "conditions": [{ "leftValue": "={{$json.action}}", "rightValue": "restart_and_alert", "operator": { "type": "string", "operation": "equals" } }] } }
          ]
        },
        "options": { "fallbackOutput": "none" }
      }
    },
    {
      "id": "http-restart",
      "name": "POST self-restart (HMAC)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [960, -120],
      "parameters": {
        "method": "POST",
        "url": "https://tf-relay-production.up.railway.app/api/admin/self-restart",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ (() => { const ts = Math.floor(Date.now()/1000); const reason = 'watchdog_3_consecutive_fails'; const crypto = require('crypto'); const sig = crypto.createHmac('sha256', $env.ADMIN_RESTART_HMAC_SECRET || 'unset').update(`${ts}:${reason}`).digest('hex'); return JSON.stringify({timestamp: ts, reason, _sig_for_header_below: sig}); })() }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "X-Restart-Signature", "value": "={{ require('crypto').createHmac('sha256', $env.ADMIN_RESTART_HMAC_SECRET || 'unset').update(`${Math.floor(Date.now()/1000)}:watchdog_3_consecutive_fails`).digest('hex') }}" }
          ]
        },
        "options": { "timeout": 10000, "response": { "response": { "neverError": true } } }
      }
    },
    {
      "id": "http-discord",
      "name": "POST Discord alert",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [960, 120],
      "parameters": {
        "method": "POST",
        "url": "={{ $env.DISCORD_OPS_WEBHOOK || 'https://tf-relay-production.up.railway.app/api/admin/discord-fallback' }}",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\n  \"content\": \"🚨 TF API unhealthy — \" + $json.consecutiveFails + \" consecutive failures. Watchdog has restarted but service still down. Manual intervention may be needed.\"\n}",
        "options": { "timeout": 10000, "response": { "response": { "neverError": true } } }
      }
    }
  ],
  "connections": {
    "Every 60s":                   { "main": [[{ "node": "GET /api/health",            "type": "main", "index": 0 }]] },
    "GET /api/health":             { "main": [[{ "node": "Track failures (workflow staticData)", "type": "main", "index": 0 }]] },
    "Track failures (workflow staticData)": { "main": [[{ "node": "Switch on action",  "type": "main", "index": 0 }]] },
    "Switch on action":            { "main": [
      [{ "node": "POST self-restart (HMAC)", "type": "main", "index": 0 }],
      [{ "node": "POST Discord alert",       "type": "main", "index": 0 }],
      [{ "node": "POST self-restart (HMAC)", "type": "main", "index": 0 }, { "node": "POST Discord alert", "type": "main", "index": 0 }]
    ]}
  },
  "settings": {
    "executionOrder": "v1",
    "saveManualExecutions": true,
    "saveDataErrorExecution": "all"
  }
}
