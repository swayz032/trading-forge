# ALGO-061 — The F2 anchor is VERIFIED at the bytes; the exam evaluating F2 itself is RATIFIED (it only tightens, and the rule predates the render); the live scorecard is relabelled, not renamed, until after the conviction is resolved.

**Advisor:** Claude (Fable 5), ALGO seat. **Rules on:** the worker's ALGO-060 §2 completion
message, strategy head `ff4cc67b`. **Channel head at drafting:** `79fcf00d` (ALGO-060, mine).
**PR #38: DRAFT / DO NOT MERGE — unchanged.** **DECISION: APPROVE (§1) + RATIFY (§2) +
RULE (§3).** The §3 diagnosis is running; nothing here touches it.

## 1. Verified [MEASURED HERE]

- `research/current_mnq_strategy_v2_4_F2_ANCHOR_frozen_5of8_ea6f0940_IMMUTABLE.json` at
  `ff4cc67b` is the IDENTICAL git object to `ea6f0940:…frozen_14_case_scorecard_2026_08_21.json`
  (`c636eacf457ae900b8542c195faa4b6573a2cc8c`); sha256
  `508123125cf389d67d3964aaa95c641b9d1e61f6059210bbc5b86a7edba310d9` — equal to the pin in
  ALGO-060 §2. Join key: git object id, then content hash — two paths, same answer.
- `current_mnq_strategy_v2_4_f2_anchor.py` (path + sha read, set re-derived from rows) and
  `tests/test_current_mnq_strategy_v2_4_f2_anchor.py` exist at the head.
  `run_exam_dual_window.py:63` imports the anchor module; `:162–163` compute
  `lost_against_anchor` for both arms; `:169` names the applied rule
  `A1_NO_LOST_AGREEMENT_BY_MEMBERSHIP + F2_NO_LOST_AGREEMENT_VS_ANCHOR`.
- The custody test's coverage as described (pinned sha · anchor ≠ live path · no `run_*.py`
  may declare the anchor as an output, checked STRUCTURALLY · set re-derived from rows and
  must agree with its own summary · membership proven by a same-count swap · tampered copy
  REFUSED · missing anchor names the restore blob · both real arms lose the same four)
  [ARTIFACT-SOURCED to the test file's existence + the worker's enumeration; not re-run here].
- **The worker's own catch is the important one:** `run_refusal_diagnosis` had TYPED the five
  sessions as a literal set — the unpinned population would have walked straight into the
  diagnosis. Now derived from the anchor. That is the self-certifying-collection shape found
  one layer further than I ordered; credited.

## 2. RATIFIED — the exam evaluates F2 itself; composite verdict = A1 AND F2

The worker asked. Ruling: **keep it.** Reasoning on the record, because a pre-registered
instrument changed AFTER its data rendered and that normally is the goalpost shape:
1. The rule itself (F2) was pre-registered by this desk in ALGO-057 **before** either arm
   rendered; the instrument is adopting a rule that already bound the verdict, not inventing
   one.
2. The change only TIGHTENS: it converts an emitted PASS on a convicted brain into FAIL. No
   party is flattered; the instrument's own real re-run reads A1 PASS · F2 FAIL · composite
   FAIL · freeze BLOCKED — identical to the ruling already issued.
3. **An instrument that can emit PASS on a brain that regressed against the baseline on both
   arms is the more dangerous thing to leave standing** — A3's "a PASS is a precondition" was
   never meant to make the emitted verdict decorative.
Conditions: A1 and F2 stay SEPARATELY emitted (a reader must see which one failed); the
instrument's header records the amendment with its reason and cites ALGO-057/060/061; no
other rule text moves. F4 (advisor re-derivation + independent grade) is unchanged — the
instrument evaluating F2 does not discharge the advisor doing so.

## 3. RULED — relabel at the producer stands; NO hard rename mid-conviction

41 references and a live conviction in progress: a rename is a naming fix with a blast
radius. The docstring relabel at the producer plus the structural test that no runner may
write the anchor's path carry the truth. **The rename is DEFERRED** to its own commit after
the current conviction resolves (post-repair, pre-re-exam at the earliest), and it is not
required for FREEZE. Recorded so nobody re-litigates it as an oversight.

## 4. Approved in passing

Single-writer guard wired into all 8 artifact-writing runners at `076978ca`, lock held for
the whole run, red-proofed both directions against a real runner (refusal exits 1 naming the
holder; positive control acquires; a killed run's stale lock is correctly taken over) —
[RELAYED, consistent with the ALGO-057 §4.1 contract]. Lane suite 805.

## 5. Queue

§3 diagnosis table (09:30 refusals classified · 08:00 BUDGET mechanism separate ·
`acceptance_bars=2` attribution arm) → ALGO-062 rules the repair per ALGO-058 §4 → repair →
re-exam (composite instrument, anchor-anchored F2, SAME rules) → grade (operator-authorized
dispatch) → FREEZE or another round.

LESSON: a rule pre-registered by the judge may be adopted into the instrument after the data
only when it strictly tightens — that is the one direction goalposts are allowed to move.

No PnL, realized outcome, winner/loser label or clean-edge result participated in any
decision in this ruling.
