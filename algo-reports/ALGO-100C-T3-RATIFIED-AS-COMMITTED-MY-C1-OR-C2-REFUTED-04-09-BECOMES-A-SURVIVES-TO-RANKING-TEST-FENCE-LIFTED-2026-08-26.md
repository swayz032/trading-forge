# ALGO-100C — T3 RATIFIED exactly as committed (`MIXED ∨ NO_DIRECTIONAL_CONTROL`), and this desk's own `C1 ∨ C2` suggestion is REFUTED on teaching grounds by the worker, before any measurement — which is the standard working. The §3.4(ii) pre-registration is AMENDED where it was unsatisfiable: 04-09 becomes a **survives-to-ranking** test, because T3 refuses and can never create an approval. Fence LIFTED — the guard run may proceed on green red-proofs. S2's census is RECEIVED and corroborates S1 independently; its 04-09 rows are a truncated instrument and are not comparable.

**Advisor:** Claude (Fable 5), ALGO seat — `trading-forge-49`. **Amends:** ALGO-100B §3.4(ii)
only; everything else in ALGO-100/100B stands. **Rules on:** T3 @ `abce4155`
(`…_t3_formalization_2026_08_26.md`), S1 @ `48e9451d`, S2's census (advisor-owned, scratch).
**Channel head at drafting:** `4be1f65d`. **Main head:** `c62bb561e015`, untouched.
**PR #38: DRAFT / DO NOT MERGE.** **Next number:** ALGO-101 (the S3 packet ruling).

## 1. T3 — RATIFIED, and my own suggestion refuted [MEASURED HERE at `abce4155`]

- **`C1 ⟹ C2` confirmed by algebra at this desk:** anything below *both* wicks is below the
  *larger*; so `C1 ∨ C2 ≡ C2` and the stricter-wins tiebreak would have decided the whole clause
  silently. **`C2` refuses the hammer, confirmed by arithmetic here:** the ALGO-071 §5.3 fixture
  `(101.6, 103.5, 101.5, 103.2)` has body 1.6, upper 0.3, lower 0.1 — `C1` false (passes), and a
  canonical hammer `(100.5, 101, 98, 100.8)` has body 0.3 vs lower 2.5 → `C2` **fires**, refusing
  the archetypal rejection candle `_rejection_wick` exists to accept. **ALGO-100B §3.1's
  parenthetical "C1∨C2 arguable" is WITHDRAWN as this desk's error.** A worker refusing an
  advisor's suggested reading, on cited teaching grounds, *before* any guard number existed, is
  exactly what the pre-commit sequencing was built to make possible.
- **RATIFIED as committed:** T3 refuses iff `MIXED` (`body < upper_wick AND body < lower_wick`)
  ∨ `NO_DIRECTIONAL_CONTROL` (`close <= (high+low)/2` long, mirrored short), on the **completed**
  story bar (ALGO-033), a disjunction of the two failure modes the teaching itself names, neither
  compensating for the other.
- **The midpoint is not a smuggled magnitude — ruled.** `(high+low)/2` is the bar's own geometric
  centre with no free parameter and no search range, unlike the retired `close_loc 0.78`
  (`v2_2_engine.py:97`, range `(0.72, 0.84)`). **Two requirements in the packet:** (a) a
  provenance line stating exactly that, and (b) the tie convention stated and tested — `close`
  *exactly* at the midpoint **refuses** (stricter-wins, and the `<=` as committed already does).
- **Decimal audit passed at this desk:** every decimal in the T3 doc is a retired-magnitude
  citation, a section reference, or a fixture OHLC value. No new constant.

## 2. S2 — the census, RECEIVED, and it corroborates S1 from a second direction

Positive control PASS: 143 approvals, **by-key multiset equal** to `after_0800.json`; X-ray join
0 misses / 0 ambiguities; forbidden-field grep clean. **The finding this desk records
[ARTIFACT-SOURCED, re-read here]:** on six of the seven entry days the bot's first approval
precedes his clock by **80–187 minutes**, with **4–26 approvals per session** — and on **04-14,
the one day the bot agrees with him, the session has exactly ONE approval all day, and it is
his.** Selection was never the difference: on the agreeing day there was nothing to select
between. **Over-admission is the difference — which is T3's exact target.** S1 (from the
teachings) and S2 (from the rows) reached the same place independently.

**S2's 04-09 section is NOT comparable and is not citable [MEASURED HERE].** `ct_after_0800.json`
@ `62722a2a` records **89 candidates at the 11:35 bucket and one survivor at 11:37**; S2 reports
zero, because it ran the X-ray truncated at the label's `replay_end` (= his 11:35 clock), so every
decision clock in that bucket fell past `as_of`. **Ruled: the campaign's canonical surface is the
full-session run** — `replay_end` is a property of the labelling session, not of the market, and
every prior artifact (the traces, the candidate tables, ALGO-096 §4's hit definition) is measured
that way. S2's other thirteen sessions match the candidate tables exactly (03-23 8, 03-31 269,
04-06 239, 04-14 95) and stand.

## 3. THE AMENDMENT — §3.4(ii), split where it was unsatisfiable

**The worker's deduction is correct and is ratified:** T3 is a **refusing** clause at the story
step; approval is decided **per candidate**, not competitively; therefore removing earlier
approvals can never promote a candidate that is not one. At `62722a2a` the 04-09 11:37 survivor
**is not among that day's 11 approvals** — something downstream of ranking refuses it — so no
T3 outcome could satisfy "the bullet lands there."

**(ii) is REPLACED by (ii-a)/(ii-b)/(ii-c):**
- **(ii-a) — 03-24 UNCHANGED, still a bullet-lands test:** the bullet lands on
  `A|L|S:2026-03-24T00:15…96923|09:32` (his 09:32 IS approval ordinal 9 of that day — verified at
  this desk), which requires the eight earlier same-day approvals to be T3-refused, with the
  per-approval T3 verdict published for every one of them.
- **(ii-b) — 04-09 becomes a SURVIVES-TO-RANKING test:** `A|L|SWING:S:2026-03-17T22:30…100322|11:37`
  must still **survive to ranking** under the batch (T3 must not kill it), measured on the
  candidate table at the layer where it is observable. Its approval status is REPORTED, never
  required.
- **(ii-c) — the downstream refusal is a NAMED FINDING, not folded into T3's verdict:** the first
  layer refusing that LONG after ranking, at its executable line with its reason literal, plus its
  computed TP1 and $ display at 15 contracts, and why the SHORT on the *same zone* at 11:27/11:28
  did approve. A probe is measuring it now at this desk; if it lands before the packet, the packet
  carries it; otherwise it is the first item of the next lane. **It is not T3's job and does not
  gate the batch.**
- **Added to the guard's published tables:** the per-approval T3 verdict for 04-09's eleven
  approvals as well — a clause that refuses his day's *other* approvals while sparing the SHORTs
  on his own zone is a finding either way.

## 3a. §3.2's red-proof BASELINE was wrong — amended, and the reason is the round's best evidence

**The worker's measurement, ratified [reported at `b7227259`, its argument checked here]:** "RED
at `6888112d`" is unachievable for the T3 half, because **at the reverted head the retired
magnitudes are still doing T3's job** — a doji/mixed touch is already refused there by
`_control`'s `0.62`/`0.78`. T3 repairs what **the batch** breaks. **AMENDED: the T3 red-proof
baseline is the batch head `62722a2a` (batch without T3); `6888112d` is kept as a third column,
where two rows fail CORRECTLY.** The four-row table *is* the argument for the whole lane:

| fixture | `6888112d` reverted | `62722a2a` batch, no T3 | batch + T3 |
|---|---|---|---|
| DOJI control | refused ✔ | **COMPLETES — the hole** | refused ✔ |
| MIXED control | refused ✔ | **COMPLETES — the hole** | refused ✔ |
| HAMMER | refused ✘ *wrong* | passes ✔ | passes ✔ |
| CLEAN §5.3 thin-wick | refused ✘ *wrong* | passes ✔ | passes ✔ |

The retired magnitudes refused the bad shapes **and the good ones**; R2 fixed the good and lost
the bad; T3 restores the bad-shape refusal without re-breaking the good. **This is S1's
conclusion re-derived from fixtures instead of citations** — a third independent path to the same
finding, and the reverted head's inability to show the hole is itself the proof that T3's content
was living inside `_control`.

**The fixture defect the worker caught before it proved anything is RATIFIED as the model:** its
first DOJI/MIXED bars closed *inside* the band, so **R2b** refused them and a green would have
exercised R2b while claiming T3. Every fixture now closes back out on the near side, so R2 and
R2b both pass them and control quality is the only thing left to decide — **and the isolation is
asserted in the test rather than assumed.** Holding the red-proofs on
`research/algo-t3-redproofs-20260826` rather than reddening the head is correct and matches
ALGO-099 §8. **Requirement carried into the packet:** the three-column table above ships as
published evidence, with the two "wrong" cells labelled as the pre-batch behaviour they are.

**FENCE LIFTED.** Step 2 (taught-fixture red-proofs, RED→GREEN against the **batch head** per
§3a) then step 3 (the
batch: R2 + R2b + F1 byte-identical from history + T3 + F-1…F-4) then step 4 (the guard) may
proceed under the amended pre-registration. All other clauses — control by key, no AGREEing
anchor day lost, membership vs 40 and 143 at both pins, silenced ZERO, no uncited number, suite
and mutation arms green, honest-partial if a teaching-committed T3 kills a hit — stand unchanged.
ALGO-101 rules the packet; re-exam #3 only after a passing guard.

LESSON: the desk proposed a reading (`C1 ∨ C2`) that was algebraically degenerate and would have
refused the strategy's archetypal candle; the worker caught it from the teachings before a single
number was measured, because the ruling required the choice to be committed first. **Sequencing a
decision ahead of its data does not slow the work down — it is what made an advisor's error
cheap.**

No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision in
this ruling.
