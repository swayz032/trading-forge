# ALGO-100 — The revert executes exactly as pre-registered. The ENTRY LAYER IS CLOSED as a repair surface: on both sides of the strongest cited narrowing, everything admitted is a taught rejection form on a live zone — there is nothing malformed to refuse. The lane is the master equation's conjunct that was never built: **FIRST_A_PLUS has no implementing predicate; the kernel spends the bullet on "first."** One combined re-land — R2+R2b+F1 plus the A+ selector — under one guard whose target is WHICH approval the bullet is spent on. Sunset pack runs in parallel.

**Advisor:** Claude (Fable 5), ALGO seat — session `trading-forge-49` (the 10pm ET usage reset
renamed the session, was `trading-forge-cf`; same conversation, state continuous). **Rules on:**
ALGO-099 @ `748cee33` (the N4 integrator) + its OPEN-item closure artifact @ strategy head
`0dfff6be`. **R2c:** `7d42d121` on `research/algo-r2c-momentum-after-20260825`, unmerged.
**Channel head at drafting:** `748cee33`. **Main-channel head:** `c62bb561e015`, untouched.
**PR #38: DRAFT / DO NOT MERGE.** **N5 grader:** dispatched by this desk (accuracy-validator,
adversarial, briefed at `0dfff6be`), IN FLIGHT — its receipt publishes as **ALGO-100A**; this
ruling stands where unrefuted, and any refuted number reopens exactly that number.
**Operator context pinned:** sunset 2026-08-27 (ALGO-026/029 stand); *"make sure we get
breakthroughs"*; *"the worker can use subagents"* (relayed and now usable); 15 MNQ micros,
$517.50 = 1R (operator-confirmed).

**DECISION:** batch + R2c NOT LANDED, revert EXECUTED per ALGO-098 §2.1 (§2) · entry layer
CLOSED as a repair surface (§3) · the FIRST_A_PLUS/bullet-selection lane opened with a graph
(§4) · sunset pack authorized in parallel (§5) · stops/queue (§6).

---

## 1. Verification [MEASURED HERE at the named trees unless graded]

- **ALGO-099's node accounting read in full** (N0/N1/N2/N3 all RECEIVED; the worker's serial
  in-seat deviation was pre-accepted; no speedup claimed). Its correction of its own seat-roll
  misread and its wrong-layer 5.3 catch are the reporting standard this ladder wants.
- **The kernel line, read here:** `kernel.py:201-208` — `rank = {"BRK5": 3, "BRK15": 2,
  "REV": 1}`; `max(candidates, key=(rank, location.quality, location.confluence))` **within a
  bucket**; clock order across buckets. `find_first_actionable_signal` (v2_3_signal.py:183,
  v2_4_signal.py:81): *"Return the earliest setup that passes location/story/room/target
  gates"*. **"A+" is implemented as "first."** My own definition-shaped greps (a_plus, A_PLUS,
  quality gate, setup grade over `research/`) return status strings and ledger notes only —
  same result as the worker's §6c grep. Two independent searches, one answer.
- **The closure artifact re-derived from ROWS at this desk** (`r2c_fields_0800.json` @
  `0dfff6be`, never its summary fields): 111 rows; early REV before his clock on convicted
  days = **26** (03-23: 10 · 03-24: 3 · 03-31: 4 · 04-06: 3 · 04-09: 6); forms
  `touch_and_reject` 21 + `prior_momentum_after_rejection` 5; zone states TESTED 15 /
  FLIPPED_RETEST 5 / ACTIVE_RESISTANCE 4 / NO_ZONE_ON_LOCATION 2 — **none BROKEN. Every
  number matches ALGO-099's follow-up exactly.**
- Earlier this round, at this desk: the batch guard re-summed from row keys (40 → 143,
  removed 0), the two hits verified at survives-to-ranking by key, F1's entailment confirmed
  by prediction (143 → 143 identical). ALGO-099 §2b's 09:30-pin corroboration and §5a
  attribution are [ARTIFACT-SOURCED]; the N5 grader attacks them.
- **Prior-art searches for this ruling** (stated per the law): the ladder for A+/window/
  selection (ALGO-025/026/029/042/043/094 found and cited), the code for FIRST_A_PLUS
  implementations (none), seat memory (E1 lesson, M1 method, two-era fork). Nothing already
  decides the A+ lane; ALGO-043 §1's pre-registered window rule REQUIRES it: *"the 08:00 arm
  may not LOSE any decided-case agreement … if the 08:00 arm degrades, that is an early-entry
  defect IN THE BRAIN."* The 26 early approvals are that defect, named in advance.

## 2. The revert — pre-registered in ALGO-098 §2.1, executed now, scope exact

**ORDERED (worker main seat, one commit on the strategy head):** restore the PRE-BATCH state
of the semantic surfaces — `derivation.py`, `force.py`, `independent_force.py` to their
`6d22524c` content, the eight 096B-rewritten tests and `KNOWN_OVERLAPS` with them (the (A,C)
pin re-holds on the old code). **KEPT (not part of the batch's semantics):** the N0 capture
instrumentation (`entry_location_id/_band/_source/_side`, `candidate_reason`, `story_kind`,
`story_all_kinds`, `zone_state_at_bucket`), the 096A module-local `UNFROZEN_CHOICES` in
`force.py` + its test (they describe the efficiency clause, which survives), every
`run_*.py`, red-proof file, derivation doc and guard artifact — history is the record.
R2c stays on its branch, unmerged. **Acceptance (conjunctive):** (i) `git diff <revert>
6d22524c -- <the three semantic files + the two test files>` is EMPTY; (ii) the full v2.4
suite GREEN; (iii) the field-enabled capture at the revert head reproduces **exactly 40 by
key** at the 08:00 pin (the baseline, now with fields); (iv) the diff of the revert commit
touches nothing else. First observable: (iii)'s artifact. **No re-exam on any head this
round until §4's landing.** The definitions are not lost — §4 re-lands them.

## 3. The entry layer is CLOSED as a repair surface

ALGO-094 retired refusal-only predicates (they silence, never produce his trade). This round
closes the admission side: the ALGO-070 walk over all 103 additions found **zero** on-broken-
zone grants and every addition carrying one of the spec's own six taught forms; the closure
artifact shows the same for the 26 that survive the strongest cited narrowing (R2c — a good
clause, rejected by its own pre-registration for killing a hit and still leaving 26). **On
both sides of R2c, everything admitted is a well-formed taught story on a live zone. There is
nothing malformed left to refuse.** A future entry-layer change requires a NEW TEACHING
(operator demonstration or held-video derivation), never a new predicate over the same
teachings. R2c's form-blindness (it refused a `sweep_and_reclaim` story through the DECISION
clause) is recorded as a named variant for that future, not built.

## 4. THE LANE — FIRST_A_PLUS: build the conjunct, spend the bullet like him

**Why this is the lane, in one line each:** the master equation requires it
(`engineer_onboarding:43` … `∧ FIRST_A_PLUS ∧ DAILY_BULLET_UNUSED`); the code implements it
as "first"; every remaining disagreement is a bullet spent early on a lawful setup; his five
labeled entries are all ≥ 09:32 while the machine's are 08:12–09:38; ALGO-043 pre-registered
this exact defect class. The worker's reframe is ADOPTED: **the guard target is WHICH approval
the bullet is spent on, not which approvals exist.**

**Graph (worker holds subagent authorization from the operator, relayed 2026-08-25):**

- **S1 — A+ DERIVATION (worker MAIN seat, judgment work; cap 2h).** ALGO-064 M1 method: from
  the TEACHINGS ONLY — ALGO-009 source words, `video_evidence.md`, the addendum, the spec's
  own quality vocabulary (`primary_quality_evidence`, `active_map_policy`,
  `avoid_chart_clutter`, `range_day_fresh_entry_zone_rule`), the held videos/screenshots —
  derive WHAT MAKES A SETUP WORTH THE BULLET for him: one clause per citation, no clause
  without one, no number without one. Map every clause onto a feature the machine already
  computes (zone quality/confluence/state/age, premarket-plan alignment, story form, room,
  session phase) or name the feature MISSING. FORBIDDEN sources: 2026 labels, PnL, anything
  in the EDGE lane, invented magnitudes. Deliverable:
  `research/current_mnq_strategy_v2_4_a_plus_derivation_2026_08_25.md`. Honest-partial: if
  the teachings do not discriminate among lawful setups, SAY SO — that closes the lane
  honestly and the residual goes to a NEW-TEACHING request, the one reserved-class ask left.
- **S2 — SELECTION CENSUS (subagent, isolated read-only worktree at `62722a2a` — the
  143-universe the selector will act on; cap 90 min).** One table, all 14 sessions: every
  in-window approval by key with S1's mapped features, and HIS entry's candidate row (exists
  at his bucket? survives to ranking?). NO threshold, NO score, NO label-derived feature.
  Deliverable: `..._selection_census_2026_08_25.json/.md`. S1∥S2 are independent (S2's
  features are the already-computed ones; S1 names which matter).
- **S3 — THE COMBINED RE-LAND (worker main, after S1+S2; land-or-close in ALGO-101).**
  One batch: R2 + R2b + F1 exactly as they landed before (from history, byte-identical
  semantics), PLUS the A+ selector implementing S1's clauses at the bullet-spend site
  (`kernel.py:201-208` + the cross-bucket first-wins walk). Red-proofs first at the revert
  head. **Pre-registered, conjunctive:** (i) 04-14 control: the bullet is spent on the SAME
  key (`B|L|SWING:R:2026-04-14T09:15…102865`), clock reported; (ii) 03-24: the bullet lands
  on `A|L|S:2026-03-24T00:15…96923|09:32`; 04-09: on `A|L|SWING:S:2026-03-17T22:30…100322|11:37`
  (both survive under R2+R2b without R2c — measured this round); (iii) NO currently-AGREEing
  anchor day loses its agreement; a day may go silent only where the anchor is not AGREE, and
  every such day is listed; (iv) the full 14-session spent-on table by key, before/after,
  with the S1 clause that decided each change; (v) no number outside S1's citations; (vi) no
  PnL anywhere. **Re-exam #3 immediately after a passing guard; FREEZE per ALGO-029 §1.2 if
  the exam passes.** If S1 cannot cite a discriminating clause, S3 does not start and
  ALGO-101 handles the residual honestly.
- **S4 — SUNSET PACK (subagent, parallel from now; §5).** No edge to S1–S3 (documentation
  and runbook; different files). Shared resource: none (pack files only).
- **N5' —** the in-flight grader's receipt (ALGO-100A) plus, after S3, a fresh
  accuracy-validator on the S3 guard, dispatched by this desk.

## 5. Sunset pack — ALGO-026 P1 / ALGO-029 checklist, authorized NOW in parallel

The pack as already ruled (runbook in plain English; self-explanation audit; kill +
heartbeat; GPT handover; seat handovers), updated for this round's state and the EDGE lane's
existence (the runbook points at the EDGE artifacts as "validation results", clearly labeled
not-fidelity). The HARD GATE stands: no TopstepX connection of any kind — broker-paper
included — before FIDELITY → FREEZE → CLEAN EDGE (ALGO-025 §2.2, re-verified in the
deployment map this desk commissioned). Deliverable paths per ALGO-026 P1a–e, on the
strategy branch. First observable: `ALGO-RUNBOOK.md` skeleton with the start/stop/check
commands that exist today.

## 6. Stops, queue, lesson

STOPS: the revert is the only semantic change authorized before S3 · no number without a
citation · nothing from 2026 labels or PnL or EDGE artifacts enters any fidelity decision or
S1 clause · break-family gates untouched (their census is on the record for a future lane) ·
the exam runs once, after S3's guard passes · no TopstepX connection.
QUEUE: revert (§2, first observable ~30 min) → S1 ∥ S2 ∥ S4 → S3 → ALGO-101 rules → re-exam
→ FREEZE path. The EDGE lane (advisor-owned) reports separately; its multi-year read lands
regardless and never gates fidelity.
LESSON: the campaign spent four rounds sharpening predicates inside a layer whose every
admission was already lawful, because a conjunct of the master equation — written down on
day one — was implemented as the word "first" and nobody grepped for its definition until a
failed batch forced the question. **The spec was complete; the build was missing a term. Grep
the equation's conjuncts for their implementations before sharpening any of them.**

No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision
in this ruling.
