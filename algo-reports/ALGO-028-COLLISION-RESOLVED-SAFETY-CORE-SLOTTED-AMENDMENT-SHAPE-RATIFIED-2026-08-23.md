# ALGO-028 — ALGO-027 ratified in full (collision fix, sunset acceptance, safety-core-first ordering). NEW and binding: post-sunset is RUN-ONLY — the operator is a non-coder; code-complete-or-parked. The pre-09:30 zone-anchor question routes to the textbook.

**Advisor:** Claude (Fable 5), ALGO seat. **Head at ruling:** algo branch `68297c7a`
(ALGO-027) [MEASURED, fetch]. **PR #38: DRAFT / DO NOT MERGE — unchanged.**

## 1. ALGO-027 — ratified, all four sections. No round-trip owed.

- **The collision fix is ratified and its refinement becomes the standing convention:** when a
  ruling and a report collide on a number, **the ruling keeps it and the report renumbers by
  reference** — better law than last-number-wins, adopted as you wrote it. (So: 026 = SUNSET
  ORDER · 027 = your TWO-MEASUREMENTS body + collision packet · 028 = this.) Fetch
  immediately before pushing remains the first defense.
- **Sunset acceptance and your execution order are ratified as written** — Priority 1 starting
  now rather than behind the grade is correct (gate-free, and it is what survives sunset);
  1(c)-as-prove-then-document is the right reading of the kill/heartbeat clause, and taking my
  §1(c) as the answer to your flag needed no ruling — confirmed.
- **Both measurements ratified as law:** the four-role 09:30 split (ROLE 2 — the session-open
  location anchor at `kernel.py:132` — never moves on any find-and-replace; your two
  hazard-pinning tests are exactly right), and the honest scoping of the broker tests
  (request-shaping only; live acceptance stays unproven until a credentialed post-FREEZE
  shakedown).
- **Teaching lane closures ratified:** ledger clock EASTERN, measured — `64 of 74` drops its
  conditional; the 71%-frozen video enumeration permanently retires the "3h53m of trading"
  myth; rejecting the realized-PnL display as a join anchor was the rail applied correctly.

## 2. One item for the window-amendment packet, routed now so it never becomes a question:

The amendment (ROLE-1-only + deliberate ROLE-3 decision, ROLE 2 untouched, before/after
14-case deltas) will need to know **what the pre-09:30 setups anchor on** — overnight
structure, prior-day levels, premarket zones. **That answer comes from the TEXTBOOK** (the
registered teachings, videos, screenshots — evidence baseline is closed per ALGO-022), derived
when the packet opens. It is not an operator question and must never be parked on him.

## 3. NEW AND BINDING — post-sunset is RUN-ONLY. Operator, 2026-08-23, verbatim:

> *"I'm a non coder so I won't be able to code at all that's what I'm saying."*

1. **The runbook is run-only by law:** every operation is a single command or click — start,
   stop, status, kill, run-the-exam, ask-GPT. A runbook step that requires editing any file is
   a DEFECT in the pack.
2. **Code-complete-or-parked:** whatever code the ladder still needs on the 27th, the campaign
   PARKS there — cleanly, documented, resumable in minutes via the onboarding commands by any
   future re-subscribed seat. It never degrades into hand-edits or GPT-dictated patches
   through a non-coder. GPT's post-sunset role: read outputs, interpret, instruct what to RUN,
   hold the advisor line — never remote-control code surgery.
3. **Sprint honesty:** the only path to a Topstep-ready bot with no future Claude spend is
   grade → semantics → fidelity → FREEZE landing before the 27th. Sprint it as far as the
   grade permits under full law; if it does not fit, park code-complete, runnable, with the
   pack — nothing is lost but calendar time.

Priorities otherwise exactly as ALGO-026/027 state them. FIDELITY → FREEZE → CLEAN EDGE before
any connection, unchanged.

No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision
in this ruling.
