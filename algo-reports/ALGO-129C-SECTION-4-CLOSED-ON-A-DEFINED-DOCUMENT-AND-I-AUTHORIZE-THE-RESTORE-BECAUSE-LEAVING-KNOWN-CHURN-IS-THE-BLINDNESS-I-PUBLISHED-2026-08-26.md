# ALGO-129C — **§4 IS CLOSED, AND CLOSED ON A DEFINED DOCUMENT: 15 passed / 0 failed / 417.20s, joined by a pre-check to `ALGO-GPT-HANDOVER.md` sha256 `56e6375d…` at head `fdc4f39b` captured AT RUN START, with no battery live.** That is the property the first run could not claim, and it is what turns *"rendered · green · produced under a declared race"* into a pass. **AND THE DECISION THE WORKER CORRECTLY ROUTED TO ME: RESTORE THE TWO DIRTY FILES.** Not because the tree should look clean — **because leaving known-meaningless churn in place is exactly the blindness I published in ALGO-129A §2: it trains the reader to scroll past diffs on the two files a writer touches.** **Verified independently before authorizing: both diffs are FOUR LINES, all `runtime_seconds`, and NO process touches the tree.** **And no exception list — institutionalizing "expect a diff here" IS the defect.**

**Advisor:** Claude (Opus 5), ALGO seat — `trading-forge-47`. **Channel head at drafting:** `52482a67`.
**Strategy head `fdc4f39b`, unmoved.** **PR #38: DRAFT. Nothing lands from this desk.**

---

## 1. §4 — CLOSED, and on the right predicate

`tests/test_algo_runbook_commands_actually_run.py` → **15 passed, 0 failed, exit 0, 417.20s.** Its
embedded pre-check captured, **at the moment the run started rather than asserted afterward**,
`ALGO-GPT-HANDOVER.md` sha256 **`56e6375d…`** and head **`fdc4f39b`** — **byte-identical to the
committed document and to the digest the red-proof restored to**, with no mutation battery live at
any point.

> **A GREEN IS WORTH WHAT ITS JOIN IS WORTH. THE FIRST RUN HAD A RESULT; THIS ONE HAS A RESULT AND A
> SUBJECT.**

**ALGO-129A §1's race is not argued away — it is repaired, in the instrument, by capturing the subject
at the start.** That is the durable form and it should be the pattern for any long guard on a mutable
document.

## 2. THE CLASS DEMONSTRATED ITSELF, TWICE, IN TWENTY MINUTES

`refusal_trace_five_clocks…json`, `runtime_seconds`, **same unchanged code**:

> **182.32 → 179.83 → 184.04**

**Three values, one file, zero semantic change.** Both halves of ALGO-129A §2's bite are now on the
record from a single artifact: **the diff always exists, and it never means anything** — and it is
precisely the file whose diff a reader learns fastest to skip.

## 3. THE DECISION — RESTORE. It is mine and here it is.

The worker declined to act and routed it here: *"reverting an artifact is a write to the evidence
corpus and that is your call."* **Correct routing.** Under `advisor-ruling §0.0` this is squarely the
desk's — reversible, bounded, no capital.

**VERIFIED HERE BEFORE AUTHORIZING, not taken from the report:**
- `git diff -U0` over the whole tree → **four changed lines, every one `runtime_seconds`**
  (`180.86→181.65`, `182.32→184.04`). **No other line of any kind.**
- Process table: **no process names the tree.** (My own filter was `python.exe` repo-wide and caught
  four `elevenlabs-mcp` servers — **broader than the class**, which produced a false scare. The
  worker's report was correct. **A filter can be wrong in both directions and I demonstrated the
  other direction within the hour.**)

**AUTHORIZED: `git checkout -- <the two paths>`.** With one condition that is the whole lesson of
tonight:

> 🛑 **RE-VERIFY THE DIFF IS STILL WALL-CLOCK-ONLY IMMEDIATELY BEFORE RESTORING, IN THE SAME MOTION.**
> The values moved twice in twenty minutes. **Acting on my verification above would be acting on a
> reading that is already minutes old — the exact error this packet spent two rulings on.**

**WHY RESTORE RATHER THAN LEAVE IT.** The worker's instinct — *"I would rather hand you a dirty tree
you understand than a clean one that hides the writer"* — **was right while the guard was live and the
finding unpublished.** It is not right now: the finding is on the ladder in three rulings, and
**leaving the churn in place manufactures the blindness I warned about.** A dirty tree is a signal;
**a permanently dirty tree is noise, and noise on exactly these two paths is how the next real change
gets scrolled past.**

**WHY NOT COMMIT IT.** Committing wall-clock churn changes committed evidence digests for zero
semantic content — it pollutes the record and breaks any future pin on those artifacts. **Refused.**

🛑 **AND NO EXCEPTION LIST.** No "known churn" note, no ignore rule, no allowlist. **Writing down
*"expect a diff on these two files"* is the defect with a procedure wrapped around it** — it converts
a thing readers might notice into a thing they are instructed not to.

**NOT ORDERED, and named so nobody drifts into it:** the real root cause is that the generators write
wall-clock into committed artifacts. **Fixing that is a change to 40+ generators and a mass rewrite of
the evidence corpus. It is not authorized, tonight or on this evidence.** ALGO-129B §5 stands: **a
bigger number is not a mandate.**

## 4. THE PACKET IS CLOSED

Rail 11 in the stop list · §7.3/7.4/7.5 in the method section · red-proofed with the defect planted
inside the newest text · landed at `fdc4f39b` and pushed · **§4 reported twice — once honestly
caveated, once clean.** **Everything assigned is done.**

**HOLD, unchanged:** the campaign is blocked on one reserved-class fact — *why he passes on an early
break-family setup that clears a stricter-than-taught trigger.* **Assignee: the operator, unprompted.
Drafted. NOT SENT.** **After the restore and its porcelain report: nothing. Stop rather than fill it.**

**STOPS unchanged**, including no cleanup into a tree a running guard is reading — **which is why §3
carries the re-verify condition rather than a bare authorization.**

---

**LESSON, minted:**

> **A DIRTY TREE IS A SIGNAL, AND A PERMANENTLY DIRTY TREE IS NOISE. THE DECISION TO LEAVE KNOWN
> CHURN IN PLACE IS A DECISION TO SPEND THE READER'S ATTENTION ON IT FOREVER — AND THE READER PAYS BY
> LEARNING NOT TO LOOK.**

The worker was right to leave it while the guard was live and right to route the write to me. **Both
halves matter: it did not act on a shared resource mid-run, and it did not treat "I understand this
diff" as authority to erase it.** The general form: **evidence you have finished learning from is no
longer evidence — it is clutter, and clutter on a diagnostic surface is worse than clutter anywhere
else.**

*No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision in
this ruling.*
