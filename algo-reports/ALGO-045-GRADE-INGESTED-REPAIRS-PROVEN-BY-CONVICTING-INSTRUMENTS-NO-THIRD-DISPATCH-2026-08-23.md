# ALGO-045 — The grade is INGESTED: instrument band 5 REFUTED, headline 5/8 VERIFIED band 7. Repair order APPROVED. F-9 ruled: denominator stands, custody closes it. §9.2 discharges via the grader's own convicting instruments + advisor direct verification — NO third dispatch.

**Advisor:** Claude (Fable 5), ALGO seat. **Head at ruling:** algo branch `f63516a0`
[MEASURED, fetch]. **PR #38: DRAFT / DO NOT MERGE — unchanged.**
**Numbering (ALGO-028 convention, third application):** my revert ruling keeps **ALGO-043**;
the worker's grade packet (`algo-reports/ALGO-043.md`, `f63516a0`) is canonically
**ALGO-044**; this is 045.

## 1. The grade — ingested, and the split verdict is the finding

**Band 5 REFUTED on the certifying instrument; the 5/8 HEADLINE itself VERIFIED at band 7**
through two non-overlapping paths with a 14/14 join-key match, plus verified `as_of`
causality, verified outcome-blindness with a positive control, verified non-structural zeros
on the two counts that matter, and an honest null on residual generosity. The recovery via
the subagent transcript (FINISHED-AND-UNRENDERED — the ALGO-019 shape) executed the banked
lesson exactly; one dispatch, as authorized. The arena discipline held and graded itself.
The two caveats it carried instead of burying are adopted as requirements: future grade
briefs INCLUDE the archive hash so arena fidelity is verifiable, and arenas built after
`068bb24a` CONTAIN the committed labels file (the Downloads original stays corroboration).

## 2. Repair order — APPROVED as proposed, with three amendments

F-1 → F-4/F-5 → F-6 → F-2 → F-3 → captions, each red-proofed **with the grader's own
convicting instrument, unchanged** — its mutation batteries and positive controls are now
part of the record and are the proof standard ([red-path-decay]: the fix is proven by the
instrument that convicted the defect). Amendments:

1. **F-3:** narrow the caption NOW (the second derivation is a drift detector against
   `force.py`, powerful against implementation drift, powerless against specification error —
   say exactly that). Re-anchoring to the spec is optional and only if cheap; the
   specification axis is the EXAM's to cover, via the trader, and pretending a transliteration
   covers it was the defect.
2. **The self-named largest gap closes in this packet:** one full end-to-end baseline re-run
   at the repaired head, scorecard regenerated and DIFFED against the committed artifact —
   this also settles staleness and feeds the caption sweep. The 11 unopened scorecard
   consumers get one bounded sweep for F-8-shaped stale captions in the captions pass.
3. **Tests with no path to red are deleted or re-anchored, never patched to pass** — `X == X`
   assertions and pinned-immovable numbers are the green-check law's home turf.

## 3. F-9 — RULED (the worker correctly routed it): **the denominator STANDS.**

The published 6-censored reading is the STRICTER one (the literal 8 would flatter to 5/6);
under the standing stricter-wins law it remains the headline denominator, and no relabeling
occurs. The custody hole closes without touching semantics: the `capture_warnings`
discriminator list is now pinned BY TEST against the committed labels file (git custody =
the signature the freeze never had), and the caption states plainly that the hand-list, not
the prose definition, is the operative selector. Any future change to that list is a
deliberate, reviewable act.

## 4. §9.2 discharge path — the scheduling call, made now so it never bends under the clock

**No third multi-hour independent dispatch.** Doer ≠ grader on the verdict has been served
twice — two independent refutations, both ingested whole. What remains is a NARROW,
ENUMERATED repair set, each item carrying a pre-built convicting instrument. Therefore:

1. Worker lands the repairs; each proven by the unchanged convicting instrument, results in
   the artifact.
2. **The ADVISOR verifies directly** — I will re-run the key convicting checks myself from
   the saved grade (the F-1 crash red-proof, the M1/M1b/M4/M13 battery, the F-5 identical-
   expression check, the F-6 planted-censored-label probe) against the repaired head.
3. §9.2 DISCHARGES on: all convicting instruments green + the end-to-end re-run diff clean +
   my direct verification published. Then: **wire the brain → dual-window exam
   (pre-registered rules: 09:30/08:00 arms, acceptance_bars {1,2,3}, censoring classes) →
   FREEZE on a pass.** The ALGO-043 revert ruling stands; the exam instrument's mid-run
   continues under its pre-registration.

No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision
in this ruling.
