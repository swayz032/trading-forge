# ALGO-098 — The §5 batch does not land (its own 5.4 decided it: 40 → 143, removals 0) and it stays on the head UNRATIFIED; the diagnosis is CONFIRMED at two named keys (the bot now sees his 03-24 09:32 and 04-09 11:37 trades at his minute). The over-grant is the shape of a TAUGHT STAGE that left with the fractions — Route A's momentum-after clause, whose magnitude-free form ALGO-068 §3 already authorized. GRAPH-ENGINEERED next round: R2c (that stage) and the Route D early-bullet census run in PARALLEL on isolated SHAs; one integrator; a fresh-context verifier; ALGO-100 rules land-or-close.

**Advisor:** Claude (Fable 5), ALGO seat — session `trading-forge-cf` (live; the worker seat rolled
`-99` → `-73`, this seat did not). **Rules on:** the landed guard artifacts at strategy head
`5b488564` (worker's direct-message report received as CLAIMS and re-measured here — its formal
packet follows as **ALGO-099** by agreement). **Channel head at drafting:** `569811f3`
(ALGO-096B). **Main-channel head:** `c62bb561e015`, untouched. **PR #38: DRAFT / DO NOT MERGE.**
**Operator directive, verbatim, 2026-08-25:** *"make sure we get breakthroughs"* and *"yes do
graph engineering"* — this ruling is written under both; `graph-engineering` invoked.

**DECISION:** batch NOT RATIFIED, head KEPT (§2.1) · F1's entailed clause OUT (§2.2) · overlap
pin at `frozenset()` RATIFIED (§2.3) · instrument gap ordered closed (§2.4) · NEXT ROUND AS A
GRAPH (§3–§5): N0 instrument ∥ N1 R2c derivation ∥ N3 Route D census → N2 R2c land+guard →
N4 integrate → N5 fresh verifier → ALGO-100.

---

## 1. Verification [MEASURED HERE at `5b488564`, in this desk's own detached worktree]

- Touched test files (`derivation`, `entry_authority`, `force`, `independent_force`,
  `breakout_derivation`): **148 passed, 2 skipped**. Full-suite 849/0 is [RELAYED].
- Approved-entry capture, 08:00 pin, byte-identical instrument in two trees (`56d9360d` baseline,
  `46b21920` landing): **40 → 143; removed 0; added 103** (re-summed from the row keys, not the
  worker's totals). First approval moves EARLIER on **10 of 14** sessions (03-24 08:17→08:12,
  03-25, 03-31 09:03→08:13, 04-02, 04-06 09:07→08:29, 04-07, 04-08, 04-09 09:37→08:43, 04-13;
  03-30 none→08:07). Control 04-14: **1 → 1, same key, clock 09:38 unmoved.**
- Candidate table at his clocks (`ct_after_0800.json`, deepest gate BY KEY): **03-24 survives at
  `A|L|S:2026-03-24T00:15…96923|09:32`** — his exact minute, the F2 anchor's own AGREE zone;
  **04-09 survives at `A|L|SWING:S:2026-03-17T22:30…100322|11:37`** — the zone this desk's
  ceiling probe named in ALGO-096 §2.2; 04-06 deepest = `REJECTION_STORY_INCOMPLETE` at
  `…97649` @ 10:02 (refusal moved to R2b's "closed inside"); control survives by key. **Three keys
  were named in advance; two hit exactly. The mechanism diagnosis of ALGO-096 stands.**
- Code at the line: `_control(row, direction, lo, hi)` = close not beyond the band
  (`derivation.py:160-176`, `_breaks` negated, clear 0 — ALGO-071 §3 verbatim);
  `_rejection_wick(row, direction, lo, hi, pad)` = traded in, closed back out near side
  (`:179-`); `two_sided_wick_conflict(row, lo, hi)` = closed INSIDE (`:342-`). `force.py`:
  module-local `UNFROZEN_CHOICES` (`:44`), `_directional_body` (`:89`), `efficient = progress > 0
  and efficiency >= body_frac` (`:163`), `progress = c - o` (`:151`).
- **F1 equivalence:** for a LONG, "close beyond open" ⇔ `c - o > 0` ⇔ `progress > 0`, which
  `efficient` already requires — the geometry clause is ENTAILED. [MEASURED at `efficient`;
  `_directional_body`'s return line RELAYED, its docstring read.] The worker's 0-RED mutation
  battery is the same fact from the other side. Its retraction of "F1 contributed nothing"
  (vs the OLD code F1 removed two fractions and is strictly looser) is correct and accepted.
- **Overlap pin at the bar:** `ROUTE_C_BARS` last completed bar `O 99.00 H 101.80 L 98.90 C
  101.60` vs band `[100, 102]` — close INSIDE; `upper .069 / lower .035 / body .897`, the old
  `0.30/0.40` rule did not fire. The old code read a bar that decided nothing as a completed
  rejection; his definition does not. **Pre-registered acceptable mechanism (ALGO-096B §3) —
  exactly.**

## 2. Decisions on the worker's four open items

1. **Head KEPT, batch UNRATIFIED-IN-PLACE. No revert now.** A RED head (production reverted,
   096B tests calling the new signatures) is the worse hazard; the batch is the first half of a
   repair whose second half is a taught stage (§3), not a retreat. **No re-exam #3 on this head**
   — the exam runs only after a ratified landing (ALGO-100). If N2 fails its pre-registration,
   R2+R2b+R2c+F1 revert **together in one commit**, patch in history, and ALGO-100 says so.
2. **F1: the entailed geometry clause comes OUT** (N0). Direction is carried by `progress > 0`
   inside `efficient`; the docstring's teaching citation moves onto that clause; the reason
   chain keeps a RED-able pin (the worker's D5 test). `UNFROZEN_CHOICES` stays. Mirror identical.
3. **`KNOWN_OVERLAPS = frozenset()` RATIFIED** (§1, at the bar). The pin's docstring cites
   096B and now this ruling.
4. **Instrument gap accepted and ordered closed** (N0): the approved-entry capture carries
   `location_id`, `zone_state_at_bucket`, `route`, `story_kind`. Until then the 103 additions are
   UNGRADED on ALGO-070 (i)(ii)(iv) — never "passing".

**Why the over-grant is informative, not a failure of his definition.** Removals 0 / additions
103 / targets 0: nothing his rule accepts was refused before; everything new is "poked the band
and closed back out" with NO further narrowing. ALGO-009 Route A is `GENUINE REJECTION/CONTROL
STORY → DIRECTIONAL 5M MOMENTUM → SUSTAINED CAUSAL FORCE → ENTER`; ALGO-052: *"rejection, then
momentum candles formed"*; ALGO-071 §3 left *"the momentum-after clause … the next stage of
Route A exactly as taught"* standing. The retired fractions were the ONLY implementation of that
stage (`_control`'s body/close on the last bar was doing double duty as "momentum candle").
ALGO-068 §3 authorized its magnitude-free form on 08-23: *"a momentum candle that takes out the
prior candle's extreme — the same extreme test Route B already uses at `normal_breakout`."*
That is R2c. It narrows by a TAUGHT clause, not by a number.

## 3. The graph (graph-engineering, required output)

**Starting authority:** strategy `5b488564`; baseline `56d9360d` (40 approvals, 08:00 pin);
rubric ALGO-096 §5.1–5.6 unchanged; population 14 sessions, his six clocks; F2 anchor frozen.

| node | job (bounded) | pinned inputs | output + schema | owner · write surface | acceptance / stop | first observable |
|---|---|---|---|---|---|---|
| **N0** INSTRUMENT | (a) capture carries `location_id`, `zone_state_at_bucket`, `route`, `story_kind`; (b) F1 entailed clause removed, reason-chain pin kept, mirror identical | `5b488564` | commit **H0** on the strategy branch; re-run capture at H0 reproduces **143/40** by key (positive control) with fields populated | worker MAIN seat · capture script, `force.py`, `independent_force.py`, their tests | suite GREEN; 143 reproduced; a planted wrong `location_id` goes RED | capture JSON with `location_id` non-null on every row |
| **N1** R2c DERIVATION (report-only) | the momentum-after stage, one clause per citation: ALGO-009 Route A · ALGO-052 · ALGO-068 §3 · ALGO-071 §3 · Route B's extreme test (`normal_breakout`); states WHICH bar's extreme, close-vs-trade, completed-vs-forming, per ALGO-033's stage placement | `5b488564` (read-only, isolated worktree) | `research/…_r2c_momentum_after_derivation_2026_08_25.md` — clause table {clause, citation, predicate spec, bar} | worker subagent A · that file only | ≥1 cited magnitude-free clause, else **R2c CLOSED** in the file (honest partial) · cap 45 min | the clause table |
| **N3** ROUTE D EARLY-BULLET CENSUS (report-only) | the five early Route D trades at the **40 baseline** (03-23 08:14 `S:03-03T09:30`; 03-24 08:17 `S:03-23T14:00` opposite direction; 03-31 09:03; 04-06 09:07; 04-09 09:37 `SWING:R:03-16T15:45`): zone id/age/kind/source, D path (accepted-break retest vs prebreak repeat test), every magnitude cleared (`reject_wick 0.35`, `acceptance_bars 3`, `_momentum` body/close) with TAUGHT/UNTAUGHT, and the taught clause (ALGO-009 §7.10–7.12: REAL initial test · MEANINGFUL reset · RETURN attack) each would face in the teaching's words. ALGO-078 H-A/H-B frame. **No predicate proposed.** | `56d9360d` (read-only, isolated worktree) | `…_route_d_early_bullet_census_2026_08_25.json` + `.md`, 5 rows, fixed columns | worker subagent B · those files only | 5 rows or an explicit INCOMPLETE row set · cap 60 min | the 5-row table |
| **N2** R2c LAND + GUARD | implement N1's clause in Route A's story (DECISION stage), red-proofs RED at H0 first; guard = capture at BOTH pins vs the 40 baseline AND vs H0's 143, candidate table at six clocks, ALGO-070 walk over every remaining addition (now claimable) | H0 + N1's clause | commit **H1**; guard artifacts under `research/_algo098_guard_2026_08_25/`; per-clause table | worker MAIN seat · `derivation.py` (Route A story only), tests, artifacts | §5 below, conjunctive · cap 2.5 h | red-proofs RED at H0 |
| **N4** INTEGRATE (barrier) | ALGO-099/-099A packet: expected {N0,N1,N2,N3} vs received/missing/failed/timed-out; combined delta by key via `diff_capture.py` (deterministic), never prose; the prior round's attribution + 09:30 pin folded in | H1 + N1 + N3 artifacts | the packet, published; H1 pushed FIRST | worker MAIN seat | every node in a terminal state; a silent node is MISSING, never omitted | filename + SHA |
| **N5** VERIFIER (fresh context) | `accuracy-validator` (doer ≠ grader, §0-CTRL), adversarial: refute §5 by KEY from the artifacts at H1; hunt the walk over the additions; never reads the worker chat | H1 + frozen §5 rubric | committed receipt file | dispatched by the worker, output owned by the grader | "no refutation found + coverage + what it could not check" is a complete answer | the receipt path |

**Hard edges (artifact carried):** N0 → N2 (H0 + capture fields) · N1 → N2 (the clause spec) ·
N2 → N4 (H1 + guard) · N3 → N4 (the census) · N4 → N5 (H1 + rubric) · N5 → ALGO-100.
**Fake edges removed:** N3 ↛ N2 (N2 consumes nothing from the census); N0 ↛ N3 (N3 pins to
`56d9360d`); N1 ↛ N3. **Layer 1 = N0 ∥ N1 ∥ N3. Serial barrier = N2 (needs N0 and N1). Barrier
= N4. Then N5.**

**Shared resources, ownership:** strategy-branch HEAD — single writer, the worker MAIN seat,
N0 then N2 serially; `derivation.py` — N2 only; `force.py` / `independent_force.py` — N0 only;
the capture script — N0 writes, N2 consumes; N1 and N3 write ONLY their own report files in
isolated explicit-SHA worktrees; the ALGO channel — two publishers (advisor, worker), each
fetches + `ls-tree` for the number before publishing (no collision guard in the script);
X-ray/data cache — read-only, shared, fine; the grade slot — N5 only, after N4.

**Fan-in contract:** N4 lists expected `{N0, N1, N2, N3}` and the received / missing /
duplicate / failed / timed-out sets; partial evidence publishes only with an explicit INCOMPLETE
verdict; no synthesis over a missing node.

**Caps:** N0 45 min · N1 45 min · N3 60 min · N2 2.5 h · N4 30 min · N5 45 min. **Critical
path estimate:** max(N0, N1) + N2 + N4 + N5 ≈ 45 + 150 + 30 + 45 ≈ **4.5 h**, vs ≈ 6 h serial —
an ESTIMATE; the speedup is stated only from measured durations in N4.

## 4. Contracts — what may and may not move

**Allowed:** N0/N2 files above; new `research/run_*.py` + artifacts; tests named in the packet.
**Forbidden this round:** `breakout_derivation.py`, `target_policy.py`, `entries.py`, every
break-family gate (Route D's `reject_wick 0.35` / `acceptance_bars 3` / `_momentum` — N3 is a
CENSUS, it touches nothing); `MIN_COMPLETED_1M_OBSERVATIONS`; the 17.25-pt stop; targets; the
exam rules; any refusal-only predicate; any value set from a 2026 label; any number without a
citation.

## 5. N2 pre-registration (conjunctive; a miss on any line and R2c does not land)

1. Control 04-14 by KEY (`B|L|SWING:R:2026-04-14T09:15…102865`), clock reported.
2. Sessions silenced: ZERO.
3. **The two hits SURVIVE R2c:** 03-24 `…96923 @ 09:32` and 04-09 `…100322 @ 11:37` still
   `SURVIVED_TO_RANKING`. A narrowing that kills either fails — his definition + a taught stage
   must still see his trade at his minute.
4. Vs the **40 baseline**: no Route A approval BEFORE his clock on a convicted day that was not
   there at 40; every remaining addition walked through ALGO-070 (i)–(v) by name with
   `location_id`; additions that fail the walk are listed, and their count is the finding.
5. 03-23 / 03-31: no candidate expected; any that appears is reported, not claimed.
6. Approval on 03-24 / 04-06 / 04-09 is REPORTED, expected NO (bullet spent early — N3's lane).
7. No PnL / outcome / winner-loser / clean-edge field read anywhere.

Re-exam #3 only after ALGO-100 ratifies a landing — once.

## 6. What this round means for the breakthrough (plain)

The bot can now see two of his three recoverable trades at his exact minute. It also says yes
too often, because a taught stage left with the fractions; R2c puts that stage back in his own
terms. The bullet is still spent 1–3 h early by Route D trades at old zones; N3 is the census
that makes the next lane a structural repair rather than another refusal predicate. Both run at
once from here.

STOPS: nothing lands on a majority literal · no number moves without a citation · nothing from
2026 labels sets a parameter · no refusal-only predicate · break-family gates untouched this
round · the exam runs once, after a ratified landing · a silent node is MISSING, never omitted.

LESSON: the batch's own conjunctive rule caught a 103-approval over-grant that a count-free
"control survives" guard would have waved through — and the two exact-key hits are why the
failure is progress: the diagnosis was measured at three keys before the code existed.

No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision in
this ruling.
