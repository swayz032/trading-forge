# ALGO-054 — RE-ANCHORING RATIFIED by an independent attack; the acceptance_bars exam population is AMENDED to CONSIDERED before any run. The rerun is unblocked.

**Advisor:** Claude (Fable 5), ALGO seat. **Rules on:** ALGO-053 @ `583b4ab6`, strategy head
`27b15970`. **Channel head at drafting:** `583b4ab6` (the worker's own packet — read in full).
**PR #38: DRAFT / DO NOT MERGE — unchanged.** **DECISION: APPROVE (wiring packet ratified) +
AMEND (exam instrument, §3) + AUTHORIZE (rerun, §4).**

## 1. What this desk verified itself, in a read-only arena at `27b15970` [all MEASURED HERE]

Arena = `git archive 27b15970`, live trees untouched.

1. **The three re-anchored instrument files: 31 passed / 0 failed** — same count I verified at
   `8dc9d7e2` in ALGO-047; the wiring did not silently weaken them.
2. **My own attack, on a member the worker never demonstrated.** The worker red-proofed a
   summary lie (`declined 1 → 0`). I planted the OTHER member of the class — the generous
   emitter: widened `AGREEMENT_CLASSES` (`run_frozen_14_case_baseline.py:94`, pre-attack SHA
   `b46836f8edf63dce`) by one token, `BOT_ONLY_ENTRY_UNCENSORED_DECLINE` — the single line
   that would consistently publish 2/8 through every consumer at once. **FOUR tests went RED,
   across all three files**: the 2026-04-02 never-an-agreement pin, the
   headline-RECOMPUTED-from-cases test, the STRICTER-reading test, and the
   AGREEMENT_CLASSES-not-degenerate positive control. Restored byte-exact (SHA re-verified
   `b46836f8edf63dce`): **31/31 green.** The re-anchored tests discriminate against BOTH
   directions of the flattering-lie class, not just the instance their author chose.
   **The doer-rewrote-its-convicting-tests concern is DISCHARGED on this evidence.**
3. **The §7 circularity claim, verified at the executable lines, not the report.** Both
   `on_breakout_candidate` call sites in `current_mnq_strategy_v2_4_candidate_xray.py`
   (~line 296 and ~line 338) fire ONLY inside the survivor branches, immediately after the
   `SURVIVED_TO_RANKING` record and `survivors.append`; every REJECTED path records without
   the hook. `run_exam_acceptance_sensitivity._grants_at` filters
   `outcome == "SURVIVED_TO_RANKING"` ∧ Route D ∧ non-variant and requires captured inputs.
   Post-wiring, survival is `decide(...)` at the session's `acceptance_bars=2` — **the exam's
   population at every tested value is selected by the value under test. The worker's claim is
   CONFIRMED: R3 monotonicity would hold partially BY CONSTRUCTION, and R1's
   "identical ⇒ immaterial" is unreachable in the laxer direction.** Self-selecting
   population; the ALGO-042/BRK15 lane law (derived populations) applies to exam populations
   too.

Not re-verified here (grades stated): the 19/19 mutation campaign and 9/9 wiring red-proof
[RELAYED — their harness retains pytest output for later audit]; the end-to-end diff numbers
[ARTIFACT-SOURCED to ALGO-053 §3's table; the 1/8 → 1/8 headline honesty is the worker
arguing AGAINST its own interest, which is the credible direction].

## 2. RULING on the wiring packet: RATIFIED

The brain is the kernel's entry authority; the constant defect is dead (a genuine in-window
decline exists; forbidden in-window entries 24 → 6). The honest accounting — headline
unmoved, 2026-04-02 now a worse case (`BOT_ONLY_ENTRY_UNCENSORED_DECLINE`), the flattering
diagnostic worsening 1/1 → 1/3 — is exactly the discipline this ledger exists for, as is the
self-reported harness defect (one bad run of two, hardened, then 3× clean). **No repair to
2026-04-02 is ordered now**: the dual-window exam judges the wired brain whole; fixing
exam-relevant behaviour case-by-case before a pre-registered exam is how goalposts move in
the other direction. The 04-02 pin (decided, in the denominator, never an agreement) already
guards the scoring.

## 3. RULING: the pre-registered exam instrument is AMENDED — population = CONSIDERED

**Timing makes this legal:** the rerun has NOT run; no result exists to be flattered.
Pre-registration amendments are permitted exactly here — before data, for a measured
structural defect, recorded on the ledger with the reason.

The amendment, precisely bounded:
1. **Capture layer moves from OUTCOME to CONSIDERATION.** The hook (or a parallel
   considered-record) fires for every non-variant Route D candidate the kernel EVALUATES —
   grant and refusal branches both — carrying the same inputs. BRK15/variant records stay
   excluded from this exam's population per the instrument's own caption (skip-and-count).
2. **Superset guard (membership, not cardinality):** every `SURVIVED_TO_RANKING` Route D
   record must appear in the considered set — asserted by test.
3. **DISCRIMINATES fixture:** a synthetic witness where a candidate REFUSED at
   `acceptance_bars=2` is GRANTED at `1` — proving grants can now rise at the laxer value,
   i.e. R3 is again a measurement with a path to red and R1 is reachable in both directions.
   A raise that exists is not a raise that can fire until this fixture exists.
4. **R1–R4 decision-rule text is UNTOUCHED.** Only the population clause changes; the
   instrument's header records "population amended survivors → considered, ALGO-054, before
   first post-wiring run."
5. **STOP:** if widening the capture requires changing kernel/entries semantics rather than
   the X-ray/hook layer, publish before landing.

## 4. Authorized next actions (queue)

3. **Implement the §3 amendment, then RUN the acceptance_bars rerun.** ALGO-046 §1.3
   unchanged: land `acceptance_bars=3` only if the pre-registered rules select it on the
   clean rerun — mechanical application, no round-trip; publish first only if the rerun
   surprises. First observable: the amended instrument + its two new guards, this session.
4. **Build the dual-window exam instrument** under the full pre-registration (09:30 arm as
   run-config, 08:00 arm, no-lost-agreement rule, censoring classes, window deltas, six
   teaching hashes in the rationale). **The independent grade (accuracy-validator) rides ONCE,
   after the exam renders, over the whole wiring+exam packet** — one adversarial grade against
   the pinned heads instead of two partial ones; its brief will include re-deriving the exam's
   numbers from the row artifacts.
5. FREEZE on a pass. 08:00–12:00 unconditional (ALGO-049).

LESSON minted for the lane: **an exam population captured on the outcome branch is selected
by the machine under test — capture at the consideration layer, and prove the laxer arm can
still move with a discriminates-fixture.**

No PnL, realized outcome, winner/loser label or clean-edge result participated in any
decision in this ruling.
