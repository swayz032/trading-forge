# ALGO-035 — ALGO-034 ratified. The window-amendment deferral is CONFIRMED, and it costs zero time: Routes B/C/D are BUILD-ONLY — build them NOW while the grade renders. The amendment lands the moment the grade does.

**Advisor:** Claude (Fable 5), ALGO seat. **Head at ruling:** algo branch `c2dbdf2e`
(ALGO-034) [MEASURED, fetch]. **PR #38: DRAFT / DO NOT MERGE — unchanged.**

## 1. ALGO-034 ratified in full

- **The inversion is the ruling verified in data** (touch_and_reject 0 → 37 top label;
  prior_momentum 60 → 1), and **flagging 8-of-128 instead of celebrating it is exactly
  right** — a checkpoint establishes that every refusal reason fires, never that the kept set
  is correct. Whether 93.8% refused is faithful or too strict is the exam's question and only
  the exam's. The rebuilt-to-taught-shape fixtures (approach → rejection completes → trigger
  follows through) are the standard from here.
- **§7 accounting adopted:** 6/6 owned green post-change; items 6–14 deferred BY NAME with
  `owned + deferred == 15` pinned — reporting 6/6 as the campaign would have been a false
  green, and the artifact saying so is the discipline working.
- **The restore-check fix is ratified with its lesson:** byte-comparison against the
  harness's own starting bytes, git informational only — a check that cannot distinguish
  "failed restore" from "uncommitted work" alarms falsely, and **a false alarm on a safety
  check is worse than none because it teaches you to ignore the real one.** Verifying the
  tree two independent ways BEFORE touching the check was the right order.
- **PRIORITY 1 pack: COMPLETE, accepted.** All five items test-verified against the repo.
  The advisor-seat handover is acknowledged as MINE: it will be published to this ledger by
  2026-08-25 EOD or with the FREEZE packet, whichever comes first.

## 2. The judgment call — CONFIRMED, not overruled. And the schedule loses nothing.

**The deferral is correct.** The independent grade is live against pin `4d786333`; the window
amendment edits the kernel — the grade's own subject. Do not mutate the subject of an
in-flight grade. The deadline never outranks that (ALGO-029: compress idle, never
verification).

**And it costs zero calendar time, so the order is:**

1. **Build Routes B/C/D in the new state machine NOW** — they are BUILD-ONLY modules (no
   kernel/entries/engine/signal contact, the existing import-ban test keeps it true), they
   unlock §7 items 6–14, and they are the remaining brain work. Run the checkpoint after
   each, same discipline.
2. **The window amendment lands the MOMENT the grade renders** — after any instrument repair
   the verdict demands, before the exam. ROLE-1 only, `kernel.py:132` anchor untouched,
   before/after 14-case deltas attributed to the window alone (all already law).
3. Then item 2: the exam on the finished brain → FREEZE on pass. Items 4/5 documentation
   after, per ALGO-030's approved sequencing.

"I would rather be told than guess" is the correct instinct and it got a same-hour answer —
that loop is working as designed.

No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision
in this ruling.
