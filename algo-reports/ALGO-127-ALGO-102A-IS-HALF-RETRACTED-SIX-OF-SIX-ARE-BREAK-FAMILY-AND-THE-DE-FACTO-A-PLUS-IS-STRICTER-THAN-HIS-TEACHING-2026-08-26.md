# ALGO-127 — **ALGO-124 RATIFIED. ALGO-102A IS HALF-RETRACTED, AND THE HALF THAT SURVIVES IS NAMED SO THE RETRACTION CANNOT OVER-REACH.** All six bullet-spends are **`BRK5`** — verified by me at the committed census artifact, not taken from the report. A break entry fills on the **far side of the level from the rejection wick**, and his band lives **entirely on the wick side**. ⇒ *"the bot never enters at the level it is trading, 6 of 6"* **is the arithmetic of two shapes meeting, not evidence of a defect** — and **ALGO-117 §5's closing position still carries the refuted sentence.** **My ALGO-121 §4 prediction: 1 confirmed / 1 refuted / 1 unmeasurable** (no re-explanation, ALGO-106 §4). And the deduction that closes a whole family of repairs before anyone builds one: **ALGO-107 already PROVED `body_frac 0.62` / `close_loc 0.78` are STRICTER than the taught momentum sentence — and those two untaught numbers gate every one of the six bullet-spends. So the de-facto "A+" is ALREADY STRICTER THAN HIS TEACHING AND THE BULLET STILL GOES TOO EARLY. Whatever makes him pass is NOT a higher bar on the same axis.**

**Advisor:** Claude (Opus 5), ALGO seat — `trading-forge-47`. **Rules on:** ALGO-124 @ `d642f57f`,
strategy head `215bef16`. **Channel head at drafting:** `06e76518`. **PR #38: DRAFT. Nothing lands.
No repair ordered. The revert stays and is not reopened.**

---

## 1. ALGO-124 — RATIFIED, with the join re-checked here

**[MEASURED HERE]** `research/current_mnq_strategy_v2_4_algo124_bullet_census_f132617c.json`,
`rows[0..4]`: **`kernel_setup` is `BRK5` on every row**, reasons `PREBREAK_REPEAT_TEST_INTRA5_FORCE`
(×2), `ACCEPTED_BREAK_RETEST_THEN_INTRA5_FORCE` (×2), `FIRST_BREAK_PRINT_THEN_INTRA5_FORCE`. With
04-14's in-window `BRK5`, **six of six.** I read the artifact rather than the table.

The revert was already verified in ALGO-125 §2 (`levels.py` byte-identical to `a355507d`, empty diff,
exit 0). **The worker pushed code before the report, ls-remote verified, and refused to infer the four
missing setup labels from ALGO-106's prose. `A REPORT IS A CLAIM AND PROSE IS NOT A FIELD` — held, on
the one column that decided a retraction.**

### 1a. 🛑 A DISCREPANCY I AM FLAGGING RATHER THAN SMOOTHING

The worker's earlier message reported **03-24 = `REV` / `ZONE_REJECTION_STORY_THEN_INTRA5_FORCE`**,
read from the **arm artifact**. The census reports **03-24 = `BRK5` /
`PREBREAK_REPEAT_TEST_INTRA5_FORCE`, `bullet_entry 2026-03-24 08:17:00-04:00`.**

**I confirmed `BRK5` at the census artifact.** My resolution: these are **two different entries in one
session** — `entry_family_receipt` covers **in-window** entries, the census covers the **bullet-spend**,
and **the bullet-spend is the object ALGO-102A measured** (it measured the bot's *fill*).

⚠️ **THAT RESOLUTION IS MY INFERENCE, NOT MY MEASUREMENT.** I did not verify that the two records
describe different entries. **Worker: confirm or correct it.** If they describe the *same* entry then
one of the two reads is wrong and the 6-of-6 needs re-deriving — **and the whole of §2 rests on it.**
**A conclusion this load-bearing does not get to stand on an unflagged inference.**

## 2. ALGO-102A — the half that dies, and the half that does not

> ### A BREAK ENTRY IS BY DEFINITION A FILL ON THE FAR SIDE OF THE LEVEL FROM THE REJECTION WICK, AND HIS BAND LIVES ENTIRELY ON THE WICK SIDE. THE TWO CAN NEVER MEET.

**RETRACTED:** *"the bot never enters at the level it is trading — its fill lands 5.75–28.17 pt beyond
its own authorising band, 6 of 6."* The measurement is correct; **the interpretation is not.** With all
six fills break-family, the displacement is **a consequence of two shapes**, and no band drawn from a
rejection wick can reduce it. On 04-14 the ruled band's `hi` moved *down* 1.58 pt and the displacement
**grew** to 29.75 — the clean demonstration.

**SURVIVING, and stated explicitly so this retraction cannot be quoted past its scope:**
- **his fill lands INSIDE a his-rule band, 5 of 5** — a rejection-side observation this finding does
  not reach;
- **3 of 13 entries place a 17.25-pt stop INSIDE the authorising band** — likewise untouched.

**ORDERED — the correction propagates.** **ALGO-117 §5's closing position states the refuted sentence
as an established finding**, and that paragraph is the handover's summary of the campaign. **Any seat
quoting it must quote this ruling beside it.** `[repair-closes-shown-instance]`: I am closing the
*claim*, and I have **not** enumerated every place it was relied on — **`ONE CITATION CORRECTED, THE
CLASS UNENUMERATED.`** A ruled-clause register (ALGO-117 §4a) would answer this; there is not one yet.

## 3. MY ALGO-121 §4 PREDICTION — final score

**3 of 6** were authorised by **ESTABLISHED** zones the build never touches — **a case-level scope
control strictly stronger than comparing zone-ID sets**, and it lands for free. Of the 3 **SWING**-
authorised: **04-06 moved INSIDE · 04-09's zone was removed · 04-14 moved FURTHER OUT.**

⇒ **1 confirmed / 1 refuted / 1 unmeasurable.** No re-explanation.

## 4. THE SHARPENING — and the deduction that closes a family of repairs

All six bullet-spends are `BRK5`. **Every break-family trigger is gated by `body_frac 0.62` and
`close_loc 0.78`** — retired from Route A **on the operator's own answer** as untaught (ALGO-071 §3)
and **still deciding every bullet** (ALGO-106).

> **"A+ IS DEFINED NOWHERE" AND "AN UNTAUGHT PAIR DECIDES EVERY BULLET" ARE THE SAME HOLE SEEN FROM
> TWO SIDES.**

| | **ENTRY** | **DESTINATION** |
|---|---|---|
| budget | 1 trade/session — **his, frozen** | 1 TP1 |
| built as | **first fully-approved** (`engine.py:76`, `return` in-loop) | **first past `$400`** (`target_policy.py:135-186`) |
| taught as | first **A+** | first **MEANINGFUL** reaction |
| qualifier **as built** | **`BRK5` gated by two UNTAUGHT magnitudes — measured 6/6** | `touches>=2 AND quality>=0.58`, **hardcoded `True`** for the dominant family |
| qualifier **as taught** | **no definition found** | **no definition found** |

**In both layers a number-set nobody derived from him stands in for a word he used and never defined.**

### 4a. 🛑 THE DEDUCTION, and it is free — it uses a proof already on the ladder

**ALGO-107 PROVED `OLD IMPLIES NEW`:** `body_frac >= 0.62` ⇒ wicks sum `<= 0.38` range ⇒ max wick <
body — **the retired pair is STRICTLY STRONGER than the taught momentum sentence.**

⇒ **The de-facto "A+" gating every bullet-spend is ALREADY STRICTER THAN WHAT HE TAUGHT, and the
bullet still goes before his clock in every measured case.** Replacing it with the taught version
would admit **more** and fire **earlier** (ALGO-108 measured exactly that).

> ## **THE MISSING SELECTIVITY IS NOT A HIGHER BAR ON THE SAME AXIS. TIGHTENING THE TRIGGER IS ALREADY MAXED OUT AND IT DID NOT HELP.**

**This closes a whole family of candidate repairs before anyone builds one** — every "make the entry
gate stricter / raise the momentum bar / demand a cleaner candle" proposal is answered by a proof this
ladder already published. **Whatever makes him pass on a qualifying setup lives on a different axis,
and ALGO-125 §7's `no citation found` says the corpus does not name it.**

## 5. THE FOURTH BARE-NUMBER POPULATION DEFECT TODAY — and where it lived

The worker corrected its own draft: the unreproducible `923 passed` figure **does not stand in the
sunset documents' headers** — `git grep` finds `923` in no sunset document, and the one apparent hit
is **the interior of a price key (`…96923`)**. **It lives on the LADDER, which is why the join guard
could never have caught it.**

Today's four: `$22,702` (candidate list read as chosen TPs) · `923` (ladder read as sunset docs) ·
the guard's `confluence` in bucket (d) · the guard's `side` join over 355 mirrored zones.
**All four matched a name and missed a population. ALL FOUR POINTED THE REASSURING WAY.**

**And the surface matters:** ALGO-120A ruled that a commit subject is a content surface no blob grep
sees. **This is its third instance today — the LADDER is a surface the sunset-doc guard does not
cover.** ⇒ **a guard's blind spot is not only which strings it checks, but which FILES are inside its
universe at all**, and both belong in its docstring.

## 6. SUITE — ratified on the right predicate  **[RELAYED — I did not run it]**

Band head `64e3f465` → **1725/7**; reverted head `f132617c` → **1711/7**; the 14-test difference is
exactly the band-shape tests the revert removes, and **the failure set is THE SAME SEVEN BY
MEMBERSHIP at both heads.** **Membership, not count** — the correct predicate, and the one that makes
"same seven" mean something.

## 7. QUEUE

1. **Worker:** confirm or correct **§1a's resolution** — highest priority, it is load-bearing for §2 ·
   then **ALGO-126 §8's nearer-than-his census** (his marked TP's rank inside the 222
   `P_destinations_considered` rows, with `kind`/`source`/`touches`/`quality`/`meaningful` for every
   candidate nearer than his). **Report only. Derive nothing.**
2. **Advisor:** the `A+` provenance census — **§4a narrows the expected answer:** the census must also
   record that **no taught surface names a selectivity axis other than trigger strength.**
3. **CLOSED by §4a — do not propose, tonight or ever, without new taught evidence:** any repair whose
   mechanism is *"make the entry trigger stricter."*
4. **HOLD:** established-path band · magnitude census · `avoid_chart_clutter` · **the three
   reserved-class asks** — **and §4a makes the third one sharper: it is now specifically *what makes
   him pass on a break-family setup that clears a stricter-than-taught trigger*.** Still drafted.
   Still **NOT SENT.**

**STOPS unchanged:** no TopstepX · the one-bullet budget untouchable · no magnitude under the frozen
contract · no width cap · `kernel.py:207` untouched · `$1,000`/`$2,000` in no predicate · no invented
pass-rule · no raise of the `$400` floor.

---

**LESSON, minted:**

> **A MEASUREMENT AND ITS INTERPRETATION HAVE DIFFERENT LIFETIMES. ALGO-102A's NUMBERS WERE RIGHT ON
> THE DAY AND ARE STILL RIGHT; WHAT DIED IS THE SENTENCE WRAPPED AROUND THEM — AND THE SENTENCE IS
> WHAT EVERY LATER RULING QUOTED.**

*"The bot never enters at the level it is trading"* travelled four rulings deep, into a handover's
closing position, carrying a defect claim that **one column — the setup family — dissolves.** The
column was always available and nobody asked for it, because the number was vivid and the
interpretation felt like a reading rather than a claim.

> **WHEN A FINDING IS QUOTED MORE OFTEN THAN IT IS RE-DERIVED, IT HAS BECOME A PREMISE. ASK OF ANY
> LOAD-BEARING SENTENCE: WHAT SINGLE COLUMN, IF ADDED, WOULD DISSOLVE IT — AND WHY HAS NOBODY ADDED
> IT?**

*No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision in
this ruling.*
