# ALGO-070 — The §4 guard FAILS on R1: forbidden-in-window entries rose 6 → 8. R1 is NOT ratified and re-exam #2 does not run. This desk wrote a COUNT where the question needed MEMBERS — owned, not relaxed. The disposition is pre-registered before the two new members are examined.

**Advisor:** Claude (Fable 5), ALGO seat. **Rules on:** the worker's §4 guard measurement at
strategy head `a2307c46` (08:00 window, canonical baseline). **Channel head at drafting:**
`9cae179c` (ALGO-069, mine). **PR #38: DRAFT / DO NOT MERGE — unchanged.**
**DECISION: R1 NOT RATIFIED (§1) + this desk's guard-design error recorded (§2) +
disposition PRE-REGISTERED (§3) + R1b HELD (§4).**

## 1. The guard, honoured when inconvenient [RELAYED; the rows will be verified by this desk under §3]

| 08:00 window, canonical baseline | before R1 | after R1 |
|---|---|---|
| in-window entries the budget forbids | 6 | **8 — VIOLATED** |
| total decisions in window | 8 | 10 |
| decisions through window end | 39 | 42 |
| agreement | 1/8 | 1/8 |
| entered in window / genuinely declined / traded at all / bullet spent pre-window | 2 / 2 / 12 / 10 | 2 / 2 / 12 / 10 — unchanged |

ALGO-063 §4, carried onto R1 by ALGO-068 §2(iv): *"08:00-arm forbidden-in-window entries
(24 → 6) may not rise"*. It rose. **R1 as landed at `a2307c46` is NOT RATIFIED; re-exam #2
does not run on it.** The worker did not touch anything to make it pass and refused to
interpret the rise away — that is exactly the conduct the guard was written to protect.
Clause 2 (no new pre-window grant) did not fail: bullet-spent-pre-window is 10 → 10.

## 2. This desk's error, on the record — and why it is NOT a licence to relax

The guard was written in ALGO-063 §4 against a MAP-WIDENING repair ("widening what the map
authorizes is how a WAIT-by-default brain becomes permissive again") and carried unchanged
onto R1, a story-REACHABILITY repair whose intended effect is that taught break entries
become possible where a deleted level made them impossible. A monotone COUNT of
budget-blocked grant attempts cannot distinguish "the brain now finds a taught entry the
bullet had already been spent against" from "the brain grants where it should not". I
ordered the property with a mechanism that inherits that gap (my own §4 law from the
advisor-ruling skill). **That is my defect. It does not get repaired by reading the number
and then deciding it does not count** — that is the goalpost move by definition. Options
(a) restate the guard now, and (b) narrow the LOOKBACK-derived window, are both refused:
(a) is post hoc, (b) is the tuned threshold ALGO-068 forbade.

## 3. PRE-REGISTERED DISPOSITION — written before either new member is examined

The guard was declared "by membership". So the lawful next step is the one the guard's own
wording provides: **NAME the two new members** (session · bucket · zone · route · story ·
sub-reasons · candle geometry — with the clock and key published, per the ALGO-068 law)
and apply the test that was pre-registered for gained agreements in ALGO-057 F3, now
applied to gained GRANT ATTEMPTS:

Each new member must be (i) a grant by the MATCHING family for the J3-classified interaction
at that zone, (ii) on a taught story of that family with its predicate cited, (iii) not a
pre-window grant, (iv) not Route A on a BROKEN zone, and (v) the reason it did not become a
trade is the one-bullet budget alone.

- **If BOTH members pass (i)–(v):** R1 stands as landed; the §4 guard is REPLACED, for R1
  and for every later repair identically, by its membership form — *every new in-window
  grant attempt must pass (i)–(v) and is listed; no count clause* — which is stricter in
  kind (it inspects each member) and looser in nothing that mattered (a permissive grant
  still fails it). Re-exam #2 then runs under ALGO-069's expectation.
- **If EITHER member fails any of (i)–(v):** R1 is REJECTED as written. It is redesigned so
  that the failing CLASS cannot grant — by story/family semantics, never by narrowing the
  visibility window — and re-measured under the same membership guard. The 03-30 recovery
  is not kept at the price of a permissive grant.
- **Residual:** a member that cannot be classified under (i)–(v) is published as
  UNCLASSIFIED and counts as a FAIL for this disposition.

This rule is derived from the teaching (taught story, matching family, one bullet), not
from the rows; it is stated before the rows are read; and it is the same rule that will
judge every later repair. This desk will read the two members itself before anything
lands.

## 4. R1b and the rest

R1b (origin-side identity) stays UNAPPLIED until §3 resolves — the worker's refusal to stack
a second repair on an unratified first is correct: the guard result must stay
attributable. The anchor note is accepted for the freeze record verbatim: **the frozen 5/8
contains at least one membership (03-31) manufactured by the convicted H1 path, and any
comparison against it inherits that.** Operator questions #1 and #2 remain open; nothing
here depends on them.

## 5. Queue

Name and classify the two new members (§3) → this desk verifies the rows → ALGO-071 rules:
R1 stands + guard replaced, or R1 rejected + redesign → R1b → 03-31 T/P/G re-row → re-exam
#2 → grade (operator-authorized) → FREEZE or another round.

LESSON: a guard that counts is a guard that cannot say WHICH — write the membership form
first, and when a count guard fires on a repair it was not written for, honour it, own the
design error, and let the membership test decide, never the reading.

No PnL, realized outcome, winner/loser label or clean-edge result participated in any
decision in this ruling.
