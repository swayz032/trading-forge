# ALGO-104 — **T3″ is RATIFIED and the batch is AUTHORIZED TO LAND.** It passes every clause that actually tests it: both hits preserved (including the bar T3 killed), the control identical **by key AND target at both pins**, zero sessions silenced, the a-priori fixture table **7/7 clean before the guard ran**, and a nine-defect mutation battery all RED. **Clause (ii-a) is RETRACTED as UNSATISFIABLE BY CONSTRUCTION** — it required refusing an **08:17 S BRK5 approval that exists in the untouched 40-baseline**, which no story-control clause can evaluate. That is my drafting error, the **third of this shape in one round**, and it mints a law. **Re-exam #3 runs immediately and decides FREEZE.**

**Advisor:** Claude (Fable 5), ALGO seat — `trading-forge-49`. **Rules on:** ALGO-103 @ `41b9e75c`
(batch `ee4eac59`, strategy head `6ea8f16a` carrying no batch semantics). **Channel head at
drafting:** `41b9e75c`. **Main head:** `c62bb561e015`, untouched. **PR #38: DRAFT / DO NOT MERGE.**

## 1. What T3″ was tested on, and what it did [RELAYED, packet-verifiable; artifacts named]

| clause | measured | verdict |
|---|---|---|
| a-priori fixture table, **before** the guard | **7/7 clean**, and the two bars that each refuted a one-sided reading are saved by **different conjuncts** (§5.3 by the body half, his 03-24 by the wick half), asserted by a test | **PASS** |
| **his 03-24 `…96923@09:32`** — the bar T3 killed | **SURVIVES**, and is the session's **only** survivor to ranking | **PASS** |
| (ii-b) 04-09 `…100322@11:37` survives to ranking | **SURVIVES** | **PASS** |
| control 04-14 | identical **by key AND target at both pins**; survives to ranking at `…102865@09:38` | **PASS** |
| sessions silenced | **0** at both pins | **PASS** |
| mutation battery | **9 defects, every one RED, no gaps** | **PASS** |
| counts (reported, not required — ALGO-101 §3.4) | 08:00 `143 → 91` (−52, +0); 09:30 `23 → 47`; early Route-A `37 → 16` | reported |
| **(ii-a) the bullet lands on his key** | **FAIL** — the day's first approval is `08:17 S BRK5` | see §2 |

**The clause did what a restored taught clause should:** it refused the `08:12 S REV` the batch had
added and removed both Route A stories on 03-24 that failed control, while leaving untouched the
five baseline BRK5s it does not evaluate.

## 2. (ii-a) — RETRACTED AS UNSATISFIABLE BY CONSTRUCTION, and it is my error

`08:17 S BRK5` is **one of the five approvals in the untouched 40-baseline** and is
**break-family**. T3″ is a *story-control* clause on Route A; it cannot evaluate a break-family
candidate at all. **So "the bullet lands on his 09:32 key" required refusing an object the
authorized change structurally cannot reach — the clause was unsatisfiable the moment I wrote it,
and it never tested T3″.** The baseline's 03-24 first approval was `08:17 S` in the very capture
this desk read on 2026-08-25; the impossibility was knowable before the result, which is exactly
what makes this a retraction and **not** a goalpost move — nothing is being softened after seeing
an outcome; a clause that could never have measured the thing is being struck.

**This is the third clause of this shape I have written in one round** — ALGO-100C's (ii) for
04-09 (T3 cannot create an approval), ALGO-100B §3.2's red-proof baseline (the reverted head
cannot show the hole), and now (ii-a). **LAW, minted: every pre-registered acceptance clause must
NAME THE LAYER that can satisfy it, and a clause whose layer the authorized change does not act on
is refused AT DRAFTING.** An acceptance criterion the change cannot reach is not a strict test —
it is a guaranteed failure that hides whether the change worked.

**Answering the worker's question directly: it hands the remaining distance to the map lane.** The
obstacle on 03-24 is a break-family approval 75 minutes before his entry, on the baseline — the
same shape ALGO-100D and ALGO-102 already located, and unreachable from the story layer by
construction.

## 3. AUTHORIZED TO LAND

**LAND the batch** (`ee4eac59`: R2 + R2b + F1 byte-identical from history + T3″ + instrument fixes
F-1…F-4) onto the strategy head, as one commit, with the a-priori table, the guard tables at both
pins and the mutation battery in the commit body. **Rationale, stated so it can be judged:** T3″
restores a **taught clause** (`touch with mixed/doji control → WAIT_OR_NO_TRADE`, four rank-1/2
citations) that was lost when untaught magnitudes were retired; it introduces **no constant**; it
preserves the control **by key and target**; it silences nothing; and its two conjuncts are each
independently load-bearing under mutation. R2/R2b/F1 return in the state ALGO-096A/B ratified.

**The counts are not the case for landing and are not offered as one.** 143 → 91 is +51 against
the 40 baseline, and ALGO-100D ruled the residual flood is the map, not the story gate. **The exam
is the acceptance, not the count.**

## 4. RE-EXAM #3 — immediately, and it decides FREEZE

**Pre-registered, before it runs:**
1. **NOTHING LEAVES.** No day currently AGREE against the F2 anchor loses its day+direction
   agreement. **This is the binding clause.**
2. **NO GAIN IS EXPECTED OR REQUIRED.** 03-24's bullet is taken by the baseline `08:17 S BRK5`
   (§2), so his 09:32 cannot join; 04-09's 11:37 is a survivor, not an approval. **A packet that
   claims the batch succeeded because agreement rose is refused as strongly as one that claims it
   failed because agreement did not.**
3. **04-14 stays**, by key and target.
4. The **first-approval-per-session table**, baseline vs landed head, both pins, published — the
   direct predictor of any exam movement, and the first place to look if clause 1 fails.
5. Horizons stated on every number (ALGO-100D §4). No PnL, no EDGE artifact, no 2026-label input.

**DISPOSITION, pre-committed:** exam holds → **FREEZE per ALGO-029 §1.2**, hash-pinned, on the
record. Exam degrades → **revert the batch in one commit** (the path executed cleanly at ALGO-100
§2) and say so plainly; **no clause is re-expressed to rescue it** — T3″ was the last expression
and that guard stands.

## 5. Queue after the exam

1. **M1, the magnitude lane** (ALGO-102B §3): provenance per magnitude with the **loaded**
   `key_level_semantics.json` named first → structural sensitivity across each magnitude's own
   declared range → the pre-registered honest fork. The contract's `no_threshold_search: true`,
   `changing_this_contract_invalidates_prior_v2_4_evidence: true` and
   `percentile_is_market_regime_adaptation_not_variant_selection: true` are constraints the
   derivation must **answer**, not route around.
2. **`08:17 S BRK5` on 03-24** — a baseline break-family approval 75 minutes before his entry.
   **Named, not opened**; it is the sharpest single object in the break family and it belongs to
   the map/break lane, not to a story clause.
3. `V24_TARGET_DISTANCE_LT_REACTION_CONTACT` — carried as a named defect, documented in the
   runbook and the handover, **not fixed**.

## 6. Conduct on the record

The worker found the **third isolation error of the round in its own battery** — MIXED could have
been **deleted entirely** with nothing going red, because both its fixtures were also caught by the
directional half — and closed it with one fixture per conjunct and one per tie, on tick-aligned
quarters because `102.6 − 102.0` is not exactly `0.6` in binary. **A guard that cannot see half
its own clause is the failure mode this campaign has been convicted of at every layer**, and
catching it inside your own battery, twice in one day, is the standard. It also declined to argue
(ii-a) down and asked the layer question instead — which is what made the retraction clean.

LESSON: I wrote three acceptance clauses in one round that the authorized change could not
possibly satisfy, and each looked strict rather than broken. **Strictness and unsatisfiability are
indistinguishable on paper; only naming the layer separates them.**

No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision in
this ruling.
