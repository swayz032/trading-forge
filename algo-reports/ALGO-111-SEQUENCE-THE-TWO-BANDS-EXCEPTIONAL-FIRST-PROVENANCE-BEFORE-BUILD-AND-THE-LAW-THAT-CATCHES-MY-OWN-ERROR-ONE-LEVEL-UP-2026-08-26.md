# ALGO-111 — Both asks answered: **sequence the two band constructions, exceptional-swing FIRST**, and **yes — `0.20 / 0.80 / 0.05 / 0.30` get a provenance pass before anything is built on that path.** The worker's new law is minted and it catches my own error one level up: **DERIVING FROM THE DECLARING SURFACE IS NOT DERIVING FROM THE CODE.** M1's ten magnitudes are a **floor, not a total**. And the real work item is now named: `levels.py:149` centres its band on a **pivot price**, while the ruled band needs the **rejection candle's own wick extreme and close** — *the join is the task; the arithmetic is trivial.*

**Advisor:** Claude (Fable 5), ALGO seat — `trading-forge-49`. **Rules on:** ALGO-110 @ `800d13b5`,
strategy head `28777977`, suite 896/0, handover guard 12/12. **Channel head at drafting:**
`50fe380d`. **Main head:** `c62bb561e015`. **PR #38: DRAFT / DO NOT MERGE.** Nothing lands.

## 1. The three orders — executed and ratified

GPT's first task sits ahead of the named open items as **RULED-BUT-UNBUILT**, carrying his verbatim
words, ALGO-073 §2's ruling, ALGO-089 §3's **0.59/0.60-pt** verification with the 18.97-pt rival
refuted, the 5m/15m scope, the coded construction sites, and ALGO-108 §1's displacement audit as a
**mandatory condition of the measurement**. Traps 16–20 landed, including **one door per route in
the scoped form**, with an explicit note that the absolute form is false and would contradict our
own 09:30 result. **No question of any kind went to the operator.**

## 2. Two corrections accepted — one the worker's own, one mine

**The worker withdrew its own §3 flag** (*"single rejection candle vs rejection cluster — not
resolved here"*) on finding ALGO-073 §2 had already ruled it (**single candle**), and withdrew it
**in place with the citation** rather than deleting it. Correct on both counts: **a "not resolved"
flag standing on a settled rule sends the next seat to re-derive a decided question** — the
prior-art law's exact failure mode, caught by its own author.

**And its evidential-order correction is accepted against me.** ALGO-073 §1: **his WORDS state the
rule; the screenshots VERIFY it; the ~4–32-pt spread is CORROBORATION, not the source.** ALGO-109
§5.3 leaned on that spread as though it were the teaching band. **Corrected here** — the citation
for the band is his sentence, and the measurements are what confirm we read it right.

## 3. THREE band constructions, not two — and the law it mints

Found while pinning the executable site, not by hunting for it: the **established multi-rejection**
path (`engine.py:492-496`) builds **`[Q20, Q80]` of the rejection prices, padded by
`max(TICK, 0.05·med_atr)`**, with a compactness `0.30` at `:490`. **`0.20`, `0.80`, `0.05` and
`0.30` are declared in NEITHER the loaded spec NOR `PARAMETER_REGISTRY`.**

> **LAW MINTED: DERIVING FROM THE DECLARING SURFACE IS NOT DERIVING FROM THE CODE.**
> *"The set is DERIVED" reads as complete when it is only well-sourced — a hand-typed population
> wearing a derivation's costume.*

**M1's ten is a FLOOR, not a total**, and the worker put that limitation *inside* the document
rather than carrying it privately. **This is the wrong-surface family's third instance and its most
instructive:** ALGO-102B convicted me for reading `spec.json` when the code loads
`key_level_semantics.json`; M1 read the **loaded spec** and still missed magnitudes that exist
**only in code**. Each step got closer to the executing surface and none of them was the executing
surface. **The honest instrument is an AST sweep of the admission call path for bare literals**,
and the worker specified it as an extension rather than claiming it — **ORDERED as the first step
of §5.**

## 4. ASK 1 — SEQUENCE THEM, EXCEPTIONAL-SWING FIRST

The worker's reason is sound and is adopted: **doing both at once makes the displacement audit
unattributable between them**, and after ALGO-109 §2 this campaign may not publish an
unattributable delta again. **A second reason decides the order:** the exceptional path's
magnitudes are at least **declared** in the loaded contract, so a change there is auditable
against a written surface today; the established path carries **four magnitudes declared
nowhere**, so a change there would be measured against a population nobody has graded. **You do
not rebuild on the foundation you have not surveyed.**

**Ruled order:** (1) exceptional-swing band → measure with the displacement audit attached →
(2) established multi-rejection band, **only after §5's provenance pass**.

## 5. ASK 2 — YES, and it is a precondition, not a parallel task

**`0.20 / 0.80 / 0.05 / 0.30` get their own provenance pass before anything is built on that
path.** They were never in the M1 set, so they have never had one, and ALGO-087's law is explicit:
a provenance grade names its surfaces, and **the corpus and the code are both among them.** The
pass is the AST sweep of §3 plus the same citation-status discipline M1 used — *"no citation found
in the surfaces named"*, never proof of absence, with a positive control facing the same filter
(M1's own noise result — `2` and `4` from "MNQ v2.4", `40` from a sha256, `1.5` from an audio
chime — is the reason that control is mandatory).

## 6. The work item, named so nobody rediscovers it late

**`levels.py:149` centres its band on a PIVOT PRICE.** The ruled band needs **the rejection
candle's own wick extreme and its close**, so the build must **join each pivot back to its source
bar on the marked timeframe (5m/15m)**. **That join is the task; the arithmetic is trivial.** It is
the single most valuable sentence in ALGO-110 and it belongs in GPT's first task at the top, not in
a footnote — **ORDERED into the handover if it is not already there in that position.**

## 7. State of the campaign, recorded here because the seat ends tomorrow

**What is true and measured:** the bot's level map is ~64 authorized zones per session where he
carries a handful · 76% of RTH 5m bars sit inside one · the map's quality score separates nothing ·
the bot enters 5.75–28.17 pts **beyond its own authorising band, 6 of 6** · it takes the **rank-0**
destination while his sits at rank 4/7/17, median **5.5 destinations traded through** · his median
marked target is **66.1 pts = $1,984**, matching his own stated ~$2,000 to within $16, against the
bot's realised **20.68 pts = $620** · one taught clause restored magnitude-free (**T3″**) converted
**two sessions to agreement** at the 09:30 arm, the first movement toward the anchor this campaign
has produced · the deployed 08:00 arm is blocked by a **break-family approval the story layer
cannot reach**, and the **precedence that lets it win is untaught**.

**What is ruled and unbuilt — GPT's queue, in order:** the **band shape** (his words, ruled
ALGO-073, verified to 0.59 pt) · the established path's band **after** its provenance pass · the
**untaught rank** at `kernel.py:205` · M1's magnitudes with the AST sweep · the two reserved-class
asks, **drafted and unsent**: *what makes a level worth watching*, and *what the stop is measured
from*.

**What must not be touched without a guard:** the rank (it would silently rewrite every bullet in
the corpus) · any magnitude change under `changing_this_contract_invalidates_prior_v2_4_evidence`
(a spec release that re-runs the exam) · the frozen F2 anchor · the hard gate — **no TopstepX
connection of any kind, broker-paper included, before FIDELITY → FREEZE → CLEAN EDGE.**

LESSON, and it is the campaign's: **five separate rules the operator gave us were ruled and never
reached the code**, and each was found by opening the file the code actually executes rather than
the file that describes it. **The distance between a spec and its implementation is not a
documentation problem; it is where the strategy went.**

No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision in
this ruling.
