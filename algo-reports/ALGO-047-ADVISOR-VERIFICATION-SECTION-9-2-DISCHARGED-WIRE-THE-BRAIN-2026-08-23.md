# ALGO-047 — ADVISOR VERIFICATION PUBLISHED: convicting instruments green at the repaired head, and my own planted M13 goes RED. **§9.2 IS DISCHARGED. WIRE THE BRAIN.**

**Advisor:** Claude (Fable 5), ALGO seat. **Verified head:** strategy `8dc9d7e2` (the ALGO-044
repairs head), in a read-only `git archive` arena — the live tree untouched. **PR #38: DRAFT /
DO NOT MERGE — unchanged.**

## 1. What I ran, myself [all MEASURED HERE]

1. **The three convicting test files at the repaired head** —
   `test_…_genuine_decline_is_reachable.py` (the F-1 positive controls + the
   crash-becomes-classification red-proof), `test_…_censoring_symmetry.py` (the repaired
   F-4/F-5 headline tests), `test_…_bot_entry_rate.py` (the now-falsifiable never-declines
   test): **31 passed, 0 failed.**
2. **My own discrimination attack — the M13 shape planted by MY hand, not the worker's:**
   re-classed 2026-03-23 `MISSED_TRADER_ENTRY → AGREE` in the scorecard with the summary
   fields updated consistently, exactly as a future generosity bug's emitter would — the
   shape that previously survived all seven tests. Result: **2 tests RED**
   (`…is_the_STRICTER_reading`, `…FLATTERS_the_bot`). Restored byte-exact: **10/10 green.**
   The repaired tests discriminate; they are no longer checking the artifact against itself.

Independence held: the doer wrote the repairs; the refutations came from two independent
grades; this verification ran the convicting instruments and an attack of my own choosing in
an isolated arena. Coverage honesty: I did not re-run the full baseline end-to-end — that
diff artifact remains the worker's item from ALGO-046 §3 and rides in its next packet.

## 2. **RULING: §9.2 IS DISCHARGED.** The gate the whole semantics phase waited on is open.

**ORDER — WIRE THE BRAIN:** connect the derivation layer + four-route WAIT-by-default state
machine (with the BRK15 variant) as the kernel's entry authority, per ALGO-009 §3/§6 and
every standing ruling (completed-bar reading, ALGO-033; the taught stories load-bearing;
force triggers a valid story but can never create one; the one-bullet budget on the shared
primitive; no case-specific branches). This is the semantic switch — the first authorized
change to kernel/entries since `068bb24a`. Mutation arms stay green through the wiring;
byte-exact restore discipline applies to everything the wiring does not intend to change.

Then, in order: the revert executes (ALGO-043-REVERT) if not already landed → the
`acceptance_bars` rerun (land 3 on a clean mechanical selection, ALGO-046 §1.3) → **the
dual-window exam** under the full pre-registration (09:30 + 08:00 arms with the
no-lost-agreement rule; censoring classes; window deltas) → **FREEZE on a pass.**

The exam is the judgment of the wired brain against the trader — 5/8 is the number to beat
honestly, and the pre-registered rules, not the score, decide what lands.

No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision
in this ruling.
