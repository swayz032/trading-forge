# ALGO-148 — **THE REGISTER'S ACTIONABLE CLASS ADJUDICATES TO ZERO. `GENUINELY_UNBUILT` = 0 of 3.** All three evidence-origin `SPEC_ONLY` rows are `BUILT_BY_BEHAVIOUR`, each at a named line. **The worker states it against its own instrument and refuses to dress it as a breakthrough — correct on both counts.** ⇒ **`avoid_chart_clutter` remains the only confirmed taught-and-unbuilt clause the register itself found, and it was found by READING, not by the register.** **🛑 BUT THE NIGHT'S REAL FINDING IS UNCHANGED AND IT IS BIGGER THAN THE REGISTER: THE TP LADDER IS TAUGHT, EVIDENCED IN THREE HASH-BOUND SCREENSHOTS, ABSENT FROM `spec.json`, AND BUILT NOWHERE — AND IT DIRECTLY EXPLAINS THE FOUR SESSIONS WHERE HIS SETUP REACHED THE TARGET LAYER AND WAS REFUSED.** **8 sessions the trade IS his setup · 4 refused only at the target · that is 12 of 14 standing on one unbuilt taught feature.**

**Advisor:** Claude (Opus 5), ALGO seat — `trading-forge-47`. **Channel head at drafting:** `850e4a29`.
**PR #38: DRAFT. Nothing built.**

---

## 1. THE THREE ROWS — all built, each at a line

| clause | verdict | site |
|---|---|---|
| `allowed_destination_families` | **BUILT_BY_BEHAVIOUR** | `KEY_ZONE_15M targets.py:263` · `LIQUIDITY_CLUSTER :282` · `FVG_15M :308` |
| `regular_level_family` | **BUILT_BY_BEHAVIOUR** | **fail-closed by a `raise`**, `levels.py:167` `V24_LEGACY_PRIOR_DAY_WEEK_REFERENCE_FORBIDDEN` |
| `resistance_behavior` | **BUILT_BY_BEHAVIOUR, both halves** | side-tagged FVGs `fvg_interaction.py:38-45` wired at `kernel.py:299` · the disrespect/clear path at `kernel.py:340-351`, **intent recorded at the line** |

**My ALGO-147 §4 caution was the right one and it held on the first row: `SPEC_ONLY` is where the real
ones live, not a list of defects.** **A clause unread by name can be enforced by a `raise`.**

## 2. 🛑 THE FOURTH SURFACE-SHAPED ERROR OF THE DAY — and the first where the answer was "it IS built"

The worker reasoned that *"must clear the opposing FVG"* would be enforced by the opposing-blocker
refusal at `target_policy.py:139`, gated on `not d.meaningful`, **and FVG destinations are hardcoded
`meaningful=True` at `targets.py:309`** — so the gate is structurally unreachable for FVGs. **It had a
live positive control** (`weak_blocker` in three production modules) **and a clean negative.**
**Everything pointed at `GENUINELY_UNBUILT`.**

**It was the wrong LAYER: a TARGET-surface search for a clause that lives in the ENTRY surface.** What
saved it was that `disrespect` matched `kernel.py` **and it read the line instead of counting it.**

> ## **A LIVE POSITIVE CONTROL IN THE WRONG SURFACE IS A BETTER FALSE PROOF THAN NO CONTROL AT ALL — IT MAKES THE ABSENCE LOOK MEASURED. NAME THE LAYER BEFORE ACCEPTING AN ABSENCE, NOT JUST THE TREE AND THE TERMS.**

**Today's four:** wrong **tree** (`FETCH_HEAD`) · wrong **population** (the `side` join, the residual-less
pre-registration) · wrong **grouping key** (the 74 contests) · wrong **layer** (this). **`[absence-claim]`
now has four dimensions and this is the one that produces a false NEGATIVE finding rather than a false
positive.**

## 3. WHAT THE REGISTER IS AND IS NOT — stated against my own order

**It is not a breakthrough and I am not presenting it as one.** Its counts do not decide anything:
`EVIDENCE_ONLY 84` is undecided by construction, `BUILT 69` is a floor, and **the class I called
"directly actionable" adjudicates to zero.**

> **ITS VALUE IS THAT IT CONVERTED AN UNBOUNDED QUESTION INTO A SMALL, FINISHABLE LIST — three rows,
> three code reads, a definite answer in under an hour. THAT IS WORTH HAVING AND IT IS NOT WHAT I
> ORDERED IT FOR.**

**I ordered it as the breakthrough. It is a triage tool.** The distinction matters for the next seat:
**a register tells you WHERE TO READ. It cannot tell you WHAT IS TRUE, because a label is not an
implementation** — which is exactly what the `17.25_point_stop` counter-example says.

## 4. AND THE ACTUAL FINDING IS STILL THE LADDER

**Confirmed, and none of it is retracted by §1:**
- **Taught:** `tp_ladder.labels = [TP1, TP2, TP3_OR_NEXT_MEANINGFUL_REACTION]`,
  `multiple_directional_tps_allowed: true`
- **Evidenced:** three hash-bound screenshots — `multi_target_ladder`, `tp1_tp2`, `tp1_tp2_tp3`
- **Absent from `spec.json`** (0 hits, positive control 7)
- **Built nowhere** — `class Target` carries one `executable_price`

**And it explains a measured outcome, which nothing else tonight does:**

| | sessions |
|---|---|
| the trade **is** his setup | **8** |
| his setup reached the target layer and was refused — **all four `TP1_REFERENCE_REWARD_UNDER_400`** | **4** |
| **⇒ standing on the target layer** | **12 of 14** |

> ## **FOUR SESSIONS ARE REFUSED BECAUSE THE BOT'S ENTIRE REWARD IS ONE CLOSE TARGET. HE DOES NOT HAVE ONE CLOSE TARGET — HE HAS A LADDER. THAT IS THE ONE UNBUILT TAUGHT FEATURE WITH A MEASURED CONSEQUENCE ATTACHED.**

⚠️ **NOT CLAIMED: that a ladder makes those four trade.** Whether TP1-under-`$400` still blocks when
TP2/TP3 exist **depends on what the floor is defined over in a laddered system, and that is precisely
the derivation ALGO-145 §5 ordered and nobody has done.** **The ceiling test removed the floor
entirely and moved 8 → 9 in both directions — a ladder is a different change and its result is
unmeasured.** **I am naming the mechanism, not predicting the outcome.**

## 5. QUEUE

1. **ALGO-145 §5's ladder derivation — from held evidence only, no rung count, no split, no partial
   size. To this desk before any code.** It is the only lane with a measured consequence.
2. **`EVIDENCE_ONLY 84`: UNDECIDED, uncharacterised, not a number that goes to anyone.**
3. **Not authorized:** any repair · the `$400` floor · the ladder build itself.

---

**LESSON, minted:**

> **I ORDERED THE REGISTER AS THE BREAKTHROUGH AND IT RETURNED A CLEAN NULL ON ITS ONLY ACTIONABLE CLASS. THE TWO REAL FINDINGS OF THE NIGHT — `avoid_chart_clutter` AND THE TP LADDER — WERE BOTH FOUND BY READING, ONE OF THEM BY THE OPERATOR HIMSELF.**

**A tool that enumerates cannot find what nobody wrote down under a searchable name**, and both real
findings were exactly that: a spec key nothing reads, and a taught structure that never reached the
spec at all. **The register would have missed the ladder entirely had he not told us it was there.**

**That is not an argument against the register. It is an argument about what it is for: it bounds the
reading, it does not replace it** — and I should have ordered it as triage rather than as the answer,
**under a deadline, which is when the difference costs the most.**

*No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision in
this ruling.*
