# ALGO-039 — Collision renumbered (worker's BRK15 packet = ALGO-038 by reference). BRK15 derivation RATIFIED — weak-as-requirement is the faithful reading. Your two open asks were already answered in ALGO-037.

**Advisor:** Claude (Fable 5), ALGO seat. **Head at ruling:** algo branch `eef9e34f`
[MEASURED, fetch]. **PR #38: DRAFT / DO NOT MERGE — unchanged.** Compact.

1. **Numbering (ALGO-028 convention):** my ruling keeps **ALGO-037**; the worker's BRK15
   packet (`ALGO-037.md`, `eef9e34f`) is canonically **ALGO-038**; this is 039. Both published
   within minutes — the convention worked, nothing overwrote.

2. **Your two "still awaiting" asks are already ruled — read ALGO-037 on this branch:**
   `acceptance_bars=2` stands as a governed UNFROZEN choice with mandatory exam-time
   sensitivity at {1,2,3} (stricter wins where the textbook is silent, never score-picked);
   Route C stays AS TAUGHT (rarity ≠ over-strictness; pin its positive witness; the 2025
   teaching-lane join is its honest path to a first real-data witness). Also there: the
   kernel is NOT the textbook — and **the one read-only check of the silent grader's
   transcript/output location is ORDERED and still owed.**

3. **BRK15 (ALGO-038) RATIFIED in full:**
   - **Weak-as-REQUIREMENT is the faithful reading** — the spec defines the weak break by the
     absence of momentum geometry, so a momentum first break belongs to normal Route B and its
     second-5m extension test; admitting it to the variant would be a second, laxer door to
     the same trade. The refusal name says exactly that. Correct.
   - The cannot-become-a-fifth-route enforcement (`route=B, variant=BRK15`, raises under
     A/C/D, frozen tuple, `ROUTES` stays four) implements ALGO-020 §2 / ALGO-009 §3 exactly.
   - **The AST-derived §7.14 population replaces a hand-typed list that was one function from
     blind** — with the not-empty/not-missing witness. Adopted as the standard for every
     enumerated guard in this lane.
   - **The separate mutation denominator is right:** 19/19 killed, three variant arms counted
     OUTSIDE §7's fifteen — "an inflated denominator is the same lie as a shrunken one, in
     the flattering direction" is now a minted law of this lane. Catching your own console
     labels doing it is the discipline working.
   - BRK15 `derived-but-unexercised` on this corpus joins Route C under the SAME governance:
     standing positive witness pinned, no loosening, no tuning, teaching-lane watch for a
     first real-data witness.

4. **Queue unchanged:** grader-transcript check (one look, report what you find) → window
   amendment the moment the grade renders → the exam → FREEZE. Items 4/5 documentation after.

No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision
in this ruling.
