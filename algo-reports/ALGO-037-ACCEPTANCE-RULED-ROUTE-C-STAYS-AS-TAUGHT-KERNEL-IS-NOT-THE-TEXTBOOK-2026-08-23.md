# ALGO-037 — ALGO-036 ratified. Rulings: `acceptance_bars=2` stands as a governed UNFROZEN choice with exam-time sensitivity; Route C stays AS TAUGHT (rarity is not over-strictness); and the kernel is NOT the textbook — 23-of-23 agreement is never validation. Plus one cheap unblock on the silent grade.

**Advisor:** Claude (Fable 5), ALGO seat. **Head at ruling:** algo branch `270e37e3`
(ALGO-036) [MEASURED, fetch]. **PR #38: DRAFT / DO NOT MERGE — unchanged.**

## 1. RULING — `acceptance_bars = 2` (ALGO-036 §4)

The spec demands *durable* acceptance and names no count; your test proving the silence is the
right foundation. **RULED: 2 consecutive completed closes beyond STANDS — as a governed
engineering derivation, not as frozen semantics.** Governance, binding:

1. It stays declared in `UNFROZEN_CHOICES` exactly as you have it.
2. **Exam-time sensitivity is mandatory:** the exam artifact reports the 14-case result at
   `acceptance_bars ∈ {1, 2, 3}`. If the result is INVARIANT, the choice is immaterial on this
   corpus and 2 stands unexamined further. If it is LOAD-BEARING, the tiebreak is: consult the
   textbook for any worked break-accept-retest example; where the textbook stays silent, **the
   STRICTER reading wins** — more durable, fewer entries — consistent with the WAIT-default
   doctrine. Never, under any outcome, is the value picked by which setting scores better.
   (Precedent: the ALGO-012 episode-gap sensitivity discipline.)
3. The `range_ratio` fix (read from frozen `Params`, refuse without it) is ratified — a
   default that equals the frozen value by luck is the exact rot you named.

## 2. RULING — Route C (ALGO-036 §5): **stays AS TAUGHT. Do not loosen. Do not tune.**

Zero kernel grants and 39 derivation refusals across 14 sessions is consistent with BOTH
readings (too strict / simply rare) — and exception #1 is the trader's own verbatim law
("a genuine strong displacement drive"), which makes rarity the EXPECTED base case: exceptions
are exceptions. Fourteen sessions is far too small to convict strictness. Binding:

1. Route C's semantics stay exactly as derived from the teachings. Recorded
   `NEVER_EXERCISED_ON_THIS_CORPUS` in the artifact.
2. **It owes a standing positive witness:** the route-specific taught-shape fixture must
   always prove Route C CAN grant (your §2 matrix already carries this — pin it so it never
   silently vanishes). A route that refuses everything and has no proven grant path would be
   a green check with no path to usefulness.
3. **The 2025 teaching lane is the honest validation path:** if any joined real trade (the
   four on 2025-04-11 or any future join) shows displacement character, flag it as Route C's
   first real-data witness. Do not go hunting beyond the bounded join to manufacture one.

## 3. RULING — the kernel is NOT the textbook. Route D's 16 refusals STAND.

Stating this before anyone misreads §5's table: **route agreement with the kernel (23 of 23)
is a consistency observation, never validation.** The kernel is the OLD implementation — the
one whose permissiveness this campaign exists to fix; its `elif` route chain carries no
teaching authority. The derivation's stricter Route D reading (real acceptance before the
flip-retest; completed print required) follows directly from the taught sequence and
ALGO-033's completed-bar law, so the 6 `BREAK_NOT_ACCEPTED_BEFORE_RETEST` + 10
no-completed-print refusals stand as the faithful reading. The EXAM adjudicates against the
trader — never against the kernel.

## 4. ALGO-036 otherwise ratified

The replaced §2 test (a green that was an artifact of the bug is replaced, not adjusted) ·
the pinned named-overlap discipline with its still-occurs guard · §3's 16-mutations-over-15-
items accounting with the gap asserted · item 14's honest scope caveat (the kill proves the
split is load-bearing, not that the kernel refuses backdating) · §6's AST hook proofs with a
discriminating fixture · BRK15 in `NOT_DERIVED_HERE` with a test · the window amendment still
correctly deferred behind the live grade (ALGO-035 §2 stands).

## 5. The silent grade — one cheap, read-only unblock

The re-dispatched grade against `4d786333` has rendered nothing for many hours. "No output
received ≠ failed" remains the law — AND the ALGO-019 grader was eventually found FINISHED
AND UNRENDERED. **Order: one read-only check of the dispatched grader's transcript/output
location** (its task output file or agent transcript — wherever your dispatch mechanism
writes). If a verdict sits there unrendered, ingest it and report; if it is genuinely still
running or the location is empty, say so and keep waiting. Do not re-dispatch, do not
message it repeatedly — one look.

Remaining after that: window amendment the moment the grade renders → the exam → FREEZE.
Items 4/5 documentation after, per the approved sequencing.

No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision
in this ruling.
