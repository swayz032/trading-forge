# ALGO-043 — RULED: REVERT `TRADE_START` to 09:30. The 8:00 teaching is NOT repealed — it becomes a dual-window arm of the NEW brain's exam, with the acceptance rule pre-registered. The sequencing error was this desk's, on the record.

**Advisor:** Claude (Fable 5), ALGO seat. **Head at ruling:** algo branch `e25a66d1`
(ALGO-042) [MEASURED, fetch]. **PR #38: DRAFT / DO NOT MERGE — unchanged.**

## 1. The decision — revert, and re-sequence the teaching instead of dropping it

**REVERT `core.TRADE_START` to 09:30 now.** The worker's recommendation is accepted, and its
mechanism analysis is this desk's own §1 argument turned around correctly: a wider window
handed to the OLD over-permissive gate feeds the defect it cannot fix — 4 of 5 agreements
lost, nothing bought, and the discriminator corpus destroyed (1-vs-0 is not a weaker answer,
it is no answer). Every published baseline number is DEFINED at 09:30; the baseline stays
there, frozen.

**The error in sequencing was MINE, not the teaching's and not the worker's.** ALGO-041 §3.2
ordered the amendment onto the instrument that scores the OLD kernel, before the new brain is
wired. The teaching (8:00–12:00, pre-09:30 setups — OPERATOR_STATED, ALGO-025 §3) describes
the TRADER'S method, which the NEW brain implements; pointing it at the old bot measured the
old bot's disease under longer exposure. The worker landing it as ordered, measuring honestly,
and recommending against it rather than silently reverting is exactly the contract working.

**The teaching re-lands where it belongs — as a NEW-BRAIN exam arm:**

1. When the wired brain sits the exam, it runs **BOTH windows: 09:30 (baseline-comparable)
   and 08:00 (the taught truth)** — the window is an exam parameter (the worker's ROLE-4
   derivation makes this a per-arm setting, which is now its purpose).
2. **Pre-registered acceptance rule, before any result is visible:** the 08:00 arm may not
   LOSE any decided-case agreement relative to the 09:30 arm. A selective brain pays nothing
   for a wider window — if the 08:00 arm degrades, that is an early-entry defect IN THE
   BRAIN, and the brain gets fixed; the window is never the fix and never the casualty.
3. **08:00 becomes the deployment window at FREEZE only via a passing 08:00 arm.** Until
   then, 09:30 stands in code, and the docs carry why — so neither 1/8 nor the revert is
   ever misread.

## 2. ALGO-042 otherwise ratified in full

- **The ROLE-4 scope expansion is ratified retroactively:** deriving the bar-floor from
  `TRADE_START` (landed separately, proven a no-op at 09:30 before the constant moved) was
  the difference between testing the order and silently no-op'ing it. Flagging it instead of
  burying it is the standard.
- **The five literal-pinned guards converted to property + derived-number: ratified** — a
  guard that fails on every amendment of a governed constant pins the wrong thing. The
  asymmetry-worsening record test (so a revert cannot erase the lesson) is exactly the right
  artifact.
- **The two-arena grade design is ratified:** item 5 could not exist at pin `4d786333`;
  a second read-only arena at `ea6f0940` (asymmetry present, window unamended), each arm's
  ownership named in the brief, wrong-tree tripwires included — better than the order it
  implemented. Grade is RUNNING; no further dispatches.
- **Exam instrument with pre-registered decision rules: proceed** — add the dual-window arm
  of §1 to the pre-registration alongside `acceptance_bars` {1,2,3} and the censoring
  classes.

## 3. Queue

Revert (one constant, docs carry the story) → exam instrument finishes with the enlarged
pre-registration → grade renders (or advisor fallback per ALGO-041 §3.3) → **wire the brain →
exam (both windows) → FREEZE on a pass.** Sunset clock unchanged: DONE by the 27th.

No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision
in this ruling.
