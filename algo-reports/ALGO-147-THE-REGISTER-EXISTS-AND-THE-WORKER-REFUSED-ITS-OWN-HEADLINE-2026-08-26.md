# ALGO-147 — **THE RULED-CLAUSE REGISTER EXISTS, RUNS, AND ITS CONTROL PASSED — AND THE WORKER REFUSED TO SHIP ITS OWN HEADLINE NUMBER BECAUSE IT IS FALSE.** **[VERIFIED HERE]** `CONTROL: PASSED` with all three known cases resolving correctly · `278` tokenised clauses · `{BUILT 69, EVIDENCE_ONLY 84, SPEC_ONLY 125}` · restricted to his evidence: `{BUILT 13, EVIDENCE_ONLY 84, SPEC_ONLY 3}` · **`13` prose clauses enumerated and EXCLUDED rather than absorbed.** **🛑 `EVIDENCE_ONLY 84` IS NOT "84 TAUGHT RULES ARE MISSING." It means the LABEL STRING is absent — and `17.25_point_stop` lands in that bucket while being the most thoroughly verified rule in the campaign.** **THE CONTROL FAILED ON ITS FIRST RUN — `avoid_chart_clutter` was not in the clause list at all, so `SPEC_ONLY` was structurally unreachable for the exact class it exists to name.** ⇒ **A CONTROL THAT CAN ONLY CONFIRM WHAT THE INPUT ALREADY CONTAINS IS NOT A CONTROL.**

**Advisor:** Claude (Opus 5), ALGO seat — `trading-forge-47`. **Channel head at drafting:** `72c81442`.
**Strategy head `9fc92807`.** **PR #38: DRAFT. `status: DIAGNOSTIC ONLY. Reports. Derives nothing.`**

---

## 1. THE INSTRUMENT — and every caveat is a FIELD, not a message

```
CONTROL                      PASSED
control_detail               avoid_chart_clutter → SPEC_ONLY  ✓
                             tp_ladder → EVIDENCE_ONLY        ✓
                             multiple_directional_tps_allowed → EVIDENCE_ONLY ✓
clauses_with_a_searchable_token   278
counts                       {BUILT 69, EVIDENCE_ONLY 84, SPEC_ONLY 125}
counts_restricted_to_HIS_EVIDENCE_origin  {BUILT 13, EVIDENCE_ONLY 84, SPEC_ONLY 3}
prose_clauses_enumerated_and_excluded     13
evidence_grade   GREP-LEVEL throughout. A production reference is a REFERENCE.
                 An unopened match is NOT a verified implementation.
production_definition  research/current_mnq_strategy_v2_*.py excluding run_*.
                 A clause referenced only by a run_* diagnostic or by a test is NOT built.
surface_NOT_covered   video_evidence.md's numbered principles - PROSE, no token,
                 enumerated below and excluded rather than silently absorbed
```

**`production_definition` encodes the `avoid_chart_clutter` lesson directly: a name that appears only
in a test does not count as built.** **`surface_NOT_covered` names its own blind spot.**
**`what_each_status_ACTUALLY_licenses` is a field in the artifact.** ⇒ **this instrument states what
it cannot decide, in the file, where the next reader will find it.**

## 2. 🛑 THE NUMBER THAT MUST NOT SHIP, AND WHY

**`EVIDENCE_ONLY` = the clause's LABEL STRING is absent. Not that the behaviour is missing.**
Counter-examples, in the artifact:

| clause | classified | truth |
|---|---|---|
| `17.25_point_stop` | `EVIDENCE_ONLY` | **built, enforced everywhere, verified all night** |
| `directional_momentum_trigger` | `EVIDENCE_ONLY` | `_momentum` appears **3×** in `derivation.py` alone |
| `PR_38_remains_draft_do_not_merge` | `EVIDENCE_ONLY` | **governance, not code at all** |
| `FVG_not_required_for_displacement_entry` | `EVIDENCE_ONLY` | **a NEGATIVE clause — "X is not required" has no implementing line to find** |

> ## **A LABEL SEARCH CANNOT DECIDE IMPLEMENTATION. SHIPPING `84` AS A COUNT OF UNBUILT CLAUSES WOULD HAVE BEEN THE LARGEST FALSE FINDING THIS CAMPAIGN COULD PRODUCE — AND IT WOULD HAVE BEEN A COMPLETE-LOOKING PARTIAL, THE SHAPE MY OWN ORDER NAMED IN THE SAME MESSAGE.**

**The worker had the headline the operator was waiting for, under a deadline, and did not send it.**

## 3. THE CONTROL CAUGHT THE INSTRUMENT'S OWN BLIND SPOT — on the first run

It returned **`CONTROL_FAILED`**, because `avoid_chart_clutter` **was not in the clause list**: the
input enumerated only his evidence files, and that clause lives in `spec.json`. ⇒ **`SPEC_ONLY` was
structurally unreachable for exactly the class it exists to name, and the register would have reported
a confident zero there.**

> ## **A CONTROL THAT CAN ONLY CONFIRM WHAT THE INPUT ALREADY CONTAINS IS NOT A CONTROL. THE THREE KNOWN CASES WERE NOT A CHECK ON THE ANSWER — THEY WERE A CHECK ON THE POPULATION, AND THAT IS THE ONLY REASON THEY BIT.**

**This is the day's population law arriving inside the instrument built to end the day's population
problems.** ⇒ **ORDERED into the method section: a positive control must include at least one case the
input was not built to contain.**

## 4. WHAT IS ACTUALLY ACTIONABLE — small, named, and honest

**`SPEC_ONLY` is the sound class**: in the machine-readable spec **under its own name**, read by **no**
production module. **That is the `avoid_chart_clutter` shape.** **Evidence-origin: 3 —
`allowed_destination_families`, `regular_level_family`, `resistance_behavior`**, plus
`avoid_chart_clutter` and the ladder already known.

⚠️ **AND THE SAME CAUTION APPLIES TO `SPEC_ONLY`, WHICH THE WORKER DID NOT STATE AND I ADD:**
`allowed_destination_families` is unread **by name** — but ALGO-122 measured all three families being
built in `targets.py`. **`SPEC_ONLY` is where the real ones live; it is not a list of defects.**
**Every row still needs one code read.**

**`BUILT 69` is a FLOOR.** **`EVIDENCE_ONLY 84` is UNDECIDED.** **The remaining work is reading the
code for each concept, and there is no shortcut** — which is exactly what the register was for: **it
converts an unbounded question into ~90 named rows.**

## 5. AND A DEFECT OF THE WORKER'S OWN, REPAIRED THE HARD WAY

`git commit -m` with **backticked identifiers**: bash executed them (`17.25_point_stop: command not
found`) and **four identifiers were deleted from the pushed message** — the counter-examples landed
blank. **Repaired by a follow-up commit through a file. Not amended. Not force-pushed.**

**Correct, and the reasoning is the campaign's:** the branch is shared, and **rewriting pushed history
to tidy an appearance is worse than the defect** — `[never take a real risk to remove an appearance]`.

## 6. QUEUE

1. **Adjudicate the `SPEC_ONLY` evidence-origin rows** — 3 named, one code read each. **Report `BUILT
   BY BEHAVIOUR` or `GENUINELY UNBUILT` by key.** Start there: it is small, bounded, and it is where
   `avoid_chart_clutter` came from.
2. **The `EVIDENCE_ONLY 84` stay UNDECIDED** and are not to be characterised until read.
3. **Not authorized:** any repair · the ladder build · the `$400` floor · anything else.

---

**LESSON, minted:**

> **THE REGISTER'S FIRST TWO FINDINGS WERE BOTH ABOUT ITSELF: A CONTROL THAT COULD NOT REACH ITS OWN CATEGORY, AND A HEADLINE NUMBER THAT MEANT SOMETHING OTHER THAN WHAT IT SAID.**

An instrument built to answer *"what did he teach that is not built?"* produced, on its first run, a
number that would have answered it **wrongly and confidently** — and the only things standing between
that number and the operator were **a control chosen to include cases the input did not contain**, and
**a worker who would not ship a headline under a deadline.** **Neither is a guard. Both are judgement,
and neither can be automated.**

*No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision in
this ruling.*
