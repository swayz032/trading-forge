# ALGO-110 — ALGO-109 executed: the ruled-but-unbuilt band is **GPT's first task** in the handover, traps 16–20 landed, and I **withdrew my own §3 flag** because ALGO-073 had already ruled it. **New finding: there are THREE band constructions, and the third's magnitudes are declared NOWHERE — M1's ten is a FLOOR, not a total.**

**Strategy head:** `28777977` (pushed, local == remote). **Suite:** 896 passed, 0 failed.
**Handover guard:** 12/12. **All 7 new line-number citations verified to resolve.**
**PR #38:** DRAFT / DO NOT MERGE. **Nothing semantic changed. Nothing further landed.**
**Reserved-class asks remain drafted and UNSENT.**

---

## 1. ORDERS, executed

**(1) GPT'S FIRST TASK is in the open queue**, ahead of the named open items, stated as
**RULED-BUT-UNBUILT** rather than as a proposal — with his verbatim words, ALGO-073 §2's ruling,
ALGO-089 §3's 0.59/0.60 pt verification, the refuted 18.97 pt rival, the 5m/15m scope, the three
coded constructions, the executable sites, and ALGO-108 §1's displacement obligation as a
mandatory condition of the measurement. It closes on your line: **fifth time this ladder has found
a ruled clause that never reached the code.**

**(2) Traps 16–20 added.** #16 predicate-proof-does-not-transfer-to-pipeline. #17 **ONE DOOR PER
ROUTE in your scoped form** — *a story refusal is effective only at buckets where no higher-ranked
route also qualifies* — with both measurements (08:00 nullified, 09:30 recovered two sessions) and
an explicit note that **the absolute form is false and would contradict our own 09:30 result**.
#18 report membership **per route**, with *re-labelled, not retracted* and the reason: per-key
conclusions are immune to a coupling that moves totals. #19 the untaught `kernel.py:205`
precedence, **NAMED NOT OPENED**. #20 the declaring-surface limitation below.

**(3) Reserved-class asks: unsent.** No question of any kind went to the operator.

## 2. I WITHDREW MY OWN §3 FLAG — it was answered before I raised it

My M1 §3 flagged *"his quote describes a single rejection candle, ALGO-071's screenshots describe
a rejection cluster; reconciling them is part of the work, and it is not resolved here."*

**ALGO-073 §2 had already ruled it: the SINGLE rejection candle.** I raised it without having read
ALGO-073. **Withdrawn in place**, with the citation and the correction visible rather than silently
deleted — a *"not resolved"* flag standing on a settled rule would send the next seat to re-derive
it, which is the specific failure this ladder keeps paying for. ALGO-073 §1 also fixes the
evidential order I had backwards: **his words STATE the rule; the screenshots VERIFY it.** The
`~4–32 pts` are corroboration, not the source.

## 3. NEW FINDING — a THIRD band construction, and its magnitudes are declared nowhere

Found while pinning the executable site for §1, not by looking for it.

| # | construction | site | untaught magnitudes | declared? |
|---|---|---|---|---|
| 1 | **symmetric** `center ± max(4 ticks, 0.06×ATR)` | `levels.py:149` (exceptional single-swing) | `key_level_pad_atr 0.06`, `4 ticks` | spec + registry |
| 2 | **quantile spread** `[Q20, Q80]` of the rejection prices `± max(TICK, 0.05×med_atr)` | **`engine.py:492-496`** (established multi-rejection) | **`0.20`, `0.80`, `0.05`** (+ compactness `0.30`, `:490`) | **NEITHER the spec NOR `PARAMETER_REGISTRY`** |
| 3 | symmetric pad around PDH/PDL/PWH/PWL | `engine.py:585` | same pad | context only — those families hold no entry authority |

**Consequence for M1, stated in the document rather than carried quietly: TEN IS A FLOOR, NOT A
TOTAL.** My instrument derives the magnitude set from **the declaring surface**, which is the right
authority for *what the spec declares* and is **not** the same as *every magnitude that reaches
admission*. A literal hard-coded in engine code that never reached a declaring surface is invisible
to it.

> **DERIVING FROM THE DECLARING SURFACE IS NOT DERIVING FROM THE CODE.** "The set is DERIVED"
> reads as complete when it is only well-sourced — the same shape as a hand-typed population,
> wearing a derivation's costume. The honest instrument is an **AST sweep of the admission call
> path for bare numeric literals**, and I have specified that as the extension rather than
> claiming the ten.

**This strengthens your item rather than complicating it:** the established path is the one
carrying the most untaught numbers *and* the least declaration, and **both** #1 and #2 are the
wrong *shape* against the ruled `[wick extreme, close]` band — not merely the wrong width.

## 4. ONE PIECE OF ENGINEERING CONTENT, named so it is not discovered late

`levels.py:149` centres its band on `row.price` — **a pivot price**. The ruled band needs the
**rejection candle's own wick extreme and close**, so the build must **join each pivot back to its
source bar on the marked timeframe**. That join is the task; the arithmetic is trivial. Named in
the handover.

## 5. WHAT I DID NOT DO

No semantic code changed — the diff is two markdown files, 130 insertions, 6 deletions, checked by
`--stat` first. No band was built. No admission magnitude moved. `kernel.py:205` untouched. R2c
stays unmerged. Nothing from B1 is on any pushed branch. Re-exam #4 not run. **The engine-crash
defect `V24_TARGET_DISTANCE_LT_REACTION_CONTACT` (`target_policy.py:157-161`) is still carried and
still unfixed.**

## 6. ASKS FOR ALGO-111

1. **Does the established path's band (#2) fall inside GPT's first task, or after it?** It is a
   different construction with different inputs, and doing both at once makes the displacement
   audit unattributable between them. My read: **sequence them, #1 first**, because the
   exceptional-swing path is where M1's declared magnitudes live and where the join work is
   proved out — but the ordering is yours.
2. **Do `0.20 / 0.80 / 0.05 / 0.30` need a provenance pass of their own before anything is built
   on that path?** They were never in the M1 set, so they have never had one.

---

**Enumerated.** 2 files changed, 130 insertions / 6 deletions. 7 line-number citations
programmatically verified against the code they name. Handover guard 12/12; suite 896 passed / 0
failed at `28777977`. Reports read to verify this packet: ALGO-071, ALGO-073, ALGO-089.

No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision in this packet.
