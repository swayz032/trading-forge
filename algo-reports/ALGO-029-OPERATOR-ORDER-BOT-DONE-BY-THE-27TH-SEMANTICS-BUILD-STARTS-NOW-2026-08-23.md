# ALGO-029 — OPERATOR ORDER: the bot must be DONE by the 27th, not parked. DONE is defined as a checklist. The semantics BUILD starts NOW (no longer serialized behind the grade); ACCEPTANCE stays fully gated. Daily exam checkpoints so misfit surfaces early.

**Advisor:** Claude (Fable 5), ALGO seat. **Head at ruling:** algo branch `a1fb7bfe`
(ALGO-028) [MEASURED, fetch]. **PR #38: DRAFT / DO NOT MERGE — unchanged.**

**Operator, 2026-08-23, verbatim:** *"No the bot has to be done already"*, reinforced
mid-ruling: *"You are the best engineer we should have the bot ready before then"* — rejecting
ALGO-028 §3's park-if-unfinished branch as the plan. Registered as an order: **the target is a
FINISHED bot by 2026-08-27, ready BEFORE the deadline.** This ruling makes that executable
without bending a single law.

---

## 1. What DONE means — the checklist that defines the order

By 2026-08-27, all of the following, on the strategy branch:

1. **Strategy brain code-complete:** the ALGO-009 §3/§6 four-route entry-authority state
   machine with the derivation layer (approach / interaction / story / displacement /
   reset-retest computed from price per the frozen teachings — nothing self-attested), the
   8:00–12:00 window amendment folded in (ROLE-1 + deliberate ROLE-3; ROLE-2 anchor untouched
   per ALGO-027/028), full §7 mutation campaign green.
2. **Fidelity exam RUN on the finished brain** with honest published numbers; **if it passes →
   FREEZE executed** (hash-pinned, on the record).
3. **Safety core PROVEN offline:** the FakeSession flatten/cancel/position tests green
   (already in flight per ALGO-027).
4. **Deployment path finished to the offline line:** ProjectX adapter verified at
   request-shaping; the credentialed shakedown + connection procedure written as RUN-ONLY
   steps (executed post-sunset by operator + GPT, after FREEZE and clean-edge — never now).
5. **Validation arsenal runnable by single command:** assess the in-family prior art
   (`…v2_3_oos.py`, `…v2_2_risk_metrics.py`, robustness harnesses) and deliver run-only
   invocations + plain-English readouts, so the post-FREEZE clean-edge/robustness ladder is
   executable by the operator with GPT reading the outputs. No new arsenal is authored;
   reuse and document.
6. **The self-sufficiency pack complete** (ALGO-026 P1 as amended by ALGO-028's run-only law).

## 2. The one re-order that makes this feasible — and what it does NOT relax

**The semantics BUILD is authorized to start IMMEDIATELY, in parallel with the outstanding
grade.** The build is TEXTBOOK-driven (the registered teachings define what a real rejection,
break, displacement, reset-retest ARE) — it does not consume exam numbers, so waiting for the
grade before building was serialization of independent work. The deadline eliminates that idle.

**NOT relaxed — acceptance stays exactly as ruled:** no semantic candidate is SELECTED or
ACCEPTED against the 14 cases until the re-dispatched grade has passed the repaired evaluator;
H1–H8 selection remains fidelity-only across all 14; every §7 mutation red-proof, positive
witness, byte-exact restore, and no-PnL law stands. If the grade comes back REFUTED, the
instrument is repaired first and every exam-derived judgment made in the interim is re-run —
say so in the report if that happens.

**Daily exam checkpoints:** run the 14-case exam at each derivation-layer milestone (clearly
labeled DIAGNOSTIC until the grade passes) so misfit with the trader surfaces on day one, not
on the 26th. A checkpoint result may never steer a rule by itself — the teachings steer; the
checkpoint only tells us early whether the build is drifting from him.

## 3. The one thing effort cannot force — stated once, plainly

Items 1 and 3–6 are effort: they will be done. **Item 2's PASS is an empirical outcome** — the
finished brain must actually decide like the trader. The checkpoints exist to make failure
early and fixable inside the window. If, despite that, the exam still refuses on the 26th, the
remaining work is code-shaped and ALGO-028 §3.2 governs (park code-complete, run-only,
resumable) — that is the fallback of record, not the plan. The plan is DONE.

**Execution notes:** worker sequences P1-pack items and the brain build as it judges best —
both must land; the grade takes precedence the moment it renders. Advisor turnaround on any
blocking question: publish and continue on non-blocked work; do not idle on a round-trip.
FIDELITY → FREEZE → CLEAN EDGE before any live connection — unchanged and not in tension with
this order: DONE means finished and frozen, with connection a run-only post-sunset step.

No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision
in this ruling.
