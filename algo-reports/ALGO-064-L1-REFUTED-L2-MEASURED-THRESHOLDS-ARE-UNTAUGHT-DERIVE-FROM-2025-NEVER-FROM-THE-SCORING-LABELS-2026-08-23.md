# ALGO-064 — L1 refuted at the row (accepted). L2 is measured: the map's level-quality thresholds are ENGINE CONSTRUCTIONS, not teaching — the spec confirms only the SCOPE. They may be re-derived from the 2025 teaching era, never from the 2026 scoring labels. One more measurement first: pivot confirmation lag.

**Advisor:** Claude (Fable 5), ALGO seat. **Rules on:** the committed diagnosis at strategy
head `baa58101` — `…refusal_diagnosis_lost_four_2026_08_23.json` + `…location_map_resolution_2026_08_23.json`
+ the two runnable probes. **Channel head at drafting:** `8c29c4d1` (ALGO-063, mine).
**PR #38: DRAFT / DO NOT MERGE — unchanged.** **DECISION: ACCEPT refutation (§1) + RULE
contamination law (§2) + ORDER the derivation and one measurement (§3) + HOLD on the repair (§5).**

## 1. L1 refuted — the control did its job [rows read by this desk at `baa58101`]

Recomputing the SAME map rule causally as of his `marked_time` places no zone on his level
either. **03-24 row [MEASURED HERE]:** map at 09:30 and at 09:32 both 53 built / 53
authorized / 0 covering; nearest band 24219.78–24235.97, gap 27.53; nearest same-side pivot
24193.5 at 1.38 pts, wick 0.038 < `min_wick` 0.20 (operative), disp 1.798 < 2.189 (Q75 of
340 reference pivots) → `L2_MAP_RULE_EXCLUDES`. **04-14 control row [MEASURED HERE]:** his
level 25620.50–25620.75 is INSIDE band `SWING:S:2026-04-14T07:45` (25618.55–25620.95,
source `STRONG_SWING_DISPLACEMENT`, authorized) — **a pre-market 07:45 swing that happened
to sit under his 09:30 rejection level.** That is the "accident" shape. **04-06 row
[MEASURED HERE]:** n=1 — at his 10:04 clock a single Route A candidate at the OLD band
`S:2026-03-25T06:30` (24407.93–24435.07, covering his 24421.5) was refused
`TOUCH_WITHOUT_DIRECTIONAL_CONTROL` (`WAIT_STORY_INCOMPLETE`); acceptance not operative;
at-clock and census disagree. A lead, as labelled — not a finding.
My ALGO-063 §2 hypothesis is dead at the row, which is the only place a hypothesis should die.
L2 MAP_RULE_EXCLUDES it is — with a rule and a parameter, as ordered:
03-24: nearest same-side pivot 1.38 pts away, wick 0.038 < `min_wick` 0.20 → excluded
(also disp 1.798 < 2.189). 03-30: 1.88 pts away, wick 0.589 OK, disp 1.474 < 2.079 →
excluded; the established path unavailable (the pivot is alone in its cluster; needs 2).
04-14: 0.88 pts away, wick 0.223 ≥ 0.20, disp 2.100 ≥ 2.094 → ENTERS — **by 0.023 wick and
0.006 ATR.** In every case a pivot sits 1–2 points from the level he marked; the quality
bar decides it is not a level.

The worker's own ladder defect (L3 branch named the opposite of its condition, firing only
on the control row) — split into L3a and re-run: accepted; the control row existing to be
checked is why it was caught.

## 2. What the thresholds ARE — measured, and it decides the repair's shape [MEASURED HERE]

- `research/current_mnq_strategy_v2_4_key_level_semantics.json` (`MNQ-V2.4-SR-LOCATION-EQUATION-5`,
  frozen 2026-08-20): `purpose = "Encode the trader-confirmed location scope: structural
  support/resistance is the only regular key-level family"`. **The trader confirmed the
  SCOPE — which family of level counts.** The established path's `authority = existing
  causal multi-rejection quality engine`, `minimum_independent_rejections = 2`; the
  exceptional path's `minimum_wick_fraction_uses_frozen_strategy_parameter = min_wick`,
  `absolute_displacement_floor_atr = 1.0`, `recent_displacement_percentile = 0.75`, and
  `anti_overfit/no_threshold_search = True` + `changing_this_contract_invalidates_prior_v2_4_evidence = True`.
- `min_wick` is a v2.2 ENGINE Params default — `current_mnq_strategy_v2_2_engine.py:65`
  `min_wick: float = 0.20`, with a declared search range at `:91` `(0.16, 0.26, "minimum
  rejection wick fraction")`. A parameter born with a tuning range is a construction, not a
  quotation.
- The plan of record (ALGO-009) defines authority as `AUTHORIZED SR/FVG → REAL INTERACTION →
  GENUINE REJECTION/CONTROL STORY …` and "a touch alone is never rejection authority" — the
  rejection semantics there are the ENTRY story at a level, not a pivot-wick bar for level
  CREATION. No teaching citation for `min_wick`, the 1.0-ATR floor, Q75, or "2 independent
  rejections" was found in the spec, the plan of record, or the levels module.

**Ruling: the level-quality thresholds are UNTAUGHT parameters.** The trader taught WHICH
levels (structural S/R), not a wick fraction. ALGO-058/063 §4(b) applies: they resolve from
his DEMONSTRATED behaviour — and the anti-overfit clauses in the spec are honoured exactly
by doing that from teaching evidence, not by searching thresholds against a score.

## 3. THE CONTAMINATION LAW, and two measurements ordered before any repair

**Law (minted):** the 2026 labels are the SCORING era (ALGO-020 two-era fork). Any parameter
resolved from them is fitted to its own exam. **Thresholds may be derived ONLY from the 2025
TEACHING era and the pinned teaching artifacts; the 2026 labels may VERIFY the result
afterward, never set it.** Held 2025 evidence [MEASURED HERE]: the ledger carries 74 MNQ
trades over 55 days (2025-04-02..06-20) with ENTRY PRICES (outcome fields barred, unread);
the six pinned screenshots (ALGO-050/051/052) show his DRAWN zones at real bars; the
2025-04-11 video-to-ledger join is verified. No marked-zone file exists for 2025 — the entry
prices and the drawn zones are the demonstrations.

**The worker's honest objection is accepted and reshapes M1:** no 2025 marked-zone artifact
exists in the repo (the worker searched; I searched), six screenshots are not a distribution,
and no percentile may be derived from six. The worker's own source-to-spec grep converges
with §2 (zero occurrences of citation/provenance/teacher/transcript/taught/source_quote/
derived_from; the established path's "authority" is prior code — self-referential). So the
repair is not "re-fit the numbers on better data"; **the machine's definition of a level was
inherited from a prior engine and never derived from him.** M1 is therefore a DERIVATION,
with a verification set that is held, not requested:

**M1 — derive the level DEFINITION from the teaching; verify on held 2025 evidence.**
(a) From the ALGO-009 source words and the six pinned screenshots, write the level rule
    the teacher actually gives — what makes a price a key level for him (structural S/R;
    a prior visible reaction/rejection; the timeframe it is read on; whether a single
    visible rejection suffices) — **one clause per teaching citation, no clause without one.**
    Where the teaching implies a magnitude ("visible", "clean", "strong"), express it as the
    candle-geometry the teaching shows (e.g. traded into and closed away), not as a wick
    fraction nobody said. Result = a candidate spec release 6 with provenance lines.
(b) Quantitative verification WITHOUT touching 2026: the held 2025 replay video (3h53m,
    2025-04-11 browsed to 04-30, video-to-ledger join already verified) shows his drawn
    zones at real bars across many sessions. Extract his drawn zones from held frames (held
    evidence — nothing is requested of the operator), join them to the ledger sessions, and
    measure the candidate rule's RECALL (his zones the rule admits, by membership) and
    PRECISION (levels the rule admits that he did not draw, counted) on those sessions. The
    74 ledger entry prices are a secondary sanity join only (entry within a bounded distance
    of an admitted level), never a level proxy. **One pre-registered rule, run once: if recall
    fails, go back to the teaching — never to a threshold.**
(c) Only after (b): the 2026 labels VERIFY (membership of the five marked levels; the
    ALGO-063 §4 guard; the flip-margin report of §4). If the video frames prove
    unextractable, say so, and (a) proceeds with (c) as the only verification — declared in
    the packet as the WEAKER evidence path, not dressed as (b).
The spec's own clause says changing it invalidates prior v2.4 evidence — accepted; the exam
re-runs under the same rules regardless. No outcome field is read; nothing is chosen by
agreement rate.

**M2 — confirmation lag (H-CONFIRM, a hypothesis, labelled).** On 03-24 his level was
marked at 09:32 = his entry minute, `source_method VISIBLE_REJECTION`: he marked the
rejection he had just watched. The spec requires `reference_pivots_must_confirm_before_candidate`
and confirmed-before-freeze. A pivot confirmed by later bars cannot exist at the minute the
rejection is happening. So for each of the five sessions the table must carry: the machine's
nearest pivot's CONFIRMATION time vs his `marked_time`, and the geometry of the candle(s) at
his marked_time on his marked timeframe. If his level IS the in-progress rejection candle,
the gap is SEMANTIC (level admission from a live rejection with the taught story) and no
threshold fixes it — that is a different repair than M1 and must not be conflated with it.

## 4. Minted finding — the lone agreement is fragile

04-14 clears by 0.023 wick / 0.006 ATR. Both directions follow: the four losses are not a
wildly mis-set bar, and a small move flips several sessions at once. Therefore any location
repair must (a) show 04-14 still agrees BY MEMBERSHIP, (b) satisfy the ALGO-063 §4 guard —
08:00-arm forbidden-in-window entries (24 → 6) may not rise, no new pre-window grant, by
membership on the same corpus — and (c) report the margin of every session that flips, so
a repair that lands on another 0.006 edge is visible as what it is.

## 5. HOLD — and the second class

No threshold moves, no code, until M1 + M2 render in the committed artifact. The
STORY_NOT_RECOGNIZED pair (03-31 MERE_APPROACH_WITHOUT_TOUCH; 04-06
TOUCH_WITHOUT_DIRECTIONAL_CONTROL, n=1) is a separate lane at the approach/interaction
layer; it is ruled only after this desk reads the 04-06 row against raw records. ALGO-065
rules the repairs.

## 6. Queue

Commit table + L-resolution → M1 (2025 envelope) + M2 (confirmation lag) in one artifact →
advisor row check (04-06 raw; one L2 row) → ALGO-065 rules both repairs with the §4 guard →
repair(s) → re-exam under the SAME rules → grade (operator-authorized) → FREEZE or another
round.

LESSON: a frozen spec can be trader-confirmed in SCOPE and engine-invented in NUMBERS — read
the purpose line and the Params default side by side before calling a threshold "taught".

No PnL, realized outcome, winner/loser label or clean-edge result participated in any
decision in this ruling.
