# ALGO-152 — **0 OF 5. NONE OF THE EXCEPTIONAL GATE'S MAGNITUDES CARRIES A CITATION FROM HIM — AND THE STRUCTURAL FINDING UNDER IT IS BIGGER THAN THE PROVENANCE ONE.** **[VERIFIED HERE] `_reference_threshold` returns `max(floor_atr, quantile(prior_disp, percentile))` — and the worker measured the percentile winning `1958 of 1958`. THE ABSOLUTE TERM NEVER BINDS.** **[VERIFIED HERE] `ATR` · `average true range` · `true range` return ZERO across all four of his evidence files** — positive controls live (`momentum` 37 · `rejection` 30 · `displacement` 21 · `key level` 11 · `wick` 8). **He has never once mentioned the unit the only absolute term is denominated in.** ⇒ **HIS TEACHING SAYS *"a MAJOR swing high/low followed by DECISIVE displacement."* THE GATE ENFORCES *"top quarter of recent same-side pivots."* THOSE ARE DIFFERENT KINDS OF PREDICATE, AND THE DIFFERENCE IS NOT A THRESHOLD VALUE — IT IS RELATIVE VERSUS ABSOLUTE.** **A percentile gate admits a fixed fraction of whatever happened lately: in a quiet regime, the top quarter of quiet. It cannot enforce "decisive", because it has no absolute referent — and the one term that could give it one never fires.**

**Advisor:** Claude (Opus 5), ALGO seat — `trading-forge-47`. **Channel head at drafting:** `88aa4d9d`.
**PR #38: DRAFT. No threshold proposed, tuned, or checked against the sessions.**

---

## 1. THE PROVENANCE LINE

| magnitude | value | cited by him? |
|---|---|---|
| `min_wick` | `0.20` | **no** (0 hits, 4 names; ALGO-064's measurement cited, not re-derived) |
| `absolute_displacement_floor_atr` | `1.0` | **no** — **and its UNIT appears nowhere in his corpus** |
| `recent_displacement_percentile` | `0.75` | **no** (0 under `percentile` / `top quarter` / `strongest` / `compared to recent`) |
| `minimum_reference_pivots_for_percentile` | `4` | **no** (0, 4 names) |
| `lookback_calendar_days` | `40` | **the CONCEPT is his** — *"Recent levels are emphasized"* `[video_evidence, source 6]` — **the `40` is not** |

**Instrument sound, and the worker caught its own control:** its first positive control was
`"rejection wick"` — **a phrase, not a term certain to exist** — which returned zero and made the run
uninterpretable. **It re-ran with terms certain to be present.** > **A control that is not certain to
be there is not a control.**

## 2. 🛑 THE TAUTOLOGY IT REFUSED — and it was my hypothesis

I asked for the admitted pivots' rank within their own reference, expecting *"if they sit mid-pack,
`decisive` is not being enforced."* **Result: 1,958 admitted, min rank `0.751`, median `0.896`, and
`0.0%` at or below Q75.**

**The worker refused to report that as a finding, and it is right: the gate admits `disp >= Q75(prior)`,
so every admission ranks ≥ 0.75 BY CONSTRUCTION.** **My hypothesis is technically refuted and the
refutation carries no information** — the fourth entailed number of this campaign, after
`7 of 7`, `SAME_ZONE 69` and `identical clocks`. **I keep designing measurements whose answer is fixed
by the thing I am measuring.**

## 3. THE TWO REAL FINDINGS

**(a) THE ABSOLUTE FLOOR IS INERT.** `PERCENTILE_Q75` admitted **1958 of 1958**;
`absolute_displacement_floor_atr = 1.0` **bound zero times.** **[VERIFIED HERE at the line]** the
threshold is `max(floor_atr, quantile(...))` — **a `max()` in which one argument has never won.** **An
uncited magnitude that does nothing at all.**

**(b) AND THE ONE THAT ANSWERS THE WEEK — from the gate's STRUCTURE, not from the data:**

> ## **A PERCENTILE GATE ADMITS A FIXED FRACTION OF WHATEVER HAPPENED LATELY. IT CANNOT ENFORCE AN ABSOLUTE QUALITY LIKE *"DECISIVE"*, BECAUSE IT HAS NO ABSOLUTE REFERENT.**

**This is why the map holds ~41 exceptional zones a session** (ALGO-151): **the count is proportional
to how many pivots exist, not to whether any of them were major.** ⇒ **YOU CANNOT TUNE OUT OF IT.**
`Q75`, `Q90`, `Q99` — every one still admits a fixed fraction. **The map's size is set by the
market's pivot count, not by his standard.**

**And the unit is the tell.** The only term that could make the gate absolute is denominated in **ATR
— a unit that appears ZERO times in everything he has ever given us.** **His vocabulary is the
candle**: wick, displacement, momentum, close. ⇒ **the absolute half of this gate was written in a
language he does not speak, and then never fired.**

**Same shape as ALGO-132's *"`max()` is the wrong operation, not the wrong order"* — which held.**

## 4. 🛑 WHAT IS NOT SAID, AND WILL NOT BE

- **This is a PROVENANCE result. It does NOT say the gate is wrong.** An uncited magnitude can still
  be right — **ALGO-107 proved that retiring one made things worse.**
- **No threshold is proposed, tuned, or checked against the sessions. No percentile, no floor, no
  direction of change.** *"What should it be"* has no citable answer and asking it of the data is the
  fit every rail forbids.
- **`no citation found in the surfaces named`, 5 of 5, residual empty.**

## 5. QUEUE

1. **The side-by-side — HIS session next to the BOT's, 14 rows — is running and it outranks this.**
   §3(b) says the map is sized by a relative gate; **the overlay says whether his levels are even in
   it.** Ship partial rows as they exist.
2. **Derive what *"major"* and *"decisive"* are IN HIS VOCABULARY** — candle-relative, since ATR is
   not his word — **from held evidence only. Derivation to this desk before any code.** If it cannot
   be expressed without a number, **STOP and say so.**
3. **Not authorized:** any threshold · the floor · the percentile · any repair.

---

**LESSON, minted:**

> **THE GATE'S ABSOLUTE TERM IS UNCITED, INERT, AND MEASURED IN A UNIT HE HAS NEVER USED. ALL THREE FACTS POINT AT THE SAME THING: NOBODY WHO WROTE THAT LINE WAS READING HIM.**

**And the deeper one, which is why the count could never be fixed by tuning:** *"decisive"* is an
**absolute** judgement about one candle; `Q75(prior)` is a **relative** judgement about a population.
**Swapping the number changes the fraction admitted. It cannot change the KIND of thing being
admitted** — and every repair this campaign has attempted on the map has been a value change to a
predicate of the wrong kind.

*No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision in
this ruling.*
