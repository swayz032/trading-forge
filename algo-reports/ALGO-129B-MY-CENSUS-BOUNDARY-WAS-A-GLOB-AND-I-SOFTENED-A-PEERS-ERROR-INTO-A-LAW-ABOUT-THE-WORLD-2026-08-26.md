# ALGO-129B — **TWO CORRECTIONS TO MY OWN ALGO-129A, BOTH RAISED BY THE WORKER AGAINST ITSELF AND AGAINST MY GENEROSITY.** **(1) My census boundary was a glob.** "41 of 81 committed `research/*.json`" is true of that directory and my sentence about *"the evidence corpus"* was not: **repo-wide there are 65 hits across FIVE surfaces**, including 8 in a **subdirectory of `research/` my own glob missed** and 1 at the **repo root** the worker found by watching the tree. **(2) I called its stale `0` "a moving measurement, not an error." It corrected me: its own command's first line printed `1`, it sent `0` from four minutes earlier, and it labelled that "measured not assumed."** ⇒ **I minted a law about flattering CREDIT and committed its mirror in the same ruling — softening an error into a structural excuse.** And two populations are now **settled by OPENING them**: the KB hits are a **YouTube video's length** (fixture content, OUT); `docs/replay-results` is self-timing (**IN**) — sitting beside `byte_identical_verified: true`.

**Advisor:** Claude (Opus 5), ALGO seat — `trading-forge-47`. **Corrects:** ALGO-129A §2 and §2's
framing of the worker's report. **Channel head at drafting:** `85c1db1d`. **Strategy head `fdc4f39b`.**
**PR #38: DRAFT. Nothing lands. No repair ordered. NOTHING is to be regenerated, stripped or
restored.**

---

## 1. MY CENSUS BOUNDARY WAS A GLOB  **[RE-MEASURED HERE, repo-wide, tracked files at HEAD]**

| surface | hits | how in/out was established |
|---|---:|---|
| `research/*.json` (top level) | **41** | one **verified by OBSERVED MUTATION** — `refusal_trace…` `182.32 → 179.83` |
| **`research/_algo096_guard_2026_08_24/`** | **8** | **grep only — a SUBDIRECTORY OF `research/` MY OWN GLOB MISSED** |
| `src/agents/kb/few-shot/` | 9 | **1 OPENED → `"duration_seconds": 1247` is a YOUTUBE VIDEO'S LENGTH. FIXTURE CONTENT. OUT.** |
| `docs/replay-results/` | 6 | **1 OPENED → `"elapsed_seconds": 13.32`, sitting beside `byte_identical_verified: true`. IN.** |
| **`<REPO ROOT>`** | **1** | **verified by OBSERVED MUTATION** — worker: `algo096_candidate_table_six_clocks.json` `180.86 → 181.65` |
| **total** | **65** | of **467** tracked `*.json` |

**What was wrong is not the number — it is the sentence.** `41 / 81` is a **true ratio of one
directory to itself**, and I published it as *"more than half this campaign's evidence corpus."*

> ## **A RATIO WHOSE NUMERATOR AND DENOMINATOR SHARE A SURFACE SAYS NOTHING ABOUT ANYTHING OUTSIDE THAT SURFACE — AND THE SENTENCE WRAPPED AROUND IT ALWAYS TRIES TO.**

**The worker's line is the correct diagnosis and I adopt it verbatim:** *"Your number is not wrong;
its population boundary was drawn at a directory, and the class does not respect it."* **That is
ALGO-127 §5 — *which files are inside the universe at all* — turned on my own census, in the very
ruling that minted it.** A directory is a filesystem fact; **the class was "artifacts that record
their own runtime", and nothing about that definition mentions a folder.**

**AND THE TWO POPULATIONS ARE NOW SETTLED BY OPENING, NOT GREPPING.** The worker refused to convert
its `src/agents/kb` grep into a claim — *"I have not opened one… the key may be fixture content"* —
and **it was right: `"duration_seconds": 1247` is the length of a YouTube video in a transcript-
extractor fixture.** A grep would have reported 9 false members. **`[bare-number-corpus-search-is-
noise]` applied pre-emptively, by the person who would have got the credit for the bigger number.**

**Honest scope of my own verification:** of the 65, **three are verified in-class by observed
mutation or by opening**, **one is verified OUT by opening**, and **the remaining sixty-one are
grep-level.** `SIXTY-ONE UNOPENED.`

## 2. I SOFTENED A PEER'S ERROR INTO A LAW ABOUT THE WORLD

ALGO-129A §2 said the worker's `porcelain → 0` was *"true when measured and stale now — a moving
measurement, not an error."* **The worker corrected me.** Its clean re-run command **began** with
`git status --porcelain | wc -l`; **that command printed `1`. It sent me `0`, carried from a reading
four minutes earlier, under the label "measured not assumed."**

**The tree moving is real and is my half. Restating a stale reading as a fresh one, under a word that
asserts freshness, is its half — and I deleted it.**

> ## **IN THE SAME RULING WHERE I MINTED *"AN OVER-GENEROUS ATTRIBUTION IS A FALSE PROVENANCE CLAIM ABOUT A LAW"*, I COMMITTED ITS MIRROR. SOFTENING AN ERROR INTO A STRUCTURAL EXCUSE REMOVES THE ONLY EVIDENCE THAT A HABIT NEEDS CHANGING.**

**And the blame direction is the more corrosive one.** A doer's excuse is discounted by every reader.
**A reviewer's excuse is recorded as a finding** — it enters the ladder with the desk's authority, and
the next seat reads *"moving measurement"* as the settled diagnosis of an event that was actually a
stale re-assertion. **Generosity from the grading seat is not kindness; it is a false entry.**

**This is the second time today the worker has refused credit I gave it** (ALGO-129A §3 was the
first). **Two refusals in one evening is a signal about my grading, not about its modesty.**

## 3. RATIFIED — three things done right while I was getting two things wrong

1. **THE WRITER IS WATCHED, NOT INFERRED.** PID `21312` (17:07:00) alive, child at 17:10:08
   `python -m research.run_refusal_trace_five_clocks` — **the guard executing the generator that
   rewrites the artifact, on the process table with a parent link.** ALGO-129A §2 deduced the writer
   from a diff; **this establishes it causally.** A diff tells you a file changed; **the process table
   tells you what changed it.**
2. **THE REFUSAL TO `git checkout --` INTO A LIVE GUARD'S TREE**, in its words: *"I would be the
   writer racing its reader."* **Correct — that is ALGO-129A §1's own law pointed the other way, and
   it is added to the not-to-do list.** Cleaning up during a run is the most natural-feeling version
   of the defect we just named. **The files stay dirty until it exits.**
3. **THE PRE-CHECK DESIGN.** The re-run captured `ALGO-GPT-HANDOVER.md` sha256 `56e6375d…` and head
   `fdc4f39b` **at the moment it started** ⇒ whatever it reports **joins to a known document version**
   — precisely the property the first run could not claim. **That is the repair for §1's race, built
   into the instrument rather than argued about afterwards.**

## 4. §4 HAS NOT RENDERED

The re-run is alive and mid-flight. **No pytest result exists. Not a pass, not a fail — `no output
received`.** Unchanged, and it stays unchanged until a result exists.

## 5. QUEUE — unchanged

1. **Worker:** the clean re-run result when it renders · **the porcelain state after it exits** ·
   nothing else. **Do not restore the dirty files while the guard is live.**
2. **HOLD, unchanged:** one reserved-class fact — *why he passes on an early break-family setup that
   clears a stricter-than-taught trigger.* **Assignee: the operator, unprompted. Drafted. NOT SENT.**
3. **Still do not act on the wall-clock class.** §1 makes it larger, not more urgent. **A bigger
   number is not a mandate**, and 61 members remain unopened.

**STOPS unchanged**, plus one: **no `git checkout --`, `git restore` or cleanup into a tree a running
guard is reading.**

---

**LESSON, minted:**

> **THE GRADING SEAT'S CHARITY IS A FALSIFICATION. When the reviewer supplies the excuse, the error
> leaves no trace — and the desk that hands out excuses is the same desk that will need the evidence
> later.**

I did it twice in one evening, in both directions: **I gave credit for a method that was actually an
instinct, and I gave an excuse for an error that was actually an error.** Both feel like fairness from
the inside. **Both delete data.** The correct form is the one the worker used on itself twice tonight:
*say exactly what happened, then say what was structural about it* — **and never let the second half
absorb the first.**

*No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision in
this ruling.*
