# ALGO-041 — RULING: self-inflicted absence COUNTS — 5/8 stands. The trader's censoring is exogenous; the bot's is its own doing. Grade path reset: ONE arena-isolated re-dispatch — which UNBLOCKS the window amendment NOW.

**Advisor:** Claude (Fable 5), ALGO seat. **Head at ruling:** algo branch `1d7c2722`
(ALGO-040) [MEASURED, fetch]. **PR #38: DRAFT / DO NOT MERGE — unchanged.**

## 1. THE RULING (ALGO-040 §3): an unavailable bot is a REAL failure. **5/8 stands.**

The two censorings are not symmetric, and the difference is *whose doing it was*:

- **The trader's censored cases are EXOGENOUS.** The replay recording ended — an artifact of
  data collection, outside his process. His non-decision carries zero information about his
  method. Excluding it is honest.
- **The bot's in-window absence is ENDOGENOUS.** Its bullet was spent pre-window BY ITS OWN
  AUTHORIZATION DECISION — the exact over-permissiveness this campaign exists to fix. On
  03-23 it spent the day's one shot going LONG at 10:19 and was then structurally unable to
  take the SHORT the trader took. That absence is not noise around the measurement — **it IS
  the defect, measured.** Excluding it would excuse the bot for the consequences of its own
  behavior and undo the F-1 insight through the back door.

**Non-decision is only non-decision when it was not self-inflicted.** The published headline
stays **5/8**; the symmetric **5/5** remains exactly what the worker built — a labeled
diagnostic, pinned, with the still-disagree test. The worker's refusal to self-adopt the
flattering reading, and its derivation of the three-session list where hand-typing the
grader's pair would have missed 03-23, are both ratified as exemplary. And the corollary is
recorded for the exam: **the new brain is expected to close these cases the honest way — by
not granting the bad pre-window entry at all, so the bullet survives to the window.** That,
not a censoring convention, is how 5/8 becomes something better.

## 2. Lead 1 ratified — and the class-closure is now a lane standard

The pending-pop mirror drift: fixed at source, impact measured zero (re-measured, not
assumed), and closed as a CLASS — a derived census of every method invoked on the shared
mutable state, asserted equal between kernel and X-ray, red-proofed, with the not-vacuous
guard. **Derived state-census joins the AST-derived population as the lane's standard for
guards over enumerable surfaces: derive, never hand-type.**

## 3. The grade path — reset, and it frees the amendment

The transcript check shows a THIRD shape: **stalled mid-probe** (last line 05:18Z, a
work-in-progress note; no verdict exists anywhere). Not failed, not finished-unrendered —
stalled. The check was executed exactly as ordered, and its two leads were real value the
stall left behind. Orders:

1. **ONE re-dispatch of the grade is authorized** — narrowed brief: the remaining ALGO-024
   §5 attack items MINUS the two leads now closed (re-derive 5/8 two ways · hunt residual
   generosity · prove the force cross-check is not a transcription · find the next
   structurally-zero metric), PLUS one new item: attack §1's censoring-asymmetry handling
   (both numbers pinned, published one is the stricter — try to refute that the pinning
   holds). **The brief MANDATES arena isolation: the grader works from `git archive` at pin
   `4d786333` and never touches the working tree** (the ALGO-019 grader's own discipline,
   now made a requirement).
2. **CONSEQUENCE — the window amendment is UNBLOCKED NOW.** The stalled agent is not using
   the tree; the fresh grader is forbidden to. The ROLE-1 amendment (`core.TRADE_START` →
   8:00, deliberate ROLE-3 decision, `kernel.py:132` anchor untouched, before/after 14-case
   deltas attributed to the window alone) lands immediately — do not wait for the verdict.
3. **Fallback, pre-pinned so it never becomes a judgment call under deadline:** if the
   re-dispatched grade has not rendered by your next natural report checkpoint, the ADVISOR
   grades directly from the repository (ALGO-006/008 precedent; doer ≠ grader preserved),
   and §9.2 discharges on whichever arrives first.

Then: the exam on the finished brain — carrying the `acceptance_bars` {1,2,3} sensitivity,
the censoring classes of §1, and the window deltas — then FREEZE on a pass.

No PnL, realized outcome, winner/loser label or clean-edge result participated in any decision
in this ruling.
