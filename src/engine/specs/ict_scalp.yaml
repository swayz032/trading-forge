concept: ict_scalp
description: "Sweep→MSS→Displacement→FVG retrace scalp during institutional killzones"

required_imports:
  - detect_fvg
  - detect_displacement
  - detect_swings
  - detect_mss
  - detect_sweep
required_imports_any:
  - is_asia_killzone
  - is_london_killzone
  - is_nyam_killzone
  - detect_buyside_liquidity
  - detect_sellside_liquidity

forbidden_imports:
  - compute_rsi         # not part of ICT Scalp concept
  - compute_macd        # not part of ICT Scalp concept
  - compute_bbands      # not part of ICT Scalp concept

required_time_windows:
  - {start: "20:00", end: "00:00", tz: "US/Eastern", label: "Asia Killzone"}
  - {start: "02:00", end: "05:00", tz: "US/Eastern", label: "London Killzone"}
  - {start: "08:00", end: "11:00", tz: "US/Eastern", label: "NY AM Killzone"}

required_concepts:
  - liquidity_sweep       # must detect BSL/SSL sweep as trigger
  - market_structure_shift # MSS must confirm directional shift after sweep
  - displacement           # strong candle confirming MSS
  - fvg_entry              # enter on retrace into FVG formed by displacement
  - killzone_filter        # entries only inside institutional killzones

runtime_assertions:
  - all_entries_in_killzones  # 0% tolerance — every entry must be in Asia, London, or NY AM
  - signal_count_nonzero
  - signal_density_reasonable  # not every bar
