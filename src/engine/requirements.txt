# Trading Forge - Python Engine Dependencies
# Core - required for all operations
numpy>=1.26.0
pandas>=2.2.0
polars>=1.0.0             # Primary data layer — 5-10x faster than Pandas for Parquet
pyarrow>=15.0.0           # Parquet support
duckdb>=1.0.0             # Query Parquet on S3 directly with SQL, zero-copy
scipy>=1.12.0             # Statistical functions, EVT, QMC, BCa intervals
vectorbt>=1.0.0,<2.0      # Backtesting framework (Pandas boundary only). v1.0.0 parity-proven
                          # vs 0.28.5 (byte-identical trades table, 2026-07-02 probe) and live in
                          # prod since 2026-05-18. <2.0 cap: next major must re-run the parity probe.
pydantic>=2.6.0           # Config validation
click>=8.1.0              # CLI
optuna>=3.6.0             # Bayesian param search for robustness testing (TPE sampler)
requests>=2.31.0
boto3>=1.34.0             # AWS S3
databento>=0.40.0         # Historical market data

# Statistical / ML - required for backtesting pipeline
numba>=0.59.0             # JIT compilation for MC block bootstrap
arch>=7.0.0               # StationaryBootstrap, optimal_block_length, GARCH
hmmlearn>=0.3.2           # Hidden Markov Model regime detection
ruptures>=1.1.9           # PELT change-point detection
scikit-learn>=1.4.0       # LedoitWolf, MinCovDet, GraphicalLassoCV, KMeans

# Analytics
quantstats>=0.0.62        # QuantStats performance analytics
psutil>=5.9.0             # Hardware profiling

# Determinism enforcement (A1 — W9)
threadpoolctl>=3.0.0      # Runtime BLAS/OpenMP thread-count limiter (BSD-3-Clause, free)

# C11 Macro Regime Overlay (W18) — free government APIs
# fredapi: MIT-licensed Python wrapper for FRED API (free tier: 120 req/min).
# NOTE: The existing src/data/macro/fred_client.py uses stdlib urllib (no fredapi dep).
# fredapi is included as an alternative / convenience wrapper if needed in future.
# Current C11 implementation uses fred_client.py directly (zero new deps).
fredapi>=0.5.2

# Property-based testing (A6 — W11) — TEST-ONLY dependency.
# MIT-licensed, free. Hypothesis fuzzes inputs to catch edge cases human tests miss.
# Uses fixed seed by default (settings.deriving_via = ...) for CI reproducibility.
hypothesis>=6.100

# Cross-engine parity testing (A3 — W10) — TEST-ONLY dependency.
# NEVER import backtrader from any production path (backtester.py, paper engine, etc.).
# GPL-3 license; personal/non-commercial use only (matches Trading Forge usage).
backtrader==1.9.78.123

# Snapshot regression testing (A9 — W11) — TEST-ONLY dependencies.
# pytest-snapshot: stores/diffs JSON metric snapshots; MIT license; free.
# ruff: fast Python linter used in pre-commit hook; MIT license; free.
pytest-snapshot>=0.9.0
ruff>=0.4.0

# Quantum (optional — graceful fallback to classical when unavailable)
# INSTITUTIONAL NOTE (2026-07-06): these were DECLARED but NOT installed on the tower for a long time, so
# "quantum" silently ran classical fallback (qiskit-aer/pennylane/dwave-neal all absent). Verify the real
# runtime state any time with `npm run check:quantum-backends` (src/engine/quantum_backend_status.py) — the
# declaration below is necessary but NOT sufficient; the tower Python must actually have them installed.
# pip install qiskit qiskit-aer qiskit-algorithms dwave-samplers dwave-neal quimb pennylane pennylane-lightning
qiskit>=2.3.0
qiskit-aer>=0.15.0
qiskit-algorithms>=0.3.0
dwave-samplers>=1.7.0
dwave-neal>=0.6.0
quimb>=1.13.0
pennylane>=0.42.0
# pennylane-lightning: fast C++ CPU backend. quantum_device_selector.py prefers `lightning.qubit` over the
# ~100x-slower pure-Python `default.qubit` (kept entropy-filter QCNN inference under the 500ms signal budget).
pennylane-lightning>=0.42.0

# Cloud QPU (optional)
qiskit-ibm-runtime>=0.46.0
amazon-braket-sdk>=1.90.0
pennylane-braket>=1.30.0

# DeepAR Regime Forecaster (local)
gluonts[torch]>=0.16.0
