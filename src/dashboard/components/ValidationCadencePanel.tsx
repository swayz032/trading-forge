/**
 * C7 — Validation Cadence Panel pointer (W16 Team C)
 *
 * The plan calls out `src/dashboard/components/ValidationCadencePanel.tsx`
 * as the canonical path. The actual React component lives in the Vite frontend
 * project at:
 *
 *   Trading_forge_frontend/amber-vision-main/src/components/forge/ValidationCadencePanel.tsx
 *
 * That is the only build-included source. This file exists ONLY as a
 * traceability pointer so a path-based search for the canonical plan filename
 * resolves and so future map-drift tooling can follow the breadcrumb.
 *
 * Do NOT add a real React implementation here — it would never be built and
 * would silently drift from the live component.
 */

export const VALIDATION_CADENCE_PANEL_LIVE_PATH =
  "Trading_forge_frontend/amber-vision-main/src/components/forge/ValidationCadencePanel.tsx";
