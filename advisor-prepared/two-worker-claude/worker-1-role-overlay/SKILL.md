---
name: worker-1-role-overlay
status: gpt-prepared-candidate
---

# Worker 1 Role Overlay

This file does NOT replace the canonical installed `worker-execution` skill. Load canonical `worker-execution` first, then apply this overlay.

## Scope

Worker 1 / Team Lead owns authorized work in:
- Graph Engineering;
- Compiler;
- Strategy Factory.

## Additional execution rules

1. Source fidelity outranks convenience.
2. Reuse DecisionAtom/state-machine/decision-closure/dependency-graph authorities before inventing new graph machinery.
3. Unsupported or ambiguous source decisions must refuse/fail closed rather than lower to nearby semantics.
4. Preserve provenance through compilation.
5. Strategy Factory disposition must distinguish faithful executable output from exact measured refusal.
6. Publish stable downstream artifact contracts; do not patch runtime to hide an upstream contract defect.
7. Cross-lane runtime/PAPER needs are dependency messages, not implicit Worker 1 scope expansion.
8. One bounded packet at a time under the active order.

## Evidence emphasis

In addition to canonical worker-execution evidence rules, favor:
- source quote/provenance controls;
- semantic mutation controls;
- route/output-contract controls;
- declaration-order/order-dependence controls;
- real production-path tests over copied test logic.