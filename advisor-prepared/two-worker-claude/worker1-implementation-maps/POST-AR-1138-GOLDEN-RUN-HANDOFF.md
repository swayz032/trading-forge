# Worker 1 Ready-to-Edit Map — Golden Run Handoff

## Gate
AR-1138 GPT PASS + Graph/Compiler/Factory vertical green. This card connects Worker 1 to AR-1183; it does not authorize Worker 2 or broker egress by itself.

## Goal
Package the first real source-faithful strategy so downstream stages can prove the full factory without reinterpreting its semantics.

## Candidate
The first golden candidate is the exact AR-1138 strategy accepted by GPT. Do not swap in an easier strategy after seeing results.

## Worker 1 handoff artifact must contain
- source/video/extraction identity;
- pinned source evidence identifiers/quotes by reference, not paraphrase authority;
- canonical graph/DecisionAtom artifact hash;
- compiler input/output hash;
- factory executable artifact hash OR exact refusal;
- symbol/timeframe/session/direction;
- ordered entry requirements;
- invalidation;
- source-owned exits;
- known unsupported/ambiguous facts = none for a compiled golden candidate;
- code SHA and relevant fixture/test receipt.

## Boundary
Worker 1 stops at a source-faithful Strategy Factory artifact. Context Observer may observe later but may not rewrite it. Qualification may judge later but may not rewrite it. PAPER may execute later but may not reinterpret it.

## Required conservation control
A downstream consumer must be able to compare its received strategy artifact hash/semantic manifest against Worker 1's handoff. Silent change => STOP.

## Forbidden
- no choosing the best backtest result before freezing the candidate;
- no parameter tuning to improve performance;
- no framework overlay changing source entry logic;
- no broker/account identity here;
- no PAPER activation from Worker 1.

## Completion receipt
Golden candidate ID; source hash; graph hash; compiler hash; factory hash; exact tests; commit/push; handoff message using `WORKER-HANDOFF-CONTRACT.md`; STOP for GPT/downstream authorization.