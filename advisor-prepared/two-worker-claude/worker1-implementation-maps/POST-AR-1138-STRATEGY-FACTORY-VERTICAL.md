# Worker 1 Ready-to-Edit Map — Post-AR-1138 Strategy Factory Vertical

## Gate
AR-1138 GPT PASS + one source-faithful compiler vertical green.

## Owner
Worker 1 / COMPILER-FACTORY only.

## Goal
Turn the ONE accepted compiled strategy into the existing Strategy Factory executable artifact, or produce an exact machine-readable refusal. Prove there is no silent semantic delta between compiler output and executable strategy representation.

## First move
1. Use the exact compiled artifact/hash from the compiler vertical.
2. Follow the production consumer reached by current code; reuse existing factory/executable representation.
3. Freeze compiler input hash and factory output/refusal hash.
4. Add ONE RED conservation test before changing code if a real handoff gap exists.

## Required proof
For the accepted strategy, the factory must preserve:
- source strategy identity/provenance;
- symbol/timeframe/session;
- direction authority;
- ordered entry requirements;
- invalidation;
- source-owned exit instructions;
- refusal status/reason if compiler output is not executable.

No qualification score, context overlay, risk optimization, or broker detail may mutate the source entry semantics at this boundary.

## RED controls
A mutation to a required compiled condition/parameter/dependency must either:
- change the executable artifact in an explainable conserved way, or
- trigger exact refusal.
It must not silently disappear.

## Forbidden detours
- no Context Observer implementation;
- no backtest optimization/challenger generation;
- no PAPER/runtime/broker work;
- no auto-repair of refused source semantics;
- no alternate second Strategy Factory architecture if a production consumer already exists.

## GREEN
Focused factory handoff test + compiler regressions needed to prove the same compiled bytes were consumed. If an executable evaluation fixture exists, execute a tiny deterministic fixture only; do not launch qualification.

## Expected touched-file boundary
Factory consumer/adapter + focused test/fixture. No schema/migration unless a measured persistence contract blocker exists; if so STOP for GPT.

## Completion receipt
Starting SHA; compiler artifact hash; exact factory consumer; RED; changed files; GREEN; mutation; executable/refusal hash; commit/push; known limit; STOP GPT.