diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@
   build:
     name: Build Check
     runs-on: ubuntu-latest
-    needs: [test-node, test-python, test-parity]
+    # Build has its own fresh checkout/dependencies and consumes no test artifacts.
+    # A/B proof run 31863281670 executed the exact Build command/gate set both
+    # immediately after the real upstream lint gate and after an unrelated test tail;
+    # both were GREEN. Starting earlier removes queue tail without weakening tests.
+    needs: lint
