{
  "schema": "gpt-claude-hook-guard-v1",
  "worker": "worker-1",
  "_authority": "AR-1265 §4/§5/§9 (live registration + hash-pinned dirty exception) on top of AR-1254 §10 + AR-1236 §11 (activate the existing toolbox, do not rebuild it)",
  "_what_this_is": "The manifest the pinned toolbox's claude-hook-runner.mjs requires. It does NOT restate the lane rules — those live in lane-boundary-guard.mjs::classifyPath inside the pinned toolbox, and a second copy of a boundary rule is a rule that drifts and stops biting while still reporting PASS. edit_scope NARROWS this packet on top of that.",
  "_toolbox_pin": "18108039056a0994c1fc1be9583812b0838dba50",
  "_toolbox_bundle_sha256": "1d12f61277d8d3c502df9bd7dea5dac541e64335e469fd7176187f4b02144b06",
  "_toolbox_pin_history": "dd1bc230 (AR-1254) -> 6a06ffae (AR-1266 §E) -> e0c44ca4 (AR-1268 §G, on AR-1267 §9G) -> 18108039 (AR-1270 §C, on AR-1269 §6C). Each new pin is a DESCENDANT of the previous one, verified with `git merge-base --is-ancestor`. 18108039 carries AR-1270 A/B, the two trust-surface gaps AR-1269 §4/§5 left RED: (A) the frozen G2 queue and the real one-shot receipt namespace are now SELF_PROTECTED, so the guarded worker can no longer manufacture the .raw/.completion file-existence state that the forced-capture gate reads as evidence of capture; (B) a protected-surface FENCE runs BEFORE the mutation-spelling blacklist on the Bash arm, which previously never reached the path classifier at all. bundle_sha256 is COMPUTED by scripts/claude_toolbox.mjs materialize over 42 files (41 + the new AR-1270 control suite), never hand-copied.",
  "_this_pair_is_the_expected_identity": "AR-1267 §4: the live doorway reads _toolbox_pin and _toolbox_bundle_sha256 from THIS file and verifies the TEMP cache against both — stamp pin, stamp bundle, and a re-hash of the cached bytes — before executing the child. Previously it reused the cache on the mere EXISTENCE of a stamp and a runner, so a stale permissive toolbox could keep running after a deliberate re-pin while every artifact in sight agreed with the manifest. The expected identity lives here rather than in the activator because this file is self-protected and the activator, until AR-1268, was not.",

  "session_anchor": {
    "expected_branch": "claude/worker1-h1-20260815",
    "expected_head": "claude/worker1-h1-20260815",
    "require_clean": true,
    "allowed_dirty": [
      {
        "path": "docs/wave25-exit-engine-ab-report.md",
        "diff_sha256": "e200765c11e85aeb9d5f0eb6d04cb04ea3985bfffc0a33e762c9960652547170",
        "authority": "AR-1265 §4"
      }
    ],
    "_expected_head_is_a_ref_on_purpose": "resume-anchor-guard resolves expected_head with `rev-parse <x>^{commit}`, so a REF NAME works and a pinned SHA would go stale on the next commit — a guard that is stale by design gets disabled by whoever it annoys. Anchoring to the branch still catches a detached HEAD, the wrong branch, and a checkout that is not at the branch tip.",
    "_dirty_disposition": "AR-1265 §4 REPLACES the standing block. The desk refused BOTH remedies the worker could have taken alone: do NOT commit the timestamp-only docs/wave25-exit-engine-ab-report.md merely to make the guard green, and do NOT set require_clean:false. Instead this exception pins the exact path AND the sha256 of the exact `git diff HEAD --binary -- <path>` bytes. Any future edit to that file changes the diff and blocks SessionStart, so the historical finding is preserved without letting anything else hide behind the same filename. diff_sha256 was COMPUTED and cross-checked by two instruments (the guard's own dirtyDiffSha256 and `git diff | sha256sum`), never hand-copied.",
    "_still_forbidden": "A blanket allow-dirty, a glob, a directory, an entry with no diff hash, an entry with no authority, and allowed_dirty combined with require_clean:false are all refused by resume-anchor-guard by construction, not by convention. Untracked files remain blocking and cannot be laundered by listing them."
  },

  "edit_scope": {
    "allowed_prefixes": [
      "src/engine/extraction/",
      "src/engine/tests/",
      "scripts/",
      "docs/replay-results/",
      ".claude/"
    ],
    "allowed_exact": [
      "docs/designs/SYSTEM-INVENTORY.md"
    ],
    "_not_listed_is_not_allowed": "Runtime, PAPER, broker/Topstep, compiler execution semantics and the backtester are absent deliberately. AR-1249 §12 and AR-1254 §12 keep them LOCKED, and Worker-2 production authority is not unlocked here.",
    "_claude_prefix_is_not_authority_over_the_guard": "`.claude/` appears above, but AR-1263 §7A self-protection is evaluated FIRST and is never scope-overridable — this manifest, .claude/settings*.json, .claude/hooks/ and the pinned toolbox all DENY regardless of what this list says. That is deliberate: a worker must never gain permission to edit the file that declares its own permissions merely because a broad prefix covers it."
  },

  "g2_precall": {
    "enabled": true,
    "strict_session": false,
    "queue_path": "docs/replay-results/svkm-extraction-certified/grade/opus-v2/isolated_fallback_queue_t1.json",
    "receipt_dir": "docs/replay-results/svkm-extraction-certified/grade/opus-v2/isolated-receipts-t1",
    "native_call_manifest_path": "docs/replay-results/svkm-extraction-certified/grade/opus-v2/native_call_manifest_t1.json",
    "_why_a_native_call_manifest": "AR-1267 §6.2. The queue's task_input_sha256 binds the LOGICAL task; it says nothing about the bytes that reach the model, so a permit for the right condition could accompany a prompt carrying a batch answer, a prior winner or a GPT hint. This artifact was frozen BEFORE any answer exists, derived only from the frozen queue, the frozen benchmark packet and the pinned transcript, and the pre-call guard hash-matches the ACTUAL Agent tool input against it. It is an EXECUTION-layer artifact: it does not select, reorder, add to or remove from the eight-condition queue.",
    "_why_enabled_now": "AR-1265 §5.4. With PreToolUse now registering Agent/Task, a G2-shaped subagent dispatch without a valid durable pre-call permit is refused BEFORE the model call. Post-call refusal cannot un-spend a one-shot attempt.",
    "_why_strict_session_is_false_here": "AR-1265 §3.2 arms strict mode for the DEDICATED eight-call execution session only, where every Agent/Task dispatch requires a permit and ordinary helper work is simply not allowed. This packet is not that session, and the non-G2 calibration must run BEFORE strict mode is armed rather than as an exemption carved inside it. Flipping this to true is a deliberate act for the execution session, not a default."
  },

  "finish": {
    "enabled": false,
    "_why_disabled": "claude-finish-check needs a committed receipt file describing a finished packet. G2-D has not run, so there is no finished packet to check and enabling it would make TaskCompleted assert against a receipt that does not exist. It is switched on when a real finish receipt exists — an enabled check with nothing to check is the false-green shape this desk keeps convicting. AR-1265 §5.7 also holds full `P1 NATIVE ACTIVE` back until TaskCompleted finish enforcement is proven, and AR-1264 §7 reported that claude-finish-check still carries the old structural REVIEW_REQUIRED problem — it will bite the moment finish is armed.",
    "receipt_file": null,
    "base": "eaf205252230732274c20b8174ab942da856b45b"
  }
}
