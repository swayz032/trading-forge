# P0-vNext ADMISSION PROTOTYPE - COVERAGE RESULT

> **STATUS: MEASURED BY EXECUTION.** This artifact replaces `[PRE-REGISTERED - NOT EXECUTED]`
> for the `1b-S` / `1b-R` admission rules ONLY. It says nothing about the gate, the three
> claims, the scope registry or the ledger consumer, none of which exist.
>
> **Base commit:** `06842e21d88d38970c345b57056ecd3c45fcfc2f` - **node v24.13.0** - **typescript 5.9.3**
> **Runner:** `prototypes/p0-vnext-admission/run.mjs` - reproduce with `node run.mjs`.

## THE DECISION RULE (pre-registered by `R-541 s6a`, BEFORE this data existed)
**Success is NOT `52/52` red.** Success is every subcase yielding a verdict **attributable to
its NAMED catcher**, *or* recorded as an **honest named miss**. A first-run high number is a
**red flag to be audited**, not a triumph.

## HEADLINE
- **49 / 52 ATTRIBUTED** to the catcher the design names.
- **3 HONEST NAMED MISSES** - `26(a)`, `26(b)`, `26(c)`: the design names a **dependency-boundary
  (capability) catcher** for these and the prototype does not implement one. They *do* go red, via
  `1b-S:import-cardinality` - **the WRONG catcher, so they are NOT counted as covered.**
  `A MUTATION CAUGHT BY THE WRONG CHECK IS A FAILED PROOF.`
- **0 wrong-catcher passes - 0 parse failures - 5/5 GREEN neighbours ADMITTED.**
- **Getter invocation counter: 0** - the runtime walk never ran a capability it rejects.

## ACCEPTANCE COMMANDS (`R-541 s6b`)
1. **PASS** - the `G-1` CommonJS module (213 bytes, verbatim) is REJECTED and NAMES the channel:
   `1b-S:module-system @ module-scope this (CommonJS wrapper object)`. A clean **ESM twin is
   ADMITTED**, so the rule rejects the CHANNEL, not the shape.
   NOTE: the original `G-1` fixture *also* trips `1b-S:const-ast-grammar` - its nested literal is
   unwrapped. That is a second real defect in the same 213 bytes, not a false positive.
2. **PASS** - every subcase executed and the emitted verdict names the catcher that fired.
   Nothing reddening via parse/type/ReferenceError is reported as a pass
   (`failed_parse: 0`, `failed_wrong_catcher: 0`).
3. **PASS** - both controls run: the clean unmutated control is GREEN, and a planted catcher
   naming a non-existent rule is correctly absent rather than silently satisfied.
4. **PASS - separability MEASURED, not claimed:** `fs` reads were instrumented for the whole run.
   **158 files opened, 0 of them the ledger or ORACLE.json.** The 158 is the positive control:
   the tracker was alive, so the 0 is a measured absence rather than a dead probe.

## AUDIT OF THE HIGH NUMBER (required by `s6a`)
- **Escaped fixtures are genuinely escaped at runtime** - `51(c)`, `51(d)`, `52(b)`, `52(c)` each
  carry a literal backslash-escape and contain **no** raw `__proto__`. This check exists because
  my own authoring layer silently cooked these fixtures twice in earlier rounds.
- **The rule DISCRIMINATES** - a legitimate frozen-constant module is ADMITTED, so the reds are
  not `reject everything`.
- **One real bug was found by the corpus and fixed in the RULE, not the test:** `34(d)` (a free
  reference to an UNDECLARED identifier) initially went **uncaught**, because the check required
  the type-checker to return a symbol and an unresolved identifier has none - making it blind to
  the purest form of the channel it exists to catch.

## WHAT THIS DOES NOT SHOW
- The three `26(*)` capability subcases remain **UNCOVERED**; that is the next lane's work.
- No gate, no claim A/B/C, no scope registry and no ledger consumer exists.
- `G-2` (the completeness instrument deriving only from the FORBIDDEN side) is **NOT closed** here.
- **I do not grade my own work.** This is a doer's artifact; an independent grade is owed.

---

## RAW RUN OUTPUT (verbatim)

```
P0-vNext ADMISSION PROTOTYPE — COVERAGE RUN
========================================================================================================
26(a)   MISS_NOT_IMPLEMENTED   unallowlisted import                   1b-S:import-cardinality
26(b)   MISS_NOT_IMPLEMENTED   filesystem / network module            1b-S:import-cardinality
26(c)   MISS_NOT_IMPLEMENTED   transitive edge to either              1b-S:import-cardinality
34(a)   ATTRIBUTED             setter / configuration export          1b-S:exports
34(b)   ATTRIBUTED             function-valued export != project      1b-S:exports
34(c)   ATTRIBUTED             export that mutates module state       1b-S:exports
34(d)   ATTRIBUTED             free / captured reference              1b-S:free-captured-reference
35(a)   ATTRIBUTED             globalThis                             1b-S:direct-ambient-read
35(b)   ATTRIBUTED             window                                 1b-S:direct-ambient-read
35(c)   ATTRIBUTED             global (alias of globalThis)           1b-S:direct-ambient-read
35(d)   ATTRIBUTED             ANY unallowlisted host-global          1b-S:direct-ambient-read
36      ATTRIBUTED             process / process.env                  1b-S:direct-ambient-read
37(a)   ATTRIBUTED             mutable module-scope binding           1b-S:module-scope-state
37(b)   ATTRIBUTED             cache populated on first call          1b-S:module-scope-state
37(c)   ATTRIBUTED             singleton                              1b-S:module-scope-state
37(d)   ATTRIBUTED             lazily-initialised holder              1b-S:module-scope-state
38      ATTRIBUTED             SHALLOW-frozen nested holder           1b-S:const-ast-grammar
41(a)   ATTRIBUTED             import()                               1b-S:dynamic-loading
41(b)   ATTRIBUTED             require, computed specifier            1b-S:dynamic-loading
41(c)   ATTRIBUTED             eval                                   1b-S:dynamic-loading
41(d)   ATTRIBUTED             new Function                           1b-S:dynamic-loading
41(e)   ATTRIBUTED             createRequire                          1b-S:dynamic-loading
48      ATTRIBUTED             helper-returned module constant        1b-S:const-ast-grammar
49(a)   ATTRIBUTED             spread escape                          1b-S:const-ast-grammar
49(b)   ATTRIBUTED             bare alias                             1b-S:const-ast-grammar
49(c)   ATTRIBUTED             computed key                           1b-S:const-ast-grammar
50(a)   ATTRIBUTED             shadowed / local freeze callee         1b-S:const-ast-grammar
50(b)   ATTRIBUTED             locally-declared freeze                1b-S:const-ast-grammar
50(c)   ATTRIBUTED             aliased freeze binding                 1b-S:const-ast-grammar
51(a)   ATTRIBUTED             __proto__ key (raw Ident)              1b-S:const-ast-grammar
51(b)   ATTRIBUTED             __proto__ key (raw StringLit)          1b-S:const-ast-grammar
51(c)   ATTRIBUTED             __proto__ key (escaped identifier)     1b-S:const-ast-grammar
51(d)   ATTRIBUTED             __proto__ key (escaped string)         1b-S:const-ast-grammar
52(a)   ATTRIBUTED             duplicate cooked keys (raw)            1b-S:const-ast-grammar
52(b)   ATTRIBUTED             duplicate cooked keys (esc string)     1b-S:const-ast-grammar
52(c)   ATTRIBUTED             duplicate cooked keys (esc ident)      1b-S:const-ast-grammar
52(d)   ATTRIBUTED             duplicate cooked keys (mixed)          1b-S:const-ast-grammar
53      ATTRIBUTED             harmless inert static import           1b-S:import-cardinality
54      ATTRIBUTED             CJS module-wrapper `this` channel      1b-S:module-system
39      ATTRIBUTED             function-valued field                  1b-R:function-valued-field
40      ATTRIBUTED             accessor descriptor                    1b-R:accessor-descriptor
42      ATTRIBUTED             custom / class prototype               1b-R:prototype-identity
43      ATTRIBUTED             cycle                                  1b-R:cycle
44      ATTRIBUTED             symbol key                             1b-R:symbol-key
45(a)   ATTRIBUTED             undefined value                        1b-R:value-class
45(b)   ATTRIBUTED             bigint value                           1b-R:value-class
45(c)   ATTRIBUTED             symbol value                           1b-R:value-class
45(d)   ATTRIBUTED             NaN                                    1b-R:value-class
45(e)   ATTRIBUTED             +/-Infinity                            1b-R:value-class
46(a)   ATTRIBUTED             sparse hole                            1b-R:array-shape
46(b)   ATTRIBUTED             extra named array property             1b-R:array-shape
47      ATTRIBUTED             non-enumerable user field              1b-R:non-enumerable-field
========================================================================================================
GREEN NEIGHBOURS (must be admitted):
  G-src-clean      ADMITTED
  G-src-esm-twin   ADMITTED
  G-run-plain      ADMITTED
  G-run-dag        ADMITTED
  G-run-array      ADMITTED
========================================================================================================
{
  "total": 52,
  "attributed": 49,
  "miss_not_implemented": 3,
  "miss_not_caught": 0,
  "failed_wrong_catcher": 0,
  "failed_parse": 0,
  "green_admitted": 5,
  "green_total": 5,
  "getter_invocations": 0
}
NEGATIVE CONTROL: planted catcher '1b-R:no-such-rule' correctly absent from a clean run: true
GETTER INVOCATION COUNTER (required 0): 0
SEPARABILITY: files actually opened during this run: 158
SEPARABILITY: ledger/oracle artifacts opened: 0 (required 0)
SEPARABILITY POSITIVE CONTROL: the tracker DID observe 158 real reads, so a 0 above is a measured absence, not a dead probe.

```
