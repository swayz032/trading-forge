// STUB ONLY. Row 26(c) imports this for side effect, to exercise the transitive-edge
// subcase of the dependency-boundary channel. It has no transitive edge of its own.
export const marker = 'helper';
