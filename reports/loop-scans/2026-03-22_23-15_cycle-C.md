# Cycle C Scan Report — 2026-03-22 23:15 ET (FINAL)

## Baseline Gates
| Gate | Result |
|------|--------|
| pytest | **PASS** — 1008/0/52w (13.66s, post all fixes) |
| tsc | **PASS** |
| eslint | **PASS** |

---

## Agent 1: Strategy Files (19 strategies)

### Confirmed & Fixed (6 bugs)

| # | File | Bug | Fix |
|---|------|-----|-----|
| 1 | `strategies/propulsion.py:134-173` | Same-bar entry+exit — no `exited_this_bar` guards | Added `exited_this_bar_long`/`exited_this_bar_short` flags |
| 2 | `strategies/ote_strategy.py:131` | Missing mutual exclusion — short entry doesn't check `long_entry_bar` | Added `and long_entry_bar < 0` guard |
| 3 | `strategies/silver_bullet.py:161` | Missing mutual exclusion — short entry doesn't check `long_entry_bar` | Added `and long_entry_bar < 0` guard |
| 4 | `strategies/turtle_soup.py:153` | Missing mutual exclusion — short entry doesn't check `long_entry_bar` | Added `and long_entry_bar < 0` guard |
| 5 | `strategies/breaker.py:45-89` (Numba) | Same-bar entry+exit — exit sets `in_long=False`, entry fires on same bar | Added `exited_this_bar` flags to `_apply_exits` |
| 6 | `strategies/mitigation.py:45-81` (Numba) | Same-bar entry+exit — identical pattern to breaker.py | Added `exited_this_bar` flags to `_apply_exits` |

### Dismissed — False Positives (5)

| # | File | Reported Bug | Why Dismissed |
|---|------|-------------|---------------|
| 1 | `strategies/ict_2022.py` | Missing mutual exclusion | `pd_list[i] == "discount"` vs `"premium"` provides natural exclusion |
| 2 | `strategies/quarterly_swing.py` | Missing mutual exclusion | `close < to_price` vs `close > to_price` mutually exclusive |
| 3 | `strategies/ict_swing.py` | Missing mutual exclusion | Discount vs premium zones mutually exclusive |
| 4 | `strategies/midnight_open.py` | Missing mutual exclusion | `close < mo` vs `close > mo` mutually exclusive |
| 5 | `strategies/judas_swing.py` | Missing mutual exclusion | `opening_move_dir` is single string — natural exclusion |

---

## Agent 2: Indicator Files (9 files)

### Confirmed & Fixed (2 bugs)

| # | Sev | File | Bug | Fix |
|---|-----|------|-----|-----|
| 1 | P2 | `indicators/liquidity.py:202` | `detect_sweep` skips last bar — `range(1, len(df) - 1)` | Changed to `range(1, len(df))` — no forward-looking logic, last bar is safe |
| 2 | P2 | `indicators/liquidity.py:269-280` | `detect_raid` places signal at bar `i` using bar `i+1` data (1-bar lookahead) | Moved signal to `result[next_i]` instead of `result[i]` |

### Confirmed — NOT Fixed (latent, no strategy uses these)

| # | Sev | File | Bug | Why Deferred |
|---|-----|------|-----|-------------|
| 1 | P1 | `indicators/price_delivery.py:80` | `detect_ifvg` scans ALL future bars for fill — lookahead bias | No strategy imports `detect_ifvg`. Latent only. Fix when first strategy uses it. |
| 2 | P3 | `indicators/sessions.py:186-189` | `quarterly_theory` 4h mode maps fixed clock hours, not session quarters | Only affects 4h timeframe; conceptual issue |
| 3 | P3 | `indicators/core.py:86,93` | ADX `safe_atr`/`safe_di_sum` uses Python list loop instead of vectorized Polars | Performance only, not a logic bug |

### Clean Files (no bugs)
`fibonacci.py`, `smt.py`, `market_structure.py`, `order_flow.py`

---

## Agent 3: Cross-File Validation

### CHECK 1: 3-Way Firm Config Sync — **PASS**
All 8 firms match across `firm_config.py`, `prop_compliance.py`, `firm-config.ts` on all fields (account_size, fees, drawdown, splits, commissions, contract caps, consistency rules, daily loss limits).

### CHECK 2: R:R Enforcement — **PASS**
- `backtester.py _compute_tier`: `winner_loser_ratio >= 2.0` — correct
- `performance_gate.py:73`: `avg_winner_to_loser_ratio >= 2.0` — correct
- Both match CLAUDE.md spec

### CHECK 3: Contract Caps — **PASS**
`CONTRACT_CAP_MIN=10`, `CONTRACT_CAP_MAX=20` in both Python and TypeScript. All 8 firms `maxContracts=15`.

### CHECK 4: Prop Firm Rules Freshness — **FAIL** (6 stale values in docs)
| # | Location | Issue |
|---|----------|-------|
| 1 | `docs/prop-firm-rules.md:165` | TPT payout formula includes `+ 130` activation fee (now $0) |
| 2 | `docs/prop-firm-rules.md:330` | Apex `+ 85` labeled "Activation" (actually ongoing fee) |
| 3 | `docs/prop-firm-rules.md:411` | FFN `+ 120` activation fee (now $0) |
| 4 | `docs/prop-firm-rules.md:980-984` | Stale: Topstep $149 activation, TPT $150/mo (should be $170), Apex $85 activation |
| 5 | `docs/prop-firm-rules.md:914,919` | Firm keys `alpha_standard_50k`/`ffn_standard_50k` don't match canonical `alpha_50k`/`ffn_50k` |
| 6 | `docs/prop-firm-rules.md:706` | Drawdown check `>=` in docs vs `>` in code |

### CHECK 5: E2E Pipeline Connectivity — **PASS**
All 6 critical routes wired: `/api/backtests`, `/api/monte-carlo`, `/api/paper`, `/api/journal`, `/api/graveyard`, `/api/compliance`.

---

## Agent 4: n8n Workflow Health

### Instance: 53 workflows (25 active, 28 inactive)

### 6 Known Issues — ALL STILL BROKEN

| # | Workflow | Issue | Severity |
|---|---------|-------|----------|
| 1 | **0A-health-monitor** | References removed node. 48+ failures in last 50 executions. ~288 failures/day. | CRITICAL |
| 2 | **8B-source-quality-review** | Unterminated string literal `lines.join('\n` — SyntaxError every hourly run | CRITICAL |
| 3 | **5H-reddit-scout** | HTTP 400 — API expects object, receives bare array | CRITICAL |
| 4 | **Weekly Strategy Hunt** | "Format Scout Context for LLM" returns primitive value | WARNING |
| 5 | **Nightly Strategy Research Loop** | Same primitive return error | WARNING |
| 6 | **Strategy Generation Loop** | Cycle (infinite loop) + missing toolDescription on 2 nodes | WARNING |

### Validation: 3 workflows with errors, 22 pass. No NEW issues found.

---

## Agent 5: Utilization Audit

### Critical Finding: `context/` Engine is UNWIRED
8 Python modules (bias_engine, eligibility_gate, htf_context, playbook_router, session_context, location_score, structural_stops, structural_targets) are fully built but have **zero server integration**. No route, no service import, no n8n workflow calls it. This is the biggest utilization gap.

### Ollama Models: One Generation Behind
- `trading-quant` (Qwen3-Coder:30b) → upgrade candidate: **Qwen3-Coder-Next** (MoE)
- `fast` (Llama 3.1:8b) → upgrade candidate: **DeepSeek-R1 32B**

### Subsystem Wiring: 10/12 wired
- **Wired:** governor, survival, decay, graveyard, skip_engine, anti_setups, archetypes, compiler, paper trading, journal critic
- **Unwired:** `context/` (no route), `validation/` (no dedicated route)

### Paper Trading Path: FULLY WIRED
Signal → risk gate → execution → DB → post-trade hooks (drift, decay, consistency). Complete.

### Top 3 Priority Actions
1. Wire `context/` engine to `/api/context` route + paper-signal-service
2. Upgrade Ollama models (Qwen3-Coder-Next + DeepSeek-R1 32B)
3. Expose `validation/` engine via `/api/validation` route

---

## Cumulative Session Stats (Cycles A + B + C — FINAL)

| Metric | Count |
|--------|-------|
| Source code bugs fixed | **19** (11 Cycle A/B + 6 strategies + 2 indicators) |
| False positives dismissed | **5** |
| Latent bugs noted (deferred) | **3** (IFVG lookahead, quarterly_theory 4h, ADX perf) |
| Doc mismatches found | **6** (prop-firm-rules.md stale values) |
| n8n workflows broken | **6** (all pre-existing, no new issues) |
| Unwired subsystems | **2** (context/, validation/) |
| Test regressions | **0** |
| Files modified this cycle | 8 (6 strategies + 1 indicator file) |

## Pattern: Same-Bar Entry+Exit

Dominant bug class in Cycle C. vectorbt silently drops trades when `entry_X[i]` and `exit_X[i]` are both True on the same bar.

**Now protected:** propulsion, ote_strategy, silver_bullet, turtle_soup, breaker, mitigation
**Natural exclusion (no fix needed):** ict_2022, quarterly_swing, ict_swing, midnight_open, judas_swing
