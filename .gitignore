node_modules/
dist/
/data/
!src/data/
.env
*.log
.DS_Store
__pycache__/
*.pyc
.venv/
.pytest_cache/
.worktrees/
*.parquet
# .claude/ ephemeral subdirs (worktrees, session state, projects).
# .claude/agents/ IS tracked (specialist agent definitions document the workflow).
.claude/worktrees/
.claude/projects/
.claude/skills/
.claude/local/
.claude/cache/
.claude/sessions/
.claude/state/
.claude/agent-memory/
.claude/commands/
Trading_forge_frontend/amber-vision-main/.claude/

# Operator diagnostic scratch files at repo root (Wave 26 Pass G master close)
/tmp-*.ts

# TypeScript build artifacts (should not be committed — produced by tsc)
scripts/*.d.ts
scripts/*.d.ts.map
scripts/*.js
scripts/*.js.map

# Carter agent audit output (live API snapshot — do not commit)
scripts/carter/agent-baseline.json
Trading_forge_frontend/amber-vision-main/tsconfig.*.tsbuildinfo
.clawhub/
data_cache/
.numba_cache/
backups/
logs/
models/

# Ephemeral cache / scratch / brainstorm artifacts — never commit (Phase 0 hardening 2026-06-22)
%TEMP%/
tmp/
.superpowers/
CTemp*
npm-cache-tf/
*.pid
*.bak

# Runtime session/credential persistence files (survive NSSM restart) — never commit (deepscan 2026-06-28)
.bw-session-runtime
.cookie-runtime-*

# Operational dumps / scratch that leak the attack surface — never commit (deep-scan #6 S2, 2026-07-01)
# n8n workflow-graph dumps expose node bodies, relay endpoints, and the HMAC scheme. Export
# n8n via the tracked workflows/n8n/*.json backups (ID-stable), never ad-hoc root dumps.
*all-wf.json
wf_full/
wf_live.json
wf_live_*.json
brave_disc.json
active_ids.txt
all_ids.txt
Userstonio*
scripts/_tmp_*
workflows/n8n-prefix/

# SWEEP-F10 (R-646): test-run outputs that must never dirty a tracked path.
# The committed docs/A12-AUDIT-REPORT.md is refreshed deliberately via
# TF_A12_REPORT_PATH; the .local.md sibling is what an ordinary pytest run writes.
docs/A12-AUDIT-REPORT.local.md
docs/scaling-validation/cli-report-existence-run.md
docs/scaling-validation/cli-smoke-risky-run.md
